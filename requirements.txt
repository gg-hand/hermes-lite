anthropic>=0.40.0
openai>=1.50.0
chromadb>=0.5.0
fastapi>=0.115.0
uvicorn>=0.32.0
tiktoken>=0.8.0
pydantic>=2.9.0
pyyaml>=6.0
python-dotenv>=1.0.0
httpx>=0.27.0
numpy>=1.26.0
python-multipart>=0.0.9
jsonschema>=4.20
# multiagent 协作模块依赖
watchdog>=4.0
aiofiles>=24.0
portalocker>=2.7
cryptography>=42.0
# OCR 分层依赖（PaddleOCR 2.x 主引擎 + Tesseract 兜底 + 预处理）
pytesseract>=0.3.10   # Tesseract OCR 引擎（L2 兜底，需另装 tesseract）
Pillow>=9.0.0         # 图片处理
paddleocr>=2.7.0,<3.0 # PaddleOCR 2.x 主引擎（3.x 改云端 API + paddlex，与 paddlepaddle 3.3 不兼容）
paddlepaddle>=2.6.0   # PaddleOCR 推理后端
opencv-python>=4.9.0  # Tesseract 预处理（灰度+Otsu+去噪）
# 可选：其他文件解析库
# PyPDF2>=3.0.0       # PDF 解析（可选）
# python-docx>=1.1.0  # DOCX 解析（可选）
