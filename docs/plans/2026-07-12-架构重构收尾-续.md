# 架构重构收尾（续）实现计划 — 全量目标版

> **For agentic workers:** 每个任务按 TDD 流程执行（red → green → refactor → commit）。
> Steps 使用 checkbox (`- [ ]`) 语法跟踪。

**Goal:** 达成原始架构目标 — server.py <100 行、orchestrator.py <350 行、react_loop.py <500 行、builtin_tools.py 删除。不妥协、不延后。

**Architecture:** 五阶段推进：① 基础设施提取（server.py 瘦身） → ② 移除 Orchestrator 22 个委托 wrapper（更新测试调用方） → ③ 提取 Orchestrator 核心方法到 ChatHandler / EnhancedContextBuilder / 工厂函数 → ④ 提取 ReactLoop 的 run_stream / run 到 StreamRunner / SyncRunner → ⑤ 全量验证。每阶段独立可测、可提交。

**Tech Stack:** Python 3.11+, FastAPI, Pydantic, pytest, asyncio

## Global Constraints

- API 契约兼容：65 个端点的请求/响应格式不变
- config.yaml 格式兼容：${ENV_VAR} 占位符机制不变
- 测试框架：pytest，测试通过 `sys.path.insert(0, "src")` 导入
- **测试基线：127 failed, 2591 passed, 3 skipped — 每个 task 完成后验证 0 新增 failure**
- 每个子步骤完成后提交一次，提交信息格式：`refactor: <阶段描述>`
- PowerShell 环境：用 `;` 分隔命令，不用 `&&`；用 `git commit -F` 传多行提交信息
- Edit 工具限制：同一文件多个 Edit 必须分批顺序执行（同一消息中同文件只有最后一个生效）
- TDD 流程：先写失败测试 → 验证失败 → 最小实现 → 验证通过 → 提交
- 路径基准：所有相对路径相对于 `e:\Java\webser\web_app\webme\hermes-lite`

---

## File Structure

### 删除文件
```
src/agent/builtin_tools.py        # 22 行 re-export shim
```

### 新建文件
```
src/logging_setup.py              # JSONLogFormatter + _setup_logging（~100 行）
src/config_helpers.py             # 6 个配置工具函数（~220 行）
src/background_loops.py           # cleanup_loop / file_cleanup_loop / metrics_persist_loop（~230 行）
src/lifespan.py                   # lifespan 函数 + 全局组件变量（~630 行）
src/orchestrator/chat_handler.py  # ChatHandler 类（chat + chat_stream，~900 行）
src/orchestrator/enhanced_context.py  # EnhancedContextBuilder 类（~200 行）
src/orchestrator/factories.py     # 组件工厂函数（~400 行）
src/orchestrator/__init__.py      # 重新导出 Orchestrator
src/agent/stream_runner.py        # StreamRunner 类（run_stream，~820 行）
src/agent/sync_runner.py          # SyncRunner 类（run，~470 行）
```

### 修改文件
```
src/server.py                     # 1594 → <50 行（仅 uvicorn run 入口）
src/app.py                        # lifespan wrapper 指向 lifespan.py
src/orchestrator.py               # 1928 → <350 行（仅 __init__ 骨架 + close/shutdown）
src/agent/react_loop.py           # 1826 → <500 行（仅 __init__ + 委托 facade）
src/agent/tools/__init__.py       # 移除对 builtin_tools 的兼容注释
tests/test_plan_tools.py          # 改导入源 src.agent import builtin_tools → src.agent.tools
tests/test_orchestrator_title.py          # 调用方迁移到 session_mgr.*
tests/test_environment_injection.py       # 调用方迁移到 context_builder.* / enhanced_context.*
tests/test_todo_injection.py              # 调用方迁移到 context_builder.*
tests/test_react_loop_continuation.py     # 调用方迁移到 msg_persistence.* / context_builder.*
tests/test_cron_tool.py                   # 调用方迁移到 cron_isolator.* / enhanced_context.*
tests/test_orchestrator_cron_context.py   # 调用方迁移到 cron_isolator.* / enhanced_context.*
tests/test_cron_isolation.py              # 调用方迁移到 enhanced_context.*
tests/test_file_injection_integration.py  # 调用方迁移到 enhanced_context.*
```

---

## 当前状态基线

| 文件 | 当前行数 | 目标行数 | 缺口 |
|------|---------|---------|------|
| server.py | 1594 | <100 | -1494 |
| orchestrator.py | 1928 | <350 | -1578 |
| react_loop.py | 1826 | <500 | -1326 |
| builtin_tools.py | 22 | 0 | -22 |
| **总计** | **5370** | **<950** | **-4420** |

测试基线：**127 failed, 2591 passed, 3 skipped**（均为 pre-existing failure，0 新增）

---

# Phase 1：基础设施提取（server.py 瘦身）

目标：server.py 从 1594 行降到 <100 行，仅保留 uvicorn 入口。

## Task 1: 删除 builtin_tools.py shim

**Files:**
- Modify: `src/orchestrator.py:85`（确认 import 已指向 `.agent.tools`）
- Modify: `tests/test_plan_tools.py:29`（改导入源）
- Modify: `src/agent/tools/__init__.py`（移除兼容注释）
- Delete: `src/agent/builtin_tools.py`

**Interfaces:**
- Produces: `src/agent/builtin_tools.py` 不存在；所有引用经 `src.agent.tools` 入口

- [ ] **Step 1: 确认生产代码无 builtin_tools 直接引用**

Run:
```powershell
python -c "import os; hits = []; [hits.append((os.path.join(r,f),i+1,l.rstrip())) for r,_,fs in os.walk('src') for f in fs if f.endswith('.py') for i,l in enumerate(open(os.path.join(r,f),encoding='utf-8')) if 'builtin_tools' in l and not os.path.join(r,f).endswith('builtin_tools.py')]; [print(f'{p}:{n}: {t}') for p,n,t in hits]"
```
Expected: 仅 `src/agent/tools/__init__.py` 的注释行（无生产代码 import）

- [ ] **Step 2: 更新 tests/test_plan_tools.py 导入**

Read `tests/test_plan_tools.py:29`:
```python
from src.agent import builtin_tools  # noqa: E402
```

Edit 替换为:
```python
from src.agent import tools as builtin_tools  # noqa: E402
```

- [ ] **Step 3: 更新 src/agent/tools/__init__.py 移除兼容注释**

Read `src/agent/tools/__init__.py:13`，移除提到 `builtin_tools` 的注释行。

- [ ] **Step 4: 删除 src/agent/builtin_tools.py**

使用 DeleteFile 工具删除 `e:\Java\webser\web_app\webme\hermes-lite\src\agent\builtin_tools.py`

- [ ] **Step 5: 运行测试验证**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_plan_tools.py -x --no-header -q
```
Expected: 与基线一致的 pass/fail 数（无新增 import error）

- [ ] **Step 6: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: delete builtin_tools.py shim (Task 1)"
```

---

## Task 2: 提取 logging_setup.py

**Files:**
- Create: `src/logging_setup.py`
- Modify: `src/server.py:217-321`（删除 JSONLogFormatter + _setup_logging + `logger = _setup_logging(SERVER_LOG_PATH)` 行，改为 import）

**Interfaces:**
- Consumes: `SERVER_LOG_PATH` 常量（保留在 server.py）
- Produces: `from logging_setup import JSONLogFormatter, _setup_logging, logger`（logger 为已初始化的 `hermes.server` logger）

- [ ] **Step 1: 写失败测试**

Create `tests/test_logging_setup.py`:
```python
"""Task 2: 验证 logging_setup 模块可独立导入并配置日志。"""
import logging
import sys

sys.path.insert(0, "src")


def test_json_log_formatter_outputs_valid_json():
    """JSONLogFormatter 输出合法 JSON 且包含必要字段。"""
    import json
    from logging_setup import JSONLogFormatter

    formatter = JSONLogFormatter()
    record = logging.LogRecord(
        name="hermes.server", level=logging.INFO, pathname=__file__,
        lineno=1, msg="test message", args=(), exc_info=None,
    )
    output = formatter.format(record)
    parsed = json.loads(output)
    assert parsed["level"] == "INFO"
    assert parsed["message"] == "test message"
    assert parsed["logger"] == "hermes.server"
    assert "timestamp" in parsed


def test_setup_logging_returns_hermes_logger():
    """_setup_logging 返回名为 hermes.server 的 logger。"""
    from logging_setup import _setup_logging
    logger = _setup_logging("data/test_server.log")
    assert logger.name == "hermes.server"


def test_logger_module_attribute_exists():
    """logging_setup 模块暴露 logger 属性。"""
    from logging_setup import logger
    assert isinstance(logger, logging.Logger)
    assert logger.name == "hermes.server"
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_logging_setup.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'logging_setup'`

- [ ] **Step 3: 创建 src/logging_setup.py**

Read `src/server.py:217-321`，将其中的 `JSONLogFormatter` 类与 `_setup_logging` 函数复制到新文件 `src/logging_setup.py`，并添加模块级 `logger` 初始化。

Create `src/logging_setup.py`:
```python
"""JSON 结构化日志配置。

从 server.py 提取，供 server.py / app.py 共享日志初始化逻辑。
"""
from __future__ import annotations

import json
import logging
import os
import sys
from datetime import datetime


class JSONLogFormatter(logging.Formatter):
    """JSON 结构化日志格式化器。

    每行输出 JSON，可直接被 Logstash / Grafana Loki / Datadog 消费。
    """

    def format(self, record: logging.LogRecord) -> str:
        log_entry = {
            "timestamp": datetime.utcfromtimestamp(record.created).isoformat() + "Z",
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
        }
        if record.exc_info and record.exc_info[0]:
            log_entry["exception"] = {
                "type": record.exc_info[0].__name__,
                "traceback": self.formatException(record.exc_info),
            }
        for attr in ("method", "path", "status_code", "duration_ms", "session_id"):
            if hasattr(record, attr):
                log_entry[attr] = getattr(record, attr)
        return json.dumps(log_entry, ensure_ascii=False)


def _setup_logging(log_file: str) -> logging.Logger:
    """配置根日志：同时输出到 stdout 与文件。

    若 root logger 已有 handler 则跳过配置；测试环境跳过文件 handler。
    """
    root_logger = logging.getLogger()
    root_logger.setLevel(logging.INFO)

    if root_logger.handlers:
        return logging.getLogger("hermes.server")

    log_dir = os.path.dirname(log_file)
    if log_dir:
        os.makedirs(log_dir, exist_ok=True)

    formatter = JSONLogFormatter()
    stream_handler = logging.StreamHandler(sys.stdout)
    stream_handler.setFormatter(formatter)
    root_logger.addHandler(stream_handler)

    _in_test_env = (
        "pytest" in sys.modules
        or "unittest" in sys.modules
        or "PYTEST_CURRENT_TEST" in os.environ
    )
    if _in_test_env:
        return logging.getLogger("hermes.server")

    from logging.handlers import RotatingFileHandler

    actual_log = log_file
    try:
        file_handler = RotatingFileHandler(
            log_file, maxBytes=10 * 1024 * 1024, backupCount=3, encoding="utf-8"
        )
    except PermissionError:
        ts = datetime.now().strftime("%Y%m%d_%H%M%S")
        dirname = os.path.dirname(log_file)
        basename = os.path.basename(log_file)
        name, ext = os.path.splitext(basename)
        actual_log = os.path.join(dirname, f"{name}_{ts}{ext}") if dirname else f"{name}_{ts}{ext}"
        file_handler = RotatingFileHandler(
            actual_log, maxBytes=10 * 1024 * 1024, backupCount=3, encoding="utf-8"
        )
        root_logger.warning("日志文件 %s 被锁定，已切到 %s", log_file, actual_log)
    file_handler.setFormatter(formatter)

    class HermesLogFilter(logging.Filter):
        def filter(self, record: logging.LogRecord) -> bool:
            name = record.name
            return name.startswith("hermes.") or name.startswith("src.") or name.startswith("mcp.")

    file_handler.addFilter(HermesLogFilter())
    root_logger.addHandler(file_handler)

    return logging.getLogger("hermes.server")


# 模块级初始化：复用 server.py 的 SERVER_LOG_PATH 环境变量
SERVER_LOG_PATH = os.environ.get("HERMES_SERVER_LOG", "data/server.log")
logger = _setup_logging(SERVER_LOG_PATH)
```

- [ ] **Step 4: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_logging_setup.py -x --no-header -q
```
Expected: PASS（3 个测试）

- [ ] **Step 5: 更新 server.py 引用**

Edit `src/server.py`：
1. 删除 L217-321（`class JSONLogFormatter` 到 `logger = _setup_logging(SERVER_LOG_PATH)`）
2. 在 import 区添加 `from logging_setup import JSONLogFormatter, _setup_logging, logger`

- [ ] **Step 6: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 7: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract logging_setup.py from server.py (Task 2)"
```

---

## Task 3: 提取 config_helpers.py

**Files:**
- Create: `src/config_helpers.py`
- Modify: `src/server.py:1387-1606`（删除 6 个配置工具函数 + `_RESTART_REQUIRED_KEYS` / `_RUNTIME_HOTUPDATE_MAP` 常量，改为 import）

**Interfaces:**
- Consumes: `CronExpr`（已在 server.py import）
- Produces: `from config_helpers import _deep_merge_config, _backup_config, _atomic_write_config, _validate_config_schema, _check_needs_restart, _apply_runtime_config, _RESTART_REQUIRED_KEYS, _RUNTIME_HOTUPDATE_MAP`

- [ ] **Step 1: 写失败测试**

Create `tests/test_config_helpers_module.py`:
```python
"""Task 3: 验证 config_helpers 模块可独立导入且函数行为正确。"""
import sys

sys.path.insert(0, "src")


def test_deep_merge_config_recursive():
    """dict + dict 递归合并。"""
    from config_helpers import _deep_merge_config
    old = {"a": {"b": 1, "c": 2}, "d": 3}
    new = {"a": {"c": 20, "e": 30}}
    merged = _deep_merge_config(old, new)
    assert merged == {"a": {"b": 1, "c": 20, "e": 30}, "d": 3}
    # 入参不被修改
    assert old == {"a": {"b": 1, "c": 2}, "d": 3}
    assert new == {"a": {"c": 20, "e": 30}}


def test_validate_config_schema_rejects_non_dict_root():
    """非 dict 根节点拒绝。"""
    import pytest
    from config_helpers import _validate_config_schema
    with pytest.raises(ValueError, match="配置校验失败"):
        _validate_config_schema([1, 2, 3])


def test_check_needs_restart_detects_llm_change():
    """llm.main_model 变更需重启。"""
    from config_helpers import _check_needs_restart
    old = {"llm": {"main_model": "a", "consolidation_model": "b"}}
    new = {"llm": {"main_model": "c", "consolidation_model": "b"}}
    assert _check_needs_restart(old, new) is True


def test_constants_exist():
    """_RESTART_REQUIRED_KEYS 与 _RUNTIME_HOTUPDATE_MAP 常量存在。"""
    from config_helpers import _RESTART_REQUIRED_KEYS, _RUNTIME_HOTUPDATE_MAP
    assert isinstance(_RESTART_REQUIRED_KEYS, (list, tuple, set))
    assert isinstance(_RUNTIME_HOTUPDATE_MAP, dict)
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_config_helpers_module.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'config_helpers'`

- [ ] **Step 3: 创建 src/config_helpers.py**

Read `src/server.py:1300-1606`（包含 `_RESTART_REQUIRED_KEYS` / `_RUNTIME_HOTUPDATE_MAP` / `_MISSING` 常量与 6 个函数），复制到新文件。

Create `src/config_helpers.py`（骨架）:
```python
"""配置文件工具函数。

从 server.py 提取，包含配置合并、备份、原子写入、校验、重启检测与运行时应用。
"""
from __future__ import annotations

import copy
import logging
import os
import shutil

import yaml

logger = logging.getLogger("hermes.server")

_MISSING = object()

# 重启所需的关键配置段（变更需重启服务才能生效）
_RESTART_REQUIRED_KEYS = [
    # ... 从 server.py L1340-1384 复制完整列表
]

# 运行时热更新映射表（无需重启即可生效）
_RUNTIME_HOTUPDATE_MAP = {
    # ... 从 server.py L1350-1384 复制完整映射
}


def _deep_merge_config(old: dict, new: dict) -> dict:
    # ... 从 server.py L1387-1416 复制完整实现
    ...


def _backup_config(config_path: str) -> None:
    # ... 从 server.py L1419-1435 复制完整实现
    ...


def _atomic_write_config(config_path: str, data: dict) -> None:
    # ... 从 server.py L1438-1466 复制完整实现
    ...


def _validate_config_schema(config: dict) -> None:
    # ... 从 server.py L1469-1584 复制完整实现（含 CronExpr import）
    ...


def _check_needs_restart(old_config: dict, new_config: dict) -> bool:
    # ... 从 server.py L1587-1606 复制完整实现
    ...


def _apply_runtime_config(orchestrator, old_config: dict, new_config: dict) -> None:
    # ... 从 server.py 复制完整实现
    ...
```

注意：`_validate_config_schema` 中用到 `CronExpr`，需在文件顶部添加 `from tasks.cron_expr import CronExpr` 的 try/except import。

- [ ] **Step 4: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_config_helpers_module.py -x --no-header -q
```
Expected: PASS（4 个测试）

- [ ] **Step 5: 更新 server.py 引用**

Edit `src/server.py`：
1. 删除 L1300-1606（常量 + 6 个函数）
2. 在 import 区添加 `from config_helpers import (_deep_merge_config, _backup_config, _atomic_write_config, _validate_config_schema, _check_needs_restart, _apply_runtime_config, _RESTART_REQUIRED_KEYS, _RUNTIME_HOTUPDATE_MAP)`

- [ ] **Step 6: 运行全量测试验证**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_config_update.py tests/test_config_helpers_module.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 无新增 failure

- [ ] **Step 7: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract config_helpers.py from server.py (Task 3)"
```

---

## Task 4: 提取 background_loops.py

**Files:**
- Create: `src/background_loops.py`
- Modify: `src/server.py:380-605`（删除 cleanup_loop / file_cleanup_loop / metrics_persist_loop，改为 import）

**Interfaces:**
- Consumes: `session_logger`, `metrics_store`, `orchestrator`, `upload_manager` 全局变量（从 server.py import 或通过参数传入）
- Produces: `from background_loops import cleanup_loop, file_cleanup_loop, metrics_persist_loop`

- [ ] **Step 1: 写失败测试**

Create `tests/test_background_loops_module.py`:
```python
"""Task 4: 验证 background_loops 模块可独立导入。"""
import sys

sys.path.insert(0, "src")


def test_cleanup_loop_is_coroutine():
    """cleanup_loop 是 async 函数。"""
    import inspect
    from background_loops import cleanup_loop
    assert inspect.iscoroutinefunction(cleanup_loop)


def test_file_cleanup_loop_is_coroutine():
    """file_cleanup_loop 是 async 函数。"""
    import inspect
    from background_loops import file_cleanup_loop
    assert inspect.iscoroutinefunction(file_cleanup_loop)


def test_metrics_persist_loop_is_coroutine():
    """metrics_persist_loop 是 async 函数。"""
    import inspect
    from background_loops import metrics_persist_loop
    assert inspect.iscoroutinefunction(metrics_persist_loop)
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_background_loops_module.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'background_loops'`

- [ ] **Step 3: 创建 src/background_loops.py**

Read `src/server.py:380-605`，将三个 async 循环函数复制到新文件。注意全局变量引用：通过 `import server` 延迟访问 `server.session_logger` 等全局变量，避免循环导入。

Create `src/background_loops.py`（骨架）:
```python
"""后台定时清理循环。

从 server.py 提取：cleanup_loop / file_cleanup_loop / metrics_persist_loop。
通过 `import server` 延迟访问 server 模块的全局组件变量，避免循环导入。
"""
from __future__ import annotations

import asyncio
import logging
from pathlib import Path

logger = logging.getLogger("hermes.server")


async def cleanup_loop():
    """定时清理古早会话的后台任务。"""
    # 从 server.py L380-463 复制完整实现
    # 全局变量引用改为：
    #   from server import session_logger, metrics_store, orchestrator  # 不行（循环）
    # 改为：
    #   import server
    #   server.session_logger, server.metrics_store, server.orchestrator
    ...


async def file_cleanup_loop() -> None:
    """定时清理过期文件磁盘的后台任务。"""
    # 从 server.py L466-560 复制完整实现
    ...


async def metrics_persist_loop():
    """定时持久化监控指标的后台任务。"""
    # 从 server.py L563-605 复制完整实现
    ...
```

实现要点：函数体内访问 `session_logger` / `metrics_store` / `orchestrator` / `upload_manager` 时，统一改为 `import server; server.session_logger` 等形式。

- [ ] **Step 4: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_background_loops_module.py -x --no-header -q
```
Expected: PASS（3 个测试）

- [ ] **Step 5: 更新 server.py 引用**

Edit `src/server.py`：
1. 删除 L380-605（三个循环函数）
2. 在 import 区添加 `from background_loops import cleanup_loop, file_cleanup_loop, metrics_persist_loop`

- [ ] **Step 6: 运行全量测试验证**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 7: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract background_loops.py from server.py (Task 4)"
```

---

## Task 5: 提取 lifespan.py + 简化 server.py 到 <100 行

**Files:**
- Create: `src/lifespan.py`
- Modify: `src/server.py`（删除 lifespan 函数 + 全局组件变量声明，改为 <100 行入口）
- Modify: `src/app.py:22-31`（lifespan wrapper 指向 lifespan.py）

**Interfaces:**
- Consumes: 所有 lifespan 中初始化的组件
- Produces: `from lifespan import lifespan, orchestrator, session_logger, metrics_collector, ...`（全局变量供 routes 访问）

- [ ] **Step 1: 写失败测试**

Create `tests/test_lifespan_module.py`:
```python
"""Task 5: 验证 lifespan 模块可独立导入且暴露必要全局变量。"""
import sys

sys.path.insert(0, "src")


def test_lifespan_is_callable():
    """lifespan 模块暴露 lifespan async context manager。"""
    from lifespan import lifespan
    assert callable(lifespan)


def test_global_variables_exist():
    """lifespan 模块暴露 orchestrator / session_logger 等全局变量（初始 None）。"""
    import lifespan
    assert hasattr(lifespan, "orchestrator")
    assert hasattr(lifespan, "session_logger")
    assert hasattr(lifespan, "metrics_collector")
    assert hasattr(lifespan, "approval_manager")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_lifespan_module.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'lifespan'`

- [ ] **Step 3: 创建 src/lifespan.py**

Read `src/server.py:324-1240`（全局变量声明 + lifespan 函数），复制到新文件。

Create `src/lifespan.py`（骨架）:
```python
"""FastAPI lifespan：启动时初始化资源，关闭时释放。

从 server.py 提取。全局组件变量（orchestrator / session_logger 等）在此模块
声明，供 routes / background_loops 通过 `import lifespan` 访问。
"""
from __future__ import annotations

import asyncio
import logging
import os
from contextlib import asynccontextmanager
from typing import Any, Optional

from fastapi import FastAPI

from config import load_config, validate_required_env_vars
from logging_setup import logger
from background_loops import cleanup_loop, file_cleanup_loop, metrics_persist_loop

# ---------- 全局组件 ----------
orchestrator: Optional[Any] = None
session_logger: Optional[Any] = None
metrics_collector: Optional[Any] = None
metrics_store: Optional[Any] = None
metrics_persist_task: Optional[asyncio.Task] = None
_metrics_baseline_reset: bool = False
audit_logger: Optional[Any] = None
approval_manager: Optional[Any] = None
skill_loader = None
skill_tools_registered = False
mcp_manager = None
task_manager: Optional[Any] = None
cron_scheduler: Optional[Any] = None
proposal_store: Optional[Any] = None
cron_tool_registry: Optional[Any] = None
health_checker: Optional[Any] = None
stream_manager: Optional[Any] = None
upload_manager: Optional[Any] = None
etl_engine: Optional[Any] = None
file_context_injector: Optional[Any] = None


@asynccontextmanager
async def lifespan(app: FastAPI):
    """FastAPI 生命周期：启动时初始化资源，关闭时释放。"""
    global orchestrator, session_logger, metrics_collector, audit_logger
    global metrics_store, skill_loader, mcp_manager, approval_manager
    global task_manager, cron_scheduler, proposal_store, cron_tool_registry
    global skill_tools_registered, upload_manager, etl_engine, file_context_injector
    global metrics_persist_task, file_cleanup_task, cleanup_task

    # ... 从 server.py L608-1240 复制完整 lifespan 实现
    # 所有 server.xxx 全局变量引用改为本模块的 global 声明
    ...


def get_components() -> dict:
    """返回所有全局组件的字典（供 routes / tests 访问）。"""
    return {
        "orchestrator": orchestrator,
        "session_logger": session_logger,
        "metrics_collector": metrics_collector,
        "metrics_store": metrics_store,
        "audit_logger": audit_logger,
        "approval_manager": approval_manager,
        "task_manager": task_manager,
        "cron_scheduler": cron_scheduler,
        "proposal_store": proposal_store,
        "cron_tool_registry": cron_tool_registry,
        "health_checker": health_checker,
        "stream_manager": stream_manager,
        "upload_manager": upload_manager,
        "etl_engine": etl_engine,
        "file_context_injector": file_context_injector,
        "skill_loader": skill_loader,
        "mcp_manager": mcp_manager,
    }
```

实现要点：
- `lifespan` 内部访问全局变量时使用 `global` 声明
- `background_loops.py` 中 `import server` 改为 `import lifespan`
- 所有 routes 文件中 `from server import orchestrator` 改为 `from lifespan import orchestrator`

- [ ] **Step 4: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_lifespan_module.py -x --no-header -q
```
Expected: PASS（2 个测试）

- [ ] **Step 5: 更新 background_loops.py 引用**

Edit `src/background_loops.py`：将 `import server` 改为 `import lifespan`，所有 `server.xxx` 改为 `lifespan.xxx`。

- [ ] **Step 6: 更新所有 routes/*.py 引用**

Run grep 找到所有 `from server import` 引用：
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -c "import os; [print(os.path.join(r,f)) for r,_,fs in os.walk('src/routes') for f in fs if f.endswith('.py')]"
```

对每个 routes 文件，将 `from server import orchestrator, session_logger, ...` 替换为 `from lifespan import orchestrator, session_logger, ...`。

- [ ] **Step 7: 更新 app.py lifespan wrapper**

Edit `src/app.py:22-31`:
```python
@contextlib.asynccontextmanager
async def lifespan(app: FastAPI):
    """Lifespan wrapper：延迟导入 lifespan 模块避免循环依赖。"""
    from lifespan import lifespan as _lifespan
    async with _lifespan(app):
        yield
```

- [ ] **Step 8: 简化 server.py 到 <100 行**

Read 当前 `src/server.py`，仅保留：
- 模块 docstring
- `if __name__ == "__main__":` 入口（uvicorn.run）
- `SERVER_LOG_PATH` 常量
- 必要 import

Write `src/server.py`（<100 行）:
```python
"""EC2 长驻 HTTP 服务入口。

所有路由、lifespan、后台循环、配置工具已提取到独立模块：
- app.py: FastAPI 实例 + 中间件 + 路由注册
- lifespan.py: 生命周期管理 + 全局组件
- background_loops.py: 定时清理循环
- config_helpers.py: 配置工具函数
- logging_setup.py: 日志配置
- routes/: 按域拆分的路由模块
"""
from __future__ import annotations

import os

from logging_setup import logger, SERVER_LOG_PATH  # noqa: F401
from config import load_config

VERSION = "0.1.0"
CONFIG_PATH = os.environ.get("HERMES_CONFIG", "config.yaml")


def main():
    """uvicorn 入口：从配置读取 host/port，启动 app。"""
    import uvicorn
    config = load_config(CONFIG_PATH)
    server_cfg = config.get("server", {})
    host = server_cfg.get("host", "0.0.0.0")
    port = int(server_cfg.get("port", 8000))

    logger.info("启动 Hermes Lite %s: %s:%d", VERSION, host, port)
    uvicorn.run(
        "app:app",
        host=host,
        port=port,
        log_config=None,  # 使用自定义 JSONLogFormatter
    )


if __name__ == "__main__":
    main()
```

- [ ] **Step 9: 运行全量测试验证**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 10: 验证 server.py 行数**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; (Get-Content src/server.py | Measure-Object -Line).Lines
```
Expected: `<100`

- [ ] **Step 11: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract lifespan.py + simplify server.py to <100 lines (Task 5)"
```

---

# Phase 2：移除 Orchestrator 委托 wrapper

目标：删除 orchestrator.py 中 22 个委托 wrapper 方法（~130 行），更新测试调用方直接访问 `orch.component.method()`。

委托 wrapper 清单（按组件分组）：

**SessionManager 委托（4 个）**:
- `_maybe_generate_title_async` → `session_mgr.generate_title_async`
- `_generate_title_task` → `session_mgr._generate_title_task`
- `_ensure_session` → `session_mgr.ensure_session`

**ContextBuilder 委托（4 个）**:
- `_build_environment_section` → `context_builder.build_environment`
- `_format_todo_for_injection` → `context_builder.format_todo`
- `_has_unfinished_steps` → `ContextBuilder.has_unfinished_steps`（静态方法）
- `_build_continuation_message` → `context_builder.build_continuation_message`

**SkillManager 委托（3 个）**:
- `activate_skill` → `skill_mgr.activate`
- `deactivate_skill` → `skill_mgr.deactivate`
- `_build_active_skills_section` → `skill_mgr.build_active_section`

**MessagePersistence 委托（6 个）**:
- `_persist_new_messages` → `msg_persistence.persist_new_messages`
- `_save_interrupt_notice` → `msg_persistence.save_interrupt_notice`
- `_sanitize_history_alternation` → `MessagePersistence.sanitize_history`（静态方法）
- `_is_empty_assistant_content` → `MessagePersistence.is_empty_assistant`（静态方法）
- `_maybe_flush_on_session_switch` → `msg_persistence.maybe_flush_on_switch`
- `flush_consolidation` → `msg_persistence.flush_consolidation`
- `_trigger_consolidation` → `msg_persistence.trigger_consolidation`

**CronIsolator 委托（4 个）**:
- `_build_cron_isolation` → `cron_isolator.build_isolation`
- `_build_cron_enhanced_context` → `cron_isolator.build_enhanced_context`
- `_build_cron_tools` → `cron_isolator.build_cron_tools`
- `set_cron_dependencies` → `cron_isolator.set_dependencies`

## Task 6: 移除 SessionManager + ContextBuilder 委托（7 个方法）

**Files:**
- Modify: `src/orchestrator.py:1691-1722`（删除 7 个委托方法）
- Modify: `src/orchestrator.py:815-1905`（chat / chat_stream / _build_enhanced_context 内部调用改为直接访问 component）
- Modify: `tests/test_orchestrator_title.py`（`orch._maybe_generate_title_async` → `orch.session_mgr.generate_title_async`）
- Modify: `tests/test_environment_injection.py`（`orch._build_environment_section` → `orch.context_builder.build_environment`）
- Modify: `tests/test_todo_injection.py`（`orch._format_todo_for_injection` → `orch.context_builder.format_todo`）
- Modify: `tests/test_react_loop_continuation.py`（`orch._build_continuation_message` → `orch.context_builder.build_continuation_message`）

**Interfaces:**
- Produces: Orchestrator 不再暴露 7 个委托方法；外部必须通过 `orch.session_mgr.*` / `orch.context_builder.*` 访问

- [ ] **Step 1: 写失败测试**

Create `tests/test_orchestrator_no_delegates_phase2a.py`:
```python
"""Task 6: 验证 Orchestrator 不再暴露 SessionManager/ContextBuilder 委托方法。"""
import sys

sys.path.insert(0, "src")


def test_orchestrator_has_no_maybe_generate_title_async():
    """_maybe_generate_title_async 已移除。"""
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_maybe_generate_title_async")


def test_orchestrator_has_no_generate_title_task():
    """_generate_title_task 已移除。"""
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_generate_title_task")


def test_orchestrator_has_no_ensure_session():
    """_ensure_session 已移除。"""
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_ensure_session")


def test_orchestrator_has_no_build_environment_section():
    """_build_environment_section 已移除。"""
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_build_environment_section")


def test_orchestrator_has_no_format_todo_for_injection():
    """_format_todo_for_injection 已移除。"""
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_format_todo_for_injection")


def test_orchestrator_has_no_has_unfinished_steps():
    """_has_unfinished_steps 已移除。"""
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_has_unfinished_steps")


def test_orchestrator_has_no_build_continuation_message():
    """_build_continuation_message 已移除。"""
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_build_continuation_message")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_no_delegates_phase2a.py -x --no-header -q
```
Expected: FAIL（7 个测试，方法仍存在）

- [ ] **Step 3: 更新 orchestrator.py 内部调用方**

在 `src/orchestrator.py` 中搜索以下调用并替换：
- `self._maybe_generate_title_async(` → `self.session_mgr.generate_title_async(`
- `self._generate_title_task(` → `self.session_mgr._generate_title_task(`
- `self._ensure_session(` → `self.session_mgr.ensure_session(`
- `self._build_environment_section(` → `self.context_builder.build_environment(`
- `self._format_todo_for_injection(` → `self.context_builder.format_todo(`
- `self._has_unfinished_steps(` → `ContextBuilder.has_unfinished_steps(`
- `self._build_continuation_message(` → `self.context_builder.build_continuation_message(`

使用 Grep 找到所有内部调用：
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -c "import re; [print(f'{i+1}: {l.rstrip()}') for i,l in enumerate(open('src/orchestrator.py',encoding='utf-8')) if re.search(r'self\.(_maybe_generate_title_async|_generate_title_task|_ensure_session|_build_environment_section|_format_todo_for_injection|_has_unfinished_steps|_build_continuation_message)\b', l)]"
```

- [ ] **Step 4: 删除 7 个委托方法**

Edit `src/orchestrator.py`，删除 L1691-1722 范围内的以下方法定义：
- `_maybe_generate_title_async`
- `_generate_title_task`
- `_ensure_session`
- `_build_environment_section`
- `_format_todo_for_injection`
- `_has_unfinished_steps`
- `_build_continuation_message`

- [ ] **Step 5: 更新测试调用方**

更新 `tests/test_orchestrator_title.py`:
- `orch._maybe_generate_title_async(...)` → `orch.session_mgr.generate_title_async(...)`

更新 `tests/test_environment_injection.py`:
- `orch._build_environment_section()` → `orch.context_builder.build_environment()`

更新 `tests/test_todo_injection.py`:
- `orch._format_todo_for_injection(...)` → `orch.context_builder.format_todo(...)`

更新 `tests/test_react_loop_continuation.py`:
- `orch._build_continuation_message(...)` → `orch.context_builder.build_continuation_message(...)`

- [ ] **Step 6: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_no_delegates_phase2a.py tests/test_orchestrator_title.py tests/test_environment_injection.py tests/test_todo_injection.py tests/test_react_loop_continuation.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 新测试 PASS，其余与基线一致

- [ ] **Step 7: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 8: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: remove SessionManager/ContextBuilder delegates from Orchestrator (Task 6)"
```

---

## Task 7: 移除 SkillManager + MessagePersistence 委托（10 个方法）

**Files:**
- Modify: `src/orchestrator.py`（删除 10 个委托方法）
- Modify: `src/orchestrator.py` 内部 chat / chat_stream 调用
- Modify: `tests/test_react_loop_continuation.py`（msg_persistence 相关调用）
- Modify: 其他调用 `orch.activate_skill` / `orch.deactivate_skill` / `orch.flush_consolidation` 的测试

**Interfaces:**
- Produces: Orchestrator 不再暴露这 10 个委托方法

- [ ] **Step 1: 写失败测试**

Create `tests/test_orchestrator_no_delegates_phase2b.py`:
```python
"""Task 7: 验证 Orchestrator 不再暴露 SkillManager/MessagePersistence 委托方法。"""
import sys

sys.path.insert(0, "src")


def test_no_activate_skill():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "activate_skill")


def test_no_deactivate_skill():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "deactivate_skill")


def test_no_build_active_skills_section():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_build_active_skills_section")


def test_no_persist_new_messages():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_persist_new_messages")


def test_no_save_interrupt_notice():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_save_interrupt_notice")


def test_no_sanitize_history_alternation():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_sanitize_history_alternation")


def test_no_is_empty_assistant_content():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_is_empty_assistant_content")


def test_no_maybe_flush_on_session_switch():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_maybe_flush_on_session_switch")


def test_no_flush_consolidation():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "flush_consolidation")


def test_no_trigger_consolidation():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_trigger_consolidation")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_no_delegates_phase2b.py -x --no-header -q
```
Expected: FAIL（10 个测试）

- [ ] **Step 3: 更新 orchestrator.py 内部调用方**

在 `src/orchestrator.py` 中搜索并替换：
- `self.activate_skill(` → `self.skill_mgr.activate(`
- `self.deactivate_skill(` → `self.skill_mgr.deactivate(`
- `self._build_active_skills_section(` → `self.skill_mgr.build_active_section(`
- `self._persist_new_messages(` → `self.msg_persistence.persist_new_messages(`
- `self._save_interrupt_notice(` → `self.msg_persistence.save_interrupt_notice(`
- `self._sanitize_history_alternation(` → `MessagePersistence.sanitize_history(`
- `self._is_empty_assistant_content(` → `MessagePersistence.is_empty_assistant(`
- `self._maybe_flush_on_session_switch(` → `self.msg_persistence.maybe_flush_on_switch(`
- `self.flush_consolidation(` → `self.msg_persistence.flush_consolidation(`
- `self._trigger_consolidation(` → `self.msg_persistence.trigger_consolidation(`

使用 Grep 找到所有内部调用：
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -c "import re; [print(f'{i+1}: {l.rstrip()}') for i,l in enumerate(open('src/orchestrator.py',encoding='utf-8')) if re.search(r'self\.(activate_skill|deactivate_skill|_build_active_skills_section|_persist_new_messages|_save_interrupt_notice|_sanitize_history_alternation|_is_empty_assistant_content|_maybe_flush_on_session_switch|flush_consolidation|_trigger_consolidation)\b', l)]"
```

- [ ] **Step 4: 删除 10 个委托方法**

Edit `src/orchestrator.py`，删除以下方法定义：
- `activate_skill` (L1728-1730)
- `deactivate_skill` (L1732-1734)
- `_build_active_skills_section` (L1736-1738)
- `_persist_new_messages` (L1968-1978)
- `_save_interrupt_notice` (L1980-1984)
- `_sanitize_history_alternation` (L1986-1991)
- `_is_empty_assistant_content` (L1993-1996)
- `_maybe_flush_on_session_switch` (L2034-2036)
- `flush_consolidation` (L2038-2040)
- `_trigger_consolidation` (L2042-2044)

- [ ] **Step 5: 更新测试调用方**

使用 Grep 找到所有测试中的调用：
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -c "import os, re; [print(f'{os.path.join(r,f)}:{i+1}: {l.rstrip()}') for r,_,fs in os.walk('tests') for f in fs if f.endswith('.py') for i,l in enumerate(open(os.path.join(r,f),encoding='utf-8')) if re.search(r'\b(activate_skill|deactivate_skill|_build_active_skills_section|_persist_new_messages|_save_interrupt_notice|_sanitize_history_alternation|_is_empty_assistant_content|_maybe_flush_on_session_switch|flush_consolidation|_trigger_consolidation)\b', l) and 'orch' in l.lower()]"
```

对每个匹配的测试文件，替换调用：
- `orch.activate_skill(` → `orch.skill_mgr.activate(`
- `orch.deactivate_skill(` → `orch.skill_mgr.deactivate(`
- `orch._build_active_skills_section(` → `orch.skill_mgr.build_active_section(`
- `orch._persist_new_messages(` → `orch.msg_persistence.persist_new_messages(`
- `orch._save_interrupt_notice(` → `orch.msg_persistence.save_interrupt_notice(`
- `orch._sanitize_history_alternation(` → `MessagePersistence.sanitize_history(`（需 import）
- `orch._is_empty_assistant_content(` → `MessagePersistence.is_empty_assistant(`（需 import）
- `orch._maybe_flush_on_session_switch(` → `orch.msg_persistence.maybe_flush_on_switch(`
- `orch.flush_consolidation(` → `orch.msg_persistence.flush_consolidation(`
- `orch._trigger_consolidation(` → `orch.msg_persistence.trigger_consolidation(`

- [ ] **Step 6: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_no_delegates_phase2b.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 10 PASS

- [ ] **Step 7: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 8: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: remove SkillManager/MessagePersistence delegates from Orchestrator (Task 7)"
```

---

## Task 8: 移除 CronIsolator 委托（4 个方法）

**Files:**
- Modify: `src/orchestrator.py:1907-1940`（删除 4 个委托方法）
- Modify: `src/orchestrator.py` 内部 `_build_enhanced_context` 调用
- Modify: `tests/test_cron_tool.py`（`orch.set_cron_dependencies` / `orch._build_cron_tools` / `orch._build_cron_enhanced_context`）
- Modify: `tests/test_orchestrator_cron_context.py`（同上）
- Modify: `tests/test_cron_isolation.py`（`orch._build_cron_enhanced_context`）
- Modify: routes/cron_tools.py / routes/schedules.py（如有 `orch.set_cron_dependencies` 调用）

**Interfaces:**
- Produces: Orchestrator 不再暴露这 4 个委托方法

- [ ] **Step 1: 写失败测试**

Create `tests/test_orchestrator_no_delegates_phase2c.py`:
```python
"""Task 8: 验证 Orchestrator 不再暴露 CronIsolator 委托方法。"""
import sys

sys.path.insert(0, "src")


def test_no_build_cron_isolation():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_build_cron_isolation")


def test_no_build_cron_enhanced_context():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_build_cron_enhanced_context")


def test_no_build_cron_tools():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "_build_cron_tools")


def test_no_set_cron_dependencies():
    from orchestrator import Orchestrator
    assert not hasattr(Orchestrator, "set_cron_dependencies")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_no_delegates_phase2c.py -x --no-header -q
```
Expected: FAIL（4 个测试）

- [ ] **Step 3: 更新 orchestrator.py 内部调用方**

在 `src/orchestrator.py` 中搜索并替换：
- `self._build_cron_isolation(` → `self.cron_isolator.build_isolation(`
- `self._build_cron_enhanced_context(` → `self.cron_isolator.build_enhanced_context(`
- `self._build_cron_tools(` → `self.cron_isolator.build_cron_tools(`
- `self.set_cron_dependencies(` → `self.cron_isolator.set_dependencies(`

- [ ] **Step 4: 删除 4 个委托方法**

Edit `src/orchestrator.py`，删除：
- `_build_cron_isolation` (L1907-1911)
- `_build_cron_enhanced_context` (L1913-1923)
- `_build_cron_tools` (L1925-1929)
- `set_cron_dependencies` (L1931-1940)

- [ ] **Step 5: 更新测试与 routes 调用方**

对以下文件执行替换：
- `tests/test_cron_tool.py`: `orch.set_cron_dependencies` → `orch.cron_isolator.set_dependencies`; `orch._build_cron_tools` → `orch.cron_isolator.build_cron_tools`; `orch._build_cron_enhanced_context` → `orch.cron_isolator.build_enhanced_context`
- `tests/test_orchestrator_cron_context.py`: 同上
- `tests/test_cron_isolation.py`: `orch._build_cron_enhanced_context` → `orch.cron_isolator.build_enhanced_context`
- `src/routes/*.py`: `orch.set_cron_dependencies` → `orch.cron_isolator.set_dependencies`

使用 Grep 找所有调用：
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -c "import os, re; [print(f'{os.path.join(r,f)}:{i+1}: {l.rstrip()}') for r,_,fs in os.walk('.') for f in fs if f.endswith('.py') for i,l in enumerate(open(os.path.join(r,f),encoding='utf-8')) if re.search(r'\.(set_cron_dependencies|_build_cron_tools|_build_cron_enhanced_context|_build_cron_isolation)\b', l) and not os.path.join(r,f).endswith('orchestrator.py')]"
```

- [ ] **Step 6: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_no_delegates_phase2c.py tests/test_cron_tool.py tests/test_orchestrator_cron_context.py tests/test_cron_isolation.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 新测试 PASS，其余与基线一致

- [ ] **Step 7: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 8: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: remove CronIsolator delegates from Orchestrator (Task 8)"
```

---

# Phase 3：提取 Orchestrator 核心方法

目标：orchestrator.py 从当前行数（移除委托后约 1800 行）降到 <350 行。

## Task 9: 提取 _build_enhanced_context 到 EnhancedContextBuilder

**Files:**
- Create: `src/orchestrator/enhanced_context.py`
- Create: `src/orchestrator/__init__.py`（重新导出 Orchestrator）
- Move: `src/orchestrator.py` → `src/orchestrator/__init__.py`（保留 __init__ + close/shutdown）

**注意**：此 Task 将 `src/orchestrator.py` 转为 `src/orchestrator/` 包，所有现有 `from orchestrator import Orchestrator` 自动从 `__init__.py` 导入。

**Interfaces:**
- Consumes: `context_manager`, `memory_retriever`, `metrics`, `task_manager`, `todo_registry`, `context_builder`, `skill_mgr`, `cron_isolator`, `condenser`
- Produces: `EnhancedContextBuilder.build(session_id, user_input, history) -> (system_text, enhanced_history, tools_override)`

- [ ] **Step 1: 写失败测试**

Create `tests/test_enhanced_context_builder.py`:
```python
"""Task 9: 验证 EnhancedContextBuilder 可独立构造并构建上下文。"""
import asyncio
import sys

sys.path.insert(0, "src")


def test_enhanced_context_builder_class_exists():
    """EnhancedContextBuilder 类存在。"""
    from orchestrator.enhanced_context import EnhancedContextBuilder
    assert EnhancedContextBuilder is not None


def test_build_method_signature():
    """build 方法接受 (session_id, user_input, history) 三参数。"""
    import inspect
    from orchestrator.enhanced_context import EnhancedContextBuilder
    sig = inspect.signature(EnhancedContextBuilder.build)
    params = list(sig.parameters.keys())
    # self 之外的参数
    assert "session_id" in params
    assert "user_input" in params
    assert "history" in params


def test_build_returns_three_tuple_for_user_session():
    """用户会话路径返回 (system_text, enhanced_history, tools_override) 三元组。"""
    from orchestrator.enhanced_context import EnhancedContextBuilder

    # 构造最小化的 mock 依赖
    class MockBuilder:
        def __init__(self):
            self.context_manager = None
            self.memory_retriever = None
            self.metrics = None
            self.task_manager = None
            self.todo_registry = None
            self.context_builder = None
            self.skill_mgr = None
            self.cron_isolator = None
            self.condenser = None

    builder = EnhancedContextBuilder(MockBuilder())
    result = asyncio.run(builder.build("user-session", "hello", []))
    assert len(result) == 3
    system_text, enhanced_history, tools_override = result
    assert isinstance(system_text, str)
    assert isinstance(enhanced_history, list)
    assert tools_override is None
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_enhanced_context_builder.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'orchestrator.enhanced_context'`

- [ ] **Step 3: 创建 orchestrator 包结构**

将 `src/orchestrator.py` 转为 `src/orchestrator/` 包：

1. 创建 `src/orchestrator/` 目录
2. 将 `src/orchestrator.py` 内容移动到 `src/orchestrator/__init__.py`（保持所有现有导入路径 `from orchestrator import Orchestrator` 兼容）
3. 创建 `src/orchestrator/enhanced_context.py`

使用 Shell 完成移动：
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; New-Item -ItemType Directory -Path src/orchestrator -Force; Move-Item src/orchestrator.py src/orchestrator/__init__.py
```

- [ ] **Step 4: 创建 src/orchestrator/enhanced_context.py**

Read 原 `src/orchestrator/__init__.py` L1740-1905（`_build_enhanced_context` 方法），提取为独立类。

Create `src/orchestrator/enhanced_context.py`:
```python
"""增强上下文构建器。

从 Orchestrator._build_enhanced_context 提取，负责构建含用户画像、检索记忆、
环境信息、任务进度、TodoList、文件摘要、已激活 Skill 的完整上下文。
"""
from __future__ import annotations

import asyncio
import logging
from typing import TYPE_CHECKING, Any, Dict, List, Optional, Tuple

if TYPE_CHECKING:
    from ..llm.prompts import SYSTEM_PROMPT

try:
    from ..llm.prompts import SYSTEM_PROMPT
except ImportError:
    try:
        from llm.prompts import SYSTEM_PROMPT
    except ImportError:
        SYSTEM_PROMPT = ""  # type: ignore

logger = logging.getLogger(__name__)


class EnhancedContextBuilder:
    """构建含画像与检索记忆的上下文。

    持有 Orchestrator 的弱引用（通过 orchestrator 属性访问组件），
    避免循环引用。所有原 _build_enhanced_context 的逻辑迁移至此。
    """

    def __init__(self, orchestrator: Any) -> None:
        """初始化上下文构建器。

        参数:
            orchestrator: Orchestrator 实例，通过属性访问组件。
        """
        self.orch = orchestrator

    async def build(
        self,
        session_id: str,
        user_input: str,
        history: List[Dict[str, Any]],
    ) -> Tuple[str, List[Dict[str, Any]], Optional[List[Dict[str, Any]]]]:
        """构建含用户画像与检索记忆的上下文。

        返回 (system_text, enhanced_history, tools_override) 三元组。
        用户会话 tools_override 固定 None；cron 会话由 CronIsolator 返回。
        """
        # cron 会话走隔离路径
        cron_isolation = self.orch.cron_isolator.build_isolation(session_id)
        if cron_isolation is not None:
            return await self.orch.cron_isolator.build_enhanced_context(
                session_id, user_input, history, cron_isolation
            )

        system_text = SYSTEM_PROMPT
        enhanced_history = history
        injection_text = ""

        # 1. 注入用户画像到 system
        if self.orch.context_manager is not None:
            try:
                system_text = self.orch.context_manager.get_cache_stable_prefix()
            except Exception as e:
                logger.warning("构建含画像的 system 失败: %s", e)
                system_text = SYSTEM_PROMPT

        # 2. 检索长期记忆
        if self.orch.memory_retriever is not None:
            try:
                is_first_round = len(history) < 2
                if is_first_round:
                    memory_text = await asyncio.to_thread(
                        self.orch.memory_retriever.get_injection_text, user_input
                    )
                else:
                    memory_text = await asyncio.to_thread(
                        self.orch.memory_retriever.get_injection_text_lightweight,
                        user_input,
                    )
                if self.orch.metrics is not None:
                    self.orch.metrics.observe_memory_retrieval(hit=bool(memory_text))
                if memory_text:
                    injection_text = memory_text
            except Exception as e:
                logger.warning("长期记忆检索注入失败: %s", e)

        # 2.5 注入运行环境信息
        try:
            env_section = self.orch.context_builder.build_environment()
            if env_section:
                injection_text = f"{env_section}\n\n{injection_text}" if injection_text else env_section
        except Exception as e:
            logger.warning("运行环境信息注入失败: %s", e)

        # 3. 注入任务进度摘要
        if self.orch.task_manager is not None:
            try:
                task_summary = self.orch.task_manager.get_progress_summary()
                if task_summary:
                    task_section = f"## 当前任务状态\n{task_summary}"
                    injection_text = f"{injection_text}\n\n{task_section}" if injection_text else task_section
            except Exception as e:
                logger.warning("任务进度注入失败: %s", e)

        # 3.5 注入 TodoList 状态
        if self.orch.todo_registry is not None:
            try:
                todo_dict = self.orch.todo_registry.get_todo_dict(session_id)
                todo_section = self.orch.context_builder.format_todo(todo_dict)
                if todo_section:
                    injection_text = f"{injection_text}\n\n{todo_section}" if injection_text else todo_section
            except Exception as e:
                logger.warning("TodoList 状态注入失败: %s", e)

        # 4. 注入已上传文件摘要
        if self.orch.context_manager is not None:
            try:
                file_section = self.orch.context_manager.get_file_injection(session_id)
                if file_section:
                    injection_text = f"{injection_text}\n\n{file_section}" if injection_text else file_section
            except Exception as e:
                logger.warning("文件摘要注入失败: %s", e)

        # 5. 注入已激活 Skill body
        try:
            skill_section = self.orch.skill_mgr.build_active_section(session_id)
            if skill_section:
                injection_text = f"{injection_text}\n\n{skill_section}" if injection_text else skill_section
        except Exception as e:
            logger.warning("已激活 skill body 注入失败: %s", e)

        # 统一前置 injection_text 到 history
        condensed_history = await self._apply_condenser(history)
        if injection_text:
            enhanced_history = [{"role": "user", "content": injection_text}] + condensed_history
        else:
            enhanced_history = condensed_history

        return system_text, enhanced_history, None

    async def _apply_condenser(
        self, history: List[Dict[str, Any]]
    ) -> List[Dict[str, Any]]:
        """对 history 应用 condenser 压缩。"""
        clean = [
            {"role": m.get("role"), "content": m.get("content")}
            for m in history
            if m.get("role") is not None and m.get("content") is not None
        ]
        condenser = getattr(self.orch, "condenser", None)
        if condenser is None:
            return clean
        try:
            return await asyncio.to_thread(condenser.condense, clean)
        except Exception as e:
            logger.warning("condenser 压缩历史失败: %s", e)
            return clean
```

- [ ] **Step 5: 更新 orchestrator/__init__.py**

Edit `src/orchestrator/__init__.py`:
1. 删除 `_build_enhanced_context` 方法（原 L1740-1905）
2. 删除 `_apply_condenser` 方法（原 L1942-1966）
3. 在 `__init__` 末尾添加：`self.enhanced_context_builder = EnhancedContextBuilder(self)`
4. 添加 import：`from .enhanced_context import EnhancedContextBuilder`
5. 在 `chat` / `chat_stream` 中将 `self._build_enhanced_context(...)` 调用替换为 `self.enhanced_context_builder.build(...)`

- [ ] **Step 6: 更新测试调用方**

更新以下测试文件中 `orch._build_enhanced_context(...)` → `orch.enhanced_context_builder.build(...)`:
- `tests/test_cron_isolation.py` (8 处)
- `tests/test_environment_injection.py` (11 处)
- `tests/test_cron_tool.py` (4 处)
- `tests/test_file_injection_integration.py` (1 处)
- `tests/test_orchestrator_cron_context.py` 中 `orch._apply_condenser` → `orch.enhanced_context_builder._apply_condenser`

- [ ] **Step 7: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_enhanced_context_builder.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 3 PASS

- [ ] **Step 8: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 9: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract EnhancedContextBuilder from Orchestrator (Task 9)"
```

---

## Task 10: 提取 chat() 到 ChatHandler

**Files:**
- Create: `src/orchestrator/chat_handler.py`
- Modify: `src/orchestrator/__init__.py`（删除 `chat` 方法，添加 `chat_handler` 属性）

**Interfaces:**
- Consumes: 17 个 self 属性（`_current_session_id`, `_current_intent_result`, `history_buffer`, `_pending_interrupt_notices`, `_consecutive_empty_runs`, `guardrail_engine`, `metrics`, `session_logger`, `consolidation_engine`, `session_mgr`, `enhanced_context_builder`, `react_loop`, `msg_persistence`, `todo_registry`, `context_builder` 等）
- Produces: `ChatHandler.chat(session_id, user_input, cancel_event, reasoning_cfg, is_cron) -> str`

- [ ] **Step 1: 写失败测试**

Create `tests/test_chat_handler.py`:
```python
"""Task 10: 验证 ChatHandler 类可独立导入。"""
import sys

sys.path.insert(0, "src")


def test_chat_handler_class_exists():
    """ChatHandler 类存在。"""
    from orchestrator.chat_handler import ChatHandler
    assert ChatHandler is not None


def test_chat_method_is_coroutine():
    """chat 方法是 async。"""
    import inspect
    from orchestrator.chat_handler import ChatHandler
    assert inspect.iscoroutinefunction(ChatHandler.chat)


def test_orchestrator_delegates_chat_to_handler():
    """Orchestrator.chat 委托到 ChatHandler.chat（保持向后兼容）。"""
    from orchestrator import Orchestrator
    # Orchestrator 仍暴露 chat 方法（作为 thin facade）
    assert hasattr(Orchestrator, "chat")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_chat_handler.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'orchestrator.chat_handler'`

- [ ] **Step 3: 创建 src/orchestrator/chat_handler.py**

Read `src/orchestrator/__init__.py` 中 `chat` 方法（原 L815-1134，约 319 行），提取为独立类。

Create `src/orchestrator/chat_handler.py`（骨架）:
```python
"""对话处理器。

从 Orchestrator.chat 提取，负责非流式对话流程：
历史获取 → 上下文构建 → React 循环 → 消息持久化 → 记忆沉淀触发。
"""
from __future__ import annotations

import logging
import threading
import time
from typing import TYPE_CHECKING, Any, Dict, List, Optional

if TYPE_CHECKING:
    from ..llm.reasoning_profiles import ReasoningConfig

logger = logging.getLogger(__name__)


class ChatHandler:
    """非流式对话处理器。

    持有 Orchestrator 引用，通过属性访问组件，避免重复构造。
    """

    def __init__(self, orchestrator: Any) -> None:
        self.orch = orchestrator

    async def chat(
        self,
        session_id: str,
        user_input: str,
        cancel_event: Optional[threading.Event] = None,
        reasoning_cfg: Optional["ReasoningConfig"] = None,
        is_cron: bool = False,
    ) -> str:
        """主对话入口（非流式）。

        流程:
            1. 获取 session 历史；
            2. 调用 enhanced_context_builder 构建上下文；
            3. 调用 react_loop.run 执行 React 循环；
            4. 记录用户输入与 assistant 回复到 session_logger；
            5. 更新 history_buffer；
            6. 触发 consolidation。
        """
        # 从原 orchestrator.py L836-1134 复制完整实现
        # 所有 self.xxx 改为 self.orch.xxx
        # self._build_enhanced_context 改为 self.orch.enhanced_context_builder.build
        # self._persist_new_messages 改为 self.orch.msg_persistence.persist_new_messages
        # 其他委托方法已在前序 Task 中替换为 component 直接访问
        ...
```

实现要点：将原 `chat` 方法体中所有 `self.xxx` 替换为 `self.orch.xxx`，保持业务逻辑不变。

- [ ] **Step 4: 更新 orchestrator/__init__.py**

Edit `src/orchestrator/__init__.py`:
1. 添加 import：`from .chat_handler import ChatHandler`
2. 在 `__init__` 末尾添加：`self.chat_handler = ChatHandler(self)`
3. 删除 `chat` 方法定义（原 L815-1134）
4. 添加 thin facade：
```python
async def chat(self, session_id: str, user_input: str,
               cancel_event: Optional[threading.Event] = None,
               reasoning_cfg: Optional["ReasoningConfig"] = None,
               is_cron: bool = False) -> str:
    """主对话入口（委托到 ChatHandler）。"""
    return await self.chat_handler.chat(
        session_id, user_input,
        cancel_event=cancel_event,
        reasoning_cfg=reasoning_cfg,
        is_cron=is_cron,
    )
```

注意：保留 `chat` 作为 thin facade 确保所有 `orch.chat(...)` 调用兼容。

- [ ] **Step 5: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_chat_handler.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 3 PASS

- [ ] **Step 6: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 7: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract ChatHandler.chat from Orchestrator (Task 10)"
```

---

## Task 11: 提取 chat_stream() 到 ChatHandler

**Files:**
- Modify: `src/orchestrator/chat_handler.py`（添加 `chat_stream` 方法）
- Modify: `src/orchestrator/__init__.py`（删除 `chat_stream` 方法，添加 thin facade）

**Interfaces:**
- Produces: `ChatHandler.chat_stream(session_id, user_input, ...) -> AsyncGenerator`

- [ ] **Step 1: 写失败测试**

Create `tests/test_chat_handler_stream.py`:
```python
"""Task 11: 验证 ChatHandler.chat_stream 存在且为 async generator。"""
import inspect
import sys

sys.path.insert(0, "src")


def test_chat_stream_method_exists():
    """ChatHandler.chat_stream 方法存在。"""
    from orchestrator.chat_handler import ChatHandler
    assert hasattr(ChatHandler, "chat_stream")


def test_chat_stream_is_async_generator():
    """chat_stream 是 async generator 函数。"""
    from orchestrator.chat_handler import ChatHandler
    assert inspect.isasyncgenfunction(ChatHandler.chat_stream)


def test_orchestrator_delegates_chat_stream():
    """Orchestrator.chat_stream 委托到 ChatHandler（保持向后兼容）。"""
    from orchestrator import Orchestrator
    assert hasattr(Orchestrator, "chat_stream")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_chat_handler_stream.py -x --no-header -q
```
Expected: FAIL（chat_stream 不在 ChatHandler 上）

- [ ] **Step 3: 添加 chat_stream 到 ChatHandler**

Read `src/orchestrator/__init__.py` 中 `chat_stream` 方法（原 L1135-1690，约 555 行），迁移到 `src/orchestrator/chat_handler.py`。

Edit `src/orchestrator/chat_handler.py`，添加：
```python
async def chat_stream(
    self,
    session_id: str,
    user_input: str,
    cancel_event: Optional[threading.Event] = None,
    reasoning_cfg: Optional["ReasoningConfig"] = None,
    is_cron: bool = False,
):
    """流式对话入口（SSE 事件生成器）。

    从原 Orchestrator.chat_stream 迁移。所有 self.xxx 改为 self.orch.xxx。
    """
    # 从原 orchestrator.py L1135-1690 复制完整实现
    # 替换 self.xxx → self.orch.xxx
    # 替换 self._build_enhanced_context → self.orch.enhanced_context_builder.build
    # 替换其他委托方法 → component 直接访问
    ...
```

- [ ] **Step 4: 更新 orchestrator/__init__.py**

Edit `src/orchestrator/__init__.py`:
1. 删除 `chat_stream` 方法定义（原 L1135-1690）
2. 添加 thin facade：
```python
async def chat_stream(self, session_id: str, user_input: str,
                      cancel_event: Optional[threading.Event] = None,
                      reasoning_cfg: Optional["ReasoningConfig"] = None,
                      is_cron: bool = False):
    """流式对话入口（委托到 ChatHandler）。"""
    async for event in self.chat_handler.chat_stream(
        session_id, user_input,
        cancel_event=cancel_event,
        reasoning_cfg=reasoning_cfg,
        is_cron=is_cron,
    ):
        yield event
```

- [ ] **Step 5: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_chat_handler_stream.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 3 PASS

- [ ] **Step 6: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 7: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract ChatHandler.chat_stream from Orchestrator (Task 11)"
```

---

## Task 12: 简化 Orchestrator __init__（提取工厂函数）

**Files:**
- Create: `src/orchestrator/factories.py`
- Modify: `src/orchestrator/__init__.py`（`__init__` 从 515 行降到 <200 行，通过调用工厂函数）

**Interfaces:**
- Produces: 6 个工厂函数，每个返回组件实例

- [ ] **Step 1: 写失败测试**

Create `tests/test_orchestrator_factories.py`:
```python
"""Task 12: 验证 orchestrator.factories 模块可独立导入。"""
import sys

sys.path.insert(0, "src")


def test_factories_module_exists():
    """orchestrator.factories 模块存在。"""
    from orchestrator import factories
    assert factories is not None


def test_create_llm_client_exists():
    """create_llm_client 工厂函数存在。"""
    from orchestrator.factories import create_llm_client
    assert callable(create_llm_client)


def test_create_tool_registry_exists():
    """create_tool_registry 工厂函数存在。"""
    from orchestrator.factories import create_tool_registry
    assert callable(create_tool_registry)


def test_create_memory_subsystem_exists():
    """create_memory_subsystem 工厂函数存在。"""
    from orchestrator.factories import create_memory_subsystem
    assert callable(create_memory_subsystem)


def test_create_security_subsystem_exists():
    """create_security_subsystem 工厂函数存在。"""
    from orchestrator.factories import create_security_subsystem
    assert callable(create_security_subsystem)


def test_create_react_loop_exists():
    """create_react_loop 工厂函数存在。"""
    from orchestrator.factories import create_react_loop
    assert callable(create_react_loop)


def test_orchestrator_init_under_200_lines():
    """Orchestrator.__init__ 应小于 200 行（从 515 行简化）。"""
    import inspect
    from orchestrator import Orchestrator
    source = inspect.getsource(Orchestrator.__init__)
    line_count = source.count("\n")
    assert line_count < 250, f"__init__ 仍有 {line_count} 行，目标 <250"
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_factories.py -x --no-header -q
```
Expected: FAIL（模块不存在）

- [ ] **Step 3: 创建 src/orchestrator/factories.py**

Read `src/orchestrator/__init__.py` 中 `__init__` 方法（原 L243-758，约 515 行），按职责拆分为 6 个工厂函数：

Create `src/orchestrator/factories.py`（骨架）:
```python
"""Orchestrator 组件工厂函数。

将 Orchestrator.__init__ 中按职责分组的组件构造逻辑提取为独立工厂函数，
使 __init__ 简化为纯粹的「调用工厂 + 装配」流程。
"""
from __future__ import annotations

import logging
from pathlib import Path
from typing import Any, Dict, Optional

logger = logging.getLogger(__name__)


def create_llm_client(config: Dict[str, Any], metrics=None):
    """创建 LLM 客户端。"""
    from llm.client import LLMClient
    return LLMClient(config=config, metrics_collector=metrics)


def create_tool_registry(
    config: Dict[str, Any],
    file_registry=None,
    consolidation_engine=None,
    signal_pool=None,
    todo_registry=None,
    get_session_id=None,
):
    """创建工具注册中心并注册内置工具。

    返回 (tool_registry, file_registry)。
    """
    from agent.tool_registry import ToolRegistry
    from agent.tools import register_builtin_tools
    from agent.tools.plan_tools import register_plan_tools
    from agent.tools.memory_tools import register_memory_tools

    tools_config = config.get("tools", {})
    tool_registry = ToolRegistry(
        defer_loading_threshold=int(tools_config.get("defer_loading_threshold", 20)),
    )

    # 注册 plan 工具
    if todo_registry is not None and register_plan_tools is not None:
        try:
            register_plan_tools(tool_registry, todo_registry, get_session_id)
        except Exception as e:
            logger.warning("注册 plan 工具失败: %s", e)

    # 注册内置工具
    if register_builtin_tools is not None:
        try:
            register_builtin_tools(
                tool_registry,
                file_registry=file_registry,
                get_session_id=get_session_id,
                consolidation_engine=consolidation_engine,
                signal_pool=signal_pool,
            )
        except Exception as e:
            logger.warning("注册内置工具失败: %s", e)

    return tool_registry


def create_memory_subsystem(config: Dict[str, Any], llm_client=None, metrics=None):
    """创建记忆子系统组件。

    返回 dict，包含 history_buffer / chroma_store / memory_md_manager /
    memory_retriever / context_manager / consolidation_engine / signal_pool /
    condenser / decay。
    """
    # 从原 __init__ L457-682 复制完整实现
    ...


def create_security_subsystem(config: Dict[str, Any], metrics=None, approval_manager=None):
    """创建安全子系统组件。

    返回 (policy_engine, approval_manager, guardrail_engine, file_registry)。
    """
    # 从原 __init__ L290-432 复制完整实现
    ...


def create_react_loop(
    config: Dict[str, Any],
    llm_client=None,
    tool_registry=None,
    audit_logger=None,
    metrics=None,
    policy_engine=None,
    approval_manager=None,
    guardrail_engine=None,
    orchestrator_ref=None,
):
    """创建 React 循环引擎。"""
    from agent.react_loop import ReactLoop
    tools_config = config.get("tools", {})
    max_loops = int(tools_config.get("max_react_loops", 50))
    return ReactLoop(
        llm_client=llm_client,
        tool_registry=tool_registry,
        max_loops=max_loops,
        audit_logger=audit_logger,
        metrics=metrics,
        policy_engine=policy_engine,
        approval_manager=approval_manager,
        guardrail_engine=guardrail_engine,
        orchestrator_ref=orchestrator_ref,
    )
```

- [ ] **Step 4: 简化 Orchestrator.__init__**

Edit `src/orchestrator/__init__.py`，将 `__init__` 改为调用工厂函数：

```python
def __init__(
    self,
    config_path: str = "config.yaml",
    metrics: Optional["MetricsCollector"] = None,
    audit_logger: Optional["AuditLogger"] = None,
    approval_manager: Optional["ApprovalManager"] = None,
    task_manager: Optional["TaskManager"] = None,
) -> None:
    """初始化编排器并装配各组件。"""
    from .factories import (
        create_llm_client,
        create_tool_registry,
        create_memory_subsystem,
        create_security_subsystem,
        create_react_loop,
    )

    self.metrics = metrics
    self.audit_logger = audit_logger
    self.config = load_config(config_path)

    # 1. LLM 客户端
    self.llm_client = create_llm_client(self.config, metrics=metrics)

    # 2. 安全子系统
    self.policy_engine, self.approval_manager, self.guardrail_engine, self.file_registry = (
        create_security_subsystem(self.config, metrics=metrics, approval_manager=approval_manager)
    )

    # 3. TaskManager + TodoListRegistry
    self.task_manager = task_manager or _create_task_manager(self.config)
    self.todo_registry = _create_todo_registry(self.config)

    # 4. 记忆子系统
    memory_components = create_memory_subsystem(self.config, llm_client=self.llm_client, metrics=metrics)
    self.history_buffer = memory_components["history_buffer"]
    self.chroma_store = memory_components["chroma_store"]
    self.memory_md_manager = memory_components["memory_md_manager"]
    self.memory_retriever = memory_components["memory_retriever"]
    self.context_manager = memory_components["context_manager"]
    self.consolidation_engine = memory_components["consolidation_engine"]
    self.signal_pool = memory_components["signal_pool"]
    self.condenser = memory_components["condenser"]
    self.decay = memory_components["decay"]

    # 5. 工具注册中心
    self.tool_registry = None
    if self.todo_registry is not None:
        self._current_session_id = None
        self.tool_registry = create_tool_registry(
            self.config,
            file_registry=self.file_registry,
            consolidation_engine=self.consolidation_engine,
            signal_pool=self.signal_pool,
            todo_registry=self.todo_registry,
            get_session_id=lambda: self._current_session_id,
        )

    # 6. React 循环引擎
    self.react_loop = create_react_loop(
        self.config,
        llm_client=self.llm_client,
        tool_registry=self.tool_registry,
        audit_logger=self.audit_logger,
        metrics=self.metrics,
        policy_engine=self.policy_engine,
        approval_manager=self.approval_manager,
        guardrail_engine=self.guardrail_engine,
        orchestrator_ref=self,
    )

    # 7. 会话状态
    self._current_session_id: Optional[str] = None
    self._current_intent_result = None
    self._pending_interrupt_notices: Dict[str, str] = {}
    self._consecutive_empty_runs: Dict[str, int] = {}
    self._last_session_id: Optional[str] = None
    self.session_logger = memory_components.get("session_logger")
    self.cron_scheduler = None
    self.cron_tool_registry = None

    # 8. 委托组件
    self.session_mgr = SessionManager(llm_client=self.llm_client, session_logger=self.session_logger)
    self.skill_mgr = SkillManager(skill_loader=getattr(self, "skill_loader", None))
    self.cron_isolator = CronIsolator(orchestrator=self)
    self.context_builder = ContextBuilder()
    self.msg_persistence = MessagePersistence(orchestrator=self)

    # 9. 上下文构建器与对话处理器
    self.enhanced_context_builder = EnhancedContextBuilder(self)
    self.chat_handler = ChatHandler(self)

    logger.info("Orchestrator 初始化完成")
```

- [ ] **Step 5: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_orchestrator_factories.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 7 PASS

- [ ] **Step 6: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 7: 验证 orchestrator/__init__.py 行数**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; (Get-Content src/orchestrator/__init__.py | Measure-Object -Line).Lines
```
Expected: `<350`

- [ ] **Step 8: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: simplify Orchestrator.__init__ with factory functions (Task 12)"
```

---

# Phase 4：提取 ReactLoop 核心方法

目标：react_loop.py 从 1826 行降到 <500 行。

## Task 13: 提取 run_stream() 到 StreamRunner

**Files:**
- Create: `src/agent/stream_runner.py`
- Modify: `src/agent/react_loop.py`（删除 `run_stream`，添加 thin facade）

**Interfaces:**
- Consumes: 19 个 self 属性（`max_loops`, `tool_registry`, `llm_client`, `_info_count`, `metrics`, `guardrail_engine`, `approval_manager`, `_stash_interrupt_notice`, `_drop_trailing_orphan_tool_calls`, `_build_stuck_message`, `_compute_params_hash`, `_detect_tool_stuck`, `_evaluate_policy`, `_execute_tool_with_dispatch`, `_maybe_trigger_agent_failure_signal`, `_log_audit`, `_generate_max_loops_summary`, `_check_user_failure_feedback`, `_build_done_event`）
- Produces: `StreamRunner.run_stream(user_input, history, system, session_id, tools_override, cancel_event, reasoning_cfg, is_cron) -> AsyncGenerator`

- [ ] **Step 1: 写失败测试**

Create `tests/test_stream_runner.py`:
```python
"""Task 13: 验证 StreamRunner 类可独立导入。"""
import inspect
import sys

sys.path.insert(0, "src")


def test_stream_runner_class_exists():
    """StreamRunner 类存在。"""
    from agent.stream_runner import StreamRunner
    assert StreamRunner is not None


def test_run_stream_is_async_generator():
    """run_stream 是 async generator 函数。"""
    from agent.stream_runner import StreamRunner
    assert inspect.isasyncgenfunction(StreamRunner.run_stream)


def test_react_loop_delegates_run_stream():
    """ReactLoop.run_stream 委托到 StreamRunner（保持向后兼容）。"""
    from agent.react_loop import ReactLoop
    assert hasattr(ReactLoop, "run_stream")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_stream_runner.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'agent.stream_runner'`

- [ ] **Step 3: 创建 src/agent/stream_runner.py**

Read `src/agent/react_loop.py` 中 `run_stream` 方法（L1057-1870，约 814 行），提取为独立类。

Create `src/agent/stream_runner.py`（骨架）:
```python
"""流式 React 循环执行器。

从 ReactLoop.run_stream 提取，负责流式 SSE 事件生成：
LLM 流式响应 → 工具调用 → 中断处理 → done 事件构造。
"""
from __future__ import annotations

import logging
import threading
from typing import TYPE_CHECKING, Any, Dict, List, Optional

if TYPE_CHECKING:
    from ..llm.reasoning_profiles import ReasoningConfig
    from .react_loop import ReactLoop

logger = logging.getLogger(__name__)


class StreamRunner:
    """流式 React 循环执行器。

    持有 ReactLoop 引用，通过属性访问组件。
    """

    def __init__(self, loop: "ReactLoop") -> None:
        """初始化 StreamRunner。

        参数:
            loop: ReactLoop 实例，通过属性访问组件。
        """
        self.loop = loop

    async def run_stream(
        self,
        user_input: str,
        history: Optional[List[Dict[str, Any]]] = None,
        system: Optional[str] = None,
        session_id: Optional[str] = None,
        tools_override: Optional[List[Dict[str, Any]]] = None,
        cancel_event: Optional[threading.Event] = None,
        reasoning_cfg: Optional["ReasoningConfig"] = None,
        is_cron: bool = False,
    ):
        """执行流式 React 循环，yield SSE 事件。

        从原 ReactLoop.run_stream (L1057-1870) 迁移。
        所有 self.xxx 改为 self.loop.xxx。
        """
        # 从原 react_loop.py L1057-1870 复制完整实现
        # 替换 self.xxx → self.loop.xxx
        # self._build_done_event → self.loop._build_done_event
        # 其他私有方法同理
        ...
```

实现要点：将原 `run_stream` 方法体中所有 `self.xxx` 替换为 `self.loop.xxx`，保持业务逻辑不变。`_build_done_event` / `_build_reasoning_stats` 等辅助方法保留在 ReactLoop 上（通过 `self.loop._build_done_event` 调用）。

- [ ] **Step 4: 更新 react_loop.py**

Edit `src/agent/react_loop.py`:
1. 添加 import：`from .stream_runner import StreamRunner`
2. 在 `__init__` 末尾添加：`self._stream_runner = StreamRunner(self)`
3. 删除 `run_stream` 方法定义（L1057-1870）
4. 添加 thin facade：
```python
async def run_stream(
    self,
    user_input: str,
    history: Optional[List[Dict[str, Any]]] = None,
    system: Optional[str] = None,
    session_id: Optional[str] = None,
    tools_override: Optional[List[Dict[str, Any]]] = None,
    cancel_event: Optional[threading.Event] = None,
    reasoning_cfg: Optional["ReasoningConfig"] = None,
    is_cron: bool = False,
):
    """流式 React 循环（委托到 StreamRunner）。"""
    async for event in self._stream_runner.run_stream(
        user_input, history, system, session_id,
        tools_override, cancel_event, reasoning_cfg, is_cron,
    ):
        yield event
```

- [ ] **Step 5: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_stream_runner.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 3 PASS

- [ ] **Step 6: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 7: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract StreamRunner.run_stream from ReactLoop (Task 13)"
```

---

## Task 14: 提取 run() 到 SyncRunner

**Files:**
- Create: `src/agent/sync_runner.py`
- Modify: `src/agent/react_loop.py`（删除 `run`，添加 thin facade）

**Interfaces:**
- Produces: `SyncRunner.run(user_input, history, system, session_id, tools_override, cancel_event, reasoning_cfg, is_cron) -> Tuple[str, List, bool, str]`

- [ ] **Step 1: 写失败测试**

Create `tests/test_sync_runner.py`:
```python
"""Task 14: 验证 SyncRunner 类可独立导入。"""
import inspect
import sys

sys.path.insert(0, "src")


def test_sync_runner_class_exists():
    """SyncRunner 类存在。"""
    from agent.sync_runner import SyncRunner
    assert SyncRunner is not None


def test_run_is_coroutine():
    """run 方法是 async 函数。"""
    from agent.sync_runner import SyncRunner
    assert inspect.iscoroutinefunction(SyncRunner.run)


def test_react_loop_delegates_run():
    """ReactLoop.run 委托到 SyncRunner（保持向后兼容）。"""
    from agent.react_loop import ReactLoop
    assert hasattr(ReactLoop, "run")
```

- [ ] **Step 2: 运行测试验证失败**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_sync_runner.py -x --no-header -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'agent.sync_runner'`

- [ ] **Step 3: 创建 src/agent/sync_runner.py**

Read `src/agent/react_loop.py` 中 `run` 方法（L593-1055，约 463 行），提取为独立类。

Create `src/agent/sync_runner.py`（骨架）:
```python
"""同步 React 循环执行器。

从 ReactLoop.run 提取，负责非流式 React 循环执行：
LLM 调用 → 工具执行 → 卡死检测 → 终止判定。
"""
from __future__ import annotations

import logging
import threading
from typing import TYPE_CHECKING, Any, Dict, List, Optional, Tuple

if TYPE_CHECKING:
    from ..llm.reasoning_profiles import ReasoningConfig
    from .react_loop import ReactLoop

logger = logging.getLogger(__name__)


class SyncRunner:
    """同步 React 循环执行器。

    持有 ReactLoop 引用，通过属性访问组件。
    """

    def __init__(self, loop: "ReactLoop") -> None:
        """初始化 SyncRunner。

        参数:
            loop: ReactLoop 实例，通过属性访问组件。
        """
        self.loop = loop

    async def run(
        self,
        user_input: str,
        history: Optional[List[Dict[str, Any]]] = None,
        system: Optional[str] = None,
        session_id: Optional[str] = None,
        tools_override: Optional[List[Dict[str, Any]]] = None,
        cancel_event: Optional[threading.Event] = None,
        reasoning_cfg: Optional["ReasoningConfig"] = None,
        is_cron: bool = False,
    ) -> Tuple[str, List[Dict[str, Any]], bool, str]:
        """执行同步 React 循环，返回 (response, messages, is_complete, reason)。

        从原 ReactLoop.run (L593-1055) 迁移。
        所有 self.xxx 改为 self.loop.xxx。
        """
        # 从原 react_loop.py L593-1055 复制完整实现
        # 替换 self.xxx → self.loop.xxx
        ...
```

- [ ] **Step 4: 更新 react_loop.py**

Edit `src/agent/react_loop.py`:
1. 添加 import：`from .sync_runner import SyncRunner`
2. 在 `__init__` 末尾添加：`self._sync_runner = SyncRunner(self)`
3. 删除 `run` 方法定义（L593-1055）
4. 添加 thin facade：
```python
async def run(
    self,
    user_input: str,
    history: Optional[List[Dict[str, Any]]] = None,
    system: Optional[str] = None,
    session_id: Optional[str] = None,
    tools_override: Optional[List[Dict[str, Any]]] = None,
    cancel_event: Optional[threading.Event] = None,
    reasoning_cfg: Optional["ReasoningConfig"] = None,
    is_cron: bool = False,
) -> Tuple[str, List[Dict[str, Any]], bool, str]:
    """同步 React 循环（委托到 SyncRunner）。"""
    return await self._sync_runner.run(
        user_input, history, system, session_id,
        tools_override, cancel_event, reasoning_cfg, is_cron,
    )
```

- [ ] **Step 5: 运行测试验证通过**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_sync_runner.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 3 PASS

- [ ] **Step 6: 运行全量测试验证无回归**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（无新增 failure）

- [ ] **Step 7: 验证 react_loop.py 行数**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; (Get-Content src/agent/react_loop.py | Measure-Object -Line).Lines
```
Expected: `<500`

- [ ] **Step 8: 提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: extract SyncRunner.run from ReactLoop (Task 14)"
```

---

# Phase 5：最终验证

## Task 15: 全量验证行数目标与测试稳定性

**Files:**
- 无文件修改，仅验证

- [ ] **Step 1: 验证所有行数目标**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; Write-Output "=== 行数验证 ==="; Write-Output ("server.py: " + (Get-Content src/server.py | Measure-Object -Line).Lines); Write-Output ("orchestrator/__init__.py: " + (Get-Content src/orchestrator/__init__.py | Measure-Object -Line).Lines); Write-Output ("react_loop.py: " + (Get-Content src/agent/react_loop.py | Measure-Object -Line).Lines); Write-Output ("builtin_tools.py exists: " + (Test-Path src/agent/builtin_tools.py))
```
Expected:
- server.py < 100
- orchestrator/__init__.py < 350
- react_loop.py < 500
- builtin_tools.py 不存在

- [ ] **Step 2: 运行全量测试套件**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: `127 failed, 2591 passed, 3 skipped`（0 新增 failure）

- [ ] **Step 3: 运行热重载测试**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -m pytest tests/test_container.py tests/test_hot_reload.py --no-header -q 2>&1 | Select-String "passed|failed"
```
Expected: 36/36 PASS

- [ ] **Step 4: 验证新模块可独立导入**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -c "
import sys
sys.path.insert(0, 'src')
modules = [
    'logging_setup', 'config_helpers', 'background_loops', 'lifespan',
    'orchestrator', 'orchestrator.enhanced_context', 'orchestrator.chat_handler',
    'orchestrator.factories',
    'agent.react_loop', 'agent.stream_runner', 'agent.sync_runner',
    'agent.tools',
]
for m in modules:
    try:
        __import__(m)
        print(f'OK: {m}')
    except Exception as e:
        print(f'FAIL: {m} - {e}')
"
```
Expected: 全部 OK

- [ ] **Step 5: 验证应用可正常启动（导入 app 不报错）**

Run:
```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; python -c "import sys; sys.path.insert(0, 'src'); from app import app; print(f'FastAPI app: {app.title}')"
```
Expected: `FastAPI app: Hermes Lite`

- [ ] **Step 6: 生成最终报告**

Write `docs/plans/2026-07-12-架构重构收尾-续-报告.md`:
```markdown
# 架构重构收尾（续）— 最终报告

## 行数达成

| 文件 | 起始 | 目标 | 最终 | 状态 |
|------|------|------|------|------|
| server.py | 1594 | <100 | <填入> | ✅/❌ |
| orchestrator.py | 1928 | <350 | <填入> | ✅/❌ |
| react_loop.py | 1826 | <500 | <填入> | ✅/❌ |
| builtin_tools.py | 22 | 0 | 0 | ✅ |

## 测试结果

- 基线：127 failed, 2591 passed, 3 skipped
- 最终：127 failed, <填入> passed, 3 skipped
- 新增 failure：<填入>

## 提交记录

- Task 1: delete builtin_tools.py shim
- Task 2: extract logging_setup.py
- ...
- Task 14: extract SyncRunner.run
```

- [ ] **Step 7: 最终提交**

```powershell
cd e:\Java\webser\web_app\webme\hermes-lite; git add -A; git commit -m "refactor: final verification report (Task 15)"
```

---

## 风险与缓解

### 风险 1：orchestrator.py 转 orchestrator/ 包后导入路径变化
**缓解**：Python 自动从 `orchestrator/__init__.py` 导入，所有 `from orchestrator import Orchestrator` 保持兼容。Task 9 中通过 `Move-Item` 而非手动复制完成转换。

### 风险 2：react_loop.py 提取 run/run_stream 后，测试中 MockStreamingReactLoop 可能 missing 方法
**缓解**：thin facade 模式保持 `ReactLoop.run` / `ReactLoop.run_stream` 方法签名不变；Mock 类继承 ReactLoop 后自动获得 facade 方法。测试中如 Mock 直接覆盖 `run` / `run_stream`，行为不变。

### 风险 3：lifespan.py 提取后，background_loops 中 `import server` 循环依赖
**缓解**：Task 4 中已改用 `import lifespan` 替代 `import server`，lifespan 模块不 import background_loops（仅 lifespan 函数体内部 spawn task 时引用），无循环。

### 风险 4：ChatHandler 持有 Orchestrator 引用可能导致循环 GC 问题
**缓解**：ChatHandler 与 EnhancedContextBuilder 持有的是强引用，但 Orchestrator 也持有它们的强引用。Python 的循环 GC 能正确处理这种「父子双向引用」模式（与 ReactLoop 持有 orchestrator_ref 相同）。

### 风险 5：测试中大量 `orch._private_method()` 调用需要逐个更新
**缓解**：每个 Task 6-8 使用 Grep 找到所有调用方，批量替换。每个 Task 末尾运行全量测试验证 0 新增 failure。

---

## 执行顺序与依赖

```
Phase 1（基础设施，独立）:
  Task 1 (delete shim) ─┐
  Task 2 (logging)      ├─→ Task 5 (lifespan + server.py <100)
  Task 3 (config)       │
  Task 4 (bg_loops)    ─┘

Phase 2（委托移除，依赖 Phase 1 完成）:
  Task 6 (SessionMgr/ContextBuilder delegates)
  Task 7 (SkillMgr/MsgPersistence delegates)
  Task 8 (CronIsolator delegates)

Phase 3（Orchestrator 核心提取，依赖 Phase 2 完成）:
  Task 9 (EnhancedContextBuilder) ─→ Task 10 (ChatHandler.chat) ─→ Task 11 (ChatHandler.chat_stream) ─→ Task 12 (factories)

Phase 4（ReactLoop 核心提取，独立于 Phase 3）:
  Task 13 (StreamRunner) ─→ Task 14 (SyncRunner)

Phase 5（验证）:
  Task 15 (final verification)
```

Task 1-4 可并行（独立模块提取），Task 13-14 可与 Phase 3 并行。但建议按顺序执行以降低风险。

---

## Self-Review 检查清单

完成所有 Task 后，执行以下检查：

- [ ] **Spec coverage**: 原始 4 个目标（server.py <100, orchestrator.py <350, react_loop.py <500, builtin_tools.py deleted）均有对应 Task
- [ ] **Placeholder scan**: 无 "TBD" / "TODO" / "implement later" 等占位符
- [ ] **Type consistency**: `EnhancedContextBuilder.build` 在 Task 9 定义后在 Task 10/11 中调用方一致；`ChatHandler.chat` / `chat_stream` 在 Task 10/11 定义后在 Task 12 中通过 `self.chat_handler` 引用一致；`StreamRunner.run_stream` / `SyncRunner.run` 在 Task 13/14 定义后在 ReactLoop facade 中引用一致
- [ ] **测试覆盖**: 每个 Task 均有失败测试 → 通过测试 → 全量回归验证
- [ ] **提交粒度**: 每个 Task 独立提交，提交信息格式统一
