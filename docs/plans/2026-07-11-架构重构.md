# teage-liu 架构重构实现计划

> **For agentic workers:** Use the java-dev-skills workflow to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 teage-liu 从 5 个巨型文件(>1900 行)重构为模块化单体 + DI 容器架构,支持所有配置选项热重载。

**Architecture:** DI 容器管理组件生命周期和热重载;Orchestrator 瘦化为纯协调器(<300 行),5 个子管理器独立注册;server.py 拆为 routes/ + schemas/;热重载通过原子性重建 + 延迟关闭实现。

**Tech Stack:** Python 3.11+, FastAPI, Pydantic, pytest, asyncio

## Global Constraints

- API 契约兼容:65 个端点的请求/响应格式不变,前端无需改动
- config.yaml 格式兼容:${ENV_VAR} 占位符机制不变
- 数据文件兼容:chroma/sqlite/history_buffer/schedules 格式不变
- 测试框架:pytest,测试通过 `sys.path.insert(0, "src")` 导入
- 每个阶段完成后运行 `python -m pytest tests/ -x -q` 确保无回归
- 每个子步骤完成后提交一次,提交信息格式: `refactor: <阶段描述>`

---

## File Structure

### 新建文件

```
src/
├── container.py              # DI 容器(~300 行)
├── app.py                    # FastAPI 应用入口 + lifespan(~80 行)
├── schemas/                  # Pydantic 模型(从 server.py 提取)
│   ├── __init__.py
│   ├── chat.py               # ChatRequest, ChatResponse, ChatStreamChunk...
│   ├── config.py             # ConfigResponse, ConfigUpdateResponse
│   ├── schedules.py          # ScheduleRequest, ScheduleResponse...
│   ├── approvals.py          # ApprovalResolveRequest, ApprovalListResponse...
│   ├── proposals.py          # Proposal 相关模型
│   ├── memory.py             # Memory 相关模型
│   ├── files.py              # FileUploadResponse, FileListResponse...
│   └── common.py             # 通用响应模型
├── routes/                   # 路由层(从 server.py 拆出)
│   ├── __init__.py
│   ├── chat.py               # /chat, /chat/stream, /chat/cancel
│   ├── config.py             # /config GET/PUT + 热重载
│   ├── schedules.py          # /schedules CRUD + history + audit + memories
│   ├── approvals.py          # /approvals
│   ├── proposals.py          # /proposals
│   ├── memory.py             # /memories, /profile
│   ├── health.py             # /health, /metrics, /signals
│   ├── files.py              # /files/*
│   ├── skills.py             # /skills/*
│   ├── cron_tools.py         # /cron_tools/*
│   ├── sessions.py           # /sessions/*
│   └── misc.py               # /tools, /audit, /reasoning, /recall, /restart, /
├── agent/
│   ├── session_manager.py    # ~200 行
│   ├── context_builder.py    # ~300 行
│   ├── skill_manager.py      # ~150 行
│   ├── cron_isolator.py      # ~200 行
│   ├── msg_persistence.py    # ~250 行
│   ├── tool_executor.py      # 从 react_loop.py 提取
│   ├── stream_handler.py     # 从 react_loop.py 提取
│   └── tools/                # 从 builtin_tools.py 拆出
│       ├── __init__.py
│       ├── file_tools.py
│       ├── shell_tools.py
│       ├── web_tools.py
│       ├── memory_tools.py
│       └── plan_tools.py
└── errors.py                 # 统一异常体系
```

### 修改文件

```
src/orchestrator.py           # 2528 行 → <300 行(瘦化)
src/agent/react_loop.py       # 1992 行 → <500 行(提取 ToolExecutor/StreamHandler)
src/config.py                 # 增加热重载分类标记 + 敏感字段处理
src/server.py                 # 最终删除或改为仅 `from app import app`
```

### 删除文件

```
src/agent/builtin_tools_fixed.py  # 1652 行死代码
```

---

## Task 1: 创建 DI 容器骨架

**Files:**
- Create: `src/container.py`
- Create: `src/errors.py`
- Test: `tests/test_container.py`

**Interfaces:**
- Produces: `Container` 类(register/get/reload/validate/close), `ContainerConfigError`, `ConfigReloadError`, `ComponentRef`

- [ ] **Step 1: 写 container.py 的失败测试**

```python
# tests/test_container.py
"""DI 容器测试:注册/获取/热重载/级联/循环检测/原子性回滚。"""
import sys, os, threading, time
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from container import Container, ContainerConfigError, ConfigReloadError, ComponentRef


class FakeLLM:
    def __init__(self, config):
        self.config = config
        self.closed = False
    def close(self):
        self.closed = True

class FakeOrchestrator:
    def __init__(self, llm_client):
        self.llm_client = llm_client
        self.closed = False
    def close(self):
        self.closed = True


class TestContainerBasics:
    def test_register_and_get(self):
        c = Container({"llm": {"model": "gpt-4"}})
        c.register("llm_client", lambda c: FakeLLM(c.config["llm"]), deps=[], hot_reloadable=True)
        client = c.get("llm_client")
        assert client.config == {"model": "gpt-4"}

    def test_singleton(self):
        c = Container({})
        c.register("llm_client", lambda c: FakeLLM({}), deps=[], hot_reloadable=True)
        assert c.get("llm_client") is c.get("llm_client")

    def test_dependency_injection(self):
        c = Container({})
        c.register("llm_client", lambda c: FakeLLM({}), deps=[], hot_reloadable=True)
        c.register("orchestrator", lambda c: FakeOrchestrator(c.get("llm_client")),
                   deps=["llm_client"], hot_reloadable=False)
        orch = c.get("orchestrator")
        assert orch.llm_client is c.get("llm_client")


class TestValidation:
    def test_cycle_detection(self):
        c = Container({})
        c.register("a", lambda c: None, deps=["b"])
        c.register("b", lambda c: None, deps=["a"])
        with pytest.raises(ContainerConfigError, match="循环依赖"):
            c.validate()

    def test_missing_dep(self):
        c = Container({})
        c.register("a", lambda c: None, deps=["nonexistent"])
        with pytest.raises(ContainerConfigError, match="未注册"):
            c.validate()


class TestHotReload:
    def test_reload_rebuilds_component(self):
        c = Container({"llm": {"model": "v1"}})
        c.register("llm_client", lambda c: FakeLLM(c.config["llm"]),
                   deps=[], hot_reloadable=True)
        c.register("orchestrator", lambda c: FakeOrchestrator(c.get("llm_client")),
                   deps=["llm_client"], hot_reloadable=False)
        old_orch = c.get("orchestrator")
        old_llm = c.get("llm_client")

        c.reload({"llm"}, {"llm": {"model": "v2"}})

        new_orch = c.get("orchestrator")
        new_llm = c.get("llm_client")
        assert new_llm is not old_llm
        assert new_orch is not old_orch
        assert new_llm.config == {"model": "v2"}

    def test_reload_atomic_rollback(self):
        class FailingLLM:
            def __init__(self, config): self.config = config
            def close(self): pass

        fail_count = [0]
        def factory(c):
            if fail_count[0] == 0:
                fail_count[0] += 1
                raise RuntimeError("创建失败")
            return FailingLLM(c.config["llm"])

        c = Container({"llm": {"model": "v1"}})
        c.register("llm_client", lambda c: FakeLLM(c.config["llm"]),
                   deps=[], hot_reloadable=True)
        original = c.get("llm_client")

        # 替换 factory 为会失败的版本
        c._factories["llm_client"] = factory
        with pytest.raises(ConfigReloadError):
            c.reload({"llm"}, {"llm": {"model": "v2"}})

        # 旧实例不变
        assert c.get("llm_client") is original
        assert c.config["llm"] == {"model": "v1"}

    def test_delayed_close(self):
        c = Container({"llm": {"model": "v1"}, "server": {"hot_reload_grace_period": 0}})
        c.register("llm_client", lambda c: FakeLLM(c.config["llm"]),
                   deps=[], hot_reloadable=True)
        old = c.get("llm_client")

        c.reload({"llm"}, {"llm": {"model": "v2"}, "server": {"hot_reload_grace_period": 0}})
        # grace period=0,等待短暂时间后旧实例应被关闭
        time.sleep(0.5)
        assert old.closed is True


class TestComponentRef:
    def test_ref_forwards_to_latest(self):
        c = Container({"llm": {"model": "v1"}})
        c.register("llm_client", lambda c: FakeLLM(c.config["llm"]),
                   deps=[], hot_reloadable=True)
        ref = ComponentRef(c, "llm_client")
        assert ref.config == {"model": "v1"}

        c.reload({"llm"}, {"llm": {"model": "v2"}})
        assert ref.config == {"model": "v2"}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_container.py -x -q`
Expected: FAIL (ModuleNotFoundError: No module named 'container')

- [ ] **Step 3: 实现 errors.py**

```python
# src/errors.py
"""统一异常体系。"""


class ToolError(Exception):
    """工具错误基类。"""

class ToolNotFoundError(ToolError):
    """工具不存在。"""

class ToolExecutionError(ToolError):
    """工具执行失败。"""

class ToolPermissionDenied(ToolError):
    """权限拒绝。"""

class ToolTimeoutError(ToolError):
    """工具超时。"""


class ConfigError(Exception):
    """配置错误基类。"""

class ConfigValidationError(ConfigError):
    """schema 校验失败(400)。"""

class ConfigReloadError(ConfigError):
    """热重载重建失败(500)。"""

class ContainerConfigError(ConfigError):
    """容器配置错误(循环依赖/未注册依赖)。"""
```

- [ ] **Step 4: 实现 container.py**

```python
# src/container.py
"""轻量 DI 容器:组件单例 + 工厂延迟创建 + 热重载原子性重建 + 延迟关闭。"""
from __future__ import annotations

import json
import logging
import threading
from typing import Any, Callable, Set

from errors import ContainerConfigError, ConfigReloadError

logger = logging.getLogger(__name__)

Factory = Callable[["Container"], Any]


def _get_grace_period(config: dict) -> int:
    """动态计算 CLOSE_GRACE_PERIOD。"""
    explicit = config.get("server", {}).get("hot_reload_grace_period")
    if explicit:
        return explicit
    llm_timeout = config.get("llm", {}).get("activity_timeout", 60)
    max_loops = config.get("tools", {}).get("max_loops", 10)
    return int(llm_timeout * max_loops)


CONFIG_TO_COMPONENTS: dict[str, list[str]] = {
    "llm":        ["llm_client"],
    "security":   ["policy_engine"],
    "storage":    ["chroma_store", "sqlite_log", "history_buffer"],
    "memory":     [],
    "tasks":      [],
    "guardrails": [],
    "server":     [],
}


class ComponentRef:
    """代理对象:__getattr__ 转发到容器中最新的组件实例。
    用于不参与级联重建的组件访问可重建组件,避免引用过期。"""

    def __init__(self, container: "Container", name: str):
        self._container = container
        self._name = name

    def __getattr__(self, attr: str) -> Any:
        return getattr(self._container.get(self._name), attr)


class Container:
    """轻量 DI 容器:组件单例 + 工厂延迟创建 + 热重载重建。"""

    def __init__(self, config: dict):
        self._config = config
        self._instances: dict[str, Any] = {}
        self._factories: dict[str, Factory] = {}
        self._deps: dict[str, list[str]] = {}
        self._hot_reloadable: dict[str, bool] = {}
        self._lock = threading.RLock()

    @property
    def config(self) -> dict:
        return self._config

    def register(self, name: str, factory: Factory, deps: list[str],
                 hot_reloadable: bool = False) -> None:
        with self._lock:
            self._factories[name] = factory
            self._deps[name] = deps
            self._hot_reloadable[name] = hot_reloadable

    def get(self, name: str) -> Any:
        with self._lock:
            if name not in self._factories:
                raise KeyError(f"组件 '{name}' 未注册")
            if name not in self._instances:
                self._instances[name] = self._factories[name](self)
            return self._instances[name]

    def validate(self) -> None:
        self._validate_no_cycles()
        self._validate_deps_registered()

    def _validate_no_cycles(self) -> None:
        WHITE, GRAY, BLACK = 0, 1, 2
        color = {name: WHITE for name in self._factories}

        def dfs(name: str, path: list[str]) -> None:
            if name not in color:
                return
            if color[name] == GRAY:
                cycle = " → ".join(path + [name])
                raise ContainerConfigError(f"检测到循环依赖: {cycle}")
            if color[name] == BLACK:
                return
            color[name] = GRAY
            for dep in self._deps.get(name, []):
                dfs(dep, path + [name])
            color[name] = BLACK

        for name in self._factories:
            dfs(name, [])

    def _validate_deps_registered(self) -> None:
        for name, deps in self._deps.items():
            for dep in deps:
                if dep not in self._factories:
                    raise ContainerConfigError(
                        f"组件 '{name}' 依赖未注册的组件 '{dep}'"
                    )

    def reload(self, changed_sections: set[str], new_config: dict) -> list[str]:
        """原子性重建:先创建全部新实例,全部成功后才替换,失败则回滚。"""
        with self._lock:
            directly_affected = set()
            for section in changed_sections:
                directly_affected.update(CONFIG_TO_COMPONENTS.get(section, []))

            to_rebuild = self._topo_sort_dependents(directly_affected)
            if not to_rebuild:
                self._config = new_config
                return []

            old_config = self._config

            # 阶段1:先创建所有新实例(不替换 _instances)
            new_instances = {}
            try:
                self._config = new_config
                for name in to_rebuild:
                    new_instances[name] = self._factories[name](self)
            except Exception as e:
                self._config = old_config
                for inst in new_instances.values():
                    if inst and hasattr(inst, "close"):
                        try:
                            inst.close()
                        except Exception:
                            pass
                raise ConfigReloadError(f"热重载重建失败: {e}") from e

            # 阶段2:全部成功,批量替换
            old_instances = {}
            for name in to_rebuild:
                old_instances[name] = self._instances.get(name)
                self._instances[name] = new_instances[name]

            # 阶段3:延迟关闭旧实例
            grace = _get_grace_period(self._config)
            for name, old in old_instances.items():
                if old and hasattr(old, "close"):
                    timer = threading.Timer(
                        grace,
                        lambda o=old, n=name: self._safe_close(o, n)
                    )
                    timer.daemon = True
                    timer.start()

            return to_rebuild

    def _topo_sort_dependents(self, directly_affected: set[str]) -> list[str]:
        """拓扑排序:返回所有需要重建的组件(直接受影响 + 级联依赖者)。"""
        # 构建反向依赖图:谁依赖我 → 我重建时谁也要重建
        reverse_deps: dict[str, list[str]] = {}
        for name, deps in self._deps.items():
            for dep in deps:
                reverse_deps.setdefault(dep, []).append(name)

        result: list[str] = []
        visited: set[str] = set()
        queue = list(directly_affected)

        while queue:
            name = queue.pop(0)
            if name in visited:
                continue
            visited.add(name)
            result.append(name)
            for dependent in reverse_deps.get(name, []):
                if dependent not in visited:
                    queue.append(dependent)

        # 拓扑排序:按依赖顺序排列(先重建被依赖的,后重建依赖者)
        ordered: list[str] = []
        temp_mark: set[str] = set()
        perm_mark: set[str] = set()

        def visit(n: str) -> None:
            if n in perm_mark:
                return
            if n in temp_mark:
                return  # 已处理
            temp_mark.add(n)
            for dep in self._deps.get(n, []):
                if dep in visited:
                    visit(dep)
            temp_mark.discard(n)
            perm_mark.add(n)
            ordered.append(n)

        for name in result:
            visit(name)

        return ordered

    @staticmethod
    def _safe_close(instance: Any, name: str) -> None:
        try:
            instance.close()
        except Exception as e:
            logger.warning("关闭组件 %s 失败: %s", name, e)

    def close(self) -> None:
        """关闭所有实例(逆序关闭)。"""
        with self._lock:
            for name in reversed(list(self._instances)):
                inst = self._instances.get(name)
                if inst and hasattr(inst, "close"):
                    self._safe_close(inst, name)
            self._instances.clear()


def detect_changed_sections(old: dict, new: dict) -> set[str]:
    """比较新旧 config 的顶层段,返回变更的段名集合。"""
    changed: set[str] = set()
    for key in set(old) | set(new):
        if json.dumps(old.get(key), sort_keys=True) != \
           json.dumps(new.get(key), sort_keys=True):
            changed.add(key)
    return changed
```

- [ ] **Step 5: 运行测试验证通过**

Run: `python -m pytest tests/test_container.py -x -q`
Expected: PASS (所有测试通过)

- [ ] **Step 6: 确保现有测试不回归**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS (现有测试全部通过,container.py 是新文件不影响现有代码)

- [ ] **Step 7: 提交**

```bash
cd e:\Java\webser\web_app\webme\teage-liu
git add src/container.py src/errors.py tests/test_container.py
git commit -m "refactor: 创建 DI 容器骨架(container.py + errors.py)"
```

---

## Task 2: 拆分 server.py → routes/ + schemas/ + app.py

**Files:**
- Create: `src/app.py`, `src/schemas/__init__.py`, `src/schemas/chat.py`, `src/schemas/config.py`, `src/schemas/schedules.py`, `src/schemas/approvals.py`, `src/schemas/proposals.py`, `src/schemas/memory.py`, `src/schemas/files.py`, `src/schemas/common.py`
- Create: `src/routes/__init__.py`, `src/routes/chat.py`, `src/routes/config.py`, `src/routes/schedules.py`, `src/routes/approvals.py`, `src/routes/proposals.py`, `src/routes/memory.py`, `src/routes/health.py`, `src/routes/files.py`, `src/routes/skills.py`, `src/routes/cron_tools.py`, `src/routes/sessions.py`, `src/routes/misc.py`
- Modify: `src/server.py` → 改为 `from app import app` 的薄包装(保持向后兼容)
- Test: `tests/test_server.py`(现有,调整 import)

**Interfaces:**
- Consumes: 现有 server.py 中的全部路由和 Pydantic 模型
- Produces: `app` (FastAPI 实例), `container` (Container 实例,app 启动时初始化)

**迁移策略:**

1. **提取 Pydantic 模型** → `schemas/`:将 server.py 中所有 `class XxxRequest(BaseModel)` / `class XxxResponse(BaseModel)` 按领域分组移到 schemas/ 文件。每个 schema 文件 `from pydantic import BaseModel` 开头。

2. **提取路由** → `routes/`:每个路由文件定义 `router = APIRouter()`,将 server.py 中对应的 `@app.post(...)` 改为 `@router.post(...)`。路由通过 `container.get("orchestrator")` 获取组件(过渡期用全局变量兼容)。

3. **创建 app.py** → 创建 FastAPI 实例,注册所有 router,lifespan 中初始化容器(过渡期:容器中注册旧 Orchestrator,通过 config_path 创建)。

4. **server.py 改为薄包装** → `from app import app` + `if __name__ == "__main__": uvicorn.run(app, ...)`。

- [ ] **Step 1: 提取 schemas/ — 写 import 测试**

```python
# tests/test_schemas_import.py
"""验证 schemas 模块可正确导入。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

def test_schemas_import():
    from schemas.chat import ChatRequest, ChatResponse
    from schemas.config import ConfigResponse, ConfigUpdateResponse
    from schemas.schedules import ScheduleListResponse, ScheduleResponse
    from schemas.approvals import ApprovalListResponse, ApprovalResolveResponse
    from schemas.files import FileUploadResponse, FileListResponse
    from schemas.common import DeleteSessionResponse
    assert ChatRequest is not None
```

- [ ] **Step 2: 运行验证失败**

Run: `python -m pytest tests/test_schemas_import.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 创建 schemas/ 文件**

从 server.py 中提取所有 Pydantic 模型,按领域分组:

```python
# src/schemas/__init__.py
"""Pydantic 模型,按领域分组。"""
```

```python
# src/schemas/chat.py
"""聊天相关 Pydantic 模型。从 server.py 提取。"""
from pydantic import BaseModel
from typing import Any, Optional

class ChatRequest(BaseModel):
    session_id: Optional[str] = None
    message: str
    # ... 从 server.py 复制完整字段

class ChatResponse(BaseModel):
    session_id: str
    content: str
    # ... 从 server.py 复制完整字段

# 从 server.py 复制所有 chat 相关模型
```

**操作指南:** 在 server.py 中搜索 `class.*BaseModel`,按领域分组复制到对应 schema 文件。每个模型完整复制,不修改字段。

- [ ] **Step 4: 运行 schema 测试通过**

Run: `python -m pytest tests/test_schemas_import.py -x -q`
Expected: PASS

- [ ] **Step 5: 创建 routes/ 文件 — 每个路由文件提取对应端点**

```python
# src/routes/__init__.py
"""路由模块。"""
```

```python
# src/routes/chat.py
"""聊天路由。从 server.py 提取 /chat, /chat/stream, /chat/cancel。"""
from fastapi import APIRouter
router = APIRouter()

# 从 server.py 移动以下端点(保持函数体不变):
# @app.post("/chat") → @router.post("/chat")
# @app.post("/chat/stream") → @router.post("/chat/stream")
# @app.post("/chat/cancel") → @router.post("/chat/cancel")
# 函数体中的全局变量引用(orchestrator, session_logger 等)保持不变
```

**操作指南:** 对每个路由文件:
1. 在 server.py 中找到对应端点
2. 将 `@app.xxx` 改为 `@router.xxx`
3. 复制函数体(保持不变)
4. 导入 schemas 替代内联模型引用

路由文件对照表:

| routes/ 文件 | server.py 行范围 | 端点 |
|---|---|---|
| chat.py | L1532-L1847 | /chat, /chat/stream, /chat/cancel |
| config.py | L4180-L4298 | /config GET/PUT |
| schedules.py | L2156-L2520 | /schedules/* |
| approvals.py | L2110-L2156 | /approvals/* |
| proposals.py | L2520-L2747 | /proposals/* |
| memory.py | L4298-L4452 | /memories, /profile |
| health.py | L1958-L2068 | /health, /metrics/*, /signals |
| files.py | L3357-L3610 | /files/*, /sessions/*/files |
| skills.py | L2948-L3206 | /skills/* |
| cron_tools.py | L2747-L2948 | /cron_tools/* |
| sessions.py | L3206-L3357 | /sessions/* |
| misc.py | L1847-L1958, L2068-L2110, L3610-L4180, L4452-L4504 | /consolidation, /reasoning, /tools, /audit, /recall, /restart, / |

- [ ] **Step 6: 创建 app.py**

```python
# src/app.py
"""FastAPI 应用入口。创建 app、注册路由、lifespan 初始化。"""
from __future__ import annotations

import logging
from contextlib import asynccontextmanager

from fastapi import FastAPI

logger = logging.getLogger(__name__)


@asynccontextmanager
async def lifespan(app: FastAPI):
    """应用生命周期:启动时初始化组件,关闭时清理。"""
    # 过渡期:使用旧 Orchestrator(后续阶段替换为容器)
    from orchestrator import Orchestrator
    from stream_manager import StreamManager
    from agent.audit import AuditLogger

    config_path = "config.yaml"
    app.state.orchestrator = Orchestrator(config_path=config_path)
    app.state.stream_manager = StreamManager(app.state.orchestrator)
    app.state.session_logger = app.state.orchestrator

    yield

    # 关闭
    if hasattr(app.state, "orchestrator"):
        app.state.orchestrator.close()


def create_app() -> FastAPI:
    app = FastAPI(lifespan=lifespan)

    # 注册路由
    from routes import chat, config, schedules, approvals, proposals
    from routes import memory, health, files, skills, cron_tools, sessions, misc

    app.include_router(chat.router)
    app.include_router(config.router)
    app.include_router(schedules.router)
    app.include_router(approvals.router)
    app.include_router(proposals.router)
    app.include_router(memory.router)
    app.include_router(health.router)
    app.include_router(files.router)
    app.include_router(skills.router)
    app.include_router(cron_tools.router)
    app.include_router(sessions.router)
    app.include_router(misc.router)

    return app


app = create_app()
```

**注意:** 路由文件中引用的全局变量(`orchestrator`, `session_logger`, `stream_manager`)需要改为从 `request.app.state` 获取。过渡期可以用模块级全局变量兼容:

```python
# routes/chat.py 顶部
_orchestrator = None
_session_logger = None
_stream_manager = None

def init_deps(orchestrator, session_logger, stream_manager):
    global _orchestrator, _session_logger, _stream_manager
    _orchestrator = orchestrator
    _session_logger = session_logger
    _stream_manager = stream_manager
```

在 app.py 的 lifespan 中调用每个路由模块的 `init_deps()`。

- [ ] **Step 7: 修改 server.py 为薄包装**

```python
# src/server.py
"""向后兼容入口。实际应用在 app.py 中。"""
from app import app

if __name__ == "__main__":
    import uvicorn
    uvicorn.Server.config = uvicorn.Config(app, host="0.0.0.0", port=18394)
    server = uvicorn.Server(uvicorn.Config(app, host="0.0.0.0", port=18394))
    server.run()
```

- [ ] **Step 8: 调整 test_server.py 的 import**

```python
# tests/test_server.py 中
# 旧: patch("server.orchestrator")
# 新: patch("routes.chat._orchestrator") 或保持 patch("server.orchestrator") 通过兼容层
```

**操作指南:** 在 server.py 中保留 `orchestrator = None` 全局变量作为兼容层,app.py lifespan 中同步设置。现有测试的 `patch("server.orchestrator")` 继续工作。

- [ ] **Step 9: 运行全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS (所有现有测试通过,可能需要调整部分 import)

- [ ] **Step 10: 提交**

```bash
git add src/app.py src/schemas/ src/routes/ src/server.py tests/
git commit -m "refactor: 拆分 server.py → routes/ + schemas/ + app.py"
```

---

## Task 3a: 拆出 SessionManager

**Files:**
- Create: `src/agent/session_manager.py`
- Modify: `src/orchestrator.py` — 提取会话相关方法
- Test: `tests/test_session_manager.py`

**Interfaces:**
- Consumes: `orchestrator.py` 中的 `_ensure_session`, `_maybe_generate_title_async`, `_generate_title_task` 方法
- Produces: `SessionManager` 类,方法: `ensure_session(session_id) -> Session`, `generate_title_async(session_id, first_message)`, `get_session(session_id) -> Session`

- [ ] **Step 1: 写 SessionManager 的失败测试**

```python
# tests/test_session_manager.py
"""SessionManager 测试:会话创建、标题生成。"""
import sys, os, asyncio
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from unittest.mock import MagicMock, AsyncMock
from agent.session_manager import SessionManager


class TestSessionManager:
    def test_ensure_session_creates_new(self):
        mgr = SessionManager()
        session = mgr.ensure_session("test-1")
        assert session.session_id == "test-1"

    def test_ensure_session_returns_existing(self):
        mgr = SessionManager()
        session1 = mgr.ensure_session("test-1")
        session2 = mgr.ensure_session("test-1")
        assert session1 is session2

    def test_generate_title_async(self):
        mgr = SessionManager(llm_client=MagicMock())
        mgr.llm_client.generate_title = AsyncMock(return_value="测试标题")
        # 标题生成是异步的,不阻塞 ensure_session
        mgr.generate_title_async("test-1", "你好")
        # 不立即有标题,异步生成中
        assert "test-1" in mgr._sessions
```

- [ ] **Step 2: 运行验证失败**

Run: `python -m pytest tests/test_session_manager.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 实现 SessionManager**

从 orchestrator.py 提取以下方法到 SessionManager:
- `_ensure_session` (L1750) → `ensure_session`
- `_maybe_generate_title_async` (L1672) → `generate_title_async`
- `_generate_title_task` (L1707) → `_generate_title_task`
- 会话状态相关字段(`self.sessions` 等)

```python
# src/agent/session_manager.py
"""会话管理:创建/获取会话、异步标题生成。"""
from __future__ import annotations

import logging
from typing import Any, Dict, Optional

logger = logging.getLogger(__name__)


class SessionManager:
    """管理会话生命周期。"""

    def __init__(self, llm_client=None) -> None:
        self._sessions: Dict[str, Any] = {}
        self._llm_client = llm_client

    def ensure_session(self, session_id: str) -> Any:
        """获取或创建会话。"""
        # 从 orchestrator.py _ensure_session (L1750) 移动代码
        if session_id not in self._sessions:
            self._sessions[session_id] = self._create_session(session_id)
        return self._sessions[session_id]

    def _create_session(self, session_id: str) -> Any:
        """创建新会话对象。"""
        # 从 orchestrator.py 复制会话创建逻辑
        ...

    def generate_title_async(self, session_id: str, first_message: str) -> None:
        """异步生成会话标题。"""
        # 从 orchestrator.py _maybe_generate_title_async (L1672) 移动代码
        ...

    async def _generate_title_task(self, session_id: str, first_message: str) -> None:
        """标题生成后台任务。"""
        # 从 orchestrator.py _generate_title_task (L1707) 移动代码
        ...

    def get_session(self, session_id: str) -> Optional[Any]:
        return self._sessions.get(session_id)

    def list_sessions(self) -> list[str]:
        return list(self._sessions.keys())

    def delete_session(self, session_id: str) -> bool:
        if session_id in self._sessions:
            del self._sessions[session_id]
            return True
        return False
```

- [ ] **Step 4: 修改 Orchestrator 委托给 SessionManager**

在 orchestrator.py 的 `__init__` 中创建 SessionManager,原方法改为委托:

```python
# orchestrator.py __init__ 中添加:
self.session_mgr = SessionManager(llm_client=self.llm_client)

# 原 _ensure_session 改为:
def _ensure_session(self, session_id: str) -> None:
    return self.session_mgr.ensure_session(session_id)
```

- [ ] **Step 5: 运行测试通过**

Run: `python -m pytest tests/test_session_manager.py tests/test_orchestrator_title.py -x -q`
Expected: PASS

- [ ] **Step 6: 运行全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add src/agent/session_manager.py src/orchestrator.py tests/test_session_manager.py
git commit -m "refactor: 拆出 SessionManager(阶段 3a)"
```

---

## Task 3b: 拆出 SkillManager

**Files:**
- Create: `src/agent/skill_manager.py`
- Modify: `src/orchestrator.py` — 提取技能相关方法
- Test: `tests/test_skill_manager.py`

**Interfaces:**
- Consumes: orchestrator.py 中的 `activate_skill`, `deactivate_skill`, `_build_active_skills_section`
- Produces: `SkillManager` 类,方法: `activate(skill_name, session_id)`, `deactivate(skill_name, session_id)`, `build_active_section(session_id) -> str`, `get_active_skills(session_id) -> list[str]`

- [ ] **Step 1: 写 SkillManager 的失败测试**

```python
# tests/test_skill_manager.py
"""SkillManager 测试:技能激活/停用。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from agent.skill_manager import SkillManager


class TestSkillManager:
    def test_activate_skill(self):
        mgr = SkillManager()
        mgr.activate("test-skill", "session-1")
        assert "test-skill" in mgr.get_active_skills("session-1")

    def test_deactivate_skill(self):
        mgr = SkillManager()
        mgr.activate("test-skill", "session-1")
        mgr.deactivate("test-skill", "session-1")
        assert "test-skill" not in mgr.get_active_skills("session-1")

    def test_isolation_between_sessions(self):
        mgr = SkillManager()
        mgr.activate("test-skill", "session-1")
        assert "test-skill" not in mgr.get_active_skills("session-2")
```

- [ ] **Step 2: 运行验证失败**

Run: `python -m pytest tests/test_skill_manager.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 实现 SkillManager**

从 orchestrator.py 提取:
- `activate_skill` (L1950) → `activate`
- `deactivate_skill` (L1964) → `deactivate`
- `_build_active_skills_section` (L1976) → `build_active_section`
- 技能状态字段(`self.active_skills` 等)

```python
# src/agent/skill_manager.py
"""技能管理:激活/停用/构建技能上下文。"""
from __future__ import annotations

import logging
from typing import Dict, List, Set

logger = logging.getLogger(__name__)


class SkillManager:
    """管理技能激活状态(per-session 隔离)。"""

    def __init__(self) -> None:
        self._active_skills: Dict[str, Set[str]] = {}

    def activate(self, skill_name: str, session_id: str = "default") -> None:
        if session_id not in self._active_skills:
            self._active_skills[session_id] = set()
        self._active_skills[session_id].add(skill_name)

    def deactivate(self, skill_name: str, session_id: str = "default") -> None:
        if session_id in self._active_skills:
            self._active_skills[session_id].discard(skill_name)

    def get_active_skills(self, session_id: str = "default") -> List[str]:
        return list(self._active_skills.get(session_id, set()))

    def build_active_section(self, session_id: str) -> str:
        """构建激活技能的 system prompt 段落。"""
        # 从 orchestrator.py _build_active_skills_section (L1976) 移动代码
        ...

    def clear_session(self, session_id: str) -> None:
        self._active_skills.pop(session_id, None)
```

- [ ] **Step 4: 修改 Orchestrator 委托**

```python
# orchestrator.py
self.skill_mgr = SkillManager()

def activate_skill(self, skill_name: str, session_id: str = "default") -> None:
    self.skill_mgr.activate(skill_name, session_id)

def deactivate_skill(self, skill_name: str, session_id: str = "default") -> None:
    self.skill_mgr.deactivate(skill_name, session_id)
```

- [ ] **Step 5: 运行测试**

Run: `python -m pytest tests/test_skill_manager.py tests/test_skill_tools.py tests/test_skill_self_extension.py -x -q`
Expected: PASS

- [ ] **Step 6: 全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add src/agent/skill_manager.py src/orchestrator.py tests/test_skill_manager.py
git commit -m "refactor: 拆出 SkillManager(阶段 3b)"
```

---

## Task 3c: 拆出 CronIsolator

**Files:**
- Create: `src/agent/cron_isolator.py`
- Modify: `src/orchestrator.py` — 提取 cron 隔离相关方法
- Test: `tests/test_cron_isolator.py` (补充,现有 `tests/test_cron_isolation.py` 可复用)

**Interfaces:**
- Consumes: orchestrator.py 中的 `_build_cron_isolation`, `_build_cron_enhanced_context`, `_build_cron_tools`, `set_cron_dependencies`
- Produces: `CronIsolator` 类,方法: `build_isolation(session) -> dict`, `build_enhanced_context(session, message) -> str`, `build_cron_tools(session) -> list`, `set_dependencies(...)`

- [ ] **Step 1: 写 CronIsolator 的失败测试**

```python
# tests/test_cron_isolator.py
"""CronIsolator 测试:cron 上下文隔离。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from agent.cron_isolator import CronIsolator


class TestCronIsolator:
    def test_build_isolation_returns_dict(self):
        iso = CronIsolator(tool_registry=None)
        result = iso.build_isolation(session_id="cron:test-1")
        assert isinstance(result, dict)
        assert "session_id" in result or "cron_session_id" in result

    def test_build_cron_tools_returns_list(self):
        iso = CronIsolator(tool_registry=None)
        tools = iso.build_cron_tools(session_id="cron:test-1")
        assert isinstance(tools, list)
```

- [ ] **Step 2: 运行验证失败**

Run: `python -m pytest tests/test_cron_isolator.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 实现 CronIsolator**

从 orchestrator.py 提取:
- `_build_cron_isolation` (L2171) → `build_isolation`
- `_build_cron_enhanced_context` (L2193) → `build_enhanced_context`
- `_build_cron_tools` (L2307) → `build_cron_tools`
- `set_cron_dependencies` (L2416) → `set_dependencies`

```python
# src/agent/cron_isolator.py
"""Cron 上下文隔离:构建 cron 专用上下文和工具集。"""
from __future__ import annotations

import logging
from typing import Any, Dict, List, Optional

logger = logging.getLogger(__name__)


class CronIsolator:
    """cron 会话与用户会话隔离。"""

    def __init__(self, tool_registry=None) -> None:
        self._tool_registry = tool_registry
        self._cron_config: dict = {}

    def build_isolation(self, session_id: str) -> Dict[str, Any]:
        """构建 cron 隔离上下文。"""
        # 从 orchestrator.py _build_cron_isolation (L2171) 移动
        ...

    async def build_enhanced_context(self, session_id: str, message: str) -> str:
        """构建 cron 增强上下文。"""
        # 从 orchestrator.py _build_cron_enhanced_context (L2193) 移动
        ...

    def build_cron_tools(self, session_id: str) -> List[Any]:
        """构建 cron 工具集。"""
        # 从 orchestrator.py _build_cron_tools (L2307) 移动
        ...

    def set_dependencies(self, **kwargs) -> None:
        """设置 cron 依赖。"""
        # 从 orchestrator.py set_cron_dependencies (L2416) 移动
        ...
```

- [ ] **Step 4: 修改 Orchestrator 委托**

```python
# orchestrator.py
self.cron_isolator = CronIsolator(tool_registry=self.tool_registry)
```

- [ ] **Step 5: 运行测试**

Run: `python -m pytest tests/test_cron_isolator.py tests/test_cron_isolation.py tests/test_orchestrator_cron_context.py -x -q`
Expected: PASS

- [ ] **Step 6: 全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add src/agent/cron_isolator.py src/orchestrator.py tests/test_cron_isolator.py
git commit -m "refactor: 拆出 CronIsolator(阶段 3c)"
```

---

## Task 3d: 拆出 ContextBuilder

**Files:**
- Create: `src/agent/context_builder.py`
- Modify: `src/orchestrator.py` — 提取上下文构建方法
- Test: `tests/test_context_builder.py`

**Interfaces:**
- Consumes: SkillManager (3b), CronIsolator (3c), orchestrator.py 中的 `_build_environment_section`, `_build_enhanced_context`, TODO 注入相关方法
- Produces: `ContextBuilder` 类,方法: `build(session, message) -> str`, `build_environment() -> str`

- [ ] **Step 1: 写 ContextBuilder 的失败测试**

```python
# tests/test_context_builder.py
"""ContextBuilder 测试:system prompt 构建。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from agent.context_builder import ContextBuilder
from agent.skill_manager import SkillManager
from agent.cron_isolator import CronIsolator


class TestContextBuilder:
    def test_build_environment(self):
        cb = ContextBuilder(skill_mgr=SkillManager(), cron_isolator=CronIsolator())
        env = cb.build_environment()
        assert isinstance(env, str)
        assert len(env) > 0

    def test_build_returns_str(self):
        cb = ContextBuilder(skill_mgr=SkillManager(), cron_isolator=CronIsolator())
        result = cb.build(session_id="test-1", message="你好")
        assert isinstance(result, str)
```

- [ ] **Step 2: 运行验证失败**

Run: `python -m pytest tests/test_context_builder.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 实现 ContextBuilder**

从 orchestrator.py 提取:
- `_build_environment_section` (L1763) → `build_environment`
- `_build_enhanced_context` (L2004) → `build`
- `_format_todo_for_injection` (L1831) → `_format_todo`
- `_has_unfinished_steps` (L1885) → `_has_unfinished_steps`
- `_build_continuation_message` (L1906) → `_build_continuation_message`

```python
# src/agent/context_builder.py
"""上下文构建:环境信息 + 技能注入 + cron 隔离 + TODO 注入 → system prompt。"""
from __future__ import annotations

import logging
from typing import Any, Dict, Optional

logger = logging.getLogger(__name__)


class ContextBuilder:
    """构建 LLM system prompt。"""

    def __init__(self, skill_mgr, cron_isolator) -> None:
        self._skill_mgr = skill_mgr
        self._cron_isolator = cron_isolator

    def build_environment(self) -> str:
        """构建环境信息段落。"""
        # 从 orchestrator.py _build_environment_section (L1763) 移动
        ...

    async def build(self, session_id: str, message: str,
                    todo_dict: Optional[dict] = None) -> str:
        """构建完整 system prompt(环境 + 技能 + cron + TODO)。"""
        # 从 orchestrator.py _build_enhanced_context (L2004) 移动
        ...

    def _format_todo(self, todo_dict: Optional[dict]) -> str:
        # 从 orchestrator.py _format_todo_for_injection (L1831) 移动
        ...

    def _has_unfinished_steps(self, todo_dict: Optional[dict]) -> bool:
        # 从 orchestrator.py _has_unfinished_steps (L1885) 移动
        ...

    def _build_continuation_message(self, todo_dict: Optional[dict]) -> str:
        # 从 orchestrator.py _build_continuation_message (L1906) 移动
        ...
```

- [ ] **Step 4: 修改 Orchestrator 委托**

```python
# orchestrator.py
self.context_builder = ContextBuilder(self.skill_mgr, self.cron_isolator)
```

- [ ] **Step 5: 运行测试**

Run: `python -m pytest tests/test_context_builder.py tests/test_system_prompt.py tests/test_environment_injection.py tests/test_todo_injection.py -x -q`
Expected: PASS

- [ ] **Step 6: 全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add src/agent/context_builder.py src/orchestrator.py tests/test_context_builder.py
git commit -m "refactor: 拆出 ContextBuilder(阶段 3d)"
```

---

## Task 3e: 拆出 MessagePersistence + 瘦化 Orchestrator

**Files:**
- Create: `src/agent/msg_persistence.py`
- Modify: `src/orchestrator.py` — 提取持久化方法,瘦化到 <300 行
- Test: `tests/test_msg_persistence.py`

**Interfaces:**
- Consumes: orchestrator.py 中的 `_persist_new_messages`, `_save_interrupt_notice`, `_sanitize_history_alternation`, `_is_empty_assistant_content`, `apply_condenser_config`, `_maybe_flush_on_session_switch`, `flush_consolidation`
- Produces: `MessagePersistence` 类

- [ ] **Step 1: 写 MessagePersistence 的失败测试**

```python
# tests/test_msg_persistence.py
"""MessagePersistence 测试。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from unittest.mock import MagicMock
from agent.msg_persistence import MessagePersistence


class TestMessagePersistence:
    def test_persist_new_messages(self):
        chroma = MagicMock()
        buffer = MagicMock()
        mp = MessagePersistence(chroma_store=chroma, history_buffer=buffer)
        mp.persist_new_messages(session_id="test-1", messages=[{"role": "user", "content": "hi"}])
        # 验证 chroma/buffer 被调用
        assert chroma.add.called or buffer.append.called
```

- [ ] **Step 2: 运行验证失败**

Run: `python -m pytest tests/test_msg_persistence.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 实现 MessagePersistence**

从 orchestrator.py 提取:
- `_persist_new_messages` (L2470) → `persist_new_messages`
- `_save_interrupt_notice` (L2499) → `save_interrupt_notice`
- `_sanitize_history_alternation` (L2524) → `sanitize_history`
- `_is_empty_assistant_content` (L2570) → `is_empty_assistant`
- `apply_condenser_config` (L2598) → `apply_condenser_config`
- `_maybe_flush_on_session_switch` (L2634) → `maybe_flush_on_switch`
- `flush_consolidation` (L2672) → `flush_consolidation`
- `_trigger_consolidation` (L2697) → `trigger_consolidation`

```python
# src/agent/msg_persistence.py
"""消息持久化:写入存储、中断通知、历史清理、归档回调。"""
from __future__ import annotations

import logging
from typing import Any, Dict, List, Optional

logger = logging.getLogger(__name__)


class MessagePersistence:
    """消息持久化管理。"""

    def __init__(self, chroma_store, history_buffer, consolidation_engine=None) -> None:
        self._chroma = chroma_store
        self._buffer = history_buffer
        self._consolidation = consolidation_engine

    def persist_new_messages(self, session_id: str, messages: List[dict]) -> None:
        # 从 orchestrator.py _persist_new_messages (L2470) 移动
        ...

    def save_interrupt_notice(self, session_id: str, text: str) -> None:
        # 从 orchestrator.py _save_interrupt_notice (L2499) 移动
        ...

    def sanitize_history(self, messages: List[dict]) -> List[dict]:
        # 从 orchestrator.py _sanitize_history_alternation (L2524) 移动
        ...

    @staticmethod
    def is_empty_assistant(content: Any) -> bool:
        # 从 orchestrator.py _is_empty_assistant_content (L2570) 移动
        ...

    def maybe_flush_on_switch(self, session_id: str) -> None:
        # 从 orchestrator.py _maybe_flush_on_session_switch (L2634) 移动
        ...

    def flush_consolidation(self, session_id: Optional[str] = None) -> Dict[str, int]:
        # 从 orchestrator.py flush_consolidation (L2672) 移动
        ...

    async def trigger_consolidation(self, session_id: Optional[str] = None) -> None:
        # 从 orchestrator.py _trigger_consolidation (L2697) 移动
        ...
```

- [ ] **Step 4: 瘦化 Orchestrator 到 <300 行**

Orchestrator 只保留:
- `__init__`:创建/注入 5 个子管理器(从 500 行降到 ~30 行)
- `chat()`:协调流程(session_mgr → context_builder → react_loop → msg_persistence)
- `chat_stream()`:流式版本
- `close()` / `shutdown()`:委托清理

```python
# src/orchestrator.py (瘦化后)
"""瘦编排器:只做流程协调,具体逻辑下沉到子管理器。"""
from __future__ import annotations

import logging
from typing import Any, AsyncGenerator, Dict, Optional

logger = logging.getLogger(__name__)


class Orchestrator:
    """瘦编排器:协调子管理器完成 chat 流程。"""

    def __init__(self, config_path: str = "config.yaml",
                 metrics=None, audit_logger=None,
                 approval_manager=None, task_manager=None) -> None:
        from config import load_config
        from llm.client import LLMClient
        from agent.session_manager import SessionManager
        from agent.skill_manager import SkillManager
        from agent.cron_isolator import CronIsolator
        from agent.context_builder import ContextBuilder
        from agent.msg_persistence import MessagePersistence
        from agent.react_loop import ReactLoop

        self.config = load_config(config_path)
        self.llm_client = LLMClient(config=self.config, metrics_collector=metrics)
        self.metrics = metrics
        self.audit_logger = audit_logger

        self.session_mgr = SessionManager(llm_client=self.llm_client)
        self.skill_mgr = SkillManager()
        self.cron_isolator = CronIsolator()
        self.context_builder = ContextBuilder(self.skill_mgr, self.cron_isolator)
        self.msg_persistence = MessagePersistence(
            chroma_store=None,  # 后续从容器注入
            history_buffer=None,
        )
        self.react_loop = ReactLoop(
            llm_client=self.llm_client,
            # ... 其他依赖
        )

    async def chat(self, session_id: str, user_input: str, **kwargs) -> Dict[str, Any]:
        session = self.session_mgr.ensure_session(session_id)
        context = await self.context_builder.build(session_id, user_input)
        result = await self.react_loop.run(session, context, user_input, **kwargs)
        self.msg_persistence.persist_new_messages(session_id, result.get("messages", []))
        return result

    async def chat_stream(self, session_id: str, user_input: str, **kwargs) -> AsyncGenerator:
        session = self.session_mgr.ensure_session(session_id)
        context = await self.context_builder.build(session_id, user_input)
        async for event in self.react_loop.run_stream(session, context, user_input, **kwargs):
            yield event

    def flush_consolidation(self, session_id: Optional[str] = None) -> Dict[str, int]:
        return self.msg_persistence.flush_consolidation(session_id)

    def close(self) -> None:
        if hasattr(self.llm_client, "close"):
            self.llm_client.close()
```

- [ ] **Step 5: 运行测试**

Run: `python -m pytest tests/test_msg_persistence.py tests/test_orchestrator_persistence.py tests/test_archive_callback.py -x -q`
Expected: PASS

- [ ] **Step 6: 全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS

- [ ] **Step 7: 检查 Orchestrator 行数**

Run: `python -c "lines = len(open('src/orchestrator.py').readlines()); print(f'orchestrator.py: {lines} lines'); assert lines < 350, f'Too large: {lines}'"`
Expected: PASS (orchestrator.py < 350 行)

- [ ] **Step 8: 提交**

```bash
git add src/agent/msg_persistence.py src/orchestrator.py tests/test_msg_persistence.py
git commit -m "refactor: 拆出 MessagePersistence + 瘦化 Orchestrator(阶段 3e)"
```

---

## Task 4: 拆分 builtin_tools.py + 删除死代码

**Files:**
- Create: `src/agent/tools/__init__.py`, `src/agent/tools/file_tools.py`, `src/agent/tools/shell_tools.py`, `src/agent/tools/web_tools.py`, `src/agent/tools/memory_tools.py`, `src/agent/tools/plan_tools.py`
- Delete: `src/agent/builtin_tools_fixed.py`
- Modify: `src/agent/builtin_tools.py` → 改为 re-export 或删除
- Test: `tests/test_file_tools.py`, `tests/test_builtin_tools_exec.py` (现有)

**Interfaces:**
- Consumes: 现有 `builtin_tools.py` 中的工具函数
- Produces: `tools/` 目录下的 5 个模块,每个导出工具函数和 schema

- [ ] **Step 1: 创建 tools/ 目录结构**

```python
# src/agent/tools/__init__.py
"""内置工具,按领域分组。"""
from .file_tools import *
from .shell_tools import *
from .web_tools import *
from .memory_tools import *
from .plan_tools import *
```

- [ ] **Step 2: 按领域拆分工具函数**

从 builtin_tools.py 移动函数到对应文件:

| 目标文件 | 工具函数 |
|---------|---------|
| file_tools.py | file_read, file_write, file_listdir, file_glob, file_grep, file_delete |
| shell_tools.py | execute_command |
| web_tools.py | web_fetch, web_search |
| memory_tools.py | search_memory, profile_update |
| plan_tools.py | todo_create, todo_update, todo_get, todo_complete 等 |

每个工具文件结构:
```python
# src/agent/tools/file_tools.py
"""文件操作工具。"""
from typing import Any, Dict

def file_read(path: str, **kwargs) -> str:
    """读取文件内容。"""
    # 从 builtin_tools.py 移动函数体
    ...

# 工具 schema
FILE_READ_SCHEMA = {
    "name": "file_read",
    "description": "读取文件内容",
    "input_schema": {
        "type": "object",
        "properties": {
            "path": {"type": "string", "description": "文件路径"},
        },
        "required": ["path"],
    },
}

TOOLS = [FILE_READ_SCHEMA]
```

- [ ] **Step 3: 删除死代码**

```bash
# 确认无导入
python -c "import ast; ast.parse(open('src/agent/builtin_tools_fixed.py').read())"
# 删除
rm src/agent/builtin_tools_fixed.py
```

- [ ] **Step 4: 更新 builtin_tools.py 为 re-export(过渡兼容)**

```python
# src/agent/builtin_tools.py
"""向后兼容:re-export tools/ 中的工具。"""
from agent.tools.file_tools import *
from agent.tools.shell_tools import *
from agent.tools.web_tools import *
from agent.tools.memory_tools import *
from agent.tools.plan_tools import *
```

- [ ] **Step 5: 运行测试**

Run: `python -m pytest tests/test_file_tools.py tests/test_builtin_tools_exec.py tests/test_builtin_tools_http.py tests/test_memory_tools.py tests/test_plan_tools.py -x -q`
Expected: PASS

- [ ] **Step 6: 全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add src/agent/tools/ src/agent/builtin_tools.py
git rm src/agent/builtin_tools_fixed.py
git commit -m "refactor: 拆分 builtin_tools.py → tools/ + 删除死代码(阶段 4)"
```

---

## Task 5: 接入热重载(LLM + 安全权限 + 存储路径)

**Files:**
- Create: `tests/test_hot_reload.py`
- Modify: `src/app.py` — lifespan 中初始化容器
- Modify: `src/routes/config.py` — PUT /config 接入容器热重载
- Modify: `src/config.py` — 增加敏感字段处理

**Interfaces:**
- Consumes: `Container` (Task 1), `detect_changed_sections` (Task 1)
- Produces: 热重载功能(LLM/安全/存储三种场景)

- [ ] **Step 1: 写热重载失败测试**

```python
# tests/test_hot_reload.py
"""热重载测试:LLM/安全/存储三种场景。"""
import sys, os, json, time
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from unittest.mock import MagicMock, patch
from container import Container, detect_changed_sections, ComponentRef


class TestDetectChangedSections:
    def test_no_change(self):
        old = {"llm": {"model": "gpt-4"}, "server": {"port": 8080}}
        new = {"llm": {"model": "gpt-4"}, "server": {"port": 8080}}
        assert detect_changed_sections(old, new) == set()

    def test_llm_change(self):
        old = {"llm": {"model": "gpt-4"}}
        new = {"llm": {"model": "claude-3"}}
        assert detect_changed_sections(old, new) == {"llm"}

    def test_multiple_sections_change(self):
        old = {"llm": {"model": "v1"}, "security": {"enabled": True}}
        new = {"llm": {"model": "v2"}, "security": {"enabled": False}}
        assert detect_changed_sections(old, new) == {"llm", "security"}

    def test_new_section_added(self):
        old = {"llm": {}}
        new = {"llm": {}, "memory": {"threshold": 7}}
        assert detect_changed_sections(old, new) == {"memory"}


class TestLLMHotReload:
    def test_llm_reload_rebuilds_client_and_orchestrator(self):
        c = Container({"llm": {"model": "v1"}, "server": {"hot_reload_grace_period": 0}})

        class FakeLLM:
            def __init__(self, config): self.model = config["model"]; self.closed = False
            def close(self): self.closed = True

        class FakeOrch:
            def __init__(self, llm): self.llm = llm
            def close(self): pass

        c.register("llm_client", lambda c: FakeLLM(c.config["llm"]), deps=[], hot_reloadable=True)
        c.register("orchestrator", lambda c: FakeOrch(c.get("llm_client")),
                   deps=["llm_client"], hot_reloadable=False)

        old_llm = c.get("llm_client")
        old_orch = c.get("orchestrator")

        reloaded = c.reload({"llm"}, {"llm": {"model": "v2"}, "server": {"hot_reload_grace_period": 0}})

        assert "llm_client" in reloaded
        assert "orchestrator" in reloaded
        assert c.get("llm_client").model == "v2"
        assert c.get("orchestrator") is not old_orch
        time.sleep(0.5)
        assert old_llm.closed is True


class TestSecurityHotReload:
    def test_security_reload_rebuilds_policy_engine(self):
        c = Container({"security": {"enabled": True}, "server": {"hot_reload_grace_period": 0}})

        class FakePolicy:
            def __init__(self, config): self.enabled = config["enabled"]
            def close(self): pass

        c.register("policy_engine", lambda c: FakePolicy(c.config["security"]),
                   deps=[], hot_reloadable=True)
        c.register("orchestrator", lambda c: object(),
                   deps=["policy_engine"], hot_reloadable=False)

        reloaded = c.reload({"security"}, {"security": {"enabled": False}, "server": {"hot_reload_grace_period": 0}})
        assert "policy_engine" in reloaded
        assert c.get("policy_engine").enabled is False


class TestComponentRefInReload:
    def test_component_ref_gets_latest(self):
        c = Container({"llm": {"model": "v1"}, "server": {"hot_reload_grace_period": 0}})

        class FakeLLM:
            def __init__(self, config): self.model = config["model"]
            def close(self): pass

        class FakeCron:
            def __init__(self, orch_ref): self._ref = orch_ref

        c.register("llm_client", lambda c: FakeLLM(c.config["llm"]), deps=[], hot_reloadable=True)
        c.register("orchestrator", lambda c: FakeLLM(c.config["llm"]),
                   deps=["llm_client"], hot_reloadable=False)
        c.register("cron_scheduler",
                   lambda c: FakeCron(ComponentRef(c, "orchestrator")),
                   deps=[], hot_reloadable=False)

        cron = c.get("cron_scheduler")
        c.reload({"llm"}, {"llm": {"model": "v2"}, "server": {"hot_reload_grace_period": 0}})
        # ComponentRef 应转发到新 orchestrator
        assert cron._ref.model == "v2"
```

- [ ] **Step 2: 运行验证失败**

Run: `python -m pytest tests/test_hot_reload.py -x -q`
Expected: FAIL (如果 container.py 已实现则部分 PASS,热重载场景可能需要调试)

- [ ] **Step 3: 在 config.py 中增加敏感字段处理**

```python
# src/config.py 末尾添加

SENSITIVE_FIELDS = {
    "llm.api_key": "LLM_API_KEY",
    "llm.api_secret": "LLM_API_SECRET",
}

def _get_nested(config: dict, path: str) -> Any:
    """获取嵌套配置值。"""
    keys = path.split(".")
    value = config
    for key in keys:
        if not isinstance(value, dict):
            return None
        value = value.get(key)
    return value

def _set_nested(config: dict, path: str, value: Any) -> None:
    """设置嵌套配置值。"""
    keys = path.split(".")
    for key in keys[:-1]:
        if key not in config:
            config[key] = {}
        config = config[key]
    config[keys[-1]] = value

def _update_env_file(env_path: str, updates: dict[str, str]) -> None:
    """更新 .env 文件(追加或覆盖对应行)。"""
    from pathlib import Path
    path = Path(env_path)
    lines = []
    if path.exists():
        lines = path.read_text(encoding="utf-8").splitlines()
    existing_keys = {line.split("=")[0] for line in lines if "=" in line}
    for key, value in updates.items():
        if key in existing_keys:
            lines = [f"{key}={value}" if line.startswith(f"{key}=") else line for line in lines]
        else:
            lines.append(f"{key}={value}")
    path.write_text("\n".join(lines) + "\n", encoding="utf-8")

def write_config_with_sensitive_separation(
    new_config: dict, config_path: str, env_path: str
) -> None:
    """非敏感字段写 config.yaml,敏感字段实际值写 .env。"""
    from copy import deepcopy
    import yaml
    config_to_write = deepcopy(new_config)
    env_updates = {}

    for field_path, env_var in SENSITIVE_FIELDS.items():
        actual_value = _get_nested(config_to_write, field_path)
        if actual_value and not str(actual_value).startswith("${"):
            env_updates[env_var] = actual_value
            _set_nested(config_to_write, field_path, f"${{{env_var}}}")

    _update_env_file(env_path, env_updates)

    from pathlib import Path
    Path(config_path).write_text(
        yaml.dump(config_to_write, allow_unicode=True, default_flow_style=False),
        encoding="utf-8",
    )
```

- [ ] **Step 4: 修改 routes/config.py 接入热重载**

```python
# src/routes/config.py 中 PUT /config 端点修改
from container import detect_changed_sections, ConfigReloadError
from config import write_config_with_sensitive_separation, load_config, clear_config_cache

@router.put("/config", response_model=ConfigUpdateResponse)
async def update_config(request: ConfigUpdateRequest):
    # 1. 保存旧 config
    old_config = load_config()

    # 2. 备份 + 写盘(敏感字段分离)
    from pathlib import Path
    import shutil
    config_path = "config.yaml"
    env_path = ".env"
    if Path(config_path).exists():
        shutil.copy2(config_path, config_path + ".bak")
    write_config_with_sensitive_separation(request.config, config_path, env_path)

    # 3. 清缓存 + 重新加载
    clear_config_cache()
    new_config = load_config()

    # 4. 检测变更段
    changed = detect_changed_sections(old_config, new_config)

    # 5. 容器热重载
    needs_restart = "server" in changed
    reloaded = []
    error_msg = None

    if changed and not needs_restart:
        try:
            from app import get_container
            container = get_container()
            reloaded = container.reload(changed, new_config)
        except ConfigReloadError as e:
            error_msg = str(e)

    if error_msg:
        from fastapi import HTTPException
        raise HTTPException(status_code=500, detail={
            "error": "ConfigReloadError",
            "message": error_msg,
        })

    return ConfigUpdateResponse(
        status="saved",
        message=f"配置已保存。{len(reloaded)} 个组件已热重载。",
        needs_restart=needs_restart,
        reloaded=reloaded,
        skipped=[],
    )
```

- [ ] **Step 5: 修改 app.py 初始化容器**

```python
# src/app.py 中添加容器初始化
from container import Container

_container: Container | None = None

def get_container() -> Container:
    global _container
    if _container is None:
        raise RuntimeError("容器未初始化")
    return _container

@asynccontextmanager
async def lifespan(app: FastAPI):
    global _container
    from config import load_config
    config = load_config()

    _container = Container(config)
    # 注册组件(使用旧 Orchestrator 过渡)
    from orchestrator import Orchestrator
    _container.register("orchestrator",
        lambda c: Orchestrator(config_path="config.yaml"),
        deps=[], hot_reloadable=False)

    app.state.container = _container
    app.state.orchestrator = _container.get("orchestrator")
    # ... 其他初始化

    yield

    if _container:
        _container.close()
```

- [ ] **Step 6: 运行热重载测试**

Run: `python -m pytest tests/test_hot_reload.py -x -q`
Expected: PASS

- [ ] **Step 7: 运行全量测试**

Run: `python -m pytest tests/ -x -q --timeout=30`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add src/app.py src/routes/config.py src/config.py tests/test_hot_reload.py
git commit -m "refactor: 接入热重载(LLM + 安全 + 存储)(阶段 5)"
```

---

## Task 6: 统一错误处理 + 测试迁移 + 清理

**Files:**
- Modify: `src/app.py` — 注册全局异常处理器
- Modify: `tests/` — 调整导入路径
- Test: `tests/test_errors.py`

**Interfaces:**
- Consumes: `errors.py` (Task 1)
- Produces: 统一错误响应格式

- [ ] **Step 1: 写错误处理测试**

```python
# tests/test_errors.py
"""统一异常处理测试。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from fastapi.testclient import TestClient
from errors import ToolNotFoundError, ToolExecutionError, ConfigValidationError


class TestErrorHandling:
    def test_tool_not_found_returns_404(self):
        # 通过 TestClient 测试异常处理器
        ...

    def test_config_validation_returns_400(self):
        ...

    def test_tool_execution_returns_500(self):
        ...
```

- [ ] **Step 2: 在 app.py 注册全局异常处理器**

```python
# src/app.py 中添加
from fastapi import Request
from fastapi.responses import JSONResponse
from errors import (
    ToolError, ToolNotFoundError, ToolExecutionError,
    ToolPermissionDenied, ToolTimeoutError,
    ConfigError, ConfigValidationError, ConfigReloadError,
)

def _register_exception_handlers(app: FastAPI):
    @app.exception_handler(ToolNotFoundError)
    async def handle_tool_not_found(request: Request, exc: ToolNotFoundError):
        return JSONResponse(status_code=404, content={
            "error": "tool_not_found", "message": str(exc)
        })

    @app.exception_handler(ToolPermissionDenied)
    async def handle_permission_denied(request: Request, exc: ToolPermissionDenied):
        return JSONResponse(status_code=403, content={
            "error": "permission_denied", "message": str(exc)
        })

    @app.exception_handler(ToolTimeoutError)
    async def handle_timeout(request: Request, exc: ToolTimeoutError):
        return JSONResponse(status_code=504, content={
            "error": "tool_timeout", "message": str(exc)
        })

    @app.exception_handler(ToolExecutionError)
    async def handle_tool_error(request: Request, exc: ToolExecutionError):
        return JSONResponse(status_code=500, content={
            "error": "tool_execution_error", "message": str(exc)
        })

    @app.exception_handler(ConfigValidationError)
    async def handle_config_validation(request: Request, exc: ConfigValidationError):
        return JSONResponse(status_code=400, content={
            "error": "config_validation_error", "message": str(exc)
        })

    @app.exception_handler(ConfigReloadError)
    async def handle_config_reload(request: Request, exc: ConfigReloadError):
        return JSONResponse(status_code=500, content={
            "error": "config_reload_error", "message": str(exc)
        })
```

- [ ] **Step 3: 调整测试导入路径**

全局搜索测试中的 `from server import` 和 `patch("server.` 引用,改为从新模块路径导入:

```bash
# 查找需要修改的测试
grep -rn "from server import\|from server\." tests/
grep -rn 'patch("server\.' tests/
```

修改策略:
- `from server import XxxModel` → `from schemas.xxx import XxxModel`
- `patch("server.orchestrator")` → `patch("routes.chat._orchestrator")` 或保持(兼容层)

- [ ] **Step 4: 运行错误处理测试**

Run: `python -m pytest tests/test_errors.py -x -q`
Expected: PASS

- [ ] **Step 5: 运行全量测试**

Run: `python -m pytest tests/ -x -q --timeout=60`
Expected: PASS (所有测试通过)

- [ ] **Step 6: 验证文件行数**

```bash
python -c "
import os
files = [
    'src/orchestrator.py',
    'src/server.py',
    'src/agent/react_loop.py',
    'src/container.py',
    'src/app.py',
]
for f in files:
    path = os.path.join('e:/Java/webser/web_app/webme/teage-liu', f)
    if os.path.exists(path):
        lines = len(open(path, encoding='utf-8').readlines())
        status = 'OK' if (f != 'src/orchestrator.py' or lines < 350) else 'TOO LARGE'
        print(f'{f}: {lines} lines [{status}]')
"
```
Expected: orchestrator.py < 350 行

- [ ] **Step 7: 验证死代码已删除**

```bash
python -c "
import os
path = 'e:/Java/webser/web_app/webme/teage-liu/src/agent/builtin_tools_fixed.py'
assert not os.path.exists(path), '死代码未删除'
print('OK: 死代码已删除')
"
```

- [ ] **Step 8: 最终提交**

```bash
git add src/app.py tests/ src/errors.py
git commit -m "refactor: 统一错误处理 + 测试迁移 + 清理(阶段 6)"
```

---

## Self-Review

### Spec Coverage

| Spec 章节 | 对应 Task |
|-----------|-----------|
| 第 3 节:整体架构 | Task 1 (container) + Task 2 (routes/schemas) |
| 第 4 节:DI 容器 | Task 1 (container.py) + Task 5 (热重载接入) |
| 第 5 节:Orchestrator 拆分 | Task 3a-3e |
| 第 6 节:server.py 拆分 | Task 2 |
| 第 7 节:工具系统重组 | Task 4 |
| 第 8 节:热重载流程 | Task 5 |
| 第 9 节:错误处理 | Task 6 |
| 第 10 节:测试策略 | 每个 Task 内含测试 |
| 第 11 节:迁移路径 | Task 1-6 按序对应阶段 1-6 |

### Type Consistency

- `Container.register(name, factory, deps, hot_reloadable)` — Task 1 定义,Task 5 使用 ✅
- `Container.reload(changed_sections, new_config) -> list[str]` — Task 1 定义,Task 5 使用 ✅
- `ComponentRef(container, name)` — Task 1 定义,Task 5 测试 ✅
- `detect_changed_sections(old, new) -> set[str]` — Task 1 定义,Task 5 使用 ✅
- `SessionManager.ensure_session(session_id)` — Task 3a 定义,Task 3e Orchestrator 使用 ✅
- `ContextBuilder.build(session_id, message)` — Task 3d 定义,Task 3e Orchestrator 使用 ✅

### Placeholder Scan

- `...` 用于表示"从 orchestrator.py 移动代码" — 这是移动现有代码,不是占位符。实际执行时需从源文件复制对应行范围的代码。
- 每个 Task 都有具体的文件路径、测试代码、验证命令。

---

## Execution Handoff

Plan complete and saved. Each task follows the TDD cycle (red → green → refactor → commit). Start with Task 1 and work through in order.

**任务概览:**

| Task | 内容 | 关键产出 |
|------|------|----------|
| Task 1 | DI 容器骨架 | `container.py` + `errors.py` + `ComponentRef` |
| Task 2 | 拆分 server.py | `routes/`(12 文件) + `schemas/`(8 文件) + `app.py` |
| Task 3a-3e | 拆分 Orchestrator | SessionManager / SkillManager / CronIsolator / ContextBuilder / MessagePersistence |
| Task 4 | 拆分 builtin_tools | `tools/`(5 文件) + 删除 1652 行死代码 |
| Task 5 | 接入热重载 | LLM/安全/存储三种热重载场景 + 敏感字段分离 |
| Task 6 | 错误处理 + 清理 | 全局异常处理器 + 测试迁移 + 行数验证 |

**执行顺序:** Task 1 → Task 2 → Task 3a → 3b → 3c → 3d → 3e → Task 4 → Task 5 → Task 6

每个 Task 完成后运行 `python -m pytest tests/ -x -q` 确保无回归,然后提交。
