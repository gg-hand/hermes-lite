# 存储扩展接管落盘 实现计划

> **给执行者：** 按此计划逐任务实现，任务内步骤按顺序执行。
> 步骤用复选框(`- [ ]`)语法跟踪进度。
>
> **注意（与本仓库规则的一致性）：** 本计划**不含任何仓库内测试文件**
> （仓库纯净原则）；每个任务的验证经「仓库外临时脚本（`d:/tmp/`，用完即删）」
> 或「行为套件 runner」完成。git 写操作与 config 文件改动有独立审批点（见全局约束）。

**目标：** 存储后端以异语言 stdio 扩展形态静态接管 liu2 全部落盘
（`StorageProvider` + `HistoryStore`/`MessageStore` 两通道），core 契约零改动。

**架构：** 外壳新增两个与具体后端无关的通用件——①插槽制装载器
（`server/host_components.py`，配置驱动把扩展提供的 backend 注入装配点）；
②stdio 通用代理（`server/storage_stdio_proxy.py`，P-4 线协议宿主侧参考实现，
方法调用机械映射为 JSON 帧）。协议候选登记 `PROTOCOL/PENDING.md`（P-4/P-5）。

**技术栈：** Python 3.11 stdlib（asyncio subprocess / threading / json），
零新依赖。设计文档：`docs/plans/2026-09-09-存储扩展接管落盘-设计.md`。

## 全局约束

- Python 解释器必须用 `D:\soft\Python311\python.exe`（`python`/`py` 是 Store stub）。
- **core 零改动**：不得修改 `teage_liu2/core/` 下任何文件（只允许 import）。
- 新增文件仅限：`teage_liu2/server/host_components.py`、
  `teage_liu2/server/storage_stdio_proxy.py`；修改仅限：`server/app.py`、
  `PROTOCOL/PENDING.md`、`config.yaml.example`、`teage_liu2/docs/plans/开发日志.md`。
- 仓库纯净：不新增任何 `test_*` / `*_test` / `spec` 文件入库；验证脚本一律放
  `d:/tmp/`，当次任务结束前删除。
- `config.yaml`（本地不入库）与 `config.yaml.example`（入库）的任何改动**必须先获用户批准**。
- git 写操作（add/commit/push）**必须先征得用户批准**；本计划默认不自动提交。
- liu2 开发实例端口 = 7878；本计划端到端验证用 7879 避让可能运行的 dev 实例。
- 全链路 async、降级优先：代理写路径失败不得中断主对话（由 StorageWriter
  现有兜底承担）；启动期错误（握手失败/ABC 不满足/未知配置）一律快速失败。
- **已知代价（core 零改动约束下不可消除，须向用户明示）**：`core/pipeline.py`
  的 `chat_stream` **直接同步**调用 `history_store.ensure_session/get_session_messages`
  （不经 to_thread、不经 StorageWriter）——接管后每轮对话在事件循环上有
  ≤2 次 IPC 等待（最坏 2×request_timeout）。缓解：`request_timeout_seconds`
  建议 5-10s；彻底消除须改 core 读路径为 to_thread（违反本计划 core 零改动，
  留作后续独立计划）。

---

### 任务 1: 插槽制装载器 `server/host_components.py`

**文件:**
- 新建: `teage_liu2/server/host_components.py`

**接口:**
- 消费: `core.storage.StorageProvider`、`core.history.HistoryStore`、`core.storage.MessageStore`（ABC）、`core.history.SQLiteHistoryStore`、`core.storage.SQLiteStorageProvider`（sqlite backend 工厂用）
- 产出: `SLOTS: dict[str, tuple]`、`BACKENDS: dict[str, dict[str, Callable]]`、
  `load_host_components(cfg: dict) -> dict[str, Any]`（返回 `{slot 名: 对象}`，未接管的插槽不出现在结果里）

- [ ] **步骤 1: 写实现**

```python
"""宿主组件插槽装载器(P-5 插槽契约的宿主侧参考实现,设计 §4)。

插槽白名单制:每个插槽绑定 core ABC,声明"该装配位置允许由扩展提供的
backend 接管"。装载器不知道任何扩展名,只认 config 的 backend 注册表;
isinstance 快速失败(不满足插槽 ABC = 启动失败,不静默降级)。

config 形态::

    host_components:            # 缺省省略 = 全部插槽用 core 默认实现
      - slot: storage
        backend: stdio-proxy    # 缺省 sqlite
        options: {...}          # backend 私有选项

未来扩展:新插槽 = SLOTS 加一行;新 backend = BACKENDS 注册一行,本体不改。
"""
from __future__ import annotations

import logging
from typing import Any, Callable, Dict

from ..core.history import HistoryStore, SQLiteHistoryStore
from ..core.storage import MessageStore, SQLiteStorageProvider, StorageProvider

logger = logging.getLogger(__name__)

#: 插槽白名单:插槽名 → 该插槽对象必须满足的全部 core ABC
SLOTS: Dict[str, tuple] = {
    "storage": (StorageProvider,),
    "history": (HistoryStore, MessageStore),
}


def _sqlite_backend(cfg: dict, options: dict) -> Dict[str, Any]:
    """core 默认 backend:SQLite 双通道(与既有装配语义一致)。"""
    storage_cfg = cfg.get("storage", {}) or {}
    path = options.get("sqlite_path") or storage_cfg.get(
        "sqlite_path", "data2/sessions.db"
    )
    return {
        "storage": SQLiteStorageProvider(path),
        "history": SQLiteHistoryStore(path),
    }


#: 插槽 → backend 名 → 工厂(factory(cfg, options) -> {slot 名: 对象})
BACKENDS: Dict[str, Dict[str, Callable[[dict, dict], Dict[str, Any]]]] = {
    "storage": {
        "sqlite": _sqlite_backend,
    },
}


def load_host_components(cfg: dict) -> Dict[str, Any]:
    """按 config 装载宿主组件插槽;返回 {slot 名: 对象}。

    任何非法配置(未知插槽/未知 backend/重复接管/ABC 不满足)抛
    ValueError/TypeError = 启动失败(可读错误,不静默降级)。
    """
    specs = cfg.get("host_components") or []
    if not isinstance(specs, list):
        raise ValueError(f"host_components 必须是数组,实际 {type(specs).__name__}")
    loaded: Dict[str, Any] = {}
    for i, spec in enumerate(specs):
        if not isinstance(spec, dict):
            raise ValueError(f"host_components[{i}] 必须是映射")
        slot = spec.get("slot")
        if slot not in SLOTS:
            raise ValueError(
                f"host_components[{i}] 未知插槽 {slot!r}(白名单: {sorted(SLOTS)})"
            )
        if slot in loaded:
            raise ValueError(f"插槽 {slot!r} 被重复接管(host_components[{i}])")
        backend = spec.get("backend", "sqlite")
        registry = BACKENDS.get(slot, {})
        if backend not in registry:
            raise ValueError(
                f"插槽 {slot!r} 未知 backend {backend!r}(可选: {sorted(registry)})"
            )
        options = spec.get("options") or {}
        if not isinstance(options, dict):
            raise ValueError(f"插槽 {slot!r} 的 options 必须是映射")
        objs = registry[backend](cfg, options)
        if not isinstance(objs, dict) or not objs:
            raise TypeError(
                f"backend {backend!r} 必须返回非空 {{slot 名: 对象}} 映射"
            )
        for name, obj in objs.items():
            if name not in SLOTS:
                raise TypeError(f"backend {backend!r} 返回未知插槽 {name!r}")
            if name in loaded:
                raise ValueError(
                    f"插槽 {name!r} 被重复接管(host_components[{i}] backend {backend!r})"
                )
            for abc in SLOTS[name]:
                if not isinstance(obj, abc):
                    raise TypeError(
                        f"插槽 {name!r} 的实现未满足 {abc.__name__}"
                        f"(backend {backend!r},实际 {type(obj).__name__})"
                    )
            loaded[name] = obj
            logger.info("宿主组件插槽 %s 已由 backend %s 接管", name, backend)
    return loaded
```

- [ ] **步骤 2: 仓库外验证（用完即删）**

写 `d:/tmp/verify_host_components.py`：

```python
"""验证 load_host_components:默认/显式 sqlite/非法配置三态。"""
import sys
sys.path.insert(0, r"d:\dev\teage-liu\teage-liu")
from teage_liu2.server.host_components import load_host_components

# ① 缺省 → 空 dict
assert load_host_components({}) == {}, "缺省应为空"
# ② 显式 sqlite → 两插槽都有对象
r = load_host_components({"host_components": [{"slot": "storage", "backend": "sqlite"}]})
assert "storage" in r and "history" in r, f"应有双插槽: {r.keys()}"
# ③ 未知插槽 → ValueError
try:
    load_host_components({"host_components": [{"slot": "llm"}]})
    raise SystemExit("应抛 ValueError")
except ValueError as e:
    assert "未知插槽" in str(e)
# ④ 未知 backend → ValueError
try:
    load_host_components({"host_components": [{"slot": "storage", "backend": "nope"}]})
    raise SystemExit("应抛 ValueError")
except ValueError as e:
    assert "未知 backend" in str(e)
# ⑤ ABC 不满足 → TypeError
import types
from teage_liu2.server import host_components as hc
hc.BACKENDS["storage"]["bad"] = lambda cfg, opt: {"storage": object()}
try:
    load_host_components({"host_components": [{"slot": "storage", "backend": "bad"}]})
    raise SystemExit("应抛 TypeError")
except TypeError as e:
    assert "StorageProvider" in str(e)
print("verify_host_components: 全部通过")
```

运行（PowerShell，仓库根执行）：

```powershell
D:\soft\Python311\python.exe d:\tmp\verify_host_components.py
```

预期输出：`verify_host_components: 全部通过`

- [ ] **步骤 3: 清理** —— 删除 `d:/tmp/verify_host_components.py`。

---

### 任务 2: 装配点接线 `server/app.py`（默认行为不变）

**文件:**
- 修改: `teage_liu2/server/app.py`（第 133-139 行装配段 + import 区）

**接口:**
- 消费: 任务 1 的 `load_host_components(cfg) -> dict[str, Any]`
- 产出: `history_store` / `storage_provider` 两个局部变量（后续引用全部不变，
  含 `TransportBus(...)`、`ChatPipeline(history_store=...)`、
  `registry.shutdown(message_store=..., storage_provider=...)`、`app.state.*`）

- [ ] **步骤 1: 加 import**（在 `from .routes import router` 附近）

```python
from .host_components import load_host_components
```

- [ ] **步骤 2: 替换装配段**（原第 133-139 行「2. 历史存储 / 2.5 存储平台」两段）

```python
    # 2. 历史存储 + 存储平台:插槽制装载(P-5,设计 §4)。
    #    host_components 未接管 = SQLite 默认,与既有行为完全一致
    loaded_components = load_host_components(cfg)
    storage_cfg = cfg.get("storage", {}) or {}
    sqlite_path = storage_cfg.get("sqlite_path", "data2/sessions.db")
    history_store = loaded_components.get("history") or SQLiteHistoryStore(sqlite_path)
    # 2.5 存储平台(D1):枝干通用落盘通道;接管时与 history 同对象(见 stdio-proxy)
    storage_provider = loaded_components.get("storage") or SQLiteStorageProvider(sqlite_path)
```

- [ ] **步骤 3: 默认路径回归验证**

启动 dev 实例（PowerShell，仓库根执行，后台）：

```powershell
D:\soft\Python311\python.exe -m uvicorn teage_liu2.server.app:create_app --factory --host 127.0.0.1 --port 7878
```

验证：

```powershell
curl.exe -s http://127.0.0.1:7878/health
```

预期：200，JSON 含 `"protocol_version": "v1.0.0"`（与改动前一致）。
再发一轮对话确认历史落盘正常（`POST /chat`），然后停止实例。

- [ ] **步骤 4: 提交（须用户批准）** —— 本任务与任务 1 一并提交；
  提交信息格式执行前与用户确认（仓库内无 commit 模板，git 写操作先获批准）。

---

### 任务 3: PENDING 登记 P-5（插槽契约）

**文件:**
- 修改: `teage_liu2/PROTOCOL/PENDING.md`（候选清单末尾、`---` 分隔线前追加）

- [ ] **步骤 1: 追加条目**（严格套用 PENDING 条目模板）

```markdown
### P-5 宿主组件插槽契约（host_components 装载机制）
- 状态: experimental
- 提案日期 / 来源: 2026-09-09 / 存储扩展接管落盘设计（`docs/plans/2026-09-09-存储扩展接管落盘-设计.md`，用户逐项定案）
- 涉及域: config（host_components 段）+ lifecycle（宿主组件装配/关闭语义）
- 动机: 宿主装配点（composition root）需要语言无关的"可接管组件"声明——哪些宿主组件允许被扩展 backend 接管、接管者须满足什么 SPI、backend 形态如何命名，需要稳定契约供扩展生态参照
- 现状: 宿主侧参考实现 `teage_liu2/server/host_components.py`（插槽白名单 SLOTS + backend 注册表 BACKENDS + isinstance 快速失败）；storage 插槽已落地（backend: sqlite / stdio-proxy）；config 段 = `host_components: [{slot, backend, options}]`
- 入协议条件: ①storage 插槽经真实异语言后端实战验证一轮（含快速失败路径）②插槽清单与工厂签名冻结（factory(cfg, options) -> {slot: obj}，同实例可覆盖多插槽）③行为套件补用例（默认不接管 = SQLite、未知插槽/ABC 不满足 = 启动失败）④明确归入 config/lifecycle 域文件
- 入协议记录: （未并入）
```

- [ ] **步骤 2: 自查** —— 条目字段与模板一致；无占位符。

---

### 任务 4: stdio 通用代理 `server/storage_stdio_proxy.py`

**文件:**
- 新建: `teage_liu2/server/storage_stdio_proxy.py`

**接口:**
- 消费: `core.storage._validate_kind`（kind 白名单，只 import 不修改）、`core.storage.StorageProvider/MessageStore`、`core.history.HistoryStore`（ABC 基类）
- 产出: `StdioStorageProxy(command: list[str], request_timeout: float = 10.0)`，
  实现 `StorageProvider` + `HistoryStore` + `MessageStore` 全部方法；
  `start()`（spawn + 握手，失败抛 = 启动失败）、`close()`（幂等）。
  常量 `PROTOCOL_NAME = "storage-stdio"`、`PROTOCOL_VERSION = "0.1.0"`。

- [ ] **步骤 1: 写实现**

```python
"""stdio 存储代理:P-4 storage-stdio 线协议的宿主侧参考实现(设计 §5)。

定位:与具体后端无关的通用协议件——方法调用机械映射为 JSON 帧
(op = 方法名),不含任何后端知识;换/加后端本文件零改动。

并发模型(同 StorageWriter 线程思路):内嵌独立 IO 线程 + 专用事件循环,
asyncio 管理子进程与读写帧;同步方法经 run_coroutine_threadsafe 投递 +
超时等待(StorageWriter 写线程 / asyncio.to_thread 读路径直接兼容)。

错误处理(降级优先):请求超时/断连抛异常;写路径由 StorageWriter 现有
兜底吞掉记日志,主对话不中断;读路径异常向上冒泡由调用方降级。
"""
from __future__ import annotations

import asyncio
import concurrent.futures
import itertools
import json
import logging
import threading
from typing import Any, Dict, List, Optional

from ..core.history import HistoryStore
from ..core.storage import MessageStore, StorageProvider, _validate_kind

logger = logging.getLogger(__name__)

PROTOCOL_NAME = "storage-stdio"
PROTOCOL_VERSION = "0.1.0"

#: 握手超时(秒)——进程 spawn + 首帧往返
_HANDSHAKE_TIMEOUT = 15.0
#: close 时 bye 后等待子进程自行退出的超时(秒)
_SHUTDOWN_TIMEOUT = 5.0


class _BackendConnection:
    """子进程连接:专用事件循环线程内管理 spawn/读帧/写帧/关闭。"""

    def __init__(self, command: List[str]) -> None:
        self._command = list(command)
        self.loop = asyncio.new_event_loop()
        self._thread = threading.Thread(
            target=self._run, daemon=True, name="stdio-storage-proxy"
        )
        #: 握手完成/失败信号(set 在 loop 线程,threading.Event 线程安全)
        self._handshake_done = threading.Event()
        self._handshake_error: Optional[BaseException] = None
        self._proc: Optional[asyncio.subprocess.Process] = None
        self._pending: Dict[int, asyncio.Future] = {}
        self._ids = itertools.count(1)
        #: 同一帧内串行写 stdin(单 loop 线程访问,无需锁)
        self._closed = False

    # -- 线程外接口(同步) ------------------------------------------------
    def start(self) -> None:
        """spawn 子进程并完成握手;失败抛异常(启动失败)。"""
        self._thread.start()
        if not self._handshake_done.wait(timeout=_HANDSHAKE_TIMEOUT + 5.0):
            self.abort()
            raise RuntimeError(
                f"存储后端握手超时({_HANDSHAKE_TIMEOUT}s): {self._command!r}"
            )
        if self._handshake_error is not None:
            self.abort()
            raise self._handshake_error

    def request(self, op: str, payload: dict,
                timeout: Optional[float] = None) -> Any:
        """同步请求-响应:投递到内部 loop 并等待结果(线程安全)。

        连接已断开(loop 停止)立即抛 RuntimeError——不悬挂到超时、不掩盖死因;
        超时抛 TimeoutError;关闭后调用抛 RuntimeError。
        """
        if self._closed:
            raise RuntimeError("存储后端连接已关闭")
        if not self._thread.is_alive() or not self.loop.is_running():
            raise RuntimeError("存储后端连接已断开(loop 已停止)")
        fut = asyncio.run_coroutine_threadsafe(
            self._request(op, payload), self.loop
        )
        try:
            return fut.result(timeout)
        except concurrent.futures.TimeoutError:
            fut.cancel()
            raise TimeoutError(f"存储后端 {op!r} 请求超时") from None

    def abort(self) -> None:
        """无条件的立即终止(启动失败/异常路径用,幂等)。"""
        self._closed = True
        if self.loop.is_running():
            self.loop.call_soon_threadsafe(self._stop)

    def close(self) -> None:
        """优雅关闭:bye → 等 IO 线程结束 → 终止兜底(幂等)。

        确定性时序:同步投递 bye 帧写入(不经协程等待)→ join IO 线程
        (后端收到 bye 退出 → stdout EOF → _serve 返回)→ 兜底杀进程。
        避免协程与 loop 停止的竞态(EOF 会让 run_until_complete 返回,
        挂在 loop 上的关闭协程将永不完成)。
        """
        if self._closed:
            return
        self._closed = True
        if self.loop.is_running():
            try:
                self.loop.call_soon_threadsafe(self._send_bye)
            except RuntimeError:
                pass  # loop 正在关闭
        if self._thread.is_alive():
            self._thread.join(timeout=_SHUTDOWN_TIMEOUT + 5.0)
        self._stop_sync()
        if self._thread.is_alive():
            logger.warning("存储后端 IO 线程未在期限内退出,跳过 loop.close")
        else:
            self.loop.close()

    def _send_bye(self) -> None:
        """loop 线程内:同步写 bye 帧(小帧,无需 drain 等待)。"""
        if self._proc is None or self._proc.stdin is None or self._proc.stdin.is_closing():
            return
        try:
            self._proc.stdin.write(
                (json.dumps({"id": 0, "op": "bye", "p": {}}) + "\n").encode("utf-8")
            )
        except Exception as e:
            logger.warning("发送 bye 失败: %s", e)

    # -- loop 线程内 ------------------------------------------------------
    def _run(self) -> None:
        asyncio.set_event_loop(self.loop)
        try:
            self.loop.run_until_complete(self._serve())
        except BaseException as e:  # noqa: BLE001 —— 线程边界,必须捕获
            if not self._handshake_done.is_set():
                self._handshake_error = e
            else:
                logger.error("存储后端连接异常终止: %s", e)
            self._fail_pending(RuntimeError(f"存储后端连接已断开: {e}"))
        finally:
            self._handshake_done.set()
            self._fail_pending(RuntimeError("存储后端连接已关闭"))

    async def _serve(self) -> None:
        self._proc = await asyncio.create_subprocess_exec(
            *self._command,
            stdin=asyncio.subprocess.PIPE,
            stdout=asyncio.subprocess.PIPE,
            stderr=asyncio.subprocess.PIPE,
        )
        pump = asyncio.ensure_future(self._pump_stderr())
        reader = asyncio.ensure_future(self._read_loop())
        try:
            # 握手:hello 与"进程退出(EOF)"竞速——后端秒退时立即失败,不等满超时;
            # _request 返回已解包的 r(ok:false 时经 future 直接抛后端错误)
            hello = asyncio.ensure_future(self._request("hello", {
                "protocol": PROTOCOL_NAME, "version": PROTOCOL_VERSION,
            }))
            done, _pending = await asyncio.wait(
                {hello, reader},
                timeout=_HANDSHAKE_TIMEOUT,
                return_when=asyncio.FIRST_COMPLETED,
            )
            if hello not in done:
                if reader in done:
                    raise RuntimeError(
                        "存储后端进程在握手完成前退出(检查后端启动错误,见日志)"
                    )
                raise RuntimeError(
                    f"存储后端握手超时({_HANDSHAKE_TIMEOUT}s): {self._command!r}"
                )
            resp = hello.result()
            ver = str(resp.get("version", "")) if isinstance(resp, dict) else ""
            if not ver:
                raise RuntimeError(f"存储后端握手响应缺少 version: {resp!r}")
            if ver.split(".")[0] != PROTOCOL_VERSION.split(".")[0]:
                raise RuntimeError(
                    f"存储后端协议主版本不兼容: 后端 {ver!r} vs 宿主 {PROTOCOL_VERSION!r}"
                )
            logger.info("存储后端握手完成(protocol=%s, backend=%s)", PROTOCOL_NAME, ver)
            # 握手成功即时放行 start();失败路径仍由 _run 的 finally 兜底置位
            self._handshake_done.set()
            await reader  # 常驻:进程退出/stdout EOF 才返回
        finally:
            # 握手失败/异常路径:取消常驻任务 + 杀进程等退出,防 pending 告警
            for t in (reader, pump, hello):
                if t.done():
                    try:
                        t.exception()  # 显式取走,防 "never retrieved" 告警
                    except asyncio.CancelledError:
                        pass
                else:
                    t.cancel()
            proc = self._proc
            if proc is not None and proc.returncode is None:
                proc.kill()
                try:
                    await proc.wait()
                except Exception:
                    pass

    async def _request(self, op: str, payload: dict) -> Any:
        """发送一帧并等待响应(loop 线程内协程)。"""
        assert self._proc is not None and self._proc.stdin is not None
        req_id = next(self._ids)
        frame = json.dumps({"id": req_id, "op": op, "p": payload},
                           ensure_ascii=False) + "\n"
        fut: asyncio.Future = self.loop.create_future()
        self._pending[req_id] = fut
        try:
            self._proc.stdin.write(frame.encode("utf-8"))
            await self._proc.stdin.drain()
        except BaseException:
            self._pending.pop(req_id, None)
            raise
        return await fut

    async def _read_loop(self) -> None:
        assert self._proc is not None and self._proc.stdout is not None
        while True:
            line = await self._proc.stdout.readline()
            if not line:
                break  # EOF:后端退出
            try:
                frame = json.loads(line.decode("utf-8"))
            except UnicodeDecodeError as e:
                # P-4 帧编码条款:后端 stdio 必须输出 UTF-8;违反 = 连接终止(快速失败)
                raise RuntimeError(
                    "存储后端返回非 UTF-8 数据(违反 P-4 帧编码条款,检查后端"
                    " stdout/stderr 编码配置)"
                ) from e
            except json.JSONDecodeError:
                logger.warning("存储后端返回非 JSON 行,忽略: %r", line[:200])
                continue
            fut = self._pending.pop(frame.get("id"), None)
            if fut is None or fut.done():
                continue
            if frame.get("ok"):
                fut.set_result(frame.get("r"))
            else:
                fut.set_exception(RuntimeError(str(frame.get("e"))))
        logger.warning("存储后端 stdout 已关闭(进程退出)")

    async def _pump_stderr(self) -> None:
        """后端 stderr 透传到宿主日志(排查用)。"""
        assert self._proc is not None and self._proc.stderr is not None
        while True:
            line = await self._proc.stderr.readline()
            if not line:
                break
            logger.info("[storage-backend] %s", line.decode("utf-8", "replace").rstrip())

    async def _graceful_close(self) -> None:
        if self._proc is None:
            return
        try:
            if self._proc.stdin is not None and not self._proc.stdin.is_closing():
                self._proc.stdin.write(
                    (json.dumps({"id": 0, "op": "bye", "p": {}}) + "\n")
                    .encode("utf-8")
                )
                await self._proc.stdin.drain()
        except Exception as e:
            logger.warning("发送 bye 失败: %s", e)
        try:
            await asyncio.wait_for(self._proc.wait(), timeout=_SHUTDOWN_TIMEOUT)
        except asyncio.TimeoutError:
            logger.warning("存储后端未在 %ss 内退出,强制终止", _SHUTDOWN_TIMEOUT)
            self._proc.kill()
            await self._proc.wait()

    def _stop(self) -> None:
        self.loop.create_task(self._kill())

    async def _kill(self) -> None:
        if self._proc is not None:
            self._proc.kill()

    def _stop_sync(self) -> None:
        """极端路径:loop 不可用时直接同步杀进程。"""
        proc = self._proc
        if proc is not None:
            try:
                proc.kill()
            except ProcessLookupError:
                pass

    def _fail_pending(self, error: Exception) -> None:
        for fut in self._pending.values():
            if not fut.done():
                fut.set_exception(error)
        self._pending.clear()


class StdioStorageProxy(StorageProvider, HistoryStore, MessageStore):
    """通用 stdio 存储代理:方法调用机械映射为 P-4 帧,无后端知识。"""

    def __init__(self, command: List[str], request_timeout: float = 10.0) -> None:
        if not command or not all(isinstance(c, str) and c for c in command):
            raise ValueError(f"command 必须是非空字符串数组,实际 {command!r}")
        if request_timeout <= 0:
            raise ValueError(f"request_timeout 必须为正数,实际 {request_timeout!r}")
        self._request_timeout = request_timeout
        self._conn = _BackendConnection(command)
        self._close_lock = threading.Lock()

    def start(self) -> None:
        """spawn + 握手(装配点调用;失败抛 = 启动失败)。"""
        self._conn.start()

    # -- 内部:同步调用 → 内部 loop --------------------------------------
    def _call(self, op: str, payload: dict) -> Any:
        return self._conn.request(op, payload, timeout=self._request_timeout)

    # -- StorageProvider ---------------------------------------------------
    def write(self, kind: str, doc):
        _validate_kind(kind)
        if isinstance(doc, list):
            if not doc:
                raise ValueError("批量写入 docs 不能为空列表")
            return self._call("write", {"kind": kind, "docs": doc})
        return self._call("write", {"kind": kind, "docs": [doc]})[0]

    def read(self, kind: str, doc_id: str):
        _validate_kind(kind)
        return self._call("read", {"kind": kind, "doc_id": doc_id})

    def query(self, kind: str, limit: Optional[int] = None, **filters):
        _validate_kind(kind)
        if limit is not None:
            if not isinstance(limit, int) or isinstance(limit, bool) or limit < 1:
                raise ValueError(f"limit 必须是正整数,实际 {limit!r}")
        return self._call("query", {
            "kind": kind, "limit": limit, "filters": filters,
        })

    def delete(self, kind: str, doc_id: str) -> None:
        _validate_kind(kind)
        self._call("delete", {"kind": kind, "doc_id": doc_id})

    # -- HistoryStore / MessageStore ---------------------------------------
    def ensure_session(self, session_id: str) -> None:
        self._call("ensure_session", {"session_id": session_id})

    def log_message(
        self,
        session_id: str,
        role: str,
        content: str,
        tool_name: Optional[str] = None,
        tool_call_id: Optional[str] = None,
        token_count: int = 0,
        is_error: bool = False,
        reasoning: Optional[str] = None,
        content_blocks: Optional[List[dict]] = None,
        message_type: Optional[str] = None,
    ) -> None:
        self._call("log_message", {
            "session_id": session_id, "role": role, "content": content,
            "tool_name": tool_name, "tool_call_id": tool_call_id,
            "token_count": token_count, "is_error": is_error,
            "reasoning": reasoning, "content_blocks": content_blocks,
            "message_type": message_type,
        })

    def get_session_messages(
        self, session_id: str, limit: Optional[int] = None,
        before_id: Optional[int] = None,
    ) -> List[Dict[str, Any]]:
        return self._call("get_session_messages", {
            "session_id": session_id, "limit": limit, "before_id": before_id,
        })

    def update_session_title(self, session_id: str, title: str) -> None:
        self._call("update_session_title", {"session_id": session_id, "title": title})

    def get_session_title(self, session_id: str) -> Optional[str]:
        return self._call("get_session_title", {"session_id": session_id})

    def search_messages(
        self, keyword: str, session_id: Optional[str] = None, limit: int = 20
    ) -> List[Dict[str, Any]]:
        return self._call("search_messages", {
            "keyword": keyword, "session_id": session_id, "limit": limit,
        })

    # -- 生命周期 ------------------------------------------------------------
    def close(self) -> None:
        """优雅关闭(幂等;registry.shutdown 会对同一实例调两次)。"""
        with self._close_lock:
            if self._conn._closed:
                return
            self._conn.close()
```

- [ ] **步骤 2: 仓库外单体验证（用完即删）**

先写 `d:/tmp/mini_backend.py`（最小后端，只实现握手 + echo op，仅供本步验证代理管线）：

```python
"""最小 stdio 存储后端:仅握手 + write/read echo,验证代理帧管线。"""
import json, sys

for line in sys.stdin:
    req = json.loads(line)
    op = req["op"]
    if op == "hello":
        resp = {"id": req["id"], "ok": True, "r": {"version": "0.1.0"}}
    elif op == "bye":
        resp = {"id": req["id"], "ok": True, "r": None}
        sys.stdout.write(json.dumps(resp) + "\n"); sys.stdout.flush()
        break
    elif op == "write":
        resp = {"id": req["id"], "ok": True, "r": ["echo-" + str(len(req["p"]["docs"]))]}
    else:
        resp = {"id": req["id"], "ok": False, "e": "mini_backend 未实现 " + op}
    sys.stdout.write(json.dumps(resp) + "\n"); sys.stdout.flush()
```

再写 `d:/tmp/verify_proxy.py`：

```python
"""验证 StdioStorageProxy:握手/write 往返/超时/幂等 close。"""
import sys, time
sys.path.insert(0, r"d:\dev\teage-liu\teage-liu")
from teage_liu2.server.storage_stdio_proxy import StdioStorageProxy

proxy = StdioStorageProxy(
    command=[sys.executable, r"d:\tmp\mini_backend.py"], request_timeout=5.0,
)
proxy.start()
doc_id = proxy.write("demo.kind", {"v": 1})
assert doc_id == "echo-1", doc_id
ids = proxy.write("demo.kind", [{"v": 2}, {"v": 3}])
assert ids == ["echo-2"], ids  # mini 后端只回一条
# 未实现 op → 可读异常
try:
    proxy.read("demo.kind", "x")
    raise SystemExit("应抛异常")
except RuntimeError as e:
    assert "未实现" in str(e)
t0 = time.monotonic()
proxy.close(); proxy.close()  # 幂等
assert time.monotonic() - t0 < 10, "close 应快速返回"
print("verify_proxy: 全部通过")
```

运行：

```powershell
D:\soft\Python311\python.exe d:\tmp\verify_proxy.py
```

预期输出：`verify_proxy: 全部通过`

- [ ] **步骤 3: 清理** —— 删除 `d:/tmp/mini_backend.py`、`d:/tmp/verify_proxy.py`。

---

### 任务 5: 注册 stdio-proxy backend + config 接线

**文件:**
- 修改: `teage_liu2/server/host_components.py`（BACKENDS 注册 + 新工厂函数）

**接口:**
- 消费: 任务 4 的 `StdioStorageProxy(command, request_timeout)`、`start()`、`close()`
- 产出: backend 名 `"stdio-proxy"` 可用于 `host_components` 配置

- [ ] **步骤 1: 加工厂函数**（`_sqlite_backend` 之后）

```python
def _stdio_proxy_backend(cfg: dict, options: dict) -> Dict[str, Any]:
    """stdio-proxy backend:P-4 通用代理,一个实例覆盖 storage+history 双插槽。"""
    from .storage_stdio_proxy import StdioStorageProxy

    command = options.get("command")
    if (
        not isinstance(command, list)
        or not command
        or not all(isinstance(c, str) and c for c in command)
    ):
        raise ValueError(
            "stdio-proxy backend 的 options.command 必须是非空字符串数组,"
            f"实际 {command!r}"
        )
    timeout = options.get("request_timeout_seconds", 10.0)
    if not isinstance(timeout, (int, float)) or isinstance(timeout, bool) or timeout <= 0:
        raise ValueError(
            f"stdio-proxy 的 request_timeout_seconds 必须为正数,实际 {timeout!r}"
        )
    proxy = StdioStorageProxy(command=command, request_timeout=float(timeout))
    proxy.start()  # 握手失败抛 = 启动失败(不静默降级)
    return {"storage": proxy, "history": proxy}
```

- [ ] **步骤 2: 注册进 BACKENDS**

```python
BACKENDS: Dict[str, Dict[str, Callable[[dict, dict], Dict[str, Any]]]] = {
    "storage": {
        "sqlite": _sqlite_backend,
        "stdio-proxy": _stdio_proxy_backend,
    },
}
```

- [ ] **步骤 3: 仓库外验证（用完即删）**

写 `d:/tmp/verify_wire.py`：复用任务 4 的思路——mini 后端不支持 history op，
因此只验证「stdio-proxy 经装载器正确实例化 + 插槽双覆盖 + close 链路」：

```python
import sys
sys.path.insert(0, r"d:\dev\teage-liu\teage-liu")
# 复用 mini_backend:握手通过即可装载
open(r"d:\tmp\mini_backend.py", "w").write(MINI)  # MINI 同任务 4 步骤 2 的脚本文本
from teage_liu2.server.host_components import load_host_components
loaded = load_host_components({"host_components": [{
    "slot": "storage", "backend": "stdio-proxy",
    "options": {"command": [sys.executable, r"d:\tmp\mini_backend.py"],
                "request_timeout_seconds": 5},
}]})
assert loaded["storage"] is loaded["history"], "stdio-proxy 应双插槽同实例"
loaded["storage"].close()
print("verify_wire: 全部通过")
```

（`MINI` 内容 = 任务 4 步骤 2 的 `mini_backend.py` 全文，作为字符串内嵌。）

运行：`D:\soft\Python311\python.exe d:\tmp\verify_wire.py`
预期：`verify_wire: 全部通过`

- [ ] **步骤 4: 清理** —— 删除 `d:/tmp/verify_wire.py`、`d:/tmp/mini_backend.py`。

---

### 任务 6: PENDING 登记 P-4（storage-stdio 线协议）

**文件:**
- 修改: `teage_liu2/PROTOCOL/PENDING.md`（**插入到 P-5 条目之前**——任务 3 已把 P-5 追加在清单末尾，本任务插在其前面，维持 P-1→P-5 编号顺序展示）

- [ ] **步骤 1: 插入条目（P-5 之前）**

```markdown
### P-4 storage-stdio 存储线协议（异语言存储后端）
- 状态: experimental
- 提案日期 / 来源: 2026-09-09 / 存储扩展接管落盘设计（`docs/plans/2026-09-09-存储扩展接管落盘-设计.md`，用户逐项定案）
- 涉及域: storage（SPI 承载）+ transport（stdio 帧语义，独立于 transport 12 消息）
- 动机: "扩展接管宿主落盘"需要语言无关的宿主↔后端进程契约；任意语言实现的存储后端满足本协议即可经宿主通用代理接管 `StorageProvider` + `HistoryStore`/`MessageStore` 双通道
- 现状: 宿主侧参考实现 `teage_liu2/server/storage_stdio_proxy.py`（方法调用机械映射为帧，无后端知识）。帧格式：JSON Lines over stdio，请求 `{"id","op","p"}`，响应 `{"id","ok","r"|"e"}`；握手 `hello`（major 版本不匹配 = 启动失败）、关闭 `bye`；op 集 = write/read/query/delete + ensure_session/log_message/get_session_messages/update_session_title/get_session_title/search_messages
- 语义条款（后端实现方义务）: ①kind 白名单 `^[a-z0-9_.]+$`（宿主先行校验，双重防线）②`query` 按写入序返回、filters 为 doc 顶层字段精确匹配 ③批量写 `docs[]` 原子提交 ④**write 响应 `r` 恒为 doc_id 数组**（单条与批量一致，数组顺序与 docs 对应；宿主单条写取 `[0]`——后端不得对单条返回字符串，否则 `s[0]` 静默截断）⑤`search_messages` = 子串匹配即可（FTS 是 SQLite 实现细节非契约）⑥单进程内串行应答（一请求一响应）⑦**帧编码必须 UTF-8**（stdin/stdout/stderr 均是；Python 后端在 Windows 默认 GBK，须 `sys.stdin/stdout/stderr.reconfigure(encoding="utf-8")`——实测 stdin 不切则含中文帧致子进程解码崩溃，宿主对非 UTF-8 数据快速失败）
- 入协议条件: ①至少 1 个真实异语言后端跑通 spawn→握手→双通道读写→重启恢复→bye 全链 ②语义条款逐条验证 ③行为套件补用例（P-4 专用 case）④与 P-2 异语言验证互相印证后一并评审
- 入协议记录: （未并入）
```

- [ ] **步骤 2: 自查** —— 与设计文档 §5 逐字一致；无占位符。

---

### 任务 7: 参考后端端到端联调（仓库外，用完即删）

**文件:**
- 新建（仓库外临时件，验收后删除）: `d:/tmp/liu2_storage_backend.py`、`d:/tmp/liu2_storage_e2e.py`
- 数据目录（临时）: `d:/tmp/liu2_backend_data/`

**接口:**
- 消费: 任务 1-5 的装载链 + P-4 线协议
- 产出: 端到端验收证据（健康检查 / 聊天落盘 / 重启恢复 / 快速失败）

- [ ] **步骤 1: 写参考后端** `d:/tmp/liu2_storage_backend.py`
  （JSONL 文件后端：`sessions.jsonl` / `messages.jsonl` / `doc_<kind>.jsonl`，
  每个 op 在 stderr 打一行日志供联调观察；全文检索 = 子串匹配）

```python
"""P-4 参考后端(联调临时件):JSONL 文件存储,验收后删除。
用法: python liu2_storage_backend.py <数据目录>
"""
import json, os, sys, threading

# P-4 条款⑦:帧编码必须 UTF-8(Windows Python 默认 GBK,须显式切换)
sys.stdout.reconfigure(encoding="utf-8")
sys.stderr.reconfigure(encoding="utf-8")

DATA = sys.argv[1] if len(sys.argv) > 1 else "."
os.makedirs(DATA, exist_ok=True)
# RLock:log_message 在持锁状态调用 _append(内部再取锁),非重入 Lock 会自死锁
_lock = threading.RLock()

def _path(name):
    return os.path.join(DATA, name)

def _append(name, obj):
    with _lock, open(_path(name), "a", encoding="utf-8") as f:
        f.write(json.dumps(obj, ensure_ascii=False) + "\n")

def _load(name):
    try:
        with open(_path(name), encoding="utf-8") as f:
            return [json.loads(l) for l in f if l.strip()]
    except FileNotFoundError:
        return []

def _rewrite(name, rows):
    with _lock, open(_path(name), "w", encoding="utf-8") as f:
        for r in rows:
            f.write(json.dumps(r, ensure_ascii=False) + "\n")

import uuid
from datetime import datetime

def handle(op, p):
    if op == "write":
        docs = p["docs"]
        ids = [uuid.uuid4().hex for _ in docs]
        now = datetime.now().isoformat()
        rows = _load("doc_" + p["kind"] + ".jsonl")
        for i, d in enumerate(docs):
            rows.append({"doc_id": ids[i], "doc": d, "created_at": now})
        _rewrite("doc_" + p["kind"] + ".jsonl", rows)
        return ids  # P-4 条款④:write 响应恒为 doc_id 数组(单条/批量一致)
    if op == "read":
        for r in _load("doc_" + p["kind"] + ".jsonl"):
            if r["doc_id"] == p["doc_id"]:
                return r["doc"]
        return None
    if op == "query":
        docs = [r["doc"] for r in _load("doc_" + p["kind"] + ".jsonl")]
        flt = p.get("filters") or {}
        docs = [d for d in docs if all(d.get(k) == v for k, v in flt.items())]
        lim = p.get("limit")
        return docs[:lim] if lim else docs
    if op == "delete":
        rows = _load("doc_" + p["kind"] + ".jsonl")
        _rewrite("doc_" + p["kind"] + ".jsonl",
                 [r for r in rows if r["doc_id"] != p["doc_id"]])
        return None
    if op == "ensure_session":
        if not any(s["session_id"] == p["session_id"] for s in _load("sessions.jsonl")):
            _append("sessions.jsonl", {"session_id": p["session_id"], "title": None})
        return None
    if op == "log_message":
        # id 用递增整数(对齐 SQLite rowid 契约,before_id 游标为 int)
        with _lock:
            row = dict(p); row["id"] = len(_load("messages.jsonl")) + 1
            row["created_at"] = datetime.now().isoformat()
            _append("messages.jsonl", row)
        return None
    if op == "get_session_messages":
        msgs = [m for m in _load("messages.jsonl")
                if m["session_id"] == p["session_id"]]
        if p.get("before_id"):
            idx = next((i for i, m in enumerate(msgs) if m["id"] == p["before_id"]), None)
            if idx is not None:
                msgs = msgs[:idx]
        lim = p.get("limit")
        return msgs[-lim:] if lim else msgs
    if op == "update_session_title":
        rows = _load("sessions.jsonl")
        for s in rows:
            if s["session_id"] == p["session_id"]:
                s["title"] = p["title"]
        _rewrite("sessions.jsonl", rows)
        return None
    if op == "get_session_title":
        for s in _load("sessions.jsonl"):
            if s["session_id"] == p["session_id"]:
                return s.get("title")
        return None
    if op == "search_messages":
        hits = []
        for m in _load("messages.jsonl"):
            if p.get("session_id") and m["session_id"] != p["session_id"]:
                continue
            if p["keyword"] in (m.get("content") or ""):
                hits.append(m)
                if len(hits) >= p.get("limit", 20):
                    break
        return hits
    raise ValueError("未知 op: " + op)

for line in sys.stdin:
    req = json.loads(line)
    op = req.get("op")
    if op == "hello":
        out = {"id": req["id"], "ok": True, "r": {"version": "0.1.0"}}
    elif op == "bye":
        sys.stdout.write(json.dumps({"id": req["id"], "ok": True, "r": None}) + "\n")
        sys.stdout.flush()
        break
    else:
        try:
            out = {"id": req["id"], "ok": True, "r": handle(op, req.get("p") or {})}
        except Exception as e:
            out = {"id": req["id"], "ok": False, "e": f"{type(e).__name__}: {e}"}
    sys.stderr.write(f"[ref-backend] {op} ok={out.get('ok')}\n"); sys.stderr.flush()
    sys.stdout.write(json.dumps(out, ensure_ascii=False) + "\n"); sys.stdout.flush()
```

- [ ] **步骤 2: 写 e2e 启动脚本** `d:/tmp/liu2_storage_e2e.py`
  （不改动仓库 `config.yaml`：读仓库配置 → 深拷贝注入 host_components → 7879 端口）

```python
"""e2e:backend: stdio-proxy 启动 liu2(端口 7879,临时,不碰仓库 config.yaml)。"""
import copy, sys
sys.path.insert(0, r"d:\dev\teage-liu\teage-liu")
from teage_liu2.core.config import load_config
from teage_liu2.server.app import create_app

cfg = copy.deepcopy(load_config(r"d:\dev\teage-liu\teage-liu\config.yaml"))
cfg["host_components"] = [{
    "slot": "storage", "backend": "stdio-proxy",
    "options": {
        "command": [sys.executable, r"d:\tmp\liu2_storage_backend.py",
                    r"d:\tmp\liu2_backend_data"],
        "request_timeout_seconds": 10,
    },
}]
import uvicorn
uvicorn.run(create_app(config=cfg, config_path=r"d:\dev\teage-liu\teage-liu\config.yaml"),
            host="127.0.0.1", port=7879, log_level="info")
```

- [ ] **步骤 3: 端到端验收**（PowerShell）

```powershell
# ① 启动
D:\soft\Python311\python.exe d:\tmp\liu2_storage_e2e.py
# ② 健康检查
curl.exe -s http://127.0.0.1:7879/health     # 预期 200 + protocol_version
# ③ 聊天一轮(落盘经代理 → 后端 JSONL)
curl.exe -s -X POST http://127.0.0.1:7879/chat -H "Content-Type: application/json" -d '{\"session_id\":\"e2e-storage\",\"user_input\":\"记住我喜欢喝咖啡\"}'
# ④ 观察后端数据
Get-Content d:\tmp\liu2_backend_data\sessions.jsonl
Get-Content d:\tmp\liu2_backend_data\messages.jsonl   # 预期含 user/assistant 消息行
# ⑤ Ctrl+C 停止 → 重新启动 → 再次聊天,确认 get_session_messages 经代理取回历史(后端 stderr 有该 op 日志,回复延续上下文)
# ⑥ 停止,观察进程退出日志含 "存储后端握手完成" 与优雅关闭无报错
```

- [ ] **步骤 4: 快速失败路径验证**（临时改 e2e 脚本 command 指向不存在文件 →
  启动应报"存储后端握手超时/失败"且进程退出非 0；改回后不再保留）。

- [ ] **步骤 5: 清理** —— 删除 `d:/tmp/liu2_storage_backend.py`、
  `d:/tmp/liu2_storage_e2e.py`、`d:/tmp/liu2_backend_data/`（临时件用完即删；
  后端正式实现由用户按 P-4 自研，不入库）。

---

### 任务 8: 文档回写与 config 示例（含审批点）

**文件:**
- 修改: `teage_liu2/docs/plans/开发日志.md`（顶部追加）
- 修改（**须用户批准**）: `config.yaml.example`（host_components 注释示例段）

- [ ] **步骤 1: config.yaml.example 追加示例段**（注释形态，默认不启用；
  **执行前向用户说明改动内容并获批准**）

```yaml
# 宿主组件插槽(P-5,experimental):缺省省略 = 全部用 core 默认 SQLite。
# storage 插槽可由异语言 stdio 后端接管(P-4):
# host_components:
#   - slot: storage
#     backend: stdio-proxy
#     options:
#       command: ["python", "/path/to/storage_backend.py", "/path/to/data"]
#       request_timeout_seconds: 10
```

- [ ] **步骤 2: 开发日志追加**（`teage_liu2/docs/plans/开发日志.md` 顶部，
  格式对齐现有条目）：变更摘要 = 插槽制装载器 + stdio 通用代理 +
  装配点接线 + P-4/P-5 登记 + 默认行为不变说明。

- [ ] **步骤 3: 全量回归** —— 不配置 host_components 重新启动 dev 实例（7878），
  健康检查 + 一轮对话；确认默认路径与改动前一致。

- [ ] **步骤 4: 提交（须用户批准）** —— 汇总剩余文件生成 commit 方案
  （提交信息格式执行前与用户确认）交用户批准后执行。

---

## 自审记录

- **规格覆盖**: 设计 §4→任务 1/2/3；§5→任务 4/5/6；§7 验收→任务 2/7/8；P-4/P-5 登记→任务 3/6。无缺口。
- **占位符**: 全部步骤含完整代码/命令；无 TBD/TODO。
- **类型一致性**: `load_host_components(cfg)->dict[str,Any]`（任务 1 定义，任务 2/5 消费）；
  `StdioStorageProxy(command, request_timeout)`（任务 4 定义，任务 5 消费）；
  `factory(cfg, options)->{slot: obj}`（任务 1 约定，任务 5 遵循）。
- **已知边界**: ①`registry.shutdown` 会对共享实例调两次 close → 代理 close 幂等（任务 4 已实现）；②`transport.py` 对 storage 的调用是鸭子类型 + `asyncio.to_thread`，代理同步方法天然兼容；③行为套件 P-4 用例留作 P-4 入 stable 条件（YAGNI，不阻塞本计划）。

## 执行前审查记录（2026-09-09，plan-verifier）

**结论：修复后可执行。** 12 项断言核实通过（app.py 行号/ABC 表面/调用方兼容/_validate_kind/
配置链路/PENDING 模板/命令真实性/设计一致性等）；以下问题已在本计划中修复：

| 级别 | 问题 | 修复落点 |
|---|---|---|
| 阻断 | e2e 聊天请求体字段 `message` 错误（routes.py 实际读 `user_input`） | 任务 7 步骤 3 |
| 阻断 | write 响应契约缺失（后端单条返回字符串会被 `[0]]` 静默截断） | 任务 6 语义条款④ + 任务 7 参考后端注释 |
| 应修 | `pipeline.chat_stream` 直接同步调 `ensure_session/get_session_messages`（不经 to_thread），接管后事件循环上有 ≤2 次 IPC 等待——计划自审"读路径全部 to_thread"表述不准确 | 全局约束"已知代价"条目 |
| 应修 | 后端连接死亡后请求悬挂至超时且错误误导 | 任务 4 代理代码：`request()` 投递前检查 loop 存活，死连接立即抛可读错误 |
| 应修 | `_call` 跨类访问私有方法 `self._conn._request`，`request()` 成为死代码 | 任务 4 代理代码：`request()` 加 timeout 参数，`_call` 委托之 |
| 建议 | PENDING 条目顺序（P-5 先追加导致 P-4 排在其后） | 任务 6 改为插入到 P-5 之前 |
| 建议 | 设计 §5"SQLiteHistoryStore 全部公开方法"措辞与实际范围不符（create_session 不在代理表面且无生产调用方） | 设计文档 §5 改为"HistoryStore+MessageStore ABC 全部方法" |
| 建议 | 参考后端消息 id 用 uuid 与 before_id(int) 契约不符；write 返回冗余表达式 | 任务 7 参考后端改递增 int id / 简化 return |
| 建议 | 握手失败路径常驻任务未取消（loop.close pending 告警） | 任务 4 `_serve` try/finally 取消 reader/pump |
| 风险提示 | 仓库内无 commit 模板，"主题行与 -- 根因：间留空行"来源无法在仓库核实 | 任务 2/8 提交措辞改为"格式执行前与用户确认" |

另两项核实为"无风险"：Windows 3.11 默认 ProactorEventLoop 策略使子线程内
`create_subprocess_exec` 可行（前提"子线程默认 Selector"不成立）；uvicorn 0.52.1
`--factory` 实测可用。
