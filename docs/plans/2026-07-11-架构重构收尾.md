# 架构重构收尾实现计划

> **For agentic workers:** Use the java-dev-skills workflow to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成前一轮重构的收尾工作：清理死代码、注册组件到DI容器、分层拆分server.py、让热重载真正生效。

**Architecture:** 执行顺序 1→3→2→4。先清理死代码，再注册组件到容器（路由通过容器访问组件），然后分层拆分server.py（schemas→lifespan→routes），最后清理orchestrator/react_loop/builtin_tools。

**Tech Stack:** Python 3.11+, FastAPI, Pydantic, pytest, asyncio

## Global Constraints

- API 契约兼容：65个端点的请求/响应格式不变，前端无需改动
- config.yaml 格式兼容：${ENV_VAR} 占位符机制不变
- 数据文件兼容：chroma/sqlite/history_buffer/schedules 格式不变
- 测试框架：pytest，测试通过 `sys.path.insert(0, "src")` 导入
- 每个阶段完成后运行 `python -m pytest tests/ -x -q --ignore=tests/test_monitor_page.py` 确保无回归
- 每个子步骤完成后提交一次，提交信息格式：`refactor: <阶段描述>`
- PowerShell 环境：用 `;` 分隔命令，不用 `&&`；用多个 `-m` 传提交信息

---

## File Structure

### 删除文件
```
src/routes/                    # 12文件死代码（含5个导入bug）
src/schemas/                   # 8文件死代码
scripts/split_server.py        # 生成bug脚手架的脚本
tests/test_schemas_import.py   # 测试死代码
src/agent/builtin_tools.py     # 最终删除（代码迁移到tools/）
```

### 新建文件
```
src/schemas/                   # 重新创建，干净版本
├── __init__.py
├── chat.py                    # ChatRequest, CancelRequest, ChatResponse
├── config.py                  # ConfigResponse, ConfigUpdateRequest, ConfigUpdateResponse
├── schedules.py               # ScheduleCreateRequest, ScheduleUpdateRequest...
├── approvals.py               # ApprovalResolveRequest, ApprovalResolveResponse...
├── proposals.py               # ProposalModifyRequest
├── files.py                   # FileUploadResponse, FileItem, FileListResponse...
├── memory.py                  # memory相关模型（如有）
└── common.py                  # HealthResponse, SessionItem, MessageItem, FlushResponse...
src/routes/                    # 重新创建，干净版本
├── __init__.py
├── misc.py                    # /, /chat(GET), /monitor, /scheduler, /workflow, /tools, /audit, /reasoning, /recall, /restart, /consolidation/flush
├── health.py                  # /health, /metrics/*
├── sessions.py                # /sessions/*
├── config.py                  # /config GET/PUT
├── approvals.py               # /approvals/*
├── proposals.py               # /proposals/*
├── skills.py                  # /skills/*
├── files.py                   # /files/*, /admin/files/*
├── cron_tools.py              # /cron_tools/*
├── memory.py                  # /memories/*, /profile
├── schedules.py               # /schedules/*
└── chat.py                    # /chat, /chat/stream, /chat/cancel
src/agent/tool_executor.py     # 从react_loop.py提取
src/agent/stream_handler.py    # 从react_loop.py提取
```

### 修改文件
```
src/container.py               # 更新CONFIG_TO_COMPONENTS映射
src/app.py                     # 接收lifespan + 组件注册 + 路由注册
src/server.py                  # 4578行 → <100行（最终变为 from app import app）
src/orchestrator.py            # 移除18个委托wrapper
src/agent/react_loop.py        # 2164行 → <500行（提取ToolExecutor/StreamHandler）
src/agent/tools/*.py           # 从builtin_tools.py迁移实际代码
```

---

## Task 1: 清理死代码

**Files:**
- Delete: `src/routes/` (整个目录)
- Delete: `src/schemas/` (整个目录)
- Delete: `scripts/split_server.py`
- Delete: `tests/test_schemas_import.py`

**Interfaces:**
- Produces: 干净的代码库，无死代码引用

- [ ] **Step 1: 确认无生产代码依赖 routes/schemas**

Run: `python -c "import ast, sys; sys.exit(0)"` (前置验证已在 grilling 中完成：只有 routes/ 自身和 test_schemas_import.py 引用 schemas/)

- [ ] **Step 2: 删除死代码文件**

```bash
# PowerShell
Remove-Item -Recurse -Force src/routes
Remove-Item -Recurse -Force src/schemas
Remove-Item -Force scripts/split_server.py
Remove-Item -Force tests/test_schemas_import.py
```

- [ ] **Step 3: 验证全量测试无回归**

Run: `python -m pytest tests/ -x -q --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 3`
Expected: 与删除前相同的 pass/fail 数（2588 passed, 106 failed）

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "refactor: 删除有bug的routes/schemas死代码脚手架（Task 1）" -m "删除12个routes文件(~2600行)和8个schemas文件(~296行)，均含导入bug且未接入app。删除生成脚本split_server.py和对应测试。"
```

---

## Task 2: 更新 CONFIG_TO_COMPONENTS 映射

**Files:**
- Modify: `src/container.py:26-34`
- Test: `tests/test_container.py`

**Interfaces:**
- Produces: 更新后的 CONFIG_TO_COMPONENTS，新增 monitoring/skills/files/cron/history/tools 段

- [ ] **Step 1: 写失败测试**

在 `tests/test_container.py` 末尾新增：

```python
class TestConfigMapping:
    def test_llm_maps_to_orchestrator(self):
        """llm配置变更应触发orchestrator重建。"""
        c = Container({"llm": {"model": "v1"}, "server": {"hot_reload_grace_period": 0}})
        c.register("orchestrator", lambda c: FakeOrchestrator(c.get("session_logger")),
                   deps=["session_logger"], hot_reloadable=False)
        c.register("session_logger", lambda c: FakeLLM(c.config.get("storage", {})),
                   deps=[], hot_reloadable=True)
        old = c.get("orchestrator")
        c.reload({"llm"}, {"llm": {"model": "v2"}, "server": {"hot_reload_grace_period": 0}})
        assert c.get("orchestrator") is not old

    def test_monitoring_maps_to_metrics_components(self):
        """monitoring配置变更应触发metrics_collector/metrics_store/audit_logger重建。"""
        from container import CONFIG_TO_COMPONENTS
        assert "metrics_collector" in CONFIG_TO_COMPONENTS.get("monitoring", [])
        assert "metrics_store" in CONFIG_TO_COMPONENTS.get("monitoring", [])
        assert "audit_logger" in CONFIG_TO_COMPONENTS.get("monitoring", [])

    def test_all_config_sections_covered(self):
        """所有配置段都应在CONFIG_TO_COMPONENTS中有映射。"""
        from container import CONFIG_TO_COMPONENTS
        expected_sections = {"llm", "security", "storage", "memory", "monitoring",
                           "tasks", "skills", "files", "guardrails", "cron",
                           "history", "tools", "server"}
        assert expected_sections.issubset(set(CONFIG_TO_COMPONENTS.keys()))
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_container.py::TestConfigMapping -x -q`
Expected: FAIL (KeyError: 'monitoring' 或 AssertionError)

- [ ] **Step 3: 更新 CONFIG_TO_COMPONENTS**

替换 `src/container.py` 第26-34行：

```python
CONFIG_TO_COMPONENTS: dict[str, list[str]] = {
    "llm":        ["orchestrator"],
    "security":   ["approval_manager", "orchestrator"],
    "storage":    ["session_logger", "orchestrator"],
    "memory":     ["orchestrator"],
    "monitoring": ["metrics_collector", "metrics_store", "audit_logger"],
    "tasks":      ["task_manager", "cron_scheduler"],
    "skills":     ["mcp_manager"],
    "files":      ["upload_manager", "etl_engine"],
    "guardrails": ["orchestrator"],
    "cron":       ["orchestrator"],
    "history":    ["orchestrator"],
    "tools":      ["orchestrator"],
    "server":     [],
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_container.py -x -q`
Expected: PASS (所有容器测试通过)

- [ ] **Step 5: 提交**

```bash
git add src/container.py tests/test_container.py
git commit -m "refactor: 更新CONFIG_TO_COMPONENTS映射覆盖全部配置段（Task 2）"
```

---

## Task 3: 注册外部组件到容器

**Files:**
- Modify: `src/app.py` (新增 `register_components` 函数)
- Test: `tests/test_container.py`

**Interfaces:**
- Produces: `register_components(container, config)` 函数，注册所有外部组件

- [ ] **Step 1: 写失败测试**

在 `tests/test_container.py` 末尾新增：

```python
class TestComponentRegistration:
    def test_register_all_external_components(self):
        """register_components应注册15个组件。"""
        from app import register_components
        c = Container({"storage": {}, "monitoring": {}, "security": {},
                       "tasks": {}, "skills": {}, "files": {}, "llm": {},
                       "memory": {}, "guardrails": {}, "cron": {},
                       "history": {}, "tools": {}})
        register_components(c)
        expected = {"session_logger", "metrics_collector", "metrics_store",
                    "audit_logger", "approval_manager", "task_manager",
                    "orchestrator", "stream_manager", "skill_loader",
                    "mcp_manager", "upload_manager", "etl_engine",
                    "cron_scheduler", "proposal_store", "health_checker"}
        registered = set(c._factories.keys())
        assert expected.issubset(registered), f"缺少: {expected - registered}"

    def test_container_validate_no_cycles(self):
        """注册后容器应通过循环依赖检测。"""
        from app import register_components
        c = Container({"storage": {}, "monitoring": {}, "security": {},
                       "tasks": {}, "skills": {}, "files": {}, "llm": {},
                       "memory": {}, "guardrails": {}, "cron": {},
                       "history": {}, "tools": {}})
        register_components(c)
        c.validate()  # 不抛异常即通过
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_container.py::TestComponentRegistration -x -q`
Expected: FAIL (ImportError: cannot import name 'register_components')

- [ ] **Step 3: 实现 register_components**

在 `src/app.py` 中新增函数。每个组件的工厂函数需要引用对应的类。由于这些类分布在多个模块，使用延迟导入：

```python
def register_components(container) -> None:
    """向容器注册所有 lifespan 组件。

    组件按依赖顺序注册。工厂函数中使用延迟导入避免循环依赖。
    Orchestrator 作为整体注册（方案B），内部组件对容器透明。
    """
    from orchestrator import Orchestrator

    # 1. 无依赖组件
    container.register("session_logger",
        lambda c: _create_session_logger(c.config),
        deps=[], hot_reloadable=True)

    container.register("metrics_collector",
        lambda c: _create_metrics_collector(c.config),
        deps=[], hot_reloadable=True)

    container.register("audit_logger",
        lambda c: _create_audit_logger(c.config),
        deps=[], hot_reloadable=True)

    container.register("task_manager",
        lambda c: _create_task_manager(c.config),
        deps=[], hot_reloadable=True)

    container.register("stream_manager",
        lambda c: _create_stream_manager(),
        deps=[], hot_reloadable=False)

    container.register("skill_loader",
        lambda c: _create_skill_loader(),
        deps=[], hot_reloadable=False)

    container.register("proposal_store",
        lambda c: _create_proposal_store(),
        deps=[], hot_reloadable=False)

    # 2. 依赖其他组件
    container.register("metrics_store",
        lambda c: _create_metrics_store(c.config, c.get("session_logger")),
        deps=["session_logger"], hot_reloadable=True)

    container.register("approval_manager",
        lambda c: _create_approval_manager(c.config, c.get("metrics_collector")),
        deps=["metrics_collector"], hot_reloadable=True)

    container.register("mcp_manager",
        lambda c: _create_mcp_manager(c.config, c.get("skill_loader")),
        deps=["skill_loader"], hot_reloadable=True)

    container.register("upload_manager",
        lambda c: _create_upload_manager(c.config),
        deps=[], hot_reloadable=True)

    # 3. Orchestrator（依赖外部组件，整体注册）
    container.register("orchestrator",
        lambda c: Orchestrator(
            config_path=c.config.get("_config_path", "config.yaml"),
            metrics=c.get("metrics_collector"),
            audit_logger=c.get("audit_logger"),
            approval_manager=c.get("approval_manager"),
            task_manager=c.get("task_manager"),
        ),
        deps=["metrics_collector", "audit_logger", "approval_manager", "task_manager"],
        hot_reloadable=False)

    # 4. 依赖 Orchestrator
    container.register("etl_engine",
        lambda c: _create_etl_engine(c.config, c.get("upload_manager"), c.get("orchestrator")),
        deps=["upload_manager", "orchestrator"], hot_reloadable=True)

    container.register("cron_scheduler",
        lambda c: _create_cron_scheduler(c.config, c.get("orchestrator")),
        deps=["orchestrator"], hot_reloadable=False)

    container.register("health_checker",
        lambda c: _create_health_checker(c.get("orchestrator"), c.get("session_logger"), c.get("mcp_manager")),
        deps=["orchestrator", "session_logger", "mcp_manager"], hot_reloadable=False)
```

每个 `_create_*` 辅助函数封装了原有的初始化逻辑，从 server.py lifespan 中提取。具体实现需要参考 server.py 中对应的初始化代码。

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_container.py -x -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/app.py tests/test_container.py
git commit -m "refactor: 注册15个外部组件到DI容器（Task 3）"
```

---

## Task 4: Lifespan 接入容器

**Files:**
- Modify: `src/server.py` (lifespan 函数)
- Test: `tests/test_hot_reload.py`

**Interfaces:**
- Produces: lifespan 通过 `container.get()` 获取组件，保留全局变量兼容

- [ ] **Step 1: 写失败测试**

在 `tests/test_hot_reload.py` 末尾新增端到端测试：

```python
class TestLifespanUsesContainer:
    def test_lifespan_registers_components(self):
        """lifespan启动后容器应包含所有组件。"""
        from app import get_container
        # 模拟lifespan启动
        # 验证 get_container() 返回的容器有 orchestrator 等组件
        container = get_container()
        if container:
            assert "orchestrator" in container._factories
            assert "session_logger" in container._factories
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_hot_reload.py::TestLifespanUsesContainer -x -q`
Expected: FAIL (容器中没有注册组件)

- [ ] **Step 3: 修改 lifespan**

在 `src/server.py` 的 lifespan 函数中，在现有组件初始化之前添加容器注册：

```python
# 在 lifespan 开头（配置加载之后）：
from app import init_container, register_components, get_container
init_container(config)
container = get_container()
register_components(container)

# 保留现有组件初始化，但改为从容器获取：
# orchestrator = Orchestrator(...) → orchestrator = container.get("orchestrator")
# session_logger = SessionLogger(...) → session_logger = container.get("session_logger")
# ... 其他组件同理

# 保留全局变量赋值兼容现有路由代码：
# orchestrator = container.get("orchestrator")  # 已在上面
# session_logger = container.get("session_logger")
# ...
```

注意：过渡期保留全局变量赋值，路由代码不需要改动。后续 Task 6-17 提取路由时再改为通过容器访问。

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_hot_reload.py -x -q`
Expected: PASS

- [ ] **Step 5: 运行全量测试验证无回归**

Run: `python -m pytest tests/ -x -q --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 3`
Expected: 2588 passed, 106 failed（与之前相同）

- [ ] **Step 6: 提交**

```bash
git add src/server.py src/app.py tests/test_hot_reload.py
git commit -m "refactor: lifespan接入容器，组件通过container.get()获取（Task 4）"
```

---

## Task 5: 验证热重载端到端

**Files:**
- Test: `tests/test_hot_reload.py`

**Interfaces:**
- Produces: 端到端热重载测试通过

- [ ] **Step 1: 写端到端测试**

```python
class TestHotReloadEndToEnd:
    def test_llm_config_change_rebuilds_orchestrator(self):
        """修改llm配置应触发orchestrator重建。"""
        from app import get_container
        from container import detect_changed_sections
        container = get_container()
        if not container:
            self.skipTest("容器未初始化")

        old_orch = container.get("orchestrator")

        # 模拟配置变更
        new_config = dict(container.config)
        new_config["llm"] = dict(new_config.get("llm", {}))
        new_config["llm"]["model"] = "new-model"

        changed = detect_changed_sections(container.config, new_config)
        assert "llm" in changed

        container.reload(changed, new_config)
        new_orch = container.get("orchestrator")
        assert new_orch is not old_orch

    def test_monitoring_config_change_does_not_rebuild_orchestrator(self):
        """修改monitoring配置不应触发orchestrator重建。"""
        from app import get_container
        from container import detect_changed_sections
        container = get_container()
        if not container:
            self.skipTest("容器未初始化")

        old_orch = container.get("orchestrator")

        new_config = dict(container.config)
        new_config["monitoring"] = dict(new_config.get("monitoring", {}))
        new_config["monitoring"]["enabled"] = not new_config["monitoring"].get("enabled", True)

        changed = detect_changed_sections(container.config, new_config)
        container.reload(changed, new_config)

        # orchestrator 不在 monitoring 的映射中，不应重建
        assert container.get("orchestrator") is old_orch
```

- [ ] **Step 2: 运行测试**

Run: `python -m pytest tests/test_hot_reload.py -x -q`
Expected: PASS（如果容器正确注册了组件）

- [ ] **Step 3: 提交**

```bash
git add tests/test_hot_reload.py
git commit -m "test: 端到端热重载测试验证容器重建（Task 5）"
```

---

## Task 6: 提取 Pydantic 模型到 schemas/

**Files:**
- Create: `src/schemas/__init__.py`, `src/schemas/chat.py`, `src/schemas/config.py`, `src/schemas/schedules.py`, `src/schemas/approvals.py`, `src/schemas/proposals.py`, `src/schemas/files.py`, `src/schemas/common.py`
- Modify: `src/server.py` (移除模型定义，改为从 schemas/ 导入)
- Test: `tests/test_schemas_import.py` (重新创建)

**Interfaces:**
- Produces: 27个 Pydantic 模型从 server.py 提取到 schemas/ 包

**模型分组：**
- `schemas/chat.py`: ChatRequest(359), CancelRequest(368), ChatResponse(381)
- `schemas/common.py`: HealthResponse(389), SessionItem(402), SessionListResponse(415), SessionTitleUpdate(421), MessageItem(430), MessageListResponse(454), DeleteSessionResponse(460), FlushResponse(1862)
- `schemas/config.py`: ConfigResponse(467), ConfigUpdateRequest(473), ConfigUpdateResponse(479)
- `schemas/approvals.py`: ApprovalResolveRequest(487), ApprovalResolveResponse(494), ApprovalListItem(502), ApprovalListResponse(512)
- `schemas/schedules.py`: ScheduleCreateRequest(519), ScheduleUpdateRequest(536), ScheduleListResponse(549), ScheduleResponse(555)
- `schemas/files.py`: FileUploadResponse(563), FileItem(571), FileListResponse(587), FileDeleteResponse(593)
- `schemas/proposals.py`: ProposalModifyRequest(2524)

- [ ] **Step 1: 创建 schemas/ 包**

创建 `src/schemas/__init__.py`:
```python
"""Pydantic 数据模型，按域分组。"""
```

创建每个域文件，将 server.py 中对应的类定义（包括所有字段和验证器）原样复制过去。每个文件顶部添加：
```python
from __future__ import annotations
from pydantic import BaseModel, Field
from typing import Optional, List, Any
```

- [ ] **Step 2: 创建导入测试**

```python
# tests/test_schemas_import.py
"""验证所有 schemas 模块可正确导入。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

def test_schemas_import():
    from schemas.chat import ChatRequest, CancelRequest, ChatResponse
    from schemas.config import ConfigResponse, ConfigUpdateRequest, ConfigUpdateResponse
    from schemas.schedules import ScheduleCreateRequest, ScheduleUpdateRequest, ScheduleListResponse, ScheduleResponse
    from schemas.approvals import ApprovalResolveRequest, ApprovalResolveResponse, ApprovalListItem, ApprovalListResponse
    from schemas.proposals import ProposalModifyRequest
    from schemas.files import FileUploadResponse, FileItem, FileListResponse, FileDeleteResponse
    from schemas.common import (HealthResponse, SessionItem, SessionListResponse,
                                SessionTitleUpdate, MessageItem, MessageListResponse,
                                DeleteSessionResponse, FlushResponse)
    # 验证类可实例化
    req = ChatRequest(message="test", session_id="s1")
    assert req.message == "test"
```

- [ ] **Step 3: 运行测试验证失败**

Run: `python -m pytest tests/test_schemas_import.py -x -q`
Expected: FAIL (ModuleNotFoundError: No module named 'schemas.chat')

- [ ] **Step 4: 实现 schemas/ 文件**

将 server.py 中第359-593行和第1862行、第2524行的类定义复制到对应的 schemas/ 文件中。

- [ ] **Step 5: 在 server.py 中替换为导入**

在 server.py 顶部添加：
```python
from schemas.chat import ChatRequest, CancelRequest, ChatResponse
from schemas.config import ConfigResponse, ConfigUpdateRequest, ConfigUpdateResponse
from schemas.schedules import (ScheduleCreateRequest, ScheduleUpdateRequest,
                               ScheduleListResponse, ScheduleResponse)
from schemas.approvals import (ApprovalResolveRequest, ApprovalResolveResponse,
                               ApprovalListItem, ApprovalListResponse)
from schemas.proposals import ProposalModifyRequest
from schemas.files import (FileUploadResponse, FileItem, FileListResponse,
                           FileDeleteResponse)
from schemas.common import (HealthResponse, SessionItem, SessionListResponse,
                           SessionTitleUpdate, MessageItem, MessageListResponse,
                           DeleteSessionResponse, FlushResponse)
```

删除 server.py 中原有的类定义（第359-593行、第1862行、第2524行）。

- [ ] **Step 6: 运行测试验证通过**

Run: `python -m pytest tests/test_schemas_import.py tests/ -x -q --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 3`
Expected: 全量测试无回归

- [ ] **Step 7: 提交**

```bash
git add src/schemas/ src/server.py tests/test_schemas_import.py
git commit -m "refactor: 提取27个Pydantic模型到schemas/包（Task 6）"
```

---

## Task 7: 提取 lifespan 到 app.py

**Files:**
- Modify: `src/app.py` (接收 lifespan + 全局变量 + 路由注册)
- Modify: `src/server.py` (变为 `from app import app`)

**Interfaces:**
- Produces: app.py 承载 FastAPI 实例 + lifespan，server.py <10行

- [ ] **Step 1: 运行全量测试记录基线**

Run: `python -m pytest tests/ -q --tb=no --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 1`
Expected: 记录 passed/failed 数量

- [ ] **Step 2: 将 lifespan 移到 app.py**

把 server.py 中的以下内容移到 app.py：
- `app = FastAPI(lifespan=lifespan)` 实例化
- `lifespan` 异步上下文管理器函数
- 15个模块级全局变量声明（orchestrator, session_logger 等）
- CORS 中间件配置
- 静态文件挂载

app.py 结构：
```python
app = FastAPI(lifespan=lifespan)

# 全局组件变量（lifespan 中赋值，路由中读取）
orchestrator: Optional[Orchestrator] = None
session_logger: Optional[SessionLogger] = None
# ... 其他13个全局变量

async def lifespan(app: FastAPI):
    # ... 原有 lifespan 逻辑
    yield
    # ... 原有清理逻辑
```

- [ ] **Step 3: server.py 变为薄壳**

```python
# src/server.py
"""向后兼容入口，实际 app 在 app.py 中。"""
from app import app  # noqa: F401
```

- [ ] **Step 4: 运行全量测试验证无回归**

Run: `python -m pytest tests/ -x -q --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 3`
Expected: 与 Step 1 基线相同

- [ ] **Step 5: 提交**

```bash
git add src/app.py src/server.py
git commit -m "refactor: 提取lifespan到app.py，server.py变为薄壳（Task 7）"
```

---

## Task 8-19: 按域提取路由

**通用模式（每个域遵循相同的 TDD 步骤）：**

每个域的路由提取遵循以下模式：

1. 创建 `src/routes/{domain}.py`，定义 `router = APIRouter()`
2. 把 app.py 中该域的路由 handler 移到 router（`@app.post` → `@router.post`）
3. handler 内引用的全局变量通过 `from app import orchestrator, session_logger, ...` 获取
4. app.py 中 `app.include_router(router)`，删除原路由定义
5. 运行全量测试验证无回归
6. 提交

**注意：** 过渡期路由仍通过全局变量访问组件。后续可逐步改为 `get_container().get()` 方式。

### Task 8: misc 路由（12个端点）
路由：`/`, `/chat`(GET), `/monitor`, `/scheduler`, `/workflow`, `/tools`, `/audit/logs`, `/reasoning/toggle`, `/reasoning/status`, `/recall`, `/restart`, `/consolidation/flush`

### Task 9: health 路由（5个端点）
路由：`/health`, `/metrics`, `/metrics/history`, `/metrics/reset`, `/metrics/signals`

### Task 10: sessions 路由（4个端点）
路由：`/sessions`, `/sessions/{id}/messages`, `/sessions/{id}`(PATCH), `/sessions/{id}`(DELETE)

### Task 11: config 路由（2个端点）
路由：`/config`(GET), `/config`(PUT) — 含热重载逻辑

### Task 12: approvals 路由（2个端点）
路由：`/approvals/{id}/resolve`, `/approvals`

### Task 13: proposals 路由（5个端点）
路由：`/proposals`, `/proposals/{id}`, `/proposals/{id}/confirm`, `/proposals/{id}/modify`, `/proposals/{id}/reject`

### Task 14: skills 路由（5个端点）
路由：`/skills`, `/skills/{name}`, `/skills/{name}/reload`, `/skills/{name}/toggle`, `/skills/{name}`(DELETE)

### Task 15: files 路由（6个端点）
路由：`/files/upload`, `/files`, `/files/{id}`, `/files/{id}/raw`, `/sessions/{id}/files`, `/admin/files/{id}`(DELETE)

### Task 16: cron_tools 路由（6个端点）
路由：`/cron_tools/pending`, `/cron_tools/{name}/activate`, `/cron_tools/{name}/reject`, `/cron_tools`, `/cron_tools/{name}`(DELETE), `/cron_tools/{name}`(PUT)

### Task 17: memory 路由（4个端点）
路由：`/memories`, `/memories/all`, `/memories/{id}`(DELETE), `/profile`

### Task 18: schedules 路由（11个端点）
路由：`/schedules`(GET/POST), `/schedules/runs`, `/schedules/{id}`(PUT/DELETE), `/schedules/{id}/trigger`, `/schedules/{id}/history`, `/schedules/{id}/audit`, `/schedules/{id}/audit/{run_id}`, `/schedules/{id}/memories`, `/schedules/{id}/memories/{memory_id}`(DELETE)

### Task 19: chat 路由（3个端点，最复杂）
路由：`/chat`(POST), `/chat/stream`, `/chat/cancel`

每个 Task 8-19 的提交格式：
```bash
git add src/routes/{domain}.py src/app.py
git commit -m "refactor: 提取{domain}路由到routes/{domain}.py（Task N）"
```

---

## Task 20: 移除 Orchestrator 委托 wrapper

**Files:**
- Modify: `src/orchestrator.py` (删除18个委托方法)
- Modify: 所有调用方（`src/app.py`, `src/agent/react_loop.py`, tests/）

**Interfaces:**
- Produces: Orchestrator 只保留 __init__ 和必要的协调方法，<350行

- [ ] **Step 1: 识别所有委托方法**

```bash
# 查找所有委托方法
python -c "
import re
with open('src/orchestrator.py', encoding='utf-8') as f:
    content = f.read()
# 查找所有 '委托到' 注释的方法
for m in re.finditer(r'def (\w+)\(.*?委托到.*?\n', content):
    print(m.group(1))
"
```

- [ ] **Step 2: 找到所有调用方**

```bash
# 查找每个委托方法的调用方
python -c "
import re, os
methods = ['_persist_new_messages', '_save_interrupt_notice', '_sanitize_history_alternation',
           '_is_empty_assistant_content', '_maybe_flush_on_session_switch',
           'flush_consolidation', '_trigger_consolidation',
           '_ensure_session', '_generate_title_async',
           '_activate_skill', '_deactivate_skill', '_build_active_skills_section',
           '_run_cron_session',
           '_build_environment_section', '_build_file_context', '_has_unfinished_steps']
for method in methods:
    for root, dirs, files in os.walk('src'):
        for f in files:
            if f.endswith('.py'):
                path = os.path.join(root, f)
                with open(path, encoding='utf-8') as fh:
                    if method in fh.read():
                        print(f'{method}: {path}')
"
```

- [ ] **Step 3: 逐个替换调用方**

每个调用方从 `orchestrator._method()` 改为 `orchestrator.manager.method()`：
- `orchestrator._persist_new_messages(...)` → `orchestrator.msg_persistence.persist_new_messages(...)`
- `orchestrator._ensure_session(...)` → `orchestrator.session_mgr.ensure_session(...)`
- `orchestrator._build_environment_section()` → `orchestrator.context_builder.build_environment()`
- 等等

- [ ] **Step 4: 删除 Orchestrator 中的委托方法**

- [ ] **Step 5: 运行全量测试**

Run: `python -m pytest tests/ -x -q --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 3`
Expected: 无新增 failure

- [ ] **Step 6: 提交**

```bash
git add src/orchestrator.py src/app.py src/agent/react_loop.py tests/
git commit -m "refactor: 移除Orchestrator委托wrapper，调用方直接访问manager（Task 20）"
```

---

## Task 21: 从 react_loop.py 提取 ToolExecutor

**Files:**
- Create: `src/agent/tool_executor.py`
- Modify: `src/agent/react_loop.py`

**Interfaces:**
- Produces: `ToolExecutor` 类，封装工具执行逻辑

- [ ] **Step 1: 识别工具执行逻辑边界**

react_loop.py 中的工具执行逻辑包括：
- 单工具重试检测（recent_calls, is_stuck 检测）
- 卡死硬终止（warned_pairs）
- 安全拦截（PolicyEngine confirm）
- 工具调用执行（registry.execute_tool）
- 工具结果缓存（tool_result_cache）

- [ ] **Step 2: 写失败测试**

```python
# tests/test_tool_executor.py
"""ToolExecutor 单元测试。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import pytest
from unittest.mock import AsyncMock, MagicMock
from agent.tool_executor import ToolExecutor

class TestToolExecutor:
    def test_creation(self):
        registry = MagicMock()
        executor = ToolExecutor(registry=registry, max_retries=2)
        assert executor.max_retries == 2

    async def test_execute_tool_success(self):
        registry = MagicMock()
        registry.execute_tool.return_value = "result"
        executor = ToolExecutor(registry=registry)
        result = await executor.execute("search", {"q": "test"}, session_id="s1")
        assert result == "result"
```

- [ ] **Step 3: 运行测试验证失败**

Run: `python -m pytest tests/test_tool_executor.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 4: 实现 ToolExecutor**

从 react_loop.py 提取工具执行相关逻辑到 `src/agent/tool_executor.py`。

- [ ] **Step 5: react_loop.py 委托到 ToolExecutor**

- [ ] **Step 6: 运行测试验证通过**

Run: `python -m pytest tests/test_tool_executor.py tests/test_react_loop_continuation.py -x -q`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add src/agent/tool_executor.py src/agent/react_loop.py tests/test_tool_executor.py
git commit -m "refactor: 从react_loop提取ToolExecutor（Task 21）"
```

---

## Task 22: 从 react_loop.py 提取 StreamHandler

**Files:**
- Create: `src/agent/stream_handler.py`
- Modify: `src/agent/react_loop.py`

**Interfaces:**
- Produces: `StreamHandler` 类，封装 SSE 流处理逻辑

- [ ] **Step 1: 识别流处理逻辑边界**

react_loop.py 中的流处理逻辑包括：
- chunk 拼接（text accumulation）
- 中断处理（cancel_event 轮询）
- SSE 事件转发（tool_use, tool_result, text 事件）
- stream.close() 调用

- [ ] **Step 2-7: 同 Task 21 的 TDD 流程**

- [ ] **Step 7: 提交**

```bash
git add src/agent/stream_handler.py src/agent/react_loop.py tests/test_stream_handler.py
git commit -m "refactor: 从react_loop提取StreamHandler（Task 22）"
```

---

## Task 23: 从 builtin_tools.py 迁移代码到 tools/

**Files:**
- Modify: `src/agent/tools/file_tools.py`, `shell_tools.py`, `web_tools.py`, `memory_tools.py`, `plan_tools.py`
- Delete: `src/agent/builtin_tools.py` (最终删除)

**Interfaces:**
- Produces: tools/ 包含实际代码（非 re-export），builtin_tools.py 删除

- [ ] **Step 1: 迁移 file_tools.py**

将 builtin_tools.py 中的以下函数 + register_file_tools 迁移到 tools/file_tools.py：
- read_file, write_file, delete_file, list_directory, file_edit, file_glob, file_grep
- register_file_tools（含内部嵌套函数 _file_list_uploads, _file_query, _file_read_uploaded）

- [ ] **Step 2: 迁移 shell_tools.py**

将以下函数迁移：
- _has_shell_metachar, _set_running_proc, _get_and_clear_running_proc, kill_running_process
- _execute_command_inner, register_bash_tool
- 模块级状态：_running_proc, _running_proc_lock, _SHELL_META_RE

- [ ] **Step 3: 迁移 web_tools.py**

将以下函数迁移：
- _html_to_plain_text, _extract_domain, _load_domain_state, _save_domain_state
- _classify_quality, _build_enhanced_headers, _persist_success_headers, _search_baidu
- http_request, web_search
- 模块级常量：_DEFAULT_HTTP_HEADERS, _MAX_HTTP_BODY_CHARS, _DOMAIN_STATE_PATH 等

- [ ] **Step 4: 迁移 memory_tools.py**

将 register_memory_tools 及其内部嵌套函数迁移。

- [ ] **Step 5: 迁移 plan_tools.py**

将 register_plan_tools 及其内部嵌套函数迁移。

- [ ] **Step 6: 更新所有导入方**

```bash
# 查找所有 from agent.builtin_tools import 或 from .builtin_tools import
python -c "
import os
for root, dirs, files in os.walk('src'):
    for f in files:
        if f.endswith('.py'):
            path = os.path.join(root, f)
            with open(path, encoding='utf-8') as fh:
                content = fh.read()
                if 'builtin_tools' in content:
                    print(path)
"
```

将 `from agent.builtin_tools import xxx` 改为 `from agent.tools.file_tools import xxx` 等。

- [ ] **Step 7: 删除 builtin_tools.py**

- [ ] **Step 8: 运行全量测试**

Run: `python -m pytest tests/ -x -q --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 3`
Expected: 无新增 failure

- [ ] **Step 9: 提交**

```bash
git add src/agent/tools/ src/orchestrator.py
git rm src/agent/builtin_tools.py
git commit -m "refactor: 迁移builtin_tools代码到tools/包，删除原文件（Task 23）"
```

---

## Task 24: 最终验证

**Files:**
- 无修改，仅验证

- [ ] **Step 1: 验证文件行数**

Run:
```bash
python -c "
import os
files = {
    'src/server.py': 100,
    'src/orchestrator.py': 350,
    'src/agent/react_loop.py': 500,
}
for f, target in files.items():
    if os.path.exists(f):
        lines = len(open(f, encoding='utf-8').readlines())
        status = 'PASS' if lines <= target else 'FAIL'
        print(f'{f}: {lines} lines (target <{target}) [{status}]')

# builtin_tools.py 应已删除
bt = 'src/agent/builtin_tools.py'
print(f'{bt}: {\"DELETED\" if not os.path.exists(bt) else \"STILL EXISTS\"}')
"
```

- [ ] **Step 2: 运行全量测试**

Run: `python -m pytest tests/ -q --tb=no --ignore=tests/test_monitor_page.py 2>&1 | Select-Object -Last 3`
Expected: passed >= 2588, failed <= 106（无新增 failure）

- [ ] **Step 3: 验证热重载端到端**

Run: `python -m pytest tests/test_hot_reload.py -v`
Expected: 所有热重载测试通过

- [ ] **Step 4: 验证容器注册**

Run: `python -m pytest tests/test_container.py -v`
Expected: 所有容器测试通过

- [ ] **Step 5: 最终提交（如有修复）**

```bash
git add -A
git commit -m "refactor: 架构重构收尾完成（Task 24）" -m "server.py <100行, orchestrator.py <350行, react_loop.py <500行, builtin_tools.py 删除"
```

---

## 验证标准

| 文件 | 目标行数 | 当前行数 |
|------|----------|----------|
| server.py | <100 | 4578 |
| orchestrator.py | <350 | 2100 |
| react_loop.py | <500 | 2164 |
| builtin_tools.py | 删除 | 2304 |
| app.py | ~300 | 135 |

全量测试：passed >= 2588，failed <= 106（无新增 failure）。
