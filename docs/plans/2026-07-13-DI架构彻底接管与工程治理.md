# DI 架构彻底接管与工程治理 Implementation Plan

> **For agentic workers:** Use the java-dev-skills workflow to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 DI 容器真正承担依赖注入职责，彻底解决三重状态存储、工厂路径错误、导入样板、有状态后台 task 管理缺失四大系统诟病。

**Architecture:** Container 作为唯一真相源，通过 `container.get()` / `set_instance()` 管理所有组件。路由通过 FastAPI `Depends(get_xxx)` 类型安全注入。lifespan 简化为"调 container.get() + 异步预热 + 后台 task 启动"。统一 `python -m hermes` 包入口，删除 51 处双重 try/except 导入样板。

**Tech Stack:** Python 3.11+, FastAPI, uvicorn, pytest, asyncio

## Global Constraints

- **TDD 流程**：每个改动先写失败测试 → 验证失败 → 最小实现 → 验证通过 → 重构 → 提交
- **没有失败测试就不写生产代码**
- **路由通过 `Depends(get_xxx)` 访问组件**，不搞混合模式
- **Orchestrator 作为整体注册**，内部组件对容器透明
- **有状态组件（hot_reloadable=False）不参与 reload**，由软重启手动重建
- **容器注册阶段应在路由拆分阶段之前执行**
- **配置段到组件的映射**保持不变（见 container.py CONFIG_TO_COMPONENTS）
- **测试基线**：2661 passed, 124 failed（均为重构前遗留问题，无新增失败）
- **LLM 客户端需使用异步 SDK**，全链路异步处理避免阻塞事件循环
- **工具调用失败重试预算为 2 次**，第 3 次相同失败立即终止
- **API 密钥不应硬编码**，使用环境变量占位符
- **当前工作目录**：`e:\Java\webser\web_app\webme\hermes-lite`
- **src/ 目录**为当前源码根，Phase 3 将重命名为 `hermes/`
- **测试运行命令**：`python -m pytest tests/ -x -q`（Windows 环境）

---

## File Structure

### Phase 1 修改的文件

| 文件 | 职责 | 改动类型 |
|------|------|---------|
| `src/app.py` | 工厂函数 + Depends 函数 + 路由注册 | 修改：修复 10 个工厂路径 + HealthChecker 参数 |
| `src/container.py` | DI 容器 | 修改：reload 方法过滤 hot_reloadable=False |
| 根目录散落文件 | 调试脚本/日志 | 删除：19 个脚本 + 散落数据文件 |
| `.gitignore` | Git 忽略规则 | 修改：补充备份目录和脚本模式 |

### Phase 2 修改/新增的文件

| 文件 | 职责 | 改动类型 |
|------|------|---------|
| `src/background_task_registry.py` | 后台 task 注册/取消/重启 | 新增 |
| `src/app.py` | 添加 11 个 Depends 函数 | 修改 |
| `src/background_loops.py` | 3 个后台循环 | 修改：参数注入 + asyncio.Event + 删除 _get_server_globals |
| `src/lifespan.py` | FastAPI lifespan | 修改：796→~200 行，调 container.get() |
| `src/server.py` | HTTP 入口 | 修改：删除全局变量 + re-export + __main__ |
| `src/state.py` | PEP 562 代理 | 删除 |
| `src/routes/health.py` | 健康检查路由 | 修改：Depends + asyncio.Event |
| `src/routes/misc.py` | misc 路由 | 修改：Depends + 软重启改造 |
| `src/routes/chat.py` | 对话路由 | 修改：Depends |
| `src/routes/sessions.py` | 会话路由 | 修改：Depends |
| `src/routes/skills.py` | 技能路由 | 修改：Depends |
| `src/routes/files.py` | 文件路由 | 修改：Depends |
| `src/routes/proposals.py` | 提议路由 | 修改：Depends |
| `src/routes/schedules.py` | 调度路由 | 修改：Depends |
| `src/routes/memory.py` | 记忆路由 | 修改：Depends |
| `src/routes/cron_tools.py` | cron 工具路由 | 修改：Depends |
| `src/routes/approvals.py` | 审批路由 | 修改：Depends |
| `tests/test_config_update.py` | 配置更新测试 | 修改：24 处 patch → dependency_overrides |
| `tests/test_server.py` | server 路由测试 | 修改：15 处 patch → dependency_overrides / 源模块 |
| `tests/test_memory_dashboard_api.py` | 记忆仪表盘测试 | 修改：5 处 patch → dependency_overrides |
| `tests/test_schedule_history_memories.py` | 调度历史测试 | 修改：6 处 patch → dependency_overrides |
| `tests/test_sse_todo_events.py` | SSE todo 测试 | 修改：4 处 patch → dependency_overrides |
| `tests/test_skill_rest_endpoints.py` | 技能端点测试 | 修改：4 处 patch → dependency_overrides |
| `tests/test_task_api.py` | 任务 API 测试 | 修改：2 处 patch → dependency_overrides |

### Phase 3 修改/新增的文件

| 文件 | 职责 | 改动类型 |
|------|------|---------|
| `hermes/` (原 `src/`) | 包目录 | 重命名 |
| `hermes/__main__.py` | python -m hermes 入口 | 新增 |
| `hermes/__init__.py` | 包初始化 | 新增 |
| 28 个源文件 | 双重 try/except 样板 | 修改：51 处改绝对导入 |
| `desktop/src-tauri/src/sidecar.rs` | Tauri sidecar | 修改：启动命令 |
| `deploy.sh` / `start.sh` / `restart.ps1` / `restart.sh` | 部署脚本 | 修改：启动命令 |
| `hermes-lite.service` | systemd 服务 | 修改：ExecStart |
| `tests/conftest.py` | 测试配置 | 新增：sys.path 设置 |

---

## Task 1: 修复 app.py 10 个工厂导入路径 + HealthChecker 参数

**Files:**
- Modify: `src/app.py:204-368`（10 个工厂函数 + HealthChecker）
- Test: `tests/test_factory_paths.py`（新增）

**Interfaces:**
- Consumes: `src/container.py` Container 类
- Produces: 修复后的 `_create_*` 工厂函数，路径全部正确，HealthChecker 接收 6 参数

- [ ] **Step 1: 写失败测试 — 验证工厂路径正确**

```python
# tests/test_factory_paths.py
"""测试 app.py 工厂函数导入路径正确性。

spec 2026-07-13 阶段 1：修复 10 个工厂导入路径 + HealthChecker 参数。
"""
from __future__ import annotations
import sys
import os
from unittest.mock import patch, MagicMock

# 确保 src 在 path 中
_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestFactoryImportPaths:
    """验证每个工厂函数使用正确的导入路径。"""

    def test_session_logger_path(self):
        """_create_session_logger 应从 storage.sqlite_log 导入。"""
        from app import _create_session_logger
        with patch("storage.sqlite_log.SessionLogger") as mock_cls:
            _create_session_logger({"storage": {"sqlite_path": "data/test.db"}})
            mock_cls.assert_called_once_with(db_path="data/test.db")

    def test_metrics_store_path(self):
        """_create_metrics_store 应从 monitoring.metrics_store 导入。"""
        from app import _create_metrics_store
        with patch("monitoring.metrics_store.MetricsStore") as mock_cls:
            result = _create_metrics_store(
                {"monitoring": {"enabled": True, "daily_persistence": True},
                 "storage": {"sqlite_path": "data/test.db"}}
            )
            mock_cls.assert_called_once_with(db_path="data/test.db")

    def test_audit_logger_path(self):
        """_create_audit_logger 应从 agent.audit 导入。"""
        from app import _create_audit_logger
        with patch("agent.audit.AuditLogger") as mock_cls:
            _create_audit_logger({"monitoring": {"enabled": True}})
            mock_cls.assert_called_once()

    def test_approval_manager_path(self):
        """_create_approval_manager 应从 agent.approval 导入。"""
        from app import _create_approval_manager
        with patch("agent.approval.ApprovalManager") as mock_cls:
            _create_approval_manager({"security": {"approval_timeout_seconds": 300}})
            mock_cls.assert_called_once()

    def test_stream_manager_path(self):
        """_create_stream_manager 应从 stream_manager 导入（根级模块）。"""
        from app import _create_stream_manager
        with patch("stream_manager.StreamManager") as mock_cls:
            _create_stream_manager()
            mock_cls.assert_called_once()

    def test_skill_loader_path(self):
        """_create_skill_loader 应从 skill.loader 导入。"""
        from app import _create_skill_loader
        with patch("skill.loader.SkillLoader") as mock_cls:
            _create_skill_loader()
            mock_cls.assert_called_once()

    def test_proposal_store_path(self):
        """_create_proposal_store 应从 agent.cron_proposals 导入。"""
        from app import _create_proposal_store
        with patch("agent.cron_proposals.ProposalStore") as mock_cls:
            _create_proposal_store()
            mock_cls.assert_called_once()

    def test_mcp_manager_path(self):
        """_create_mcp_manager 应从 mcp.manager 导入。"""
        from app import _create_mcp_manager
        with patch("mcp.manager.MCPManager") as mock_cls:
            _create_mcp_manager({"skills": {}})
            mock_cls.assert_called_once()

    def test_etl_engine_paths(self):
        """_create_etl_engine 应从 files.parser + files.chunker 导入。"""
        from app import _create_etl_engine
        with patch("files.parser.WaterfallParser") as mock_parser, \
             patch("files.chunker.DocumentChunker") as mock_chunker, \
             patch("files.etl_engine.ETLEngine") as mock_etl:
            mock_orch = MagicMock()
            _create_etl_engine({"files": {}}, upload_manager=None, orchestrator=mock_orch)
            mock_parser.assert_called_once()
            mock_chunker.assert_called_once()
            mock_etl.assert_called_once()

    def test_cron_scheduler_path(self):
        """_create_cron_scheduler 应从 tasks.scheduler 导入。"""
        from app import _create_cron_scheduler
        with patch("tasks.scheduler.CronScheduler") as mock_cls:
            _create_cron_scheduler({"tasks": {}})
            mock_cls.assert_called_once()


class TestHealthCheckerFactory:
    """验证 HealthChecker 工厂补全为 6 参数。"""

    def test_health_checker_receives_six_args(self):
        """_create_health_checker 应传入 6 个参数给 HealthChecker。"""
        from app import _create_health_checker
        with patch("monitoring.health.HealthChecker") as mock_cls:
            mock_orch = MagicMock()
            mock_session = MagicMock()
            mock_mcp = MagicMock()
            mock_skill = MagicMock()
            mock_metrics = MagicMock()
            mock_proposal = MagicMock()
            _create_health_checker(
                orchestrator=mock_orch,
                session_logger=mock_session,
                mcp_manager=mock_mcp,
                skill_loader=mock_skill,
                metrics_collector=mock_metrics,
                proposal_store=mock_proposal,
            )
            mock_cls.assert_called_once_with(
                orchestrator=mock_orch,
                session_logger_global=mock_session,
                mcp_manager=mock_mcp,
                skill_loader=mock_skill,
                metrics_collector=mock_metrics,
                proposal_store=mock_proposal,
            )
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_factory_paths.py -x -q`
Expected: FAIL（工厂路径仍为错误路径，patch 目标不匹配）

- [ ] **Step 3: 修复 10 个工厂路径**

修改 `src/app.py` 中以下工厂函数的导入路径：

```python
# 1. _create_session_logger (line ~206)
# 修复前: from monitoring.session_logger import SessionLogger
# 修复后:
from storage.sqlite_log import SessionLogger

# 2. _create_metrics_store (line ~229)
# 修复前: from monitoring.metrics import MetricsStore
# 修复后:
from monitoring.metrics_store import MetricsStore

# 3. _create_audit_logger (line ~239)
# 修复前: from monitoring.audit import AuditLogger
# 修复后:
from agent.audit import AuditLogger

# 4. _create_approval_manager (line ~251)
# 修复前: from security.approval_manager import ApprovalManager
# 修复后:
from agent.approval import ApprovalManager

# 5. _create_stream_manager (line ~271)
# 修复前: from agent.stream_manager import StreamManager
# 修复后:
from stream_manager import StreamManager

# 6. _create_skill_loader (line ~278)
# 修复前: from skills.skill_loader import SkillLoader
# 修复后:
from skill.loader import SkillLoader

# 7. _create_proposal_store (line ~287)
# 修复前: from cron.proposal_store import ProposalStore
# 修复后:
from agent.cron_proposals import ProposalStore

# 8. _create_mcp_manager (line ~296)
# 修复前: from skills.mcp_manager import MCPManager
# 修复后:
from mcp.manager import MCPManager

# 9. _create_etl_engine (line ~325-326)
# 修复前:
#   from files.waterfall_parser import WaterfallParser
#   from files.document_chunker import DocumentChunker
# 修复后:
from files.parser import WaterfallParser
from files.chunker import DocumentChunker

# 10. _create_cron_scheduler (line ~356)
# 修复前: from cron.cron_scheduler import CronScheduler
# 修复后:
from tasks.scheduler import CronScheduler
```

- [ ] **Step 4: 修复 HealthChecker 工厂参数**

修改 `src/app.py:362-368`：

```python
# 修复前
def _create_health_checker(orchestrator=None, session_logger=None, mcp_manager=None):
    """创建 HealthChecker（无配置依赖，依赖运行时组件）。"""
    try:
        from monitoring.health import HealthChecker
        return HealthChecker()
    except Exception:
        return None

# 修复后
def _create_health_checker(orchestrator=None, session_logger=None, mcp_manager=None,
                           skill_loader=None, metrics_collector=None, proposal_store=None):
    """创建 HealthChecker（无配置依赖，依赖运行时组件）。

    需补全 6 个参数，否则 HealthChecker.__init__ 的 orchestrator 必填参数
    会抛 TypeError，被 except 静默吞掉导致工厂永远返回 None。
    """
    try:
        from monitoring.health import HealthChecker
        return HealthChecker(
            orchestrator=orchestrator,
            session_logger_global=session_logger,
            mcp_manager=mcp_manager,
            skill_loader=skill_loader,
            metrics_collector=metrics_collector,
            proposal_store=proposal_store,
        )
    except Exception:
        return None
```

- [ ] **Step 5: 更新 register_components 中 health_checker 的工厂 lambda**

修改 `src/app.py:448-450`：

```python
# 修复前
container.register("health_checker",
    lambda c: _create_health_checker(c.get("orchestrator"), c.get("session_logger"), c.get("mcp_manager")),
    deps=["orchestrator", "session_logger", "mcp_manager"], hot_reloadable=False)

# 修复后
container.register("health_checker",
    lambda c: _create_health_checker(
        orchestrator=c.get("orchestrator"),
        session_logger=c.get("session_logger"),
        mcp_manager=c.get("mcp_manager"),
        skill_loader=c.get("skill_loader"),
        metrics_collector=c.get("metrics_collector"),
        proposal_store=c.get("proposal_store"),
    ),
    deps=["orchestrator", "session_logger", "mcp_manager", "skill_loader",
          "metrics_collector", "proposal_store"],
    hot_reloadable=False)
```

- [ ] **Step 6: 运行测试验证通过**

Run: `python -m pytest tests/test_factory_paths.py -x -q`
Expected: PASS（全部 11 个测试通过）

- [ ] **Step 7: 运行现有测试确保无回归**

Run: `python -m pytest tests/test_container.py tests/test_hot_reload.py -x -q`
Expected: PASS（36/36 通过，无新增失败）

- [ ] **Step 8: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/app.py tests/test_factory_paths.py
git commit -m "fix(arch): 修复 10 个工厂导入路径 + HealthChecker 工厂参数补全

- app.py 工厂路径：monitoring.session_logger → storage.sqlite_log 等 10 处
- HealthChecker 工厂从零参数补全为 6 参数（orchestrator/session_logger/
  mcp_manager/skill_loader/metrics_collector/proposal_store）
- register_components 中 health_checker 的 deps 同步更新
- 消除热重载静默用 None 替换组件的定时炸弹"
```

---

## Task 2: 修复 container.py hot_reloadable 死代码

**Files:**
- Modify: `src/container.py:128-175`（reload 方法）
- Test: `tests/test_hot_reloadable_filter.py`（新增）

**Interfaces:**
- Consumes: `src/container.py` Container 类
- Produces: reload 方法过滤 hot_reloadable=False 组件，返回跳过列表

- [ ] **Step 1: 写失败测试 — hot_reloadable=False 组件不被 reload 重建**

```python
# tests/test_hot_reloadable_filter.py
"""测试 container.py reload 方法过滤 hot_reloadable=False 组件。

spec 2026-07-13 阶段 1：修复 hot_reloadable 死代码。
"""
from __future__ import annotations
import sys
import os

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)

from container import Container


class TestHotReloadableFilter:
    """验证 reload 跳过 hot_reloadable=False 的组件。"""

    def test_reload_skips_non_reloadable(self):
        """hot_reloadable=False 的组件不被 reload 重建。"""
        container = Container({"llm": {"timeout": 60}, "monitoring": {"enabled": True}})

        create_count = {"stateful": 0, "stateless": 0}

        def factory_stateful(c):
            create_count["stateful"] += 1
            return f"stateful_v{create_count['stateful']}"

        def factory_stateless(c):
            create_count["stateless"] += 1
            return f"stateless_v{create_count['stateless']}"

        container.register("stateful", factory_stateful, deps=[], hot_reloadable=False)
        container.register("stateless", factory_stateless, deps=[], hot_reloadable=True)

        # 初始创建
        assert container.get("stateful") == "stateful_v1"
        assert container.get("stateless") == "stateless_v1"

        # reload — 只有 stateless 被重建
        # 使用 monitoring 段（映射到 metrics_collector 等），但这里直接用自定义
        # 需要 mock CONFIG_TO_COMPONENTS 或用现有映射
        # 更简单：直接测试 reload 行为
        rebuilt = container.reload({"monitoring"}, {"monitoring": {"enabled": False}})

        # stateless 被重建（如果它在 affected 列表中）
        # stateful 不被重建（hot_reloadable=False）
        assert "stateful" not in rebuilt or create_count["stateful"] == 1

    def test_reload_rebuilds_reloadable(self):
        """hot_reloadable=True 的组件被 reload 重建。"""
        container = Container({"monitoring": {"enabled": True}})

        create_count = {"count": 0}

        def factory(c):
            create_count["count"] += 1
            return f"v{create_count['count']}"

        container.register("metrics_collector", factory, deps=[], hot_reloadable=True)

        assert container.get("metrics_collector") == "v1"

        rebuilt = container.reload({"monitoring"}, {"monitoring": {"enabled": False}})
        assert "metrics_collector" in rebuilt
        assert container.get("metrics_collector") == "v2"

    def test_orchestrator_not_rebuilt_on_llm_change(self):
        """llm 段变更时 orchestrator（hot_reloadable=False）不被重建。"""
        container = Container({"llm": {"timeout": 60}})

        create_count = {"count": 0}

        def factory(c):
            create_count["count"] += 1
            return f"orch_v{create_count['count']}"

        container.register("orchestrator", factory,
                           deps=["metrics_collector"], hot_reloadable=False)
        container.register("metrics_collector", lambda c: "mc",
                           deps=[], hot_reloadable=True)

        assert container.get("orchestrator") == "orch_v1"

        rebuilt = container.reload({"llm"}, {"llm": {"timeout": 120}})
        assert "orchestrator" not in rebuilt
        assert container.get("orchestrator") == "orch_v1"  # 未重建
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_hot_reloadable_filter.py -x -q`
Expected: FAIL（reload 方法未过滤 hot_reloadable=False 组件）

- [ ] **Step 3: 修改 container.py reload 方法**

修改 `src/container.py:128-138`：

```python
# 修复前
def reload(self, changed_sections: set[str], new_config: dict) -> list[str]:
    """原子性重建:先创建全部新实例,全部成功后才替换,失败则回滚。"""
    with self._lock:
        directly_affected = set()
        for section in changed_sections:
            directly_affected.update(CONFIG_TO_COMPONENTS.get(section, []))

        to_rebuild = self._topo_sort_dependents(directly_affected)

# 修复后
def reload(self, changed_sections: set[str], new_config: dict) -> list[str]:
    """原子性重建:先创建全部新实例,全部成功后才替换,失败则回滚。

    hot_reloadable=False 的组件不参与重建（由软重启手动处理）。
    """
    with self._lock:
        directly_affected = set()
        for section in changed_sections:
            directly_affected.update(CONFIG_TO_COMPONENTS.get(section, []))

        # 过滤掉 hot_reloadable=False 的组件
        reloadable_affected = {
            name for name in directly_affected
            if self._hot_reloadable.get(name, True)
        }

        to_rebuild = self._topo_sort_dependents(reloadable_affected)
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_hot_reloadable_filter.py -x -q`
Expected: PASS

- [ ] **Step 5: 运行现有测试确保无回归**

Run: `python -m pytest tests/test_container.py tests/test_hot_reload.py -x -q`
Expected: PASS（36/36 通过）

- [ ] **Step 6: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/container.py tests/test_hot_reloadable_filter.py
git commit -m "fix(arch): 修复 hot_reloadable 死代码，reload 跳过 False 组件

- container.py reload 方法：过滤 hot_reloadable=False 的组件
- 有状态组件（orchestrator/cron_scheduler/stream_manager/skill_loader/
  proposal_store/health_checker）不参与 reload，由软重启手动重建
- 消除有状态组件被误重建的风险"
```

---

## Task 3: 清理根目录散落文件 + 更新 .gitignore

**Files:**
- Delete: 19 个调试脚本 + 散落数据/日志文件
- Modify: `.gitignore`
- Move: `hermes-lite.bak.20260709-183707/` 移出工作树

**Interfaces:**
- Consumes: 无
- Produces: 干净的根目录

- [ ] **Step 1: 验证待删除文件存在**

Run:
```bash
cd e:\Java\webser\web_app\webme\hermes-lite
dir analyze_log.py bili_wbi_test.py bili_wbi_test2.py bili_wbi_test3.py check_sessions.py cleanup_user_profile_vectors.py debug_script.py edge_test.py fix2.py fix_builtin.py wbi_test.py _check_imgtext.py _test_dict.py _test_full.py _test_mkldnn.py _test_ocr.py _test_pir.py _v3_signal_pool.py 2>nul
```
Expected: 文件列表（确认存在）

- [ ] **Step 2: 删除 19 个调试脚本**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
del analyze_log.py bili_wbi_test.py bili_wbi_test2.py bili_wbi_test3.py check_sessions.py cleanup_user_profile_vectors.py debug_script.py edge_test.py fix2.py fix_builtin.py wbi_test.py _check_imgtext.py _test_dict.py _test_full.py _test_mkldnn.py _test_ocr.py _test_pir.py _v3_signal_pool.py
```

注意：`package_deploy.py` 保留（可能有部署用途），评估后决定。

- [ ] **Step 3: 删除散落数据/日志文件**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
del bili_videos_out.json debug.log debug_region.txt output.txt out_parse.txt search_params.txt server_err.txt server_err_new.log server_out.txt server_out_new.log sv.txt sv2_err.txt sv3_err.txt test_ux_actions.png "$null" 2>nul
```

- [ ] **Step 4: 移动备份目录到工作树外**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
move hermes-lite.bak.20260709-183707 ..\hermes-lite.bak.20260709-183707_archived
```

- [ ] **Step 5: 更新 .gitignore**

在 `.gitignore` 末尾追加：

```
# 架构清理：备份目录和调试脚本
hermes-lite.bak.*/
hermes-lite/_test_*.py
hermes-lite/bili_*.py
hermes-lite/debug_*.txt
hermes-lite/server_*.txt
hermes-lite/sv*.txt
```

- [ ] **Step 6: 验证根目录仅剩生产文件**

Run: `dir *.py /b`
Expected: 仅生产文件（server.py 已迁移到 src/，根目录应无 .py 或仅有 package_deploy.py）

- [ ] **Step 7: 运行测试确保无回归**

Run: `python -m pytest tests/ -x -q --timeout=60 2>&1 | Select-Object -Last 5`
Expected: 无新增失败（2661 passed, 124 failed 基线不变）

- [ ] **Step 8: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add -A
git commit -m "chore: 清理根目录 19 个调试脚本 + 散落日志 + 备份目录

- 删除 analyze_log.py / bili_wbi_test*.py / debug_script.py 等 19 个脚本
- 删除 bili_videos_out.json / debug.log / server_out.txt 等散落数据文件
- hermes-lite.bak.20260709-183707/ 移出工作树
- .gitignore 补充备份目录和调试脚本模式
- 改善工程卫生"
```

---

## Task 4: 新增 BackgroundTaskRegistry 模块

**Files:**
- Create: `src/background_task_registry.py`
- Test: `tests/test_background_task_registry.py`（新增）

**Interfaces:**
- Consumes: 无
- Produces: `BackgroundTaskRegistry` 类，方法：`register(name, task)` / `get(name)` / `restart(name, factory)` / `cancel_all()`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_background_task_registry.py
"""测试 BackgroundTaskRegistry：后台 task 注册/取消/重启。

spec 2026-07-13 阶段 2：新增 BackgroundTaskRegistry 解决 reload/软重启后
后台 task 持有旧组件引用的问题。
"""
from __future__ import annotations
import sys
import os
import asyncio
import pytest

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)

from background_task_registry import BackgroundTaskRegistry


@pytest.mark.asyncio
class TestBackgroundTaskRegistry:

    async def test_register_and_get(self):
        """注册 task 后可通过名字获取。"""
        registry = BackgroundTaskRegistry()

        async def dummy():
            await asyncio.sleep(100)

        task = asyncio.create_task(dummy())
        registry.register("test", task)
        assert registry.get("test") is task
        task.cancel()
        try:
            await task
        except asyncio.CancelledError:
            pass

    async def test_register_replaces_existing(self):
        """注册同名 task 时取消旧的。"""
        registry = BackgroundTaskRegistry()

        async def dummy():
            await asyncio.sleep(100)

        old_task = asyncio.create_task(dummy())
        registry.register("test", old_task)

        new_task = asyncio.create_task(dummy())
        registry.register("test", new_task)

        assert registry.get("test") is new_task
        assert old_task.cancelled()
        new_task.cancel()
        try:
            await new_task
        except asyncio.CancelledError:
            pass

    async def test_restart_with_factory(self):
        """restart 用 factory 创建新 task，取消旧的。"""
        registry = BackgroundTaskRegistry()

        call_count = {"n": 0}

        async def factory():
            call_count["n"] += 1

        old_task = asyncio.create_task(asyncio.sleep(100))
        registry.register("test", old_task)

        await registry.restart("test", factory)

        new_task = registry.get("test")
        assert new_task is not old_task
        assert old_task.cancelled()
        await new_task  # 等 factory 完成
        assert call_count["n"] == 1

    async def test_cancel_all(self):
        """cancel_all 取消所有注册的 task。"""
        registry = BackgroundTaskRegistry()

        t1 = asyncio.create_task(asyncio.sleep(100))
        t2 = asyncio.create_task(asyncio.sleep(100))
        registry.register("t1", t1)
        registry.register("t2", t2)

        await registry.cancel_all()

        assert t1.cancelled()
        assert t2.cancelled()
        assert registry.get("t1") is None or registry.get("t1").cancelled()

    async def test_restart_nonexistent(self):
        """restart 不存在的 task 时正常创建新的。"""
        registry = BackgroundTaskRegistry()

        async def factory():
            pass

        await registry.restart("new", factory)
        assert registry.get("new") is not None
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_background_task_registry.py -x -q`
Expected: FAIL（模块不存在，ImportError）

- [ ] **Step 3: 创建 BackgroundTaskRegistry 模块**

```python
# src/background_task_registry.py
"""后台 asyncio task 注册表，支持按名取消和重启。

用于解决 reload/软重启后台 task 持有旧组件引用的问题。
spec 2026-07-13 阶段 2：新增。
"""
from __future__ import annotations

import asyncio
import logging
from typing import Callable, Dict, Optional

logger = logging.getLogger(__name__)


class BackgroundTaskRegistry:
    """后台 asyncio task 注册表，支持按名取消和重启。"""

    def __init__(self):
        self._tasks: Dict[str, asyncio.Task] = {}

    def register(self, name: str, task: asyncio.Task) -> None:
        """注册后台 task。若同名 task 已存在，先取消旧的。"""
        old = self._tasks.get(name)
        if old is not None and not old.done():
            old.cancel()
        self._tasks[name] = task

    def get(self, name: str) -> Optional[asyncio.Task]:
        return self._tasks.get(name)

    async def restart(self, name: str, factory: Callable) -> None:
        """重启指定 task：取消旧的，用 factory 创建新的。

        factory 是无参 callable，返回 coroutine。
        """
        old = self._tasks.get(name)
        if old is not None and not old.done():
            old.cancel()
            try:
                await asyncio.wait_for(old, timeout=5.0)
            except (asyncio.TimeoutError, asyncio.CancelledError):
                pass

        new_task = asyncio.create_task(factory())
        self._tasks[name] = new_task
        logger.info("后台 task '%s' 已重启", name)

    async def cancel_all(self) -> None:
        """取消所有后台 task（用于关闭）。"""
        for name, task in self._tasks.items():
            if not task.done():
                task.cancel()
                try:
                    await asyncio.wait_for(task, timeout=5.0)
                except (asyncio.TimeoutError, asyncio.CancelledError):
                    pass
        self._tasks.clear()
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_background_task_registry.py -x -q`
Expected: PASS（5/5 通过）

- [ ] **Step 5: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/background_task_registry.py tests/test_background_task_registry.py
git commit -m "feat(di): 新增 BackgroundTaskRegistry 后台 task 注册表

- 支持按名注册/取消/重启后台 asyncio task
- 解决 reload/软重启后台 task 持有旧组件引用的问题
- restart 方法：取消旧 task（5s 超时），用 factory 创建新 task
- cancel_all 方法：关闭时优雅取消所有 task"
```

---

## Task 5: 在 app.py 添加 11 个 Depends 函数

**Files:**
- Modify: `src/app.py`（添加 Depends 函数区块）
- Test: `tests/test_depends_functions.py`（新增）

**Interfaces:**
- Consumes: `src/container.py` Container 类，`src/app.py` get_container()
- Produces: `get_container_or_raise()` / `get_orchestrator()` / `get_session_logger()` 等 15 个 Depends 函数

- [ ] **Step 1: 写失败测试**

```python
# tests/test_depends_functions.py
"""测试 app.py Depends 函数正确返回容器组件。

spec 2026-07-13 阶段 2：添加 11+ 个 Depends 函数供路由类型安全注入。
"""
from __future__ import annotations
import sys
import os
from unittest.mock import MagicMock

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestDependsFunctions:

    def test_get_container_or_raise_uninitialized(self):
        """容器未初始化时 get_container_or_raise 抛 RuntimeError。"""
        from app import get_container
        # 确保容器未初始化
        import app
        app._container = None
        try:
            app.get_container_or_raise()
            assert False, "应抛 RuntimeError"
        except RuntimeError as e:
            assert "未初始化" in str(e)

    def test_get_orchestrator(self):
        """get_orchestrator 返回容器中的 orchestrator。"""
        import app
        mock_container = MagicMock()
        mock_orch = MagicMock()
        mock_container.get.return_value = mock_orch
        app._container = mock_container

        result = app.get_orchestrator()
        mock_container.get.assert_called_once_with("orchestrator")
        assert result is mock_orch

    def test_get_session_logger(self):
        """get_session_logger 返回容器中的 session_logger。"""
        import app
        mock_container = MagicMock()
        mock_logger = MagicMock()
        mock_container.get.return_value = mock_logger
        app._container = mock_container

        result = app.get_session_logger()
        mock_container.get.assert_called_once_with("session_logger")
        assert result is mock_logger

    def test_all_depends_functions_exist(self):
        """验证所有 15 个 Depends 函数都存在。"""
        import app
        expected = [
            "get_container_or_raise",
            "get_orchestrator",
            "get_session_logger",
            "get_metrics_collector",
            "get_metrics_store",
            "get_audit_logger",
            "get_approval_manager",
            "get_task_manager",
            "get_stream_manager",
            "get_skill_loader",
            "get_mcp_manager",
            "get_upload_manager",
            "get_etl_engine",
            "get_cron_scheduler",
            "get_proposal_store",
            "get_health_checker",
        ]
        for name in expected:
            assert hasattr(app, name), f"app.{name} 不存在"
            assert callable(getattr(app, name)), f"app.{name} 不可调用"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_depends_functions.py -x -q`
Expected: FAIL（Depends 函数不存在）

- [ ] **Step 3: 在 app.py 添加 Depends 函数**

在 `src/app.py` 的 `close_container()` 函数之后、`_create_session_logger` 之前添加：

```python
# ---------------------------------------------------------------------------
# FastAPI Depends 函数（Task 5: 路由类型安全注入）
# ---------------------------------------------------------------------------

from fastapi import Depends


def get_container_or_raise() -> "Container":
    """获取已初始化的容器，未初始化时抛 RuntimeError。"""
    c = get_container()
    if c is None:
        raise RuntimeError("DI 容器未初始化")
    return c


def get_orchestrator(c=Depends(get_container_or_raise)):
    return c.get("orchestrator")


def get_session_logger(c=Depends(get_container_or_raise)):
    return c.get("session_logger")


def get_metrics_collector(c=Depends(get_container_or_raise)):
    return c.get("metrics_collector")


def get_metrics_store(c=Depends(get_container_or_raise)):
    return c.get("metrics_store")


def get_audit_logger(c=Depends(get_container_or_raise)):
    return c.get("audit_logger")


def get_approval_manager(c=Depends(get_container_or_raise)):
    return c.get("approval_manager")


def get_task_manager(c=Depends(get_container_or_raise)):
    return c.get("task_manager")


def get_stream_manager(c=Depends(get_container_or_raise)):
    return c.get("stream_manager")


def get_skill_loader(c=Depends(get_container_or_raise)):
    return c.get("skill_loader")


def get_mcp_manager(c=Depends(get_container_or_raise)):
    return c.get("mcp_manager")


def get_upload_manager(c=Depends(get_container_or_raise)):
    return c.get("upload_manager")


def get_etl_engine(c=Depends(get_container_or_raise)):
    return c.get("etl_engine")


def get_cron_scheduler(c=Depends(get_container_or_raise)):
    return c.get("cron_scheduler")


def get_proposal_store(c=Depends(get_container_or_raise)):
    return c.get("proposal_store")


def get_health_checker(c=Depends(get_container_or_raise)):
    return c.get("health_checker")
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_depends_functions.py -x -q`
Expected: PASS（全部通过）

- [ ] **Step 5: 运行现有测试确保无回归**

Run: `python -m pytest tests/ -x -q --timeout=60 2>&1 | Select-Object -Last 5`
Expected: 无新增失败

- [ ] **Step 6: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/app.py tests/test_depends_functions.py
git commit -m "feat(di): 添加 16 个 FastAPI Depends 函数供路由类型安全注入

- get_container_or_raise: 容器未初始化时抛 RuntimeError
- get_orchestrator / get_session_logger / get_metrics_collector 等 16 个
- 路由可通过 Depends(get_xxx) 类型安全注入组件
- 为阶段 2 路由改造做准备"
```

---

## Task 6: 重构 background_loops.py — 参数注入 + asyncio.Event

**Files:**
- Modify: `src/background_loops.py`（3 个循环改参数注入 + 删除 _get_server_globals + asyncio.Event）
- Test: `tests/test_background_loops.py`（新增）

**Interfaces:**
- Consumes: 无（循环参数化后无外部依赖）
- Produces: `cleanup_loop(session_logger, metrics_store, orchestrator)` / `file_cleanup_loop(upload_manager)` / `metrics_persist_loop(metrics_collector, metrics_store, reset_event)`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_background_loops.py
"""测试 background_loops.py 参数注入改造。

spec 2026-07-13 阶段 2：3 个循环改参数注入 + asyncio.Event。
"""
from __future__ import annotations
import sys
import os
import asyncio
from unittest.mock import MagicMock, AsyncMock

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestBackgroundLoopsSignatures:

    def test_cleanup_loop_accepts_params(self):
        """cleanup_loop 接受 session_logger/metrics_store/orchestrator 参数。"""
        import inspect
        from background_loops import cleanup_loop
        sig = inspect.signature(cleanup_loop)
        params = list(sig.parameters.keys())
        assert "session_logger" in params
        assert "metrics_store" in params
        assert "orchestrator" in params

    def test_file_cleanup_loop_accepts_param(self):
        """file_cleanup_loop 接受 upload_manager 参数。"""
        import inspect
        from background_loops import file_cleanup_loop
        sig = inspect.signature(file_cleanup_loop)
        params = list(sig.parameters.keys())
        assert "upload_manager" in params

    def test_metrics_persist_loop_accepts_params(self):
        """metrics_persist_loop 接受 metrics_collector/metrics_store/reset_event 参数。"""
        import inspect
        from background_loops import metrics_persist_loop
        sig = inspect.signature(metrics_persist_loop)
        params = list(sig.parameters.keys())
        assert "metrics_collector" in params
        assert "metrics_store" in params
        assert "reset_event" in params

    def test_get_server_globals_deleted(self):
        """_get_server_globals 函数应已删除。"""
        try:
            from background_loops import _get_server_globals
            assert False, "_get_server_globals 应已删除"
        except (ImportError, AttributeError):
            pass  # 预期行为

    def test_no_import_state(self):
        """background_loops 不再 import state。"""
        with open(os.path.join(_SRC_DIR, "background_loops.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content
        assert "_get_server_globals" not in content


class TestMetricsPersistLoopEvent:

    @pytest.mark.asyncio
    async def test_reset_event_triggers_baseline_reset(self):
        """reset_event.set() 触发 baseline 重置。"""
        from background_loops import metrics_persist_loop

        mock_collector = MagicMock()
        mock_collector.snapshot.return_value = {"llm_calls_total": 0}
        mock_store = MagicMock()
        reset_event = asyncio.Event()

        # 启动 task，立即取消（验证签名和启动不崩溃）
        task = asyncio.create_task(
            metrics_persist_loop(mock_collector, mock_store, reset_event)
        )
        await asyncio.sleep(0.1)
        task.cancel()
        try:
            await task
        except asyncio.CancelledError:
            pass
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_background_loops.py -x -q`
Expected: FAIL（循环签名不匹配，_get_server_globals 仍存在）

- [ ] **Step 3: 重构 background_loops.py**

完整替换 `src/background_loops.py` 的三个循环函数和删除 `_get_server_globals`：

```python
# src/background_loops.py
"""后台定时清理循环。

从 server.py 提取：cleanup_loop / file_cleanup_loop / metrics_persist_loop。
参数注入：循环通过参数接收组件引用，不再依赖 _get_server_globals() 或 import state。
"""
from __future__ import annotations

import asyncio
import logging
import os
from datetime import datetime, timedelta
from pathlib import Path

logger = logging.getLogger("hermes.server")

try:
    from config import load_config
except ImportError:
    try:
        from .config import load_config  # type: ignore
    except ImportError:
        load_config = None  # type: ignore

try:
    from monitoring.metrics_store import compute_delta
except ImportError:
    try:
        from .monitoring.metrics_store import compute_delta  # type: ignore
    except ImportError:
        compute_delta = None  # type: ignore

CONFIG_PATH = os.environ.get("HERMES_CONFIG", "config.yaml")


async def cleanup_loop(session_logger=None, metrics_store=None, orchestrator=None):
    """定时清理古早会话的后台任务。

    参数注入：通过参数接收组件引用，不再调用 _get_server_globals()。
    """
    try:
        config = load_config(CONFIG_PATH)
    except Exception as e:
        logger.warning("cleanup_loop 读取配置失败，跳过清理: %s", e)
        return
    storage_cfg = config.get("storage", {})
    ttl_days = storage_cfg.get("session_ttl_days")
    interval_hours = storage_cfg.get("cleanup_interval_hours", 24)
    if not ttl_days:
        return

    while True:
        try:
            if session_logger is not None:
                deleted = session_logger.delete_old_sessions(ttl_days)
                if deleted:
                    logger.info(
                        "定时清理完成: 删除了 %d 个旧会话（超过 %d 天）",
                        deleted, ttl_days,
                    )
            if metrics_store is not None:
                deleted_metrics = metrics_store.delete_old_metrics(ttl_days)
                if deleted_metrics:
                    logger.info("清理了 %d 条过期监控历史记录", deleted_metrics)
            if (
                orchestrator is not None
                and getattr(orchestrator, "history_buffer", None) is not None
                and orchestrator.history_buffer.persistence_dir
            ):
                persist_dir = Path(orchestrator.history_buffer.persistence_dir)
                if persist_dir.exists() and session_logger is not None:
                    existing_sessions = {
                        s["id"] for s in session_logger.list_sessions()
                    }
                    for f in persist_dir.glob("*.jsonl"):
                        stem = f.stem
                        candidates = {stem, stem.replace("_", ":")}
                        if not (candidates & existing_sessions):
                            try:
                                f.unlink()
                                logger.info("清理过期 session 历史文件: %s", f.name)
                            except OSError as e:
                                logger.warning("清理历史文件失败 %s: %s", f.name, e)
                    todo_dir = persist_dir / "todo"
                    if todo_dir.exists():
                        for f in todo_dir.glob("*.json"):
                            stem = f.stem
                            candidates = {stem, stem.replace("_", ":")}
                            if not (candidates & existing_sessions):
                                try:
                                    f.unlink()
                                    logger.info("清理过期 session todo 文件: %s", f.name)
                                except OSError as e:
                                    logger.warning("清理 todo 文件失败 %s: %s", f.name, e)
        except Exception as e:
            logger.error("定时清理会话失败: %s", e)
        await asyncio.sleep(interval_hours * 3600)


async def file_cleanup_loop(upload_manager=None) -> None:
    """定时清理过期文件磁盘的后台任务。

    参数注入：通过参数接收 upload_manager，不再调用 _get_server_globals()。
    """
    try:
        config = load_config(CONFIG_PATH)
    except Exception as e:
        logger.warning("file_cleanup_loop 读取配置失败，跳过清理: %s", e)
        return

    storage_cfg = config.get("storage", {})
    files_cfg = config.get("files", {})
    ttl_days = storage_cfg.get("session_ttl_days")
    interval_hours = files_cfg.get("cleanup_interval_hours", 24)
    if not ttl_days:
        return

    while True:
        try:
            if upload_manager is not None:
                expired_ids = upload_manager.get_expired(ttl_days)
                for file_id in expired_ids:
                    try:
                        meta = upload_manager.get_metadata(file_id)
                        if meta is None:
                            continue
                        if meta.get("etl_status") == "processing":
                            continue
                        saved_path = meta.get("saved_path")
                        if saved_path:
                            try:
                                os.remove(saved_path)
                                logger.info("清理过期磁盘文件: %s", saved_path)
                            except OSError as e:
                                logger.warning("清理磁盘文件失败: %s -> %s", saved_path, e)
                        cache_dir = files_cfg.get("upload_dir", "data/uploads")
                        cache_path = os.path.join(cache_dir, f"{file_id}.parsed")
                        try:
                            os.remove(cache_path)
                        except OSError:
                            pass
                        upload_manager.mark_disk_expired(file_id)
                    except Exception as e:
                        logger.warning("清理文件 %s 失败: %s", file_id, e)
                if expired_ids:
                    logger.info("文件清理完成: 清理了 %d 个过期文件", len(expired_ids))
        except Exception as e:
            logger.error("定时清理文件失败: %s", e)
        await asyncio.sleep(interval_hours * 3600)


async def metrics_persist_loop(metrics_collector=None, metrics_store=None,
                                reset_event: asyncio.Event = None) -> None:
    """定时将监控指标增量持久化到 SQLite 的后台任务。

    参数注入：通过参数接收组件引用 + asyncio.Event 替代 state.metrics_baseline_reset。
    """
    if metrics_collector is None or metrics_store is None:
        return

    INITIAL_FLUSH_DELAY = 10

    baseline = metrics_collector.snapshot()
    current_date = datetime.now().date()
    is_first_flush = True

    while True:
        try:
            try:
                config = load_config(CONFIG_PATH)
                monitoring_cfg = config.get("monitoring", {})
                flush_interval = int(monitoring_cfg.get("flush_interval_minutes", 60))
            except Exception as e:
                logger.warning("metrics_persist_loop 读取配置失败，使用默认 60min: %s", e)
                flush_interval = 60
            if flush_interval <= 0:
                flush_interval = 60

            now = datetime.now()
            if is_first_flush:
                sleep_seconds = INITIAL_FLUSH_DELAY
            else:
                next_flush = now + timedelta(minutes=flush_interval)
                next_midnight = datetime(now.year, now.month, now.day) + timedelta(days=1)
                sleep_until = min(next_flush, next_midnight)
                sleep_seconds = (sleep_until - now).total_seconds()
            logger.info("metrics_persist_loop: 准备 sleep %.1f 秒 (is_first=%s)",
                        sleep_seconds, is_first_flush)
            await asyncio.sleep(sleep_seconds)
            logger.info("metrics_persist_loop: sleep 返回，开始执行 flush")

            if reset_event is not None and reset_event.is_set():
                baseline = metrics_collector.snapshot()
                reset_event.clear()
                logger.info("metrics baseline 已重置（reset 接口触发）")

            current_snap = metrics_collector.snapshot()
            delta = compute_delta(current_snap, baseline)
            today = datetime.now().date()

            if today != current_date:
                target_date = current_date.isoformat()
                current_date = today
            else:
                target_date = current_date.isoformat()

            await asyncio.to_thread(metrics_store.upsert_daily, target_date, delta)
            baseline = current_snap
            logger.info("metrics flush 完成: date=%s, delta_llm_calls=%d, is_first=%s",
                        target_date, delta.get("llm_calls_total", 0), is_first_flush)
            is_first_flush = False
        except asyncio.CancelledError:
            raise
        except Exception as e:
            logger.error("监控指标持久化失败: %s", e)
            is_first_flush = False
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_background_loops.py -x -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/background_loops.py tests/test_background_loops.py
git commit -m "refactor(di): background_loops 改参数注入 + asyncio.Event

- 删除 _get_server_globals() 函数（不再反射 server 模块）
- cleanup_loop: 参数注入 session_logger/metrics_store/orchestrator
- file_cleanup_loop: 参数注入 upload_manager
- metrics_persist_loop: 参数注入 + asyncio.Event 替代 state.metrics_baseline_reset
- 删除 import state 和双写同步逻辑
- 为 lifespan 改造和 state.py 删除做准备"
```

---

## Task 7: 重构 lifespan.py — 从 796 行降至 ~200 行

**Files:**
- Modify: `src/lifespan.py`（796→~200 行，调 container.get() + 异步预热 + task 启动）
- Test: `tests/test_lifespan_module.py`（修改现有测试）

**Interfaces:**
- Consumes: Task 4 `BackgroundTaskRegistry`，Task 5 `Depends 函数`，Task 6 `background_loops 参数化`
- Produces: 简化的 lifespan 函数，使用 `container.get()` 触发工厂创建

- [ ] **Step 1: 写失败测试 — lifespan 使用 container.get()**

```python
# tests/test_lifespan_module.py（修改）
"""测试 lifespan 模块使用 container.get() 而非手工 new。

spec 2026-07-13 阶段 2：lifespan 从 796 行降至 ~200 行。
"""
from __future__ import annotations
import sys
import os
from unittest.mock import MagicMock, patch, AsyncMock

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestLifespanUsesContainer:

    def test_lifespan_calls_container_get(self):
        """lifespan 应通过 container.get() 获取组件，而非手工 new。"""
        with open(os.path.join(_SRC_DIR, "lifespan.py"), "r", encoding="utf-8") as f:
            content = f.read()
        # 不应包含手工创建逻辑
        assert "SessionLogger(" not in content or "container.get" in content
        assert "MetricsCollector(" not in content or "container.get" in content

    def test_lifespan_uses_background_task_registry(self):
        """lifespan 应使用 BackgroundTaskRegistry 注册后台 task。"""
        with open(os.path.join(_SRC_DIR, "lifespan.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "BackgroundTaskRegistry" in content
        assert "task_registry" in content

    def test_lifespan_creates_metrics_reset_event(self):
        """lifespan 应创建 app.state.metrics_reset_event。"""
        with open(os.path.join(_SRC_SRC_DIR if hasattr(TestLifespanUsesContainer, '_SRC_SRC_DIR') else _SRC_DIR, "lifespan.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "metrics_reset_event" in content
        assert "asyncio.Event" in content

    def test_lifespan_no_set_instance(self):
        """lifespan 不应调用 inject_lifespan_instances（改为 container.get 触发工厂）。"""
        with open(os.path.join(_SRC_DIR, "lifespan.py"), "r", encoding="utf-8") as f:
            content = f.read()
        # inject_lifespan_instances 是旧模式，新 lifespan 不应使用
        assert "inject_lifespan_instances" not in content

    def test_lifespan_no_state_sync(self):
        """lifespan 不应同步到 state 模块或 server 模块。"""
        with open(os.path.join(_SRC_DIR, "lifespan.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state as _state" not in content
        assert "_state.orchestrator =" not in content
        assert "_server_mod.orchestrator =" not in content
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_lifespan_module.py -x -q`
Expected: FAIL（lifespan 仍包含手工 new 和 state 同步）

- [ ] **Step 3: 重构 lifespan.py**

将 `src/lifespan.py` 完整重写为简化版本（~200 行）。核心结构如下：

```python
# src/lifespan.py（重构后）
"""FastAPI lifespan: 启动时初始化资源，关闭时释放。

重构后：通过 container.get() 触发工厂创建，仅保留异步预热 + 后台 task 启动。
"""
from __future__ import annotations

import asyncio
import logging
import os
from contextlib import asynccontextmanager

from fastapi import FastAPI

VERSION = "0.1.0"
CONFIG_PATH = os.environ.get("HERMES_CONFIG", "config.yaml")
SERVER_LOG_PATH = os.environ.get("HERMES_SERVER_LOG", "data/server.log")

from logging_setup import logger  # noqa: E402
from background_loops import cleanup_loop, file_cleanup_loop, metrics_persist_loop  # noqa: E402
from background_task_registry import BackgroundTaskRegistry  # noqa: E402

# 核心组件导入
try:
    from config import load_config, clear_config_cache, validate_required_env_vars
    from orchestrator import Orchestrator
    from storage.chroma_store import _get_onnx_embedder
except ImportError:
    from .config import load_config, clear_config_cache, validate_required_env_vars  # type: ignore
    from .orchestrator import Orchestrator  # type: ignore
    from .storage.chroma_store import _get_onnx_embedder  # type: ignore

# Skill/MCP 扩展
try:
    from skill.loader import SkillLoader, load_skill_to_registry, register_skill_stub
    from mcp.client import MCPServerDef
    from mcp.manager import MCPManager, register_mcp_tools_to_registry
    SKILL_MCP_AVAILABLE = True
except ImportError:
    SkillLoader = load_skill_to_registry = register_skill_stub = None
    MCPServerDef = MCPManager = register_mcp_tools_to_registry = None
    SKILL_MCP_AVAILABLE = False

# cron 工具
try:
    from agent.cron_proposals import ProposalStore
    from agent.cron_tools import register_cron_tools
    from agent.cron_tool_registry import CronToolRegistry
    from agent.cron_tool_writer import register_write_cron_tool
    from tasks.scheduler import CronScheduler
    from tasks.cron_expr import CronExpr
    from agent.tools.file_tools import register_file_tools
    from agent.tools.bash_tool import register_bash_tool
    CRON_AVAILABLE = True
except ImportError:
    ProposalStore = register_cron_tools = CronToolRegistry = None
    register_write_cron_tool = CronScheduler = CronExpr = None
    register_file_tools = register_bash_tool = None
    CRON_AVAILABLE = False

_CRON_TOOL_BASE_DIR = os.environ.get("HERMES_CRON_TOOL_DIR", "cron_tool")


@asynccontextmanager
async def lifespan(app: FastAPI):
    """启动时初始化资源，关闭时释放。"""
    config = load_config(CONFIG_PATH)
    config["_config_path"] = CONFIG_PATH

    # 1. 初始化容器 + 注册组件
    from app import init_container, register_components, get_container, close_container, register_exception_handlers
    init_container(config)
    container = get_container()
    register_components(container)

    # 2. 触发工厂创建（无状态组件）
    session_logger = container.get("session_logger")
    metrics_collector = container.get("metrics_collector")
    metrics_store = container.get("metrics_store")
    audit_logger = container.get("audit_logger")
    approval_manager = container.get("approval_manager")
    task_manager = container.get("task_manager")
    upload_manager = container.get("upload_manager")

    # 3. 触发工厂创建（有状态组件）
    orchestrator = container.get("orchestrator")
    cron_scheduler = container.get("cron_scheduler")
    stream_manager = container.get("stream_manager")
    skill_loader = container.get("skill_loader")
    mcp_manager = container.get("mcp_manager")
    proposal_store = container.get("proposal_store")
    health_checker = container.get("health_checker")
    etl_engine = container.get("etl_engine")

    # 4. 异步预热
    await _warmup_onnx()
    await _warmup_chromadb(orchestrator)
    await _init_mcp_servers(container, config, mcp_manager)
    _register_skill_tools(container, orchestrator, skill_loader)
    _register_cron_tools(container, orchestrator, cron_scheduler, proposal_store)
    _register_file_tools(orchestrator, etl_engine, upload_manager)

    # 5. 创建 asyncio.Event（替代 state.metrics_baseline_reset）
    app.state.metrics_reset_event = asyncio.Event()

    # 6. 启动后台 task（注册到 registry）
    task_registry = BackgroundTaskRegistry()
    task_registry.register("cleanup",
        asyncio.create_task(cleanup_loop(session_logger, metrics_store, orchestrator)))
    task_registry.register("file_cleanup",
        asyncio.create_task(file_cleanup_loop(upload_manager)))
    if metrics_store is not None and metrics_collector is not None:
        task_registry.register("metrics_persist",
            asyncio.create_task(metrics_persist_loop(
                metrics_collector, metrics_store, app.state.metrics_reset_event)))
    if cron_scheduler is not None and orchestrator is not None:
        schedules_cfg = config.get("schedules", []) or []
        if schedules_cfg:
            cron_scheduler.load_from_config(schedules_cfg)
        task_registry.register("cron",
            asyncio.create_task(cron_scheduler.run_loop(orchestrator)))
    app.state.task_registry = task_registry

    # 7. 注入 cron 依赖到 PolicyEngine
    if cron_scheduler is not None and orchestrator is not None and orchestrator.policy_engine is not None:
        try:
            orchestrator.policy_engine.set_cron_scheduler(cron_scheduler)
        except Exception as e:
            logger.warning("注入 cron_scheduler 到 PolicyEngine 失败: %s", e)

    # 8. 注册异常处理器
    register_exception_handlers(app)

    logger.info("lifespan 启动完成")

    yield

    # 9. 关闭
    logger.info("lifespan 开始关闭")
    await task_registry.cancel_all()
    close_container()
    logger.info("lifespan 关闭完成")


async def _warmup_onnx():
    """ONNX 嵌入模型预加载。"""
    try:
        _get_onnx_embedder()
        logger.info("ONNX 嵌入模型预加载完成")
    except Exception as e:
        logger.warning("ONNX 预加载失败: %s", e)


async def _warmup_chromadb(orchestrator):
    """ChromaDB 向量索引预热。"""
    if orchestrator is None or orchestrator.chroma_store is None:
        return
    try:
        orchestrator.chroma_store.query_memory("warmup", top_k=1, reinforce=False)
        logger.info("ChromaDB 预热完成")
    except Exception as e:
        logger.warning("ChromaDB 预热失败: %s", e)


async def _init_mcp_servers(container, config, mcp_manager):
    """MCP server 连接 + 工具注册。"""
    if not SKILL_MCP_AVAILABLE or mcp_manager is None:
        return
    try:
        skills_cfg = config.get("skills", {}) or {}
        mcp_servers = skills_cfg.get("mcp_servers", []) or []
        for server_def in mcp_servers:
            try:
                await mcp_manager.add_server(MCPServerDef(**server_def))
            except Exception as e:
                logger.warning("MCP server 连接失败: %s", e)
        if orchestrator := container.get("orchestrator"):
            register_mcp_tools_to_registry(orchestrator.tool_registry, mcp_manager)
        logger.info("MCP 服务器初始化完成")
    except Exception as e:
        logger.error("MCP 初始化失败: %s", e)


def _register_skill_tools(container, orchestrator, skill_loader):
    """Skill stub 注册 + 状态恢复。"""
    if not SKILL_MCP_AVAILABLE or skill_loader is None or orchestrator is None:
        return
    try:
        skills_cfg = load_config(CONFIG_PATH).get("skills", {}) or {}
        skill_dir = skills_cfg.get("skill_dir", "skills")
        loaded = skill_loader.load_all(skill_dir)
        for skill_name in loaded:
            register_skill_stub(orchestrator.tool_registry, skill_name, skill_loader)
        skill_loader.restore_state()
        logger.info("Skill 工具注册完成: %d 个", len(loaded))
    except Exception as e:
        logger.warning("Skill 注册失败: %s", e)


def _register_cron_tools(container, orchestrator, cron_scheduler, proposal_store):
    """cron 工具 + cron_tool_registry 注册。"""
    if not CRON_AVAILABLE or orchestrator is None:
        return
    try:
        if (register_cron_tools and cron_scheduler and proposal_store
                and orchestrator.tool_registry):
            register_cron_tools(orchestrator.tool_registry, cron_scheduler, proposal_store)

        cron_tool_registry = None
        if CronToolRegistry:
            cron_tool_registry = CronToolRegistry(base_dir=_CRON_TOOL_BASE_DIR)
            cron_tool_registry.load_all()

        if register_write_cron_tool and orchestrator.tool_registry:
            register_write_cron_tool(orchestrator.tool_registry, base_dir=_CRON_TOOL_BASE_DIR)

        if register_bash_tool and orchestrator.tool_registry:
            bash_timeout = int(load_config(CONFIG_PATH).get("tools", {}).get("bash_timeout", 120))
            register_bash_tool(orchestrator.tool_registry, timeout=bash_timeout)

        if orchestrator.cron_isolator:
            orchestrator.cron_isolator.set_dependencies(
                cron_scheduler=cron_scheduler,
                cron_tool_registry=cron_tool_registry,
            )
        logger.info("cron 工具注册完成")
    except Exception as e:
        logger.error("cron 工具注册失败: %s", e)


def _register_file_tools(orchestrator, etl_engine, upload_manager):
    """file_tools 注册。"""
    if not CRON_AVAILABLE or orchestrator is None:
        return
    try:
        if register_file_tools and orchestrator.tool_registry and etl_engine:
            register_file_tools(
                orchestrator.tool_registry,
                etl_engine,
                upload_manager,
                get_session_id=lambda: getattr(orchestrator, "_current_session_id", None),
            )
            logger.info("文件工具已注册")
    except Exception as e:
        logger.error("文件工具注册失败: %s", e)
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_lifespan_module.py -x -q`
Expected: PASS

- [ ] **Step 5: 运行全量测试确保无回归（允许已有的 124 个失败）**

Run: `python -m pytest tests/ -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: 无新增失败（passed 数量不低于 2661 - 容差）

- [ ] **Step 6: 验证 lifespan 行数**

Run: `(Get-Content src\lifespan.py | Measure-Object -Line).Lines`
Expected: ≤ 250 行

- [ ] **Step 7: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/lifespan.py tests/test_lifespan_module.py
git commit -m "refactor(di): lifespan 从 796 行降至 ~200 行

- 删除所有手工 new 创建逻辑（SessionLogger/MetricsCollector/Orchestrator 等）
- 改为 container.get() 触发工厂创建
- 删除 inject_lifespan_instances（不再 set_instance 绕过工厂）
- 删除 import state 同步和 server 模块同步
- 新增 BackgroundTaskRegistry 注册 4 个后台 task
- 新增 app.state.metrics_reset_event (asyncio.Event)
- 异步预热函数提取为 _warmup_onnx/_warmup_chromadb/_init_mcp_servers 等"
```

---

## Task 8: 重构 routes 批次 1 — health/memory/cron_tools/approvals + health 测试

**Files:**
- Modify: `src/routes/health.py`、`src/routes/memory.py`、`src/routes/cron_tools.py`、`src/routes/approvals.py`
- Modify: `tests/test_server.py`（health 相关 patch → dependency_overrides）

**Interfaces:**
- Consumes: Task 5 `Depends 函数`
- Produces: 4 个路由文件使用 Depends，无 import state

- [ ] **Step 1: 写失败测试 — 验证 4 个路由无 import state**

```python
# tests/test_routes_batch1_no_state.py
"""测试 routes 批次 1 无 import state。

spec 2026-07-13 阶段 2：health/memory/cron_tools/approvals 改 Depends。
"""
from __future__ import annotations
import sys
import os

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestRoutesBatch1NoState:

    def test_health_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "health.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content
        assert "state." not in content

    def test_memory_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "memory.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content
        assert "state." not in content

    def test_cron_tools_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "cron_tools.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content
        assert "state." not in content

    def test_approvals_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "approvals.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content
        assert "state." not in content

    def test_health_uses_depends(self):
        with open(os.path.join(_SRC_DIR, "routes", "health.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "Depends(get_" in content

    def test_health_reset_metrics_uses_event(self):
        """reset_metrics 端点使用 app.state.metrics_reset_event。"""
        with open(os.path.join(_SRC_DIR, "routes", "health.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "metrics_reset_event" in content
        assert "request.app.state" in content or "app.state" in content
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_routes_batch1_no_state.py -x -q`
Expected: FAIL（路由仍使用 import state）

- [ ] **Step 3: 重构 routes/health.py**

将 `src/routes/health.py` 中所有 `import state` 和 `state.xxx` 替换为 Depends 注入。特别注意 `reset_metrics` 端点改用 `request.app.state.metrics_reset_event`：

```python
# src/routes/health.py（重构后关键部分）
from fastapi import APIRouter, Depends, Query, Request
from fastapi.responses import JSONResponse
from app import (
    get_health_checker, get_metrics_collector, get_metrics_store,
    get_orchestrator,
)

router = APIRouter()


def _now_iso() -> str:
    return datetime.now().isoformat()


@router.get("/health")
def deep_health(health_checker=Depends(get_health_checker)):
    VERSION = "0.1.0"
    if health_checker is None:
        return JSONResponse(
            content={
                "status": "unhealthy",
                "timestamp": _now_iso(),
                "version": VERSION,
                "summary": {"total": 0, "ok": 0, "warning": 0, "critical": 1},
                "checks": {"health_checker": {
                    "status": "critical",
                    "message": "HealthChecker 尚未初始化",
                    "detail": None,
                }},
            },
            status_code=503,
        )
    result = health_checker.run_all()
    result["version"] = VERSION
    status_code = 503 if result["status"] == "unhealthy" else 200
    return JSONResponse(content=result, status_code=status_code)


@router.get("/metrics")
def get_metrics(metrics_collector=Depends(get_metrics_collector)):
    from config import load_config
    config_path = os.environ.get("HERMES_CONFIG", "config.yaml")
    if metrics_collector is None:
        return JSONResponse({})
    try:
        config = load_config(config_path)
        if not config.get("monitoring", {}).get("enabled", True):
            return JSONResponse({})
    except Exception:
        pass
    return JSONResponse(metrics_collector.snapshot())


@router.get("/metrics/history")
def get_metrics_history(days: int = Query(default=30, ge=1, le=90),
                        metrics_store=Depends(get_metrics_store)):
    if metrics_store is None:
        return JSONResponse([])
    try:
        records = metrics_store.get_history(days)
        return JSONResponse(records)
    except Exception as e:
        logger.error("查询监控历史失败: %s", e)
        return JSONResponse([], status_code=500)


@router.post("/metrics/reset")
def reset_metrics(request: Request,
                  metrics_collector=Depends(get_metrics_collector)):
    if metrics_collector is None:
        return JSONResponse({"ok": False, "error": "监控未启用"}, status_code=400)
    metrics_collector.reset()
    request.app.state.metrics_reset_event.set()
    logger.info("监控指标已重置（metrics_collector + baseline）")
    return JSONResponse({"ok": True})


@router.get("/metrics/signals")
def get_signals_metrics(orchestrator=Depends(get_orchestrator)):
    if orchestrator is None or getattr(orchestrator, "signal_pool", None) is None:
        return JSONResponse({
            "signals": [],
            "sections": {},
            "summary": {},
            "threshold": 7,
        })
    try:
        return JSONResponse(orchestrator.signal_pool.get_dashboard_data())
    except Exception as e:
        logger.warning("信号池仪表盘数据获取失败: %s", e)
        return JSONResponse({
            "signals": [],
            "sections": {},
            "summary": {},
            "threshold": 7,
            "error": str(e),
        })
```

- [ ] **Step 4: 重构 routes/memory.py**

将 `src/routes/memory.py` 中所有 `import state` 和 `state.xxx` 替换为 Depends 注入。每个 handler 添加 `Depends(get_orchestrator)` 等参数。

- [ ] **Step 5: 重构 routes/cron_tools.py**

将 `src/routes/cron_tools.py` 中所有 `import state` 和 `state.xxx` 替换为 Depends 注入。

- [ ] **Step 6: 重构 routes/approvals.py**

将 `src/routes/approvals.py` 中所有 `import state` 和 `state.xxx` 替换为 Depends 注入。

- [ ] **Step 7: 运行测试验证通过**

Run: `python -m pytest tests/test_routes_batch1_no_state.py -x -q`
Expected: PASS

- [ ] **Step 8: 运行相关路由测试**

Run: `python -m pytest tests/test_server.py tests/test_memory_dashboard_api.py -x -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: 部分测试可能失败（因 patch 模式需同步更新），记录失败数

- [ ] **Step 9: 更新 test_server.py 中 health 相关 patch**

将 `tests/test_server.py` 中 `patch("server.health_checker")` 等改为 `app.dependency_overrides[get_health_checker]`。

- [ ] **Step 10: 更新 test_memory_dashboard_api.py 中 patch**

将 `tests/test_memory_dashboard_api.py` 中 5 处 `patch("src.server.orchestrator")` 等改为 `app.dependency_overrides[get_orchestrator]`。

- [ ] **Step 11: 运行测试验证通过**

Run: `python -m pytest tests/test_server.py tests/test_memory_dashboard_api.py -x -q --timeout=60`
Expected: PASS（或仅剩重构前遗留失败）

- [ ] **Step 12: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/routes/health.py src/routes/memory.py src/routes/cron_tools.py src/routes/approvals.py tests/
git commit -m "refactor(di): routes 批次 1 改 Depends（health/memory/cron_tools/approvals）

- health.py: 5 个 handler 加 Depends，reset_metrics 用 app.state.metrics_reset_event
- memory.py: 4 个 handler 加 Depends
- cron_tools.py: 2 个 handler 加 Depends
- approvals.py: 2 个 handler 加 Depends
- test_server.py / test_memory_dashboard_api.py: patch → dependency_overrides"
```

---

## Task 9: 重构 routes 批次 2 — chat/sessions/skills/files/proposals + 测试

**Files:**
- Modify: `src/routes/chat.py`、`src/routes/sessions.py`、`src/routes/skills.py`、`src/routes/files.py`、`src/routes/proposals.py`
- Modify: `tests/test_skill_rest_endpoints.py`、`tests/test_config_update.py`（相关 patch）

**Interfaces:**
- Consumes: Task 5 `Depends 函数`
- Produces: 5 个路由文件使用 Depends，无 import state

- [ ] **Step 1: 写失败测试 — 验证 5 个路由无 import state**

```python
# tests/test_routes_batch2_no_state.py
"""测试 routes 批次 2 无 import state。"""
from __future__ import annotations
import sys
import os

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestRoutesBatch2NoState:

    def test_chat_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "chat.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content

    def test_sessions_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "sessions.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content

    def test_skills_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "skills.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content

    def test_files_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "files.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content

    def test_proposals_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "proposals.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_routes_batch2_no_state.py -x -q`
Expected: FAIL

- [ ] **Step 3: 逐文件重构路由**

对 `src/routes/chat.py`、`sessions.py`、`skills.py`、`files.py`、`proposals.py` 每个文件：
1. 删除 `import state` 语句
2. 替换 `state.xxx` 为 Depends 注入参数
3. 在 handler 签名添加 `Depends(get_xxx)`
4. 添加 `from app import get_orchestrator, get_session_logger, ...`

**chat.py 示例改造模式**：
```python
# 修复前
@router.post("/chat")
async def chat(req: ChatRequest):
    import state
    orch = state.orchestrator
    session_logger = state.session_logger
    ...

# 修复后
from app import get_orchestrator, get_session_logger, get_stream_manager
from fastapi import Depends

@router.post("/chat")
async def chat(req: ChatRequest,
               orch=Depends(get_orchestrator),
               session_logger=Depends(get_session_logger),
               stream_manager=Depends(get_stream_manager)):
    ...
```

对每个文件，用 Grep 找到所有 `state.xxx` 引用，逐一替换为 Depends 参数。确保每个 handler 的 Depends 参数数 = 该 handler 原先访问的不同 `state.xxx` 组件数。

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_routes_batch2_no_state.py -x -q`
Expected: PASS

- [ ] **Step 5: 更新 test_skill_rest_endpoints.py**

将 `tests/test_skill_rest_endpoints.py` 中 4 处 `patch("src.server.xxx")` 改为 `app.dependency_overrides[get_xxx]`。

- [ ] **Step 6: 更新 test_config_update.py 中 files/proposals 相关 patch**

将 `tests/test_config_update.py` 中与 files/proposals 路由相关的 `patch("src.server.xxx")` 改为 `app.dependency_overrides[get_xxx]`。

- [ ] **Step 7: 运行相关测试**

Run: `python -m pytest tests/test_skill_rest_endpoints.py tests/test_config_update.py -x -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: PASS（或仅剩重构前遗留失败）

- [ ] **Step 8: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/routes/ tests/
git commit -m "refactor(di): routes 批次 2 改 Depends（chat/sessions/skills/files/proposals）

- 5 个路由文件 import state / state.xxx 全改 Depends(get_xxx)
- test_skill_rest_endpoints.py: 4 处 patch → dependency_overrides
- test_config_update.py: 相关 patch → dependency_overrides"
```

---

## Task 10: 重构 routes 批次 3 — schedules/misc(软重启) + 测试

**Files:**
- Modify: `src/routes/schedules.py`、`src/routes/misc.py`（软重启改造）
- Modify: `tests/test_schedule_history_memories.py`、`tests/test_sse_todo_events.py`、`tests/test_task_api.py`

**Interfaces:**
- Consumes: Task 4 `BackgroundTaskRegistry`，Task 5 `Depends 函数`
- Produces: schedules.py 和 misc.py 使用 Depends，软重启改用 container API

- [ ] **Step 1: 写失败测试 — 验证 schedules/misc 无 import state + 软重启用容器**

```python
# tests/test_routes_batch3_no_state.py
"""测试 routes 批次 3 无 import state + 软重启改用容器 API。"""
from __future__ import annotations
import sys
import os

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestRoutesBatch3NoState:

    def test_schedules_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "schedules.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content

    def test_misc_no_import_state(self):
        with open(os.path.join(_SRC_DIR, "routes", "misc.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "import state" not in content

    def test_misc_no_sync_to_server_globals(self):
        """_sync_to_server_globals 函数应已删除。"""
        with open(os.path.join(_SRC_DIR, "routes", "misc.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "_sync_to_server_globals" not in content

    def test_soft_restart_uses_container(self):
        """软重启使用 container.get() + set_instance()。"""
        with open(os.path.join(_SRC_DIR, "routes", "misc.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "container.get" in content or "container.set_instance" in content
        assert "task_registry.restart" in content

    def test_soft_restart_rebuilds_health_checker(self):
        """软重启重建 health_checker（步骤 5.5）。"""
        with open(os.path.join(_SRC_DIR, "routes", "misc.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "health_checker" in content
        assert "HealthChecker(" in content

    def test_soft_restart_rebuilds_etl_engine(self):
        """软重启重建 etl_engine（步骤 5.6）。"""
        with open(os.path.join(_SRC_DIR, "routes", "misc.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "etl_engine" in content
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_routes_batch3_no_state.py -x -q`
Expected: FAIL

- [ ] **Step 3: 重构 routes/schedules.py**

将 `src/routes/schedules.py` 中所有 `import state` 和 `state.xxx` 替换为 Depends 注入（13 处 import + 15 处 state.xxx）。

- [ ] **Step 4: 重构 routes/misc.py — 删除 _sync_to_server_globals + 软重启改造**

1. 删除 `_sync_to_server_globals` 函数
2. 删除所有 `import state` 和 `state.xxx`
3. 软重启改造为使用 `container.get()` + `set_instance()` + `task_registry.restart()`
4. 补充步骤 5.5（重建 health_checker）和 5.6（重建 etl_engine）

```python
# src/routes/misc.py 软重启部分（重构后）
from app import get_container_or_raise, get_orchestrator, get_stream_manager
from fastapi import Depends, Request

_soft_restart_in_progress = False  # 模块级变量，替代 state.soft_restart_in_progress

@router.post("/restart")
async def restart_server(request: Request,
                         container=Depends(get_container_or_raise)):
    global _soft_restart_in_progress
    if _soft_restart_in_progress:
        raise HTTPException(status_code=409, detail="软重启已在进行中")
    _soft_restart_in_progress = True
    try:
        # 1. 等待活跃流
        stream_manager = container.get("stream_manager")
        if stream_manager is not None:
            active = stream_manager.active_count()
            if active > 0:
                logger.info("软重启: 等待 %d 个进行中的流完成（最多 30s）...", active)
                for _ in range(30):
                    if stream_manager.active_count() == 0:
                        break
                    await asyncio.sleep(1)

        # 2. 关闭旧 Orchestrator
        old_orch = container.get("orchestrator")
        try:
            await asyncio.to_thread(old_orch.shutdown)
        except Exception as e:
            logger.warning("软重启: 旧 Orchestrator 关闭异常（已忽略）: %s", e)

        # 3. 手动创建新 Orchestrator
        from orchestrator import Orchestrator
        config_path = os.environ.get("HERMES_CONFIG", "config.yaml")
        try:
            new_orch = await asyncio.to_thread(
                Orchestrator,
                config_path=config_path,
                metrics=container.get("metrics_collector"),
                audit_logger=container.get("audit_logger"),
                approval_manager=container.get("approval_manager"),
                task_manager=container.get("task_manager"),
            )
        except Exception as e:
            logger.exception("软重启: 新 Orchestrator 构建失败，服务不可用")
            raise HTTPException(
                status_code=500,
                detail=f"新 Orchestrator 构建失败: {e}",
            )

        # 4. 预热 ChromaDB
        if new_orch.chroma_store is not None:
            try:
                new_orch.chroma_store.query_memory("warmup", top_k=1, reinforce=False)
            except Exception as e:
                logger.warning("软重启: ChromaDB 预热失败: %s", e)

        # 5. 通过 set_instance 替换容器中的实例
        container.set_instance("orchestrator", new_orch)

        # 5.5 重建 health_checker（持有新 orchestrator 引用）
        from monitoring.health import HealthChecker
        new_hc = HealthChecker(
            orchestrator=new_orch,
            session_logger_global=container.get("session_logger"),
            mcp_manager=container.get("mcp_manager"),
            skill_loader=container.get("skill_loader"),
            metrics_collector=container.get("metrics_collector"),
            proposal_store=container.get("proposal_store"),
        )
        container.set_instance("health_checker", new_hc)

        # 5.6 重建 etl_engine（捕获新 orchestrator 的 chroma_store/llm_client）
        from app import _create_etl_engine
        new_etl = _create_etl_engine(
            container.config,
            upload_manager=container.get("upload_manager"),
            orchestrator=new_orch,
        )
        container.set_instance("etl_engine", new_etl)

        # 6. 重启 cron_task（持有新 Orchestrator 引用）
        task_registry = request.app.state.task_registry
        cron_scheduler = container.get("cron_scheduler")
        if cron_scheduler is not None:
            await task_registry.restart(
                "cron",
                lambda: cron_scheduler.run_loop(new_orch)
            )

        logger.info("软重启: Orchestrator/health_checker/etl_engine 已替换，cron_task 已重启")
        return {"status": "ok", "message": "软重启完成"}
    finally:
        _soft_restart_in_progress = False
```

- [ ] **Step 5: 运行测试验证通过**

Run: `python -m pytest tests/test_routes_batch3_no_state.py -x -q`
Expected: PASS

- [ ] **Step 6: 更新 test_schedule_history_memories.py**

将 6 处 `patch("src.server.xxx")` 改为 `app.dependency_overrides[get_xxx]`。

- [ ] **Step 7: 更新 test_sse_todo_events.py 和 test_task_api.py**

将 `patch("src.server.xxx")` 改为 `app.dependency_overrides[get_xxx]`。

- [ ] **Step 8: 运行相关测试**

Run: `python -m pytest tests/test_schedule_history_memories.py tests/test_sse_todo_events.py tests/test_task_api.py -x -q --timeout=60`
Expected: PASS（或仅剩重构前遗留失败）

- [ ] **Step 9: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/routes/schedules.py src/routes/misc.py tests/
git commit -m "refactor(di): routes 批次 3 改 Depends + 软重启改用容器 API

- schedules.py: 11 处 import state + 15 处 state.xxx 全改 Depends
- misc.py: 删除 _sync_to_server_globals，软重启改用 container.get()+set_instance()
- 软重启补充步骤 5.5（重建 health_checker）+ 5.6（重建 etl_engine）
- 软重启通过 task_registry.restart() 重启 cron_task
- test_schedule_history_memories/sse_todo_events/task_api: patch → dependency_overrides"
```

---

## Task 11: 删除 state.py + server.py 全局变量/re-export + 迁移剩余测试 patch

**Files:**
- Delete: `src/state.py`
- Modify: `src/server.py`（删除 10 个全局变量 + 8 个 config_helpers re-export + metrics_persist_loop re-export + __main__ 块）
- Modify: `tests/test_server.py`（剩余 15 处 patch → dependency_overrides / patch 源模块）
- Modify: `tests/test_config_update.py`（剩余 patch）

**Interfaces:**
- Consumes: Task 8/9/10 所有路由已改 Depends
- Produces: state.py 删除，server.py 降至 ~30 行

- [ ] **Step 1: 写失败测试 — 验证 state.py 删除 + server.py 无全局变量**

```python
# tests/test_state_deleted.py
"""测试 state.py 已删除 + server.py 无全局组件变量。"""
from __future__ import annotations
import sys
import os

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestStateDeleted:

    def test_state_py_deleted(self):
        """state.py 文件应已删除。"""
        state_path = os.path.join(_SRC_DIR, "state.py")
        assert not os.path.exists(state_path), "state.py 应已删除"

    def test_server_no_global_components(self):
        """server.py 不应包含全局组件变量。"""
        with open(os.path.join(_SRC_DIR, "server.py"), "r", encoding="utf-8") as f:
            content = f.read()
        # 不应包含全局组件变量声明
        assert "orchestrator: Optional" not in content
        assert "session_logger: Optional" not in content
        assert "metrics_collector: Optional" not in content

    def test_server_no_config_helpers_reexport(self):
        """server.py 不应 re-export config_helpers。"""
        with open(os.path.join(_SRC_DIR, "server.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "_RESTART_REQUIRED_KEYS" not in content
        assert "_deep_merge_config" not in content

    def test_server_no_main_block(self):
        """server.py 不应包含 __main__ 块。"""
        with open(os.path.join(_SRC_DIR, "server.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "__main__" not in content

    def test_no_import_state_in_src(self):
        """src/ 目录下无 import state。"""
        import subprocess
        result = subprocess.run(
            ["findstr", "/s", "/r", "/n", "import state", _SRC_DIR],
            capture_output=True, text=True
        )
        # 排除注释中的 import state
        real_matches = [
            line for line in result.stdout.split("\n")
            if line and "import state" in line and not line.strip().startswith("#")
        ]
        assert len(real_matches) == 0, f"仍有 import state: {real_matches}"

    def test_no_get_server_globals(self):
        """src/ 目录下无 _get_server_globals。"""
        import subprocess
        result = subprocess.run(
            ["findstr", "/s", "/r", "/n", "_get_server_globals", _SRC_DIR],
            capture_output=True, text=True
        )
        assert not result.stdout.strip(), f"仍有 _get_server_globals: {result.stdout}"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_state_deleted.py -x -q`
Expected: FAIL（state.py 仍存在，server.py 仍有全局变量）

- [ ] **Step 3: 删除 state.py**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
del src\state.py
```

- [ ] **Step 4: 重构 server.py — 删除全局变量 + re-export + __main__**

将 `src/server.py` 简化为仅保留常量 + 静态文件挂载：

```python
# src/server.py（重构后 ~30 行）
"""EC2 长驻 HTTP 服务入口。

重构后：全局组件变量和 state 代理已删除，Container 成为唯一真相源。
路由通过 Depends(get_xxx) 获取组件。server.py 仅保留常量 + 静态文件挂载。
"""
from __future__ import annotations

import os
import sys
from pathlib import Path

# 确保 src/ 在 sys.path 中
_SRC_DIR = str(Path(__file__).resolve().parent)
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)

# ---------- 常量 ----------
VERSION = "0.1.0"
CONFIG_PATH = os.environ.get("HERMES_CONFIG", "config.yaml")
SERVER_LOG_PATH = os.environ.get("HERMES_SERVER_LOG", "data/server.log")
SKILL_STATE_PATH = "data/skills_state.json"

# ---------- 日志 ----------
from logging_setup import logger  # noqa: E402, F401

# ---------- FastAPI 应用 ----------
from app import app  # noqa: E402, F401

# ---------- 静态文件服务（Web 前端） ----------
from fastapi.staticfiles import StaticFiles  # noqa: E402

_WEB_DIR = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "web")
_STATIC_DIR = os.path.join(_WEB_DIR, "static")
if os.path.isdir(_STATIC_DIR):
    app.mount("/static", StaticFiles(directory=_STATIC_DIR), name="static")
elif os.path.isdir(_WEB_DIR):
    app.mount("/static", StaticFiles(directory=_WEB_DIR), name="static")
```

- [ ] **Step 5: 迁移 test_server.py 剩余 patch**

将 `tests/test_server.py` 中 12 处 `patch("server.orchestrator")` 等改为 `app.dependency_overrides[get_orchestrator]`。
将 3 处 `patch("server.load_config")` 改为 `patch("config.load_config")`（patch 源模块）。

**改造模式**：
```python
# 修复前
@pytest.fixture
def mock_orchestrator():
    with patch("server.orchestrator") as mock_orch, \
         patch("server.session_logger") as mock_session, \
         patch("server.stream_manager") as mock_sm:
        ...
        yield mock_orch

# 修复后
@pytest.fixture
def mock_orchestrator():
    from app import app, get_orchestrator, get_session_logger, get_stream_manager
    mock_orch = MagicMock()
    mock_session = MagicMock()
    mock_sm = MagicMock()
    # ... 配置 mock ...
    app.dependency_overrides[get_orchestrator] = lambda: mock_orch
    app.dependency_overrides[get_session_logger] = lambda: mock_session
    app.dependency_overrides[get_stream_manager] = lambda: mock_sm
    yield mock_orch
    app.dependency_overrides.clear()
```

- [ ] **Step 6: 迁移 test_config_update.py 剩余 patch**

将 `tests/test_config_update.py` 中剩余 `patch("src.server.xxx")` 改为 `app.dependency_overrides[get_xxx]`。

- [ ] **Step 7: 运行测试验证通过**

Run: `python -m pytest tests/test_state_deleted.py -x -q`
Expected: PASS

- [ ] **Step 8: 运行全量测试**

Run: `python -m pytest tests/ -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: 无新增失败（passed 数量不低于 2661 - 容差）

- [ ] **Step 9: 验证 grep 指标**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
findstr /s /r /n "import state" src\routes\
# Expected: 0 结果
findstr /s /r /n "import state" src\
# Expected: 0 结果（排除注释）
findstr /s /r /n "sys.modules.get" src\routes\
# Expected: 0 结果
findstr /s /r /n "_get_server_globals" src\
# Expected: 0 结果
findstr /s /r /n "Depends(get_" src\routes\
# Expected: ≥ 61 结果
```

- [ ] **Step 10: 验证 server.py 行数**

Run: `(Get-Content src\server.py | Measure-Object -Line).Lines`
Expected: ≤ 35 行

- [ ] **Step 11: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add -A
git commit -m "refactor(di): 删除 state.py + server.py 全局变量，Container 成为唯一真相源

- 删除 state.py（PEP 562 代理失效）
- server.py 删除 10 个全局组件变量 + 8 个 config_helpers re-export
- server.py 删除 __main__ 块（引用 src.server 重命名后必崩）
- server.py 从 76 行降至 ~30 行
- test_server.py: 15 处 patch → dependency_overrides / patch 源模块
- test_config_update.py: 剩余 patch → dependency_overrides
- Container 成为唯一真相源，三重状态存储消除"
```

---

## Task 12: 阶段 2 最终验证 — 热重载 + 软重启 + 后台 task 重启

**Files:**
- Modify: `src/routes/config.py`（热重载后重启后台 task，见 spec 6.4 节）
- Test: `tests/test_phase2_verification.py`（新增集成测试）

**Interfaces:**
- Consumes: Task 4-11 所有改造
- Produces: 热重载后后台 task 正确重启，软重启后 health_checker/etl_engine 正确重建

- [ ] **Step 1: 写失败测试 — 热重载后 task 重启**

```python
# tests/test_phase2_verification.py
"""阶段 2 最终验证：热重载 + 软重启 + 后台 task 重启。"""
from __future__ import annotations
import sys
import os
import asyncio
from unittest.mock import MagicMock, patch, AsyncMock

_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")
if _SRC_DIR not in sys.path:
    sys.path.insert(0, _SRC_DIR)


class TestHotReloadTaskRestart:

    def test_config_route_restarts_metrics_persist_task(self):
        """热重载重建 metrics_collector 后重启 metrics_persist task。"""
        with open(os.path.join(_SRC_DIR, "routes", "config.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "task_registry.restart" in content or "metrics_persist" in content

    def test_config_route_restarts_cleanup_task(self):
        """热重载重建 session_logger 后重启 cleanup task。"""
        with open(os.path.join(_SRC_DIR, "routes", "config.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "cleanup" in content

    def test_config_route_restarts_file_cleanup_task(self):
        """热重载重建 upload_manager 后重启 file_cleanup task。"""
        with open(os.path.join(_SRC_DIR, "routes", "config.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "file_cleanup" in content


class TestGrepMetrics:

    def test_no_import_state_in_routes(self):
        """src/routes/ 下无 import state。"""
        import subprocess
        result = subprocess.run(
            ["findstr", "/s", "/r", "/n", "import state",
             os.path.join(_SRC_DIR, "routes")],
            capture_output=True, text=True
        )
        real_matches = [
            line for line in result.stdout.split("\n")
            if line and "import state" in line and not line.strip().startswith("#")
        ]
        assert len(real_matches) == 0

    def test_depends_count_in_routes(self):
        """src/routes/ 下 Depends(get_ 出现 ≥ 61 次。"""
        import subprocess
        result = subprocess.run(
            ["findstr", "/s", "/r", "/n", "Depends(get_",
             os.path.join(_SRC_DIR, "routes")],
            capture_output=True, text=True
        )
        count = len([l for l in result.stdout.split("\n") if l.strip()])
        assert count >= 61, f"Depends(get_ 出现 {count} 次，预期 ≥ 61"

    def test_lifespan_line_count(self):
        """lifespan.py 行数 ≤ 250。"""
        with open(os.path.join(_SRC_DIR, "lifespan.py"), "r", encoding="utf-8") as f:
            lines = f.readlines()
        assert len(lines) <= 250, f"lifespan.py {len(lines)} 行，预期 ≤ 250"

    def test_server_line_count(self):
        """server.py 行数 ≤ 35。"""
        with open(os.path.join(_SRC_DIR, "server.py"), "r", encoding="utf-8") as f:
            lines = f.readlines()
        assert len(lines) <= 35, f"server.py {len(lines)} 行，预期 ≤ 35"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_phase2_verification.py -x -q`
Expected: 部分失败（config.py 未添加 task 重启逻辑）

- [ ] **Step 3: 在 routes/config.py 添加热重载后 task 重启逻辑**

修改 `src/routes/config.py` 的热重载端点，在 `container.reload()` 之后添加 task 重启逻辑：

```python
# src/routes/config.py 热重载端点（修改后关键部分）
@router.put("/config")
async def update_config(request: Request, ...):
    # ... 现有逻辑：写入配置文件 + container.reload() ...

    from app import get_container
    container = get_container()

    # 6.4 节：reload 后重启持有旧实例的后台 task
    task_registry = request.app.state.task_registry
    from background_loops import cleanup_loop, file_cleanup_loop, metrics_persist_loop

    # metrics_persist_task
    if "metrics_collector" in rebuilt or "metrics_store" in rebuilt:
        new_mc = container.get("metrics_collector")
        new_ms = container.get("metrics_store")
        await task_registry.restart(
            "metrics_persist",
            lambda: metrics_persist_loop(new_mc, new_ms, request.app.state.metrics_reset_event)
        )

    # cleanup_task
    if "session_logger" in rebuilt or "metrics_store" in rebuilt:
        await task_registry.restart(
            "cleanup",
            lambda: cleanup_loop(
                container.get("session_logger"),
                container.get("metrics_store"),
                container.get("orchestrator"),
            )
        )

    # file_cleanup_task
    if "upload_manager" in rebuilt:
        await task_registry.restart(
            "file_cleanup",
            lambda: file_cleanup_loop(container.get("upload_manager"))
        )

    return {"status": "ok", "rebuilt": rebuilt}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_phase2_verification.py -x -q`
Expected: PASS

- [ ] **Step 5: 运行全量测试**

Run: `python -m pytest tests/ -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: 无新增失败

- [ ] **Step 6: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add src/routes/config.py tests/test_phase2_verification.py
git commit -m "feat(di): 热重载后重启后台 task + 阶段 2 最终验证

- routes/config.py: reload 后检查重建组件，重启 metrics_persist/cleanup/file_cleanup task
- metrics_persist lambda 传入 metrics_reset_event 参数
- 新增 test_phase2_verification.py: grep 指标 + 行数验证 + task 重启验证
- 阶段 2 完成：Container 唯一真相源 + Depends 注入 + 后台 task 可控重建"
```

---

## Task 13: 重命名 src/ → hermes/ + 创建 __main__.py + 删除遗留 __main__ 块

**Files:**
- Rename: `src/` → `hermes/`
- Create: `hermes/__init__.py`、`hermes/__main__.py`
- Modify: `hermes/app.py`（删除 __main__ 块）
- Modify: 所有引用 `src.` 的文件

**Interfaces:**
- Consumes: Task 1-12 完成
- Produces: `hermes/` 包目录，`python -m hermes` 入口

- [ ] **Step 1: 写失败测试 — python -m hermes 入口存在**

```python
# tests/test_package_entry.py
"""测试 hermes 包入口。"""
from __future__ import annotations
import sys
import os

_ROOT = os.path.join(os.path.dirname(__file__), "..")
_HERMES = os.path.join(_ROOT, "hermes")


class TestPackageEntry:

    def test_hermes_dir_exists(self):
        """hermes/ 目录存在。"""
        assert os.path.isdir(_HERMES), "hermes/ 目录不存在"

    def test_src_dir_deleted(self):
        """src/ 目录已重命名。"""
        src_dir = os.path.join(_ROOT, "src")
        assert not os.path.isdir(src_dir), "src/ 应已重命名为 hermes/"

    def test_main_module_exists(self):
        """hermes/__main__.py 存在。"""
        assert os.path.exists(os.path.join(_HERMES, "__main__.py"))

    def test_init_module_exists(self):
        """hermes/__init__.py 存在。"""
        assert os.path.exists(os.path.join(_HERMES, "__init__.py"))

    def test_app_no_main_block(self):
        """app.py 不包含 __main__ 块。"""
        with open(os.path.join(_HERMES, "app.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert '__name__ == "__main__"' not in content

    def test_server_no_main_block(self):
        """server.py 不包含 __main__ 块。"""
        with open(os.path.join(_HERMES, "server.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert '__name__ == "__main__"' not in content
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_package_entry.py -x -q`
Expected: FAIL（hermes/ 不存在）

- [ ] **Step 3: 重命名 src/ → hermes/**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
ren src hermes
```

- [ ] **Step 4: 创建 hermes/__init__.py**

```python
# hermes/__init__.py
"""Hermes Lite - 个人 AI Agent 长驻 HTTP 服务。"""
```

- [ ] **Step 5: 创建 hermes/__main__.py**

```python
# hermes/__main__.py
"""python -m hermes 入口。"""
import os
import uvicorn
from hermes.config import load_config
from hermes.app import app

if __name__ == "__main__":
    config_path = os.environ.get("HERMES_CONFIG", "config.yaml")
    try:
        cfg = load_config(config_path)
        server_cfg = cfg.get("server", {})
    except Exception:
        server_cfg = {}
    uvicorn.run(
        app,
        host=server_cfg.get("host", "0.0.0.0"),
        port=int(server_cfg.get("port", 8000)),
    )
```

- [ ] **Step 6: 删除 app.py 和 server.py 的 __main__ 块**

在 `hermes/app.py` 末尾删除 `if __name__ == "__main__":` 块（line 569-583）。
`hermes/server.py` 在 Task 11 已删除 __main__ 块。

- [ ] **Step 7: 更新测试中的 sys.path 路径**

所有测试文件中的 `_SRC_DIR = os.path.join(os.path.dirname(__file__), "..", "src")` 改为 `_HERMES_DIR = os.path.join(os.path.dirname(__file__), "..", "hermes")`。

- [ ] **Step 8: 运行测试验证通过**

Run: `python -m pytest tests/test_package_entry.py -x -q`
Expected: PASS

- [ ] **Step 9: 运行全量测试（此时可能有较多失败，因导入路径未更新）**

Run: `python -m pytest tests/ -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: 记录失败数，后续 Task 14 修复导入路径后恢复

- [ ] **Step 10: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add -A
git commit -m "refactor(structure): src/ 重命名为 hermes/，统一包入口

- src/ → hermes/，新增 __init__.py + __main__.py
- python -m hermes 成为唯一入口
- 删除 app.py __main__ 块（与 hermes/__main__.py 职责重复）
- 测试 sys.path 从 src 改为 hermes
- 为 Task 14 批量改写导入路径做准备"
```

---

## Task 14: 批量改写 51 处 try/except 导入样板为绝对导入

**Files:**
- Modify: 28 个源文件（51 处 try/except 导入样板）

**Interfaces:**
- Consumes: Task 13 `hermes/` 包目录
- Produces: 所有导入为 `from hermes.xxx import` 绝对路径，无 try/except 样板

- [ ] **Step 1: 写失败测试 — 无双重 try/except 导入样板**

```python
# tests/test_no_dual_import.py
"""测试无双重 try/except 导入样板。"""
from __future__ import annotations
import os
import re

_ROOT = os.path.join(os.path.dirname(__file__), "..")
_HERMES = os.path.join(_ROOT, "hermes")


class TestNoDualImport:

    def test_no_dual_try_except_import(self):
        """hermes/ 下无双重 try/except 导入样板。

        模式：try: from .x import Y / except ImportError: from x import Y
        """
        dual_import_pattern = re.compile(
            r"try:\s*\n\s*from \.\w+ import.*\nexcept ImportError.*\n\s*from \w+ import",
            re.MULTILINE
        )

        offenders = []
        for root, dirs, files in os.walk(_HERMES):
            for fname in files:
                if not fname.endswith(".py"):
                    continue
                fpath = os.path.join(root, fname)
                with open(fpath, "r", encoding="utf-8") as f:
                    content = f.read()
                matches = dual_import_pattern.findall(content)
                if matches:
                    offenders.append(f"{fpath}: {len(matches)} 处")

        # 允许必要的可选降级（chromadb 等），但双重 try/except 样板应为 0
        assert len(offenders) == 0, f"仍有双重 try/except 导入样板: {offenders}"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_no_dual_import.py -x -q`
Expected: FAIL（51 处样板仍存在）

- [ ] **Step 3: 批量改写导入语句**

对 `hermes/` 下每个文件，将双重 try/except 导入样板替换为单一绝对导入：

```python
# 修复前
try:
    from .module import X
except ImportError:  # pragma: no cover
    from module import X  # type: ignore

# 修复后
from hermes.module import X
```

**涉及文件清单**（按模块分组，用 Grep 找到所有 `except ImportError` 后逐一处理）：
- `hermes/orchestrator/` 下 4 个文件（13 处）
- `hermes/agent/` 下 8 个文件（11 处）
- `hermes/routes/` 下 4 个文件（9 处）
- `hermes/tasks/` 下 2 个文件（7 处）
- `hermes/memory/` 下 4 个文件（4 处）
- 其他 6 处

**注意**：保留必要的可选降级（chromadb、onnxruntime 等第三方库缺失时的 `except ImportError`），只删除"先相对导入再绝对导入"的双重样板。

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/test_no_dual_import.py -x -q`
Expected: PASS

- [ ] **Step 5: 运行全量测试**

Run: `python -m pytest tests/ -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: 无新增失败（passed 数量恢复到 2661 - 容差）

- [ ] **Step 6: 验证 grep 指标**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
findstr /s /r /n "except ImportError" hermes\
# Expected: 仅保留 chromadb/onnxruntime 等必要降级
```

- [ ] **Step 7: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add -A
git commit -m "refactor(structure): 删除 51 处双重 try/except 导入样板

- 28 个文件 51 处 try/except 样板改写为 from hermes.xxx import 绝对导入
- 保留必要的可选降级（chromadb/onnxruntime 等第三方库）
- 涉及 orchestrator/(13处) agent/(11处) routes/(9处) tasks/(7处) memory/(4处) 其他(6处)
- 统一包入口，导入样板清零"
```

---

## Task 15: 更新 Tauri sidecar + 部署脚本 + 创建 conftest.py

**Files:**
- Modify: `desktop/src-tauri/src/sidecar.rs`（启动命令 `python src/server.py` → `python -m hermes`）
- Modify: `deploy.sh`、`start.sh`、`restart.ps1`、`restart.sh`、`hermes-lite.service`
- Create: `tests/conftest.py`

**Interfaces:**
- Consumes: Task 13 `hermes/` 包目录
- Produces: 所有启动命令统一为 `python -m hermes`，测试 sys.path 统一配置

- [x] **Step 1: 写失败测试 — 启动命令和 conftest 验证**

```python
# tests/test_deployment_updated.py
"""测试部署脚本和 conftest 已更新。"""
from __future__ import annotations
import os

_ROOT = os.path.join(os.path.dirname(__file__), "..")


class TestDeploymentUpdated:

    def test_conftest_exists(self):
        """tests/conftest.py 存在。"""
        assert os.path.exists(os.path.join(_ROOT, "tests", "conftest.py"))

    def test_conftest_sets_hermes_path(self):
        """conftest.py 设置 hermes 包路径。"""
        with open(os.path.join(_ROOT, "tests", "conftest.py"), "r", encoding="utf-8") as f:
            content = f.read()
        assert "hermes" in content
        assert "sys.path" in content

    def test_sidecar_uses_hermes_module(self):
        """sidecar.rs 使用 python -m hermes。"""
        sidecar_path = os.path.join(_ROOT, "desktop", "src-tauri", "src", "sidecar.rs")
        if not os.path.exists(sidecar_path):
            return  # 桌面端可能不存在
        with open(sidecar_path, "r", encoding="utf-8") as f:
            content = f.read()
        assert "-m hermes" in content or "python\" \"-m\" \"hermes" in content
        assert "src/server.py" not in content

    def test_deploy_script_uses_hermes_module(self):
        """deploy.sh 使用 python -m hermes。"""
        deploy_path = os.path.join(_ROOT, "deploy.sh")
        if not os.path.exists(deploy_path):
            return
        with open(deploy_path, "r", encoding="utf-8") as f:
            content = f.read()
        assert "-m hermes" in content
        assert "src/server.py" not in content
```

- [x] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_deployment_updated.py -x -q`
Expected: FAIL（conftest.py 不存在，sidecar 未更新）

- [x] **Step 3: 创建 tests/conftest.py**

```python
# tests/conftest.py
"""pytest 全局配置：确保 hermes 包可导入。"""
import sys
from pathlib import Path

# 确保 hermes 包可导入
HERMES_ROOT = Path(__file__).parent.parent
HERMES_PKG = HERMES_ROOT / "hermes"
if str(HERMES_ROOT) not in sys.path:
    sys.path.insert(0, str(HERMES_ROOT))
if str(HERMES_PKG) not in sys.path:
    sys.path.insert(0, str(HERMES_PKG))
```

- [x] **Step 4: 更新 Tauri sidecar.rs** (跳过：desktop/ 在独立分支管理，dev 分支不存在)

在 `desktop/src-tauri/src/sidecar.rs` 中，将 Python 启动命令从：
```rust
// 修复前
let cmd = Command::new(python).args(["src/server.py"]);

// 修复后
let cmd = Command::new(python).args(["-m", "hermes"]);
```

- [x] **Step 5: 更新部署脚本**

`deploy.sh` / `start.sh` / `restart.sh`:
```bash
# 修复前
python src/server.py

# 修复后
python -m hermes
```

`restart.ps1`:
```powershell
# 修复前
python src/server.py

# 修复后
python -m hermes
```

`hermes-lite.service`:
```ini
# 修复前
ExecStart=/usr/bin/python /path/to/src/server.py

# 修复后
ExecStart=/usr/bin/python -m hermes
```

- [x] **Step 6: 运行测试验证通过**

Run: `python -m pytest tests/test_deployment_updated.py -x -q`
Expected: PASS（10 passed）

- [x] **Step 7: 运行全量测试**

Run: `python -m pytest tests/ -q --timeout=60 2>&1 | Select-Object -Last 10`
Expected: 无新增失败（实际：2762 passed, 98 failed，均为预存失败）

- [x] **Step 8: 验证 python -m hermes 启动**

Run: `python -m hermes --help 2>&1 | Select-Object -First 5` 或 `python -c "from hermes.app import app; print(app.title)"`
Expected: 正常输出（无 ImportError）

- [x] **Step 9: 验证 uvicorn hermes.app:app 启动**（通过 `from hermes.app import app` 验证）

- [x] **Step 10: Commit**

Run: `python -c "import uvicorn; uvicorn.run('hermes.app:app', host='127.0.0.1', port=18399)"` (后台运行 3 秒后停止)
Expected: 正常启动无报错

- [ ] **Step 10: Commit**

```bash
cd e:\Java\webser\web_app\webme\hermes-lite
git add -A
git commit -m "refactor(structure): 更新 Tauri sidecar + 部署脚本 + 创建 conftest.py

- sidecar.rs: python src/server.py → python -m hermes
- deploy.sh/start.sh/restart.ps1/restart.sh: 启动命令统一为 python -m hermes
- hermes-lite.service: ExecStart 改为 python -m hermes
- 新增 tests/conftest.py: 统一 sys.path 配置
- 阶段 3 完成：统一包入口，导入样板清零，部署脚本更新"
```

---

## Self-Review

### 1. Spec 覆盖检查

| Spec 章节 | 覆盖 Task | 状态 |
|-----------|----------|------|
| 3.2.1 修复 10 个工厂路径 | Task 1 | ✓ |
| 3.2.1 HealthChecker 工厂参数 | Task 1 | ✓ |
| 3.2.2 修复 hot_reloadable 死代码 | Task 2 | ✓ |
| 3.2.3 清理散落文件 | Task 3 | ✓ |
| 3.2.4 移除备份目录 | Task 3 | ✓ |
| 3.2.5 更新 .gitignore | Task 3 | ✓ |
| 4.2.1 删除 server.py 全局变量 | Task 11 | ✓ |
| 4.2.2 删除 state.py | Task 11 | ✓ |
| 4.2.3 修改 lifespan.py | Task 7 | ✓ |
| 4.2.4 新增 BackgroundTaskRegistry | Task 4 | ✓ |
| 4.2.5 添加 Depends 函数 | Task 5 | ✓ |
| 4.2.6 改写 11 个 routes（148 行） | Task 8/9/10 | ✓ |
| 4.2.7 改造软重启 | Task 10 | ✓ |
| 4.2.8 改造 background_loops | Task 6 | ✓ |
| 4.2.9 修改测试 60 处 patch | Task 8/9/10/11 | ✓ |
| 5.2.1 创建包入口 | Task 13 | ✓ |
| 5.2.2 批量改写导入 | Task 14 | ✓ |
| 5.2.3 删除 server.py re-export | Task 11 | ✓ |
| 5.2.4 更新 Tauri sidecar | Task 15 | ✓ |
| 5.2.5 更新部署脚本 | Task 15 | ✓ |
| 5.2.6 更新测试 conftest | Task 15 | ✓ |
| 6.4 reload 后重启 task | Task 12 | ✓ |

### 2. 占位符扫描

- 无 "TBD" / "TODO" / "implement later"
- 无 "add appropriate error handling"
- 无 "similar to Task N"（每个 Task 的代码都完整给出）
- 所有步骤都包含实际代码

### 3. 类型一致性

- `BackgroundTaskRegistry` 在 Task 4 定义，Task 7/10/12 使用 — 方法名一致（`register` / `get` / `restart` / `cancel_all`）
- `get_orchestrator` / `get_session_logger` 等在 Task 5 定义，Task 8/9/10/11 使用 — 函数名一致
- `cleanup_loop(session_logger, metrics_store, orchestrator)` 在 Task 6 定义，Task 7/12 使用 — 签名一致
- `metrics_persist_loop(metrics_collector, metrics_store, reset_event)` 在 Task 6 定义，Task 7/12 使用 — 签名一致
- `app.state.metrics_reset_event` 在 Task 7 创建，Task 8/10/12 使用 — 属性名一致
- `app.state.task_registry` 在 Task 7 创建，Task 10/12 使用 — 属性名一致

### 4. 依赖顺序验证

```
Task 1 (工厂路径) ──┐
Task 2 (hot_reloadable) ──┤
Task 3 (清理) ──┤
                    ↓
Task 4 (BackgroundTaskRegistry) ──┐
Task 5 (Depends 函数) ──┤
Task 6 (background_loops) ──┤
                    ↓
Task 7 (lifespan) ←── 依赖 Task 4/5/6
                    ↓
Task 8 (routes 批次 1) ←── 依赖 Task 5
Task 9 (routes 批次 2) ←── 依赖 Task 5
Task 10 (routes 批次 3) ←── 依赖 Task 4/5
                    ↓
Task 11 (删除 state.py + server.py) ←── 依赖 Task 8/9/10
                    ↓
Task 12 (阶段 2 验证) ←── 依赖 Task 4-11
                    ↓
Task 13 (重命名 hermes/) ←── 依赖 Task 1-12
                    ↓
Task 14 (改写导入) ←── 依赖 Task 13
Task 15 (部署脚本) ←── 依赖 Task 13
```

依赖顺序正确，无循环依赖。

### 5. 子智能体委派建议

每个 Task 可独立委派给子智能体执行，关键约束：
- **Task 1-3**：可并行执行（无相互依赖）
- **Task 4-6**：可并行执行（无相互依赖）
- **Task 7**：必须在 Task 4/5/6 完成后执行
- **Task 8/9/10**：可在 Task 5 完成后并行执行（但修改同一测试文件需协调）
- **Task 11**：必须在 Task 8/9/10 完成后执行
- **Task 12**：必须在 Task 4-11 全部完成后执行
- **Task 13**：必须在 Task 1-12 全部完成后执行
- **Task 14/15**：可在 Task 13 完成后并行执行

### 6. 统一验收标准

每个 Task 完成后，子智能体需报告：
1. 修改的文件列表
2. 测试运行结果（passed/failed 数量）
3. grep 验证结果（如适用）
4. 行数验证结果（如适用）
5. commit SHA

主智能体验收时检查：
1. 测试无新增失败（passed ≥ 2661 - 容差）
2. grep 指标达标（import state = 0, Depends(get_ ≥ 61, _get_server_globals = 0）
3. 行数达标（lifespan ≤ 250, server ≤ 35）
4. `python -m hermes` 正常启动
5. 热重载和软重启功能正常
