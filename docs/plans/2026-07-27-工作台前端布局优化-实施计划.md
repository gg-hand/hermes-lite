# 工作台前端布局优化 Implementation Plan

> **For agentic workers:** Use the java-dev-skills workflow to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复工作台前端布局问题——左栏协作列表后端索引几乎永远为空、Agent Card 字段堆砌缺信息层级、右栏上下空窗严重；同时补全古早记录分页加载和跳转定位能力。

**Architecture:** 三层修复：Layer A 后端新增 events API + 扩展 messages 支持 before_seq 分页 + 扩展 status 返回 epoch_history；Layer B 前端左栏重设为「Agents 接入 + 活动档案时间线」双区，移除协作列表；Layer C 前端 Agent Card 模态框重设计为分层卡片（头部摘要 / 主字段 / 元数据 / 操作区）；Layer D 右栏重组为「Director 控制台」单一概念（状态卡 + 快捷操作 + 实时指标）；Layer E 响应式 + 主题适配 + 古早记录分页加载。所有改动遵循项目「轻量化、避免臃肿、毛玻璃按钮、圆角处理、统一错误处理」硬约束。

**Tech Stack:** Python 3.11+ / FastAPI / asyncio / pytest / pytest-asyncio / vanilla JS (ES5 兼容) / CSS Grid + Flexbox / 设计令牌（tokens.css）

## Global Constraints

- 所有改动必须保持现有测试通过（执行 `pytest tests/multiagent/` 验证回归）
- 后端改动遵循 TDD：先写失败测试 → 验证失败 → 最小实现 → 验证通过 → 提交
- 前端无自动化测试框架，采用手动验证步骤替代（每步明确预期效果）
- 路径解析使用运行时动态获取（`_get_bb_root()` 已存在，复用），禁止绝对路径硬编码
- API 端点错误返回精炼错误信息（HTTPException detail 字符串清晰），不抛 500
- 新增 API 必须支持 multiagent.enabled=False 时返回 404（与现有路由一致）
- CSS 复用 tokens.css 设计令牌，禁止硬编码颜色值（除语义色 fallback `#ef4444` 等）
- JS 文件保持 ES5 兼容（IIFE 模式，`var` 声明，避免箭头函数/let/const）
- HTML 结构改动必须同时更新 CSS 缓存版本号（`?v=2026072Xx`）
- 模态框复用 components.css 的 .modal 基础样式，仅扩展不重写
- 跳转锚点定位使用 `data-seq` 属性 + `scrollIntoView` + 临时 highlight 类
- 时间分组相对时间使用 `Intl.RelativeTimeFormat` 或简化的本地函数
- 浅色主题适配必须同步测试（切换主题验证视觉无破损）

---

## 问题全景

### 用户报告的问题

1. 左栏「协作列表」真的有作用吗？真的能读取到数据吗？
2. 左栏打开的 Agent Card 卡片样式需要优化，不要杂乱窗口展示
3. 右侧栏明显的上下内容填充不足，导致有空窗
4. 整体细节需要揪出更多问题，制定详细方案优化

### 根因分析

| 问题 | 根因 | 影响 |
|---|---|---|
| 协作列表永远为空 | `CollabWriter.append` 仅在 `collab_id` 非空 **且** `type="request"` 时自动更新索引；而 `/broadcast`、`/directive`、`/forward`、`/announce` 端点都不传 collab_id | 用户感知「功能死掉」 |
| Agent Card 字段堆砌 | 18 字段平铺 grid（140px label + 1fr value），无头部摘要、无状态徽章、无视觉锚点 | 信息层级缺失，无法快速识别重要字段 |
| 右栏空窗 | 3 个面板内容稀疏（Director 状态 4 行 / 操作 3 按钮 / 统计 3 数字），260px 宽下大量留白 | 视觉空洞，未利用空间展示有用信息 |
| 古早记录无法回溯 | 消息流仅 `loadRecentMessages` 加载 50 条，无分页；活动事件无时间分组 | 历史内容不可达 |

### 同类问题（一并修复）

- Agent 列表卡片：id 与 name 无层级区分，能力仅显示前 2 个+数字
- 消息流缺时间分组、缺 agent 头像/标识图标
- 900px 以下隐藏右栏但左栏仍 200px，挤压中栏
- 浅色主题下 Agent Card 模态框未单独调优

---

## File Structure

| 文件 | 职责 | 改动类型 |
|---|---|---|
| `teage_liu/multiagent/blackboard.py` | 扩展 `read_collab_messages` 支持 `before_seq` 参数 | Modify |
| `teage_liu/multiagent/collaboration_routes.py` | 新增 `/events` 端点；扩展 `/messages` 支持 `before_seq`；扩展 `/collabs` 占位说明 | Modify |
| `teage_liu/multiagent/event_extractor.py` | 新增模块：从 collab messages 提取活动事件 + 分页 | Create |
| `teage_liu/api/multiagent_routes.py` | 扩展 `/status` 返回 `epoch_history` 字段 | Modify |
| `tests/multiagent/test_event_extractor.py` | event_extractor 单元测试 | Create |
| `tests/multiagent/test_collaboration_routes.py` | 扩展 events/before_seq 端点测试 | Modify |
| `teage-liu/web/workbench.html` | 左栏重设双区结构 / 右栏 Director 控制台三区 / Agent Card 模态框结构改造 / 移除协作列表面板 | Modify |
| `teage-liu/web/static/css/workbench.css` | 左栏双区样式 / Agent Card 分层卡片样式 / 右栏控制台样式 / 响应式调优 | Modify |
| `teage-liu/web/static/css/collab-workbench.css` | 活动档案时间线样式 / 跳转高亮闪烁动画 | Modify |
| `teage-liu/web/static/js/workbench.js` | Agent Card 分层渲染 / 右栏指标渲染 / 模板指令填充 / 响应式抽屉 | Modify |
| `teage-liu/web/static/js/collab-workbench.js` | 移除协作列表逻辑 / 新增活动档案时间线渲染 + 分页 + 跳转 / 消息流古早加载 | Modify |

---

## 任务依赖图

```
[Task 1] events API 提取器 ─┬─→ [Task 4] /events 端点
                           └─→ [Task 9] 时间线渲染依赖
[Task 2] read_collab_messages before_seq ─→ [Task 5] /messages 扩展 ─→ [Task 19] 古早加载
[Task 3] /status epoch_history ─→ [Task 13] epoch 时间线渲染
[Task 6] 左栏 HTML 双区结构 ─→ [Task 7] Agents 卡片升级 ─→ [Task 8] 时间线渲染
[Task 10] Agent Card HTML 结构 ─→ [Task 11] 分层卡片样式 ─→ [Task 12] 进度条+复制 JSON
[Task 14] 右栏 HTML 控制台结构 ─→ [Task 15] Director 状态卡 ─→ [Task 16] 快捷模板 ─→ [Task 17] 实时指标
[Task 18] 响应式 + 主题适配
[Task 19] 中栏古早加载
[Task 20] 统一验收审查
```

---

## 阶段 1：后端 API 扩展（Task 1-5）

### Task 1: 活动事件提取器（event_extractor 模块）

**Files:**
- Create: `teage_liu/multiagent/event_extractor.py`
- Test: `tests/multiagent/test_event_extractor.py`

**Interfaces:**
- Consumes: `teage_liu.multiagent.blackboard.read_collab_messages(bb_root, collab_id=None) -> list[dict]`
- Produces: `extract_events(messages: list[dict], before_ts: Optional[str] = None, limit: int = 50) -> list[dict]`，每条 event 含 `event_type` / `ts` / `agent_id` / `summary` / `seq` 字段

- [ ] **Step 1: 写失败测试 — 基本事件提取**

创建 `tests/multiagent/test_event_extractor.py`：

```python
"""event_extractor 单元测试。"""
import pytest
from datetime import datetime, timezone

from teage_liu.multiagent.event_extractor import extract_events


def _msg(seq, type_, from_, ts, **extra):
    """构造测试消息。"""
    return {"seq": seq, "type": type_, "from": from_, "timestamp": ts, **extra}


def test_extract_events_agent_online():
    """agent announce online 消息应提取为 agent_online 事件。"""
    ts = "2026-07-27T10:00:00+00:00"
    messages = [
        _msg(1, "announce", "agent-001", ts, action="online",
             agent_name="Worker-A", capabilities=["task_exec"]),
    ]
    events = extract_events(messages)
    assert len(events) == 1
    assert events[0]["event_type"] == "agent_online"
    assert events[0]["agent_id"] == "agent-001"
    assert events[0]["ts"] == ts
    assert events[0]["seq"] == 1
    assert "Worker-A" in events[0]["summary"]


def test_extract_events_user_broadcast():
    """用户广播（from=user, type=request）应提取为 user_broadcast 事件。"""
    ts = "2026-07-27T11:00:00+00:00"
    messages = [
        _msg(2, "request", "user", ts, content="请所有 agent 汇报进度", to="*"),
    ]
    events = extract_events(messages)
    assert len(events) == 1
    assert events[0]["event_type"] == "user_broadcast"
    assert events[0]["agent_id"] == "user"
    assert "请所有 agent 汇报进度" in events[0]["summary"]


def test_extract_events_directive_injected():
    """director directive 消息应提取为 directive_injected 事件。"""
    ts = "2026-07-27T12:00:00+00:00"
    messages = [
        _msg(3, "directive", "director", ts, content="优先处理用户请求",
             rule_type="ordering", priority="high"),
    ]
    events = extract_events(messages)
    assert len(events) == 1
    assert events[0]["event_type"] == "directive_injected"
    assert events[0]["agent_id"] == "director"
    assert "ordering" in events[0]["summary"]
    assert "high" in events[0]["summary"]


def test_extract_events_filters_untracked_types():
    """relay / response / status / result 等非事件类型应被过滤。"""
    ts = "2026-07-27T13:00:00+00:00"
    messages = [
        _msg(1, "relay", "agent-001", ts, content="hello"),
        _msg(2, "response", "agent-002", ts, content="ok"),
        _msg(3, "status", "agent-001", ts, content="running"),
    ]
    events = extract_events(messages)
    assert events == []


def test_extract_events_sorted_desc_by_ts():
    """事件应按时间倒序返回（最新在前）。"""
    messages = [
        _msg(1, "announce", "a1", "2026-07-27T10:00:00+00:00", action="online"),
        _msg(2, "announce", "a2", "2026-07-27T12:00:00+00:00", action="online"),
        _msg(3, "announce", "a3", "2026-07-27T11:00:00+00:00", action="online"),
    ]
    events = extract_events(messages)
    assert len(events) == 3
    assert events[0]["agent_id"] == "a2"  # 12:00 最新
    assert events[1]["agent_id"] == "a3"  # 11:00
    assert events[2]["agent_id"] == "a1"  # 10:00 最早


def test_extract_events_before_ts_pagination():
    """before_ts 游标分页：仅返回 ts 严格小于 before_ts 的事件。"""
    messages = [
        _msg(1, "announce", "a1", "2026-07-27T10:00:00+00:00", action="online"),
        _msg(2, "announce", "a2", "2026-07-27T11:00:00+00:00", action="online"),
        _msg(3, "announce", "a3", "2026-07-27T12:00:00+00:00", action="online"),
    ]
    # 取 11:00 之前的事件
    events = extract_events(messages, before_ts="2026-07-27T11:00:00+00:00")
    assert len(events) == 1
    assert events[0]["agent_id"] == "a1"


def test_extract_events_limit_caps_count():
    """limit 参数限制返回数量。"""
    messages = [
        _msg(i, "announce", f"a{i}", f"2026-07-27T1{i:02d}:00:00+00:00", action="online")
        for i in range(1, 6)
    ]
    events = extract_events(messages, limit=3)
    assert len(events) == 3
    # 应取最新的 3 条（12/13/14/15 中最新的）
    assert events[0]["agent_id"] == "a5"


def test_extract_events_agent_offline():
    """agent offline 消息应提取为 agent_offline 事件。"""
    ts = "2026-07-27T14:00:00+00:00"
    messages = [
        _msg(1, "announce", "agent-001", ts, action="offline"),
    ]
    events = extract_events(messages)
    assert len(events) == 1
    assert events[0]["event_type"] == "agent_offline"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/multiagent/test_event_extractor.py -v`
Expected: FAIL（模块不存在 ImportError）

- [ ] **Step 3: 实现 event_extractor 模块**

创建 `teage_liu/multiagent/event_extractor.py`：

```python
"""活动事件提取器。

从协作消息中提取关键事件，供工作台左栏「活动档案时间线」展示。

事件类型：
- agent_online       agent announce action=online
- agent_offline      agent announce action=offline
- user_broadcast     from=user, type=request, to=*（用户广播）
- directive_injected from=director, type=directive（Director 注入引导）

过滤：relay / response / status / result 等非事件类型不提取。

排序：按 ts 时间倒序（最新在前）。
分页：支持 before_ts 游标（返回 ts 严格小于 before_ts 的事件）+ limit 上限。
"""
from __future__ import annotations

from typing import Optional


def _parse_ts(ts: str) -> float:
    """解析 ISO 时间戳为 epoch 秒。失败返回 0。"""
    if not ts:
        return 0.0
    try:
        from datetime import datetime
        # 处理带时区和不带时区两种格式
        ts_str = ts.replace("Z", "+00:00")
        dt = datetime.fromisoformat(ts_str)
        if dt.tzinfo is None:
            from datetime import timezone
            dt = dt.replace(tzinfo=timezone.utc)
        return dt.timestamp()
    except Exception:
        return 0.0


def _summarize(msg: dict, event_type: str) -> str:
    """根据事件类型生成摘要。"""
    content = msg.get("content", "")
    if event_type == "agent_online":
        name = msg.get("agent_name") or msg.get("from", "")
        caps = msg.get("capabilities") or []
        cap_text = ", ".join(caps[:2]) if caps else "无能力声明"
        return f"{name} 上线 · {cap_text}"
    if event_type == "agent_offline":
        return f"{msg.get('from', '')} 离线"
    if event_type == "user_broadcast":
        return content[:80] + ("…" if len(content) > 80 else "")
    if event_type == "directive_injected":
        rule = msg.get("rule_type", "ordering")
        pri = msg.get("priority", "normal")
        return f"{rule} / {pri} · {content[:60]}"
    return content[:80]


def _classify(msg: dict) -> Optional[str]:
    """根据消息字段分类事件类型。返回 None 表示非事件。"""
    msg_type = msg.get("type", "")
    action = msg.get("action", "")
    sender = msg.get("from", "")

    if msg_type == "announce":
        if action == "online":
            return "agent_online"
        if action == "offline":
            return "agent_offline"
        return None

    if msg_type == "request" and sender == "user":
        return "user_broadcast"

    if msg_type == "directive" and sender == "director":
        return "directive_injected"

    return None


def extract_events(
    messages: list[dict],
    before_ts: Optional[str] = None,
    limit: int = 50,
) -> list[dict]:
    """从协作消息列表提取活动事件。

    Args:
        messages: 协作消息列表（来自 read_collab_messages）
        before_ts: 游标分页，仅返回 ts 严格小于此值的事件；None 表示不限制
        limit: 返回数量上限，默认 50

    Returns:
        事件列表，按 ts 倒序排列。每条含：
        - event_type: str  事件类型
        - ts: str          原始时间戳
        - agent_id: str    发起方标识（agent_id / user / director）
        - summary: str     一句话摘要
        - seq: int         对应消息的 seq（用于跳转定位）
    """
    cursor_ts = _parse_ts(before_ts) if before_ts else None

    events: list[dict] = []
    for msg in messages:
        event_type = _classify(msg)
        if event_type is None:
            continue

        ts = msg.get("timestamp") or msg.get("ts") or ""
        ts_epoch = _parse_ts(ts)

        # before_ts 游标：仅保留严格小于游标的事件
        if cursor_ts is not None and ts_epoch >= cursor_ts:
            continue

        events.append({
            "event_type": event_type,
            "ts": ts,
            "agent_id": msg.get("from", ""),
            "summary": _summarize(msg, event_type),
            "seq": msg.get("seq", 0),
        })

    # 按 ts 倒序（最新在前）
    events.sort(key=lambda e: _parse_ts(e["ts"]), reverse=True)

    # limit 截断
    if limit > 0:
        events = events[:limit]

    return events
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/multiagent/test_event_extractor.py -v`
Expected: PASS（8 个测试全通过）

- [ ] **Step 5: 提交**

```bash
git add teage_liu/multiagent/event_extractor.py tests/multiagent/test_event_extractor.py
git commit -m "feat(multiagent): add event_extractor module for activity timeline"
```

---

### Task 2: 扩展 read_collab_messages 支持 before_seq

**Files:**
- Modify: `teage_liu/multiagent/blackboard.py:501-512`
- Test: `tests/multiagent/test_collab_blackboard.py`（已有文件，新增测试）

**Interfaces:**
- Consumes: 现有 `read_collab_messages(bb_root, collab_id=None) -> list[dict]`
- Produces: `read_collab_messages(bb_root, collab_id=None, before_seq=None, limit=None) -> list[dict]`，向后兼容（新参数均默认 None）

- [ ] **Step 1: 写失败测试 — before_seq 过滤**

在 `tests/multiagent/test_collab_blackboard.py` 末尾追加：

```python
@pytest.mark.asyncio
async def test_read_collab_messages_before_seq(bb_root, monkeypatch):
    """read_collab_messages 支持 before_seq 参数：返回 seq 严格小于 before_seq 的消息。"""
    from teage_liu.multiagent.blackboard import append_collab_message, read_collab_messages

    # 写入 3 条消息
    for i in range(1, 4):
        await append_collab_message(bb_root, {
            "type": "status", "from": "agent-x",
            "content": f"msg-{i}", "timestamp": f"2026-07-27T1{i}:00:00+00:00",
        })

    # 取 seq < 3 的消息（应为 seq=1, 2）
    msgs = await read_collab_messages(bb_root, before_seq=3)
    seqs = [m["seq"] for m in msgs]
    assert seqs == [1, 2]


@pytest.mark.asyncio
async def test_read_collab_messages_limit(bb_root):
    """limit 参数限制返回数量（取最新的 N 条）。"""
    from teage_liu.multiagent.blackboard import append_collab_message, read_collab_messages

    for i in range(1, 6):
        await append_collab_message(bb_root, {
            "type": "status", "from": "agent-y",
            "content": f"msg-{i}", "timestamp": f"2026-07-27T1{i}:00:00+00:00",
        })

    # 取最新 2 条（应为 seq=4, 5）
    msgs = await read_collab_messages(bb_root, limit=2)
    seqs = [m["seq"] for m in msgs]
    assert seqs == [4, 5]


@pytest.mark.asyncio
async def test_read_collab_messages_backward_compatible(bb_root):
    """新参数不传时行为与旧版完全一致。"""
    from teage_liu.multiagent.blackboard import append_collab_message, read_collab_messages

    await append_collab_message(bb_root, {
        "type": "status", "from": "agent-z", "content": "test",
    })
    msgs = await read_collab_messages(bb_root)
    assert len(msgs) == 1
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/multiagent/test_collab_blackboard.py::test_read_collab_messages_before_seq -v`
Expected: FAIL（`TypeError: read_collab_messages() got an unexpected keyword argument 'before_seq'`）

- [ ] **Step 3: 修改 read_collab_messages 签名与实现**

修改 `teage_liu/multiagent/blackboard.py:501-512`：

```python
async def read_collab_messages(
    bb_root: Path,
    collab_id: Optional[str] = None,
    before_seq: Optional[int] = None,
    limit: Optional[int] = None,
) -> list[dict]:
    """读取协作消息（全局或指定协作）。

    复用 _parse_frontmatter_blocks 解析逻辑（A1 修复）。

    Args:
        bb_root: 黑板根目录
        collab_id: 协作 ID。None → 全局 collaboration.md；否则 → collabs/{collab_id}.md
        before_seq: 游标分页，仅返回 seq 严格小于此值的消息；None 表示不限制
        limit: 返回数量上限，取最新的 N 条；None 或 0 表示不限制

    Returns:
        消息列表。before_seq 应用后再按 seq 升序返回；limit 截取尾部最新 N 条。
    """
    file_path = _get_collab_file(bb_root, collab_id)
    if not file_path.exists():
        return []
    content = file_path.read_text(encoding="utf-8")
    messages = _parse_frontmatter_blocks(content)

    # before_seq 过滤（严格小于）
    if before_seq is not None:
        messages = [m for m in messages
                    if isinstance(m.get("seq"), int) and m["seq"] < before_seq]

    # limit 截断（取最新 N 条）
    if limit is not None and limit > 0:
        messages = messages[-limit:] if len(messages) > limit else messages

    return messages
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/multiagent/test_collab_blackboard.py -v -k "read_collab_messages"`
Expected: PASS（4 个测试全通过，包括原有 + 新增 3 个）

- [ ] **Step 5: 运行全部 multiagent 测试验证无回归**

Run: `pytest tests/multiagent/ -v`
Expected: PASS（所有原有测试仍通过）

- [ ] **Step 6: 提交**

```bash
git add teage_liu/multiagent/blackboard.py tests/multiagent/test_collab_blackboard.py
git commit -m "feat(blackboard): add before_seq/limit pagination to read_collab_messages"
```

---

### Task 3: 扩展 /status 端点返回 epoch_history

**Files:**
- Modify: `teage_liu/api/multiagent_routes.py:72-115`（get_status 函数）
- Test: `tests/multiagent/test_multiagent_routes.py`（新增或扩展）

**Interfaces:**
- Consumes: `read_collab_messages(bb_root)`（已存在）
- Produces: `/api/multiagent/status` 响应新增 `epoch_history: list[dict]`，每条含 `epoch: int` / `ts: str` / `seq: int`

- [ ] **Step 1: 写失败测试 — status 返回 epoch_history**

在 `tests/multiagent/test_multiagent_routes.py` 中追加（若无此文件则创建）：

```python
"""multiagent_routes 端点测试。"""
import pytest
from pathlib import Path

from teage_liu.api.multiagent_routes import create_multiagent_router


def _make_container(bb_root, enabled=True, role="worker"):
    """构造测试用 DI 容器 mock。"""
    class _Cfg:
        def __init__(self, d): self._d = d
        def get(self, k, default=None): return self._d.get(k, default)
    class _Container:
        def __init__(self):
            self.config = _Cfg({
                "multiagent": {
                    "enabled": enabled,
                    "role": role,
                    "blackboard_dir": str(bb_root),
                }
            })
        def get(self, k): return None
    return _Container()


@pytest.mark.asyncio
async def test_status_returns_epoch_history(bb_root, monkeypatch):
    """/status 响应应包含 epoch_history 字段，从 collab directive 消息提取。"""
    monkeypatch.setenv("TEAGE_BB_ROOT", str(bb_root))
    from teage_liu.multiagent.blackboard import append_collab_message

    # 写入 3 条 directive 消息模拟 epoch 推进
    for i in range(1, 4):
        await append_collab_message(bb_root, {
            "type": "directive", "from": "director",
            "content": f"directive-{i}",
            "timestamp": f"2026-07-27T1{i}:00:00+00:00",
        })

    container = _make_container(bb_root)
    router = create_multiagent_router(container)

    # 找到 get_status 路由并直接调用
    status_route = next(r for r in router.routes if getattr(r, "path", "") == "/api/multiagent/status")
    response = await status_route.endpoint()

    assert "epoch_history" in response
    history = response["epoch_history"]
    assert isinstance(history, list)
    assert len(history) <= 5  # 最多 5 个节点
    # 每条应有 epoch / ts / seq 字段
    if history:
        assert "epoch" in history[0]
        assert "ts" in history[0]
        assert "seq" in history[0]
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/multiagent/test_multiagent_routes.py::test_status_returns_epoch_history -v`
Expected: FAIL（`KeyError: 'epoch_history'` 或类似）

- [ ] **Step 3: 修改 get_status 返回 epoch_history**

修改 `teage_liu/api/multiagent_routes.py:72-115`，在 `return` 前添加 epoch_history 提取逻辑：

```python
    @router.get("/status")
    async def get_status() -> dict:
        """获取协作总览状态。"""
        from teage_liu.multiagent.agent_registry import AgentRegistry
        from teage_liu.multiagent.blackboard import (
            read_director_md, read_json, _read_last_message_seq,
            read_collab_messages,
        )
        from teage_liu.multiagent.schema_validator import SchemaValidator

        status = read_json(bb_root / "status.json") if (bb_root / "status.json").exists() else {}
        director_md = await read_director_md(bb_root) or {}
        registry = AgentRegistry(bb_root, SchemaValidator(enabled=False))
        agents = await registry.list_active_agents()

        director_state = _determine_director_state(director_md)
        director_agent_id = status.get("director_signature", "") or director_md.get("director_id", "")
        director_epoch = director_md.get("current_epoch", 0)
        last_tick = director_md.get("last_director_tick", "")

        autonomous_mode = status.get("recovery_stage", "idle") == "autonomous"

        last_message_seq = await _read_last_message_seq(bb_root)

        local_agent_id = ""
        if multiagent_cfg.get("role") == "worker":
            local_agent_id = multiagent_cfg.get("worker", {}).get("agent_id", "")
        elif multiagent_cfg.get("role") == "director":
            local_agent_id = director_agent_id

        # epoch_history：从 collab directive 消息提取最近 5 个节点
        # 每条 directive 视为一次 epoch 推进；若无历史则仅返回当前 epoch 单节点
        epoch_history: list[dict] = []
        try:
            collab_msgs = await read_collab_messages(bb_root)
            directive_msgs = [
                m for m in collab_msgs
                if m.get("type") == "directive" and m.get("from") == "director"
            ]
            # 取最新 5 条，按时间倒序
            directive_msgs_sorted = sorted(
                directive_msgs,
                key=lambda m: m.get("timestamp", ""),
                reverse=True,
            )[:5]
            # epoch 编号：从当前 epoch 倒推（最新 directive = 当前 epoch）
            for idx, m in enumerate(directive_msgs_sorted):
                epoch_history.append({
                    "epoch": director_epoch - idx,
                    "ts": m.get("timestamp", ""),
                    "seq": m.get("seq", 0),
                })
        except Exception:
            # 提取失败时降级为空列表，不阻塞 status 响应
            pass

        # 若 epoch_history 为空，至少返回当前 epoch 单节点
        if not epoch_history and director_epoch:
            epoch_history = [{
                "epoch": director_epoch,
                "ts": last_tick or "",
                "seq": 0,
            }]

        return {
            "enabled": True,
            "role": multiagent_cfg.get("role", "worker"),
            "local_agent_id": local_agent_id,
            "director": {
                "agent_id": director_agent_id,
                "epoch": director_epoch,
                "last_tick": last_tick,
                "state": director_state,
            },
            "agents": agents,
            "autonomous_mode": autonomous_mode,
            "last_message_seq": last_message_seq,
            "epoch_history": epoch_history,
        }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/multiagent/test_multiagent_routes.py::test_status_returns_epoch_history -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add teage_liu/api/multiagent_routes.py tests/multiagent/test_multiagent_routes.py
git commit -m "feat(multiagent): expose epoch_history in /status endpoint"
```

---

### Task 4: 新增 /api/multiagent/collab/events 端点

**Files:**
- Modify: `teage_liu/multiagent/collaboration_routes.py`（在 `create_collab_router` 内新增路由）
- Test: `tests/multiagent/test_collaboration_routes.py`（扩展）

**Interfaces:**
- Consumes: `teage_liu.multiagent.event_extractor.extract_events(messages, before_ts, limit)`（Task 1 产出）
- Consumes: `read_collab_messages(bb_root)`（已存在）
- Produces: `GET /api/multiagent/collab/events?before_ts={iso_ts}&limit={int}` 返回 `{"events": [...], "next_before_ts": str|null}`

- [ ] **Step 1: 写失败测试 — events 端点返回活动事件**

在 `tests/multiagent/test_collaboration_routes.py` 中追加：

```python
@pytest.mark.asyncio
async def test_events_endpoint_returns_activity_events(bb_root, monkeypatch):
    """/events 端点应从协作消息提取活动事件。"""
    monkeypatch.setenv("TEAGE_BB_ROOT", str(bb_root))
    from teage_liu.multiagent.collaboration_routes import create_collab_router
    from teage_liu.multiagent.blackboard import append_collab_message

    # 写入混合消息
    await append_collab_message(bb_root, {
        "type": "announce", "from": "agent-a", "action": "online",
        "agent_name": "AgentA", "capabilities": ["task_exec"],
        "timestamp": "2026-07-27T10:00:00+00:00",
    })
    await append_collab_message(bb_root, {
        "type": "request", "from": "user", "content": "hello all", "to": "*",
        "timestamp": "2026-07-27T11:00:00+00:00",
    })
    await append_collab_message(bb_root, {
        "type": "relay", "from": "agent-a", "content": "noise",
        "timestamp": "2026-07-27T11:30:00+00:00",
    })  # 应被过滤

    router = create_collab_router()
    events_route = next(r for r in router.routes if getattr(r, "path", "") == "/api/multiagent/collab/events")
    response = await events_route.endpoint()

    assert "events" in response
    assert len(response["events"]) == 2  # 仅 announce + broadcast，relay 被过滤
    types = [e["event_type"] for e in response["events"]]
    assert "agent_online" in types
    assert "user_broadcast" in types


@pytest.mark.asyncio
async def test_events_endpoint_before_ts_pagination(bb_root, monkeypatch):
    """/events 端点支持 before_ts 游标分页。"""
    monkeypatch.setenv("TEAGE_BB_ROOT", str(bb_root))
    from teage_liu.multiagent.collaboration_routes import create_collab_router
    from teage_liu.multiagent.blackboard import append_collab_message

    for i in range(1, 4):
        await append_collab_message(bb_root, {
            "type": "announce", "from": f"agent-{i}", "action": "online",
            "timestamp": f"2026-07-27T1{i}:00:00+00:00",
        })

    router = create_collab_router()
    events_route = next(r for r in router.routes if getattr(r, "path", "") == "/api/multiagent/collab/events")
    response = await events_route.endpoint(before_ts="2026-07-27T12:00:00+00:00")

    # 应返回 11:00 和 10:00 两条（seq=1, 2）
    assert len(response["events"]) == 2
    # next_before_ts 应指向最早事件的 ts，便于前端继续分页
    assert response["next_before_ts"] is not None
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/multiagent/test_collaboration_routes.py -v -k "events_endpoint"`
Expected: FAIL（路由不存在）

- [ ] **Step 3: 在 collaboration_routes.py 中新增 events 路由**

在 `teage_liu/multiagent/collaboration_routes.py` 的 `create_collab_router` 内（`list_collabs` 路由之后、`collab_sse` 路由之前）插入：

```python
    @router.get("/events")
    async def list_events(before_ts: Optional[str] = None, limit: int = 50):
        """列出活动事件（按时间倒序）。

        供工作台左栏「活动档案时间线」使用。从协作消息提取关键事件：
        - agent_online / agent_offline（announce）
        - user_broadcast（from=user, type=request）
        - directive_injected（from=director, type=directive）

        Args:
            before_ts: ISO 时间戳游标，仅返回 ts 严格小于此值的事件；None 表示从头开始
            limit: 返回数量上限，默认 50

        Returns:
            events: 事件列表（每条含 event_type/ts/agent_id/summary/seq）
            next_before_ts: 下一次分页应使用的游标（最早事件的 ts）；
                           无更多数据时为 None
        """
        from teage_liu.multiagent.event_extractor import extract_events

        bb_root = _get_bb_root()
        messages = await read_collab_messages(bb_root)
        events = extract_events(messages, before_ts=before_ts, limit=limit)

        # 计算 next_before_ts：最早事件的 ts
        next_before_ts = None
        if events and len(events) == limit:
            # 返回数量等于 limit，可能还有更早数据
            next_before_ts = events[-1].get("ts") or None

        return {"events": events, "next_before_ts": next_before_ts}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/multiagent/test_collaboration_routes.py -v -k "events_endpoint"`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add teage_liu/multiagent/collaboration_routes.py tests/multiagent/test_collaboration_routes.py
git commit -m "feat(collab): add /events endpoint for activity timeline"
```

---

### Task 5: 扩展 /api/multiagent/collab/messages 支持 before_seq 参数

**Files:**
- Modify: `teage_liu/multiagent/collaboration_routes.py:110-115`（get_messages 函数）
- Test: `tests/multiagent/test_collaboration_routes.py`

**Interfaces:**
- Consumes: `read_collab_messages(bb_root, collab_id, before_seq, limit)`（Task 2 产出）
- Produces: `GET /api/multiagent/collab/messages?collab_id=&before_seq=&limit=` 返回符合游标条件的消息

- [ ] **Step 1: 写失败测试 — before_seq 分页**

在 `tests/multiagent/test_collaboration_routes.py` 追加：

```python
@pytest.mark.asyncio
async def test_messages_endpoint_before_seq(bb_root, monkeypatch):
    """/messages 端点支持 before_seq 游标分页。"""
    monkeypatch.setenv("TEAGE_BB_ROOT", str(bb_root))
    from teage_liu.multiagent.collaboration_routes import create_collab_router
    from teage_liu.multiagent.blackboard import append_collab_message

    for i in range(1, 5):
        await append_collab_message(bb_root, {
            "type": "status", "from": "agent-x", "content": f"m{i}",
            "timestamp": f"2026-07-27T1{i}:00:00+00:00",
        })

    router = create_collab_router()
    msg_route = next(r for r in router.routes if getattr(r, "path", "") == "/api/multiagent/collab/messages")
    response = await msg_route.endpoint(before_seq=3, limit=10)

    assert "messages" in response
    seqs = [m["seq"] for m in response["messages"]]
    assert seqs == [1, 2]  # 严格小于 3
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/multiagent/test_collaboration_routes.py::test_messages_endpoint_before_seq -v`
Expected: FAIL（参数不被接受或不生效）

- [ ] **Step 3: 修改 get_messages 端点签名**

修改 `teage_liu/multiagent/collaboration_routes.py:110-115`：

```python
    @router.get("/messages")
    async def get_messages(
        collab_id: Optional[str] = None,
        before_seq: Optional[int] = None,
        limit: Optional[int] = None,
    ):
        """获取协作消息列表。

        Args:
            collab_id: 协作 ID；None 表示全局
            before_seq: 游标分页，仅返回 seq 严格小于此值的消息；None 表示不限制
            limit: 返回数量上限，取最新 N 条；None 或 0 表示不限制
        """
        bb_root = _get_bb_root()
        messages = await read_collab_messages(
            bb_root, collab_id=collab_id,
            before_seq=before_seq, limit=limit,
        )
        return {"messages": messages, "total": len(messages)}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/multiagent/test_collaboration_routes.py -v`
Expected: PASS（原有 + 新增测试全通过）

- [ ] **Step 5: 提交**

```bash
git add teage_liu/multiagent/collaboration_routes.py tests/multiagent/test_collaboration_routes.py
git commit -m "feat(collab): add before_seq pagination to /messages endpoint"
```

---

## 阶段 2：左栏重设（Task 6-9）

### Task 6: 左栏 HTML 双区结构改造

**Files:**
- Modify: `teage-liu/web/workbench.html:78-100`（左栏 aside 内容）
- Modify: `teage-liu/web/workbench.html:10-14`（更新 CSS 缓存版本号）

**Interfaces:**
- Consumes: 无（纯结构改造）
- Produces: 左栏新结构 `.wb-sidebar` 含 `.wb-agents-panel`（保留） + `.wb-activity-panel`（替换原 .wb-collabs-panel）

- [ ] **Step 1: 修改 workbench.html 左栏结构**

将 `teage-liu/web/workbench.html:78-100` 替换为：

```html
  <!-- ===== 左栏：Agent 列表 + 活动档案时间线 ===== -->
  <aside class="wb-sidebar">
    <section class="wb-panel wb-agents-panel">
      <div class="wb-panel-header">
        <span class="wb-panel-title">接入 Agents</span>
        <span class="cw-agents-count" id="cwAgentsCount">0</span>
        <button class="cw-icon-btn" id="cwRefreshAgentsBtn" title="刷新列表">↻</button>
      </div>
      <div class="cw-agents-list" id="cwAgentsList">
        <div class="cw-empty">加载中…</div>
      </div>
    </section>

    <section class="wb-panel wb-activity-panel">
      <div class="wb-panel-header">
        <span class="wb-panel-title">活动档案</span>
        <span class="cw-activity-count" id="cwActivityCount">0</span>
        <button class="cw-icon-btn" id="cwRefreshActivityBtn" title="刷新活动档案">↻</button>
      </div>
      <div class="cw-activity-timeline" id="cwActivityTimeline">
        <div class="cw-empty">加载中…</div>
      </div>
    </section>
  </aside>
```

- [ ] **Step 2: 更新 CSS/JS 缓存版本号**

修改 `teage-liu/web/workbench.html:13-14` 和 `:264-265`：

```html
  <link rel="stylesheet" href="/static/css/collab-workbench.css?v=20260727a">
  <link rel="stylesheet" href="/static/css/workbench.css?v=20260727a">
```

```html
<script src="/static/js/collab-workbench.js?v=20260727a"></script>
<script src="/static/js/workbench.js?v=20260727a"></script>
```

- [ ] **Step 3: 手动验证**

启动服务（参考项目 README），浏览器打开 `/workbench`。
Expected: 左栏显示两个面板「接入 Agents」和「活动档案」，均显示「加载中…」（数据未接入时为「暂无」）。无 JS 错误（控制台无红字）。

- [ ] **Step 4: 提交**

```bash
git add teage-liu/web/workbench.html
git commit -m "refactor(workbench): replace collabs panel with activity timeline panel"
```

---

### Task 7: Agents 卡片样式升级

**Files:**
- Modify: `teage-liu/web/static/css/collab-workbench.css:264-328`（.cw-agent-card 相关）
- Modify: `teage-liu/web/static/js/collab-workbench.js:125-153`（renderAgents 函数）
- Modify: `teage-liu/web/static/js/workbench.js`（updateStats 函数，新增 agent count）

**Interfaces:**
- Consumes: 无
- Produces: Agent 卡片含状态点光晕 + 双行（name + id）+ 能力 tag 最多 2 + "+N"

- [ ] **Step 1: 升级 renderAgents 函数**

修改 `teage-liu/web/static/js/collab-workbench.js:125-153`：

```javascript
  function renderAgents(agents) {
    var container = document.getElementById("cwAgentsList");
    var countEl = document.getElementById("cwAgentsCount");
    if (!container) return;
    if (countEl) countEl.textContent = String(agents.length);

    if (!agents || agents.length === 0) {
      container.innerHTML = '<div class="cw-empty">等待 agent 接入</div>';
      return;
    }

    container.innerHTML = agents
      .map(function (agent) {
        var id = escapeHtml(agent.agent_id || "unknown");
        var name = escapeHtml(agent.agent_name || agent.agent_id || "");
        var status = escapeHtml(agent.status || "active");
        var caps = agent.capabilities || [];
        var capText = caps.length > 0 ? escapeHtml(caps.slice(0, 2).join(", ")) : "";
        var extra = caps.length > 2 ? "+" + (caps.length - 2) : "";
        var lastHb = agent.last_heartbeat ? escapeHtml(agent.last_heartbeat) : "";
        return (
          '<div class="cw-agent-card status-' + status + '" data-agent-id="' + id + '" title="' + name + ' · ' + status + (lastHb ? ' · ' + lastHb : '') + '">' +
            '<span class="cw-agent-dot status-' + status + '"></span>' +
            '<div class="cw-agent-info">' +
              '<div class="cw-agent-name">' + (name || id) + '</div>' +
              '<div class="cw-agent-id-line">' + id + (lastHb ? ' · ' + lastHb : '') + '</div>' +
            '</div>' +
            (capText ? '<span class="cw-agent-cap" title="' + escapeHtml(caps.join(", ")) + '">' + capText + (extra ? ' ' + extra : '') + '</span>' : '') +
          '</div>'
        );
      })
      .join('');
  }
```

- [ ] **Step 2: 升级 .cw-agent-card CSS**

修改 `teage-liu/web/static/css/collab-workbench.css:264-328`：

```css
/* ========== Agent 列表 ========== */
.cw-agents-list {
  display: flex;
  flex-direction: column;
  gap: 4px;
  overflow-y: auto;
  flex: 1;
  min-height: 0;
}
.cw-agents-count {
  font-size: 10px;
  color: var(--text-muted);
  padding: 1px 7px;
  border-radius: var(--radius-full, 9999px);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  margin-left: auto;
}
.cw-agent-card {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 10px;
  border-radius: var(--radius-lg);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  cursor: pointer;
  transition: all var(--transition);
}
.cw-agent-card:hover {
  background: var(--bg-hover, var(--bg-tertiary));
  border-color: var(--accent);
  transform: translateY(-1px);
  box-shadow: 0 2px 8px var(--accent-glow);
}
.cw-agent-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--text-muted);
  flex-shrink: 0;
}
.cw-agent-dot.status-active,
.cw-agent-dot.status-online {
  background: var(--success, #3fb950);
  box-shadow: 0 0 6px rgba(63, 185, 80, 0.6);
}
.cw-agent-dot.status-inactive,
.cw-agent-dot.status-offline {
  background: var(--text-muted);
}
.cw-agent-info {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 2px;
}
.cw-agent-name {
  font-size: 12px;
  font-weight: 600;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.cw-agent-id-line {
  font-size: 10px;
  color: var(--text-muted);
  font-family: var(--font-mono);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.cw-agent-cap {
  font-size: 10px;
  color: var(--accent);
  padding: 2px 7px;
  border-radius: var(--radius-full, 9999px);
  background: var(--glass-bg-active);
  border: 1px solid var(--glass-border, var(--border));
  flex-shrink: 0;
  max-width: 80px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

- [ ] **Step 3: 手动验证**

刷新 `/workbench` 页面，若有 agent 接入：卡片应有 name（主）+ id（次，mono 字体）双行；hover 时 accent 边框 + 抬升 1px + 阴影。无 agent 时显示「等待 agent 接入」。

- [ ] **Step 4: 提交**

```bash
git add teage-liu/web/static/css/collab-workbench.css teage-liu/web/static/js/collab-workbench.js
git commit -m "style(workbench): upgrade agent card with name/id hierarchy and hover lift"
```

---

### Task 8: 活动档案时间线渲染 + 时间分组 + 跳转

**Files:**
- Modify: `teage-liu/web/static/css/collab-workbench.css`（新增活动时间线样式）
- Modify: `teage-liu/web/static/js/collab-workbench.js`（新增 loadActivityTimeline / renderActivityTimeline / switchCollabView 改造为时间线跳转）

**Interfaces:**
- Consumes: `GET /api/multiagent/collab/events?before_ts=&limit=`（Task 4 产出）
- Produces: 时间线渲染函数 `loadActivityTimeline()` / `renderActivityTimeline(events)` + 跳转函数 `jumpToMessage(seq)`

- [ ] **Step 1: 新增活动时间线 CSS**

在 `teage-liu/web/static/css/collab-workbench.css` 末尾追加：

```css
/* ========== 活动档案时间线 ========== */
.cw-activity-count {
  font-size: 10px;
  color: var(--text-muted);
  padding: 1px 7px;
  border-radius: var(--radius-full, 9999px);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  margin-left: auto;
}
.cw-activity-timeline {
  flex: 1;
  overflow-y: auto;
  padding: 8px 10px;
  display: flex;
  flex-direction: column;
  gap: 4px;
  min-height: 0;
}
.cw-activity-group {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-bottom: 6px;
}
.cw-activity-group-title {
  font-size: 10px;
  font-weight: 600;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  padding: 4px 0;
  border-bottom: 1px solid var(--border-subtle);
  margin-bottom: 4px;
}
.cw-activity-item {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  padding: 6px 8px;
  border-radius: var(--radius-md, 6px);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  cursor: pointer;
  transition: all var(--transition);
}
.cw-activity-item:hover {
  background: var(--bg-hover, var(--bg-tertiary));
  border-color: var(--accent);
}
.cw-activity-icon {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 10px;
  flex-shrink: 0;
  font-weight: 600;
}
.cw-activity-icon.type-agent_online {
  background: rgba(63, 185, 80, 0.15);
  color: var(--success, #3fb950);
  border: 1px solid rgba(63, 185, 80, 0.3);
}
.cw-activity-icon.type-agent_offline {
  background: rgba(138, 125, 111, 0.15);
  color: var(--text-muted);
  border: 1px solid var(--border);
}
.cw-activity-icon.type-user_broadcast {
  background: rgba(168, 85, 247, 0.15);
  color: var(--accent);
  border: 1px solid rgba(168, 85, 247, 0.3);
}
.cw-activity-icon.type-directive_injected {
  background: rgba(210, 153, 34, 0.15);
  color: var(--warning, #d29922);
  border: 1px solid rgba(210, 153, 34, 0.3);
}
.cw-activity-body {
  flex: 1;
  min-width: 0;
}
.cw-activity-summary {
  font-size: 12px;
  color: var(--text-primary);
  line-height: 1.4;
  word-break: break-word;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}
.cw-activity-meta {
  display: flex;
  gap: 8px;
  margin-top: 2px;
  font-size: 10px;
  color: var(--text-muted);
  font-family: var(--font-mono);
}
.cw-activity-agent {
  color: var(--accent);
  font-weight: 600;
}
.cw-activity-time {
  margin-left: auto;
}
.cw-activity-loading {
  text-align: center;
  padding: 12px;
  color: var(--text-muted);
  font-size: 11px;
}
.cw-activity-load-more {
  text-align: center;
  padding: 8px;
  margin-top: 4px;
}
.cw-activity-load-more button {
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-muted);
  padding: 4px 12px;
  border-radius: var(--radius-md, 6px);
  font-size: 11px;
  cursor: pointer;
  font-family: inherit;
}
.cw-activity-load-more button:hover {
  color: var(--accent);
  border-color: var(--accent);
}

/* 跳转高亮闪烁动画 */
.cw-msg.cw-msg-flash {
  animation: cw-flash 2s ease-out;
}
@keyframes cw-flash {
  0%, 100% { box-shadow: 0 0 0 0 var(--accent-glow); }
  25%, 75% { box-shadow: 0 0 0 4px var(--accent-glow); }
  50% { box-shadow: 0 0 0 8px var(--accent-glow); }
}
```

- [ ] **Step 2: 在 collab-workbench.js 中新增活动时间线逻辑**

在 `teage-liu/web/static/js/collab-workbench.js` 的 `// ========== Task 12: 协作列表 ==========` 段（约 L224 起）替换为：

```javascript
  // ========== 活动档案时间线 ==========

  /** 时间线已渲染事件（按 ts 倒序）+ 分页游标 */
  var _activityEvents = [];
  var _activityNextBeforeTs = null;
  var _activityLoading = false;

  /** 时间分组标签 */
  function _activityGroupLabel(ts) {
    if (!ts) return "更早";
    try {
      var d = new Date(ts);
      var now = new Date();
      var today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
      var yesterday = new Date(today.getTime() - 86400000);
      var weekAgo = new Date(today.getTime() - 7 * 86400000);
      if (d >= today) return "今天";
      if (d >= yesterday) return "昨天";
      if (d >= weekAgo) return "本周";
      return "更早";
    } catch (e) {
      return "更早";
    }
  }

  /** 相对时间格式化（"3 分钟前" / "2 小时前" / "1 天前"） */
  function _relativeTime(ts) {
    if (!ts) return "";
    try {
      var d = new Date(ts);
      var diff = (Date.now() - d.getTime()) / 1000;
      if (diff < 60) return "刚刚";
      if (diff < 3600) return Math.floor(diff / 60) + " 分钟前";
      if (diff < 86400) return Math.floor(diff / 3600) + " 小时前";
      if (diff < 7 * 86400) return Math.floor(diff / 86400) + " 天前";
      return d.toLocaleDateString();
    } catch (e) {
      return "";
    }
  }

  /** 事件类型图标映射 */
  var _activityIcons = {
    agent_online: "↑",
    agent_offline: "↓",
    user_broadcast: "📡",
    directive_injected: "⚡",
  };

  /**
   * 加载活动档案时间线
   * @param {boolean} append - true=追加更早数据；false=重新加载
   */
  function loadActivityTimeline(append) {
    var container = document.getElementById("cwActivityTimeline");
    if (!container || _activityLoading) return Promise.resolve([]);
    _activityLoading = true;

    if (!append) {
      _activityEvents = [];
      _activityNextBeforeTs = null;
      container.innerHTML = '<div class="cw-activity-loading">加载中…</div>';
    }

    var url = "/api/multiagent/collab/events?limit=50";
    if (_activityNextBeforeTs) {
      url += "&before_ts=" + encodeURIComponent(_activityNextBeforeTs);
    }

    return fetch(url)
      .then(function (resp) {
        if (resp.status === 404) return { events: [], next_before_ts: null };
        if (!resp.ok) throw new Error("HTTP " + resp.status);
        return resp.json();
      })
      .then(function (data) {
        var events = (data && data.events) || [];
        _activityNextBeforeTs = (data && data.next_before_ts) || null;
        if (append) {
          _activityEvents = _activityEvents.concat(events);
        } else {
          _activityEvents = events;
        }
        renderActivityTimeline();
        return events;
      })
      .catch(function (err) {
        console.warn("loadActivityTimeline 失败:", err);
        container.innerHTML = '<div class="cw-empty">加载失败</div>';
        return [];
      })
      .finally(function () {
        _activityLoading = false;
      });
  }

  /** 渲染活动档案时间线（按时间分组） */
  function renderActivityTimeline() {
    var container = document.getElementById("cwActivityTimeline");
    var countEl = document.getElementById("cwActivityCount");
    if (!container) return;
    if (countEl) countEl.textContent = String(_activityEvents.length);

    if (!_activityEvents || _activityEvents.length === 0) {
      container.innerHTML = '<div class="cw-empty">暂无活动</div>';
      return;
    }

    // 按时间分组
    var groups = {};
    var groupOrder = ["今天", "昨天", "本周", "更早"];
    _activityEvents.forEach(function (e) {
      var label = _activityGroupLabel(e.ts);
      if (!groups[label]) groups[label] = [];
      groups[label].push(e);
    });

    var html = "";
    groupOrder.forEach(function (label) {
      if (!groups[label]) return;
      html += '<div class="cw-activity-group">';
      html += '<div class="cw-activity-group-title">' + label + '</div>';
      groups[label].forEach(function (e) {
        var icon = _activityIcons[e.event_type] || "•";
        var summary = escapeHtml(e.summary || "");
        var agent = escapeHtml(e.agent_id || "");
        var relTime = escapeHtml(_relativeTime(e.ts));
        var seq = e.seq || 0;
        html +=
          '<div class="cw-activity-item" data-seq="' + seq + '" title="点击跳转到对应消息">' +
            '<span class="cw-activity-icon type-' + escapeHtml(e.event_type) + '">' + icon + '</span>' +
            '<div class="cw-activity-body">' +
              '<div class="cw-activity-summary">' + summary + '</div>' +
              '<div class="cw-activity-meta">' +
                '<span class="cw-activity-agent">' + agent + '</span>' +
                '<span class="cw-activity-time">' + relTime + '</span>' +
              '</div>' +
            '</div>' +
          '</div>';
      });
      html += '</div>';
    });

    // 加载更多按钮
    if (_activityNextBeforeTs) {
      html += '<div class="cw-activity-load-more">';
      html += '<button id="cwActivityLoadMoreBtn">加载更早</button>';
      html += '</div>';
    }

    container.innerHTML = html;

    // 绑定点击事件
    container.querySelectorAll(".cw-activity-item").forEach(function (item) {
      item.addEventListener("click", function () {
        var seq = parseInt(item.getAttribute("data-seq"), 10);
        if (seq > 0) jumpToMessage(seq);
      });
    });
    var loadMoreBtn = document.getElementById("cwActivityLoadMoreBtn");
    if (loadMoreBtn) {
      loadMoreBtn.addEventListener("click", function () {
        loadActivityTimeline(true);
      });
    }
  }

  /**
   * 跳转到中栏消息流的指定 seq 位置
   * 若消息不在当前 DOM（古早记录未加载），先加载该 seq 之前的消息
   * @param {number} seq - 目标消息 seq
   */
  function jumpToMessage(seq) {
    var stream = document.getElementById("cwStream");
    if (!stream) return;

    // 先尝试在现有 DOM 中查找
    var target = stream.querySelector('.cw-msg[data-seq="' + seq + '"]');
    if (target) {
      _scrollAndFlash(target);
      return;
    }

    // 不在 DOM 中：加载 seq 之前的消息（包括目标 seq）
    stream.innerHTML = '<div class="cw-empty">加载消息 seq=' + seq + ' …</div>';
    _renderedSeqs.clear();
    var url = "/api/multiagent/collab/messages?before_seq=" + (seq + 1) + "&limit=50";
    fetch(url)
      .then(function (resp) {
        if (!resp.ok) throw new Error("HTTP " + resp.status);
        return resp.json();
      })
      .then(function (data) {
        var messages = (data && data.messages) || [];
        // 移除占位
        var empty = stream.querySelector(".cw-empty");
        if (empty) empty.remove();
        messages.forEach(function (msg) {
          renderCollabMessage(msg);
        });
        // 加载完成后定位
        setTimeout(function () {
          var target2 = stream.querySelector('.cw-msg[data-seq="' + seq + '"]');
          if (target2) _scrollAndFlash(target2);
        }, 50);
      })
      .catch(function (err) {
        console.warn("jumpToMessage 加载失败:", err);
        showToast("跳转失败: " + err.message, "error");
      });
  }

  /** 滚动到目标元素 + 闪烁高亮 2 秒 */
  function _scrollAndFlash(el) {
    el.scrollIntoView({ behavior: "smooth", block: "center" });
    el.classList.remove("cw-msg-flash");
    // 触发重绘
    void el.offsetWidth;
    el.classList.add("cw-msg-flash");
    setTimeout(function () {
      el.classList.remove("cw-msg-flash");
    }, 2000);
  }
```

- [ ] **Step 3: 修改 renderCollabMessage 添加 data-seq 属性**

修改 `teage-liu/web/static/js/collab-workbench.js` 的 `renderCollabMessage` 函数（约 L195），在 `item.dataset.type = type;` 后追加一行：

```javascript
    item.dataset.seq = (msg.seq != null) ? String(msg.seq) : "";
```

- [ ] **Step 4: 替换 init 中的协作列表加载逻辑**

修改 `teage-liu/web/static/js/collab-workbench.js` 的 `init` 函数（约 L654-693），将所有 `loadCollabList()` 调用替换为 `loadActivityTimeline(false)`：

```javascript
  function init() {
    bindEvents();
    fetch("/api/multiagent/status")
      .then(function (resp) {
        if (resp.status === 404) {
          setDirectorState("disabled");
          return null;
        }
        if (!resp.ok) throw new Error("HTTP " + resp.status);
        return resp.json();
      })
      .then(function (status) {
        if (status === null) {
          var listEl = document.getElementById("cwAgentsList");
          if (listEl) listEl.innerHTML = '<div class="cw-empty">协作未启用</div>';
          var tlEl = document.getElementById("cwActivityTimeline");
          if (tlEl) tlEl.innerHTML = '<div class="cw-empty">协作未启用</div>';
          return;
        }
        if (status.enabled) {
          localAgentId = status.local_agent_id || "";
          setDirectorState("observing");
          var pane = document.getElementById("collabPane");
          if (!pane || !pane.hidden) {
            loadAgents();
            loadActivityTimeline(false);
            loadRecentMessages();
            connectCollabSSE();
          }
        } else {
          setDirectorState("disabled");
        }
      })
      .catch(function () {
        setDirectorState("unknown");
      });
  }
```

- [ ] **Step 5: 修改 bindEvents 中的刷新按钮绑定**

修改 `teage-liu/web/static/js/collab-workbench.js` 的 `bindEvents` 函数（约 L549-590），将 `cwRefreshCollabsBtn` 改为 `cwRefreshActivityBtn`：

```javascript
    // 刷新活动档案
    var refreshActivityBtn = document.getElementById("cwRefreshActivityBtn");
    if (refreshActivityBtn) {
      refreshActivityBtn.addEventListener("click", function () {
        loadActivityTimeline(false);
      });
    }
```

同时移除 `cwRefreshCollabsBtn` 相关代码（已不存在）。

- [ ] **Step 6: 修改 workbench.js 中的 setupRefreshAll**

修改 `teage-liu/web/static/js/workbench.js:147-152`（setupRefreshAll 函数），将 `loadCollabList` 改为 `loadActivityTimeline`：

```javascript
    btn.addEventListener("click", function () {
      if (window.CollabWorkbench) {
        if (window.CollabWorkbench.loadAgents) window.CollabWorkbench.loadAgents();
        if (window.CollabWorkbench.loadActivityTimeline) window.CollabWorkbench.loadActivityTimeline(false);
        if (window.CollabWorkbench.loadRecentMessages) window.CollabWorkbench.loadRecentMessages();
        if (window.CollabWorkbench.loadLocalAgentId) window.CollabWorkbench.loadLocalAgentId();
      }
      loadDirectorDetails();
      setTimeout(updateStats, 500);
    });
```

- [ ] **Step 7: 修改 updateStats 中的协作计数**

修改 `teage-liu/web/static/js/workbench.js:69-84`（updateStats 函数），将协作计数改为活动计数：

```javascript
  function updateStats() {
    var agentCount = 0;
    var agentList = $("cwAgentsList");
    if (agentList) {
      agentCount = agentList.querySelectorAll(".cw-agent-card").length;
    }
    var activityCount = 0;
    var timeline = $("cwActivityTimeline");
    if (timeline) {
      activityCount = timeline.querySelectorAll(".cw-activity-item").length;
    }
    $("wbStatTotal").textContent = allMessages.length;
    $("wbStatAgents").textContent = agentCount;
    $("wbStatCollabs").textContent = activityCount; // 复用字段，标签后续改为「活动数」
  }
```

- [ ] **Step 8: 导出新函数到 window.CollabWorkbench**

修改 `teage-liu/web/static/js/collab-workbench.js:696-709`（导出对象）：

```javascript
  window.CollabWorkbench = {
    loadAgents: loadAgents,
    renderCollabMessage: renderCollabMessage,
    broadcastMessage: broadcastMessage,
    injectDirective: injectDirective,
    connectCollabSSE: connectCollabSSE,
    clearStream: clearStream,
    setDirectorState: setDirectorState,
    loadRecentMessages: loadRecentMessages,
    loadActivityTimeline: loadActivityTimeline,
    jumpToMessage: jumpToMessage,
    loadLocalAgentId: loadLocalAgentId,
    getLocalAgentId: function () { return localAgentId; },
  };
```

移除 `loadCollabList` / `switchCollabView` 导出。

- [ ] **Step 9: 手动验证**

刷新 `/workbench` 页面，触发以下操作（如发广播、注入引导、agent 上线）：
- 活动档案区域出现新事件，按时间分组（今天/昨天/本周/更早）
- 每个事件显示彩色图标 + 摘要 + agent 标识 + 相对时间
- 点击事件 → 中栏消息流定位到对应 seq + 闪烁高亮 2 秒
- 若消息不在当前 50 条内，先加载更早消息再定位
- 「加载更早」按钮可点击加载下一页

- [ ] **Step 10: 提交**

```bash
git add teage-liu/web/static/css/collab-workbench.css teage-liu/web/static/js/collab-workbench.js teage-liu/web/static/js/workbench.js
git commit -m "feat(workbench): replace collabs list with activity timeline + jump to message"
```

---

## 阶段 3：Agent Card 重设计（Task 9-12）

### Task 9: Agent Card 模态框 HTML 结构改造

**Files:**
- Modify: `teage-liu/web/workbench.html:242-259`（Agent Card 模态框）

**Interfaces:**
- Consumes: 无
- Produces: 模态框结构含 `.wb-card-banner` / `.wb-card-fields-main` / `.wb-card-meta` / `.wb-card-actions`

- [ ] **Step 1: 重写模态框 HTML 结构**

修改 `teage-liu/web/workbench.html:242-259`：

```html
<!-- ========== 模态框：Agent Card 查看（只读，分层卡片） ========== -->
<div class="modal" id="wbAgentCardModal" hidden>
  <div class="modal-overlay" data-close></div>
  <div class="modal-content modal-content-wide wb-card-modal">
    <div class="modal-header">
      <h3>Agent Card <span class="wb-card-title-id" id="wbCardTitleId">—</span></h3>
      <button class="modal-close" data-close>×</button>
    </div>
    <div class="modal-body">
      <div class="wb-card-view" id="wbCardView">
        <div class="wb-card-loading">加载中…</div>
      </div>
    </div>
    <div class="modal-footer wb-card-actions">
      <button class="btn btn-ghost" id="wbCardRefreshBtn" title="重新拉取最新数据">
        <span>↻</span><span>刷新</span>
      </button>
      <button class="btn btn-primary" id="wbCardCopyJsonBtn" title="复制完整 JSON 到剪贴板">
        <span>⧉</span><span>复制 JSON</span>
      </button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: 手动验证**

刷新 `/workbench`，点击右栏「Agent Card」按钮：
Expected: 模态框打开，header 显示「Agent Card —」，body 显示「加载中…」，footer 显示「刷新」+「复制 JSON」两个按钮。

- [ ] **Step 3: 提交**

```bash
git add teage-liu/web/workbench.html
git commit -m "refactor(workbench): restructure agent card modal with banner/actions"
```

---

### Task 10: Agent Card 分层卡片样式

**Files:**
- Modify: `teage-liu/web/static/css/workbench.css:553-623`（替换 .wb-card-* 样式块）

**Interfaces:**
- Consumes: Task 9 产出的新 HTML 结构
- Produces: 分层卡片样式：banner / fields-main / meta / actions

- [ ] **Step 1: 重写 Agent Card 样式**

修改 `teage-liu/web/static/css/workbench.css:553-623`，替换为：

```css
/* ========== Agent Card 分层卡片 ========== */
.workbench-page .modal-content-wide {
  max-width: 640px;
}
.workbench-page .wb-card-modal .modal-body {
  padding: 0;
  max-height: 65vh;
  overflow-y: auto;
}
.workbench-page .wb-card-modal .modal-footer {
  display: flex;
  justify-content: space-between;
  gap: 8px;
  padding: 12px 20px;
  border-top: 1px solid var(--border);
  position: sticky;
  bottom: 0;
  background: var(--bg-elevated, var(--bg-secondary));
}
.workbench-page .wb-card-title-id {
  font-size: 12px;
  color: var(--accent);
  font-family: var(--font-mono);
  margin-left: 6px;
}
.workbench-page .wb-card-view {
  display: flex;
  flex-direction: column;
}
.workbench-page .wb-card-loading,
.workbench-page .wb-card-empty {
  padding: 32px;
  text-align: center;
  color: var(--text-muted);
  font-size: 13px;
}

/* --- 头部摘要 banner --- */
.workbench-page .wb-card-banner {
  padding: 18px 20px 14px;
  background: linear-gradient(135deg,
    var(--accent-glow) 0%,
    transparent 70%);
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  gap: 12px;
}
.workbench-page .wb-card-banner-status {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  flex-shrink: 0;
  box-shadow: 0 0 8px currentColor;
}
.workbench-page .wb-card-banner-status.status-active,
.workbench-page .wb-card-banner-status.status-online {
  background: var(--success, #3fb950);
  color: var(--success, #3fb950);
}
.workbench-page .wb-card-banner-status.status-inactive,
.workbench-page .wb-card-banner-status.status-offline {
  background: var(--text-muted);
  color: var(--text-muted);
}
.workbench-page .wb-card-banner-id {
  flex: 1;
  min-width: 0;
}
.workbench-page .wb-card-banner-id-text {
  font-size: 16px;
  font-weight: 600;
  font-family: var(--font-mono);
  color: var(--text-primary);
  word-break: break-all;
  line-height: 1.3;
}
.workbench-page .wb-card-banner-meta {
  font-size: 11px;
  color: var(--text-muted);
  margin-top: 4px;
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}
.workbench-page .wb-card-banner-badges {
  display: flex;
  flex-direction: column;
  gap: 4px;
  flex-shrink: 0;
}
.workbench-page .wb-card-badge {
  font-size: 10px;
  font-weight: 600;
  padding: 3px 8px;
  border-radius: var(--radius-full, 9999px);
  background: var(--glass-bg-active);
  border: 1px solid var(--accent);
  color: var(--accent);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  white-space: nowrap;
}
.workbench-page .wb-card-badge.wb-badge-status {
  background: var(--bg-tertiary, var(--bg-secondary));
  border-color: var(--border);
  color: var(--text-secondary);
}

/* --- 主字段区（capabilities/specialties/dangerous_tools） --- */
.workbench-page .wb-card-fields-main {
  padding: 14px 20px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  border-bottom: 1px solid var(--border);
}
.workbench-page .wb-card-field-block {
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.workbench-page .wb-card-field-block-label {
  font-size: 11px;
  font-weight: 600;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.workbench-page .wb-card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.workbench-page .wb-card-tag {
  display: inline-block;
  padding: 3px 9px;
  border-radius: var(--radius-full, 9999px);
  background: var(--glass-bg-active);
  border: 1px solid var(--glass-border, var(--border));
  color: var(--accent);
  font-size: 11px;
  font-family: var(--font-sans);
}
.workbench-page .wb-card-tag.wb-tag-specialty {
  color: var(--info, #58a6ff);
  border-color: rgba(88, 166, 255, 0.3);
  background: rgba(88, 166, 255, 0.08);
}
.workbench-page .wb-card-tag.wb-tag-danger {
  color: var(--danger, #ef4444);
  border-color: rgba(239, 68, 68, 0.3);
  background: rgba(239, 68, 68, 0.08);
}

/* --- 元数据描述列表 --- */
.workbench-page .wb-card-meta {
  padding: 14px 20px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px 16px;
  border-bottom: 1px solid var(--border);
}
.workbench-page .wb-card-meta-item {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}
.workbench-page .wb-card-meta-label {
  font-size: 10px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.04em;
}
.workbench-page .wb-card-meta-value {
  font-size: 12px;
  color: var(--text-primary);
  font-family: var(--font-mono);
  word-break: break-all;
  line-height: 1.4;
}

/* --- 进度条（trust_score / max_concurrent_tasks） --- */
.workbench-page .wb-card-progress {
  display: flex;
  flex-direction: column;
  gap: 4px;
  min-width: 0;
}
.workbench-page .wb-card-progress-bar {
  height: 6px;
  background: var(--bg-tertiary, var(--bg-secondary));
  border-radius: var(--radius-full, 9999px);
  overflow: hidden;
  border: 1px solid var(--border);
}
.workbench-page .wb-card-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent), var(--accent-hover, var(--accent)));
  border-radius: var(--radius-full, 9999px);
  transition: width var(--transition-slow);
}
.workbench-page .wb-card-progress-text {
  font-size: 11px;
  color: var(--text-secondary);
  font-family: var(--font-mono);
}

/* 浅色主题适配 */
[data-theme="light"] .workbench-page .wb-card-banner {
  background: linear-gradient(135deg,
    rgba(168, 85, 247, 0.06) 0%,
    transparent 70%);
}
```

- [ ] **Step 2: 手动验证**

刷新 `/workbench`，点击 Agent Card 按钮：
Expected: 模态框打开时显示「加载中…」文案，居中显示。模态框宽度 640px。无 CSS 报错（控制台无 404）。

- [ ] **Step 3: 提交**

```bash
git add teage-liu/web/static/css/workbench.css
git commit -m "style(workbench): add layered card styles for agent card modal"
```

---

### Task 11: Agent Card 分层渲染 + 进度条 + 复制 JSON

**Files:**
- Modify: `teage-liu/web/static/js/workbench.js:221-345`（setupAgentCardView + showAgentCard + renderAgentCard）

**Interfaces:**
- Consumes: Task 9 HTML + Task 10 CSS
- Produces: `renderAgentCard(card, agentId)` 输出分层 HTML；新增进度条渲染 + 复制 JSON + 刷新按钮逻辑

- [ ] **Step 1: 重写 renderAgentCard 函数**

修改 `teage-liu/web/static/js/workbench.js:281-345`（renderAgentCard 函数），替换为：

```javascript
  /** 渲染 agent card 到分层视图 */
  function renderAgentCard(card, agentId) {
    var view = $("wbCardView");
    if (!view) return;

    if (!card || Object.keys(card).length === 0) {
      view.innerHTML = '<div class="wb-card-empty">无 Agent Card 数据</div>';
      return;
    }

    var html = "";

    // --- 头部摘要 banner ---
    var status = card.status || "active";
    var role = card.role || "";
    var lastHb = card.last_heartbeat || "";
    var registeredAt = card.registered_at || "";
    var hbRel = lastHb ? _relativeTimeShort(lastHb) : "";
    var regRel = registeredAt ? _relativeTimeShort(registeredAt) : "";

    html += '<div class="wb-card-banner">';
    html += '<span class="wb-card-banner-status status-' + escapeHtml(status) + '"></span>';
    html += '<div class="wb-card-banner-id">';
    html += '<div class="wb-card-banner-id-text">' + escapeHtml(agentId || card.agent_id || "—") + '</div>';
    html += '<div class="wb-card-banner-meta">';
    if (hbRel) html += '<span>心跳 ' + escapeHtml(hbRel) + '</span>';
    if (regRel) html += '<span>注册 ' + escapeHtml(regRel) + '</span>';
    html += '</div>';
    html += '</div>';
    html += '<div class="wb-card-banner-badges">';
    if (role) html += '<span class="wb-card-badge">' + escapeHtml(role) + '</span>';
    html += '<span class="wb-card-badge wb-badge-status">' + escapeHtml(status) + '</span>';
    html += '</div>';
    html += '</div>';

    // --- 主字段区（capabilities / specialties / dangerous_tools） ---
    var hasMain = card.capabilities || card.specialties || card.dangerous_tools;
    if (hasMain) {
      html += '<div class="wb-card-fields-main">';
      html += _renderTagBlock("能力列表", card.capabilities, "");
      html += _renderTagBlock("专长领域", card.specialties, "wb-tag-specialty");
      html += _renderTagBlock("危险工具", card.dangerous_tools, "wb-tag-danger");
      html += '</div>';
    }

    // --- 元数据描述列表 ---
    var metaFields = [
      { key: "endpoint", label: "服务端点" },
      { key: "owner", label: "所有者" },
      { key: "auth_method", label: "认证方式" },
      { key: "agent_version", label: "Agent 版本" },
      { key: "protocol_version", label: "协议版本" },
      { key: "pid", label: "进程 PID" },
      { key: "host", label: "主机" },
    ];
    var metaHtml = "";
    metaFields.forEach(function (f) {
      var val = card[f.key];
      if (val === undefined || val === null || val === "") return;
      metaHtml += '<div class="wb-card-meta-item">';
      metaHtml += '<div class="wb-card-meta-label">' + escapeHtml(f.label) + '</div>';
      metaHtml += '<div class="wb-card-meta-value">' + escapeHtml(String(val)) + '</div>';
      metaHtml += '</div>';
    });

    // trust_score 进度条
    if (card.trust_score !== undefined && card.trust_score !== null) {
      var trust = parseFloat(card.trust_score);
      var trustPct = isNaN(trust) ? 0 : Math.min(100, Math.max(0, trust));
      metaHtml += '<div class="wb-card-progress">';
      metaHtml += '<div class="wb-card-meta-label">信任分数</div>';
      metaHtml += '<div class="wb-card-progress-bar"><div class="wb-card-progress-fill" style="width:' + trustPct + '%"></div></div>';
      metaHtml += '<div class="wb-card-progress-text">' + escapeHtml(String(card.trust_score)) + '</div>';
      metaHtml += '</div>';
    }

    // max_concurrent_tasks 进度条
    if (card.max_concurrent_tasks !== undefined && card.max_concurrent_tasks !== null) {
      var maxC = parseInt(card.max_concurrent_tasks, 10);
      var maxPct = isNaN(maxC) ? 0 : Math.min(100, (maxC / 10) * 100); // 假设上限 10
      metaHtml += '<div class="wb-card-progress">';
      metaHtml += '<div class="wb-card-meta-label">最大并发任务</div>';
      metaHtml += '<div class="wb-card-progress-bar"><div class="wb-card-progress-fill" style="width:' + maxPct + '%"></div></div>';
      metaHtml += '<div class="wb-card-progress-text">' + escapeHtml(String(card.max_concurrent_tasks)) + '</div>';
      metaHtml += '</div>';
    }

    if (metaHtml) {
      html += '<div class="wb-card-meta">' + metaHtml + '</div>';
    }

    if (!html) {
      view.innerHTML = '<div class="wb-card-empty">无可用字段</div>';
    } else {
      view.innerHTML = html;
    }
  }

  /** 渲染 tag 块（capabilities / specialties / dangerous_tools） */
  function _renderTagBlock(label, values, extraClass) {
    if (!values || (Array.isArray(values) && values.length === 0)) return "";
    var arr = Array.isArray(values) ? values : [values];
    var html = '<div class="wb-card-field-block">';
    html += '<div class="wb-card-field-block-label">' + escapeHtml(label) + '</div>';
    html += '<div class="wb-card-tags">';
    arr.forEach(function (item) {
      var cls = "wb-card-tag" + (extraClass ? " " + extraClass : "");
      html += '<span class="' + cls + '">' + escapeHtml(String(item)) + '</span>';
    });
    html += '</div></div>';
    return html;
  }

  /** 相对时间简短格式（"3s" / "2m" / "1h" / "2d"） */
  function _relativeTimeShort(ts) {
    if (!ts) return "";
    try {
      var d = new Date(ts);
      var diff = (Date.now() - d.getTime()) / 1000;
      if (diff < 60) return Math.floor(diff) + "s 前";
      if (diff < 3600) return Math.floor(diff / 60) + "m 前";
      if (diff < 86400) return Math.floor(diff / 3600) + "h 前";
      if (diff < 7 * 86400) return Math.floor(diff / 86400) + "d 前";
      return d.toLocaleDateString();
    } catch (e) {
      return "";
    }
  }
```

- [ ] **Step 2: 新增复制 JSON + 刷新按钮逻辑**

在 `teage-liu/web/static/js/workbench.js` 的 `setupAgentCardView` 函数（约 L222-241）末尾追加：

```javascript
    // 复制 JSON 按钮
    var copyBtn = $("wbCardCopyJsonBtn");
    if (copyBtn) {
      copyBtn.addEventListener("click", function () {
        var view = $("wbCardView");
        if (!view) return;
        var currentCard = view.dataset.cardJson || "";
        if (!currentCard) {
          showToast("无数据可复制", "error");
          return;
        }
        if (navigator.clipboard && navigator.clipboard.writeText) {
          navigator.clipboard.writeText(currentCard).then(function () {
            showToast("JSON 已复制到剪贴板", "success");
          }).catch(function () {
            showToast("复制失败", "error");
          });
        } else {
          // 降级方案：使用 textarea
          var ta = document.createElement("textarea");
          ta.value = currentCard;
          document.body.appendChild(ta);
          ta.select();
          try {
            document.execCommand("copy");
            showToast("JSON 已复制到剪贴板", "success");
          } catch (e) {
            showToast("复制失败", "error");
          }
          document.body.removeChild(ta);
        }
      });
    }

    // 刷新按钮
    var refreshBtn = $("wbCardRefreshBtn");
    if (refreshBtn) {
      refreshBtn.addEventListener("click", function () {
        var titleId = $("wbCardTitleId");
        var agentId = titleId ? titleId.textContent : "";
        if (agentId && agentId !== "—") {
          showAgentCard(agentId);
        }
      });
    }
```

- [ ] **Step 3: 在 showAgentCard 中保存 card JSON 用于复制**

修改 `teage-liu/web/static/js/workbench.js:267-279`（showAgentCard 的 fetch.then 部分）：

```javascript
    fetch(url)
      .then(function (resp) {
        if (!resp.ok) throw new Error("HTTP " + resp.status);
        return resp.json();
      })
      .then(function (data) {
        var card = data.card || {};
        // 保存原始 JSON 字符串供复制使用
        var view = $("wbCardView");
        if (view) view.dataset.cardJson = JSON.stringify(card, null, 2);
        renderAgentCard(card, actualId);
      })
      .catch(function (err) {
        view.innerHTML = '<div class="wb-card-empty">加载失败: ' + escapeHtml(err.message) + "</div>";
      });
```

- [ ] **Step 4: 手动验证**

刷新 `/workbench`：
- 点击 Agent Card 按钮：模态框展示 banner（agent_id 大字 + role/status 徽章 + 心跳相对时间）
- capabilities/specialties/dangerous_tools 用彩色 tag 展示（accent / info 蓝 / danger 红）
- 元数据 2 列 grid + 进度条显示 trust_score 和 max_concurrent_tasks
- 点击「复制 JSON」：toast 提示「JSON 已复制到剪贴板」
- 点击「刷新」：模态框重新加载最新数据

- [ ] **Step 5: 提交**

```bash
git add teage-liu/web/static/js/workbench.js
git commit -m "feat(workbench): render agent card as layered view with progress bars and copy json"
```

---

## 阶段 4：右栏重组为 Director 控制台（Task 12-15）

### Task 12: 右栏 HTML 控制台结构改造

**Files:**
- Modify: `teage-liu/web/workbench.html:126-188`（右栏 aside 内容）

**Interfaces:**
- Consumes: 无
- Produces: 右栏三区结构 `.wb-director-status-card` / `.wb-quick-actions` / `.wb-realtime-metrics`

- [ ] **Step 1: 重写右栏 HTML 结构**

修改 `teage-liu/web/workbench.html:126-188`：

```html
  <!-- ===== 右栏：Director 控制台 ===== -->
  <aside class="wb-action-panel">
    <!-- 区1：Director 状态卡 -->
    <section class="wb-panel wb-director-status-card">
      <div class="wb-panel-header">
        <span class="wb-panel-title">Director 控制台</span>
      </div>
      <div class="wb-director-card-body">
        <div class="wb-director-state-large" id="wbDirectorStateLarge">
          <span class="wb-director-state-dot"></span>
          <span class="wb-director-state-text">未启用</span>
        </div>
        <div class="wb-director-info-compact">
          <div class="wb-info-row">
            <span class="wb-info-label">角色</span>
            <span class="wb-info-value" id="wbDirectorRole">—</span>
          </div>
          <div class="wb-info-row">
            <span class="wb-info-label">Epoch</span>
            <span class="wb-info-value" id="wbDirectorEpoch">—</span>
          </div>
          <div class="wb-info-row">
            <span class="wb-info-label">最后心跳</span>
            <span class="wb-info-value" id="wbDirectorTick">—</span>
          </div>
          <div class="wb-info-row">
            <span class="wb-info-label">自治模式</span>
            <span class="wb-info-value" id="wbAutonomousMode">—</span>
          </div>
        </div>
        <div class="wb-epoch-timeline" id="wbEpochTimeline">
          <div class="wb-epoch-timeline-label">Epoch 推进</div>
          <div class="wb-epoch-nodes" id="wbEpochNodes">
            <span class="wb-epoch-empty">无历史</span>
          </div>
        </div>
      </div>
    </section>

    <!-- 区2：快捷操作 -->
    <section class="wb-panel wb-quick-actions">
      <div class="wb-panel-header">
        <span class="wb-panel-title">快捷操作</span>
      </div>
      <div class="wb-ops-list">
        <button class="cw-btn cw-btn-primary" id="cwBroadcastBtn" title="向所有 agent 发布广播">
          <span class="cw-btn-icon">📡</span><span>发布广播</span>
        </button>
        <button class="cw-btn cw-btn-ghost" id="cwDirectiveBtn" title="向协作流注入引导指令">
          <span class="cw-btn-icon">⚡</span><span>注入引导</span>
        </button>
        <button class="cw-btn cw-btn-ghost" id="wbAgentCardBtn" title="查看本地 Agent Card">
          <span class="cw-btn-icon">⚙</span><span>Agent Card</span>
        </button>
      </div>
      <div class="wb-template-list">
        <div class="wb-template-label">快捷指令模板</div>
        <div class="wb-template-items" id="wbTemplateItems">
          <button class="wb-template-btn" data-content="请优先处理用户请求" data-rule="ordering" data-priority="high">
            优先处理用户请求
          </button>
          <button class="wb-template-btn" data-content="暂停当前任务等待指令" data-rule="constraint" data-priority="normal">
            暂停任务等待指令
          </button>
          <button class="wb-template-btn" data-content="请汇报当前进度" data-rule="ordering" data-priority="normal">
            汇报当前进度
          </button>
          <button class="wb-template-btn" data-content="切换至自治模式" data-rule="intervention" data-priority="high">
            切换自治模式
          </button>
        </div>
      </div>
    </section>

    <!-- 区3：实时指标 -->
    <section class="wb-panel wb-realtime-metrics">
      <div class="wb-panel-header">
        <span class="wb-panel-title">实时指标</span>
      </div>
      <div class="wb-metrics-body">
        <div class="wb-metric-block">
          <div class="wb-metric-label">消息类型分布</div>
          <div class="wb-metric-chart" id="wbMsgTypeChart">
            <div class="wb-metric-empty">暂无数据</div>
          </div>
        </div>
        <div class="wb-metric-block">
          <div class="wb-metric-label">Agent 活跃度 Top 3</div>
          <div class="wb-metric-list" id="wbAgentRanking">
            <div class="wb-metric-empty">暂无数据</div>
          </div>
        </div>
        <div class="wb-metric-block">
          <div class="wb-metric-label">最近广播</div>
          <div class="wb-metric-broadcast" id="wbLastBroadcast">
            <div class="wb-metric-empty">暂无广播</div>
          </div>
        </div>
        <div class="wb-metric-block">
          <div class="wb-metric-label">总消息趋势</div>
          <div class="wb-metric-trend" id="wbMsgTrend">
            <span class="wb-metric-trend-value">0</span>
            <span class="wb-metric-trend-arrow">→</span>
            <span class="wb-metric-trend-label">最近 1 小时</span>
          </div>
        </div>
      </div>
    </section>
  </aside>
```

同时移除原 `.wb-stats-panel` 区块（统计已合并到实时指标）。

- [ ] **Step 2: 手动验证**

刷新 `/workbench`：
Expected: 右栏显示三个面板「Director 控制台」「快捷操作」「实时指标」。Director 状态区显示大状态药丸 + 4 行键值 + Epoch 时间线占位。快捷操作区显示 3 个按钮 + 4 个模板按钮。实时指标区显示 4 个指标块占位。

- [ ] **Step 3: 提交**

```bash
git add teage-liu/web/workbench.html
git commit -m "refactor(workbench): restructure right column as Director console with 3 sections"
```

---

### Task 13: Director 状态卡 + epoch 时间线渲染

**Files:**
- Modify: `teage-liu/web/static/css/workbench.css`（新增 Director 控制台样式）
- Modify: `teage-liu/web/static/js/workbench.js`（loadDirectorDetails 扩展 + 新增 renderEpochTimeline）

**Interfaces:**
- Consumes: `/api/multiagent/status` 响应含 `epoch_history`（Task 3 产出）
- Produces: epoch 时间线水平展示最近 5 个节点

- [ ] **Step 1: 新增 Director 控制台 CSS**

在 `teage-liu/web/static/css/workbench.css` 末尾追加：

```css
/* ========== Director 控制台 ========== */
.wb-director-status-card {
  flex-shrink: 0;
}
.wb-director-card-body {
  padding: 14px 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.wb-director-state-large {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border-radius: var(--radius-lg, 10px);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  font-size: 13px;
  font-weight: 600;
}
.wb-director-state-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--text-muted);
  flex-shrink: 0;
}
.wb-director-state-large.state-observing .wb-director-state-dot {
  background: var(--info, #58a6ff);
  box-shadow: 0 0 8px rgba(88, 166, 255, 0.6);
  animation: cw-pulse 2.4s ease-in-out infinite;
}
.wb-director-state-large.state-observing {
  color: var(--info, #58a6ff);
  border-color: rgba(88, 166, 255, 0.3);
  background: rgba(88, 166, 255, 0.06);
}
.wb-director-state-large.state-injected {
  color: var(--warning, #d29922);
  border-color: rgba(210, 153, 34, 0.3);
  background: rgba(210, 153, 34, 0.06);
}
.wb-director-state-large.state-injected .wb-director-state-dot {
  background: var(--warning, #d29922);
  box-shadow: 0 0 8px rgba(210, 153, 34, 0.6);
}
.wb-director-state-large.state-disabled,
.wb-director-state-large.state-unknown {
  color: var(--text-muted);
}
.wb-director-info-compact {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

/* epoch 时间线 */
.wb-epoch-timeline {
  border-top: 1px solid var(--border-subtle);
  padding-top: 10px;
}
.wb-epoch-timeline-label {
  font-size: 10px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 8px;
}
.wb-epoch-nodes {
  display: flex;
  align-items: center;
  gap: 6px;
  overflow-x: auto;
  padding-bottom: 4px;
}
.wb-epoch-nodes::-webkit-scrollbar { height: 4px; }
.wb-epoch-node {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  flex-shrink: 0;
  min-width: 48px;
  cursor: pointer;
}
.wb-epoch-node:hover .wb-epoch-node-dot {
  transform: scale(1.3);
}
.wb-epoch-node-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--accent);
  border: 2px solid var(--bg-primary);
  box-shadow: 0 0 0 1px var(--accent);
  transition: transform var(--transition);
}
.wb-epoch-node.wb-epoch-current .wb-epoch-node-dot {
  background: var(--accent-hover, var(--accent));
  box-shadow: 0 0 0 2px var(--accent), 0 0 8px var(--accent-glow);
}
.wb-epoch-node-epoch {
  font-size: 10px;
  font-family: var(--font-mono);
  color: var(--text-secondary);
  font-weight: 600;
}
.wb-epoch-node-time {
  font-size: 9px;
  color: var(--text-muted);
  font-family: var(--font-mono);
}
.wb-epoch-empty {
  font-size: 11px;
  color: var(--text-muted);
  font-style: italic;
  padding: 4px 0;
}
.wb-epoch-connector {
  width: 12px;
  height: 1px;
  background: var(--border);
  flex-shrink: 0;
}
```

- [ ] **Step 2: 修改 loadDirectorDetails 渲染 epoch 时间线**

修改 `teage-liu/web/static/js/workbench.js:48-67`（loadDirectorDetails 函数）：

```javascript
  function loadDirectorDetails() {
    fetch("/api/multiagent/status")
      .then(function (resp) {
        if (!resp.ok) return null;
        return resp.json();
      })
      .then(function (data) {
        if (!data || !data.enabled) {
          _setDirectorStateLarge("disabled");
          return;
        }
        var director = data.director || {};
        $("wbDirectorRole").textContent = data.role || "—";
        $("wbDirectorEpoch").textContent = director.epoch != null ? director.epoch : "—";
        $("wbDirectorTick").textContent = director.last_tick || "—";
        $("wbAutonomousMode").textContent = data.autonomous_mode ? "是" : "否";
        updateLocalAgentDisplay(data.local_agent_id || "");

        // 同步顶栏 Director 状态药丸
        var state = director.state || "observing";
        _setDirectorStateLarge(state);

        // 渲染 epoch 时间线
        renderEpochTimeline(data.epoch_history || [], director.epoch);
      })
      .catch(function (err) {
        console.warn("workbench: loadDirectorDetails 失败", err);
        _setDirectorStateLarge("unknown");
      });
  }

  /** 设置大状态药丸显示 */
  function _setDirectorStateLarge(state) {
    var el = $("wbDirectorStateLarge");
    if (!el) return;
    var text = el.querySelector(".wb-director-state-text");
    var labels = {
      observing: "● 观察中",
      injected: "▲ 已注入引导",
      disabled: "○ 未启用",
      unknown: "? 未知",
    };
    el.className = "wb-director-state-large state-" + state;
    if (text) text.textContent = labels[state] || state;
  }

  /** 渲染 epoch 时间线 */
  function renderEpochTimeline(history, currentEpoch) {
    var container = $("wbEpochNodes");
    if (!container) return;
    if (!history || history.length === 0) {
      container.innerHTML = '<span class="wb-epoch-empty">无历史</span>';
      return;
    }
    var html = "";
    history.forEach(function (node, idx) {
      var isCurrent = node.epoch === currentEpoch;
      var time = node.ts ? _formatEpochTime(node.ts) : "";
      html += '<div class="wb-epoch-node' + (isCurrent ? " wb-epoch-current" : "") + '" data-seq="' + (node.seq || 0) + '" title="Epoch ' + node.epoch + (time ? ' · ' + time : '') + '">';
      html += '<span class="wb-epoch-node-dot"></span>';
      html += '<span class="wb-epoch-node-epoch">E' + escapeHtml(String(node.epoch)) + '</span>';
      if (time) html += '<span class="wb-epoch-node-time">' + escapeHtml(time) + '</span>';
      html += '</div>';
      if (idx < history.length - 1) {
        html += '<div class="wb-epoch-connector"></div>';
      }
    });
    container.innerHTML = html;

    // 点击 epoch 节点跳转对应消息
    container.querySelectorAll(".wb-epoch-node").forEach(function (node) {
      node.addEventListener("click", function () {
        var seq = parseInt(node.getAttribute("data-seq"), 10);
        if (seq > 0 && window.CollabWorkbench && window.CollabWorkbench.jumpToMessage) {
          window.CollabWorkbench.jumpToMessage(seq);
        }
      });
    });
  }

  /** 格式化 epoch 节点时间（仅显示时分） */
  function _formatEpochTime(ts) {
    if (!ts) return "";
    try {
      var d = new Date(ts);
      var hh = d.getHours().toString().padStart(2, "0");
      var mm = d.getMinutes().toString().padStart(2, "0");
      return hh + ":" + mm;
    } catch (e) {
      return "";
    }
  }
```

- [ ] **Step 3: 手动验证**

刷新 `/workbench`：
- Director 状态卡显示大药丸（彩色背景 + 动画点）
- 4 行键值紧凑显示
- Epoch 时间线水平排列最近 5 个节点（圆点 + E1/E2/... + 时分）
- 当前 epoch 节点高亮（双层光环）
- 点击节点跳转到对应 directive 消息

- [ ] **Step 4: 提交**

```bash
git add teage-liu/web/static/css/workbench.css teage-liu/web/static/js/workbench.js
git commit -m "feat(workbench): render director state card with epoch timeline"
```

---

### Task 14: 快捷指令模板填充功能

**Files:**
- Modify: `teage-liu/web/static/css/workbench.css`（新增模板按钮样式）
- Modify: `teage-liu/web/static/js/workbench.js`（新增 setupTemplateButtons）

**Interfaces:**
- Consumes: `cwDirectiveModal`（已存在）+ `_bindModal` 绑定逻辑
- Produces: 点击模板按钮自动填入 directive 模态框

- [ ] **Step 1: 新增模板按钮 CSS**

在 `teage-liu/web/static/css/workbench.css` 末尾追加：

```css
/* ========== 快捷指令模板 ========== */
.wb-template-list {
  padding: 0 16px 12px;
  border-top: 1px dashed var(--border);
  padding-top: 10px;
  margin-top: 4px;
}
.wb-template-label {
  font-size: 10px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 6px;
}
.wb-template-items {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.wb-template-btn {
  display: block;
  width: 100%;
  text-align: left;
  padding: 6px 10px;
  font-size: 11px;
  font-family: inherit;
  color: var(--text-secondary);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  border-radius: var(--radius-md, 6px);
  cursor: pointer;
  transition: all var(--transition);
}
.wb-template-btn:hover {
  color: var(--accent);
  border-color: var(--accent);
  background: var(--glass-bg-active);
  transform: translateX(2px);
}
```

- [ ] **Step 2: 在 workbench.js 中新增 setupTemplateButtons**

在 `teage-liu/web/static/js/workbench.js` 的 `setupAgentCardView` 函数后追加：

```javascript
  /** 快捷指令模板：点击填入 directive 模态框 */
  function setupTemplateButtons() {
    var buttons = document.querySelectorAll(".wb-template-btn");
    buttons.forEach(function (btn) {
      btn.addEventListener("click", function () {
        var content = btn.getAttribute("data-content") || "";
        var rule = btn.getAttribute("data-rule") || "ordering";
        var priority = btn.getAttribute("data-priority") || "normal";

        // 打开 directive 模态框
        var modal = $("cwDirectiveModal");
        if (!modal) return;
        modal.hidden = false;
        modal.classList.add("show");

        // 填入字段
        var inputEl = $("cwDirectiveInput");
        var ruleEl = $("cwDirectiveRuleType");
        var priEl = $("cwDirectivePriority");
        var submitBtn = $("cwDirectiveSubmitBtn");
        if (inputEl) inputEl.value = content;
        if (ruleEl) ruleEl.value = rule;
        if (priEl) priEl.value = priority;
        if (submitBtn) submitBtn.disabled = false;
        if (inputEl) inputEl.focus();
      });
    });
  }
```

- [ ] **Step 3: 在 init 中调用 setupTemplateButtons**

修改 `teage-liu/web/static/js/workbench.js:348-361`（init 函数），在 `setupAgentCardView()` 后追加：

```javascript
    setupTemplateButtons();
```

- [ ] **Step 4: 手动验证**

刷新 `/workbench`：
- 快捷操作区下方显示 4 个模板按钮（带左边框 hover 效果）
- 点击「优先处理用户请求」→ directive 模态框打开，内容已填入「请优先处理用户请求」，规则=ordering，优先级=high
- 点击「切换自治模式」→ 内容填入「切换至自治模式」，规则=intervention，优先级=high
- 直接点击「注入」可发送

- [ ] **Step 5: 提交**

```bash
git add teage-liu/web/static/css/workbench.css teage-liu/web/static/js/workbench.js
git commit -m "feat(workbench): add quick directive templates that prefill modal"
```

---

### Task 15: 实时指标区渲染（消息类型分布 + agent 活跃度 + 最近广播 + 总消息趋势）

**Files:**
- Modify: `teage-liu/web/static/css/workbench.css`（新增指标区样式）
- Modify: `teage-liu/web/static/js/workbench.js`（新增 renderRealtimeMetrics）

**Interfaces:**
- Consumes: `allMessages` 数组（已存在）+ `/api/multiagent/collab/messages`
- Produces: 4 个指标块动态渲染

- [ ] **Step 1: 新增实时指标 CSS**

在 `teage-liu/web/static/css/workbench.css` 末尾追加：

```css
/* ========== 实时指标 ========== */
.wb-realtime-metrics {
  flex: 1;
  min-height: 0;
}
.wb-metrics-body {
  padding: 12px 16px;
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.wb-metric-block {
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.wb-metric-label {
  font-size: 10px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.wb-metric-empty {
  font-size: 11px;
  color: var(--text-muted);
  font-style: italic;
}
/* 消息类型分布条形图 */
.wb-metric-chart {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.wb-metric-bar-row {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 10px;
}
.wb-metric-bar-label {
  width: 60px;
  color: var(--text-secondary);
  text-transform: lowercase;
}
.wb-metric-bar-track {
  flex: 1;
  height: 6px;
  background: var(--bg-tertiary, var(--bg-secondary));
  border-radius: var(--radius-full, 9999px);
  overflow: hidden;
  border: 1px solid var(--border);
}
.wb-metric-bar-fill {
  height: 100%;
  border-radius: var(--radius-full, 9999px);
  transition: width var(--transition-slow);
}
.wb-metric-bar-value {
  font-family: var(--font-mono);
  color: var(--text-muted);
  min-width: 24px;
  text-align: right;
}
/* Agent 活跃度排行 */
.wb-metric-list {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.wb-rank-row {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 11px;
}
.wb-rank-num {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--bg-tertiary, var(--bg-secondary));
  color: var(--text-muted);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 9px;
  font-weight: 600;
  flex-shrink: 0;
}
.wb-rank-row.wb-rank-1 .wb-rank-num {
  background: var(--accent);
  color: var(--accent-contrast, #fff);
}
.wb-rank-name {
  flex: 1;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  font-family: var(--font-mono);
}
.wb-rank-count {
  color: var(--accent);
  font-family: var(--font-mono);
  font-weight: 600;
}
/* 最近广播 */
.wb-metric-broadcast {
  padding: 8px 10px;
  background: var(--bg-tertiary, var(--bg-secondary));
  border-radius: var(--radius-md, 6px);
  border: 1px solid var(--border);
  border-left: 3px solid var(--accent);
}
.wb-broadcast-content {
  font-size: 11px;
  color: var(--text-primary);
  line-height: 1.4;
  word-break: break-word;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.wb-broadcast-time {
  font-size: 10px;
  color: var(--text-muted);
  margin-top: 4px;
  font-family: var(--font-mono);
}
/* 总消息趋势 */
.wb-metric-trend {
  display: flex;
  align-items: baseline;
  gap: 6px;
}
.wb-metric-trend-value {
  font-size: 22px;
  font-weight: 600;
  color: var(--accent);
  font-family: var(--font-mono);
}
.wb-metric-trend-arrow {
  font-size: 14px;
  color: var(--text-muted);
}
.wb-metric-trend-arrow.up { color: var(--success, #3fb950); }
.wb-metric-trend-arrow.down { color: var(--danger, #ef4444); }
.wb-metric-trend-label {
  font-size: 10px;
  color: var(--text-muted);
}
```

- [ ] **Step 2: 在 workbench.js 中新增 renderRealtimeMetrics**

在 `teage-liu/web/static/js/workbench.js` 的 `updateStats` 函数后追加：

```javascript
  /** 渲染实时指标区 */
  function renderRealtimeMetrics() {
    _renderMsgTypeChart();
    _renderAgentRanking();
    _renderLastBroadcast();
    _renderMsgTrend();
  }

  /** 消息类型分布条形图 */
  function _renderMsgTypeChart() {
    var container = $("wbMsgTypeChart");
    if (!container) return;
    var counts = {};
    var total = 0;
    allMessages.forEach(function (m) {
      var t = m.type || "unknown";
      counts[t] = (counts[t] || 0) + 1;
      total++;
    });
    if (total === 0) {
      container.innerHTML = '<div class="wb-metric-empty">暂无数据</div>';
      return;
    }
    var colors = {
      announce: "var(--info, #58a6ff)",
      request: "var(--warning, #d29922)",
      response: "var(--success, #3fb950)",
      relay: "var(--text-muted)",
      directive: "var(--danger, #f85149)",
      status: "var(--accent)",
    };
    var html = "";
    Object.keys(counts).forEach(function (type) {
      var count = counts[type];
      var pct = (count / total) * 100;
      var color = colors[type] || "var(--accent)";
      html += '<div class="wb-metric-bar-row">';
      html += '<span class="wb-metric-bar-label">' + escapeHtml(type) + '</span>';
      html += '<div class="wb-metric-bar-track"><div class="wb-metric-bar-fill" style="width:' + pct + '%;background:' + color + '"></div></div>';
      html += '<span class="wb-metric-bar-value">' + count + '</span>';
      html += '</div>';
    });
    container.innerHTML = html;
  }

  /** Agent 活跃度排行（按消息数 Top 3） */
  function _renderAgentRanking() {
    var container = $("wbAgentRanking");
    if (!container) return;
    var counts = {};
    allMessages.forEach(function (m) {
      var f = m.from || "?";
      counts[f] = (counts[f] || 0) + 1;
    });
    var sorted = Object.keys(counts)
      .map(function (k) { return { name: k, count: counts[k] }; })
      .sort(function (a, b) { return b.count - a.count; })
      .slice(0, 3);
    if (sorted.length === 0) {
      container.innerHTML = '<div class="wb-metric-empty">暂无数据</div>';
      return;
    }
    var html = "";
    sorted.forEach(function (item, idx) {
      var rank = idx + 1;
      html += '<div class="wb-rank-row wb-rank-' + rank + '">';
      html += '<span class="wb-rank-num">' + rank + '</span>';
      html += '<span class="wb-rank-name">' + escapeHtml(item.name) + '</span>';
      html += '<span class="wb-rank-count">' + item.count + '</span>';
      html += '</div>';
    });
    container.innerHTML = html;
  }

  /** 最近广播 */
  function _renderLastBroadcast() {
    var container = $("wbLastBroadcast");
    if (!container) return;
    var broadcasts = allMessages.filter(function (m) {
      return m.from === "user" && m.type === "request";
    });
    if (broadcasts.length === 0) {
      container.innerHTML = '<div class="wb-metric-empty">暂无广播</div>';
      return;
    }
    var last = broadcasts[broadcasts.length - 1];
    var content = last.content || "";
    var ts = last.timestamp || last.ts || "";
    var timeRel = ts ? _relativeTimeShort(ts) : "";
    var html = '<div class="wb-broadcast-content">' + escapeHtml(content) + '</div>';
    if (timeRel) html += '<div class="wb-broadcast-time">' + escapeHtml(timeRel) + '</div>';
    container.innerHTML = html;
  }

  /** 总消息趋势（最近 1 小时消息数 + 与上 1 小时对比） */
  function _renderMsgTrend() {
    var container = $("wbMsgTrend");
    if (!container) return;
    var now = Date.now();
    var oneHourAgo = now - 3600000;
    var twoHoursAgo = now - 7200000;
    var recent = 0;
    var previous = 0;
    allMessages.forEach(function (m) {
      var ts = m.timestamp || m.ts;
      if (!ts) return;
      var t = new Date(ts).getTime();
      if (isNaN(t)) return;
      if (t >= oneHourAgo) recent++;
      else if (t >= twoHoursAgo) previous++;
    });
    var valueEl = container.querySelector(".wb-metric-trend-value");
    var arrowEl = container.querySelector(".wb-metric-trend-arrow");
    if (valueEl) valueEl.textContent = String(recent);
    if (arrowEl) {
      arrowEl.className = "wb-metric-trend-arrow";
      if (recent > previous) {
        arrowEl.textContent = "↑";
        arrowEl.classList.add("up");
      } else if (recent < previous) {
        arrowEl.textContent = "↓";
        arrowEl.classList.add("down");
      } else {
        arrowEl.textContent = "→";
      }
    }
  }
```

- [ ] **Step 3: 在 setupMessageListener 中调用 renderRealtimeMetrics**

修改 `teage-liu/web/static/js/workbench.js:113-124`（setupMessageListener）：

```javascript
  function setupMessageListener() {
    window.addEventListener("collab-message", function (e) {
      var msg = e.detail || {};
      allMessages.push(msg);
      setTimeout(function () {
        updateStats();
        applyFilter();
        renderRealtimeMetrics();
      }, 0);
    });
  }
```

- [ ] **Step 4: 在 init 中首次调用 + 定时刷新**

修改 `teage-liu/web/static/js/workbench.js:348-361`（init 函数），在 `setTimeout(updateStats, 1500)` 后追加：

```javascript
    setTimeout(renderRealtimeMetrics, 1500);
```

修改 `setupPeriodicRefresh`：

```javascript
  function setupPeriodicRefresh() {
    setInterval(loadDirectorDetails, 15000);
    setInterval(function () {
      updateStats();
      renderRealtimeMetrics();
    }, 10000);
  }
```

- [ ] **Step 5: 手动验证**

刷新 `/workbench`，触发消息（广播/directive）：
- 消息类型分布：6 类消息横向条形图，按比例填充
- Agent 活跃度 Top 3：第 1 名 accent 色，显示 agent_id + 消息数
- 最近广播：accent 左边框 + 内容片段 + 相对时间
- 总消息趋势：大数字（最近 1 小时数）+ 上下箭头（绿色↑/红色↓/灰色→）

- [ ] **Step 6: 提交**

```bash
git add teage-liu/web/static/css/workbench.css teage-liu/web/static/js/workbench.js
git commit -m "feat(workbench): render realtime metrics (msg type chart / agent ranking / broadcast / trend)"
```

---

## 阶段 5：响应式 + 主题适配 + 古早记录加载（Task 16-18）

### Task 16: 响应式断点 + 浅色主题适配

**Files:**
- Modify: `teage-liu/web/static/css/workbench.css:187-202`（响应式断点）
- Modify: `teage-liu/web/static/css/workbench.css`（新增抽屉按钮样式）

**Interfaces:**
- Consumes: 无
- Produces: 1200/900/600 三档响应式 + 浅色主题适配

- [ ] **Step 1: 重写响应式断点**

修改 `teage-liu/web/static/css/workbench.css:187-202`：

```css
/* 响应式：中等屏幕收窄侧栏 */
@media (max-width: 1200px) {
  .wb-main {
    grid-template-columns: 240px 1fr 240px;
  }
}

/* 小屏幕：右栏折叠为顶部抽屉 */
@media (max-width: 900px) {
  .wb-main {
    grid-template-columns: 220px 1fr;
  }
  .wb-action-panel {
    position: fixed;
    top: 56px;
    right: 0;
    bottom: 0;
    width: 280px;
    background: var(--bg-primary);
    border-left: 1px solid var(--border);
    transform: translateX(100%);
    transition: transform var(--transition-slow);
    z-index: 50;
    overflow-y: auto;
  }
  .wb-action-panel.wb-drawer-open {
    transform: translateX(0);
    box-shadow: -8px 0 24px rgba(0, 0, 0, 0.3);
  }
  .wb-drawer-toggle {
    display: flex;
  }
}

/* 极小屏幕：左栏也折叠 */
@media (max-width: 600px) {
  .wb-main {
    grid-template-columns: 1fr;
  }
  .wb-sidebar {
    position: fixed;
    top: 56px;
    left: 0;
    bottom: 0;
    width: 240px;
    background: var(--bg-primary);
    border-right: 1px solid var(--border);
    transform: translateX(-100%);
    transition: transform var(--transition-slow);
    z-index: 50;
  }
  .wb-sidebar.wb-drawer-open {
    transform: translateX(0);
    box-shadow: 8px 0 24px rgba(0, 0, 0, 0.3);
  }
}

/* 大屏：抽屉切换按钮隐藏 */
.wb-drawer-toggle {
  display: none;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  border-radius: var(--radius-md, 6px);
  cursor: pointer;
  color: var(--text-secondary);
  font-size: 14px;
}
.wb-drawer-toggle:hover {
  color: var(--accent);
  border-color: var(--accent);
}
```

- [ ] **Step 2: 在顶栏添加抽屉切换按钮**

修改 `teage-liu/web/workbench.html:49-72`（顶栏右部），在 `<div class="wb-topbar-right">` 内最前面追加：

```html
    <button class="wb-drawer-toggle" id="wbRightDrawerToggle" title="切换右栏">≡</button>
    <button class="wb-drawer-toggle" id="wbLeftDrawerToggle" title="切换左栏" hidden>☰</button>
```

注：左栏切换按钮默认 hidden，仅在小屏（≤600px）通过 CSS 显示。

- [ ] **Step 3: 新增抽屉切换 JS 逻辑**

在 `teage-liu/web/static/js/workbench.js` 的 `init` 函数前追加：

```javascript
  /** 抽屉切换（小屏响应式） */
  function setupDrawers() {
    var rightBtn = $("wbRightDrawerToggle");
    var leftBtn = $("wbLeftDrawerToggle");
    var rightPanel = document.querySelector(".wb-action-panel");
    var leftPanel = document.querySelector(".wb-sidebar");

    if (rightBtn && rightPanel) {
      rightBtn.addEventListener("click", function () {
        rightPanel.classList.toggle("wb-drawer-open");
      });
    }
    if (leftBtn && leftPanel) {
      leftBtn.addEventListener("click", function () {
        leftPanel.classList.toggle("wb-drawer-open");
      });
    }

    // 小屏时显示左栏切换按钮
    function updateDrawerToggleVisibility() {
      if (leftBtn) leftBtn.hidden = window.innerWidth > 600;
    }
    updateDrawerToggleVisibility();
    window.addEventListener("resize", updateDrawerToggleVisibility);
  }
```

在 `init` 函数中调用 `setupDrawers()`。

- [ ] **Step 4: 浅色主题适配验证**

切换到浅色主题：
- Agent Card banner 渐变背景应为暖色 0.06 透明度
- 所有文字、边框、按钮颜色保持高对比度
- 进度条填充色跟随 accent 主题色

- [ ] **Step 5: 手动验证响应式**

调整浏览器宽度：
- 1200px 以下：左栏 240px / 右栏 240px
- 900px 以下：右栏消失，顶部出现「≡」按钮，点击展开右栏抽屉
- 600px 以下：左栏也消失，顶部出现「☰」按钮，点击展开左栏抽屉

- [ ] **Step 6: 提交**

```bash
git add teage-liu/web/static/css/workbench.css teage-liu/web/workbench.html teage-liu/web/static/js/workbench.js
git commit -m "feat(workbench): add responsive drawers and light theme adaptation"
```

---

### Task 17: 中栏消息流古早记录加载（滚动到顶部自动 prepend）

**Files:**
- Modify: `teage-liu/web/static/js/collab-workbench.js`（新增 setupStreamScrollLoader）
- Modify: `teage-liu/web/static/css/collab-workbench.css`（新增顶部 loading 样式）

**Interfaces:**
- Consumes: `GET /api/multiagent/collab/messages?before_seq=&limit=`（Task 5 产出）
- Produces: 消息流滚动到顶部自动加载更早消息 + prepend

- [ ] **Step 1: 新增顶部 loading CSS**

在 `teage-liu/web/static/css/collab-workbench.css` 末尾追加：

```css
/* 消息流顶部加载提示 */
.cw-stream-loading-top {
  text-align: center;
  padding: 8px;
  color: var(--text-muted);
  font-size: 11px;
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  border-radius: var(--radius-md, 6px);
  margin-bottom: 4px;
}
.cw-stream-loading-top.cw-stream-end {
  color: var(--text-muted);
  font-style: italic;
}
```

- [ ] **Step 2: 在 collab-workbench.js 中新增 setupStreamScrollLoader**

在 `teage-liu/web/static/js/collab-workbench.js` 的 `clearStream` 函数后追加：

```javascript
  // ========== 古早记录加载（滚动到顶部自动 prepend） ==========

  var _streamMinSeq = null;       // 当前已加载的最小 seq
  var _streamLoadingEarlier = false;
  var _streamNoMoreEarlier = false;

  /** 初始化消息流滚动加载器 */
  function setupStreamScrollLoader() {
    var stream = document.getElementById("cwStream");
    if (!stream) return;
    stream.addEventListener("scroll", function () {
      // 滚动到顶部（scrollTop < 50px）且未在加载且未到尽头
      if (stream.scrollTop < 50 && !_streamLoadingEarlier && !_streamNoMoreEarlier) {
        _loadEarlierMessages();
      }
    });
  }

  /** 加载更早的消息并 prepend 到流顶部 */
  function _loadEarlierMessages() {
    var stream = document.getElementById("cwStream");
    if (!stream) return;
    if (_streamMinSeq === null || _streamMinSeq <= 1) {
      _streamNoMoreEarlier = true;
      return;
    }

    _streamLoadingEarlier = true;
    // 在顶部插入 loading 提示
    var loadingEl = document.createElement("div");
    loadingEl.className = "cw-stream-loading-top";
    loadingEl.textContent = "加载更早消息…";
    stream.insertBefore(loadingEl, stream.firstChild);

    var beforeSeq = _streamMinSeq;
    var url = "/api/multiagent/collab/messages?before_seq=" + beforeSeq + "&limit=50";

    fetch(url)
      .then(function (resp) {
        if (!resp.ok) throw new Error("HTTP " + resp.status);
        return resp.json();
      })
      .then(function (data) {
        var messages = (data && data.messages) || [];
        loadingEl.remove();

        if (messages.length === 0) {
          _streamNoMoreEarlier = true;
          // 显示「已到最早」提示（5 秒后消失）
          var endEl = document.createElement("div");
          endEl.className = "cw-stream-loading-top cw-stream-end";
          endEl.textContent = "已到最早消息";
          stream.insertBefore(endEl, stream.firstChild);
          setTimeout(function () { if (endEl.parentNode) endEl.remove(); }, 5000);
          return;
        }

        // 保留滚动位置：记录原 scrollHeight
        var prevScrollHeight = stream.scrollHeight;
        var prevScrollTop = stream.scrollTop;

        // prepend 消息（按 seq 升序插入到顶部）
        // 先移除占位空状态
        var empty = stream.querySelector(".cw-empty");
        if (empty) empty.remove();

        messages.forEach(function (msg) {
          // 在流顶部插入，但保持 seq 升序（最早在最上）
          // 由于 messages 已按 seq 升序，从最后一条往前 insertBefore
          var item = _buildMessageElement(msg);
          if (item) {
            stream.insertBefore(item, stream.firstChild.nextSibling);
          }
        });

        // 更新最小 seq
        var newMinSeq = messages[0].seq;
        if (_streamMinSeq === null || newMinSeq < _streamMinSeq) {
          _streamMinSeq = newMinSeq;
        }

        // 恢复滚动位置（保持在原内容顶部）
        var newScrollHeight = stream.scrollHeight;
        stream.scrollTop = prevScrollTop + (newScrollHeight - prevScrollHeight);
      })
      .catch(function (err) {
        console.warn("加载更早消息失败:", err);
        loadingEl.remove();
      })
      .finally(function () {
        _streamLoadingEarlier = false;
      });
  }

  /** 构造消息元素（复用 renderCollabMessage 的渲染逻辑，但不appendChild） */
  function _buildMessageElement(msg) {
    // 此函数与 renderCollabMessage 中的元素构造逻辑保持一致
    // 为避免重复，重构 renderCollabMessage 复用此函数
    // 简化实现：直接构造并返回
    var type = msg.type || "status";
    var from = msg.from || "?";
    var to = msg.to || "";
    var content = msg.content || "";
    var ts = formatTime(msg.ts || msg.timestamp || "");
    var seq = msg.seq;

    // seq 去重：已渲染的不再构造
    if (seq != null && _renderedSeqs.has(seq)) return null;
    if (seq != null) _renderedSeqs.add(seq);

    var extras = [];
    if (msg.rule_type) extras.push('<span class="cw-msg-extra-item"><b>rule:</b> ' + escapeHtml(msg.rule_type) + "</span>");
    if (msg.priority) extras.push('<span class="cw-msg-extra-item"><b>priority:</b> ' + escapeHtml(msg.priority) + "</span>");
    if (msg.action) extras.push('<span class="cw-msg-extra-item"><b>action:</b> ' + escapeHtml(msg.action) + "</span>");
    if (msg.via) extras.push('<span class="cw-msg-extra-item"><b>via:</b> ' + escapeHtml(msg.via) + "</span>");
    if (to && to !== "*") extras.push('<span class="cw-msg-extra-item"><b>to:</b> ' + escapeHtml(to) + "</span>");

    var item = document.createElement("div");
    item.className =