# 调度执行强化 Implementation Plan

> **For agentic workers:** Use the java-dev-skills workflow to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 cron 调度流程上叠加 4 层 hook（校验/补偿/重试/通知），消除 context 注入遗漏、命名前缀不一致、失败无通知等 6 个系统性脆弱点。

**Architecture:** 方案 A+（分层增强 + 可扩展层接口）。`_trigger` 重命名为 `_run_schedule`，在 3 个接入点（before_execute / on_failure / after_execute）插入 hook 调用。每层 hook 实现 `ScheduleHook` 协议（继承 `ScheduleHookBase` no-op 基类），通过 `config.cron.hooks.{name}.enabled` 独立开关。`HookRegistry` 每次 `_run_schedule` 调用时从 `config_manager` 读取最新配置重建（即时生效，无需 Orchestrator 重建）。

**Tech Stack:** Python 3.11+ / asyncio / dataclasses / unittest / FastAPI / Jinja2 / aiosmtplib

## Global Constraints

- **测试框架**：`unittest.TestCase`（非 pytest），运行命令 `python -m unittest tests.test_xxx -v`
- **测试 mock**：所有测试文件顶部需 `from tests._mock_deps import install_mocks; install_mocks()` 后再 import hermes 模块
- **代码风格**：`from __future__ import annotations` 置于文件首行（模块 docstring 之后）
- **TDD 硬约束**：没有失败测试就不写生产代码（项目记忆约束）
- **异步 SDK**：LLM 客户端需使用异步 SDK，全链路异步处理（项目记忆约束）
- **错误处理**：后端需全局捕获 ToolError 异常，返回清晰错误信息而非 500（项目记忆约束）
- **配置热更新**：所有改进配置需支持 config.yaml 热更新，可独立开关回滚（项目记忆约束）
- **API 密钥**：禁止硬编码，使用 `${ENV_VAR}` 占位符（项目记忆约束）
- **cron 工具集请求级隔离**：不修改全局 ToolRegistry（项目记忆约束）
- **依赖批次**：批次 1→2→3→4 顺序依赖，不可跳序

## File Structure

### 新建文件
| 路径 | 职责 |
|------|------|
| `hermes/tasks/hooks/__init__.py` | hook 包入口，导出 ScheduleHook / ScheduleHookBase / HookRegistry / 4 个 hook 类 / RetryDecision |
| `hermes/tasks/hooks/base.py` | ScheduleHook Protocol + ScheduleHookBase 基类 + RetryDecision dataclass |
| `hermes/tasks/hooks/registry.py` | HookRegistry（管理 hook 实例，按 config 开关启用） |
| `hermes/tasks/hooks/validate_hook.py` | ValidateHook（L1 校验层） |
| `hermes/tasks/hooks/catchup_hook.py` | CatchUpHook（L2 补偿层） |
| `hermes/tasks/hooks/retry_hook.py` | RetryHook（L3 重试层）+ `_is_permanent_error` 函数 |
| `hermes/tasks/hooks/notify_hook.py` | NotifyHook（L4 通知层） |
| `hermes/tasks/hooks/notify_templates.py` | 邮件 HTML 模板 + Webhook JSON 模板渲染 |
| `tests/test_schedule_hook_base.py` | ScheduleHookBase 基类测试 |
| `tests/test_hook_registry.py` | HookRegistry 测试 |
| `tests/test_validate_hook.py` | ValidateHook 测试 |
| `tests/test_catchup_hook.py` | CatchUpHook 测试 |
| `tests/test_retry_hook.py` | RetryHook + _is_permanent_error 测试 |
| `tests/test_notify_hook.py` | NotifyHook 测试 |
| `tests/test_workflow_execution_error.py` | WorkflowExecutionError + 异常类测试 |
| `tests/test_run_schedule_integration.py` | _run_schedule 主流程集成测试 |
| `tests/test_cron_e2e.py` | 完整链路 e2e 测试 |
| `tests/test_cron_failure_recovery.py` | 失败→重试→恢复/通知测试 |
| `tests/test_cron_catchup.py` | 停机补偿测试 |
| `tests/test_cron_persist.py` | runs.jsonl 持久化 + read_by_run_id 测试 |

### 修改文件
| 路径 | 修改内容 |
|------|----------|
| `hermes/tasks/cron_tool_loader.py` | CronToolMeta 新增 `dir_name` / `registered_name` 字段 + `get_registered_name()` 方法 |
| `hermes/agent/cron_tool_registry.py` | `register` 用 registered_name 作 key；`_build_handler` 用 dir_name；移除 `_normalize_name` |
| `hermes/tasks/scheduler.py` | `_trigger`→`_run_schedule` 重命名 + hook 调用点；新增 `_collect_components` / `_run_schedule_direct` / `_failure_counts` / `hooks` 属性 |
| `hermes/tasks/workflow/base.py` | WorkflowContext 新增 5 个 hook 状态字段 |
| `hermes/tasks/workflow/step_trace.py` | StepTrace 新增 `retry_reason` 字段 + `from_dict` 同步 |
| `hermes/tasks/run_summary.py` | RunSummary 新增 `retry_count` / `notified` / `notification_channels` 字段 + `from_dict` 同步 + `read_by_run_id` 方法 |
| `hermes/tasks/workflow/spec.py` | `ALLOWED_ON_FAILURE_ACTIONS` 移除 `"retry"` |
| `hermes/tasks/workflow/engine.py` | 移除 RetryBudget 调用；末尾 raise WorkflowExecutionError |
| `hermes/agent/tool_error.py` | 新增 `HookAbortError` / `ValidationError` / `WorkflowExecutionError` 异常类 |
| `hermes/tasks/workflow/validator.py` | 新增 `validate_workflow_spec` 函数（工具存在性 + step_type 校验） |
| `hermes/config_helpers.py` | `_CONFIG_TO_COMPONENTS` 更新注释（cron.hooks.* 不触发 Orchestrator 重建） |
| `hermes/routes/cron_tools.py` | 新增 5 个 API 端点（执行历史/手动重跑/单次详情/最近记录/统计） |
| `web/scheduler.html` | 执行历史面板 + 手动重跑按钮 + 配置编辑器 + 状态徽章 |
| `web/monitor.html` | 调度监控面板（实时状态 + 连续失败告警 + 今日统计） |
| `web/static/js/scheduler.js` | 前端逻辑：历史加载/重跑/编辑/徽章渲染 |
| `web/static/js/monitor.js` | 前端逻辑：实时状态/告警卡片/统计 |
| `cron_tool/blog_monitor_joyehuang/run.py` | SMTP 配置从环境变量读取 |
| `config.yaml.example` | 新增 `cron.hooks.*` 配置段示例 |

---

## 批次 1：架构健壮性（P0，无依赖）

### Task 1: CronToolMeta 分离 dir_name / registered_name（Q5 决策）

**Files:**
- Modify: `hermes/tasks/cron_tool_loader.py:77-122`（CronToolMeta dataclass）
- Modify: `hermes/agent/cron_tool_registry.py:89-130, 181-200, 334-367`（register / _build_handler / 移除 _normalize_name）
- Test: `tests/test_cron_tool.py`（现有测试，需更新期望）

**Interfaces:**
- Consumes: 现有 `CronToolMeta`（9 字段）/ `CronToolRegistry.register` / `_normalize_name`
- Produces: `CronToolMeta` 新增 `dir_name: str` / `registered_name: str` / `get_registered_name() -> str`；`CronToolRegistry` 的 `_tools` / `_handlers` key 统一为 `cron_tool__{dir_name}`

- [ ] **Step 1: Write the failing test**

在 `tests/test_cron_tool.py` 末尾新增测试类：

```python
class TestCronToolMetaPrefixSeparation(unittest.TestCase):
    """Q5: 分离 dir_name（文件系统标识）与 registered_name（registry 标识）。"""

    def test_meta_has_dir_name_and_registered_name_fields(self):
        """CronToolMeta 新增 dir_name / registered_name 字段，默认空串。"""
        from hermes.tasks.cron_tool_loader import CronToolMeta
        meta = CronToolMeta(
            name="blog_monitor", version="1.0.0", description="d",
            author="a", input_schema={},
        )
        self.assertEqual(meta.dir_name, "")
        self.assertEqual(meta.registered_name, "")

    def test_get_registered_name_fallback_to_name(self):
        """registered_name 未设置时回退到 name。"""
        from hermes.tasks.cron_tool_loader import CronToolMeta
        meta = CronToolMeta(
            name="blog_monitor", version="1.0.0", description="d",
            author="a", input_schema={},
        )
        self.assertEqual(meta.get_registered_name(), "blog_monitor")

    def test_get_registered_name_returns_prefixed(self):
        """registered_name 设置后返回带前缀名。"""
        from hermes.tasks.cron_tool_loader import CronToolMeta
        meta = CronToolMeta(
            name="blog_monitor", version="1.0.0", description="d",
            author="a", input_schema={},
            dir_name="blog_monitor",
            registered_name="cron_tool__blog_monitor",
        )
        self.assertEqual(meta.get_registered_name(), "cron_tool__blog_monitor")


class TestCronToolRegistryRegisterWithPrefix(unittest.TestCase):
    """Q5: register 用 registered_name 作 key，_build_handler 用 dir_name。"""

    def test_register_stores_under_prefixed_key(self):
        """register 后 _tools key 为 cron_tool__{dir_name}。"""
        from hermes.agent.cron_tool_registry import CronToolRegistry
        registry = CronToolRegistry(base_dir="cron_tool")
        # 用 echo_text 测试工具（已存在）
        registry.register("echo_text")
        self.assertIn("cron_tool__echo_text", registry._tools)
        self.assertNotIn("echo_text", registry._tools)

    def test_has_tool_accepts_prefixed_name(self):
        """has_tool 接受带前缀名。"""
        from hermes.agent.cron_tool_registry import CronToolRegistry
        registry = CronToolRegistry(base_dir="cron_tool")
        registry.register("echo_text")
        self.assertTrue(registry.has_tool("cron_tool__echo_text"))

    def test_has_tool_rejects_unprefixed_name(self):
        """Q5: 移除 _normalize_name 后，裸名不再被接受。"""
        from hermes.agent.cron_tool_registry import CronToolRegistry
        registry = CronToolRegistry(base_dir="cron_tool")
        registry.register("echo_text")
        self.assertFalse(registry.has_tool("echo_text"))

    def test_get_tools_schema_uses_registered_name(self):
        """schema 的 name 字段用 registered_name（带前缀）。"""
        from hermes.agent.cron_tool_registry import CronToolRegistry
        registry = CronToolRegistry(base_dir="cron_tool")
        registry.register("echo_text")
        schemas = registry.get_tools_schema()
        self.assertEqual(len(schemas), 1)
        self.assertEqual(schemas[0]["name"], "cron_tool__echo_text")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_cron_tool.TestCronToolMetaPrefixSeparation tests.test_cron_tool.TestCronToolRegistryRegisterWithPrefix -v`
Expected: FAIL（`CronToolMeta` 无 `dir_name` / `registered_name` 字段；`register` 仍用裸名作 key；`has_tool` 仍接受裸名）

- [ ] **Step 3: Add dir_name / registered_name to CronToolMeta**

修改 `hermes/tasks/cron_tool_loader.py` 的 `CronToolMeta` dataclass（在 `run_interpreter` 字段之后新增）：

```python
@dataclass
class CronToolMeta:
    # ... 现有 9 个字段保持不变 ...
    name: str
    version: str
    description: str
    author: str
    input_schema: Dict[str, Any]
    timeout: Optional[int] = None
    tool_dir: str = ""
    run_script: str = ""
    run_interpreter: list = field(default_factory=list)

    # === Q5 新增 ===
    dir_name: str = ""           # 裸目录名（如 "blog_monitor_joyehuang"），用于文件路径
    registered_name: str = ""    # 带前缀的 registry key（如 "cron_tool__blog_monitor_joyehuang"）

    def get_registered_name(self) -> str:
        """返回 registry 注册名（带前缀）。未设置时回退到 name。"""
        return self.registered_name or self.name

    # ... get_timeout / to_schema 保持不变 ...
```

- [ ] **Step 4: Refactor register / _build_handler / remove _normalize_name**

修改 `hermes/agent/cron_tool_registry.py`：

```python
_PREFIX = "cron_tool__"

class CronToolRegistry:
    # ... __init__ 不变 ...

    def register(self, name: str) -> CronToolMeta:
        """注册 cron_tool。Q5: 分离 dir_name / registered_name。"""
        dir_name = name[len(_PREFIX):] if name.startswith(_PREFIX) else name
        meta = _load_tool(dir_name, base_dir=self.base_dir)
        meta.dir_name = dir_name
        meta.registered_name = f"{_PREFIX}{dir_name}"
        self._tools[meta.registered_name] = meta
        handler = self._build_handler(meta)
        self._handlers[meta.registered_name] = handler
        logger.info("已注册 cron_tool: %s (dir=%s, version=%s)",
                    meta.registered_name, meta.dir_name, meta.version)
        return meta

    def _build_handler(self, meta: CronToolMeta) -> Callable[..., str]:
        """Q5: 用 meta.dir_name 调 _execute_tool，避免路径错误。"""
        def _handler(**kwargs: Any) -> str:
            context = None
            provider = getattr(self, "_context_provider", None)
            if callable(provider):
                try:
                    context = provider() or {}
                except Exception:
                    context = {}
            return _execute_tool(
                name=meta.dir_name,  # Q5: 用裸目录名拼路径
                input=kwargs,
                context=context,
                meta=meta,
                base_dir=self.base_dir,
            )
        return _handler

    def get_tools_schema(self) -> List[Dict[str, Any]]:
        """Q5: tool name 用 registered_name（带前缀）。"""
        return [
            {"name": meta.get_registered_name(), "description": meta.description,
             "input_schema": meta.input_schema}
            for meta in self._tools.values()
        ]

    # has_tool / execute_tool / get_tool_handler / get_tool_meta: 移除 _normalize_name 调用，
    # 直接用传入名查询（调用方应传带前缀名）
    def has_tool(self, name: str) -> bool:
        return name in self._tools

    def get_tool_handler(self, name: str) -> Optional[Callable[..., str]]:
        return self._handlers.get(name)

    def get_tool_meta(self, name: str) -> Optional[CronToolMeta]:
        return self._tools.get(name)

    def execute_tool(self, tool_name: str, tool_input: dict) -> str:
        # ... schema 校验逻辑保持不变，但用传入名查询 ...
        handler = self._handlers.get(tool_name)
        meta = self._tools.get(tool_name)
        if handler is None or meta is None:
            raise ToolNotFoundError(
                tool_name=tool_name,
                reason=f"未注册的 cron_tool: {tool_name}",
                suggestion="检查 cron_tool 配置或调用 list_tools 查看可用工具",
            )
        # ... 其余逻辑不变 ...
```

**删除** `_normalize_name` 方法（整段删除）。

- [ ] **Step 5: Update existing tests that depend on _normalize_name**

在 `tests/test_cron_tool.py` 中，搜索所有调用 `has_tool("echo_text")`（裸名）的测试，改为 `has_tool("cron_tool__echo_text")`。搜索 `register("echo_text")` 后断言 `_tools["echo_text"]` 的，改为 `_tools["cron_tool__echo_text"]`。

- [ ] **Step 6: Run all cron_tool tests to verify pass**

Run: `python -m unittest tests.test_cron_tool -v`
Expected: PASS（所有测试通过）

- [ ] **Step 7: Commit**

```bash
git add hermes/tasks/cron_tool_loader.py hermes/agent/cron_tool_registry.py tests/test_cron_tool.py
git commit -m "feat(cron_tool): Q5 分离 dir_name/registered_name，移除 _normalize_name"
```

---

### Task 2: _collect_components 集中注入（治本脆弱点 2）

**Files:**
- Modify: `hermes/tasks/scheduler.py:1070-1190`（`_build_workflow_context` 方法）
- Test: `tests/test_scheduler.py`

**Interfaces:**
- Consumes: 现有 `_build_workflow_context` 中的散落 setattr
- Produces: `CronScheduler._collect_components(orchestrator) -> Dict[str, Any]`；`_INJECT_COMPONENTS` 类常量

- [ ] **Step 1: Write the failing test**

在 `tests/test_scheduler.py` 新增测试类：

```python
class TestCollectComponents(unittest.TestCase):
    """3.5: _collect_components 集中注入，消除手动 setattr 遗漏。"""

    def test_collect_components_returns_all_injected_attrs(self):
        """_collect_components 返回 _INJECT_COMPONENTS 列出的所有属性。"""
        from hermes.tasks.scheduler import CronScheduler
        from unittest.mock import MagicMock

        scheduler = CronScheduler.__new__(CronScheduler)
        mock_orch = MagicMock()
        mock_orch.tool_registry = "tr"
        mock_orch.cron_tool_registry = "ctr"
        mock_orch.policy_engine = "pe"
        mock_orch.audit_logger = "al"
        mock_orch.skill_loader = "sl"

        components = scheduler._collect_components(mock_orch)
        self.assertEqual(components["tool_registry"], "tr")
        self.assertEqual(components["cron_tool_registry"], "ctr")
        self.assertEqual(components["policy_engine"], "pe")
        self.assertEqual(components["audit_logger"], "al")
        self.assertEqual(components["skill_loader"], "sl")

    def test_collect_components_handles_missing_attrs(self):
        """orchestrator 缺少属性时返回 None，不抛 AttributeError。"""
        from hermes.tasks.scheduler import CronScheduler
        from unittest.mock import MagicMock

        scheduler = CronScheduler.__new__(CronScheduler)
        mock_orch = MagicMock(spec=[])  # 无任何属性

        components = scheduler._collect_components(mock_orch)
        self.assertIsNone(components["tool_registry"])
        self.assertIsNone(components["cron_tool_registry"])
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_scheduler.TestCollectComponents -v`
Expected: FAIL（`_collect_components` 方法不存在 / `_INJECT_COMPONENTS` 未定义）

- [ ] **Step 3: Implement _collect_components**

在 `hermes/tasks/scheduler.py` 的 `CronScheduler` 类中新增（在 `_build_workflow_context` 之前）：

```python
_INJECT_COMPONENTS = (
    "tool_registry",
    "cron_tool_registry",
    "policy_engine",
    "audit_logger",
    "skill_loader",
)

def _collect_components(self, orchestrator) -> Dict[str, Any]:
    """从 orchestrator 集中收集所有组件，消除手动 setattr 遗漏。

    新增组件时只需在 _INJECT_COMPONENTS 加一行，两个工厂路径都自动覆盖。
    """
    return {attr: getattr(orchestrator, attr, None) for attr in self._INJECT_COMPONENTS}
```

- [ ] **Step 4: Refactor _build_workflow_context to use _collect_components**

在 `_build_workflow_context` 的两个工厂路径（默认工厂 + 自定义工厂）中，将散落的 `setattr(ctx, "tool_registry", ...)` 等替换为：

```python
# 集中注入（替换原有散落的 setattr）
components = self._collect_components(orchestrator)
for attr, value in components.items():
    setattr(ctx, attr, value)
```

**注意**：保留 `run_id` / `orchestrator` / `session_logger` / `react_loop` 的 setattr（这些不在 `_INJECT_COMPONENTS` 中，因为来源不同）。

- [ ] **Step 5: Run test to verify pass**

Run: `python -m unittest tests.test_scheduler.TestCollectComponents -v`
Expected: PASS

- [ ] **Step 6: Run existing scheduler tests to verify no regression**

Run: `python -m unittest tests.test_scheduler -v`
Expected: PASS（所有现有测试通过）

- [ ] **Step 7: Commit**

```bash
git add hermes/tasks/scheduler.py tests/test_scheduler.py
git commit -m "refactor(scheduler): 3.5 _collect_components 集中注入消除 setattr 遗漏"
```

---

### Task 3: _append_run_summary 持久化失败告警（治本脆弱点 6）

**Files:**
- Modify: `hermes/tasks/scheduler.py`（`_append_run_summary` 方法，搜索定位）
- Test: `tests/test_cron_persist.py`（新建）

**Interfaces:**
- Consumes: 现有 `_append_run_summary` 方法
- Produces: 失败时 `logger.error` 告警（而非静默 return）

- [ ] **Step 1: Write the failing test**

新建 `tests/test_cron_persist.py`：

```python
"""runs.jsonl 持久化失败告警测试（治本脆弱点 6）。"""
from __future__ import annotations

import sys
import unittest
from pathlib import Path
from unittest.mock import MagicMock, patch

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.scheduler import CronScheduler


class TestAppendRunSummaryFailureAlert(unittest.TestCase):
    """runs_store 未初始化或 RunSummary 未导入时记 error 日志，不静默 return。"""

    def test_runs_store_none_logs_error(self):
        """runs_store 为 None 时记 error 日志。"""
        scheduler = CronScheduler.__new__(CronScheduler)
        scheduler.runs_store = None
        mock_schedule = MagicMock()
        mock_schedule.id = "test_sched"

        with patch("hermes.tasks.scheduler.logger") as mock_logger:
            scheduler._append_run_summary(mock_schedule, MagicMock(), "", "", 0.0, "")
            mock_logger.error.assert_called()
            error_msg = mock_logger.error.call_args[0][0]
            self.assertIn("runs_store", error_msg)

    def test_persist_exception_logs_error(self):
        """append 抛异常时记 error 日志，不传播。"""
        scheduler = CronScheduler.__new__(CronScheduler)
        scheduler.runs_store = MagicMock()
        scheduler.runs_store.append.side_effect = OSError("disk full")
        mock_schedule = MagicMock()
        mock_schedule.id = "test_sched"

        with patch("hermes.tasks.scheduler.logger") as mock_logger:
            # 不应抛异常
            scheduler._append_run_summary(mock_schedule, MagicMock(), "", "", 0.0, "")
            mock_logger.error.assert_called()


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_cron_persist -v`
Expected: FAIL（现有 `_append_run_summary` 在 `runs_store is None` 时静默 return，不记 error）

- [ ] **Step 3: Implement failure alert in _append_run_summary**

在 `hermes/tasks/scheduler.py` 的 `_append_run_summary` 方法中，将静默 return 改为 `logger.error` + return：

```python
def _append_run_summary(self, schedule, result, started_at, finished_at, duration, assistant_response=""):
    if RunSummary is None:
        logger.error("RunSummary 未导入，无法持久化执行记录")
        return
    if self.runs_store is None:
        logger.error("runs_store 未初始化，调度 %s 的执行记录将丢失", schedule.id)
        return
    try:
        # ... 现有 append 逻辑 ...
    except Exception as e:
        logger.error("持久化 RunSummary 失败 调度=%s: %s", schedule.id, e)
```

- [ ] **Step 4: Run test to verify pass**

Run: `python -m unittest tests.test_cron_persist -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add hermes/tasks/scheduler.py tests/test_cron_persist.py
git commit -m "fix(scheduler): 4.4 持久化失败告警，不再静默 return"
```

---

### Task 4: 修复预先存在的测试债务（Q16/Q17 决策）

**Files:**
- Modify: `tests/test_cron_tool.py`（11 个预先失败的测试用例）
- Modify: `tests/test_retry_budget.py`（如存在与 RetryBudget 相关的失败测试）

**Interfaces:**
- Consumes: Task 1 的 Q5 命名前缀统一（测试期望需更新为带前缀名）
- Produces: 所有预先失败的测试通过

- [ ] **Step 1: Identify all failing tests**

Run: `python -m unittest tests.test_cron_tool -v 2>&1 | findstr "FAIL ERROR"`
记录所有失败的测试名与失败原因。

- [ ] **Step 2: Fix each failing test**

针对每个失败测试：
- 如果是「期望返回字符串但代码抛异常」：更新为 `assertRaises` 断言
- 如果是「期望裸名但 Q5 后用带前缀名」：更新为 `cron_tool__{name}`
- 如果是「期望 RetryBudget 行为但 Q1 后移除」：更新为期望 `WorkflowExecutionError`

**逐个测试修复**（不要批量替换，逐个验证）。

- [ ] **Step 3: Run all cron_tool tests to verify pass**

Run: `python -m unittest tests.test_cron_tool -v`
Expected: PASS（所有测试通过，无 FAIL/ERROR）

- [ ] **Step 4: Run retry_budget tests**

Run: `python -m unittest tests.test_retry_budget -v`
Expected: 如果测试仍引用 RetryBudget 且 Q1 尚未移除（Task 10 才移除），暂保留；如果已移除则跳过此步。

- [ ] **Step 5: Commit**

```bash
git add tests/test_cron_tool.py
git commit -m "test(cron_tool): Q16/Q17 修复预先失败的测试，对齐 Q5 命名前缀"
```

---

## 批次 2：校验层（P0，依赖批次 1）

### Task 5: ScheduleHook 协议 + ScheduleHookBase + HookRegistry + WorkflowContext 新增字段（Q2 决策）

**Files:**
- Create: `hermes/tasks/hooks/__init__.py`
- Create: `hermes/tasks/hooks/base.py`
- Create: `hermes/tasks/hooks/registry.py`
- Modify: `hermes/tasks/workflow/base.py:86-163`（WorkflowContext dataclass）
- Test: `tests/test_schedule_hook_base.py`（新建）
- Test: `tests/test_hook_registry.py`（新建）

**Interfaces:**
- Consumes: `WorkflowContext`（现有 19 字段）/ `WorkflowResult`（现有）
- Produces: `ScheduleHook` Protocol / `ScheduleHookBase` 基类 / `RetryDecision` dataclass / `HookRegistry` 类 / `WorkflowContext` 新增 5 字段（`schedule` / `retry_count` / `retry_max` / `last_error` / `validation_errors`）

- [ ] **Step 1: Write the failing test for ScheduleHookBase**

新建 `tests/test_schedule_hook_base.py`：

```python
"""ScheduleHookBase 基类 + RetryDecision 测试（3.3）。"""
from __future__ import annotations

import sys
import unittest
from pathlib import Path

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.hooks.base import ScheduleHookBase, RetryDecision


class TestScheduleHookBase(unittest.TestCase):
    """ScheduleHookBase 提供 no-op 默认实现，避免 AttributeError。"""

    def test_before_execute_returns_ctx_unchanged(self):
        """默认 before_execute 返回 ctx 不变。"""
        hook = ScheduleHookBase()
        ctx = {"test": "value"}
        import asyncio
        result = asyncio.run(hook.before_execute(ctx))
        self.assertIs(result, ctx)

    def test_on_failure_returns_give_up(self):
        """默认 on_failure 返回 give_up（不重试）。"""
        hook = ScheduleHookBase()
        import asyncio
        decision = asyncio.run(hook.on_failure({}, ValueError("test")))
        self.assertFalse(decision.retry)
        self.assertEqual(decision.delay_seconds, 0)

    def test_after_execute_is_noop(self):
        """默认 after_execute 为 no-op，不抛异常。"""
        hook = ScheduleHookBase()
        import asyncio
        # 不应抛异常
        asyncio.run(hook.after_execute({}, MagicMock()))

    def test_name_default_empty(self):
        """name 默认空串。"""
        hook = ScheduleHookBase()
        self.assertEqual(hook.name, "")


class TestRetryDecision(unittest.TestCase):
    """RetryDecision 工厂方法。"""

    def test_give_up(self):
        d = RetryDecision.give_up(reason="测试放弃")
        self.assertFalse(d.retry)
        self.assertEqual(d.reason, "测试放弃")

    def test_retry_after(self):
        d = RetryDecision.retry_after(seconds=300, reason="重试")
        self.assertTrue(d.retry)
        self.assertEqual(d.delay_seconds, 300)
        self.assertEqual(d.reason, "重试")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_schedule_hook_base -v`
Expected: FAIL（`hermes.tasks.hooks.base` 模块不存在）

- [ ] **Step 3: Create hooks package with base.py**

新建 `hermes/tasks/hooks/__init__.py`（空文件，后续 task 会填充导出）。

新建 `hermes/tasks/hooks/base.py`：

```python
"""ScheduleHook 协议 + ScheduleHookBase 基类 + RetryDecision（3.3）。

设计原则：
- 签名通用（before/on_failure/after），不绑定 schedule 特有概念
- ctx 类型为 WorkflowContext（复用现有）
- result 类型为 WorkflowResult（复用现有）
- 所有 hook 实现必须继承 ScheduleHookBase，避免 HookRegistry 循环调用时 AttributeError
"""
from __future__ import annotations

from typing import Any, Protocol, runtime_checkable


class RetryDecision:
    """重试决策。"""

    def __init__(self, retry: bool, delay_seconds: int = 0, reason: str = ""):
        self.retry = retry
        self.delay_seconds = delay_seconds
        self.reason = reason

    @classmethod
    def give_up(cls, reason: str = "") -> "RetryDecision":
        return cls(retry=False, reason=reason)

    @classmethod
    def retry_after(cls, seconds: int, reason: str = "") -> "RetryDecision":
        return cls(retry=True, delay_seconds=seconds, reason=reason)


class ScheduleHook(Protocol):
    """调度执行钩子协议（类型声明，不可直接继承）。"""

    name: str

    async def before_execute(self, ctx: Any) -> Any: ...
    async def on_failure(self, ctx: Any, error: Exception) -> RetryDecision: ...
    async def after_execute(self, ctx: Any, result: Any) -> None: ...


class ScheduleHookBase:
    """ScheduleHook 协议的 no-op 默认实现基类。

    所有 hook 实现必须继承此类，按需 override 对应方法。
    未 override 的方法为 no-op，避免 HookRegistry 循环调用时 AttributeError。
    """

    name: str = ""

    async def before_execute(self, ctx: Any) -> Any:
        return ctx

    async def on_failure(self, ctx: Any, error: Exception) -> RetryDecision:
        return RetryDecision.give_up()

    async def after_execute(self, ctx: Any, result: Any) -> None:
        pass
```

- [ ] **Step 4: Run test to verify pass**

Run: `python -m unittest tests.test_schedule_hook_base -v`
Expected: PASS

- [ ] **Step 5: Add 5 new fields to WorkflowContext**

修改 `hermes/tasks/workflow/base.py` 的 `WorkflowContext` dataclass（在现有最后一个 dataclass 字段之后，`ensure_report_dir` 方法之前新增）：

```python
    # === Q2 新增：hook 间传递的状态字段（5 个，全部带默认值，向后兼容）===
    schedule: Optional[Any] = None          # 调度项引用（hooks 需要）
    retry_count: int = 0                     # 当前重试次数
    retry_max: int = 0                       # 最大重试次数（HookRegistry.get_retry_max() 写入）
    last_error: Optional[Exception] = None   # 最近一次异常
    validation_errors: list = field(default_factory=list)  # 校验错误列表
```

**注意**：需要在文件顶部确保 `field` 已从 `dataclasses` 导入，`Optional` / `Any` 已从 `typing` 导入。

- [ ] **Step 6: Write failing test for HookRegistry**

新建 `tests/test_hook_registry.py`：

```python
"""HookRegistry 测试（3.7）。"""
from __future__ import annotations

import asyncio
import sys
import unittest
from pathlib import Path
from unittest.mock import MagicMock, AsyncMock

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.hooks.base import ScheduleHookBase, RetryDecision
from hermes.tasks.hooks.registry import HookRegistry


class TestHookRegistryConstruction(unittest.TestCase):
    """HookRegistry 按 config 开关启用/禁用 hook。"""

    def test_all_hooks_enabled_by_default(self):
        """默认配置启用所有 4 个 hook。"""
        # 注意：此测试在 hook 实现类创建后才能通过
        # 此处先测试空 config 时不报错
        registry = HookRegistry(config={}, scheduler_ref=None)
        self.assertIsInstance(registry.hooks, dict)

    def test_disabled_hook_not_in_registry(self):
        """enabled=False 的 hook 不加载。"""
        config = {"validate": {"enabled": False}}
        registry = HookRegistry(config=config, scheduler_ref=None)
        self.assertNotIn("validate", registry.hooks)


class TestHookRegistryGetRetryMax(unittest.TestCase):
    """get_retry_max 返回 retry hook 的 max_retries。"""

    def test_no_retry_hook_returns_zero(self):
        registry = HookRegistry(config={}, scheduler_ref=None)
        self.assertEqual(registry.get_retry_max(), 0)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 7: Run test to verify it fails**

Run: `python -m unittest tests.test_hook_registry -v`
Expected: FAIL（`hermes.tasks.hooks.registry` 模块不存在）

- [ ] **Step 8: Create registry.py**

新建 `hermes/tasks/hooks/registry.py`：

```python
"""HookRegistry（3.7）。

管理所有 hook 实例，按 config 开关启用/禁用。
Q8 决策：_failure_counts 不放在 HookRegistry（避免重建时迁移），
        而是放在 CronScheduler 实例上（长生命周期）。
"""
from __future__ import annotations

from typing import Any, Dict, Optional

from .base import ScheduleHookBase, RetryDecision


class HookRegistry:
    """管理所有 hook 实例，按 config 开关启用/禁用。"""

    def __init__(self, config: dict, scheduler_ref: Optional[Any] = None):
        self.hooks: Dict[str, ScheduleHookBase] = {}
        self._scheduler_ref = scheduler_ref
        self._config = config

        # 延迟导入 hook 实现类，避免循环依赖
        # 各 hook 类在 Task 8/12/11/14 中实现，此处用 try/except 兼容渐进实现
        try:
            from .validate_hook import ValidateHook
            if config.get("validate", {}).get("enabled", True):
                self.hooks["validate"] = ValidateHook(config.get("validate", {}))
        except ImportError:
            pass

        try:
            from .catchup_hook import CatchUpHook
            if config.get("catchup", {}).get("enabled", True):
                self.hooks["catchup"] = CatchUpHook(config.get("catchup", {}))
        except ImportError:
            pass

        try:
            from .retry_hook import RetryHook
            if config.get("retry", {}).get("enabled", True):
                self.hooks["retry"] = RetryHook(config.get("retry", {}))
        except ImportError:
            pass

        try:
            from .notify_hook import NotifyHook
            if config.get("notify", {}).get("enabled", True):
                self.hooks["notify"] = NotifyHook(
                    config.get("notify", {}), scheduler_ref=scheduler_ref
                )
        except ImportError:
            pass

    async def before_execute(self, ctx: Any) -> Any:
        for hook in self.hooks.values():
            ctx = await hook.before_execute(ctx)
        return ctx

    async def on_failure(self, ctx: Any, error: Exception) -> RetryDecision:
        if "retry" in self.hooks:
            return await self.hooks["retry"].on_failure(ctx, error)
        return RetryDecision.give_up()

    async def after_execute(self, ctx: Any, result: Any) -> None:
        for hook in self.hooks.values():
            await hook.after_execute(ctx, result)

    def get_retry_max(self) -> int:
        """Q11: 供 ctx.retry_max 填充，NotifyHook 通知模板读取。"""
        retry_hook = self.hooks.get("retry")
        return getattr(retry_hook, "max_retries", 0) if retry_hook else 0
```

- [ ] **Step 9: Run all hook tests to verify pass**

Run: `python -m unittest tests.test_schedule_hook_base tests.test_hook_registry -v`
Expected: PASS

- [ ] **Step 10: Commit**

```bash
git add hermes/tasks/hooks/ hermes/tasks/workflow/base.py tests/test_schedule_hook_base.py tests/test_hook_registry.py
git commit -m "feat(hooks): 3.3/3.4/3.7 ScheduleHook 协议+基类+Registry+WorkflowContext 5字段"
```

---

### Task 6: 异常类定义 HookAbortError / ValidationError / WorkflowExecutionError（Q10 决策）

**Files:**
- Modify: `hermes/agent/tool_error.py`（新增 3 个异常类）
- Test: `tests/test_workflow_execution_error.py`（新建）

**Interfaces:**
- Consumes: `ToolError` 基类 / `ErrorStage` 枚举
- Produces: `HookAbortError(reason, errors)` / `ValidationError(errors)` / `WorkflowExecutionError(result)`

- [ ] **Step 1: Write the failing test**

新建 `tests/test_workflow_execution_error.py`：

```python
"""异常类定义测试（9.2）。"""
from __future__ import annotations

import sys
import unittest
from pathlib import Path
from unittest.mock import MagicMock

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.agent.tool_error import (
    HookAbortError, ValidationError, WorkflowExecutionError, ToolError, ErrorStage,
)


class TestHookAbortError(unittest.TestCase):

    def test_inherits_tool_error(self):
        err = HookAbortError(reason="校验终止", errors=["err1", "err2"])
        self.assertIsInstance(err, ToolError)

    def test_carries_errors_list(self):
        err = HookAbortError(reason="终止", errors=["e1", "e2"])
        self.assertEqual(err.errors, ["e1", "e2"])

    def test_errors_defaults_empty(self):
        err = HookAbortError(reason="终止")
        self.assertEqual(err.errors, [])

    def test_stage_is_pre_execution(self):
        err = HookAbortError(reason="终止")
        self.assertEqual(err.stage, ErrorStage.PRE_EXECUTION)


class TestValidationError(unittest.TestCase):

    def test_inherits_tool_error(self):
        err = ValidationError(errors=["step1 无效"])
        self.assertIsInstance(err, ToolError)

    def test_carries_errors_list(self):
        err = ValidationError(errors=["e1", "e2"])
        self.assertEqual(err.errors, ["e1", "e2"])

    def test_reason_joined_from_errors(self):
        err = ValidationError(errors=["e1", "e2"])
        self.assertIn("e1", err.reason)
        self.assertIn("e2", err.reason)


class TestWorkflowExecutionError(unittest.TestCase):

    def test_inherits_tool_error(self):
        mock_result = MagicMock()
        mock_result.errors = ["step1 失败"]
        err = WorkflowExecutionError(mock_result)
        self.assertIsInstance(err, ToolError)

    def test_carries_result(self):
        mock_result = MagicMock()
        mock_result.errors = ["失败"]
        err = WorkflowExecutionError(mock_result)
        self.assertIs(err.result, mock_result)

    def test_stage_is_execution(self):
        mock_result = MagicMock()
        mock_result.errors = []
        err = WorkflowExecutionError(mock_result)
        self.assertEqual(err.stage, ErrorStage.EXECUTION)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_workflow_execution_error -v`
Expected: FAIL（`HookAbortError` / `ValidationError` / `WorkflowExecutionError` 不存在）

- [ ] **Step 3: Add 3 exception classes to tool_error.py**

在 `hermes/agent/tool_error.py` 末尾新增（在现有异常类之后，`__all__` 之前）：

```python
class HookAbortError(ToolError):
    """校验层主动终止执行（9.2）。"""

    def __init__(self, reason: str, errors: list = None):
        self.errors = errors or []
        super().__init__(
            tool_name="hook",
            reason=reason,
            suggestion="检查校验错误列表",
            category="hook_abort",
            stage=ErrorStage.PRE_EXECUTION,
        )


class ValidationError(ToolError):
    """workflow spec 校验失败（9.2）。"""

    def __init__(self, errors: list):
        self.errors = errors
        super().__init__(
            tool_name="validate_hook",
            reason="; ".join(errors),
            suggestion="修正 workflow spec 配置",
            category="validation_error",
            stage=ErrorStage.PRE_EXECUTION,
        )


class WorkflowExecutionError(ToolError):
    """workflow 执行失败（Q3 决策 D，9.2）。

    WorkflowEngine.execute 末尾 if not result.success 时抛出，
    携带完整 WorkflowResult（含 step_traces），供 RetryHook 读取失败 step 的
    error_class 判断 permanent/transient。
    """

    def __init__(self, result):
        self.result = result
        error_summary = "; ".join(result.errors) if result.errors else "workflow 执行失败"
        super().__init__(
            tool_name="workflow_engine",
            reason=error_summary,
            suggestion="检查 step_traces 中的失败 step",
            category="workflow_execution_error",
            stage=ErrorStage.EXECUTION,
        )
```

更新 `__all__`（如存在）或确保新类可被 import。

- [ ] **Step 4: Run test to verify pass**

Run: `python -m unittest tests.test_workflow_execution_error -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add hermes/agent/tool_error.py tests/test_workflow_execution_error.py
git commit -m "feat(tool_error): 9.2 新增 HookAbortError/ValidationError/WorkflowExecutionError"
```

---

### Task 7: _run_schedule 主流程改造（3.6）

**Files:**
- Modify: `hermes/tasks/scheduler.py:545-560`（`_trigger` → `_run_schedule` 重命名 + hook 调用点）
- Modify: `hermes/tasks/scheduler.py:414, 1324`（`run_loop` / `trigger_now` 调用点同步重命名）
- Modify: `hermes/tasks/scheduler.py`（CronScheduler `__init__` 新增 `self.hooks` / `self._failure_counts`）
- Test: `tests/test_run_schedule_integration.py`（新建）

**Interfaces:**
- Consumes: Task 5 的 `HookRegistry` / `WorkflowContext` 新字段；Task 6 的异常类
- Produces: `CronScheduler._run_schedule(orchestrator, schedule, started_at_dt, ...)` / `CronScheduler._finalize_failure(ctx, error, wf_result=None)` / `CronScheduler._run_schedule_direct(schedule)` / `CronScheduler.hooks` 属性 / `CronScheduler._failure_counts` 字段

- [ ] **Step 1: Write the failing test**

新建 `tests/test_run_schedule_integration.py`：

```python
"""_run_schedule 主流程集成测试（3.6）。"""
from __future__ import annotations

import asyncio
import sys
import unittest
from datetime import datetime
from pathlib import Path
from unittest.mock import MagicMock, AsyncMock, patch

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.hooks.base import RetryDecision
from hermes.agent.tool_error import ValidationError


class TestRunScheduleHookIntegration(unittest.TestCase):
    """_run_schedule 在 3 个接入点调用 hooks。"""

    def setUp(self):
        """构造最小 CronScheduler 实例。"""
        from hermes.tasks.scheduler import CronScheduler
        self.scheduler = CronScheduler.__new__(CronScheduler)
        self.scheduler.hooks = MagicMock()
        self.scheduler.hooks.before_execute = AsyncMock(return_value=MagicMock(validation_errors=[]))
        self.scheduler.hooks.on_failure = AsyncMock(return_value=RetryDecision.give_up())
        self.scheduler.hooks.after_execute = AsyncMock()
        self.scheduler.hooks.get_retry_max = MagicMock(return_value=3)
        self.scheduler._failure_counts = {}
        self.scheduler._orchestrator = MagicMock()
        self.scheduler._schedules = []
        self.scheduler._cron_exprs = {}
        self.scheduler.runs_store = MagicMock()

    def test_validation_error_routes_to_finalize_failure(self):
        """ValidationError 路由到 _finalize_failure（含 after_execute）。"""
        from hermes.tasks.scheduler import CronScheduler
        from hermes.agent.tool_error import ValidationError

        # 构造 ctx 抛 ValidationError
        mock_ctx = MagicMock()
        mock_ctx.validation_errors = ["step1 工具不存在"]
        self.scheduler.hooks.before_execute = AsyncMock(return_value=mock_ctx)

        # mock _finalize_failure 验证被调用
        self.scheduler._finalize_failure = AsyncMock()
        self.scheduler._build_workflow_context = MagicMock(return_value=mock_ctx)
        self.scheduler._persist = MagicMock()

        mock_schedule = MagicMock()
        mock_schedule.id = "test"

        asyncio.run(self.scheduler._run_schedule(
            self.scheduler._orchestrator, mock_schedule, datetime.now()
        ))

        self.scheduler._finalize_failure.assert_awaited_once()

    def test_after_execute_called_unconditionally(self):
        """成功路径也调用 after_execute（P0 修复）。"""
        # ... 类似结构，mock _execute_workflow 返回成功 result ...
        pass


class TestRunScheduleDirect(unittest.TestCase):
    """_run_schedule_direct 是 thin wrapper。"""

    def test_delegates_to_run_schedule(self):
        from hermes.tasks.scheduler import CronScheduler
        scheduler = CronScheduler.__new__(CronScheduler)
        scheduler._orchestrator = MagicMock()
        scheduler._run_schedule = AsyncMock()

        mock_schedule = MagicMock()
        asyncio.run(scheduler._run_schedule_direct(mock_schedule))

        scheduler._run_schedule.assert_awaited_once()
        call_args = scheduler._run_schedule.call_args
        self.assertIs(call_args[0][0], scheduler._orchestrator)
        self.assertIs(call_args[0][1], mock_schedule)
        self.assertIsInstance(call_args[0][2], datetime)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_run_schedule_integration -v`
Expected: FAIL（`_run_schedule` / `_run_schedule_direct` / `_finalize_failure` 方法不存在）

- [ ] **Step 3: Add hooks + _failure_counts to CronScheduler.__init__**

在 `hermes/tasks/scheduler.py` 的 `CronScheduler.__init__` 末尾新增：

```python
        # === 调度执行强化（3.6/3.7/6.1）===
        from hermes.tasks.hooks.registry import HookRegistry
        cron_config = getattr(self, "_cron_config", {}) or {}
        hooks_config = cron_config.get("hooks", {}) if isinstance(cron_config, dict) else {}
        self.hooks = HookRegistry(config=hooks_config, scheduler_ref=self)
        self._failure_counts: Dict[str, int] = {}  # Q8: schedule_id → 连续失败次数
```

**注意**：`_cron_config` 的来源需根据实际 `__init__` 参数调整。如果 config 通过 `config_manager` 获取，则改为从 `config_manager` 读取 `cron.hooks`。

- [ ] **Step 4: Rename _trigger to _run_schedule + add hook call points**

将 `hermes/tasks/scheduler.py:545` 的 `async def _trigger(self, orchestrator, schedule)` 重命名为 `async def _run_schedule(self, orchestrator, schedule, started_at_dt=None)`。

在方法体内（`ctx = self._build_workflow_context(...)` 之后）插入 hook 调用点：

```python
    async def _run_schedule(self, orchestrator, schedule, started_at_dt=None):
        # === 现有 _trigger 逻辑保持不变（session_logger / task_text 渲染等）===
        if started_at_dt is None:
            started_at_dt = datetime.now()
        # ... 现有 run_id 生成 / history 清空 / session_logger / archive_callback 覆盖 ...
        ctx = self._build_workflow_context(orchestrator, schedule, started_at_dt, ...)
        ctx.schedule = schedule  # Q2: 新增字段填充
        ctx.retry_max = self.hooks.get_retry_max()  # Q11

        # ① before_execute（校验 + 补偿运行时空操作）
        try:
            ctx = await self.hooks.before_execute(ctx)
            if ctx.validation_errors:
                raise ValidationError(ctx.validation_errors)
        except (HookAbortError, ValidationError) as e:
            return await self._finalize_failure(ctx, e, wf_result=None)

        # ② 核心执行 + 重试
        wf_result = None
        while True:
            try:
                wf_result = await asyncio.to_thread(self._execute_workflow, ...)
                break
            except Exception as e:
                ctx.last_error = e
                decision = await self.hooks.on_failure(ctx, e)
                if decision.retry:
                    ctx.retry_count += 1
                    logger.info("调度 %s 第 %d 次重试（%ds 后）", schedule.id, ctx.retry_count, decision.delay_seconds)
                    await asyncio.sleep(decision.delay_seconds)
                    continue
                break

        # ③ after_execute（无条件调用，P0 修复）
        result = wf_result if wf_result is not None else WorkflowResult(
            success=False, errors=[str(ctx.last_error) if ctx.last_error else "未知错误"]
        )
        await self.hooks.after_execute(ctx, result)

        # === 现有 _trigger 逻辑保持不变（RunSummary 持久化 / next_run 更新 / _persist）===
        # ... _append_run_summary / next_run 更新 / _persist ...
```

**注意**：需在文件顶部 import `HookAbortError` / `ValidationError`：
```python
from hermes.agent.tool_error import HookAbortError, ValidationError, WorkflowExecutionError
```

- [ ] **Step 5: Add _finalize_failure method**

在 `_run_schedule` 之后新增：

```python
    async def _finalize_failure(self, ctx, error, wf_result=None):
        """P1 修复：校验/钩子终止时的统一收尾。"""
        from hermes.tasks.workflow.base import WorkflowResult
        result = wf_result if wf_result is not None else WorkflowResult(
            success=False, errors=[str(error)]
        )
        ctx.last_error = error
        await self.hooks.after_execute(ctx, result)
        # 持久化 + next_run 更新
        await asyncio.to_thread(self._append_run_summary, ctx.schedule, result, ...)
        self._persist()
```

- [ ] **Step 6: Add _run_schedule_direct thin wrapper**

```python
    async def _run_schedule_direct(self, schedule):
        """CatchUpHook 专用的补偿执行入口（5.1）。"""
        started_at_dt = datetime.now()
        orchestrator = self._orchestrator
        await self._run_schedule(orchestrator, schedule, started_at_dt)
```

- [ ] **Step 7: Update run_loop and trigger_now call sites**

搜索 `self._trigger(` 的所有调用点（约 line 414 和 1324），替换为 `self._run_schedule(`，并补充 `started_at_dt` 参数：

```python
# run_loop 中
await self._run_schedule(orchestrator, schedule, datetime.now())

# trigger_now 中
await self._run_schedule(orchestrator, schedule, datetime.now())
```

- [ ] **Step 8: Run test to verify pass**

Run: `python -m unittest tests.test_run_schedule_integration -v`
Expected: PASS

- [ ] **Step 9: Run existing scheduler tests to verify no regression**

Run: `python -m unittest tests.test_scheduler -v`
Expected: PASS（如有失败，修复调用点）

- [ ] **Step 10: Commit**

```bash
git add hermes/tasks/scheduler.py tests/test_run_schedule_integration.py
git commit -m "feat(scheduler): 3.6 _trigger→_run_schedule 重命名+hook 调用点+_finalize_failure"
```

---

### Task 8: ValidateHook + validate_workflow_spec（Q12/Q14 决策）

**Files:**
- Create: `hermes/tasks/hooks/validate_hook.py`
- Modify: `hermes/tasks/workflow/validator.py`（新增 `validate_workflow_spec` 函数）
- Test: `tests/test_validate_hook.py`（新建）

**Interfaces:**
- Consumes: Task 5 的 `ScheduleHookBase` / `WorkflowContext`；`WorkflowSpec` / `StepSpec`；`CronToolRegistry` / `ToolRegistry`
- Produces: `ValidateHook(ScheduleHookBase)` 类 / `validate_workflow_spec(spec, cron_tool_registry, tool_registry) -> List[str]` 函数

- [ ] **Step 1: Write the failing test**

新建 `tests/test_validate_hook.py`：

```python
"""ValidateHook + validate_workflow_spec 测试（4.1）。"""
from __future__ import annotations

import asyncio
import sys
import unittest
from pathlib import Path
from unittest.mock import MagicMock

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.hooks.validate_hook import ValidateHook
from hermes.tasks.workflow.validator import validate_workflow_spec
from hermes.tasks.workflow.spec import WorkflowSpec, StepSpec


class TestValidateWorkflowSpec(unittest.TestCase):
    """validate_workflow_spec 工具存在性 + step_type 校验。"""

    def test_valid_spec_returns_empty_errors(self):
        spec = WorkflowSpec(name="test", steps=[
            StepSpec(id="s1", name="step1", type="llm"),
        ])
        errors = validate_workflow_spec(spec=spec, cron_tool_registry=None, tool_registry=None)
        self.assertEqual(errors, [])

    def test_invalid_step_type_returns_error(self):
        spec = WorkflowSpec(name="test", steps=[
            StepSpec(id="s1", name="step1", type="invalid_type"),
        ])
        errors = validate_workflow_spec(spec=spec, cron_tool_registry=None, tool_registry=None)
        self.assertTrue(any("invalid_type" in e for e in errors))

    def test_tool_step_missing_tool_config_returns_error(self):
        spec = WorkflowSpec(name="test", steps=[
            StepSpec(id="s1", name="step1", type="tool", config={}),
        ])
        errors = validate_workflow_spec(spec=spec, cron_tool_registry=None, tool_registry=None)
        self.assertTrue(any("未配置" in e or "config.tool" in e for e in errors))

    def test_tool_step_with_unregistered_tool_returns_error(self):
        spec = WorkflowSpec(name="test", steps=[
            StepSpec(id="s1", name="step1", type="tool", config={"tool": "nonexistent"}),
        ])
        mock_cron_reg = MagicMock()
        mock_cron_reg.has_tool.return_value = False
        mock_tool_reg = MagicMock()
        mock_tool_reg.has_tool.return_value = False
        errors = validate_workflow_spec(
            spec=spec, cron_tool_registry=mock_cron_reg, tool_registry=mock_tool_reg
        )
        self.assertTrue(any("nonexistent" in e for e in errors))

    def test_tool_step_with_registered_cron_tool_returns_no_error(self):
        spec = WorkflowSpec(name="test", steps=[
            StepSpec(id="s1", name="step1", type="tool", config={"tool": "cron_tool__echo"}),
        ])
        mock_cron_reg = MagicMock()
        mock_cron_reg.has_tool.return_value = True
        errors = validate_workflow_spec(
            spec=spec, cron_tool_registry=mock_cron_reg, tool_registry=None
        )
        self.assertEqual(errors, [])


class TestValidateHook(unittest.TestCase):
    """ValidateHook.before_execute 填充 ctx.validation_errors。"""

    def test_before_execute_no_schedule_returns_ctx(self):
        hook = ValidateHook(config={})
        ctx = MagicMock()
        ctx.schedule = None
        result = asyncio.run(hook.before_execute(ctx))
        self.assertIs(result, ctx)

    def test_before_execute_no_workflow_returns_ctx(self):
        hook = ValidateHook(config={})
        ctx = MagicMock()
        ctx.schedule = MagicMock(workflow=None)
        result = asyncio.run(hook.before_execute(ctx))
        self.assertIs(result, ctx)

    def test_before_execute_valid_spec_no_errors(self):
        hook = ValidateHook(config={})
        ctx = MagicMock()
        ctx.schedule = MagicMock()
        ctx.schedule.workflow = WorkflowSpec(name="t", steps=[])
        ctx.cron_tool_registry = None
        ctx.tool_registry = None
        ctx.validation_errors = []
        result = asyncio.run(hook.before_execute(ctx))
        self.assertEqual(result.validation_errors, [])

    def test_before_execute_invalid_spec_fills_errors(self):
        hook = ValidateHook(config={})
        ctx = MagicMock()
        ctx.schedule = MagicMock()
        ctx.schedule.workflow = WorkflowSpec(name="t", steps=[
            StepSpec(id="s1", name="s", type="invalid"),
        ])
        ctx.cron_tool_registry = None
        ctx.tool_registry = None
        ctx.validation_errors = []
        result = asyncio.run(hook.before_execute(ctx))
        self.assertTrue(len(result.validation_errors) > 0)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_validate_hook -v`
Expected: FAIL（`hermes.tasks.hooks.validate_hook` / `validate_workflow_spec` 不存在）

- [ ] **Step 3: Implement validate_workflow_spec in validator.py**

在 `hermes/tasks/workflow/validator.py` 末尾新增函数：

```python
def validate_workflow_spec(
    spec: WorkflowSpec,
    cron_tool_registry: Any = None,
    tool_registry: Any = None,
) -> List[str]:
    """执行前校验 workflow spec（4.1）。

    校验项：
    - workflow.name 非空
    - step.type 属于 ALLOWED_STEP_TYPES（5 种）
    - tool 类型 step 的 config.tool 在 cron_tool_registry 或 tool_registry 中注册

    返回:
        错误信息列表（空列表表示通过）。
    """
    errors: List[str] = []
    if not spec.name:
        errors.append("workflow.name 为空")

    allowed_step_types = ("deterministic", "llm", "tool", "react", "subworkflow")
    for step in spec.steps:
        if step.type not in allowed_step_types:
            errors.append(
                f"step '{step.id}' 的 type '{step.type}' 不合法，允许: {allowed_step_types}"
            )
            continue
        if step.type == "tool":
            tool_name = step.config.get("tool", "") if step.config else ""
            if not tool_name:
                errors.append(f"step '{step.id}' 未配置 config.tool")
                continue
            found = False
            if cron_tool_registry and cron_tool_registry.has_tool(tool_name):
                found = True
            elif tool_registry and tool_registry.has_tool(tool_name):
                found = True
            if not found:
                errors.append(
                    f"step '{step.id}' 引用的工具 '{tool_name}' 未在任何 registry 注册"
                )
    return errors
```

- [ ] **Step 4: Implement ValidateHook**

新建 `hermes/tasks/hooks/validate_hook.py`：

```python
"""ValidateHook（L1 校验层，4.1）。

Q14 决策：validate.enabled=False 只跳过执行前校验，
创建时与启动时仍校验（数据完整性不受运行时开关影响）。
"""
from __future__ import annotations

from typing import Any

from .base import ScheduleHookBase


class ValidateHook(ScheduleHookBase):
    """L1: 执行前校验 workflow spec。"""

    name = "validate"

    def __init__(self, config: dict):
        self.config = config

    async def before_execute(self, ctx: Any) -> Any:
        schedule = getattr(ctx, "schedule", None)
        if not schedule or not getattr(schedule, "workflow", None):
            return ctx  # legacy 路径不校验

        from hermes.tasks.workflow.validator import validate_workflow_spec

        errors = validate_workflow_spec(
            spec=schedule.workflow,
            cron_tool_registry=getattr(ctx, "cron_tool_registry", None),
            tool_registry=getattr(ctx, "tool_registry", None),
        )
        if errors:
            ctx.validation_errors = errors
        return ctx
```

- [ ] **Step 5: Run test to verify pass**

Run: `python -m unittest tests.test_validate_hook -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add hermes/tasks/hooks/validate_hook.py hermes/tasks/workflow/validator.py tests/test_validate_hook.py
git commit -m "feat(hooks): 4.1 ValidateHook + validate_workflow_spec（工具存在性+step_type 校验）"
```

---

### Task 9: 创建/启动/执行前三个校验时机（Q14 决策）

**Files:**
- Modify: `hermes/routes/cron_tools.py`（create_schedule / update_schedule 端点新增校验）
- Modify: `hermes/tasks/scheduler.py`（`_load_persisted` 新增启动校验）
- Test: `tests/test_validate_hook.py`（扩展，或新建 `tests/test_validation_timing.py`）

**Interfaces:**
- Consumes: Task 8 的 `validate_workflow_spec`
- Produces: 创建/更新时返回 400 + 错误详情；启动时记 WARNING 日志

- [ ] **Step 1: Write the failing test for create_schedule validation**

在 `tests/test_validate_hook.py` 新增测试类（或新建文件）：

```python
class TestValidationTimingCreateSchedule(unittest.TestCase):
    """Q14: 创建调度项时校验，返回 400。"""

    def test_create_schedule_with_invalid_workflow_returns_400(self):
        # 测试 API 端点对无效 workflow 返回 400
        # 具体 mock 取决于现有 API 测试模式
        pass

    def test_load_persisted_invalid_workflow_logs_warning(self):
        """启动加载时校验失败记 WARNING，不阻止启动。"""
        # mock _load_persisted 调用 validate_workflow_spec
        pass
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_validate_hook.TestValidationTimingCreateSchedule -v`
Expected: FAIL

- [ ] **Step 3: Add validation to create_schedule / update_schedule**

在 `hermes/routes/cron_tools.py` 的 `create_schedule` / `update_schedule` 端点中，保存前调用 `validate_workflow_spec`，errors 非空时返回 400：

```python
from hermes.tasks.workflow.validator import validate_workflow_spec

# 在 create_schedule / update_schedule 中
if schedule.workflow:
    errors = validate_workflow_spec(
        spec=schedule.workflow,
        cron_tool_registry=app.state.cron_tool_registry,
        tool_registry=app.state.tool_registry,
    )
    if errors:
        return JSONResponse(
            status_code=400,
            content={"error": "workflow 校验失败", "details": errors},
        )
```

- [ ] **Step 4: Add validation to _load_persisted**

在 `hermes/tasks/scheduler.py` 的 `_load_persisted` 方法中，加载每个 schedule 后校验：

```python
from hermes.tasks.workflow.validator import validate_workflow_spec

# 在 _load_persisted 加载每个 schedule 后
if schedule.workflow:
    errors = validate_workflow_spec(
        spec=schedule.workflow,
        cron_tool_registry=self.cron_tool_registry,
        tool_registry=self.tool_registry,
    )
    if errors:
        logger.warning("调度 %s 的 workflow 校验失败: %s", schedule.id, errors)
```

- [ ] **Step 5: Run test to verify pass**

Run: `python -m unittest tests.test_validate_hook -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add hermes/routes/cron_tools.py hermes/tasks/scheduler.py tests/test_validate_hook.py
git commit -m "feat(validate): 4.1 Q14 创建/启动/执行前三时机校验"
```

---

## 批次 3：容错层（P1，依赖批次 2）

### Task 10: 移除 WorkflowEngine 内 RetryBudget + on_failure.action 简化（Q1/Q3 决策）

**Files:**
- Modify: `hermes/tasks/workflow/engine.py`（移除 RetryBudget 调用 + 末尾 raise WorkflowExecutionError）
- Modify: `hermes/tasks/workflow/spec.py:29-31`（`ALLOWED_ON_FAILURE_ACTIONS` 移除 `"retry"`）
- Test: `tests/test_workflow_engine.py`（更新期望）

**Interfaces:**
- Consumes: Task 6 的 `WorkflowExecutionError`
- Produces: `ALLOWED_ON_FAILURE_ACTIONS = frozenset({"fallback", "skip", "abort"})`；`WorkflowEngine.execute` 末尾 raise `WorkflowExecutionError`

- [ ] **Step 1: Write the failing test**

在 `tests/test_workflow_engine.py` 新增测试类：

```python
class TestWorkflowEngineNoRetryBudget(unittest.TestCase):
    """Q1 决策 C：移除 WorkflowEngine 内的 RetryBudget。"""

    def test_failed_workflow_raises_workflow_execution_error(self):
        """workflow 失败时抛 WorkflowExecutionError。"""
        from hermes.tasks.workflow.engine import WorkflowEngine
        from hermes.agent.tool_error import WorkflowExecutionError
        from unittest.mock import MagicMock

        engine = WorkflowEngine(...)
        # mock step 执行失败
        ...
        with self.assertRaises(WorkflowExecutionError):
            engine.execute(...)


class TestOnFailureActionsNoRetry(unittest.TestCase):
    """Q3: ALLOWED_ON_FAILURE_ACTIONS 移除 retry。"""

    def test_retry_not_in_allowed_actions(self):
        from hermes.tasks.workflow.spec import ALLOWED_ON_FAILURE_ACTIONS
        self.assertNotIn("retry", ALLOWED_ON_FAILURE_ACTIONS)

    def test_allowed_actions_are_three(self):
        from hermes.tasks.workflow.spec import ALLOWED_ON_FAILURE_ACTIONS
        self.assertEqual(ALLOWED_ON_FAILURE_ACTIONS, frozenset({"fallback", "skip", "abort"}))
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_workflow_engine.TestWorkflowEngineNoRetryBudget tests.test_workflow_engine.TestOnFailureActionsNoRetry -v`
Expected: FAIL（`ALLOWED_ON_FAILURE_ACTIONS` 仍含 `"retry"`；engine 不抛 `WorkflowExecutionError`）

- [ ] **Step 3: Remove "retry" from ALLOWED_ON_FAILURE_ACTIONS**

修改 `hermes/tasks/workflow/spec.py`：

```python
ALLOWED_ON_FAILURE_ACTIONS = frozenset({"fallback", "skip", "abort"})  # Q3: 移除 "retry"
```

- [ ] **Step 4: Remove RetryBudget from WorkflowEngine + add raise**

在 `hermes/tasks/workflow/engine.py` 的 `_execute_with_policy` 方法中，移除 RetryBudget 调用，step 失败时直接按 `on_failure.action` 处理。

在 `WorkflowEngine.execute` 末尾新增：

```python
from hermes.agent.tool_error import WorkflowExecutionError

# execute 方法末尾
if not result.success:
    raise WorkflowExecutionError(result)
return result
```

- [ ] **Step 5: Update existing tests that expect RetryBudget behavior**

搜索 `tests/test_workflow_engine.py` 和 `tests/test_retry_budget.py` 中期望 step 级重试的测试，更新为期望 `WorkflowExecutionError` 或移除。

- [ ] **Step 6: Run tests to verify pass**

Run: `python -m unittest tests.test_workflow_engine tests.test_retry_budget -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add hermes/tasks/workflow/engine.py hermes/tasks/workflow/spec.py tests/test_workflow_engine.py tests/test_retry_budget.py
git commit -m "refactor(workflow): Q1/Q3 移除 RetryBudget + on_failure.action 简化为 3 种"
```

---

### Task 11: RetryHook + RetryDecision + _is_permanent_error（Q10 决策）

**Files:**
- Create: `hermes/tasks/hooks/retry_hook.py`
- Test: `tests/test_retry_hook.py`（新建）

**Interfaces:**
- Consumes: Task 5 的 `ScheduleHookBase` / `RetryDecision`；Task 6 的异常类；`WorkflowExecutionError`
- Produces: `RetryHook(ScheduleHookBase)` 类 / `_is_permanent_error(error) -> bool` 函数

- [ ] **Step 1: Write the failing test**

新建 `tests/test_retry_hook.py`：

```python
"""RetryHook + _is_permanent_error 测试（5.2）。"""
from __future__ import annotations

import asyncio
import sys
import unittest
from pathlib import Path
from unittest.mock import MagicMock

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.hooks.base import RetryDecision
from hermes.tasks.hooks.retry_hook import RetryHook, _is_permanent_error
from hermes.agent.tool_error import (
    ToolNotFoundError, ParamError, HookAbortError, ValidationError,
    AuthRequiredError, WorkflowExecutionError,
)


class TestIsPermanentError(unittest.TestCase):
    """_is_permanent_error 错误分类（5.2）。"""

    def test_tool_not_found_is_permanent(self):
        err = ToolNotFoundError(tool_name="t", reason="r", suggestion="s")
        self.assertTrue(_is_permanent_error(err))

    def test_param_error_is_permanent(self):
        err = ParamError(tool_name="t", reason="r", suggestion="s")
        self.assertTrue(_is_permanent_error(err))

    def test_validation_error_is_permanent(self):
        err = ValidationError(errors=["e1"])
        self.assertTrue(_is_permanent_error(err))

    def test_hook_abort_error_is_permanent(self):
        err = HookAbortError(reason="abort")
        self.assertTrue(_is_permanent_error(err))

    def test_auth_required_is_permanent(self):
        """P1 修复：AuthRequiredError 加入顶层 isinstance。"""
        err = AuthRequiredError(tool_name="t", reason="r", suggestion="s")
        self.assertTrue(_is_permanent_error(err))

    def test_generic_exception_is_transient(self):
        """未分类异常默认 transient（给一次机会）。"""
        self.assertFalse(_is_permanent_error(ValueError("x")))

    def test_connection_error_is_transient(self):
        self.assertFalse(_is_permanent_error(ConnectionError("net down")))

    def test_workflow_execution_error_with_permanent_step(self):
        """WorkflowExecutionError 含 permanent step trace 视为 permanent。"""
        mock_trace = MagicMock()
        mock_trace.error_class = "permanent"
        mock_result = MagicMock()
        mock_result.step_traces = [mock_trace]
        err = WorkflowExecutionError(mock_result)
        self.assertTrue(_is_permanent_error(err))

    def test_workflow_execution_error_with_auth_required_step(self):
        """WorkflowExecutionError 含 auth_required step trace 视为 permanent。"""
        mock_trace = MagicMock()
        mock_trace.error_class = "auth_required"
        mock_result = MagicMock()
        mock_result.step_traces = [mock_trace]
        err = WorkflowExecutionError(mock_result)
        self.assertTrue(_is_permanent_error(err))

    def test_workflow_execution_error_with_transient_step(self):
        """WorkflowExecutionError 仅含 transient step trace 视为 transient。"""
        mock_trace = MagicMock()
        mock_trace.error_class = "transient"
        mock_result = MagicMock()
        mock_result.step_traces = [mock_trace]
        err = WorkflowExecutionError(mock_result)
        self.assertFalse(_is_permanent_error(err))


class TestRetryHookOnFailure(unittest.TestCase):
    """RetryHook.on_failure 决策逻辑。"""

    def test_permanent_error_gives_up(self):
        hook = RetryHook(config={"max_retries": 3, "interval_seconds": 60})
        ctx = MagicMock()
        ctx.retry_count = 0
        err = ToolNotFoundError(tool_name="t", reason="r", suggestion="s")
        decision = asyncio.run(hook.on_failure(ctx, err))
        self.assertFalse(decision.retry)
        self.assertIn("permanent", decision.reason)

    def test_transient_error_retries(self):
        hook = RetryHook(config={"max_retries": 3, "interval_seconds": 60})
        ctx = MagicMock()
        ctx.retry_count = 0
        decision = asyncio.run(hook.on_failure(ctx, ValueError("x")))
        self.assertTrue(decision.retry)
        self.assertEqual(decision.delay_seconds, 60)

    def test_max_retries_reached_gives_up(self):
        hook = RetryHook(config={"max_retries": 3, "interval_seconds": 60})
        ctx = MagicMock()
        ctx.retry_count = 3  # 已达上限
        decision = asyncio.run(hook.on_failure(ctx, ValueError("x")))
        self.assertFalse(decision.retry)
        self.assertIn("最大重试次数", decision.reason)

    def test_transient_only_false_retries_permanent(self):
        """transient_only=False 时 permanent 也重试。"""
        hook = RetryHook(config={"max_retries": 3, "interval_seconds": 60, "transient_only": False})
        ctx = MagicMock()
        ctx.retry_count = 0
        err = ToolNotFoundError(tool_name="t", reason="r", suggestion="s")
        decision = asyncio.run(hook.on_failure(ctx, err))
        self.assertTrue(decision.retry)

    def test_default_max_retries_is_3(self):
        hook = RetryHook(config={})
        self.assertEqual(hook.max_retries, 3)

    def test_default_interval_is_300(self):
        hook = RetryHook(config={})
        self.assertEqual(hook.interval_seconds, 300)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_retry_hook -v`
Expected: FAIL（`hermes.tasks.hooks.retry_hook` 模块不存在）

- [ ] **Step 3: Implement RetryHook + _is_permanent_error**

新建 `hermes/tasks/hooks/retry_hook.py`：

```python
"""RetryHook（L3 重试层，5.2）。

Q1 决策 C：RetryHook 是唯一重试层（WorkflowEngine 内的 RetryBudget 已移除）。
Q3 决策：整次重跑（不做 step 级 checkpoint）。
Q10 决策：permanent 错误类型识别 + AuthRequiredError 加入顶层 isinstance。
"""
from __future__ import annotations

import logging
from typing import Any

from .base import ScheduleHookBase, RetryDecision

logger = logging.getLogger(__name__)


def _is_permanent_error(error: Exception) -> bool:
    """判断是否为不可恢复的 permanent 错误。

    permanent 类型（Q10）：
    - ToolNotFoundError / ParamError / ValidationError / HookAbortError / AuthRequiredError
    - WorkflowExecutionError 且 step_traces 含 permanent / auth_required 错误

    transient 类型（可重试）：网络超时 / ConnectionError / SMTP 失败 / 未分类异常（默认 transient）
    """
    from hermes.agent.tool_error import (
        ToolNotFoundError, ParamError, HookAbortError, ValidationError,
        AuthRequiredError, WorkflowExecutionError,
    )
    # P1 修复：AuthRequiredError 加入顶层 isinstance 检查
    if isinstance(error, (ToolNotFoundError, ParamError, ValidationError,
                          HookAbortError, AuthRequiredError)):
        return True
    if isinstance(error, WorkflowExecutionError):
        result = error.result
        for trace in getattr(result, "step_traces", []):
            err_class = getattr(trace, "error_class", None)
            if err_class in ("permanent", "auth_required"):
                return True
    return False


class RetryHook(ScheduleHookBase):
    """L3: 失败后固定间隔重试。permanent 错误不重试。"""

    name = "retry"

    def __init__(self, config: dict):
        self.max_retries = config.get("max_retries", 3)
        self.interval_seconds = config.get("interval_seconds", 300)
        self.transient_only = config.get("transient_only", True)

    async def on_failure(self, ctx: Any, error: Exception) -> RetryDecision:
        if self.transient_only and _is_permanent_error(error):
            return RetryDecision.give_up(
                reason=f"permanent 错误，不重试: {type(error).__name__}: {error}"
            )
        if ctx.retry_count >= self.max_retries:
            return RetryDecision.give_up(
                reason=f"已达最大重试次数 {self.max_retries}"
            )
        return RetryDecision.retry_after(
            seconds=self.interval_seconds,
            reason=(f"transient 错误，第 {ctx.retry_count + 1}/{self.max_retries} "
                    f"次重试: {error}")
        )
```

- [ ] **Step 4: Run test to verify pass**

Run: `python -m unittest tests.test_retry_hook -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add hermes/tasks/hooks/retry_hook.py tests/test_retry_hook.py
git commit -m "feat(hooks): 5.2 RetryHook + _is_permanent_error（含 AuthRequiredError）"
```

---

### Task 12: CatchUpHook + _run_schedule_direct（Q9 决策，spec 5.1）

**Files:**
- Create: `hermes/tasks/hooks/catchup_hook.py`
- Modify: `hermes/tasks/scheduler.py`（新增 `_run_schedule_direct` thin wrapper + 启动时调用 `scan_and_compensate`）
- Test: `tests/test_catchup_hook.py`（新建）

**Interfaces:**
- Consumes: Task 7 的 `_run_schedule` / `_run_schedule_direct`；`CronScheduler._schedules` / `_cron_exprs`
- Produces: `CatchUpHook(ScheduleHookBase)` 类 + `scan_and_compensate(scheduler) -> List[str]` 方法；`Schedule.catch_up_policy` 字段（可选）

- [ ] **Step 1: Write the failing test**

新建 `tests/test_catchup_hook.py`：

```python
"""CatchUpHook 测试（5.1，Q9 串行 await）。"""
from __future__ import annotations

import asyncio
import sys
import unittest
from datetime import datetime, timedelta
from pathlib import Path
from unittest.mock import MagicMock, AsyncMock, patch

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.hooks.catchup_hook import CatchUpHook


class TestCatchUpHookBeforeExecute(unittest.TestCase):
    """before_execute 运行时为空操作（仅启动扫描调用 scan_and_compensate）。"""

    def test_before_execute_returns_ctx_unchanged(self):
        hook = CatchUpHook(config={})
        ctx = MagicMock()
        result = asyncio.run(hook.before_execute(ctx))
        self.assertIs(result, ctx)


class TestCatchUpHookScanAndCompensate(unittest.TestCase):
    """scan_and_compensate 启动时扫描过期调度项。"""

    def _make_scheduler(self, schedules, run_direct_mock=None):
        scheduler = MagicMock()
        scheduler._schedules = schedules
        scheduler._cron_exprs = {}
        scheduler._run_schedule_direct = run_direct_mock or AsyncMock()
        scheduler._persist = MagicMock()
        return scheduler

    def test_skip_policy_does_not_execute(self):
        """catch_up_policy=skip 时不调用 _run_schedule_direct，仅更新 next_run。"""
        hook = CatchUpHook(config={"default_policy": "skip"})
        past_dt = datetime.now() - timedelta(hours=2)
        schedule = MagicMock()
        schedule.id = "s1"
        schedule.enabled = True
        schedule.next_run = past_dt.isoformat()
        schedule.catch_up_policy = "skip"

        cron_expr = MagicMock()
        cron_expr.next_run.return_value = datetime.now() + timedelta(days=1)
        scheduler = self._make_scheduler([schedule])
        scheduler._cron_exprs = {"s1": cron_expr}

        compensated = asyncio.run(hook.scan_and_compensate(scheduler))
        self.assertEqual(compensated, [])
        scheduler._run_schedule_direct.assert_not_awaited()
        scheduler._persist.assert_called_once()

    def test_execute_once_policy_triggers_run(self):
        """catch_up_policy=execute_once 时调用 _run_schedule_direct。"""
        hook = CatchUpHook(config={"default_policy": "skip"})
        past_dt = datetime.now() - timedelta(hours=2)
        schedule = MagicMock()
        schedule.id = "s1"
        schedule.enabled = True
        schedule.next_run = past_dt.isoformat()
        schedule.catch_up_policy = "execute_once"

        cron_expr = MagicMock()
        cron_expr.next_run.return_value = datetime.now() + timedelta(days=1)
        scheduler = self._make_scheduler([schedule], run_direct_mock=AsyncMock())
        scheduler._cron_exprs = {"s1": cron_expr}

        compensated = asyncio.run(hook.scan_and_compensate(scheduler))
        self.assertEqual(compensated, ["s1"])
        scheduler._run_schedule_direct.assert_awaited_once_with(schedule)

    def test_default_policy_fallback_when_schedule_missing_attr(self):
        """schedule 无 catch_up_policy 字段时用 hook.default_policy。"""
        hook = CatchUpHook(config={"default_policy": "execute_once"})
        past_dt = datetime.now() - timedelta(hours=2)
        schedule = MagicMock(spec=["id", "enabled", "next_run"])
        schedule.id = "s1"
        schedule.enabled = True
        schedule.next_run = past_dt.isoformat()

        cron_expr = MagicMock()
        cron_expr.next_run.return_value = datetime.now() + timedelta(days=1)
        scheduler = self._make_scheduler([schedule])
        scheduler._cron_exprs = {"s1": cron_expr}

        compensated = asyncio.run(hook.scan_and_compensate(scheduler))
        self.assertEqual(compensated, ["s1"])

    def test_disabled_schedule_skipped(self):
        """enabled=False 的调度不补偿。"""
        hook = CatchUpHook(config={"default_policy": "execute_once"})
        schedule = MagicMock()
        schedule.id = "s1"
        schedule.enabled = False
        schedule.next_run = (datetime.now() - timedelta(hours=2)).isoformat()
        schedule.catch_up_policy = "execute_once"

        scheduler = self._make_scheduler([schedule])
        compensated = asyncio.run(hook.scan_and_compensate(scheduler))
        self.assertEqual(compensated, [])

    def test_future_next_run_skipped(self):
        """未过期的调度不补偿。"""
        hook = CatchUpHook(config={"default_policy": "execute_once"})
        future_dt = datetime.now() + timedelta(hours=2)
        schedule = MagicMock()
        schedule.id = "s1"
        schedule.enabled = True
        schedule.next_run = future_dt.isoformat()
        schedule.catch_up_policy = "execute_once"

        scheduler = self._make_scheduler([schedule])
        compensated = asyncio.run(hook.scan_and_compensate(scheduler))
        self.assertEqual(compensated, [])

    def test_invalid_next_run_skipped(self):
        """next_run 非 ISO 字符串时跳过（不抛异常）。"""
        hook = CatchUpHook(config={"default_policy": "execute_once"})
        schedule = MagicMock()
        schedule.id = "s1"
        schedule.enabled = True
        schedule.next_run = "not-a-date"
        schedule.catch_up_policy = "execute_once"

        scheduler = self._make_scheduler([schedule])
        compensated = asyncio.run(hook.scan_and_compensate(scheduler))
        self.assertEqual(compensated, [])

    def test_multiple_schedules_serial(self):
        """Q9: 多个过期调度串行 await（非并发）。"""
        hook = CatchUpHook(config={"default_policy": "execute_once"})
        call_order = []

        async def mock_run(sched):
            call_order.append(sched.id)

        past_dt = datetime.now() - timedelta(hours=2)
        schedules = []
        for i in range(3):
            s = MagicMock()
            s.id = f"s{i}"
            s.enabled = True
            s.next_run = past_dt.isoformat()
            s.catch_up_policy = "execute_once"
            schedules.append(s)

        cron_expr = MagicMock()
        cron_expr.next_run.return_value = datetime.now() + timedelta(days=1)
        scheduler = self._make_scheduler(schedules, run_direct_mock=mock_run)
        scheduler._cron_exprs = {f"s{i}": cron_expr for i in range(3)}

        compensated = asyncio.run(hook.scan_and_compensate(scheduler))
        self.assertEqual(compensated, ["s0", "s1", "s2"])
        self.assertEqual(call_order, ["s0", "s1", "s2"])  # 串行顺序


class TestRunScheduleDirect(unittest.TestCase):
    """_run_schedule_direct 是 thin wrapper（spec 5.1 P1 修复）。"""

    def test_delegates_to_run_schedule_with_injected_params(self):
        from hermes.tasks.scheduler import CronScheduler

        scheduler = CronScheduler.__new__(CronScheduler)
        scheduler._orchestrator = MagicMock()
        scheduler._run_schedule = AsyncMock()

        mock_schedule = MagicMock()
        asyncio.run(scheduler._run_schedule_direct(mock_schedule))

        scheduler._run_schedule.assert_awaited_once()
        call_args = scheduler._run_schedule.call_args
        self.assertEqual(len(call_args[0]), 3)
        self.assertIs(call_args[0][0], scheduler._orchestrator)
        self.assertIs(call_args[0][1], mock_schedule)
        self.assertIsInstance(call_args[0][2], datetime)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_catchup_hook -v`
Expected: FAIL（`hermes.tasks.hooks.catchup_hook` 不存在；`_run_schedule_direct` 不存在）

- [ ] **Step 3: Implement _run_schedule_direct in scheduler.py**

在 `hermes/tasks/scheduler.py` 的 `_run_schedule` 方法之后新增（如果 Task 7 未创建，则在此 Task 创建）：

```python
async def _run_schedule_direct(self, schedule: "Schedule") -> None:
    """CatchUpHook 专用的补偿执行入口（spec 5.1）。

    P1 修复：签名与 _run_schedule 不同（仅 schedule 参数），
    内部自动填充 started_at_dt / orchestrator 后委托 _run_schedule。
    """
    started_at_dt = datetime.now()
    orchestrator = self._orchestrator
    await self._run_schedule(orchestrator, schedule, started_at_dt)
```

- [ ] **Step 4: Implement CatchUpHook**

新建 `hermes/tasks/hooks/catchup_hook.py`：

```python
"""CatchUpHook（L2 补偿层，5.1，Q9 决策串行 await）。

启动时扫描过期调度项，按 catch_up_policy 处理：
- skip：跳过（默认）
- execute_once：执行一次补偿

Q9 决策：串行 await 执行补偿任务，避免与 run_loop 触发的执行竞争
cron_tool_registry / archive_callback 覆盖。
"""
from __future__ import annotations

import logging
from datetime import datetime
from typing import Any, List

from .base import ScheduleHookBase

logger = logging.getLogger(__name__)


class CatchUpHook(ScheduleHookBase):
    """L2: 启动时扫描过期调度项，按 catch_up_policy 处理。"""

    name = "catchup"

    def __init__(self, config: dict):
        self.default_policy = config.get("default_policy", "skip")

    async def before_execute(self, ctx: Any) -> Any:
        # 运行时不触发，仅启动扫描调用 scan_and_compensate
        return ctx

    async def scan_and_compensate(self, scheduler) -> List[str]:
        """启动时扫描所有过期调度项，按策略处理。

        返回已补偿执行的 schedule_id 列表（skip 策略的不计入）。
        """
        now = datetime.now()
        compensated: List[str] = []
        for schedule in scheduler._schedules:
            if not schedule.enabled or not schedule.next_run:
                continue
            try:
                next_dt = datetime.fromisoformat(schedule.next_run)
            except (ValueError, TypeError):
                continue
            if next_dt >= now:
                continue  # 未过期

            policy = getattr(schedule, "catch_up_policy", None) or self.default_policy
            if policy == "execute_once":
                logger.info(
                    "补偿执行过期调度 %s (next_run=%s)",
                    schedule.id, schedule.next_run,
                )
                # Q9: 串行 await，非 create_task
                await scheduler._run_schedule_direct(schedule)
                compensated.append(schedule.id)
            # 无论 skip / execute_once，都更新 next_run 到下个周期
            cron_expr = scheduler._cron_exprs.get(schedule.id)
            if cron_expr:
                schedule.next_run = cron_expr.next_run(now).isoformat()
        if compensated:
            scheduler._persist()
        return compensated
```

- [ ] **Step 5: Wire scan_and_compensate into scheduler startup**

在 `hermes/tasks/scheduler.py` 的 `start` 方法（或 `run_loop` 启动前）中，加载 schedules 后调用：

```python
# 在 start() 方法的 run_loop 启动前
catchup_hook = self.hooks.hooks.get("catchup")
if catchup_hook:
    compensated = await catchup_hook.scan_and_compensate(self)
    if compensated:
        logger.info("启动补偿完成，已执行 %d 个过期调度", len(compensated))
```

- [ ] **Step 6: Run test to verify pass**

Run: `python -m unittest tests.test_catchup_hook -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add hermes/tasks/hooks/catchup_hook.py hermes/tasks/scheduler.py tests/test_catchup_hook.py
git commit -m "feat(hooks): 5.1 CatchUpHook + _run_schedule_direct（Q9 串行补偿）"
```

---

### Task 13: StepTrace 新增 retry_reason + RunSummary 新增 3 字段（Q6 决策，spec 5.3）

**Files:**
- Modify: `hermes/tasks/workflow/step_trace.py:40-97`（StepTrace 新增 `retry_reason` 字段 + `from_dict` 同步）
- Modify: `hermes/tasks/run_summary.py:67-80`（RunSummary 新增 3 字段 + `from_dict` 同步 + `read_by_run_id` 方法）
- Modify: `hermes/tasks/scheduler.py`（`_run_schedule` 写入 `retry_reason` / `retry_count`）
- Test: `tests/test_step_trace.py`（扩展）
- Test: `tests/test_run_summary.py`（扩展）
- Test: `tests/test_cron_persist.py`（扩展 `read_by_run_id` 测试）

**Interfaces:**
- Consumes: 现有 StepTrace 13 字段 / RunSummary 14 字段 / RunsJsonlStore.read_recent
- Produces: `StepTrace.retry_reason: str` 字段；`RunSummary.retry_count: int` / `notified: bool` / `notification_channels: List[str]` 字段；`RunsJsonlStore.read_by_run_id(schedule_id, run_id) -> Optional[Dict]` 方法

- [ ] **Step 1: Write the failing test for StepTrace.retry_reason**

在 `tests/test_step_trace.py` 末尾新增：

```python
class TestStepTraceRetryReason(unittest.TestCase):
    """Q6: StepTrace 新增 retry_reason 字段。"""

    def test_retry_reason_defaults_empty(self):
        from hermes.tasks.workflow.step_trace import StepTrace
        trace = StepTrace(step_id="s1")
        self.assertEqual(trace.retry_reason, "")

    def test_retry_reason_roundtrip(self):
        from hermes.tasks.workflow.step_trace import StepTrace
        trace = StepTrace(step_id="s1", retry_reason="已达最大重试次数 3")
        d = trace.to_dict()
        self.assertEqual(d["retry_reason"], "已达最大重试次数 3")
        restored = StepTrace.from_dict(d)
        self.assertEqual(restored.retry_reason, "已达最大重试次数 3")

    def test_from_dict_missing_retry_reason_defaults_empty(self):
        """旧记录无 retry_reason 字段时默认空串（向后兼容）。"""
        from hermes.tasks.workflow.step_trace import StepTrace
        d = {"step_id": "s1"}  # 无 retry_reason
        trace = StepTrace.from_dict(d)
        self.assertEqual(trace.retry_reason, "")
```

- [ ] **Step 2: Write the failing test for RunSummary new fields**

在 `tests/test_run_summary.py` 末尾新增：

```python
class TestRunSummaryNewFields(unittest.TestCase):
    """Q11/Q8: RunSummary 新增 retry_count / notified / notification_channels。"""

    def test_retry_count_defaults_zero(self):
        from hermes.tasks.run_summary import RunSummary
        rs = RunSummary(schedule_id="s1", run_id="r1")
        self.assertEqual(rs.retry_count, 0)

    def test_notified_defaults_false(self):
        from hermes.tasks.run_summary import RunSummary
        rs = RunSummary(schedule_id="s1", run_id="r1")
        self.assertFalse(rs.notified)

    def test_notification_channels_defaults_empty(self):
        from hermes.tasks.run_summary import RunSummary
        rs = RunSummary(schedule_id="s1", run_id="r1")
        self.assertEqual(rs.notification_channels, [])

    def test_new_fields_roundtrip(self):
        from hermes.tasks.run_summary import RunSummary
        rs = RunSummary(
            schedule_id="s1", run_id="r1",
            retry_count=3, notified=True, notification_channels=["email", "webhook"],
        )
        d = rs.to_dict()
        self.assertEqual(d["retry_count"], 3)
        self.assertTrue(d["notified"])
        self.assertEqual(d["notification_channels"], ["email", "webhook"])
        restored = RunSummary.from_dict(d)
        self.assertEqual(restored.retry_count, 3)
        self.assertTrue(restored.notified)
        self.assertEqual(restored.notification_channels, ["email", "webhook"])

    def test_from_dict_missing_new_fields_uses_defaults(self):
        """旧记录无新字段时使用默认值（向后兼容）。"""
        from hermes.tasks.run_summary import RunSummary
        d = {"schedule_id": "s1", "run_id": "r1"}
        rs = RunSummary.from_dict(d)
        self.assertEqual(rs.retry_count, 0)
        self.assertFalse(rs.notified)
        self.assertEqual(rs.notification_channels, [])


class TestRunsJsonlStoreReadByRunId(unittest.TestCase):
    """Q13: RunsJsonlStore.read_by_run_id 查询单次执行记录。"""

    def test_read_by_run_id_returns_dict_when_found(self):
        from hermes.tasks.run_summary import RunSummary, RunsJsonlStore
        import tempfile, os
        with tempfile.TemporaryDirectory() as tmp:
            store = RunsJsonlStore(base_dir=tmp)
            rs = RunSummary(
                schedule_id="s1", run_id="abc123def456",
                started_at="2026-07-17T09:00:00",
                finished_at="2026-07-17T09:01:00",
            )
            store.append(rs)
            result = store.read_by_run_id("s1", "abc123def456")
            self.assertIsNotNone(result)
            self.assertEqual(result["run_id"], "abc123def456")
            self.assertEqual(result["schedule_id"], "s1")

    def test_read_by_run_id_returns_none_when_not_found(self):
        from hermes.tasks.run_summary import RunsJsonlStore
        import tempfile
        with tempfile.TemporaryDirectory() as tmp:
            store = RunsJsonlStore(base_dir=tmp)
            result = store.read_by_run_id("s1", "nonexistent")
            self.assertIsNone(result)
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `python -m unittest tests.test_step_trace.TestStepTraceRetryReason tests.test_run_summary.TestRunSummaryNewFields tests.test_run_summary.TestRunsJsonlStoreReadByRunId -v`
Expected: FAIL（`retry_reason` 字段不存在；`retry_count` 等字段不存在；`read_by_run_id` 方法不存在）

- [ ] **Step 4: Add retry_reason to StepTrace**

修改 `hermes/tasks/workflow/step_trace.py`，在 StepTrace dataclass 的 `files` 字段之后新增：

```python
    # === Q6 新增 ===
    retry_reason: str = ""  # 重试原因（空串=未重试）
```

同步更新 `from_dict` 方法（在末尾新增）：

```python
@classmethod
def from_dict(cls, data):
    if not data:
        return cls()
    return cls(
        # ... 现有 13 个字段保持不变 ...
        retry_reason=str(data.get("retry_reason", "")),  # Q6 新增
    )
```

- [ ] **Step 5: Add 3 fields to RunSummary + read_by_run_id**

修改 `hermes/tasks/run_summary.py`，在 RunSummary dataclass 的 `workflow_name` 字段之后新增：

```python
    # === Q11/Q8 新增 ===
    retry_count: int = 0
    notified: bool = False
    notification_channels: list = field(default_factory=list)
```

同步更新 `from_dict` 方法（缺失字段使用默认值，向后兼容）。

在 `RunsJsonlStore` 类中新增 `read_by_run_id` 方法：

```python
def read_by_run_id(self, schedule_id: str, run_id: str) -> Optional[Dict[str, Any]]:
    """Q13: 按 run_id 查询单次执行记录，返回 dict（便于 API 序列化）。

    从最近 50 条记录中查找（Q13 决策），未找到返回 None。
    """
    recent = self.read_recent(schedule_id, n=50)
    for record in recent:
        if record.run_id == run_id:
            return record.to_dict()
    return None
```

- [ ] **Step 6: Wire retry_reason + retry_count into _run_schedule**

修改 `hermes/tasks/scheduler.py` 的 `_run_schedule` 方法，在 retry 循环 break（give_up）后、`_append_run_summary` 之前：

```python
# 写入 retry_reason 到所有失败 step_traces
if ctx.last_error and wf_result is not None:
    decision_reason = ""  # 从最后一次 RetryDecision 提取
    for trace in wf_result.step_traces:
        if trace.status == "failed":
            trace.retry_reason = decision_reason
```

在构造 RunSummary 时填入 `retry_count=ctx.retry_count`：

```python
run_summary = RunSummary(
    # ... 现有字段 ...
    retry_count=ctx.retry_count,
)
```

- [ ] **Step 7: Run tests to verify pass**

Run: `python -m unittest tests.test_step_trace tests.test_run_summary tests.test_cron_persist -v`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add hermes/tasks/workflow/step_trace.py hermes/tasks/run_summary.py hermes/tasks/scheduler.py tests/test_step_trace.py tests/test_run_summary.py tests/test_cron_persist.py
git commit -m "feat(traces): 5.3 StepTrace+retry_reason / RunSummary+3字段 / read_by_run_id"
```

---

## 批次 4：通知层与可视化（P2，依赖批次 3）

### Task 14: NotifyHook + 邮件/Webhook 双通道（Q7/Q8/Q11 决策，spec 6.1）

**Files:**
- Create: `hermes/tasks/hooks/notify_hook.py`
- Create: `hermes/tasks/hooks/notify_templates.py`
- Test: `tests/test_notify_hook.py`（新建）

**Interfaces:**
- Consumes: Task 5 的 `ScheduleHookBase`；Task 7 的 `CronScheduler._failure_counts`；Task 13 的 `RunSummary.notified` / `notification_channels` 字段
- Produces: `NotifyHook(ScheduleHookBase)` 类 + `_send_email` / `_send_webhook` 方法

- [ ] **Step 1: Write the failing test**

新建 `tests/test_notify_hook.py`：

```python
"""NotifyHook 测试（6.1，Q7/Q8/Q11）。"""
from __future__ import annotations

import asyncio
import sys
import unittest
from pathlib import Path
from unittest.mock import MagicMock, AsyncMock, patch

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()

from hermes.tasks.hooks.notify_hook import NotifyHook


class TestNotifyHookFailureCount(unittest.TestCase):
    """Q8: _failure_counts 写入 CronScheduler 实例（非 HookRegistry）。"""

    def _make_scheduler_ref(self):
        scheduler = MagicMock()
        scheduler._failure_counts = {}
        return scheduler

    def test_failure_count_increments_on_failure(self):
        scheduler = self._make_scheduler_ref()
        hook = NotifyHook(config={}, scheduler_ref=scheduler)
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        result = MagicMock(success=False)

        asyncio.run(hook.after_execute(ctx, result))
        self.assertEqual(scheduler._failure_counts["s1"], 1)

    def test_failure_count_resets_on_success(self):
        scheduler = self._make_scheduler_ref()
        scheduler._failure_counts["s1"] = 2
        hook = NotifyHook(config={}, scheduler_ref=scheduler)
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        result = MagicMock(success=True)

        asyncio.run(hook.after_execute(ctx, result))
        self.assertNotIn("s1", scheduler._failure_counts)

    def test_failure_count_zero_removes_key(self):
        """count=0 时从 _failure_counts 移除 key（不留 0 值）。"""
        scheduler = self._make_scheduler_ref()
        scheduler._failure_counts["s1"] = 1
        hook = NotifyHook(config={}, scheduler_ref=scheduler)
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        result = MagicMock(success=True)

        asyncio.run(hook.after_execute(ctx, result))
        self.assertNotIn("s1", scheduler._failure_counts)


class TestNotifyHookAutoDisable(unittest.TestCase):
    """连续失败达阈值 → 自动 disable。"""

    def test_threshold_reached_disables_schedule(self):
        scheduler = MagicMock()
        scheduler._failure_counts = {}
        hook = NotifyHook(
            config={"consecutive_failures_threshold": 3},
            scheduler_ref=scheduler,
        )
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        ctx.schedule.enabled = True
        result = MagicMock(success=False)

        # 模拟前 2 次失败
        scheduler._failure_counts["s1"] = 2
        asyncio.run(hook.after_execute(ctx, result))
        self.assertFalse(ctx.schedule.enabled)

    def test_default_threshold_is_3(self):
        hook = NotifyHook(config={}, scheduler_ref=MagicMock())
        self.assertEqual(hook.threshold, 3)


class TestNotifyHookChannels(unittest.TestCase):
    """邮件 + Webhook 双通道。"""

    def test_email_disabled_not_sent(self):
        scheduler = MagicMock()
        scheduler._failure_counts = {}
        hook = NotifyHook(
            config={"channels": {"email": {"enabled": False}}},
            scheduler_ref=scheduler,
        )
        hook._send_email = AsyncMock()
        hook._send_webhook = AsyncMock()
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        result = MagicMock(success=False)

        asyncio.run(hook.after_execute(ctx, result))
        hook._send_email.assert_not_awaited()

    def test_email_enabled_sent(self):
        scheduler = MagicMock()
        scheduler._failure_counts = {}
        hook = NotifyHook(
            config={"channels": {"email": {"enabled": True}}},
            scheduler_ref=scheduler,
        )
        hook._send_email = AsyncMock()
        hook._send_webhook = AsyncMock()
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        ctx.retry_count = 0
        ctx.retry_max = 3
        result = MagicMock(success=False)

        asyncio.run(hook.after_execute(ctx, result))
        hook._send_email.assert_awaited_once()

    def test_both_channels_sent_in_parallel(self):
        scheduler = MagicMock()
        scheduler._failure_counts = {}
        hook = NotifyHook(
            config={"channels": {
                "email": {"enabled": True},
                "webhook": {"enabled": True},
            }},
            scheduler_ref=scheduler,
        )
        hook._send_email = AsyncMock()
        hook._send_webhook = AsyncMock()
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        ctx.retry_count = 0
        ctx.retry_max = 3
        result = MagicMock(success=False)

        asyncio.run(hook.after_execute(ctx, result))
        hook._send_email.assert_awaited_once()
        hook._send_webhook.assert_awaited_once()

    def test_channel_failure_does_not_propagate(self):
        """单个通道失败不影响其他通道与主流程。"""
        scheduler = MagicMock()
        scheduler._failure_counts = {}
        hook = NotifyHook(
            config={"channels": {"email": {"enabled": True}}},
            scheduler_ref=scheduler,
        )
        hook._send_email = AsyncMock(side_effect=Exception("SMTP down"))
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        ctx.retry_count = 0
        ctx.retry_max = 3
        result = MagicMock(success=False)

        # 不应抛异常
        asyncio.run(hook.after_execute(ctx, result))


class TestNotifyHookNotifiedFields(unittest.TestCase):
    """RunSummary.notified / notification_channels 由 NotifyHook 写入。"""

    def test_notified_set_true_on_send(self):
        scheduler = MagicMock()
        scheduler._failure_counts = {}
        hook = NotifyHook(
            config={"channels": {"email": {"enabled": True}}},
            scheduler_ref=scheduler,
        )
        hook._send_email = AsyncMock()
        ctx = MagicMock()
        ctx.schedule.id = "s1"
        ctx.retry_count = 0
        ctx.retry_max = 3
        result = MagicMock(success=False)
        result.notified = False
        result.notification_channels = []

        asyncio.run(hook.after_execute(ctx, result))
        self.assertTrue(result.notified)
        self.assertIn("email", result.notification_channels)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_notify_hook -v`
Expected: FAIL（`hermes.tasks.hooks.notify_hook` 模块不存在）

- [ ] **Step 3: Implement notify_templates.py**

新建 `hermes/tasks/hooks/notify_templates.py`：

```python
"""通知模板渲染（6.3，Q11 max_retries 从 ctx.retry_max 读取）。"""
from __future__ import annotations

from typing import Any, Dict


def render_email_html(ctx: Any, result: Any, consecutive_failures: int) -> str:
    """渲染邮件 HTML 模板。"""
    schedule = ctx.schedule
    schedule_name = getattr(schedule, "name", schedule.id)
    started_at = getattr(ctx, "started_at", "") or ""
    duration = getattr(result, "duration_seconds", 0) or 0
    retry_count = getattr(ctx, "retry_count", 0)
    retry_max = getattr(ctx, "retry_max", 0)
    error_summary = "; ".join(result.errors) if result.errors else "未知错误"

    step_rows = []
    for trace in getattr(result, "step_traces", []):
        step_rows.append(
            f"<tr><td>{trace.step_name}</td><td>{trace.status}</td>"
            f"<td>{trace.error_message or ''}</td>"
            f"<td>{trace.retry_reason or ''}</td></tr>"
        )
    step_traces_html = (
        "<table border='1'><tr><th>Step</th><th>状态</th>"
        "<th>错误</th><th>重试原因</th></tr>"
        + "".join(step_rows) + "</table>"
        if step_rows else "<p>无 step 轨迹</p>"
    )

    return f"""
<h2>调度任务失败: {schedule_name}</h2>
<hr>
<table>
  <tr><td>调度ID</td><td>{schedule.id}</td></tr>
  <tr><td>执行时间</td><td>{started_at}</td></tr>
  <tr><td>耗时</td><td>{duration}秒</td></tr>
  <tr><td>重试次数</td><td>{retry_count}/{retry_max}</td></tr>
  <tr><td>连续失败</td><td>{consecutive_failures}次</td></tr>
</table>
<h3>错误详情</h3>
<pre>{error_summary}</pre>
<h3>Step 轨迹</h3>
{step_traces_html}
"""


def render_webhook_payload(ctx: Any, result: Any, consecutive_failures: int) -> Dict[str, Any]:
    """渲染 Webhook JSON payload（兼容企业微信/钉钉/飞书 markdown 格式）。"""
    schedule = ctx.schedule
    schedule_name = getattr(schedule, "name", schedule.id)
    started_at = getattr(ctx, "started_at", "") or ""
    retry_count = getattr(ctx, "retry_count", 0)
    retry_max = getattr(ctx, "retry_max", 0)
    error_summary = "; ".join(result.errors) if result.errors else "未知错误"

    text = (
        f"## 调度任务失败\n"
        f"> **调度**: {schedule_name}\n"
        f"> **时间**: {started_at}\n"
        f"> **重试**: {retry_count}/{retry_max}\n"
        f"> **连续失败**: {consecutive_failures}次\n\n"
        f"**错误**:\n```\n{error_summary}\n```"
    )
    return {
        "msgtype": "markdown",
        "markdown": {"title": f"调度失败: {schedule_name}", "text": text},
    }
```

- [ ] **Step 4: Implement NotifyHook**

新建 `hermes/tasks/hooks/notify_hook.py`：

```python
"""NotifyHook（L4 通知层，6.1）。

Q7 决策：result 类型为 WorkflowResult（复用现有，不引入 RunResult）。
Q8 决策：_failure_counts 放在 CronScheduler 上（配置热更新零迁移）。
Q11 决策：max_retries 从 ctx.retry_max 读取（由 HookRegistry.get_retry_max() 填充）。
"""
from __future__ import annotations

import asyncio
import logging
from typing import Any, Optional

from .base import ScheduleHookBase

logger = logging.getLogger(__name__)


class NotifyHook(ScheduleHookBase):
    """L4: 失败后邮件 + Webhook 双通道通知。连续失败自动 disable。"""

    name = "notify"

    def __init__(self, config: dict, scheduler_ref: Optional[Any] = None):
        self.threshold = config.get("consecutive_failures_threshold", 3)
        self.channels = config.get("channels", {}) or {}
        self._scheduler_ref = scheduler_ref

    def _get_failure_count(self, schedule_id: str) -> int:
        scheduler = self._scheduler_ref
        if scheduler is None:
            return 0
        return scheduler._failure_counts.get(schedule_id, 0)

    def _set_failure_count(self, schedule_id: str, count: int) -> None:
        scheduler = self._scheduler_ref
        if scheduler is None:
            return
        if count == 0:
            scheduler._failure_counts.pop(schedule_id, None)
        else:
            scheduler._failure_counts[schedule_id] = count

    async def after_execute(self, ctx: Any, result: Any) -> None:
        if result.success:
            self._set_failure_count(ctx.schedule.id, 0)
            return

        count = self._get_failure_count(ctx.schedule.id) + 1
        self._set_failure_count(ctx.schedule.id, count)

        # 发送通知（邮件 + Webhook 并行）
        sent_channels = []
        tasks = []
        if self.channels.get("email", {}).get("enabled"):
            tasks.append(self._send_email(ctx, result, count))
            sent_channels.append("email")
        if self.channels.get("webhook", {}).get("enabled"):
            tasks.append(self._send_webhook(ctx, result, count))
            sent_channels.append("webhook")
        if tasks:
            results = await asyncio.gather(*tasks, return_exceptions=True)
            successful = []
            for ch, r in zip(sent_channels, results):
                if isinstance(r, Exception):
                    logger.warning("通知渠道 %s 发送失败: %s", ch, r)
                else:
                    successful.append(ch)
            # 写入 RunSummary 字段（Task 13 新增）
            if successful:
                result.notified = True
                result.notification_channels = successful

        # 连续失败达阈值 → 自动 disable
        if count >= self.threshold:
            logger.warning(
                "调度 %s 连续失败 %d 次，自动 disable",
                ctx.schedule.id, count,
            )
            ctx.schedule.enabled = False

    async def _send_email(self, ctx: Any, result: Any, consecutive_failures: int) -> None:
        """发送邮件通知。SMTP 配置从 config 读取（6.2）。"""
        from .notify_templates import render_email_html
        email_cfg = self.channels.get("email", {})
        html = render_email_html(ctx, result, consecutive_failures)
        await self._smtp_send(email_cfg, html)

    async def _send_webhook(self, ctx: Any, result: Any, consecutive_failures: int) -> None:
        """发送 Webhook 通知。"""
        from .notify_templates import render_webhook_payload
        import aiohttp
        webhook_cfg = self.channels.get("webhook", {})
        url = webhook_cfg.get("url", "")
        if not url:
            return
        payload = render_webhook_payload(ctx, result, consecutive_failures)
        async with aiohttp.ClientSession() as session:
            async with session.post(url, json=payload, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                resp.raise_for_status()

    async def _smtp_send(self, email_cfg: dict, html: str) -> None:
        """使用 aiosmtplib 异步发送邮件（6.2）。"""
        import aiosmtplib
        from email.mime.text import MIMEText
        from email.mime.multipart import MIMEMultipart

        msg = MIMEMultipart("alternative")
        msg["Subject"] = f"调度任务失败通知"
        msg["From"] = email_cfg.get("sender_email", "")
        msg["To"] = email_cfg.get("receiver_email", "")

        msg.attach(MIMEText(html, "html", "utf-8"))

        await aiosmtplib.send(
            msg,
            hostname=email_cfg.get("smtp_host", ""),
            port=int(email_cfg.get("smtp_port", 465)),
            username=email_cfg.get("sender_email", ""),
            password=email_cfg.get("sender_password", ""),
            use_tls=email_cfg.get("use_tls", True),
        )
```

- [ ] **Step 5: Run test to verify pass**

Run: `python -m unittest tests.test_notify_hook -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add hermes/tasks/hooks/notify_hook.py hermes/tasks/hooks/notify_templates.py tests/test_notify_hook.py
git commit -m "feat(hooks): 6.1 NotifyHook + 邮件/Webhook 双通道（Q7/Q8/Q11）"
```

---

### Task 15: SMTP 配置提取到 config + .env（spec 6.2）

**Files:**
- Modify: `cron_tool/blog_monitor_joyehuang/run.py`（SMTP 从环境变量读取）
- Modify: `config.yaml.example`（新增 `cron.hooks.notify.channels.email` 配置段）
- Modify: `hermes-lite/.env.example`（新增 SMTP_* 环境变量占位）
- Test: `tests/test_blog_monitor_smtp_env.py`（新建）

**Interfaces:**
- Consumes: 现有 `cron_tool/blog_monitor_joyehuang/run.py` 中硬编码的 SMTP 配置
- Produces: SMTP 配置从 `${ENV_VAR}` 占位符解析；`.env` 文件管理密钥

- [ ] **Step 1: Write the failing test**

新建 `tests/test_blog_monitor_smtp_env.py`：

```python
"""SMTP 配置环境变量提取测试（6.2）。"""
from __future__ import annotations

import os
import sys
import unittest
from pathlib import Path
from unittest.mock import patch

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()


class TestBlogMonitorSmtpFromEnv(unittest.TestCase):
    """blog_monitor_joyehuang 的 SMTP 配置从环境变量读取。"""

    def test_smtp_host_from_env(self):
        os.environ["SMTP_HOST"] = "smtp.test.com"
        try:
            import importlib
            from cron_tool.blog_monitor_joyehuang import run as blog_run
            if hasattr(blog_run, "get_smtp_config"):
                config = blog_run.get_smtp_config()
                self.assertEqual(config["smtp_host"], "smtp.test.com")
        finally:
            del os.environ["SMTP_HOST"]

    def test_smtp_password_from_env(self):
        os.environ["SMTP_PASSWORD"] = "test_password"
        try:
            from cron_tool.blog_monitor_joyehuang import run as blog_run
            if hasattr(blog_run, "get_smtp_config"):
                config = blog_run.get_smtp_config()
                self.assertEqual(config["sender_password"], "test_password")
        finally:
            del os.environ["SMTP_PASSWORD"]

    def test_default_smtp_host_when_env_missing(self):
        """环境变量缺失时使用默认值（不抛异常）。"""
        os.environ.pop("SMTP_HOST", None)
        from cron_tool.blog_monitor_joyehuang import run as blog_run
        if hasattr(blog_run, "get_smtp_config"):
            config = blog_run.get_smtp_config()
            self.assertTrue(config["smtp_host"])


class TestConfigYamlExampleHasSmtpSection(unittest.TestCase):
    """config.yaml.example 包含 cron.hooks.notify.channels.email 段。"""

    def test_config_example_has_smtp_placeholders(self):
        config_path = Path(_PROJECT_ROOT) / "config.yaml.example"
        if not config_path.exists():
            self.skipTest("config.yaml.example 不存在")
        content = config_path.read_text(encoding="utf-8")
        self.assertIn("cron:", content)
        self.assertIn("hooks:", content)
        self.assertIn("notify:", content)
        self.assertIn("email:", content)
        self.assertIn("${SMTP_HOST}", content)
        self.assertIn("${SMTP_PASSWORD}", content)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_blog_monitor_smtp_env -v`
Expected: FAIL（`run.py` 无 `get_smtp_config` 函数；config.yaml.example 无 SMTP 段）

- [ ] **Step 3: Refactor blog_monitor run.py to read SMTP from env**

修改 `cron_tool/blog_monitor_joyehuang/run.py`，提取 SMTP 配置到 `get_smtp_config()` 函数：

```python
def get_smtp_config() -> dict:
    """从环境变量读取 SMTP 配置（6.2）。

    缺失时使用默认值（项目记忆约束：API 密钥不硬编码）。
    """
    return {
        "smtp_host": os.environ.get("SMTP_HOST", "smtp.qq.com"),
        "smtp_port": int(os.environ.get("SMTP_PORT", "465")),
        "sender_email": os.environ.get("SMTP_USER", ""),
        "sender_password": os.environ.get("SMTP_PASSWORD", ""),
        "receiver_email": os.environ.get("NOTIFY_EMAIL", os.environ.get("SMTP_USER", "")),
        "use_tls": True,
    }


# 原 main() 中硬编码的 SMTP_HOST / SENDER_EMAIL 等改为调用 get_smtp_config()
SMTP_CONFIG = get_smtp_config()
SMTP_HOST = SMTP_CONFIG["smtp_host"]
SMTP_PORT = SMTP_CONFIG["smtp_port"]
SENDER_EMAIL = SMTP_CONFIG["sender_email"]
SENDER_PASSWORD = SMTP_CONFIG["sender_password"]
RECEIVER_EMAIL = SMTP_CONFIG["receiver_email"]
```

- [ ] **Step 4: Add SMTP section to config.yaml.example**

在 `config.yaml.example` 的 `cron:` 段下新增：

```yaml
cron:
  inject_history: true
  max_concurrency: 10
  hooks:
    validate:
      enabled: true
    catchup:
      enabled: true
      default_policy: skip
    retry:
      enabled: true
      max_retries: 3
      interval_seconds: 300
      transient_only: true
    notify:
      enabled: true
      consecutive_failures_threshold: 3
      channels:
        email:
          enabled: true
          smtp_host: ${SMTP_HOST}
          smtp_port: ${SMTP_PORT}
          sender_email: ${SMTP_USER}
          sender_password: ${SMTP_PASSWORD}
          receiver_email: ${NOTIFY_EMAIL}
          use_tls: true
        webhook:
          enabled: false
          url: ${WEBHOOK_URL}
```

- [ ] **Step 5: Add SMTP env vars to .env.example**

新增 `hermes-lite/.env.example`（如已存在则追加）：

```bash
# SMTP 配置（通知层使用，6.2）
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASSWORD=your_auth_code
NOTIFY_EMAIL=your_email@qq.com
WEBHOOK_URL=
```

- [ ] **Step 6: Run test to verify pass**

Run: `python -m unittest tests.test_blog_monitor_smtp_env -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add cron_tool/blog_monitor_joyehuang/run.py config.yaml.example .env.example tests/test_blog_monitor_smtp_env.py
git commit -m "feat(config): 6.2 SMTP 配置提取到 config + .env，移除硬编码"
```

---

### Task 16: API 端点（spec 7.3，Q13 决策）

**Files:**
- Modify: `hermes/routes/cron_tools.py`（新增 5 个 API 端点）
- Test: `tests/test_cron_api_endpoints.py`（新建）

**Interfaces:**
- Consumes: Task 13 的 `RunsJsonlStore.read_by_run_id` / `read_recent` / `read_recent_all`
- Produces: 5 个 API 端点（执行历史 / 手动重跑 / 单次详情 / 最近记录 / 统计）

- [ ] **Step 1: Write the failing test**

新建 `tests/test_cron_api_endpoints.py`：

```python
"""cron API 端点测试（7.3）。"""
from __future__ import annotations

import sys
import unittest
from pathlib import Path
from unittest.mock import MagicMock, patch, AsyncMock

_PROJECT_ROOT = str(Path(__file__).resolve().parent.parent)
if _PROJECT_ROOT not in sys.path:
    sys.path.insert(0, _PROJECT_ROOT)

from tests._mock_deps import install_mocks
install_mocks()


class TestCronRunsApiEndpoints(unittest.TestCase):
    """5 个 API 端点：执行历史 / 手动重跑 / 单次详情 / 最近记录 / 统计。"""

    def setUp(self):
        from fastapi.testclient import TestClient
        self.mock_app = MagicMock()
        self.mock_app.state.cron_scheduler = MagicMock()
        self.mock_app.state.cron_scheduler.runs_store = MagicMock()

    def test_get_schedule_runs_returns_list(self):
        """GET /cron_tools/schedules/{id}/runs 返回执行历史列表。"""
        from hermes.tasks.run_summary import RunSummary
        mock_runs = [
            RunSummary(schedule_id="s1", run_id="r1", success=True),
            RunSummary(schedule_id="s1", run_id="r2", success=False),
        ]
        self.mock_app.state.cron_scheduler.runs_store.read_recent.return_value = mock_runs

        from hermes.routes.cron_tools import get_schedule_runs
        response = get_schedule_runs("s1", limit=20, request=self._make_request())
        self.assertEqual(response.status_code, 200)

    def test_get_run_by_id_returns_detail(self):
        """GET /cron_tools/schedules/{id}/runs/{run_id} 返回单次详情。"""
        from hermes.tasks.run_summary import RunSummary
        mock_run = RunSummary(schedule_id="s1", run_id="r1", success=False)
        mock_dict = mock_run.to_dict()
        self.mock_app.state.cron_scheduler.runs_store.read_by_run_id.return_value = mock_dict

        from hermes.routes.cron_tools import get_run_detail
        response = get_run_detail("s1", "r1", request=self._make_request())
        self.assertEqual(response.status_code, 200)

    def test_get_run_by_id_not_found_returns_404(self):
        """run_id 不存在时返回 404。"""
        self.mock_app.state.cron_scheduler.runs_store.read_by_run_id.return_value = None

        from hermes.routes.cron_tools import get_run_detail
        response = get_run_detail("s1", "nonexistent", request=self._make_request())
        self.assertEqual(response.status_code, 404)

    def test_trigger_run_returns_run_id(self):
        """POST /cron_tools/schedules/{id}/run 异步触发，返回 run_id。"""
        from hermes.routes.cron_tools import trigger_schedule_run
        response = trigger_schedule_run("s1", request=self._make_request())
        self.assertEqual(response.status_code, 202)

    def test_get_recent_runs_returns_all_schedules(self):
        """GET /cron_tools/runs/recent 返回所有调度最近记录。"""
        from hermes.routes.cron_tools import get_recent_runs
        self.mock_app.state.cron_scheduler.runs_store.read_recent_all.return_value = []
        response = get_recent_runs(limit=50, request=self._make_request())
        self.assertEqual(response.status_code, 200)

    def test_get_run_stats_returns_summary(self):
        """GET /cron_tools/runs/stats 返回今日统计。"""
        from hermes.routes.cron_tools import get_run_stats
        self.mock_app.state.cron_scheduler.runs_store.read_recent_all.return_value = []
        response = get_run_stats(request=self._make_request())
        self.assertEqual(response.status_code, 200)

    def _make_request(self):
        request = MagicMock()
        request.app = self.mock_app
        return request


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m unittest tests.test_cron_api_endpoints -v`
Expected: FAIL（5 个端点函数不存在）

- [ ] **Step 3: Implement 5 API endpoints**

在 `hermes/routes/cron_tools.py` 新增 5 个端点函数：

```python
from fastapi import APIRouter, HTTPException, Request
from fastapi.responses import JSONResponse
from datetime import datetime, date

@router.get("/schedules/{schedule_id}/runs")
async def get_schedule_runs(schedule_id: str, limit: int = 20, request: Request = None):
    """获取指定调度的执行历史。"""
    scheduler = request.app.state.cron_scheduler
    if scheduler is None or scheduler.runs_store is None:
        return JSONResponse(status_code=503, content={"error": "调度器未就绪"})
    runs = scheduler.runs_store.read_recent(schedule_id, n=limit)
    return {"runs": [r.to_dict() for r in runs]}


@router.get("/schedules/{schedule_id}/runs/{run_id}")
async def get_run_detail(schedule_id: str, run_id: str, request: Request = None):
    """获取单次执行详情（含完整 StepTrace）。"""
    scheduler = request.app.state.cron_scheduler
    if scheduler is None or scheduler.runs_store is None:
        return JSONResponse(status_code=503, content={"error": "调度器未就绪"})
    detail = scheduler.runs_store.read_by_run_id(schedule_id, run_id)
    if detail is None:
        raise HTTPException(status_code=404, detail=f"run_id {run_id} 不存在")
    return detail


@router.post("/schedules/{schedule_id}/run", status_code=202)
async def trigger_schedule_run(schedule_id: str, request: Request = None):
    """手动触发调度执行（异步，返回 run_id）。"""
    scheduler = request.app.state.cron_scheduler
    if scheduler is None:
        return JSONResponse(status_code=503, content={"error": "调度器未就绪"})
    schedule = next((s for s in scheduler._schedules if s.id == schedule_id), None)
    if schedule is None:
        raise HTTPException(status_code=404, detail=f"调度 {schedule_id} 不存在")
    import asyncio
    asyncio.create_task(scheduler._run_schedule_direct(schedule))
    return {"status": "triggered", "schedule_id": schedule_id}


@router.get("/runs/recent")
async def get_recent_runs(limit: int = 50, request: Request = None):
    """获取最近所有调度的执行记录（monitor 页用）。"""
    scheduler = request.app.state.cron_scheduler
    if scheduler is None or scheduler.runs_store is None:
        return JSONResponse(status_code=503, content={"error": "调度器未就绪"})
    runs = scheduler.runs_store.read_recent_all(n=limit)
    return {"runs": runs}


@router.get("/runs/stats")
async def get_run_stats(request: Request = None):
    """获取执行统计（今日成功/失败/耗时）。"""
    scheduler = request.app.state.cron_scheduler
    if scheduler is None or scheduler.runs_store is None:
        return JSONResponse(status_code=503, content={"error": "调度器未就绪"})
    today_str = date.today().isoformat()
    all_runs = scheduler.runs_store.read_recent_all(n=500)
    today_runs = [r for r in all_runs if str(r.get("started_at", "")).startswith(today_str)]
    success_count = sum(1 for r in today_runs if r.get("success"))
    failure_count = len(today_runs) - success_count
    total_duration = sum(r.get("duration_seconds", 0) for r in today_runs)
    return {
        "today_success": success_count,
        "today_failure": failure_count,
        "total_duration_seconds": total_duration,
        "today_total": len(today_runs),
    }
```

- [ ] **Step 4: Run test to verify pass**

Run: `python -m unittest tests.test_cron_api_endpoints -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add hermes/routes/cron_tools.py tests/test_cron_api_endpoints.py
git commit -m "feat(api): 7.3 cron 执行历史/重跑/详情/最近/统计 5 个端点"
```

---

### Task 17: scheduler.html 执行历史面板（spec 7.1）

**Files:**
- Modify: `web/scheduler.html`（执行历史面板 + 手动重跑按钮 + 配置编辑器 + 状态徽章）
- Modify: `web/static/js/scheduler.js`（前端逻辑）
- Test: 手动浏览器验证（无自动化测试）

**Interfaces:**
- Consumes: Task 16 的 5 个 API 端点
- Produces: 调度列表页增强 UI

- [ ] **Step 1: Add status badges to schedule list**

在 `web/scheduler.html` 的调度列表项模板中新增状态徽章：

```html
<span class="status-badge" data-status="success">🟢 正常</span>
<span class="status-badge" data-status="failed">🔴 失败 ({{ consecutive_failures }})</span>
<span class="status-badge" data-status="running">⏳ 运行中</span>
<span class="status-badge" data-status="disabled">⚫ 已禁用</span>
```

CSS（毛玻璃按钮 + 圆角优化，遵循项目记忆 UI 偏好）：

```css
.status-badge {
  display: inline-block;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 12px;
  backdrop-filter: blur(8px);
  background: rgba(255, 255, 255, 0.6);
}
.status-badge[data-status="failed"] { background: rgba(255, 100, 100, 0.2); }
.status-badge[data-status="success"] { background: rgba(100, 255, 100, 0.2); }
```

- [ ] **Step 2: Add execution history panel**

在调度项下方新增可折叠的执行历史面板：

```html
<div class="execution-history" data-schedule-id="{{ schedule.id }}" hidden>
  <h4>执行历史（最近 20 次）</h4>
  <table class="history-table">
    <thead>
      <tr><th>时间</th><th>状态</th><th>耗时</th><th>重试</th><th>错误</th><th>详情</th></tr>
    </thead>
    <tbody></tbody>
  </table>
</div>
```

- [ ] **Step 3: Add manual rerun button**

每条调度项右侧加"立即执行"按钮：

```html
<button class="rerun-btn" data-schedule-id="{{ schedule.id }}">立即执行</button>
```

- [ ] **Step 4: Add config editor modal**

新增配置编辑模态框（catch_up_policy / on_failure.action 下拉）：

```html
<div class="modal" id="config-editor-modal" hidden>
  <div class="modal-content">
    <h3>编辑调度配置</h3>
    <label>catch_up_policy:
      <select name="catch_up_policy">
        <option value="skip">skip</option>
        <option value="execute_once">execute_once</option>
      </select>
    </label>
    <label>on_failure.action:
      <select name="on_failure_action">
        <option value="skip">skip</option>
        <option value="fallback">fallback</option>
        <option value="abort">abort</option>
      </select>
    </label>
    <button class="save-btn">保存</button>
    <button class="cancel-btn">取消</button>
  </div>
</div>
```

**注意**：Q3 决策移除了 `retry` 选项，下拉只显示 3 种。

- [ ] **Step 5: Implement frontend logic in scheduler.js**

在 `web/static/js/scheduler.js` 新增：

```javascript
// 加载执行历史
async function loadExecutionHistory(scheduleId) {
  const resp = await fetch(`/cron_tools/schedules/${scheduleId}/runs?limit=20`);
  const data = await resp.json();
  const tbody = document.querySelector(`.execution-history[data-schedule-id="${scheduleId}"] tbody`);
  tbody.innerHTML = data.runs.map(r => `
    <tr>
      <td>${r.started_at}</td>
      <td>${r.success ? '🟢 成功' : '🔴 失败'}</td>
      <td>${r.duration_seconds}s</td>
      <td>${r.retry_count}</td>
      <td>${r.errors ? r.errors.join('; ') : ''}</td>
      <td><a href="#" onclick="showRunDetail('${scheduleId}', '${r.run_id}')">查看</a></td>
    </tr>
  `).join('');
}

// 手动重跑
async function triggerRerun(scheduleId) {
  const resp = await fetch(`/cron_tools/schedules/${scheduleId}/run`, { method: 'POST' });
  if (resp.status === 202) {
    alert('已触发执行');
  }
}

// 显示单次详情
async function showRunDetail(scheduleId, runId) {
  const resp = await fetch(`/cron_tools/schedules/${scheduleId}/runs/${runId}`);
  const detail = await resp.json();
  // 渲染详情模态框（含完整 StepTrace）
}

// 保存配置
async function saveConfig(scheduleId, config) {
  const resp = await fetch(`/cron_tools/schedules/${scheduleId}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(config),
  });
}
```

- [ ] **Step 6: Manual browser verification**

启动 sidecar，访问 `http://127.0.0.1:18394/scheduler.html`，验证：
1. 调度列表显示状态徽章
2. 点击调度项展开执行历史
3. "立即执行"按钮触发重跑
4. 编辑配置模态框可保存

- [ ] **Step 7: Commit**

```bash
git add web/scheduler.html web/static/js/scheduler.js
git commit -m "feat(web): 7.1 scheduler.html 执行历史面板+重跑+编辑器+徽章"
```

---

### Task 18: monitor.html 实时状态（spec 7.2）

**Files:**
- Modify: `web/monitor.html`（实时执行状态 + 连续失败告警卡片 + 今日统计）
- Modify: `web/static/js/monitor.js`（前端逻辑）
- Test: 手动浏览器验证

**Interfaces:**
- Consumes: Task 16 的 `/cron_tools/runs/recent` / `/cron_tools/runs/stats` 端点；`CronScheduler._failure_counts`（通过统计端点间接读取）
- Produces: 监控页调度面板

- [ ] **Step 1: Add real-time execution status panel**

在 `web/monitor.html` 新增正在执行任务的实时面板：

```html
<section class="running-tasks-panel">
  <h2>正在执行</h2>
  <div class="running-tasks-list">
    <div class="running-task-card" hidden>
      <span class="spinner"></span>
      <span class="task-name">{{ schedule_name }}</span>
      <span class="started-at">{{ started_at }}</span>
      <span class="elapsed">{{ elapsed }}</span>
    </div>
  </div>
</section>
```

- [ ] **Step 2: Add consecutive failure alert cards**

```html
<section class="failure-alerts-panel">
  <h2>连续失败告警</h2>
  <div class="alert-cards"></div>
</section>
```

CSS：

```css
.alert-card {
  background: rgba(255, 80, 80, 0.15);
  border-left: 4px solid #ff5050;
  padding: 12px 16px;
  border-radius: 8px;
  margin-bottom: 8px;
  backdrop-filter: blur(8px);
}
```

- [ ] **Step 3: Add today's statistics panel**

```html
<section class="today-stats-panel">
  <h2>今日执行统计</h2>
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-value">{{ today_success }}</div>
      <div class="stat-label">成功</div>
    </div>
    <div class="stat-card">
      <div class="stat-value">{{ today_failure }}</div>
      <div class="stat-label">失败</div>
    </div>
    <div class="stat-card">
      <div class="stat-value">{{ total_duration_minutes }}min</div>
      <div class="stat-label">总耗时</div>
    </div>
  </div>
</section>
```

- [ ] **Step 4: Implement frontend logic in monitor.js**

在 `web/static/js/monitor.js` 新增（10 秒轮询刷新）：

```javascript
// 加载今日统计
async function loadTodayStats() {
  const resp = await fetch('/cron_tools/runs/stats');
  const stats = await resp.json();
  document.querySelector('.stat-card:nth-child(1) .stat-value').textContent = stats.today_success;
  document.querySelector('.stat-card:nth-child(2) .stat-value').textContent = stats.today_failure;
  const minutes = Math.round(stats.total_duration_seconds / 60);
  document.querySelector('.stat-card:nth-child(3) .stat-value').textContent = `${minutes}min`;
}

// 加载连续失败告警
async function loadFailureAlerts() {
  const resp = await fetch('/cron_tools/runs/recent?limit=100');
  const data = await resp.json();
  const failures = {};
  for (const r of data.runs) {
    if (!r.success) {
      failures[r.schedule_id] = (failures[r.schedule_id] || 0) + 1;
    } else {
      delete failures[r.schedule_id];
    }
  }
  const alertContainer = document.querySelector('.alert-cards');
  alertContainer.innerHTML = Object.entries(failures)
    .filter(([_, count]) => count >= 1)
    .map(([schedId, count]) => `
      <div class="alert-card">
        <strong>${schedId}</strong>: 连续失败 ${count} 次
      </div>
    `).join('');
}

// 10 秒轮询
setInterval(() => {
  loadTodayStats();
  loadFailureAlerts();
}, 10000);

// 初次加载
loadTodayStats();
loadFailureAlerts();
```

- [ ] **Step 5: Manual browser verification**

访问 `http://127.0.0.1:18394/monitor.html`，验证：
1. 今日统计面板显示成功/失败/耗时
2. 连续失败告警卡片正确显示
3. 10 秒轮询自动刷新

- [ ] **Step 6: Commit**

```bash
git add web/monitor.html web/static/js/monitor.js
git commit -m "feat(web): 7.2 monitor.html 实时状态+告警卡片+今日统计"
```

---

## 实施顺序与依赖关系

```
批次 1（架构健壮性，P0，无依赖）:
  Task 1 → Task 2 → Task 3 → Task 4
  （顺序执行，Task 1 的 Q5 命名前缀是 Task 4 测试修复的前置）

批次 2（校验层，P0，依赖批次 1）:
  Task 5 → Task 6 → Task 7 → Task 8 → Task 9
  （Task 5 协议/基类/Registry 是 Task 7/8 的前置；
   Task 6 异常类是 Task 7/8/10 的前置；
   Task 7 _run_schedule 主流程是 Task 8/9 ValidateHook 接入的前置）

批次 3（容错层，P1，依赖批次 2）:
  Task 10 → Task 11 → Task 12 → Task 13
  （Task 10 移除 RetryBudget 是 Task 11 RetryHook 成为唯一重试层的前置；
   Task 11 RetryHook 是 Task 12 CatchUpHook 调用 _run_schedule_direct 的前置；
   Task 13 字段扩展依赖 Task 7 的 _run_schedule 写入逻辑）

批次 4（通知与可视化，P2，依赖批次 3）:
  Task 14 → Task 15 → Task 16 → Task 17 → Task 18
  （Task 14 NotifyHook 依赖 Task 13 的 RunSummary.notified/notification_channels 字段；
   Task 15 SMTP 配置提取是 Task 14 邮件发送的前置；
   Task 16 API 端点依赖 Task 13 的 read_by_run_id 方法；
   Task 17/18 前端依赖 Task 16 API 端点）
```

## 统一验收清单（所有 Task 完成后执行）

- [ ] **1. 全量测试通过**：`python -m unittest discover tests -v`
- [ ] **2. Spec 覆盖度核查**：spec 所有 item（3.1-3.7 / 4.1-4.4 / 5.1-5.3 / 6.1-6.3 / 7.1-7.3 / 8.1-8.3 / 9.1-9.3）均有对应 Task
- [ ] **3. 占位符扫描**：plan 中无 TBD / TODO / FIXME / XXX
- [ ] **4. 类型一致性**：跨 Task 的方法签名（`_run_schedule` / `_run_schedule_direct` / `_is_permanent_error` / `validate_workflow_spec` / `read_by_run_id`）签名一致
- [ ] **5. 命名前缀统一**：所有 cron_tool 引用使用 `cron_tool__` 前缀
- [ ] **6. 异常类一致**：`HookAbortError` / `ValidationError` / `WorkflowExecutionError` / `AuthRequiredError` 在所有引用处签名一致
- [ ] **7. 配置热更新验证**：`cron.hooks.*` 配置变更不需重启（per-call 重建 HookRegistry）
- [ ] **8. 项目记忆约束核查**：
  - 测试用 `unittest.TestCase`（非 pytest）
  - 测试 mock 用 `install_mocks()`
  - API 密钥用 `${ENV_VAR}` 占位符
  - 异步 SDK 全链路异步
  - 错误处理全局捕获 ToolError
  - LLM 客户端用 AsyncOpenAI/AsyncAnthropic
- [ ] **9. 端到端验证**：手动触发一次 schedule，验证 hook 调用链 + RunSummary 持久化 + 通知发送
- [ ] **10. Git 历史整洁**：每个 Task 一个 commit，commit message 符合 `feat/fix/refactor(test)?(scope): 描述` 格式