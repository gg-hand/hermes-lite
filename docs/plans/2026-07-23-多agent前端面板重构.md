# 多 Agent 前端面板重构 Implementation Plan

> **For agentic workers:** Use the java-dev-skills workflow to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将多 agent 协作面板从固定定位 overlay 改造为基于 flex 布局的 50/50 分栏工作台，消除遮挡并内置操作指引。

**Architecture:** 在现有 sidebar-nav 增加 Director nav-item（toggle 语义，不影响 tab 切换）；将 `<main>` 内的聊天内容包裹为 `.chat-pane`，新增 `.collab-pane` 兄弟节点；Director 激活时 main 变 flex row 各占 50%；multiagent-render.js 从挂载 fixed 元素改为渲染进 collab-pane。

**Tech Stack:** 原生 HTML/CSS/JS（无框架），CSS 变量来自 tokens.css，无 JS 测试框架（采用浏览器手动验证）

## Global Constraints

- 项目路径根：`teage-liu/web/`
- CSS 变量复用 `tokens.css`（如 `--accent`、`--bg-secondary`、`--border`、`--radius-lg` 等）
- 无 JS 测试框架——每个 Task 的"测试"为浏览器手动验证，验证标准明确写在步骤中
- 提交粒度：每个 Task 一个 commit
- 不改动 multiagent-sse.js 和 multiagent-settings.js
- 不改动调度/监控的独立页跳转机制

## File Structure

| 文件 | 职责 | 改动类型 |
|------|------|---------|
| `web/chat.html` | 页面骨架：新增 Director nav-item、包裹 chat-pane、新增 collab-pane | 改 |
| `web/static/css/chat.css` | 布局：main flex row、chat-pane/collab-pane、移除 PC 侧栏强制可见 | 改 |
| `web/css/multiagent.css` | 工作台样式：wb-header/tabs/body/footer、nav-status-dot；删除 fixed 样式 | 重写 |
| `web/static/js/chat-main.js` | 事件：Director toggle 绑定 | 改 |
| `web/static/js/chat-memory.js` | 修复 switchSidebarTab 不影响 Director active 态 | 改 |
| `web/js/multiagent-render.js` | 渲染：从 fixed 元素改为渲染进 collab-pane | 重写 |

---

### Task 1: HTML 骨架 — Director nav-item + chat-pane 包裹 + collab-pane

**Files:**
- Modify: `web/chat.html:31-41`（sidebar-nav 加 Director nav-item）
- Modify: `web/chat.html:114-170`（main 内包裹 chat-pane + 新增 collab-pane）

**Interfaces:**
- Produces: `#btnDirector`（Director toggle 按钮）、`#collabPane`（工作台容器）、`.chat-pane`（聊天内容包裹层）

- [ ] **Step 1: 确认当前状态（验证失败基线）**

打开浏览器访问聊天页，确认：
- sidebar-nav 只有 3 个 nav-item（会话/记忆/文件），无 Director
- main 内无 `.chat-pane` 包裹，main-header/messages/input-area 是 main 的直接子元素
- 无 `#collabPane` 元素

- [ ] **Step 2: 在 sidebar-nav 末尾加 Director nav-item**

编辑 `web/chat.html`，在 L40 的 `</button>`（files nav-item 结束）之后、L41 的 `</nav>` 之前插入：

```html
      <button class="nav-item nav-item--toggle" data-action="toggle-director" id="btnDirector" title="Director 协作">
        <span class="nav-icon">◎</span><span class="nav-label">Director</span>
        <span class="nav-status-dot" id="directorStatusDot"></span>
      </button>
```

- [ ] **Step 3: 将 main 内三个子元素包裹为 chat-pane，新增 collab-pane**

编辑 `web/chat.html`，在 L114 `<main class="main">` 之后、L115 `<div class="main-header">` 之前插入 `<div class="chat-pane">`。

在 L170 `</div>`（input-area 结束）之后、`</main>` 之前插入闭合 `</div>` 和 collab-pane：

```html
    </div><!-- /.chat-pane -->
    <section class="collab-pane" id="collabPane" hidden>
      <!-- Director 工作台内容由 multiagent-render.js 动态填充 -->
    </section>
```

即 main 结构变为：
```html
  <main class="main">
    <div class="chat-pane">
      <div class="main-header">...</div>
      <div class="messages" id="messages">...</div>
      <div class="input-area">...</div>
    </div>
    <section class="collab-pane" id="collabPane" hidden></section>
  </main>
```

- [ ] **Step 4: 验证页面加载正常**

刷新浏览器，确认：
- sidebar-nav 出现第 4 个 nav-item "Director"（带 ◎ 图标和状态圆点）
- 页面布局无错乱（chat-pane 包裹后视觉无变化，因为还没加 flex row CSS）
- `#collabPane` 存在且 `hidden`（不可见）
- 控制台无报错

- [ ] **Step 5: Commit**

```bash
cd teage-liu
git add web/chat.html
git commit -m "feat: add Director nav-item and collab-pane skeleton to chat.html"
```

---

### Task 2: CSS 布局 — main flex row + chat-pane/collab-pane 50/50 分栏

**Files:**
- Modify: `web/static/css/chat.css`（新增 .chat-pane、.collab-pane 布局规则）

**Interfaces:**
- Produces: `.main.director-on` → flex row 50/50；`.collab-pane` 默认 hidden，director-on 时 flex:1
- Consumes: Task 1 的 `.chat-pane`、`#collabPane` DOM 结构

- [ ] **Step 1: 确认当前状态（验证失败基线）**

在浏览器控制台执行 `document.querySelector('main').classList.add('director-on')`，确认：
- 布局无变化（还没有 flex row CSS）
- collab-pane 仍然 hidden

- [ ] **Step 2: 在 chat.css 末尾新增分栏布局规则**

编辑 `web/static/css/chat.css`，在文件末尾追加：

```css
/* ========== 多 Agent 分栏布局 ========== */
.main {
  flex-direction: column;
}
.chat-pane {
  display: flex;
  flex-direction: column;
  flex: 1;
  min-width: 0;
  overflow: hidden;
}
.collab-pane {
  display: none;
  flex: 0 0 0%;
  overflow: hidden;
  border-left: 1px solid var(--border);
  background: var(--bg-secondary);
}
.main.director-on {
  flex-direction: row;
}
.main.director-on .chat-pane {
  flex: 1;
}
.main.director-on .collab-pane {
  display: flex;
  flex-direction: column;
  flex: 1;
}
/* 窄屏：聊天区 55%，面板 45% */
@media (max-width: 1100px) {
  .main.director-on .chat-pane { flex: 0 0 55%; }
  .main.director-on .collab-pane { flex: 0 0 45%; }
}
/* 移动端：面板全屏覆盖 */
@media (max-width: 769px) {
  .main.director-on .chat-pane { display: none; }
  .main.director-on .collab-pane { flex: 1; }
}
```

- [ ] **Step 3: 验证分栏效果**

刷新浏览器，在控制台执行：
```js
document.querySelector('main').classList.add('director-on');
document.getElementById('collabPane').hidden = false;
```

确认：
- main 变为左右分栏，chat-pane 和 collab-pane 各占 50%
- 聊天区（header + messages + input）完整可见不被遮挡
- collab-pane 为空白区域（有左边框和背景色）
- 控制台执行 `document.querySelector('main').classList.remove('director-on')` 后恢复单列

- [ ] **Step 4: Commit**

```bash
git add web/static/css/chat.css
git commit -m "feat: add flex row layout for director-on 50/50 split"
```

---

### Task 3: JS toggle — Director nav-item 点击开关面板

**Files:**
- Modify: `web/static/js/chat-main.js:275-277`（Director toggle 绑定）
- Modify: `web/static/js/chat-memory.js:8-12`（修复 switchSidebarTab 不清除 Director active）

**Interfaces:**
- Produces: `toggleDirector()` 全局函数，开关 `main.director-on` + `#collabPane.hidden` + `#btnDirector.active`
- Consumes: Task 1 的 `#btnDirector`、Task 2 的 `.main.director-on` CSS

- [ ] **Step 1: 确认当前状态（验证失败基线）**

点击 Director nav-item，确认：
- 无任何反应（还没绑定 click 事件）
- 控制台无报错

- [ ] **Step 2: 修复 switchSidebarTab 不影响 Director active 态**

编辑 `web/static/js/chat-memory.js`，将 L9 的选择器从 `.sidebar-nav .nav-item` 改为 `.sidebar-nav .nav-item[data-tab]`：

```js
function switchSidebarTab(tabName) {
  document.querySelectorAll('.sidebar-nav .nav-item[data-tab]').forEach(btn => {
    btn.classList.toggle('active', btn.dataset.tab === tabName);
  });
```

这确保切换会话/记忆/文件 tab 时不会清除 Director nav-item 的 active 态。

- [ ] **Step 3: 在 chat-main.js 新增 Director toggle 逻辑**

编辑 `web/static/js/chat-main.js`，在 L277（nav-item 绑定块之后）插入：

```js
  // Director 面板开关
  const btnDirector = document.getElementById('btnDirector');
  if (btnDirector) {
    btnDirector.addEventListener('click', () => {
      const main = document.querySelector('main.main');
      const pane = document.getElementById('collabPane');
      if (!main || !pane) return;
      const willOn = !main.classList.contains('director-on');
      main.classList.toggle('director-on', willOn);
      pane.hidden = !willOn;
      btnDirector.classList.toggle('active', willOn);
    });
  }
```

- [ ] **Step 4: 验证 toggle 效果**

刷新浏览器，点击 Director nav-item，确认：
- main 加 `director-on` 类，collab-pane 显示，50/50 分栏
- Director nav-item 加 `active` 高亮
- 再次点击，main 移除 `director-on`，collab-pane hidden，nav-item 取消 active
- 点击"会话"tab 后再点 Director：会话 tab active 被切换，Director 不受影响（因 Step 2 修复）
- 控制台无报错

- [ ] **Step 5: Commit**

```bash
git add web/static/js/chat-main.js web/static/js/chat-memory.js
git commit -m "feat: bind Director nav-item toggle and fix switchSidebarTab independence"
```

---

### Task 4: 工作台内容 — wb-header/tabs/body/footer 结构与样式

**Files:**
- Modify: `web/css/multiagent.css`（重写：删除 fixed 样式，新增工作台样式）
- Modify: `web/chat.html:106`（collab-pane 内填充工作台静态骨架）

**Interfaces:**
- Produces: `.wb-header`、`.wb-tabs`、`.wb-body`、`.wb-footer` DOM 结构；三个 tab（协作指引/Agent名册/接入说明）
- Consumes: Task 2 的 `.collab-pane` 容器

- [ ] **Step 1: 确认当前状态（验证失败基线）**

点击 Director 展开 collab-pane，确认：
- 面板内容为空白（只有背景色和边框）

- [ ] **Step 2: 在 chat.html 的 collab-pane 内填充工作台骨架**

编辑 `web/chat.html`，将 `<section class="collab-pane" id="collabPane" hidden></section>` 替换为：

```html
    <section class="collab-pane" id="collabPane" hidden>
      <div class="wb-header">
        <div class="wb-avatar">◎</div>
        <div class="wb-title-group">
          <div class="wb-title">Director 工作台</div>
          <div class="wb-subtitle">中间人 · 协调多Agent协作</div>
        </div>
        <div class="wb-state" id="wbState"><span class="wb-state-dot"></span><span class="wb-state-text">未启用</span></div>
      </div>
      <div class="wb-tabs">
        <button class="wb-tab active" data-wb-tab="guide">协作指引</button>
        <button class="wb-tab" data-wb-tab="roster">Agent名册</button>
        <button class="wb-tab" data-wb-tab="connect">接入说明</button>
      </div>
      <div class="wb-body" id="wbBody">
        <div class="wb-pane active" data-wb-pane="guide">
          <div class="wb-status-row">
            <span class="wb-status-dot"></span>
            <span class="wb-status-label">Director 状态</span>
            <span class="wb-status-val" id="wbStatusVal">未启用</span>
          </div>
          <div class="wb-section">
            <h5>如何发起协作</h5>
            <div class="wb-step"><span class="wb-num">1</span><div class="wb-txt">在输入框使用 <code>@director</code> 唤起 Director，它会把任务拆解并分派给合适的 agent。例：<code>@director 帮我对比这三个方案</code></div></div>
            <div class="wb-step"><span class="wb-num">2</span><div class="wb-txt">也可点击下方 <b>分派任务</b> 按钮，Director 会引导你选择目标 agent 和任务类型。</div></div>
            <div class="wb-step"><span class="wb-num">3</span><div class="wb-txt">Director 会自动汇总各 agent 结果，你可以追问或让某个 agent 接力。</div></div>
          </div>
          <div class="wb-section">
            <h5>支持的协作模式</h5>
            <div class="wb-step"><span class="wb-num">·</span><div class="wb-txt"><b>分派</b>：同一任务发给多个 agent，汇总对比</div></div>
            <div class="wb-step"><span class="wb-num">·</span><div class="wb-txt"><b>接力</b>：任务按顺序在 agent 间传递</div></div>
            <div class="wb-step"><span class="wb-num">·</span><div class="wb-txt"><b>辩论</b>：多个 agent 对同一问题给出不同视角</div></div>
          </div>
        </div>
        <div class="wb-pane" data-wb-pane="roster">
          <div class="agents-list" id="agentsList">
            <p class="wb-empty">暂无活跃 agents</p>
          </div>
        </div>
        <div class="wb-pane" data-wb-pane="connect">
          <div class="wb-section"><h5>接入新 Agent 要求</h5></div>
          <div class="wb-card">
            <div class="wb-card-title">必须项</div>
            <ul class="wb-req-list">
              <li>提供 <code>agent_card.md</code>：含 agent_id、role、endpoint</li>
              <li>endpoint 为 URL 格式（http:// 或 https://）</li>
              <li>实现 4 个原子操作：join_blackboard / speak / listen / heartbeat</li>
              <li>所有协议文件路径使用<b>相对路径</b>（相对于 BB_ROOT）</li>
            </ul>
          </div>
          <div class="wb-card">
            <div class="wb-card-title">推荐项</div>
            <ul class="wb-req-list">
              <li>支持 ed25519 签名验证（A2AGateway）</li>
              <li>JSON-RPC 2.0 协议，全链路异步</li>
              <li>心跳间隔 ≤ 30s，超时自动标记离线</li>
            </ul>
          </div>
          <div class="wb-card">
            <div class="wb-card-title">接入步骤</div>
            <ul class="wb-req-list">
              <li>1. 在共享黑板目录放置 agent_card.md</li>
              <li>2. Director 自动发现并注册</li>
              <li>3. 首次 heartbeat 后进入在线状态</li>
              <li>4. 即可被 @director 分派任务</li>
            </ul>
          </div>
        </div>
      </div>
      <div class="wb-footer">
        <button class="wb-btn wb-btn-ghost" id="wbCollapseBtn">收起</button>
        <button class="wb-btn wb-btn-primary" id="wbDispatchBtn">分派任务</button>
      </div>
    </section>
```

- [ ] **Step 3: 重写 multiagent.css — 删除 fixed 样式，新增工作台样式**

编辑 `web/css/multiagent.css`，替换全部内容为：

```css
/* ============================================================
   multiagent.css — Director 工作台样式
   依赖: tokens.css(变量)
   ============================================================ */

/* ========== nav-item 状态圆点 ========== */
.nav-status-dot {
  margin-left: auto;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--text-muted);
  flex-shrink: 0;
}
.nav-status-dot.state-healthy { background: var(--success, #22c55e); box-shadow: 0 0 6px rgba(34,197,94,0.5); }
.nav-status-dot.state-degraded { background: var(--warning, #eab308); }
.nav-status-dot.state-autonomous { background: #f97316; }
.nav-status-dot.state-fault { background: var(--danger, #ef4444); }
.nav-status-dot.state-disabled { background: var(--text-muted); }
.nav-status-dot.state-unknown { background: var(--text-muted); }

/* ========== 工作台头部 ========== */
.wb-header {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 14px 16px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}
.wb-avatar {
  width: 32px; height: 32px;
  border-radius: var(--radius-lg);
  background: linear-gradient(135deg, var(--accent), var(--accent-hover, var(--accent)));
  display: flex; align-items: center; justify-content: center;
  font-size: 15px; flex-shrink: 0;
}
.wb-title-group { min-width: 0; }
.wb-title { font-size: 13px; font-weight: 600; color: var(--text-primary); }
.wb-subtitle { font-size: 11px; color: var(--text-muted); margin-top: 2px; }
.wb-state {
  margin-left: auto;
  display: flex; align-items: center; gap: 5px;
  font-size: 11px;
  padding: 3px 8px;
  border-radius: var(--radius-full, 9999px);
  border: 1px solid var(--border);
  color: var(--text-muted);
}
.wb-state-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--text-muted); }
.wb-state.state-healthy { color: var(--success, #22c55e); border-color: rgba(34,197,94,0.3); background: rgba(34,197,94,0.08); }
.wb-state.state-healthy .wb-state-dot { background: var(--success, #22c55e); box-shadow: 0 0 6px rgba(34,197,94,0.5); }
.wb-state.state-fault { color: var(--danger, #ef4444); border-color: rgba(239,68,68,0.3); background: rgba(239,68,68,0.08); }
.wb-state.state-fault .wb-state-dot { background: var(--danger, #ef4444); }

/* ========== 工作台 Tabs ========== */
.wb-tabs {
  display: flex;
  gap: 2px;
  padding: 0 12px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}
.wb-tab {
  padding: 9px 12px;
  font-size: 12px;
  color: var(--text-muted);
  cursor: pointer;
  border: none;
  border-bottom: 2px solid transparent;
  background: transparent;
  font-family: inherit;
  font-weight: 500;
  transition: color var(--transition, 0.15s);
}
.wb-tab:hover { color: var(--text-secondary); }
.wb-tab.active { color: var(--accent); border-bottom-color: var(--accent); }

/* ========== 工作台内容区 ========== */
.wb-body {
  flex: 1;
  overflow-y: auto;
  padding: 14px 16px;
  min-height: 0;
}
.wb-pane { display: none; }
.wb-pane.active { display: block; }

.wb-status-row {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 12px;
  background: var(--glass-bg, rgba(255,255,255,0.03));
  border: 1px solid var(--glass-border, var(--border));
  border-radius: var(--radius-lg);
  margin-bottom: 14px;
}
.wb-status-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--text-muted); }
.wb-status-dot.state-healthy { background: var(--success, #22c55e); box-shadow: 0 0 8px rgba(34,197,94,0.5); }
.wb-status-label { font-size: 12px; color: var(--text-secondary); }
.wb-status-val { font-size: 12px; font-weight: 600; margin-left: auto; color: var(--text-muted); }
.wb-status-val.state-healthy { color: var(--success, #22c55e); }

.wb-section { margin-bottom: 16px; }
.wb-section h5 {
  font-size: 11px; font-weight: 600;
  color: var(--accent);
  text-transform: uppercase; letter-spacing: 0.06em;
  margin-bottom: 8px;
}
.wb-step {
  display: flex; gap: 10px;
  padding: 8px 10px;
  border-radius: var(--radius-lg);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  margin-bottom: 6px;
}
.wb-num {
  width: 18px; height: 18px;
  border-radius: 50%;
  background: rgba(168,85,247,0.15);
  color: var(--accent);
  font-size: 10px; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0; margin-top: 1px;
}
.wb-txt { font-size: 12px; color: var(--text-secondary); line-height: 1.55; }
.wb-txt b { color: var(--text-primary); font-weight: 600; }
.wb-txt code {
  font-family: var(--font-mono, monospace);
  font-size: 11px;
  background: var(--bg-tertiary, var(--bg-secondary));
  padding: 1px 5px;
  border-radius: 3px;
  color: var(--accent);
}

/* Agent 名册 */
.agents-list { display: flex; flex-direction: column; gap: 6px; }
.wb-empty { font-size: 12px; color: var(--text-muted); text-align: center; padding: 20px; }
.agent-card {
  padding: 10px 12px;
  border-radius: var(--radius-lg);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
}
.agent-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 4px; }
.agent-id { font-size: 12px; font-weight: 600; color: var(--text-primary); }
.agent-status { font-size: 10px; padding: 2px 7px; border-radius: 9999px; font-weight: 600; }
.agent-status.status-active, .agent-status.status-online { background: rgba(34,197,94,0.15); color: var(--success, #22c55e); }
.agent-status.status-offline { background: rgba(138,125,111,0.15); color: var(--text-muted); }
.agent-meta { display: flex; gap: 12px; font-size: 10px; color: var(--text-muted); }
.trust-score { margin-top: 6px; display: flex; align-items: center; gap: 6px; }
.trust-score-bar { height: 4px; border-radius: 2px; flex: 1; }
.trust-score-text { font-size: 10px; color: var(--text-muted); }

/* 接入说明卡片 */
.wb-card {
  padding: 12px;
  border-radius: var(--radius-lg);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  margin-bottom: 8px;
}
.wb-card-title { font-size: 12px; font-weight: 600; color: var(--text-primary); margin-bottom: 6px; }
.wb-req-list { list-style: none; display: flex; flex-direction: column; gap: 4px; }
.wb-req-list li {
  font-size: 11px; color: var(--text-secondary);
  padding-left: 14px; position: relative; line-height: 1.55;
}
.wb-req-list li::before { content: "›"; position: absolute; left: 2px; color: var(--accent); }
.wb-req-list code {
  font-family: var(--font-mono, monospace);
  font-size: 10px;
  background: var(--bg-secondary);
  padding: 1px 4px;
  border-radius: 3px;
  color: var(--accent);
}

/* ========== 工作台底部 ========== */
.wb-footer {
  padding: 10px 14px;
  border-top: 1px solid var(--border);
  display: flex; gap: 8px;
  flex-shrink: 0;
}
.wb-btn {
  flex: 1; padding: 8px;
  border-radius: var(--radius-lg);
  font-size: 12px; font-family: inherit;
  cursor: pointer; font-weight: 600;
  transition: all var(--transition, 0.15s);
  border: none;
}
.wb-btn-primary { background: var(--accent); color: var(--accent-contrast, #fff); }
.wb-btn-primary:hover { background: var(--accent-hover, var(--accent)); }
.wb-btn-ghost { background: transparent; color: var(--text-secondary); border: 1px solid var(--border); }
.wb-btn-ghost:hover { background: var(--glass-bg, rgba(255,255,255,0.05)); color: var(--text-primary); }
```

- [ ] **Step 4: 新增 tab 切换和收起按钮的 JS**

编辑 `web/static/js/chat-main.js`，在 Task 3 新增的 Director toggle 代码块之后追加：

```js
  // 工作台 tab 切换
  document.querySelectorAll('.wb-tab[data-wb-tab]').forEach(tab => {
    tab.addEventListener('click', () => {
      const target = tab.dataset.wbTab;
      document.querySelectorAll('.wb-tab').forEach(t => t.classList.toggle('active', t === tab));
      document.querySelectorAll('.wb-pane').forEach(p => p.classList.toggle('active', p.dataset.wbPane === target));
    });
  });
  // 收起按钮
  const wbCollapseBtn = document.getElementById('wbCollapseBtn');
  if (wbCollapseBtn) {
    wbCollapseBtn.addEventListener('click', () => {
      const main = document.querySelector('main.main');
      const pane = document.getElementById('collabPane');
      if (main && pane) {
        main.classList.remove('director-on');
        pane.hidden = true;
        if (btnDirector) btnDirector.classList.remove('active');
      }
    });
  }
```

- [ ] **Step 5: 验证工作台内容**

刷新浏览器，点击 Director 展开，确认：
- 工作台头部显示"Director 工作台"标题 + "未启用"状态药丸
- 三个 tab 可切换，内容正确显示
- 协作指引 tab 有状态条 + 操作步骤 + 协作模式
- 接入说明 tab 有必须项/推荐项/接入步骤卡片
- Agent 名册 tab 显示"暂无活跃 agents"
- 底部"收起"按钮可关闭面板
- "分派任务"按钮可见（功能后续实现）
- 控制台无报错

- [ ] **Step 6: Commit**

```bash
git add web/chat.html web/css/multiagent.css web/static/js/chat-main.js
git commit -m "feat: add Director workbench content with guide/roster/connect tabs"
```

---

### Task 5: multiagent-render.js 重写 — 渲染进 collab-pane，告警走 toast

**Files:**
- Rewrite: `web/js/multiagent-render.js`

**Interfaces:**
- Produces: `window.MultiagentRender = { initContainers, updateStatus, renderAgents, showAlert }`（API 不变，实现改变）
- Consumes: Task 4 的 `#directorStatusDot`、`#wbState`、`#wbStatusVal`、`#agentsList` DOM 元素；页面已有的 `#toast`

- [ ] **Step 1: 确认当前状态（验证失败基线）**

打开浏览器，确认：
- multiagent-render.js 仍在 body 上创建 fixed 元素（`#multiagent-indicator` 等）
- 工作台的 `#wbState` 和 `#directorStatusDot` 显示默认"未启用"态，不随 SSE 更新
- 控制台执行 `MultiagentRender.showAlert('test')` 显示 fixed 横幅而非 toast

- [ ] **Step 2: 重写 multiagent-render.js**

编辑 `web/js/multiagent-render.js`，替换全部内容为：

```js
// multiagent-render.js — Director 工作台状态渲染
// 渲染目标：collab-pane 内的工作台元素 + nav-item 状态圆点
// 告警：复用页面 #toast
// 暴露 window.MultiagentRender = { initContainers, updateStatus, renderAgents, showAlert }
(function () {
  "use strict";

  var STATE_COLORS = {
    healthy: "#22c55e",
    degraded: "#eab308",
    autonomous: "#f97316",
    fault: "#ef4444",
    unknown: "#6b7280",
    disabled: "#9ca3af",
  };

  var STATE_LABELS = {
    healthy: "健康",
    degraded: "降级",
    autonomous: "自治",
    fault: "故障",
    unknown: "未知",
    disabled: "未启用",
  };

  var ALERT_TIMEOUT_MS = 5000;

  function escapeHtml(str) {
    if (str == null) return "";
    return String(str)
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;")
      .replace(/'/g, "&#39;");
  }

  /**
   * 初始化工作台容器。幂等：collab-pane 内骨架已在 chat.html 中静态存在，此处仅做事件绑定。
   */
  function initContainers() {
    // 工作台骨架已在 chat.html 中静态定义，无需动态创建
    // tab 切换和收起按钮的绑定在 chat-main.js 中完成
  }

  /**
   * 更新状态显示。
   * @param {Object} status - /api/multiagent/status 返回的状态
   */
  function updateStatus(status) {
    var navDot = document.getElementById("directorStatusDot");
    var wbState = document.getElementById("wbState");
    var wbStatusVal = document.getElementById("wbStatusVal");
    var wbStatusDot = document.querySelector(".wb-status-dot");

    if (!status || !status.enabled) {
      // 未启用态
      if (navDot) { navDot.className = "nav-status-dot state-disabled"; }
      if (wbState) {
        wbState.className = "wb-state";
        var stateText = wbState.querySelector(".wb-state-text");
        if (stateText) stateText.textContent = STATE_LABELS.disabled;
      }
      if (wbStatusVal) { wbStatusVal.textContent = STATE_LABELS.disabled; wbStatusVal.className = "wb-status-val"; }
      if (wbStatusDot) { wbStatusDot.className = "wb-status-dot"; }
      renderAgents([]);
      return;
    }

    var directorState = (status.director && status.director.state) || "unknown";
    var stateClass = "state-" + directorState;
    var stateLabel = STATE_LABELS[directorState] || directorState;
    var onlineCount = (status.agents || []).filter(function (a) {
      return a.status === "active" || a.status === "online";
    }).length;

    // 更新 nav-item 状态圆点
    if (navDot) { navDot.className = "nav-status-dot " + stateClass; }

    // 更新工作台头部状态药丸
    if (wbState) {
      wbState.className = "wb-state " + stateClass;
      var stateText2 = wbState.querySelector(".wb-state-text");
      if (stateText2) stateText2.textContent = stateLabel;
    }

    // 更新指引 tab 状态条
    if (wbStatusVal) {
      wbStatusVal.textContent = stateLabel + " · " + onlineCount + " agents 在线";
      wbStatusVal.className = "wb-status-val " + stateClass;
    }
    if (wbStatusDot) { wbStatusDot.className = "wb-status-dot " + stateClass; }

    // 更新 Agent 列表
    renderAgents(status.agents || []);
  }

  /**
   * 渲染 Agent 列表到工作台名册 tab。
   * @param {Array} agents - agent 列表
   */
  function renderAgents(agents) {
    var listContainer = document.getElementById("agentsList");
    if (!listContainer) return;

    if (!agents || agents.length === 0) {
      listContainer.innerHTML = '<p class="wb-empty">暂无活跃 agents</p>';
      return;
    }

    listContainer.innerHTML = agents
      .map(function (agent) {
        var trustScore = agent.trust_score || 100;
        var trustColor =
          trustScore >= 60 ? "#22c55e" : trustScore >= 30 ? "#eab308" : "#ef4444";
        return (
          '<div class="agent-card" data-agent-id="' +
          escapeHtml(agent.agent_id) +
          '">' +
          '<div class="agent-header">' +
          '<span class="agent-id">' +
          escapeHtml(agent.agent_id) +
          "</span>" +
          '<span class="agent-status status-' +
          escapeHtml(agent.status || "active") +
          '">' +
          escapeHtml(agent.status || "active") +
          "</span>" +
          "</div>" +
          '<div class="agent-meta">' +
          '<span class="agent-role">' +
          escapeHtml(agent.role || "worker") +
          "</span>" +
          '<span class="agent-last-seen">' +
          escapeHtml(agent.last_seen || agent.last_heartbeat || "") +
          "</span>" +
          "</div>" +
          '<div class="trust-score">' +
          '<div class="trust-score-bar" style="width: ' +
          trustScore +
          "%; background: " +
          trustColor +
          ';"></div>' +
          '<span class="trust-score-text">' +
          trustScore +
          "/100</span>" +
          "</div>" +
          "</div>"
        );
      })
      .join("");
  }

  /**
   * 显示告警，复用页面 #toast，5 秒后自动消失。
   * @param {string} message - 告警消息
   * @param {string} level - 告警级别（info/success/warning/error）
   */
  function showAlert(message, level) {
    var toast = document.getElementById("toast");
    if (!toast) {
      // fallback：如果 toast 不存在，用 console
      console.log("[multiagent][" + (level || "info") + "] " + message);
      return;
    }
    var lvl = level || "info";
    toast.className = "toast toast-" + lvl;
    toast.textContent = message;
    toast.classList.add("show");
    if (toast._hideTimer) {
      clearTimeout(toast._hideTimer);
    }
    toast._hideTimer = setTimeout(function () {
      toast.classList.remove("show");
      toast._hideTimer = null;
    }, ALERT_TIMEOUT_MS);
  }

  // 导出全局
  window.MultiagentRender = {
    initContainers: initContainers,
    updateStatus: updateStatus,
    renderAgents: renderAgents,
    showAlert: showAlert,
  };

  // 页面加载完成后检查 multiagent 状态
  document.addEventListener("DOMContentLoaded", function () {
    fetch("/api/multiagent/status")
      .then(function (resp) {
        if (resp.ok) {
          return resp.json();
        }
        throw new Error("multiagent disabled");
      })
      .then(function (status) {
        updateStatus(status);
        if (status.enabled && window.MultiagentSSE) {
          window.MultiagentSSE.start();
        }
      })
      .catch(function () {
        updateStatus({ enabled: false });
      });
  });
})();
```

- [ ] **Step 3: 验证渲染目标切换**

刷新浏览器，确认：
- `document.body` 上不再出现 `#multiagent-indicator`、`#multiagent-agents-panel`、`#multiagent-alert-banner`（无 fixed 元素）
- 控制台执行 `MultiagentRender.showAlert('测试告警', 'info')` → 页面 `#toast` 显示消息，5 秒消失
- 如果 multiagent 后端启用：nav-status-dot 和工作台状态药丸显示正确颜色
- 如果 multiagent 未启用：nav-status-dot 灰色，工作台显示"未启用"
- Agent 名册 tab 显示"暂无活跃 agents"或实际 agent 列表
- 控制台无报错

- [ ] **Step 4: Commit**

```bash
git add web/js/multiagent-render.js
git commit -m "refactor: rewrite multiagent-render to target collab-pane and toast"
```

---

### Task 6: PC 端侧栏可收缩 — 移除强制可见规则

**Files:**
- Modify: `web/static/css/chat.css:32-36`（移除 PC 端 sidebar collapsed 无效规则）

**Interfaces:**
- Produces: PC 端 `#menuToggle` 可收起侧边栏
- Consumes: 现有 `toggleSidebar()` in chat-main.js

- [ ] **Step 1: 确认当前状态（验证失败基线）**

在 PC 端（宽屏）点击 `#menuToggle`，确认：
- 侧边栏不收起（因为 CSS L33-36 强制 `margin-left: 0`）

- [ ] **Step 2: 移除 PC 端 sidebar collapsed 无效规则**

编辑 `web/static/css/chat.css`，删除 L32-36 的整块规则：

```css
/* 删除这段：
@media (min-width: 769px) {
  .sidebar,
  .sidebar.collapsed { margin-left: 0; }
}
*/
```

替换为允许 PC 端 collapsed 生效的规则：

```css
/* PC 端：collapsed 时收起侧栏（允许 menuToggle 生效） */
@media (min-width: 769px) {
  .sidebar.collapsed {
    margin-left: calc(-1 * var(--sidebar-w));
  }
}
```

- [ ] **Step 3: 验证 PC 端侧栏收起**

刷新浏览器（PC 宽屏），确认：
- 点击 `#menuToggle`：侧边栏滑出收起，主区占满宽度
- 再次点击：侧边栏滑回展开
- Director 展开时收起侧栏：聊天+面板 50/50 各得近半屏宽度
- 移动端（<769px）行为不受影响（仍为浮层模式）
- 控制台无报错

- [ ] **Step 4: Commit**

```bash
git add web/static/css/chat.css
git commit -m "feat: allow PC sidebar collapse via menuToggle"
```

---

### Task 7: 协作消息气泡 — 紫色 collab bubble 样式

**Files:**
- Modify: `web/css/multiagent.css`（末尾追加 collab 气泡样式）

**Interfaces:**
- Produces: `.msg.collab` 气泡样式，用于聊天流中展示 Director 分派消息

- [ ] **Step 1: 确认当前状态（验证失败基线）**

在浏览器控制台执行：
```js
var messages = document.getElementById('messages');
var div = document.createElement('div');
div.className = 'msg collab';
div.innerHTML = '<div class="role">🎯 Director · 分派给 researcher-01</div><div class="bubble">已检索到 3 个方案，正在汇总…</div>';
messages.appendChild(div);
```

确认：
- 气泡样式与普通 assistant 消息无区别（还没有 collab 样式）

- [ ] **Step 2: 在 multiagent.css 末尾追加 collab 气泡样式**

编辑 `web/css/multiagent.css`，在文件末尾追加：

```css
/* ========== 协作消息气泡（聊天流内） ========== */
.msg.collab {
  align-self: flex-start;
  max-width: 88%;
}
.msg.collab .bubble {
  background: rgba(168, 85, 247, 0.08);
  border: 1px solid rgba(168, 85, 247, 0.25);
  color: var(--text-primary);
}
.msg.collab .role {
  color: var(--accent);
  font-size: 10px;
  padding: 0 4px;
}
```

- [ ] **Step 3: 验证气泡样式**

刷新浏览器，再次执行 Step 1 的控制台代码，确认：
- 气泡有紫色半透明背景和紫色边框
- role 标签文字为紫色（accent 色）
- 与普通 assistant 气泡视觉区分明显
- 控制台无报错

- [ ] **Step 4: Commit**

```bash
git add web/css/multiagent.css
git commit -m "feat: add purple collab bubble style for director dispatch messages"
```

---

## 完成后验证清单

全部 Task 完成后，依次验证：

1. [ ] 打开聊天页，sidebar-nav 有 4 个按钮（会话/记忆/文件/Director），Director 带状态圆点
2. [ ] 点击 Director：主区 50/50 分栏，右侧工作台显示，聊天不被遮挡
3. [ ] 工作台三个 tab 可切换，协作指引/Agent名册/接入说明内容完整
4. [ ] 再次点击 Director：面板收起，恢复单列聊天
5. [ ] 点击"会话"tab 后再点 Director：tab active 和 Director active 独立，不冲突
6. [ ] 点 `#menuToggle` 收起侧栏：Director 50/50 空间增大
7. [ ] `document.body` 上无 `#multiagent-indicator` 等 fixed 元素
8. [ ] `MultiagentRender.showAlert('test','info')` 走 `#toast`，不走 fixed 横幅
9. [ ] 调度/监控链接仍正常跳转独立页（/scheduler、/monitor）
10. [ ] 设置弹窗里 multiagent 配置段仍正常渲染
11. [ ] 移动端（<769px）Director 面板全屏覆盖，侧栏浮层模式不受影响
