# 审查问题修复 实现计划

> **给执行者:** 按此计划逐任务实现，每个任务走 TDD 循环（红 → 绿 → 重构 → 提交；单人开发允许合并提交）。
> 步骤用复选框（`- [ ]`）语法跟踪进度。
> 本计划解决《2026-09-10-liu2现状审查报告》与《2026-09-10-liu2深度审计-协议实现漂移与覆盖缺口》两份报告的全部发现。

**目标：** 把行为套件的"假绿"断言与覆盖缺口补实，把错误码/落盘/配置三个域的契约义务落到可机器验证的用例上，并把未回流的协议变更登记归位 —— 使 liu2 的"协议-实现一致性"从人工判断升级为可自动审计。

**架构：** 改动集中在三处：① `PROTOCOL/behavior-suite/`（runner 断言能力 + 4 个新用例）② `core/` 的少数发射点（日志补错误码前缀、error 事件补 `code`、快照上限补拒绝）③ 契约文档回流（`PROTOCOL/*/` + `CORE.md` + `PENDING.md` + `config.spec.md`）。**不改动任何运行期的架构与依赖方向**，不新增第三方依赖。

**技术栈：** Python 3.11（解释器固定 `D:\soft\Python311\python.exe`）、pytest、标准库 `logging`/`json`/`re`/`ast`；Rust 侧仅一处（FK 裁决，Phase 5，可选）。

## 全局约束

- 解释器固定：`D:\soft\Python311\python.exe`（`python`/`py` 为 Store stub 不可用）。
- 验证命令固定（下文简称"套件"/"单测"）：

```powershell
# 套件
D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py
# 单测
D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core teage_liu2\tests_branches -q
```

- **仓库纯净例外（用户 2026-09-10 授权）**：测试套件允许占用 `tests` 目录 —— `teage_liu2/tests_core/`、`teage_liu2/tests_branches/` 及本计划新增的测试文件**合法入库**。例外仅限测试文件；临时脚本、调试产物仍须落在仓库外或用完即删。
- 不得改动以下文件（除非任务显式标注"需用户批准"）：`config.yaml`、`config*.yaml`、`requirements.txt`、`pyproject.toml`、`Dockerfile`、`.github/**`。
- 所有 git 写操作（`add`/`commit`/`push`）**须先经用户批准**；本计划中的提交步骤一律为"提请批准"。
- 协议面改动流程：先登记 `PROTOCOL/PENDING.md` → 迭代稳定 → 才更新 spec/schema/用例/VERSION。**本计划不升 `VERSION`**（仍在 v1.0.0 内做契约澄清与实现对齐）。
- 主干-枝干依赖铁律不变：`core/` 不 import 枝干；外壳只依赖 core。
- 每步改动后必须重跑"套件"（17 → 21 条用例）；不允许出现回归。
- 错误码相关改动只使用 `core/errors.py` 已定义的常量，**严禁**在代码里新写裸字符串码。

---

## 发现 → 任务 追溯矩阵

| 发现（来源） | 级别 | 对应任务 |
|---|---|---|
| F-1 `persisted` 假绿 + S-1 落盘零覆盖 | 高 | T1.1 / T1.2 / T1.3 / T0.2 |
| F-1 `isolation` 假绿 + H-6 零覆盖 | 高 | T1.4 / T0.2 |
| F-2 协议套件依赖 `tests_core` | 高 | T0.1（特例声明后转为"显式化"） |
| F-3 错误码双轨 / 18 码零锚定 / CONFIG_* 死码 | 中高 | T2.1 / T2.2 / T2.3 / T2.4 / T2.5 / T2.6 |
| F-3 config 域 0 用例 | 中高 | T3.1 / T3.2 |
| F-4 `before_id` 未登记 + config 域漏键 | 中 | T4.1 / T4.2 |
| F-5 T-8 快照体积上限未拒绝 | 中 | T5.1 |
| F-6 3 处 except 缺日志 | 中 | T5.3 |
| F-7 幽灵外键约束 | 低中 | T5.2 |
| F-8 文档漂移（assembler/config.spec/缺口记录体例） | 低 | T4.1 / T4.3 |
| 报告1 #1 HEAD 落后（4 项修复未入库） | 高 | T6.4 |
| 报告1 #2 演进面滞留（P-1~P-7 全 experimental） | 中 | T4.4 / T2.6 |
| 报告1 #3/P-4/P-7 验证靠一次性脚本 | 中 | T1.1 起全部用例化 |
| 报告1 #5 文档视图未同步 `before_id` | 低 | T4.2 |
| 报告1 #6 缺口记录体例 | 低 | T4.3 |
| 报告1 #7 `branches/__pycache__` 残留 | 低 | T6.1 |
| 报告1 #8 SessionStore 无 TTL | 低 | 不处理（见 §"明确不做"） |

---

## Phase 0：可信性打底（无协议风险，先做）

### 任务 0.1：测试套件特例声明（显式化）

**文件：**
- 修改：`CODEBUDDY.md`（"关键约定"节，追加一行）
- 修改：`teage_liu2/PROTOCOL/README.md`（"行为套件"节，追加一段）
- 修改：`d:\dev\teage-liu\teage-liu\.codebuddy\memory\MEMORY.md`（追加项目约定）

**接口：**
- 消费：无
- 产出：全仓库对"测试文件可入库"的书面依据（供后续任务与后续会话引用）

- [ ] **步骤 1：CODEBUDDY.md 追加约定**

在 `## 关键约定` 的 `- **仓库纯净**：...` 一行下方插入：

```markdown
- **仓库纯净例外（2026-09-10 用户授权）**：测试套件允许占用 `tests` 目录 —— `teage_liu2/tests_core/`、`teage_liu2/tests_branches/` 及配套测试辅助模块合法入库并可被 `PROTOCOL/behavior-suite/runner.py` 引用；例外仅限测试文件。
```

- [ ] **步骤 2：PROTOCOL/README.md 追加段落**

在 `## 行为套件（黄金用例集）` 段末追加：

```markdown
**交付物边界（2026-09-10 明确）**：语言无关交付物 = `cases/*.json` + `suite.schema.json` + `matcher.schema.json`；`runner.py` 是**本实现的 Python 参考执行器**，允许依赖宿主实现（`teage_liu2/core/`）与其测试基础设施（`teage_liu2/tests_core/fake_llm.py`，即 F-2 所指依赖 —— 经用户授权显式化，非隐性耦合）。
```

- [ ] **步骤 3：验证**

运行：`Select-String -Path CODEBUDDY.md,teage_liu2\PROTOCOL\README.md -Pattern '仓库纯净例外|交付物边界'`
预期：两处各命中 1 行。

- [ ] **步骤 4：提请提交**（须用户批准）

```powershell
git add CODEBUDDY.md teage_liu2/PROTOCOL/README.md
```

---

### 任务 0.2：runner 未实现断言键 → 显式失败（根除假绿）

**文件：**
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py:290-336`（`_assert_invocations` / `_assert_final` 邻近新增支持集与守卫）
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py:377`（`_run_pipeline_case` 开头调用守卫）
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py:545`（`_run_protocol_case` 开头调用守卫）
- 测试：`teage_liu2/tests_core/test_behavior_runner.py`（新建）

**接口：**
- 消费：无
- 产出：`_check_supported(expected: dict) -> List[str]`（返回不支持项描述列表，空 = 通过）；模块常量 `_SUPPORTED_TOP_KEYS` / `_SUPPORTED_FINAL_KEYS` / `_SUPPORTED_INVOCATION_KEYS`（T1.2 / T1.4 会扩充这两个常量）

- [ ] **步骤 1：写失败测试**

```python
# teage_liu2/tests_core/test_behavior_runner.py
"""行为套件 runner 的断言能力守卫用例(2026-09-10 深度审计 F-1 修复)。"""
from __future__ import annotations

import importlib.util
import pathlib

_RUNNER = pathlib.Path(__file__).resolve().parents[1] / "PROTOCOL" / "behavior-suite" / "runner.py"
_spec = importlib.util.spec_from_file_location("bs_runner_under_test", _RUNNER)
runner = importlib.util.module_from_spec(_spec)
_spec.loader.exec_module(runner)


def test_unknown_final_key_fails_fast():
    problems = runner._check_supported({"final": {"persisted_x": {}}})
    assert problems and "persisted_x" in problems[0]


def test_unknown_invocation_key_fails_fast():
    problems = runner._check_supported(
        {"invocations": [{"hook": "before", "extension": "e", "isolation_x": {}}]}
    )
    assert problems and "isolation_x" in problems[0]


def test_known_keys_pass():
    assert runner._check_supported(
        {"final": {"termination_reason": "normal", "is_complete": True},
         "invocations": [{"hook": "before", "extension": "e", "order": 1}]}
    ) == []
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：FAIL（`AttributeError: module ... has no attribute '_check_supported'`）

- [ ] **步骤 3：写最简实现**

在 `runner.py` 的 `_assert_event_stream` 之前插入：

```python
# ---------------------------------------------------------------------------
# 断言能力守卫:用例出现了 runner 未实现的断言键 → 显式失败(绝不静默通过)
# 2026-09-10 深度审计 F-1:此前 persisted / isolation 被静默忽略 → 假绿
# ---------------------------------------------------------------------------
_SUPPORTED_TOP_KEYS = {"event_stream": None, "invocations": None, "final": None}
_SUPPORTED_FINAL_KEYS = {"termination_reason", "is_complete"}
_SUPPORTED_INVOCATION_KEYS = {
    "hook", "extension", "order", "absent", "snapshot_assert", "actions_assert",
}
_SUPPORTED_EVENT_KEYS = {"type", "absent", "payload"}


def _check_supported(expected: Dict[str, Any]) -> List[str]:
    """校验 expected 只使用已实现的断言键;返回不支持项描述(空 = 通过)。"""
    problems: List[str] = []
    for key in expected:
        if key not in _SUPPORTED_TOP_KEYS:
            problems.append(f"expected.{key} 未被 runner 实现")
    for key in (expected.get("final") or {}):
        if key not in _SUPPORTED_FINAL_KEYS:
            problems.append(f"expected.final.{key} 未被 runner 实现")
    for item in expected.get("invocations") or []:
        for key in item:
            if key not in _SUPPORTED_INVOCATION_KEYS:
                problems.append(f"expected.invocations[].{key} 未被 runner 实现")
    for item in expected.get("event_stream") or []:
        for key in item:
            if key not in _SUPPORTED_EVENT_KEYS:
                problems.append(f"expected.event_stream[].{key} 未被 runner 实现")
    return problems
```

在 `_run_pipeline_case`（`errors: List[str] = []` 那一行之后，即 `runner.py:434` 附近）与 `_run_protocol_case`（`errors: List[str] = []` 之后，即 `runner.py:549` 附近）各插入一行：

```python
    errors += _check_supported(expected)
```

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：`3 passed`

- [ ] **步骤 5：回归套件**

运行：套件
预期：`17/17 通过(100.0%)`（现有用例不涉及未实现键）

---

## Phase 1：落盘与隔离断言落地（F-1 核心，P0）

### 任务 1.1：`_EphemeralStorage` 记录落盘调用（双档可区分）

**文件：**
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py:342-364`（`_EphemeralStorage`）
- 测试：`teage_liu2/tests_core/test_behavior_runner.py`（追加）

**接口：**
- 消费：无
- 产出：`_EphemeralStorage.records: List[dict]`，每条 = `{"session_id","role","content","channel","flush","message_type","has_blocks"}`；`channel` 取 `"direct"`（flush 档，走 `log_message`）或 `"buffered"`（background 档，走 `log_message_buffered`）。T1.2 的 `persisted` 视图与 T1.3 的 S-1 用例消费此结构。

- [ ] **步骤 1：写失败测试**

```python
class _Rec:
    """复刻 runner._EphemeralStorage 的记录行为(契约测试,不启动真实对话)。"""


def test_ephemeral_records_flush_and_background_channels():
    store = runner._EphemeralStorage()
    try:
        store.ensure_session("s-x")
        store.log_message("s-x", "user", "输入", message_type=None)
        store.log_message_buffered("s-x", "assistant", "回复", message_type="assistant")
    finally:
        store.close()
    assert store.sessions == ["s-x"]
    assert [r["channel"] for r in store.records] == ["direct", "buffered"]
    assert [r["role"] for r in store.records] == ["user", "assistant"]
    assert store.records[1]["message_type"] == "assistant"
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：FAIL（`AttributeError: '...' object has no attribute 'records'`）

- [ ] **步骤 3：写最简实现**

把 `runner.py:342-364` 的 `_EphemeralStorage` 整体替换为：

```python
class _EphemeralStorage:
    """临时 SQLite 存储 + 落盘调用记录。

    2026-09-10 深度审计 F-1:原实现 log_message 为空操作,导致 storage 域
    S-1(落盘时机)无法被断言。现按调用序记录:
      - channel="direct"   : flush 档(user 前置,断连不丢)
      - channel="buffered" : background 档(assistant/tool_results,P-7 双档可选能力)
    """

    def __init__(self) -> None:
        self._tmpdir = tempfile.mkdtemp(prefix="bs_runner_")
        self.db_path = os.path.join(self._tmpdir, "cases.db")
        self.provider = SQLiteStorageProvider(self.db_path)
        self.records: List[Dict[str, Any]] = []
        self.sessions: List[str] = []

    def ensure_session(self, sid: str) -> None:
        self.sessions.append(sid)

    def get_session_messages(
        self, sid: str, limit: Optional[int] = None, before_id: Optional[int] = None
    ) -> List[Dict[str, Any]]:
        return []

    def _record(self, channel: str, flush: bool, session_id: str, role: str,
                content: str, kw: Dict[str, Any]) -> None:
        self.records.append({
            "session_id": session_id,
            "role": role,
            "content": content,
            "channel": channel,
            "flush": flush,
            "message_type": kw.get("message_type"),
            "has_blocks": bool(kw.get("content_blocks")),
        })

    def log_message(self, session_id: str, role: str, content: str, **kw: Any) -> None:
        self._record("direct", True, session_id, role, content, kw)

    def log_message_buffered(self, session_id: str, role: str, content: str, **kw: Any) -> None:
        self._record("buffered", False, session_id, role, content, kw)

    def close(self) -> None:
        try:
            self.provider.close()
        finally:
            import shutil
            shutil.rmtree(self._tmpdir, ignore_errors=True)
```

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：`4 passed`

- [ ] **步骤 5：回归套件**

运行：套件 → 预期 `17/17`

---

### 任务 1.2：实现 `persisted` 断言

**文件：**
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py`（`_SUPPORTED_FINAL_KEYS` 与 `_assert_final` / `_assert_final` 两处调用点）
- 测试：`teage_liu2/tests_core/test_behavior_runner.py`（追加）

**接口：**
- 消费：T1.1 的 `store.records`；T0.2 的 `_SUPPORTED_FINAL_KEYS`
- 产出：`_persisted_view(records: List[dict]) -> dict`，键为 `roles` / `channels` / `count` / `contents` / `message_types`（供 `expected.final.persisted` 以 `match_value` 对拍，支持 `{length}` / `{regex}` / 精确列表等匹配器）

- [ ] **步骤 1：写失败测试**

```python
def test_persisted_assertion_checks_records():
    store = runner._EphemeralStorage()
    try:
        store.log_message("s", "user", "hi")
        store.log_message_buffered("s", "assistant", "ok", message_type="assistant")
        view = runner._persisted_view(store.records)
    finally:
        store.close()
    assert view["roles"] == ["user", "assistant"]
    assert view["channels"] == ["direct", "buffered"]
    assert view["count"] == 2
    # 断言失败路径
    errs = runner._assert_final(
        {"persisted": {"roles": ["user"]}}, {"type": "done"}, [], view
    )
    assert errs and "persisted" in errs[0]
    # 断言通过路径
    assert runner._assert_final(
        {"persisted": {"roles": ["user", "assistant"]}}, {"type": "done"}, [], view
    ) == []
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：FAIL（`AttributeError: ... '_persisted_view'`）

- [ ] **步骤 3：写最简实现**

1）`_SUPPORTED_FINAL_KEYS` 改为：

```python
_SUPPORTED_FINAL_KEYS = {"termination_reason", "is_complete", "persisted"}
```

2）在 `_assert_final` 上方插入视图构造函数：

```python
def _persisted_view(records: List[Dict[str, Any]]) -> Dict[str, Any]:
    """落盘调用视图(供 expected.final.persisted 对拍)。"""
    return {
        "roles": [r.get("role") for r in records],
        "channels": [r.get("channel") for r in records],
        "contents": [r.get("content") for r in records],
        "message_types": [r.get("message_type") for r in records],
        "count": len(records),
    }
```

3）`_assert_final` 签名与实现改为（新增第 4 个形参，尾部追加断言分支）：

```python
def _assert_final(expected_final: Dict[str, Any], done_event: Optional[Dict[str, Any]],
                  error_events: List[Dict[str, Any]],
                  persisted_view: Optional[Dict[str, Any]] = None) -> List[str]:
    ...
    if expected_final.get("persisted") is not None:
        if persisted_view is None:
            errors.append("expected.final.persisted 出现在无落盘观测的用例中(该用例类型不支持)")
        elif not match_value(expected_final["persisted"], persisted_view):
            errors.append(
                f"persisted 不匹配: expect={expected_final['persisted']} actual={persisted_view}"
            )
    return errors
```

4）两处调用点更新：

`_run_pipeline_case`（原 `runner.py:450`）改为：

```python
    errors += _assert_final(
        expected.get("final") or {}, done_event, error_events,
        _persisted_view(storage.records),
    )
```

`_run_protocol_case`（原 `runner.py:640`）改为：

```python
        errors += _assert_final(
            expected.get("final") or {}, {"type": EV_DONE, "is_complete": True}, [], None
        )
```

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：`5 passed`

- [ ] **步骤 5：回归套件**

运行：套件 → 预期 `17/17`

---

### 任务 1.3：新用例 18 —— storage S-1 落盘时机

**文件：**
- 新建：`teage_liu2/PROTOCOL/behavior-suite/cases/18-storage-persist-timing.json`
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py`（`_run_protocol_case` 的 `protocol_pipeline_ids` 无需改；本用例走 pipeline 类）
- 修改：`teage_liu2/PROTOCOL/behavior-suite/cases/` 无需其他改动

**接口：**
- 消费：T1.1 `records`、T1.2 `persisted` 断言、T0.2 守卫
- 产出：S-1 的协议级锚定用例（id `storage-persist-18`，pipeline 类）

- [ ] **步骤 1：写用例文件（先写期望，跑出红）**

```json
{
  "id": "storage-persist-18",
  "protocol_version": "v1.0.0",
  "description": "storage 域 S-1 落盘时机(§storage S-1): user 前置(flush 档,断连不丢) → step_end 落 assistant(background 档) → tool_result 聚合落 user(background 档,message_type=tool_results) → 注入内容永不落盘。本用例锚定单轮工具路径的落盘调用序与档位。",
  "setup": {
    "extensions": [
      {
        "name": "ext_tools",
        "lang": "python",
        "transport": "in-process",
        "protocol_version": "v1.0.0",
        "hooks_implemented": ["on_tool_call", "build_injections"],
        "capabilities": ["tool_executor"]
      }
    ]
  },
  "inputs": {
    "session_id": "s-18",
    "user_input": "请调用工具",
    "tool_name": "echo",
    "tool_input": { "x": 1 },
    "ext_tools_result": "工具结果",
    "ext_tools_injections": [
      { "layer": "PREFIX", "content": "注入标记_不得落盘", "key": "t.prefix" }
    ]
  },
  "expected": {
    "event_stream": [
      { "type": "tool_use", "payload": { "name": "echo" } },
      { "type": "tool_result", "payload": { "name": "echo" } },
      { "type": "done", "payload": { "termination_reason": "normal", "is_complete": true } }
    ],
    "final": {
      "termination_reason": "normal",
      "persisted": {
        "roles": ["user", "assistant", "user"],
        "channels": ["direct", "buffered", "buffered"],
        "message_types": [null, "assistant", "tool_results"]
      }
    }
  }
}
```

- [ ] **步骤 2：跑用例确认失败**

运行：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py --case 18`
预期：FAIL，detail 含 `缺少有序事件 tool_use` 或 `persisted 不匹配`（取决于实际落盘序 —— 见下一步）

- [ ] **步骤 3：按实测校正期望（如与实现不符，先判定实现是否合规）**

运行：套件 `--verbose`，取 detail 中的 `actual={...}`。
判定规则（逐条，不得为了让用例通过而放宽断言）：
- `roles` 必须是 `["user", "assistant", "user"]`（user 前置 / step_end assistant / 工具结果聚合 user）；
- `channels` 必须是 `["direct", "buffered", "buffered"]`（只有 user 前置走 flush 档）；
- `message_types[2]` 必须是 `"tool_results"`；
- 注入内容（`注入标记_不得落盘`）**不得**出现在任何 `contents` 里。
若实测不符，**修实现**（`core/pipeline.py:145 / 255 / 192）而非改断言**，并在开发日志记录原因。

- [ ] **步骤 4：跑用例确认通过**

运行：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py --case 18`
预期：`[PASS] storage-persist-18`

- [ ] **步骤 5：回归套件**

运行：套件 → 预期 `18/18`

---

### 任务 1.4：新用例 19 —— hooks H-6 钩子异常隔离

**文件：**
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py`（`ScriptedBranch` 抛错支持 + `_build_behaviors` 映射 + `_assert_invocations` 的 `isolation` + `_SUPPORTED_INVOCATION_KEYS`）
- 新建：`teage_liu2/PROTOCOL/behavior-suite/cases/19-hook-isolation.json`
- 测试：`teage_liu2/tests_core/test_behavior_runner.py`（追加）

**接口：**
- 消费：T0.2 守卫常量
- 产出：`ScriptedBranch.raised: List[str]`（本支干抛错过的钩子名列表）；`_assert_invocations(expected, actual, raised)` 新增第 3 形参；用例输入键 `<name>_raise_on`（形如 `{"before": "boom"}`）

- [ ] **步骤 1：写失败测试**

```python
def test_isolation_assertion_requires_raised_extension():
    expected = [{"hook": "before", "extension": "bad", "isolation": {"error": True}}]
    assert runner._assert_invocations(expected, [], [])  # 未抛错 → 失败
    assert runner._assert_invocations(expected, [], ["bad"]) == []
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：FAIL（`TypeError: _assert_invocations() takes 2 positional arguments but 3 were given`）

- [ ] **步骤 3：写最简实现**

1）`_SUPPORTED_INVOCATION_KEYS` 增加 `"isolation"`：

```python
_SUPPORTED_INVOCATION_KEYS = {
    "hook", "extension", "order", "absent", "snapshot_assert", "actions_assert",
    "isolation",
}
```

2）`ScriptedBranch.__init__` 末尾追加 `self.raised: List[str] = []`，并新增方法：

```python
    def _maybe_raise(self, hook: str) -> None:
        """isolation 用例:按 behaviors.raise_on 在钩子内抛错(验证 H-6 隔离)。"""
        exc = (self._behaviors.get("raise_on") or {}).get(hook)
        if exc is not None:
            self.raised.append(hook)
            raise exc
```

3）在 `ScriptedBranch` 的 `before` / `after_step` / `after` / `on_error` 四个钩子方法体**第一行**插入 `self._maybe_raise("<hook>")`（用各自钩子名）。

4）`_build_behaviors` 末尾（`return b` 之前）追加：

```python
    raise_on = inputs.get(f"{name}_raise_on")
    if raise_on:
        b["raise_on"] = {h: RuntimeError(f"脚本化异常:{h}") for h in raise_on}
```

5）`_assert_invocations` 改签名并在 `continue` 之前处理 isolation：

```python
def _assert_invocations(expected_invocations: List[Dict[str, Any]],
                        actual: List[Dict[str, Any]],
                        raised: Optional[List[str]] = None) -> List[str]:
    ...
        iso = item.get("isolation")
        if iso is not None:
            hit = item.get("extension") in (raised or [])
            if iso.get("error") and not hit:
                errors.append(f"{item.get('extension')} 期望抛出异常被隔离,但未抛出")
            continue
```

6）`_run_pipeline_case` 收集 raised 并传入：

```python
    # ScriptedBranch 抛错登记(H-6 隔离断言用)
    raised: List[str] = [h for b in hooks._branches for h in getattr(b, "raised", [])]
    ...
    errors += _assert_invocations(expected.get("invocations") or [], invocations, raised)
```

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：`6 passed`

- [ ] **步骤 5：写用例文件**

```json
{
  "id": "hook-isolation-19",
  "protocol_version": "v1.0.0",
  "description": "钩子异常隔离(§hooks H-6 / §errors R-1 / 责任矩阵 HOOK_EXCEPTION): before 钩子抛错的扩展被跳过并记录日志,不中断对话;其余扩展照常参与,对话以 done(normal) 收尾。",
  "setup": {
    "extensions": [
      {
        "name": "ext_boom",
        "lang": "python",
        "transport": "in-process",
        "protocol_version": "v1.0.0",
        "hooks_implemented": ["before"],
        "capabilities": []
      },
      {
        "name": "ext_ok",
        "lang": "python",
        "transport": "in-process",
        "protocol_version": "v1.0.0",
        "hooks_implemented": ["before"],
        "capabilities": []
      }
    ]
  },
  "inputs": {
    "session_id": "s-19",
    "user_input": "隔离测试",
    "ext_boom_raise_on": { "before": "boom" }
  },
  "expected": {
    "invocations": [
      { "hook": "before", "extension": "ext_boom", "isolation": { "error": true } },
      { "hook": "before", "extension": "ext_ok", "order": 2 }
    ],
    "event_stream": [
      { "type": "done", "payload": { "termination_reason": "normal", "is_complete": true } }
    ],
    "final": { "termination_reason": "normal", "is_complete": true }
  }
}
```

- [ ] **步骤 6：跑用例并回归**

运行：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py --case 19`
预期：`[PASS] hook-isolation-19`
运行：套件 → 预期 `19/19`

---

## Phase 2：错误码锚定（F-3，P1）

> **设计定则（本 Phase 的核心决策，须在 T2.6 写入 errors.spec）**：错误码分三个可观测面 ——
> ① **事件面**：责任矩阵标注"上报 = error 事件"的码，必须出现在 `error` 事件的 `code` 字段（`events.spec` 已允许 `code?`，属补齐而非变更）；
> ② **日志面**：标注 `logger.*` 的码，必须以 `CODE: message` 前缀出现在日志文本中（供运维与套件捕获）；
> ③ **响应面**：transport 消息级码（`ERR_*`，小写）在 `{error:{code,message}}` 中返回，作为 errors 域的**子命名空间**登记。
> 三面之外不再保留"只在注释里"的码。

### 任务 2.1：runner 增加日志捕获与 `error_codes` 断言

**文件：**
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py`（新增 `_LogCapture` + `error_codes` 断言 + 守卫常量）
- 测试：`teage_liu2/tests_core/test_behavior_runner.py`（追加）

**接口：**
- 消费：`teage_liu2.core.errors.ERROR_CODES`
- 产出：`_LogCapture`（`logging.Handler` 子类，属性 `codes: List[str]`）；`_extract_codes(text: str) -> List[str]`（用 `ERROR_CODES` 全集做正则匹配）；`_assert_error_codes(expected: dict, observed: List[str], captured: List[str]) -> List[str]`；用例 `expected.error_codes: {"must_include": [...], "must_exclude": [...]}`

- [ ] **步骤 1：写失败测试**

```python
def test_extract_codes_finds_only_known_codes():
    assert runner._extract_codes("HOOK_TIMEOUT: 枝干 x 超时") == ["HOOK_TIMEOUT"]
    assert runner._extract_codes("普通日志没有任何码") == []


def test_assert_error_codes_reports_missing():
    errs = runner._assert_error_codes(
        {"must_include": ["HOOK_EXCEPTION"]}, observed=["TOOL_NO_EXECUTOR"], captured=[]
    )
    assert errs and "HOOK_EXCEPTION" in errs[0]
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：FAIL（`AttributeError: ... '_extract_codes'`）

- [ ] **步骤 3：写最简实现**

1）顶部 import 增加：

```python
from teage_liu2.core.errors import ERROR_CODES
```

2）新增：

```python
_SUPPORTED_TOP_KEYS = {"event_stream": None, "invocations": None, "final": None,
                       "error_codes": None}

_CODE_RE = re.compile("|".join(sorted(ERROR_CODES)))


def _extract_codes(text: str) -> List[str]:
    """从日志文本中提取 errors 域错误码(按全集精确匹配)。"""
    return _CODE_RE.findall(text or "")


class _LogCapture(logging.Handler):
    """捕获 teage_liu2 日志并提取错误码(错误码的"日志面"断言用)。"""

    def __init__(self) -> None:
        super().__init__(level=logging.DEBUG)
        self.records: List[str] = []

    def emit(self, record: logging.LogRecord) -> None:
        self.records.extend(_extract_codes(record.getMessage()))


def _assert_error_codes(expected: Dict[str, Any], observed: List[str],
                        captured: List[str]) -> List[str]:
    """断言错误码的"事件面 + 日志面"覆盖(must_include / must_exclude)。"""
    errors: List[str] = []
    seen = list(dict.fromkeys(list(observed) + list(captured)))
    for code in expected.get("must_include") or []:
        if code not in seen:
            errors.append(f"错误码 {code} 未出现(事件面+日志面均无), 实际={seen}")
    for code in expected.get("must_exclude") or []:
        if code in seen:
            errors.append(f"错误码 {code} 不应出现, 实际={seen}")
    return errors
```

3）`runner.py` 顶部 import 补 `import logging`；在 `run_case` 内包一层捕获（`_run_pipeline_case` 与 `_run_protocol_case` 共用）：

```python
async def run_case(case: Dict[str, Any], verbose: bool) -> _CaseResult:
    case_id = case.get("id", "")
    capture = _LogCapture()
    pkg_logger = logging.getLogger("teage_liu2")
    pkg_logger.addHandler(capture)
    try:
        ...  # 原有分发逻辑不变
    finally:
        pkg_logger.removeHandler(capture)
```

4）两个 case 执行函数在末尾追加（`expected.error_codes` 存在时才生效）：

```python
    errors += _assert_error_codes(
        expected.get("error_codes") or {},
        [e.get("code") for e in events if e.get("type") == EV_ERROR and e.get("code")],
        capture_codes,
    )
```

其中 `capture_codes` 由 `run_case` 通过形参传入（把 `capture.records` 传进 `_run_pipeline_case` / `_run_protocol_case`）。

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_behavior_runner.py -q`
预期：`8 passed`

- [ ] **步骤 5：回归套件**

运行：套件 → 预期 `19/19`

---

### 任务 2.2：core 日志补错误码前缀（日志面）

**文件：**
- 修改：`teage_liu2/core/hooks.py:231-237`（HOOK_TIMEOUT / HOOK_EXCEPTION）
- 修改：`teage_liu2/core/loop.py:301`（LOOP_MAX_REACHED）、`loop.py:245-247`（TOOL_NO_EXECUTOR）
- 修改：`teage_liu2/core/storage_writer.py`（STORAGE_WRITE_FAILED：写队列异常与丢弃路径的 `logger.error`）
- 修改：`teage_liu2/core/pipeline.py:444-458`（STORAGE_WRITE_FAILED：落盘失败两处 `logger.error`）
- 修改：`teage_liu2/core/llm.py`（STORAGE_READ_FAILED 不适用；`LLM_STREAM_FAILED` 见 T2.4）
- 修改：`teage_liu2/core/transport.py:571-598`（STORAGE_READ_FAILED / STORAGE_WRITE_FAILED 的异常日志，若存在）

**接口：**
- 消费：`core/errors.py` 的码常量（已存在）
- 产出：日志文本统一形如 `"<CODE>: <原消息>"`；不改任何函数签名、不改异常类型

- [ ] **步骤 1：写失败测试**

```python
# teage_liu2/tests_core/test_error_codes_logging.py
"""错误码日志面前缀(2026-09-10 F-3 修复)。"""
from __future__ import annotations

import asyncio
import logging

import pytest

from teage_liu2.core.errors import HOOK_EXCEPTION, LOOP_MAX_REACHED
from teage_liu2.core.hooks import Branch, HookChain


class _Boom(Branch):
    name = "boom"

    async def before(self, snapshot):
        raise RuntimeError("脚本化异常")


def test_hook_exception_logs_code(caplog):
    chain = HookChain()
    chain.register(_Boom())

    async def run():
        await chain.before_all.__wrapped__ if False else None

    with caplog.at_level(logging.ERROR, logger="teage_liu2.core.hooks"):
        asyncio.run(chain._call(_Boom(), "before", None))
    assert HOOK_EXCEPTION in caplog.text
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_error_codes_logging.py -q`
预期：FAIL（日志中无 `HOOK_EXCEPTION`）

- [ ] **步骤 3：写最简实现**

`hooks.py:231-237` 改为：

```python
        except asyncio.TimeoutError:
            logger.error(
                "%s: 枝干 %s 的 %s 钩子超时(>%.1fs),跳过该枝干",
                HOOK_TIMEOUT, branch.name, hook_name, self.hook_timeout,
            )
        except Exception as e:
            logger.error(
                "%s: 枝干 %s 的 %s 钩子异常,跳过: %s",
                HOOK_EXCEPTION, branch.name, hook_name, e,
            )
```

（`hooks.py` 顶部 import 增加 `from .errors import HOOK_EXCEPTION, HOOK_TIMEOUT`）

`loop.py:301` 改为：

```python
        logger.warning("%s: React 循环达到最大次数 %d", LOOP_MAX_REACHED, self.max_loops)
```

`loop.py:245` 附近改为：

```python
                    logger.warning(
                        "%s: 模型请求工具 %s 但无枝干执行,友好终止",
                        TOOL_NO_EXECUTOR, tool_name,
                    )
```

`pipeline.py:444` 与 `:455` 两条 `logger.error` 的消息前缀各加 `"%s: "` 与 `STORAGE_WRITE_FAILED` 实参（保持其余实参顺序不变）。`storage_writer.py` 的异常/丢弃日志同样加 `STORAGE_WRITE_FAILED:` 前缀。

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_error_codes_logging.py -q`
预期：`1 passed`

- [ ] **步骤 5：回归**

运行：套件 → `19/19`；单测 → 全绿

---

### 任务 2.3：config 层抛错补 `CONFIG_*` 前缀（消灭死码）

**文件：**
- 修改：`teage_liu2/core/config.py:345-384`（4 处 `raise ValueError`）
- 测试：`teage_liu2/tests_core/test_config.py`（追加断言）

**接口：**
- 消费：`core/errors.py` 的 `CONFIG_UNKNOWN_KEY` / `CONFIG_INVALID_VALUE` / `CONFIG_MISSING_KEY`
- 产出：配置错误消息统一以 `"<CODE>: "` 开头（可读错误 + 可断言）

- [ ] **步骤 1：写失败测试**

```python
def test_config_unknown_key_carries_code():
    with pytest.raises(ValueError, match="CONFIG_UNKNOWN_KEY"):
        core_config_from({"core": {"no_such_key": 1}})


def test_config_invalid_value_carries_code():
    with pytest.raises(ValueError, match="CONFIG_INVALID_VALUE"):
        core_config_from({"core": {"max_loops": 0}})


def test_config_missing_key_carries_code():
    with pytest.raises(ValueError, match="CONFIG_MISSING_KEY"):
        load_config(str(pathlib.Path("no_such_config.yaml")))
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_config.py -q`
预期：3 条 FAIL（消息中无码）

- [ ] **步骤 3：写最简实现**

`config.py` 顶部 import 增加 `from .errors import CONFIG_INVALID_VALUE, CONFIG_MISSING_KEY, CONFIG_UNKNOWN_KEY`，并按下列映射改消息前缀：
- `:345-350` 未知键 → `f"{CONFIG_UNKNOWN_KEY}: core 段包含未知配置键: ..."`
- `:316-319` `_require_int_range` / `:323-329` `_require_float_range` → `f"{CONFIG_INVALID_VALUE}: core.{key} ..."`
- `:352-356` mode 非法、`:358-360` system_prompt 类型、`:362-374` injection_budget_chars、`:378-380` branches、`:382-384` extensions_root → `CONFIG_INVALID_VALUE`
- `:207-211` 关键 API Key 未配置 → `CONFIG_MISSING_KEY`

其余调用点（`_resolve_value` 等）保持不变。

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_config.py -q`
预期：全绿（含既有用例；若既有用例用 `match=` 匹配中文子串则不受影响）

- [ ] **步骤 5：回归**

运行：单测 → 全绿

---

### 任务 2.4：`error` 事件补 `code` 字段（事件面）

**文件：**
- 修改：`teage_liu2/core/step.py:115-125`（LLM 异常 → error 事件）
- 修改：`teage_liu2/core/pipeline.py:224-229`、`240-241`（消息序列非法 → error 事件）
- 修改：`teage_liu2/PROTOCOL/events/events.schema.json`（`error` 事件已允许 `code?`，仅确认；如无则补）
- 测试：`teage_liu2/tests_core/test_pipeline.py`（追加）

**接口：**
- 消费：`core/errors.py` 的 `LLM_TIMEOUT` / `LLM_CANCELED` / `LLM_STREAM_FAILED` / `LLM_API_ERROR` / `HOOK_INVALID_ACTION`
- 产出：`error` 事件形如 `{"type":"error","session_id":...,"message":...,"code":"LLM_*"}`（新增 `code` 键；事件为新增可选键，不改既有键语义）

- [ ] **步骤 1：写失败测试**

```python
def test_error_event_carries_llm_code(...):
    """step 层 LLM 异常 → error 事件带 code(LLM_API_ERROR/LLM_TIMEOUT/LLM_STREAM_FAILED/LLM_CANCELED)。"""
    # 复用既有 fake LLM 注入 RuntimeError 的用例写法,断言 ev["code"] in ERROR_CODES
```

（完整实现写入 `test_pipeline.py`，断言 `{"type":"error", ...}` 的 `code` 属于 `ERROR_CODES` 且非空。）

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_pipeline.py -q`
预期：FAIL（`KeyError: 'code'`）

- [ ] **步骤 3：写最简实现**

`step.py` 的异常映射处（`asyncio.TimeoutError` → `LLM_TIMEOUT`；`StreamCancelled`/`CancelledError` → `LLM_CANCELED`；其他 → `LLM_API_ERROR`；流中断 → `LLM_STREAM_FAILED`）在 yield 的 error 事件中增加 `"code": <对应常量>`。

`pipeline.py:224-229` 的 error 事件增加 `"code": HOOK_INVALID_ACTION`。

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_pipeline.py -q`
预期：全绿

- [ ] **步骤 5：回归**

运行：套件 → `19/19`（case 16 断言 error 事件存在，新增键不影响）

---

### 任务 2.5：新用例 20 —— 错误码全覆盖锚定

**文件：**
- 新建：`teage_liu2/PROTOCOL/behavior-suite/cases/20-error-codes.json`
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py`（新增 `error-codes` 多场景分支）

**接口：**
- 消费：T2.1 的 `error_codes` 断言与日志捕获；T2.2/T2.3/T2.4 的码发射
- 产出：18 个码的协议级锚定（含"日志面"覆盖）

- [ ] **步骤 1：写用例文件**

```json
{
  "id": "error-codes-20",
  "protocol_version": "v1.0.0",
  "description": "错误码全集锚定(§errors §2 + 责任矩阵): 18 码分三面可观测 —— 事件面(error.code: LLM_* / HOOK_INVALID_ACTION)、日志面(CODE: 前缀: LOOP_MAX_REACHED / HOOK_TIMEOUT / HOOK_EXCEPTION / HOOK_TERMINAL_ACTION_IGNORED / TOOL_NO_EXECUTOR / TOOL_EXEC_FAILED / STORAGE_WRITE_FAILED / CONFIG_*)、响应面(transport ERR_* 由 12/17 用例覆盖)。",
  "setup": {
    "extensions": [
      { "name": "ext_stop", "lang": "python", "transport": "in-process", "protocol_version": "v1.0.0", "hooks_implemented": ["after"], "capabilities": [] }
    ]
  },
  "inputs": {
    "session_id": "s-20",
    "user_input": "错误码锚定",
    "scenarios": [
      { "label": "hook_exception", "kind": "raise_on", "hook": "before" },
      { "label": "hook_terminal_ignored", "kind": "terminal_action" },
      { "label": "no_tool_executor", "kind": "tool_path_no_executor" },
      { "label": "loop_max", "kind": "forever_tool" },
      { "label": "config_unknown_key", "kind": "config_invalid" }
    ]
  },
  "expected": {
    "error_codes": {
      "must_include": [
        "HOOK_EXCEPTION",
        "HOOK_TERMINAL_ACTION_IGNORED",
        "TOOL_NO_EXECUTOR",
        "LOOP_MAX_REACHED",
        "CONFIG_UNKNOWN_KEY"
      ],
      "must_exclude": []
    }
  }
}
```

- [ ] **步骤 2：跑用例确认失败**

运行：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py --case 20`
预期：FAIL（`未知用例类型: error-codes-20`）

- [ ] **步骤 3：写最简实现（runner 分支）**

在 `_run_protocol_case` 的 `elif` 链中（`lifecycle-reload` 之前）插入：

```python
    elif case_id.startswith("error-codes"):
        errors += await _run_error_code_matrix(case)
```

并新增函数：

```python
async def _run_error_code_matrix(case: Dict[str, Any]) -> List[str]:
    """20 错误码矩阵:按 scenarios 驱动各码发射路径,汇总事件面 + 日志面观测。"""
    from teage_liu2.core.config import core_config_from

    inputs = case.get("inputs") or {}
    errors: List[str] = []
    for sc in inputs.get("scenarios") or []:
        kind = sc.get("kind")
        if kind == "config_invalid":
            try:
                core_config_from({"core": {"no_such_key": 1}})
                errors.append("config_invalid: 期望抛错但通过")
            except ValueError:
                pass
        elif kind == "raise_on":
            storage = _EphemeralStorage()
            hooks = HookChain()
            b = ScriptedBranch("ext_boom",
                               {"name": "ext_boom", "hooks_implemented": ["before"], "capabilities": []},
                               {"raise_on": {sc.get("hook", "before"): RuntimeError("boom")}},
                               [])
            hooks.register(b)
            pipeline = ChatPipeline(llm_client=_build_fake_llm({}), history_store=storage,
                                    hooks=hooks, max_loops=1, storage_writer=None)
            async for _ in pipeline.chat_stream("s-20a", "x"):
                pass
            storage.close()
        elif kind == "terminal_action":
            # 复用 case 02 的构造:after 返回 action → HOOK_TERMINAL_ACTION_IGNORED 日志
            ...
        elif kind == "tool_path_no_executor":
            ...
        elif kind == "loop_max":
            ...
    return errors
```

（每个分支的完整代码按现有 `_run_error_matrix`（`runner.py:813-903`）的写法复制改造 —— 复用其 `ScriptedBranch` / `_ForeverToolLLM` 构造，不新增测试脚手架。）

- [ ] **步骤 4：跑用例确认通过**

运行：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py --case 20`
预期：`[PASS] error-codes-20`

- [ ] **步骤 5：回归套件**

运行：套件 → 预期 `20/20`

---

### 任务 2.6：errors 域契约回流（可观测面定则 + transport 8 码登记）

**文件：**
- 修改：`teage_liu2/PROTOCOL/errors/errors.spec.md`（新增 §6"错误码可观测面"）
- 修改：`teage_liu2/PROTOCOL/errors/errors.schema.json`（如存在 ErrorCode 枚举，补 transport 8 码；否则新增枚举）
- 修改：`teage_liu2/PROTOCOL/transport/transport.spec.md`（§4 补一行指向 errors 域登记）
- 修改：`teage_liu2/PROTOCOL/PENDING.md`（P-8 条目：错误码可观测面定则）

**接口：**
- 消费：T2.1~T2.5 的落地事实
- 产出：协议文本与实现一致；P-8 登记（状态 experimental）

- [ ] **步骤 1：errors.spec.md 追加 §6**

```markdown
## 6. 错误码可观测面（2026-09-10 定则）

每个错误码必须至少落在一个可观测面上，禁止"只在注释/常量里存在"：

| 面 | 位置 | 覆盖码 |
|---|---|---|
| ① 事件面 | `error` 事件的 `code` 字段 | LLM_TIMEOUT / LLM_CANCELED / LLM_STREAM_FAILED / LLM_API_ERROR / HOOK_INVALID_ACTION |
| ② 日志面 | 日志文本 `CODE: message` 前缀 | LOOP_MAX_REACHED / HOOK_TIMEOUT / HOOK_EXCEPTION / HOOK_TERMINAL_ACTION_IGNORED / TOOL_NO_EXECUTOR / TOOL_EXEC_FAILED / TOOL_REJECTED_BY_POLICY / TOOL_MODIFY_INVALID / STORAGE_WRITE_FAILED / STORAGE_READ_FAILED / CONFIG_UNKNOWN_KEY / CONFIG_INVALID_VALUE / CONFIG_MISSING_KEY |
| ③ 响应面 | transport `{error:{code,message}}`（子命名空间，小写 8 码） | invalid_frame / unknown_message_type / invalid_payload / kind_prefix_violation / capability_not_declared / unavailable / storage_failed / llm_call_failed |

**行为条款 R-2（可观测性）**: 新增错误码必须同时声明其可观测面并落地发射点；行为套件用例 20 用日志捕获 + 事件 `code` 双通道对拍。
```

- [ ] **步骤 2：PENDING.md 登记 P-8**

在 P-7 之后追加：

```markdown
### P-8 错误码可观测面定则（三面模型）+ transport 错误码子命名空间
- 状态: experimental
- 提案日期 / 来源: 2026-09-10 / 深度审计 F-3（18 码仅 2 个有发射点、CONFIG_* 为死码）
- 涉及域: errors（主）+ transport（子命名空间登记）
- 动机: 错误码此前"文档态"——责任矩阵定义 18 码，实现只有 2 个以日志前缀出现，其余只在注释里；套件 0 锚定
- 现状: 已落地三面模型（事件面 error.code / 日志面 CODE: 前缀 / 响应面 transport ERR_*）；用例 20 覆盖 5 码，其余 13 码以单测 + 日志前缀承载
- 入协议条件: ①18 码逐条有发射点且用例 20 覆盖 ≥ 12 码 ②transport 8 码在 errors.spec 登记 ③errors.schema.json 枚举与 ERROR_CODES 一致
- 入协议记录: （未并入）
```

- [ ] **步骤 3：验证一致性**

运行：`D:\soft\Python311\python.exe -c "from teage_liu2.core.errors import ERROR_CODES; print(len(ERROR_CODES))"`
预期：`18`（与 spec 表格逐条一致；若不一致，以 `errors.py` 为准回写 spec）

- [ ] **步骤 4：回归**

运行：套件 → `20/20`

---

## Phase 3：config 域覆盖（F-3 附带，P1）

### 任务 3.1：runner 支持 config 域对拍

**文件：**
- 修改：`teage_liu2/PROTOCOL/behavior-suite/runner.py`（`_run_protocol_case` 新增 `config-domain` 分支）
- 新建：`teage_liu2/PROTOCOL/behavior-suite/cases/21-config-domain.json`

**接口：**
- 消费：`teage_liu2.core.config.core_config_from`、T2.3 的 `CONFIG_*` 前缀
- 产出：`_run_config_case(case) -> List[str]`；用例 id `config-domain-21`

- [ ] **步骤 1：写用例文件**

```json
{
  "id": "config-domain-21",
  "protocol_version": "v1.0.0",
  "description": "config 域契约(§config C-1/C-2): core 段严格校验 —— 默认值/mode 白名单/范围校验/未知键拒绝(可读错误列未知键)/注入分层预算层名白名单;扩展段由扩展自校验(不在 core 域)。",
  "setup": { "extensions": [] },
  "inputs": {
    "valid": {
      "core": { "mode": "loop", "max_loops": 10, "hook_timeout": 3.0,
                "injection_budget_chars": { "PREFIX": 1000 } }
    },
    "invalid": [
      { "label": "unknown_key", "cfg": { "core": { "no_such_key": 1 } }, "code": "CONFIG_UNKNOWN_KEY" },
      { "label": "bad_mode", "cfg": { "core": { "mode": "nope" } }, "code": "CONFIG_INVALID_VALUE" },
      { "label": "range", "cfg": { "core": { "max_loops": 0 } }, "code": "CONFIG_INVALID_VALUE" },
      { "label": "bad_layer", "cfg": { "core": { "injection_budget_chars": { "NOPE": 1 } } }, "code": "CONFIG_INVALID_VALUE" }
    ]
  },
  "expected": {
    "event_stream": [
      { "type": "custom:config_result",
        "payload": { "valid_ok": true, "all_invalid_carry_code": true, "invalid_count": 4 } }
    ],
    "error_codes": { "must_include": ["CONFIG_UNKNOWN_KEY", "CONFIG_INVALID_VALUE"] }
  }
}
```

- [ ] **步骤 2：跑用例确认失败**

运行：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py --case 21`
预期：FAIL（`未知用例类型: config-domain-21`）

- [ ] **步骤 3：写最简实现**

`_run_protocol_case` 的 `elif` 链新增：

```python
    elif case_id.startswith("config-domain"):
        errors += await _run_config_case(case)
```

新增函数：

```python
async def _run_config_case(case: Dict[str, Any]) -> List[str]:
    """21 config 域契约:core 段校验对拍(码经日志捕获断言)。"""
    from teage_liu2.core.config import core_config_from

    inputs = case.get("inputs") or {}
    errors: List[str] = []
    try:
        cfg = core_config_from(inputs.get("valid") or {})
        valid_ok = cfg.mode == "loop" and cfg.max_loops == 10
    except Exception as e:
        valid_ok = False
        errors.append(f"合法配置被拒: {e}")
    all_carry = True
    invalid = inputs.get("invalid") or []
    for item in invalid:
        try:
            core_config_from(item["cfg"])
            all_carry = False
            errors.append(f"{item['label']}: 期望抛错但通过")
        except ValueError as e:
            if item["code"] not in str(e):
                all_carry = False
                errors.append(f"{item['label']}: 错误消息未携带 {item['code']}: {e}")
    custom = [{
        "type": "custom:config_result",
        "payload": {"valid_ok": valid_ok, "all_invalid_carry_code": all_carry,
                    "invalid_count": len(invalid)},
    }]
    expected = case.get("expected") or {}
    errors += _assert_event_stream(expected.get("event_stream") or [], custom)
    return errors
```

- [ ] **步骤 4：跑用例确认通过**

运行：`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py --case 21`
预期：`[PASS] config-domain-21`

- [ ] **步骤 5：回归套件**

运行：套件 → 预期 `21/21`

---

## Phase 4：契约回流与文档（F-4 / F-8，P2）

### 任务 4.1：config 域与 CORE.md 补键

**文件：**
- 修改：`teage_liu2/PROTOCOL/config/config.spec.md:13-26`（§2 core 段结构）
- 修改：`teage_liu2/docs/CORE.md:243-266`（§7 配置契约）

- [ ] **步骤 1：config.spec.md §2 补全 11 键**

```yaml
core:
  mode: loop                       # bare / loop(未知值启动失败)
  max_loops: 50                    # 1-200
  system_prompt: ...               # 字符串(默认内置)
  hook_timeout: 5.0                # 0.1-60 秒
  history_window_messages: 100     # 1-10000
  injection_budget_chars:          # 分层预算(层名白名单)
    PREFIX: 8000
  max_snapshot_bytes: 2097152      # 2 MiB(1MiB-256MiB)
  max_message_bytes: 524288        # 512 KiB(1KiB-16MiB)
  max_messages_per_conversation: 2000   # 1-?
  extensions_root: data2/extensions     # 扩展安装根(非空字符串)
  branches:                        # 扩展声明, 顺序 = 注册顺序
    guardrails: { enabled: true }
```

说明段追加一行：`extensions_root` 与 3 个资源上限键为 2026-09-08 / 09-08 前新增（原 spec 遗漏，2026-09-10 审计补录，属 minor 演进面，登记于 P-1 涉及域）。

- [ ] **步骤 2：CORE.md §7 补 `extensions_root`**

在 `max_messages_per_conversation: 2000` 之后插入：

```yaml
  extensions_root: data2/extensions   # 扩展安装根(2026-09-08 新增)
```

- [ ] **步骤 3：验证**

运行：`Select-String -Path teage_liu2\PROTOCOL\config\config.spec.md,teage_liu2\docs\CORE.md -Pattern 'extensions_root'`
预期：两文件各命中 ≥1 行，合计 ≥3 行。

---

### 任务 4.2：`before_id` 契约回流（PENDING + 实现视图）

**文件：**
- 修改：`teage_liu2/PROTOCOL/PENDING.md`（P-4 条目"现状"与"语义条款"追加）
- 修改：`teage_liu2/docs/CORE.md:307-322`（§10 存储平台，补 HistoryStore 签名）
- 修改：`docs/SUBSYSTEM-SPI.md`（§7 存储平台，同上）

- [ ] **步骤 1：PENDING P-4 现状行补 `before_id`**

在 P-4 的"现状"末尾追加：

```markdown
；2026-09-10 增补：`get_session_messages` 新增 `before_id`（id < before_id 向上翻页游标，配合 limit 实现"最近 N 条 → 更早 N 条"），Rust 后端与 stdio 代理同步实现（深度审计 F-4：此前未登记）
```

- [ ] **步骤 2：CORE.md §10 与 SPI §7 补签名**

在两处的 `MessageStore` 说明下方追加：

```markdown
HistoryStore 查询签名:`get_session_messages(session_id, limit=None, before_id=None)` —— `limit` 取最近 N 条,`before_id` 为向上翻页游标(`id < before_id`),二者可组合;不传 = 全量正序(向后兼容)。
```

- [ ] **步骤 3：验证**

运行：`Select-String -Path teage_liu2\PROTOCOL\PENDING.md,teage_liu2\docs\CORE.md,docs\SUBSYSTEM-SPI.md -Pattern 'before_id'`
预期：三文件均命中。

---

### 任务 4.3：文档漂移校正（F-8）

**文件：**
- 修改：`teage_liu2/core/assembler.py:13-14`（去掉"现状缺陷标注"陈旧措辞）
- 修改：`teage_liu2/docs/plans/CORE-缺口记录.md`（"已修复"区体例）

- [ ] **步骤 1：assembler.py docstring 校正**

把：

```python
现状缺陷标注:现状 validate_messages 仅对话开头执行一次,每轮注入/追加
不过校验——本模块为阶段 2 修复的真实缺陷(非平移项)。
```

改为：

```python
历史沿革:阶段 1 的 validate_messages 仅在对话开头执行一次、每轮注入/追加
不过校验;阶段 2 引入本模块后,任何进入 LLM 的消息序列均过 merge + 语义校验
(见 incremental_finalize),该缺陷已闭环。
```

- [ ] **步骤 2：缺口记录体例统一**

把 `## 已修复（移至开发日志）` 改名为 `## 已修复（正文保留供追溯；变更摘要已同步开发日志）`，并在该节首行加一句：

```markdown
> 本节仅保留修复记录正文供回溯；每条对应摘要已写入 [开发日志.md](./开发日志.md)。
```

- [ ] **步骤 3：验证**

运行：`Select-String -Path teage_liu2\core\assembler.py -Pattern '现状缺陷标注'`
预期：0 命中。

---

### 任务 4.4：演进面状态推进（报告1 #2）

**文件：**
- 修改：`teage_liu2/PROTOCOL/PENDING.md`（P-1 / P-2 状态与入协议条件勾稽）

- [ ] **步骤 1：评估并更新 P-1 / P-2**

逐条核对 P-1 入协议条件：①扩展目录树实战再验证一轮（含热重载换 manifest 与异常 manifest 拒绝）—— **已有 tests_core/test_registry.py 与用例 14 覆盖链级**；②manifest 字段集冻结；③新增行为套件用例（manifest 解析 + 未声明不启用 + 声明未装启动失败）；④归入 lifecycle 域文件。→ 若 ②③④ 未满足，保持 `experimental` 并在"现状"中**逐条列出缺口**（不得含糊）。

P-2 同理：①异语言全链（已由 storage_rust 兑现）；②版本协商三态各有用例（用例 15 已覆盖三态）；③行为套件补异语言用例（未做，标缺口）。

- [ ] **步骤 2：验证**

运行：`Select-String -Path teage_liu2\PROTOCOL\PENDING.md -Pattern 'experimental|stable'`
预期：每条候选均有明确状态与"入协议条件满足情况"记录。

---

## Phase 5：实现修正（F-5 / F-7 / F-6，P2）

### 任务 5.1：`max_snapshot_bytes` 超限补拒绝语义（T-8）

**文件：**
- 修改：`teage_liu2/core/snapshot.py:59-67`、`:139-145`
- 测试：`teage_liu2/tests_core/test_hooks.py` 或新建 `test_snapshot_limits.py`

**接口：**
- 消费：`RESOURCE_LIMITS["max_snapshot_bytes"]`
- 产出：`apply_action_batch` 在批次应用后若快照超限 → **回滚到入参快照**（返回原快照 + 超限描述进 `invalid`），与 `_check_message_budget` 的"拒绝"语义一致

- [ ] **步骤 1：写失败测试**

```python
def test_snapshot_over_budget_is_rejected_not_applied():
    """T-8:快照体积超限 → 拒绝(回滚),不得推进超限快照。"""
    from teage_liu2.core.snapshot import apply_action_batch
    from teage_liu2.core.types import Snapshot, RESOURCE_LIMITS

    big = "x" * (RESOURCE_LIMITS["max_message_bytes"] - 100)
    base = Snapshot(session_id="s", user_input="u", round=0, started_at="t",
                    history=[], system_text="", messages=[], tools=[], extra={},
                    revision=0)
    # 多次追加使总体积越过 max_snapshot_bytes
    cur = base
    for _ in range(6):
        cur, _bad = apply_action_batch(cur, [make_action("AppendMessage", message={"role": "user", "content": big})])
    assert len(json.dumps(cur.to_dict(), ensure_ascii=False)) <= RESOURCE_LIMITS["max_snapshot_bytes"]
```

- [ ] **步骤 2：跑测试确认失败**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_snapshot_limits.py -q`
预期：FAIL（体积已超限但仍返回超限快照）

- [ ] **步骤 3：写最简实现**

`snapshot.py:139-145` 改为：

```python
    budget = _check_snapshot_budget(new_snapshot)
    if budget:
        # T-8:快照体积超限 = 该批次拒绝(回滚到入参快照),与消息级上限语义一致
        logger.error("HOOK_INVALID_ACTION: %s", budget)
        invalid.append(budget)
        return snapshot, invalid
    return new_snapshot, invalid
```

- [ ] **步骤 4：跑测试确认通过**

运行：`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q`
预期：全绿（若既有用例依赖"超限仍推进"，按 T-8 语义修用例并在开发日志记录）

- [ ] **步骤 5：回归**

运行：套件 → `21/21`

---

### 任务 5.2：幽灵外键裁决（F-7）

**文件：**
- 修改：`teage_liu2/core/history.py:96-104`（启用时）或 `:128`（删声明时）
- 修改：`storage-rust/src/history.rs:31` + `open` 处（两后端必须同步）
- 修改：`teage_liu2/PROTOCOL/PENDING.md`（P-4 语义条款追加）

**接口：**
- 消费：无
- 产出：两后端 FK 语义一致（要么都启用、要么都删声明）

- [ ] **决策（已评估，见 `docs/plans/2026-09-10-T5.2幽灵外键约束-评估.md` —— 待用户裁决 A/B/C）**

  实测事实修正了本任务原先的倾向：FK 声明共 **4 处**（老系统 `teage_liu/storage/sqlite_log.py:74` 也有），4 处**均未启用** `PRAGMA foreign_keys`，存量两库 DDL 已含 FK，且**孤儿消息 = 0**（`data/sessions.db` 1144 条 / `data2/storage_rust/sessions.db` 21 条）。`data/sessions.db` 为**liu2 与老系统共用**。
  - 方案 A（全面启用）：需**解冻老系统**同步改，否则出现"同一张表两个写入方、一个校验一个不校验"的半强制状态；且把"静默孤儿"变成"带日志的丢消息"（P-7 分流路径）并新增代理短路失效逻辑 —— 收益≈0（0 孤儿证明不变量未被违反），成本中高。
  - **方案 B（评估推荐）**：删 4 处不生效的 FK 声明（保留索引）+ 在 P-4 语义条款登记"session 存在性由调用契约（`ensure_session` 先于 `log_message`）保证" —— 零行为变化、零跨系统影响。
  - 方案 C：保留声明但显式写 `PRAGMA foreign_keys=OFF;` + 注释（等效 B，保留 DDL 提示）。
  - 方案 A′（仅单后端启用）：**不可取**，破坏 P-4 两后端语义等价。

- [x] **步骤 1：按方案 B 改造两侧（已完成 2026-09-10）** —— 删 `teage_liu2/core/history.py` 与 `storage-rust/src/history.rs` 的 FK 声明（索引保留，理由移入代码注释而非 SQL 文本）；老系统 `teage_liu/storage/sqlite_log.py` **保持不动**（liu2 无需与老系统一致 + 老系统冻结，用户定案）。
- [x] **步骤 2：验证（已完成，全部 PASS）** —— 仓库外临时脚本（自清理）：core 新建库与 Rust 新编译产物新建库的 `messages` DDL **均无外键子句**；存量 `data/sessions.db`（1144/25）与 `data2/storage_rust/sessions.db`（21/5）行数与孤儿数（0）未变；`cargo build --release` 零 warning。
- [x] **步骤 3：回归（已完成）** —— 套件 `17/17`、`pytest` `107 passed`（此步为 Phase 5 时点基线；Phase 1~3 完成后应为 21 条用例）。
- [ ] **步骤 4：e2e（对话落盘 + 重启恢复）** —— 待 Phase 1~3 完成后随总回归一并做（届时若安装态 exe 需更新，须先停 7878 实例）。

---

### 任务 5.3：3 处 except 补 debug 日志（F-6）

**文件：**
- 修改：`teage_liu2/core/llm.py:911-915`
- 修改：`teage_liu2/core/stdio.py:206-207 / 214-216 / 233-236`
- 修改：`teage_liu2/server/routes.py:224-225`

- [ ] **步骤 1：逐处替换 `pass` 为 debug 日志**

```python
# llm.py:911-915
        finally:
            try:
                await stream.close()
            except Exception as e:  # noqa: BLE001 - 关闭为尽力而为
                logger.debug("关闭 LLM 流失败(已忽略): %s", e)

# stdio.py:206-207(其余两处同形)
            except Exception as e:
                logger.debug("关闭 stdio 通道 %s 时忽略异常: %s", self.name, e)

# routes.py:224-225
    except Exception as e:
        logger.debug("存储指标不可用(降级): %s", e)
```

（`routes.py` 顶部若无 logger，新增 `logger = logging.getLogger(__name__)`。）

- [ ] **步骤 2：验证**

运行：单测 → 全绿；套件 → `21/21`

---

## Phase 6：卫生与流程（P3）

### 任务 6.1：清理构建残留

- [ ] **步骤 1：删除 `teage_liu2/branches/__pycache__`（旧 audit/guardrails 的 pyc，源码已于 2026-09-08 消亡）**

运行：`Remove-Item -Recurse -Force teage_liu2\branches`
预期：目录消失（git 不跟踪 `__pycache__`，无需提交）

### 任务 6.2：一键审计脚本

**文件：**
- 新建：`scripts/audit_liu2.ps1`

- [ ] **步骤 1：写脚本**

```powershell
# scripts/audit_liu2.ps1 —— liu2 一致性审计一键入口(套件 + 单测 + lints)
$ErrorActionPreference = 'Stop'
$py = 'D:\soft\Python311\python.exe'
Write-Host '[1/3] 协议行为套件' -ForegroundColor Cyan
& $py teage_liu2\PROTOCOL\behavior-suite\runner.py
Write-Host '[2/3] 契约测试' -ForegroundColor Cyan
& $py -m pytest teage_liu2\tests_core teage_liu2\tests_branches -q
Write-Host '[3/3] 完成' -ForegroundColor Green
```

- [ ] **步骤 2：验证**

运行：`powershell -File scripts\audit_liu2.ps1`
预期：套件 `21/21` 且 pytest 全绿，退出码 0。

### 任务 6.3：CI 接入（**用户已批准 2026-09-10；workflow 已落地，提交待批准**）

- [x] **步骤 1：已获用户批准**（"6.3 允许"）
- [x] **步骤 2：workflow 已创建** —— `.github/workflows/liu2-audit.yml`：`ubuntu-latest` + Python 3.11 + **最小依赖**（`pytest pyyaml jsonschema`，不装整套 `requirements.txt`）+ 跑行为套件 + 跑 `tests_core`/`tests_branches`；触发 = `teage_liu2/**` 或 `storage-rust/**` 变更的 push/PR + 手动 `workflow_dispatch`；含 `concurrency` 取消重复运行。
- [x] **步骤 3：可行性已核验**：①套件/单测模块级三方依赖仅 `yaml`（`core/config.py:24`、`core/extension_loader.py:21`）→ 最小依赖集成立；②无 Windows 专有 API 进入该路径（唯一 Proactor 引用在 `server/storage_stdio_proxy.py` 注释中，套件与单测均不 import `teage_liu2.server`）；③`tests_branches/test_guardrails.py:27-30` 有 `skipif`（扩展未安装即整模块跳过）→ CI 无 `data2/` 不会失败，但**该模块在 CI 会全 skip**（覆盖损失，属已知取舍）。
- [ ] **步骤 4：提交**（git 写操作，待用户批准）

### 任务 6.4：提交缺口收敛（**暂缓 —— 用户 2026-09-10：「计划后再考虑」**；恢复执行时仍需 git 写操作批准）

- [ ] **步骤 1：向用户提交拆分方案**

工作树当前 10 个改动文件 + 本计划新增文件，建议拆为 3 笔：
1. `fix(teage_liu2): 路由与终态钩子四项修复` —— `core/pipeline.py`（陈旧快照/拦截文案）、`server/routes.py`（SSE 提前 return / `/reload` 序列化 / `/chat` response 三处）
2. `feat(teage_liu2): 会话历史分页（before_id 游标 + 分页路由 + 前端回放）` —— `core/history.py`、`server/routes.py`（分页路由）、`web/**`
3. `test(teage_liu2): 行为套件断言能力修复与覆盖补全` —— `PROTOCOL/behavior-suite/**`、`teage_liu2/tests_core/**`、`core/` 的码发射点、契约文档

`routes.py` 同时含 1 与 2 的改动，须用"剥 hunk + `git apply --cached`"手法拆分（同 2026-09-10 既有做法）。

- [ ] **步骤 2：获批后逐笔提交并复核** `git status` 应无剩余非预期改动

---

## 明确不做（附理由）

| 项 | 理由 |
|---|---|
| SessionStore TTL / 清理（报告1 #8） | 文档已定性为**外壳责任**，core 不内建清理；属独立的外壳会话治理需求，另行立项 |
| 升级 `PROTOCOL/VERSION` | 本计划全部改动为"实现对齐既有契约 + 契约澄清"，未新增协议元素；升版留待 P-1~P-7 收口评审（T4.4 后续） |
| 搬迁 `teage_liu2/tests_core/fake_llm.py` | 用户已授权测试套件占用 `tests`（T0.1），依赖关系改为**显式声明**即可，搬迁收益低于改动风险 |
| ~~给 runner 引入 `jsonschema` 依赖~~ **（已具备，不再是"不做项"）** | 复核发现 `requirements.txt:13` **本就声明 `jsonschema>=4.20`**，且本地环境已安装 → 全量 schema 校验**无需任何依赖改动**。T0.2 的"未知断言键 → 显式失败"仍先做（零依赖、立刻封死假绿）；schema 全量校验作为 T0.2 的后续增强，由用户在 T6.3（CI 已装 jsonschema）之上按需开启 |
| 重构 `pipeline._persist` 的 `storage_writer=None` 同步直写分支 | 生产路径始终配置 writer；该分支仅供测试。T1.1 的假 store 已使落盘链可断言，重构无收益 |
| 一次覆盖 18 码的全部发射路径 | 部分码（如 `STORAGE_READ_FAILED`）需注入存储故障，属独立故障注入场景；用例 20 覆盖 5 码 + 日志面覆盖其余前缀，剩余码以单测承载并在 P-8 入协议条件中登记 |

---

## 用户决策记录（2026-09-10）

| 决策点 | 用户决定 | 落地状态 |
|---|---|---|
| T5.2 幽灵外键 | **需评估怎么进行**（先评估再定） | 评估文档已产出 `docs/plans/2026-09-10-T5.2幽灵外键约束-评估.md`（推荐方案 B，待裁决 A/B/C）；本任务**暂停在执行前** |
| T6.3 CI 接入 | **允许** | `docs/plans/2026-09-10-审查问题修复-执行计划.md` T5.2 与 CI 均已回写；workflow 文件已创建 `.github/workflows/liu2-audit.yml`（提交待批准） |
| T6.4 提交收敛 | **计划后再考虑** | 暂缓，不执行任何 git 写操作 |

## 执行顺序与门禁

```
T0.1 → T0.2 ─┬→ T1.1 → T1.2 → T1.3 → T1.4
             └→ T2.1 → T2.2 → T2.3 → T2.4 → T2.5 → T2.6
                        └→ T3.1 (依赖 T2.3 的 CONFIG_* 前缀)
T4.1 / T4.2 / T4.3 / T4.4  (可并行,纯文档)
T5.1 / T5.2 / T5.3        (T5.2 需先向用户确认方案 A/B)
T6.1 / T6.2 随时;T6.3 / T6.4 需用户批准
```

**门禁**：每个 Phase 结束时必须满足 —— 套件全绿（Phase 1 后 19 条、Phase 2 后 20 条、Phase 3 后 21 条）+ 单测全绿；任一不符即停止并回到该 Phase 定位，不得带着红灯进入下一 Phase。

---

## 自审（规格覆盖 / 占位符 / 类型一致性）

**1）规格覆盖**：两份报告的 8 项深度发现（F-1~F-8）与现状报告 8 项问题（#1~#8）逐条映射到任务，见 §"发现 → 任务 追溯矩阵"；无遗漏项（#8 走"明确不做"并给了理由）。

**2）占位符扫描**：本计划仅 T2.5 步骤 3 的 `_run_error_code_matrix` 与 T2.4 步骤 1 的测试使用了"按既有写法复制改造"的省略（因完整展开会重复 `runner.py:813-903` 的 90 行既有代码 —— 该函数就在同文件内，执行者可直接复用），其余步骤均含可直接粘贴的完整代码/JSON/命令。已显式标注依赖的既有函数名与行号。**T5.2 的"方案 A/B"是需用户裁决的决策点，非占位符**。

**3）类型一致性核对**：
- `_check_supported(expected: Dict[str, Any]) -> List[str]` —— T0.2 定义，T0.2 两个调用点使用 ✔
- `_EphemeralStorage.records: List[Dict[str,Any]]` —— T1.1 定义，T1.2 `_persisted_view`、T1.3 用例、T2.5 复用 ✔
- `_persisted_view(records) -> Dict[str,Any]` —— T1.2 定义并用于 `_assert_final` 第 4 形参 ✔
- `_assert_final(expected, done_event, error_events, persisted_view=None)` —— T1.2 改签名，两处调用点（pipeline / protocol）同步更新 ✔
- `_assert_invocations(expected, actual, raised=None)` —— T1.4 改签名，T1.4 更新调用点 ✔
- `_extract_codes` / `_LogCapture.records` / `_assert_error_codes(expected, observed, captured)` —— T2.1 定义，T2.5、T3.1 的 `expected.error_codes` 消费 ✔
- `ScriptedBranch.raised: List[str]` —— T1.4 定义，T2.5 复用 ✔
- 错误码常量全部来自 `core/errors.py`（T2.2/T2.3/T2.4 只 import，不新写字面量）✔
