# 协议族立根（阶段 0）执行计划

> **给执行者:** 按此计划逐任务实现，每个任务产出**可独立校验的协议交付物**（文档 + schema + 行为套件）。
> 步骤用复选框(`- [ ]`)语法跟踪进度。
> 本计划依据权威规格 `docs/plans/2026-08-20-终极解耦架构-设计.md`（v1.14）§16 阶段 0 编写；与规格冲突时以规格为准。

**目标:** 建立 `teage_liu2/PROTOCOL/` 协议族（唯一契约源）——9 协议域全部产出 v0.1 规范（spec 文档 + JSON Schema + 行为描述），行为套件初版（含断言匹配器 schema）可执行，9 域 schema 全部可校验，CORE.md/SUBSYSTEM-SPI.md 降级为实现视图并加过渡横幅。

**架构:** 语言无关协议族 = 每域一份规范文档（`<domain>.spec.md`）+ 一份 JSON Schema（`<domain>.schema.json`）+ 行为描述内嵌于 spec 的"行为条款"节；行为套件 = 语言无关 JSON 黄金用例集 + 匹配器 schema；宿主语言（Python）实现降级为实现视图。

**技术栈:** JSON Schema (draft-07)；Python 3.11 + jsonschema 4.26（仅校验用，不入仓库依赖）；无新运行时依赖。

**协议版本:** v0.1.0（semver；v1.0 冻结为阶段 4 出口条件，本阶段不冻结）。

---

## 全局约束

- **唯一契约源**:`PROTOCOL/` 目录是协议的唯一契约源；`teage_liu2/docs/CORE.md`、`docs/SUBSYSTEM-SPI.md` 降级为实现视图，阶段 0 仅加"过渡期以 PROTOCOL/ 为准"横幅，不改实现描述。
- **仓库纯净**:不向仓库写入任何测试文件（`*Test.py`、`*.test.ts` 等）。行为套件 JSON 黄金用例是**协议交付物**，已获用户豁免可入库（2026-08-20 确认）。
- **schema 校验工具**:校验脚本放 `d:/tmp/`（仓库外），用完即删，不落盘仓库、不 `git add`。
- **命名约束**（协议级，必须写进 types 域 schema）:
  - `extension_name`: `^[a-z0-9_]+$`（禁点）
  - `kind`: `^[a-z0-9_.]+$`
  - `SetExtra` key: `^[a-z0-9_]+\.[a-z0-9_.]+$`
- **done 事件统一 9 键**:`{type, session_id, response, messages, is_complete, termination_reason, usage, content_blocks, stop_reason}`。
- **7 终止原因**:`normal / max_loops / user_cancel / no_tool_executor / llm_error / intercepted / tool_rejected`。
- **11 钩子**:`setup / teardown / build_injections / inject_round / before / pre_tool_call / on_tool_call / post_tool_call / after_step / after / on_error`。
- **6 Action**:`AppendMessage / SetTools / SetExtra / SetStop / SetSystem / ModifyToolSchema`。
- **9 消息类型**（transport）:`invoke_hook / invoke_tool / invoke_llm / storage_write / storage_read / storage_query / storage_delete / task_register / task_cancel` + `event / heartbeat / shutdown`。
- **git 写操作**:本计划不含任何 `git add/commit/push`（提交须用户另行批准）。
- **Python 入口**:`D:\soft\Python311\python.exe`（`python` 为 Store stub 不可用）。

---

## 文件结构（任务全部完成后）

```
teage_liu2/PROTOCOL/
├── README.md                     # 协议族入口：唯一契约源声明、9 域索引、版本、演进说明
├── VERSION                       # "v0.1.0"
├── types/
│   ├── types.spec.md             # types 域规范（值对象定义 + 命名约束 + 持久文档例外）
│   └── types.schema.json         # SessionId/Message/ContentBlock/ToolSchema/Usage/Injection/Snapshot
├── events/
│   ├── events.spec.md            # events 域规范（8+ 事件 + done 9 键 + 三层事件流）
│   └── events.schema.json        # Event/StepSummary/AfterResponse
├── hooks/
│   ├── hooks.spec.md             # hooks 域规范（11 钩子 + 6 Action + 时序 + 覆盖规则）
│   └── hooks.schema.json         # Invocation/Action/ActionResult/ContextSnapshot/ToolDecision
├── lifecycle/
│   ├── lifecycle.spec.md         # lifecycle 域规范（setup/teardown/热重载/进程重建）
│   └── lifecycle.schema.json     # Extension 声明/ProtocolVersion
├── storage/
│   ├── storage.spec.md           # storage 域规范（SPI + 三层能力模型 + 前缀隔离）
│   └── storage.schema.json       # StorageProvider 消息 payload
├── config/
│   ├── config.spec.md            # config 域规范（YAML + ${VAR} + schema 校验）
│   └── config.schema.json        # 配置结构（core 段 + 扩展段）
├── transport/
│   ├── transport.spec.md         # transport 域规范（绑定形态 + 消息全集 + 帧编码）
│   └── transport.schema.json     # 消息封装帧（type + payload）
├── errors/
│   ├── errors.spec.md            # errors 域规范（终止原因 + 错误码全集 + 责任矩阵）
│   └── errors.schema.json        # TerminationReason/ErrorCode
├── evolution/
│   ├── evolution.spec.md         # evolution 域规范（稳定面 vs 演进面 + 三阶演进）
│   └── evolution.schema.json     # protocol_version / custom:* 扩展点
└── behavior-suite/
    ├── suite.schema.json         # 黄金用例格式（event_stream/invocations/final/absent）
    ├── matcher.schema.json       # 断言匹配器 schema（点路径/子集/有序数组/regex/range/absent）
    └── cases/
        ├── 01-hooks-before.json      # 收口四连 + SetStop 短路
        ├── 02-terminal-ignore.json   # 终态钩子 action 一律忽略
        ├── 03-tool-path.json         # 工具路径四连（pre 链/modify）
        ├── 04-l1-invariant.json      # L1 绝不进 transport（负断言）
        └── 05-revision-increment.json # revision 每次批次 +1
```

---

## 任务 1: PROTOCOL/ 目录骨架

**文件:**
- 新建: `teage_liu2/PROTOCOL/README.md`
- 新建: `teage_liu2/PROTOCOL/VERSION`
- 新建: `teage_liu2/PROTOCOL/.gitignore`（空文件，占位防目录为空被忽略——非必需，若目录已有文件则跳过）

**接口:**
- 产出: 9 域目录 + behavior-suite 目录 + README（唯一契约源声明 + 域索引 + 引用设计文档 v1.14）
- 消费: 后续任务 2-10 往各域目录写 spec + schema

- [x] **步骤 1: 创建 README.md**

内容要求：声明 `PROTOCOL/` 为唯一契约源；9 域 + 行为套件一句话说明；版本 v0.1.0；引用设计文档为权威规格；注明 CORE.md/SPI 为实现视图。

- [x] **步骤 2: 创建 VERSION**

内容：`v0.1.0`

- [x] **步骤 3: 验证**

运行: `Get-ChildItem teage_liu2\PROTOCOL -Directory | Select-Object -ExpandProperty Name`
预期: 输出 9 个域目录名（types events hooks lifecycle storage config transport errors evolution）+ behavior-suite

---

## 任务 2: types 域（值对象 + 命名约束）

**文件:**
- 新建: `teage_liu2/PROTOCOL/types/types.spec.md`
- 新建: `teage_liu2/PROTOCOL/types/types.schema.json`

**接口:**
- 消费: 设计文档 §2（值对象定义）、§6（extension_name/kind/SetExtra 字符集）、§15-A6（资源上限阈值）
- 产出: `types.schema.json` 定义以下 `$defs`，供 events/hooks/storage/transport 域 schema 引用:
  - `SessionId` (string)
  - `Message` `{role: user|assistant|system|tool, content: string|ContentBlock[]}`
  - `ContentBlock` `{type: text|tool_use|tool_result|thinking, ...}`
  - `ToolSchema` `{name, description, input_schema}`
  - `Usage` `{input_tokens, output_tokens, cache_read_input_tokens, cache_creation_input_tokens, ...}`
  - `Injection` `{layer, content, priority, key}`
  - `Snapshot` `{session_id, user_input, round, started_at, history, system_text, messages, tools, extra, stop, revision}`
  - `extension_name` / `kind` / `SetExtraKey` 三模式（约束见全局约束节）

- [x] **步骤 1: 编写 types.schema.json**

JSON Schema (draft-07)，含上述全部 `$defs`；命名约束三模式必须逐字落实（`^[a-z0-9_]+$` 等）。

- [x] **步骤 2: 编写 types.spec.md**

含：值对象定义表（引用 §2）；命名约束三模式（引用 §6）；持久文档 schema 化例外（doc 为扩展私有数据、内核只透传）；资源上限阈值占位声明（具体数值进 schema，阶段 0 产出阈值条目）。

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py types`
预期: 输出 `types OK`（无 ERROR）

---

## 任务 3: events 域（事件流三层）

**文件:**
- 新建: `teage_liu2/PROTOCOL/events/events.spec.md`
- 新建: `teage_liu2/PROTOCOL/events/events.schema.json`

**接口:**
- 消费: 设计文档 §3.1（8 事件 + done 9 键）、§3.2（L1/L2/L3 三层 + 外壳直通事件 + observe 订阅）
- 产出: `events.schema.json` 定义:
  - `Event`（discriminator `type`，8+ 变体：step_start/text_delta/reasoning_delta/step_end/tool_use/tool_result/done/error/custom:*）
  - `DoneEvent`（9 键逐字，additionalProperties 固定）
  - `ErrorEvent`（不产生 done）
  - `StepSummary` `{round, text, content_blocks, tool_uses, usage, duration}`
  - `AfterResponse` `{text, content_blocks, usage, done_event}`
  - 三层不变量以注释/描述性说明写入 spec（L1 绝不进 transport；L2 唯一语义通道；L3 异步旁路 best-effort）

- [x] **步骤 1: 编写 events.schema.json**（含全部事件变体 + done 9 键）

- [x] **步骤 2: 编写 events.spec.md**（事件表 + done 9 键 + 三层事件流表 + 外壳直通事件 + L3 订阅声明）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py events`
预期: `events OK`

---

## 任务 4: hooks 域（11 钩子 + 6 Action + 时序）

**文件:**
- 新建: `teage_liu2/PROTOCOL/hooks/hooks.spec.md`
- 新建: `teage_liu2/PROTOCOL/hooks/hooks.schema.json`

**接口:**
- 消费: 设计文档 §4（交互模型/钩子全集/调用语义/action 应用时序）、§4.2（每钩子 Action 权限）
- 产出: `hooks.schema.json` 定义:
  - `Action`（6 变体：AppendMessage/SetTools/SetExtra/SetStop/SetSystem/ModifyToolSchema）
  - `ActionResult` `{actions: Action[]}`
  - `Invocation` `{hook, snapshot, args}`
  - `ContextSnapshot`（引用 types.Snapshot）
  - `ToolDecision` `{decision: allow|reject|modify, input?}`
  - 钩子名枚举（11 个）

- [x] **步骤 1: 编写 hooks.schema.json**

- [x] **步骤 2: 编写 hooks.spec.md**（钩子全集表 + 每钩子 Action 权限 + 立即应用/原子批次/覆盖规则 + SetStop 短路 + 收口四连 + 增量收口不变量 + 工具路径四连）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py hooks`
预期: `hooks OK`

---

## 任务 5: errors 域（错误码全集 + 责任矩阵）

**文件:**
- 新建: `teage_liu2/PROTOCOL/errors/errors.spec.md`
- 新建: `teage_liu2/PROTOCOL/errors/errors.schema.json`

**接口:**
- 消费: 设计文档 §8（终止原因/错误码体系/工具执行语义边界/责任矩阵/错误码-终止原因映射）
- 产出: `errors.schema.json` 定义:
  - `TerminationReason`（7 枚举）
  - `ErrorCode`（LLM_*/LOOP_*/HOOK_*/TOOL_*/STORAGE_*/CONFIG_* 全集）
  - `ErrorInfo` `{code, message}`
  - 责任矩阵以 spec 表格呈现（捕获层/上报/兜底）

- [x] **步骤 1: 编写 errors.schema.json**

- [x] **步骤 2: 编写 errors.spec.md**（错误码-终止原因映射表 + 错误码全集表 + 工具语义边界表）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py errors`
预期: `errors OK`

---

## 任务 6: lifecycle 域（生命周期协议）

**文件:**
- 新建: `teage_liu2/PROTOCOL/lifecycle/lifecycle.spec.md`
- 新建: `teage_liu2/PROTOCOL/lifecycle/lifecycle.schema.json`

**接口:**
- 消费: 设计文档 §5（生命周期）+ §12（Extension 声明）
- 产出: `lifecycle.schema.json` 定义:
  - `Extension` `{name, lang, transport, protocol_version, hooks_implemented[], capabilities[]}`
  - `ProtocolVersion`（semver string）
  - `capabilities` 集合枚举：observe/tool_executor/llm/self_hosted_storage

- [x] **步骤 1: 编写 lifecycle.schema.json**

- [x] **步骤 2: 编写 lifecycle.spec.md**（setup 幂等/逆序回滚/热重载原子替换/进程重建时序/终态钩子 action 忽略/invoke_llm 通道/会话态延续）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py lifecycle`
预期: `lifecycle OK`

---

## 任务 7: storage 域（SPI + 三层能力模型）

**文件:**
- 新建: `teage_liu2/PROTOCOL/storage/storage.spec.md`
- 新建: `teage_liu2/PROTOCOL/storage/storage.schema.json`

**接口:**
- 消费: 设计文档 §6（StorageProvider SPI + 三层能力模型 + 前缀隔离）+ §18.4（批量/limit）
- 产出: `storage.schema.json` 定义:
  - `StorageWrite` `{kind, docs:[]}`
  - `StorageRead` `{kind, doc_id}`
  - `StorageQuery` `{kind, filters, limit?}`
  - `StorageDelete` `{kind, doc_id}`
  - kind 模式 `^[a-z0-9_.]+$`；`{extension_name}.` 前缀强制

- [x] **步骤 1: 编写 storage.schema.json**

- [x] **步骤 2: 编写 storage.spec.md**（SPI 签名 + 三层能力模型 + kind 前缀隔离 + 会话态不在此域）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py storage`
预期: `storage OK`

---

## 任务 8: config 域（YAML + ${VAR}）

**文件:**
- 新建: `teage_liu2/PROTOCOL/config/config.spec.md`
- 新建: `teage_liu2/PROTOCOL/config/config.schema.json`

**接口:**
- 消费: 设计文档 §7（配置协议）+ §15-A8（敏感信息）
- 产出: `config.schema.json` 定义 core 段结构（mode/max_loops/system_prompt/hook_timeout/history_window_messages/injection_budget_chars/branches）+ 扩展段（自由 dict，扩展自校验）

- [x] **步骤 1: 编写 config.schema.json**

- [x] **步骤 2: 编写 config.spec.md**（YAML + ${VAR} 注入 + 未知键拒绝 + 扩展段 setup 自校验 + 敏感字段分离）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py config`
预期: `config OK`

---

## 任务 9: transport 域（消息全集 + 帧编码）

**文件:**
- 新建: `teage_liu2/PROTOCOL/transport/transport.spec.md`
- 新建: `teage_liu2/PROTOCOL/transport/transport.schema.json`

**接口:**
- 消费: 设计文档 §9（传输协议）+ §18.3（JSON 基线）
- 产出: `transport.schema.json` 定义:
  - `TransportFrame` `{type, payload, encoding: full|delta, protocol_version}`
  - 消息类型枚举（9 消息 + event/heartbeat/shutdown）
  - `invoke_llm` / `storage_*` / `task_*` 各消息 payload 结构

- [x] **步骤 1: 编写 transport.schema.json**

- [x] **步骤 2: 编写 transport.spec.md**（双绑定形态 + 消息全集 + 帧格式 + 编码层）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py transport`
预期: `transport OK`

---

## 任务 10: evolution 域（演进机制）

**文件:**
- 新建: `teage_liu2/PROTOCOL/evolution/evolution.spec.md`
- 新建: `teage_liu2/PROTOCOL/evolution/evolution.schema.json`

**接口:**
- 消费: 设计文档 §10（稳定面 vs 演进面 + 三阶演进）
- 产出: `evolution.schema.json` 定义 `protocol_version` 约束 + `custom:*` 扩展点模式（Event.type/Hook.name/Action.op 前缀）

- [x] **步骤 1: 编写 evolution.schema.json**

- [x] **步骤 2: 编写 evolution.spec.md**（稳定面/演进面表 + 三阶演进 + RFC 流程）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py evolution`
预期: `evolution OK`

---

## 任务 11: 行为套件 schema（黄金用例格式 + 匹配器）

**文件:**
- 新建: `teage_liu2/PROTOCOL/behavior-suite/suite.schema.json`
- 新建: `teage_liu2/PROTOCOL/behavior-suite/matcher.schema.json`

**接口:**
- 消费: 设计文档 §14.3（用例格式 + 断言匹配器规范）
- 产出:
  - `suite.schema.json`: 用例 `{id, protocol_version, description, setup{extensions[]}, inputs, expected{...}}`，expected 四类断言（event_stream / invocations / final / absent）
  - `matcher.schema.json`: 匹配器（字面量/regex/range/type/length/absent 全程作用域；点路径寻址；对象子集匹配；数组有序逐项）

- [x] **步骤 1: 编写 matcher.schema.json**

- [x] **步骤 2: 编写 suite.schema.json**（引用 matcher）

- [x] **步骤 3: schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py suite matcher`
预期: `suite OK` / `matcher OK`

---

## 任务 12: 行为套件初版用例（5 个黄金用例）

**文件:**
- 新建: `teage_liu2/PROTOCOL/behavior-suite/cases/01-hooks-before.json`
- 新建: `teage_liu2/PROTOCOL/behavior-suite/cases/02-terminal-ignore.json`
- 新建: `teage_liu2/PROTOCOL/behavior-suite/cases/03-tool-path.json`
- 新建: `teage_liu2/PROTOCOL/behavior-suite/cases/04-l1-invariant.json`
- 新建: `teage_liu2/PROTOCOL/behavior-suite/cases/05-revision-increment.json`

**接口:**
- 消费: suite.schema.json（任务 11）+ 设计文档 §4.4/§5/§8/§18.2
- 产出: 5 个 JSON 用例，覆盖核心语义：
  - 01: 收口四连时序 + before 后快照可见前序 action + SetStop 短路
  - 02: after/on_error 返回 action → 忽略（负断言：无 HOOK_TERMINAL_ACTION_IGNORED 之外的 action 应用）
  - 03: 工具路径四连（pre 链 reject 短路 / modify 叠加 → effective_input）
  - 04: L1 负断言（text_delta 绝不进 transport）
  - 05: revision 每次 action 批次 +1（快照视图断言）

- [x] **步骤 1: 编写 01-hooks-before.json**

- [x] **步骤 2: 编写 02-terminal-ignore.json**

- [x] **步骤 3: 编写 03-tool-path.json**

- [x] **步骤 4: 编写 04-l1-invariant.json**

- [x] **步骤 5: 编写 05-revision-increment.json**

- [x] **步骤 6: 全量用例 schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py suite`（并逐 case 校验符合 suite.schema.json）
预期: 全部 OK

---

## 任务 13: 全量校验 + 三文档职责边界定案

**文件:**
- 修改: `teage_liu2/docs/CORE.md`（顶部加过渡横幅）
- 修改: `docs/SUBSYSTEM-SPI.md`（顶部加过渡横幅）
- 新建: `docs/plans/2026-08-21-协议族立根-阶段0-验收报告.md`（本轮执行记录）

**接口:**
- 消费: 任务 1-12 全部产物
- 产出: 阶段 0 验收结论
- 注: "三文档" = `PROTOCOL/`(职责已在任务 1 README 定案)+ CORE.md + SUBSYSTEM-SPI.md

- [x] **步骤 1: 全量 schema 校验**

运行: `D:\soft\Python311\python.exe d:\tmp\validate_schemas.py all`
预期: 9 域 + suite + matcher 全部 OK

- [x] **步骤 2: CORE.md 加横幅**

在 `teage_liu2/docs/CORE.md` 顶部追加: `> **过渡期声明**: 以 `teage_liu2/PROTOCOL/` 为唯一契约源,本文档为实现视图(阶段 2 起逐步对齐)。`

- [x] **步骤 3: SUBSYSTEM-SPI.md 加横幅**

在 `docs/SUBSYSTEM-SPI.md` 顶部追加: `> **过渡期声明**: 以 `teage_liu2/PROTOCOL/` 为唯一契约源,本文档为实现视图(阶段 2 起逐步对齐)。`

- [x] **步骤 4: 写验收报告**

记录: 9 域 schema 全部可校验、行为套件 5 用例初版、三文档职责边界定案、阶段 0 出口条件逐条对照。

---

## 执行顺序与依赖

```
任务 1 → 任务 2-10（9 域，可并行，但按序执行更稳）
        → 任务 11（行为套件 schema）
        → 任务 12（5 用例）
        → 任务 13（全量校验 + 横幅 + 验收报告）
```

每任务完成后打勾；验证失败就地修复再继续。所有 schema 校验走 `d:/tmp/validate_schemas.py`（临时脚本，完工后删除，不落盘仓库）。

## 自审清单

- [x] 规格覆盖：§16 阶段 0 全部要求 → 任务 1-13 全覆盖（9 域规范 + 套件初版 + 字符集 + 资源上限阈值 + 三文档职责）
- [x] 占位符扫描：无 TBD/TODO/"以后实现"/"适当处理"字样
- [x] 类型一致性：types.schema.json 的 `$defs` 被 events/hooks/storage/transport 引用时名称一致（Snapshot/Message/ContentBlock/Injection/ToolSchema/Usage）
- [x] 命名约束三模式在 types 域逐字落实
- [x] done 9 键、7 终止原因、11 钩子、6 Action、9 消息类型与设计文档逐字一致
