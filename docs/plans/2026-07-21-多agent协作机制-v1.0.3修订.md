# 多 Agent 协作机制 v1.0.3 修订实施计划

> **For agentic workers:** 本计划为文档修订任务（非代码实现），每个任务按"修订→验收"循环执行。
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `2026-07-20-多agent协作机制-design.md` 从 v1.0.2 修订到 v1.0.3，落地 6 项 grill-me 决策（Q1-Q6）+ 49 项交叉验证问题（P0×15 / P1×22 / P2×12）。

**Architecture:** 按 spec §5 定义的 9 步执行顺序串行修订（所有修订都在同一个 design.md 文件上，并行修订会文件冲突）。Step 1 已完成，本计划覆盖 Step 2-9。每步委派子智能体执行修订 + 自验，最后统一验收。

**Tech Stack:** Markdown 文档修订；验收用 Grep 工具做客观判定（spec §6.1/§6.2 定义的可 grep 验收项）。

## Global Constraints

- 所有修订必须对齐 `project_memory.md` 硬约束：CAS 重试 2 次（非 3 次）、危险工具清单 `execute_command/write_file/call_tool`、全链路异步、`_RESTART_REQUIRED_KEYS` 仅新增 `multiagent.blackboard_dir`（变 8 项）
- audit action 必须用 13 粗粒度枚举之一（write/heartbeat/turn_advance/turn_timeout/lock_acquire/lock_release/lock_force_release/register/leave/arbitrate/snapshot/recovery_start/recovery_end），细分原因放 `details.reason`（O1 默认值）
- 协议文件路径必须用相对路径，禁止绝对路径（P0-10）
- §3.7 章节用三级标题 `### 3.7`（与 §3.5/§3.6 层级一致），子章节用四级 `#### 3.7.x`
- 所有修订内容详见 `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4 章节修订详情

## 文件结构

| 文件 | 责任 | 本计划修改范围 |
|------|------|---------------|
| `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` | v1.0.2 主文档（约 2900 行） | Step 2-9 全部修订 |
| `hermes-lite/docs/superpowers/specs/2026-07-21-多agent协作机制-v1.0.3修订-spec.md` | v1.0.3 修订 spec（权威源） | 只读参考，不修改 |

## 任务依赖图

```
Step 1（已完成）─→ Step 2 ─→ Step 3 ─→ Step 4 ─→ Step 5 ─→ Step 6 ─┐
                  │                     │         │         Step 7 ─┼→ Step 9
                  └─────────────────────┴─────────┴──→ Step 8 ──────┘
```

- Step 2 依赖 Step 1（字段补充已完成）
- Step 3 依赖 Step 2（字段属性表）
- Step 4 依赖 Step 3（锁机制）
- Step 5 依赖 Step 1-4（§11.2 引用所有章节）
- Step 6 依赖 Step 5（异常类定义）
- Step 7 独立（可与 Step 6 并行，但文件冲突需串行）
- Step 8 依赖 Step 1（§3.3.7 字段定义已完成）
- Step 9 最后（记录所有变更）

**执行策略**：由于所有修订都在 design.md 上，必须串行执行。每步委派一个子智能体执行修订 + 自验，返回后由主智能体确认再进入下一步。Step 6/7/8 虽 spec §5.3 标注可并行，但因文件冲突改为串行。

---

## Task 2: 字段属性表（§3.3.3，Q1+Q6 合并落地）

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` §3.3.3（CAS 写入协议代码块之后的"Director 是 status.json 唯一权威写者"段落 + 之后插入字段属性表）

**Interfaces:**
- Consumes: Step 1 已补充的 status.json schema 字段（last_fencing_token / grace_until / force_releasing / recovery_*）
- Produces: 字段属性表（15+ 字段写权限 + CAS 合并策略），供 Step 3 / Step 5 引用

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.3 字段属性表 + line 557-559 段落改写

- [ ] **Step 1: 改写 line 557-559 "Director 是 status.json 唯一权威写者"段落**

查找 design.md 中包含"Director 是 status.json 唯一权威写者"的段落（位于 CAS 写入协议代码块之后），整段替换为：

```markdown
**Director 是 status.json 主要写者，Worker 按字段属性表受限写入**（v1.0.3 修订，对齐 Q1 字段属性表）：

Director 是 status.json 的主要写者，负责 protocol_version / phase / epoch / current_turn / active_agents / director_status / director_signature / recovery_* 等字段的独占写入。Worker 按字段属性表受限写入以下字段：
- `locks.<lock_name>`：Worker 释放锁时直接 CAS 写入（Q1 决策），无需通过 Director 串行化
- `last_fencing_token`：Worker 写 locks 时同步递增（max 合并策略）
- `last_heartbeat.<self_agent_id>`：Worker 仅写自己的 heartbeat key（last-write-wins）
- `last_message_seq`：Worker 追加消息时 CAS 递增（max 合并策略）
- `current_turn`：仅自治模式（director_status=autonomous）下 Worker 可 CAS 写（见字段属性表例外）
- `director_status`：仅 Worker 检测到 Director 心跳超时（写 autonomous）或签名验证降级（写 degraded）时可写

其他字段（turn_history 由 Director 在推进轮次时追加）由 Director 串行化。这与 §5.1"Director 是协议执行者"定位一致，同时避免 Worker 释放锁等高频操作全部串行到 Director 形成瓶颈。
```

- [ ] **Step 2: 在改写段落之后插入字段属性表**

紧跟上述段落之后，插入 spec §4.3 定义的字段属性表（16 行字段 + 例外说明），完整内容见 spec §4.3 `[补] §3.3.3 字段属性表（Q1 + Q6 合并）`。

- [ ] **Step 3: 自验**

运行以下 Grep 验收（全部必须通过）：
- `grep '字段属性表' design.md` 命中 1+ 次
- `grep 'Director 是 status.json 主要写者' design.md` 命中 1 次
- `grep 'Worker 不直接写 status.json' design.md` 命中 0 次
- `grep 'last_fencing_token.*Worker 可 CAS' design.md` 命中 1 次（字段属性表内）

- [ ] **Step 4: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 2 - 字段属性表 + line 557-559 段落改写"
```

---

## Task 3: 锁机制补完（§7.1 / §7.2）

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` §7.1 三级锁策略（多处于代码块后补充伪代码）

**Interfaces:**
- Consumes: Step 1 的 last_fencing_token / grace_until / force_releasing 字段；Step 2 的字段属性表（locks.* Worker 可 CAS 写）
- Produces: §7.1 含 4 个新方法伪代码（_next_fencing_token / _mark_grace_period / _delayed_clear / _is_agent_alive），供 Step 5 错误处理引用

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.11（7 个修订子项）

**依赖:** Task 2（字段属性表）

- [ ] **Step 1: 补 `_next_fencing_token` 伪代码**

位置：§7.1 LockManager 类定义内（spec 标注 line 1278 之后，但行号已变，用 Grep 查找 `class LockManager` 定位）。插入 spec §4.11 第一个代码块。

- [ ] **Step 2: 补 `_mark_grace_period` / `_delayed_clear` 伪代码**

位置：§7.1 force_release 相关代码块之后（spec 标注 line 1321-1323 之后）。插入 spec §4.11 第二个代码块。

- [ ] **Step 3: 补 LockManager 单例声明**

位置：`class LockManager` 定义之前。插入：`LockManager 是 Worker 进程内单例（DI 容器单例注册），跨 ReactLoop 共享。`

- [ ] **Step 4: 补 acquire fence 检查**

位置：acquire 函数第 2 步前（spec 标注 line 1258-1301）。插入 spec §4.11 第三个代码块（fence 期检查 messages 锁）。

- [ ] **Step 5: 补 `_is_agent_alive` / `_is_heartbeat_stale` 伪代码**

位置：§7.1 探活相关位置（spec 标注 line 1445 / line 1451）。插入 spec §4.11 第四个代码块。

- [ ] **Step 6: 补 emergency_release best-effort 模式**

位置：emergency_release 函数内（spec 标注 line 1405-1423）。按 spec §4.11 第五个代码块修改，加 recovery 文件兜底。

- [ ] **Step 7: 补 renew vs force release 重读策略**

位置：renew / force_release 函数之后（spec 标注 line 1338-1352 / line 1303-1336）。插入 spec §4.11 第六个代码块说明。

- [ ] **Step 8: 自验**

运行 Grep 验收（全部必须通过）：
- `grep '_next_fencing_token' design.md` 命中 1+ 次
- `grep '_mark_grace_period' design.md` 命中 1+ 次
- `grep '_delayed_clear' design.md` 命中 1+ 次
- `grep '_is_agent_alive' design.md` 命中 1+ 次
- `grep 'LockManager 是 Worker 进程内单例' design.md` 命中 1 次
- `grep 'fence_period_messages_blocked' design.md` 命中 1 次
- `grep 'audit.jsonl.recovery' design.md` 命中 1+ 次

- [ ] **Step 9: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 3 - 锁机制补完（4 方法 + 单例 + fence + best-effort）"
```

---

## Task 4: 可靠性补完（§8.2 / §8.3）

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` §8.2 故障检测 + §8.3 故障恢复

**Interfaces:**
- Consumes: Step 3 的锁机制（grace_period / force_release）
- Produces: §8.2 时钟漂移两行改写（删除"改用单调时钟"）+ §8.3 第 0 步快照恢复 + 自治退出二次确认

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.12（3 个修订子项）+ §4.13（3 个修订子项）

**依赖:** Task 3（锁机制）

- [ ] **Step 1: 改写 §8.2 时钟漂移两行**

查找 design.md 中包含"时钟漂移 - 警告级"和"时钟漂移 - 严重级"的两行（spec 标注 line 1579-1580）。替换为：
```
| 时钟漂移 - 警告级 | 偏差 > 5 秒 | 告警 + 放大 grace_period 至 drift_offset + 2s |
| 时钟漂移 - 严重级 | 偏差 > 60 秒 | 暂停锁强制释放（防误释放他人锁） |
```

- [ ] **Step 2: 补 §8.2 watchdog 永久标记持久化**

位置：watchdog 自愈超时相关代码块之后（spec 标注 line 1640-1651 之后）。插入 spec §4.12 第二个代码块。

- [ ] **Step 3: 补 §8.2 LLM 不可用 priority 排序键注释**

位置：§8.2 LLM 不可用行（spec 标注 line 1583）。替换为：`仲裁改用 conflict_resolution.fallback_strategy=priority，排序键：last_heartbeat_age asc → agent_id asc（详见 §3.3.2）`

- [ ] **Step 4: 补 §8.3 第 0 步快照恢复**

位置：§8.3 故障恢复第 1 步之前（spec 标注 line 1681）。插入 spec §4.13 第一个代码块（步骤 0）。

- [ ] **Step 5: 补 §8.3 自治退出二次确认**

位置：`_exit_autonomous_mode` 调用之后（spec 标注 line 1862-1867）。插入 spec §4.13 第二个代码块。

- [ ] **Step 6: 改写 §8.3 自治模式表"轮次推进"行**

位置：自治模式表"轮次推进"行（spec 标注 line 1843）。替换为：`轮次推进 | 自治期 Worker 可 CAS 写 status.json.current_turn（仅此一字段；自治退出后由 Director 接管，见 §3.3.3 字段属性表例外） | 时间片轮转：每个 Worker 按 agent_id 字典序轮转，每片 30 秒`

- [ ] **Step 7: 补 §8.3 自治模式表"锁管理"行 trade-off 注释**

位置：自治模式表"锁管理"行（spec 标注 line 1842）。在原描述后补：`已知 trade-off：自治期锁恢复时间 = TTL + grace_period（默认 32 秒），慢于正常模式的即时 emergency_release。建议自治期将 TTL 缩短为 10 秒（heartbeat.timeout_seconds / 3），通过更频繁续期补偿。`

- [ ] **Step 8: 自验**

运行 Grep 验收：
- `grep '改用单调时钟' design.md` 命中 0 次
- `grep '放大 grace_period 至 drift_offset' design.md` 命中 1 次
- `grep '暂停锁强制释放' design.md` 命中 1 次
- `grep 'watchdog_state.json' design.md` 命中 1+ 次
- `grep '检查 snapshots/' design.md` 命中 1 次
- `grep 'autonomous_exit_rollback' design.md` 命中 1 次
- `grep '自治期 Worker 可 CAS 写 status.json.current_turn' design.md` 命中 1 次

- [ ] **Step 9: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 4 - 可靠性补完（时钟漂移 + 快照恢复 + 自治退出二次确认）"
```

---

## Task 5: 错误处理重构（§11.1 / §11.2 / §11.3）

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` §11.1 异常分类 + §11.2 错误处理策略 + §11.3 分层阻断策略

**Interfaces:**
- Consumes: Step 1-4 全部章节（§11.2 引用所有章节的异常场景）
- Produces: §11.1 新增 8 个异常类（6 个主类 + 2 个 A2A 占位类）+ §11.2 拆三子表 + §11.3 降级为原则章

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.20（3 个修订子项）+ §4.21（整章重组）+ §4.22（整章降级）

**依赖:** Task 4（可靠性补完，§11.2 引用 §8.2/§8.3 场景）

- [ ] **Step 1: 补 §11.1 新增 8 个异常类**

位置：§11.1 现有异常类定义之后。插入 spec §4.20 第一个代码块（DirectorSignatureError / SchemaValidationError / DiskFullError / ReadOnlyFileSystemError / ClockDriftError / RecoveryFenceTimeoutError / LockForceReleaseError + A2AGatewayError / A2ATaskStateTransitionError）。注意 `ProtocolValidationError` 改名为 `SchemaValidationError`，保留旧名作 alias。

- [ ] **Step 2: 补 §11.1 _CATEGORY_ZH 补全**

位置：_CATEGORY_ZH 字典内。插入 spec §4.20 第二个代码块（8 个新 category 中文名）。

- [ ] **Step 3: 补 §11.1 ErrorStage 枚举扩展声明**

位置：ErrorStage 枚举定义附近。插入 spec §4.20 第三个代码块（声明 PROTOCOL 值）。

- [ ] **Step 4: 重组 §11.2 整章（拆三子表）**

将 §11.2 现有单一表格替换为 5 个子章节：
- §11.2.1 异常类处理表（含阻断层列，20+ 行）
- §11.2.2 字段标记表
- §11.2.3 系统响应动作表
- §11.2.4 阻断层定义小表（4 行）
- §11.2.5 重试预算与"相同失败"判定

完整内容见 spec §4.21。**注意**：每个异常类的 audit action 必须用 13 粗粒度枚举之一 + `details.reason` 细分。

- [ ] **Step 5: 降级 §11.3 整章**

将 §11.3 现有 37 行总表替换为原则章（4 层阻断定义 + 设计原则 + 指向 §11.2.x 子表的引用）。完整内容见 spec §4.22。

- [ ] **Step 6: 自验**

运行 Grep 验收：
- `grep '#### 11.2' design.md` 命中 5 次（11.2.1-11.2.5）
- `grep 'DirectorSignatureError' design.md` 命中 2+ 次（定义 + 引用）
- `grep 'SchemaValidationError' design.md` 命中 2+ 次
- `grep '统一 15 项修订' design.md` 命中 0 次（§11.3 总表已删除）
- `grep 'file_delete/mcp_call' design.md` 命中 0 次（危险工具清单对齐）
- `grep 'execute_command/write_file/call_tool' design.md` 命中 1+ 次
- 抽样检查 5 处 `action=XXX, details.reason=YYY` 格式，XXX 必须在 13 枚举内

- [ ] **Step 7: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 5 - 错误处理重构（8 异常类 + 三子表 + §11.3 降级）"
```

---

## Task 6: ReactLoop 集成补完（§10.6）

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` §10.6 ReactLoop 集成

**Interfaces:**
- Consumes: Step 5 的异常类定义（CapabilityNotInCardError / NotMyTurnError / DirectorUnavailableError）+ Step 1 的 InjectionIsolator
- Produces: §10.6 集成点 2/3/4/5/6/7 完整定义（从原 4 个集成点扩展为 7 个）

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.19（5 个修订子项）

**依赖:** Task 5（异常类定义）

- [ ] **Step 1: 改集成点 2：capabilities 校验下沉 ToolExecutor**

删除 §10.6 集成点 2 中 ReactLoop._execute_tool 的 capabilities 校验代码，替换为 spec §4.19 第一个代码块（ToolExecutor.evaluate_policy 新增 multiagent capabilities 校验 + __init__ 新增 multiagent_state 参数）。

- [ ] **Step 2: 改集成点 3/4：改 SessionManager 注册钩子**

将原 ReactLoop._on_session_start / _on_session_end 替换为 spec §4.19 第二个代码块（SessionManager._multiagent_hooks 机制）。

- [ ] **Step 3: 补集成点 5：发言前轮次校验**

插入 spec §4.19 第三个代码块（ReactLoop._before_speak 方法，freeform 模式不阻断，非 freeform 写 pending 队列）。

- [ ] **Step 4: 补集成点 6：Director 心跳监测后台任务**

插入 spec §4.19 第四个代码块（ReactLoop._start_director_heartbeat_monitor 方法）。

- [ ] **Step 5: 补集成点 7：LLM 上下文消息隔离**

插入 spec §4.19 第五个代码块（ReactLoop._build_chat_messages 方法 + __init__ 注入 InjectionIsolator）。

- [ ] **Step 6: 自验**

运行 Grep 验收：
- `grep '集成点 5' design.md` 命中 1+ 次
- `grep '集成点 6' design.md` 命中 1+ 次
- `grep '集成点 7' design.md` 命中 1+ 次
- `grep '_before_speak' design.md` 命中 1+ 次
- `grep '_start_director_heartbeat_monitor' design.md` 命中 1+ 次
- `grep '_build_chat_messages' design.md` 命中 1+ 次
- `grep '_multiagent_hooks' design.md` 命中 1+ 次
- `grep 'multiagent_state' design.md` 命中 2+ 次（ToolExecutor 注入 + 引用）

- [ ] **Step 7: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 6 - ReactLoop 集成补完（集成点 2-7）"
```

---

## Task 7: 配置与容器（§10.0 / §10.2 / §10.3 / §10.4 / §10.5）

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` §10.0 依赖更新 + §10.2 配置段 + §10.3 配置热更新边界 + §10.4 容器注册映射 + §10.5 REST 端点

**Interfaces:**
- Consumes: 无（独立任务，spec §5.3 标注可与 Step 6 并行）
- Produces: §10.0 依赖表 + §10.2 multiagent 配置段 + §10.3 _RESTART_REQUIRED_KEYS + §10.4 CONFIG_TO_COMPONENTS + §10.5 认证机制

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.14-§4.18（5 个章节修订）

**依赖:** 无（可与 Task 6 并行，但文件冲突需串行）

- [ ] **Step 1: 改 §10.0 依赖表**

将原 6 项依赖替换为 spec §4.14 的新依赖表（6 项，含 watchdog / aiofiles / portalocker / cryptography / pyyaml / jsonschema）。

- [ ] **Step 2: 补 §10.2 multiagent 段新增配置项**

插入 spec §4.15 第一个代码块（multiagent.worker.dangerous_tools / cas.merge_on_exhausted / watchdog.reset_to_watchdog）。

- [ ] **Step 3: 补 §10.2 末尾 config_helpers 校验说明**

插入 spec §4.15 第二个说明（修改 hermes/config_helpers.py:118）。

- [ ] **Step 4: 改 §10.3 _RESTART_REQUIRED_KEYS**

将原"新增 3 项"改为"仅新增 1 项 `multiagent.blackboard_dir`"，其余 2 项改热重载。插入 spec §4.16 的注（8 项说明）。

- [ ] **Step 5: 改 §10.4 CONFIG_TO_COMPONENTS["multiagent"]**

将 `"audit_logger"` 改为 `"multiagent_audit_logger"`。

- [ ] **Step 6: 补 §10.4 容器注册位置 + hot_reloadable 标志**

插入 spec §4.17 第二个代码块（注册位置 hermes/lifespan.py + 注册顺序 + hot_reloadable 标志 + validate() 调用）。

- [ ] **Step 7: 补 §10.5 认证机制说明**

插入 spec §4.18 的说明（security.api_key 认证 + a2a_gateway 叠加 + 路由注册位置）。

- [ ] **Step 8: 自验**

运行 Grep 验收：
- `grep 'multiagent_audit_logger' design.md` 命中 2+ 次（CONFIG_TO_COMPONENTS + 注册键）
- `grep 'multiagent.blackboard_dir' design.md` 命中 1+ 次（_RESTART_REQUIRED_KEYS）
- `grep 'multiagent.a2a_gateway.listen_port' design.md` 在 _RESTART_REQUIRED_KEYS 段内命中 0 次
- `grep 'portalocker' design.md` 命中 2+ 次（依赖表 + §3.3.7）
- `grep 'hermes/lifespan.py' design.md` 命中 1+ 次
- `grep 'dangerous_tools' design.md` 命中 1+ 次

- [ ] **Step 9: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 7 - 配置与容器（依赖 + 配置段 + 热更新 + 容器 + REST）"
```

---

## Task 8: audit action 统一（全文替换 40+ 细粒度值）

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` 全文（所有 `action=XXX` 引用）

**Interfaces:**
- Consumes: Step 1 的 §3.3.7 audit action 字段定义（13 粗粒度枚举 + details.reason）
- Produces: 全文 audit action 统一为 13 枚举 + details.reason 细分

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.6 + P0-8（O1 默认值）

**依赖:** Task 1（§3.3.7 字段定义已完成）

- [ ] **Step 1: 全文搜索所有 audit action 引用**

用 Grep 搜索 `action=[a-z_]+` 找出全文所有 audit action 值。

- [ ] **Step 2: 识别不在 13 枚举中的值**

13 合法枚举：write / heartbeat / turn_advance / turn_timeout / lock_acquire / lock_release / lock_force_release / register / leave / arbitrate / snapshot / recovery_start / recovery_end

逐一比对，找出非法值（如 `director_lock_preempt` / `lock_force_release_aborted` / `autonomous_exit_rollback` / `quota_exceeded` / `injection_suspected` / `message_truncated` 等）。

- [ ] **Step 3: 逐个替换非法值为合法枚举 + details.reason**

对每个非法值，按 O1 默认值替换：
- `director_lock_preempt` → `lock_force_release, details.reason="holder_presumed_dead"`
- `lock_force_release_aborted` → `lock_force_release, details.reason="holder_renewed"`
- `autonomous_exit_rollback` → `recovery_start, details.reason="autonomous_exit_rollback"`
- `quota_exceeded` → `write, details.reason="quota_exceeded"`
- `injection_suspected` → `write, details.reason="injection_suspected"`
- `message_truncated` → `write, details.reason="message_truncated"`
- 其他非法值按语义归入 13 枚举之一 + details.reason

- [ ] **Step 4: 自验**

运行 Grep 验收：
- `grep 'action=director_lock_preempt' design.md` 命中 0 次
- `grep 'action=lock_force_release_aborted' design.md` 命中 0 次
- `grep 'action=autonomous_exit_rollback' design.md` 命中 0 次
- `grep 'action=quota_exceeded' design.md` 命中 0 次
- `grep 'action=injection_suspected' design.md` 命中 0 次
- `grep 'action=message_truncated' design.md` 命中 0 次
- 抽样检查 5 处 `action=XXX, details.reason=YYY`，XXX 必须在 13 枚举内

- [ ] **Step 5: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 8 - audit action 统一为 13 粗粒度枚举 + details.reason"
```

---

## Task 9: §17 修订日志 + §4.4 心跳阈值统一

**Files:**
- Modify: `hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` §17 遗留问题与 v1.1 计划 + §4.4 心跳三阶段阈值表 + 顶部 revision_notes

**Interfaces:**
- Consumes: Step 1-8 全部修订内容（§17 记录所有变更）
- Produces: v1.0.3 修订日志 + §4.4 心跳阈值对齐 §8.2 + 顶部版本号升级

**Spec 参考:** `2026-07-21-多agent协作机制-v1.0.3修订-spec.md` §4.10 + §4.23

**依赖:** Task 2-8 全部完成

- [ ] **Step 1: 改 §4.4 心跳三阶段阈值表**

查找 design.md §4.4 中的心跳三阶段阈值表（spec 标注 line 1116-1118），替换为：
```
| 健康 | age < 2×interval | active |
| 降级 | 2×interval ≤ age < timeout | degraded |
| 离线 | age ≥ timeout | offline（autonomous） |
```

- [ ] **Step 2: 补 §17 v1.0.3 修订日志**

位置：§17 章节顶部。插入 spec §4.23 第一个代码块（v1.0.3 修订日志，含 grill-me 6 项决策 + 交叉验证 49 项）。

- [ ] **Step 3: 补 §17.1 P1 项扩展**

位置：§17.1 P1 计划表。按 spec §4.23 第二个修订项扩展 P1 项 5/6/9 + 新增 3 项（P2-4 / P2-5 / P1-18）。

- [ ] **Step 4: 升级顶部版本号**

查找 design.md 顶部的 revision_notes 或版本号字段，新增 v1.0.3 条目。

- [ ] **Step 5: 自验**

运行 Grep 验收：
- `grep 'v1.0.3 修订日志' design.md` 命中 1 次
- `grep '| 健康 | age < 2×interval | active |' design.md` 命中 1 次
- `grep 'online' design.md` 在 §4.4 心跳表内命中 0 次（已改为 active）
- design.md 顶部 revision_notes 含 v1.0.3 条目

- [ ] **Step 6: Commit**

```bash
git add hermes-lite/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md
git commit -m "docs(multiagent): v1.0.3 Step 9 - 修订日志 + 心跳阈值统一 + 版本号升级"
```

---

## 统一验收（所有任务完成后）

执行 spec §6.2 整体验收标准全量检查：

- [ ] **验收 1: 版本号已升级** — design.md 顶部 revision_notes 含 v1.0.3 条目

- [ ] **验收 2: 硬约束对齐** — 全文 grep 以下均命中 0 次：
  - `改用单调时钟`
  - `file_delete/mcp_call`
  - `${HERMES_BB_DIR:`

- [ ] **验收 3: audit action 枚举对齐** — 全文 grep 以下均命中 0 次：
  - `action=director_lock_preempt`
  - `action=lock_force_release_aborted`
  - `action=autonomous_exit_rollback`
  - `action=quota_exceeded`

- [ ] **验收 4: line 557-559 段落改写** — grep 'Worker 不直接写 status.json' 命中 0 次；grep 'Director 是 status.json 主要写者' 命中 1 次

- [ ] **验收 5: Q1-Q6 全部落地** — 6 项决策在文档中均有对应内容：
  - Q1: §3.3.3 字段属性表
  - Q2: messages.pending.md / messages.replay_candidates.md 子 schema
  - Q3: §11.1 新增 8 异常类
  - Q4: §11.2 拆三子表 + §11.3 降级
  - Q5: §8.2 无"改用单调时钟"
  - Q6: §3.3.3 字段属性表含 CAS 合并策略列

- [ ] **验收 6: P0 全部修复** — 15 项 P0 在文档中均有对应内容

- [ ] **验收 7: P1 全部修复** — 22 项 P1 在文档中均有对应内容

- [ ] **验收 8: P2 全部记录** — 12 项 P2 在 §17.1 P1 计划列均有对应条目

---

## 子智能体委派说明

每个 Task 委派一个 `general_purpose_task` 子智能体执行，委派时需提供：

1. **任务编号与标题**（如 "Task 2: 字段属性表"）
2. **spec 文档路径**：`e:\Java\webser\web_app\webme\hermes-lite\docs\superpowers\specs\2026-07-21-多agent协作机制-v1.0.3修订-spec.md`
3. **design.md 路径**：`e:\Java\webser\web_app\webme\hermes-lite\docs\superpowers\specs\2026-07-20-多agent协作机制-design.md`
4. **spec 中的修订详情章节号**（如 §4.3）
5. **本计划中的 Task 步骤**（完整复制给子智能体）
6. **验收 Grep 命令**（子智能体执行后必须全部通过）
7. **Commit 命令**（子智能体执行修订 + 自验通过后 commit）

子智能体执行流程：
1. 读取 spec 对应章节，获取完整修订内容
2. 读取 design.md 对应位置，确认当前内容
3. 按 Task 步骤逐项 Edit
4. 执行自验 Grep 命令，全部通过后 commit
5. 返回执行摘要（修订项数 + 自验结果 + commit hash）

**串行约束**：由于所有修订都在 design.md 上，子智能体必须串行执行（一个完成后再委派下一个）。

---

## Self-Review

### 1. Spec 覆盖率

| spec 章节 | 对应 Task | 覆盖 |
|-----------|----------|------|
| §4.1 §3.0 | Step 1（已完成） | ✅ |
| §4.2 §3.3.2 | Step 1（已完成） | ✅ |
| §4.3 §3.3.3 字段属性表 + line 557-559 | Task 2 | ✅ |
| §4.4 §3.3.4 | Step 1（已完成） | ✅ |
| §4.5 §3.3.5 | Step 1（已完成） | ✅ |
| §4.6 §3.3.7 | Step 1（已完成） + Task 8（action 统一） | ✅ |
| §4.7 §3.5 | Step 1（已完成） | ✅ |
| §4.8 §3.6 | Step 1（已完成） | ✅ |
| §4.9 §3.7 | Step 1（已完成） | ✅ |
| §4.10 §4.4 心跳阈值 | Task 9 | ✅ |
| §4.11 §7.1 锁机制 | Task 3 | ✅ |
| §4.12 §8.2 故障检测 | Task 4 | ✅ |
| §4.13 §8.3 故障恢复 | Task 4 | ✅ |
| §4.14 §10.0 依赖 | Task 7 | ✅ |
| §4.15 §10.2 配置段 | Task 7 | ✅ |
| §4.16 §10.3 热更新 | Task 7 | ✅ |
| §4.17 §10.4 容器 | Task 7 | ✅ |
| §4.18 §10.5 REST | Task 7 | ✅ |
| §4.19 §10.6 ReactLoop | Task 6 | ✅ |
| §4.20 §11.1 异常类 | Task 5 | ✅ |
| §4.21 §11.2 三子表 | Task 5 | ✅ |
| §4.22 §11.3 降级 | Task 5 | ✅ |
| §4.23 §17 修订日志 | Task 9 | ✅ |

所有 spec §4 章节均有对应 Task，覆盖率 100%。

### 2. 占位符扫描

- 无 "TBD" / "TODO" / "implement later"
- 无 "Add appropriate error handling"
- 无 "Similar to Task N"（每个 Task 步骤都引用 spec 具体章节）
- 无未定义的类型/方法引用

### 3. 类型一致性

- `multiagent_audit_logger` 在 Task 7 §10.4 定义，统一验收引用一致
- `multiagent.blackboard_dir` 在 Task 7 §10.3 定义，统一验收引用一致
- `_before_speak` / `_start_director_heartbeat_monitor` / `_build_chat_messages` 在 Task 6 定义，自验引用一致
- 13 audit action 枚举在 Task 8 定义，统一验收引用一致
