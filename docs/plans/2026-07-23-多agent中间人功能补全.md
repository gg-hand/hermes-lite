# 多 Agent 中间人功能补全 Implementation Plan

> **For agentic workers:** Use the java-dev-skills workflow to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Director 中间人功能可用——用户可通过 @director 指令或分派按钮提交任务，协作消息在聊天流实时展示。

**Architecture:** 后端新增 POST /api/multiagent/dispatch 写入黑板 messages.md；前端在 sendMessage() 检测 @director 前缀走 dispatch 路径；分派按钮弹出对话框提交；监听 SSE message_append 事件渲染协作气泡。

**Tech Stack:** Python FastAPI（后端），原生 HTML/CSS/JS（前端），无 JS 测试框架（浏览器手动验证）

## Global Constraints

- 项目路径根：`teage-liu/`
- 后端路径：`teage_liu/api/multiagent_routes.py`
- 前端路径：`web/static/js/chat-core.js`、`web/static/js/chat-main.js`、`web/chat.html`、`web/css/multiagent.css`、`web/js/multiagent-render.js`
- CSS 变量复用 `tokens.css`
- 无 JS 测试框架——每个 Task 的"测试"为浏览器/API 手动验证
- 提交粒度：每个 Task 一个 commit
- 不改动 multiagent-sse.js、multiagent-settings.js、director_engine.py

## File Structure

| 文件 | 职责 | 改动类型 |
|------|------|---------|
| `teage_liu/api/multiagent_routes.py` | 新增 POST /dispatch 端点 | 改 |
| `web/static/js/chat-core.js` | @director 检测 + dispatchToDirector() | 改 |
| `web/static/js/chat-main.js` | 分派按钮绑定 + multiagent-message 监听 | 改 |
| `web/chat.html` | 分派对话框 modal 骨架 | 改 |
| `web/css/multiagent.css` | 分派对话框样式 + 消息记录样式 | 改 |
| `web/js/multiagent-render.js` | renderMessageLog() 消息记录渲染 | 改 |

---

### Task 1: 后端 POST /api/multiagent/dispatch 端点

**Files:**
- Modify: `teage_liu/api/multiagent_routes.py:37-172`（在 create_multiagent_router 内新增 POST 端点）

**Interfaces:**
- Produces: `POST /api/multiagent/dispatch`，接收 `{task, target_agents?, mode?}`，返回 `{ok, op_id, message}`
- Consumes: `teage_liu.multiagent.blackboard.append_message`

- [ ] **Step 1: 确认当前状态（验证失败基线）**

启动服务器，在控制台执行：
```js
fetch('/api/multiagent/dispatch', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({task:'test'})}).then(r=>r.json()).then(console.log)
```
确认：返回 404 或 405（端点不存在）

- [ ] **Step 2: 在 multiagent_routes.py 新增 POST /dispatch 端点**

编辑 `teage_liu/api/multiagent_routes.py`，在 `@router.get("/sse")` 之前（约 L128）插入：

```python
    @router.post("/dispatch")
    async def dispatch_task(payload: dict) -> dict:
        """提交任务到黑板，Director 引擎自动 pickup。

        请求体：
            task: 任务描述（必填）
            target_agents: 目标 agent_id 列表（可选，空则广播）
            mode: 协作模式（可选，dispatch/relay/debate，默认 dispatch）

        返回：
            ok: 是否成功
            op_id: 操作 ID
            message: 描述信息
        """
        import uuid
        from teage_liu.multiagent.blackboard import append_message, append_audit

        task = (payload.get("task") or "").strip()
        if not task:
            raise HTTPException(status_code=400, detail="task 不能为空")

        target_agents = payload.get("target_agents") or []
        mode = payload.get("mode") or "dispatch"

        op_id = str(uuid.uuid4())
        ts = datetime.now(timezone.utc).isoformat()

        message = {
            "op_id": op_id,
            "from": "user",
            "type": "task",
            "content": task,
            "target_agents": target_agents,
            "mode": mode,
            "ts": ts,
        }

        await append_message(bb_root, message)

        await append_audit(
            bb_root,
            {
                "ts": ts,
                "actor": "user",
                "action": "dispatch_task",
                "target": ", ".join(target_agents) if target_agents else "broadcast",
                "op_id": op_id,
                "details": {"task": task[:200], "mode": mode},
                "prev_hash": "",
                "hash": "",
                "signature": "",
            },
        )

        logger.info("任务已分派: op_id=%s, task=%s, targets=%s", op_id, task[:50], target_agents)

        return {
            "ok": True,
            "op_id": op_id,
            "message": "任务已提交到黑板" if not target_agents else f"任务已分派给 {', '.join(target_agents)}",
        }
```

- [ ] **Step 3: 验证端点可用**

重启服务器，在浏览器控制台执行：
```js
fetch('/api/multiagent/dispatch', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({task:'测试任务',target_agents:[],mode:'dispatch'})}).then(r=>r.json()).then(console.log)
```

确认：
- 返回 `{ok: true, op_id: "xxx", message: "任务已提交到黑板"}`
- 如果 multiagent 未启用：返回 404

- [ ] **Step 4: Commit**

```bash
cd teage-liu
git add teage_liu/api/multiagent_routes.py
git commit -m "feat: add POST /api/multiagent/dispatch endpoint"
```

---

### Task 2: @director 指令检测 + dispatchToDirector 函数

**Files:**
- Modify: `web/static/js/chat-core.js:1462-1477`（sendMessage 中插入检测）
- Modify: `web/static/js/chat-core.js`（文件末尾新增 dispatchToDirector 函数）

**Interfaces:**
- Produces: `dispatchToDirector(task)` 函数，POST 到 /api/multiagent/dispatch
- Consumes: Task 1 的 POST /dispatch 端点；现有 `appendMessage()` 和 `showToast()`

- [ ] **Step 1: 确认当前状态（验证失败基线）**

在聊天输入框输入 `@director 测试`，发送。确认：消息走普通 /chat/stream 流程，无 dispatch 行为。

- [ ] **Step 2: 在 sendMessage 中插入 @director 检测**

编辑 `web/static/js/chat-core.js`，在 `if (!text) return;`（L1464）之后、`if (streamState === StreamState.STREAMING)`（L1466）之前插入：

```javascript
  // @director 指令检测：走多 agent 分派路径
  var lowerText = text.toLowerCase();
  if (lowerText.startsWith('@director ') || lowerText.startsWith('@director\u3000')) {
    var taskText = text.slice(text.indexOf(' ') + 1).trim();
    if (!taskText) {
      showToast('请在 @director 后输入任务描述', 'warning');
      return;
    }
    return dispatchToDirector(taskText, text);
  }
```

- [ ] **Step 3: 在 chat-core.js 末尾新增 dispatchToDirector 函数**

在 `web/static/js/chat-core.js` 文件末尾追加：

```javascript

// ========== 多 Agent 分派 ==========

/**
 * 通过 @director 指令分派任务到多 agent 黑板。
 * @param {string} task - 任务描述
 * @param {string} originalText - 原始输入文本（含 @director 前缀）
 */
async function dispatchToDirector(task, originalText) {
  // 显示用户原始消息
  appendMessage('user', originalText);
  scrollMessagesToBottom(true);
  messageInputEl.value = '';
  autoResize();

  // 显示 Director 接收气泡
  var collabEl = appendCollabMessage('🎯 Director', '已接收任务，正在分派…');

  try {
    var res = await fetch(API_BASE + '/api/multiagent/dispatch', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ task: task, target_agents: [], mode: 'dispatch' }),
    });

    if (res.status === 404) {
      updateCollabMessage(collabEl, '🎯 Director', '❌ 多 agent 未启用，请在设置中开启', 'error');
      showToast('多 agent 未启用，请在设置中开启', 'warning');
      return;
    }

    var data = await res.json();
    if (data.ok) {
      updateCollabMessage(collabEl, '🎯 Director', '已分派，等待 agent 响应… (op_id: ' + data.op_id.slice(0, 8) + ')', 'success');
      showToast(data.message || '任务已分派', 'success');
    } else {
      throw new Error(data.detail || '分派失败');
    }
  } catch (err) {
    updateCollabMessage(collabEl, '🎯 Director', '❌ 分派失败: ' + err.message, 'error');
    showToast('分派失败: ' + err.message, 'error');
  }
}

/**
 * 追加协作消息气泡到聊天流。
 * @param {string} role - 角色标签（如 "🎯 Director"）
 * @param {string} content - 消息内容
 * @returns {HTMLElement} 气泡元素（可用于后续更新）
 */
function appendCollabMessage(role, content) {
  var messagesEl = document.getElementById('messages');
  if (!messagesEl) return null;

  var msg = document.createElement('div');
  msg.className = 'msg collab';
  msg.innerHTML =
    '<div class="role">' + escapeHtml(role) + '</div>' +
    '<div class="bubble">' + escapeHtml(content) + '</div>';
  messagesEl.appendChild(msg);
  scrollMessagesToBottom(true);
  return msg;
}

/**
 * 更新已有协作气泡的内容。
 * @param {HTMLElement} el - appendCollabMessage 返回的元素
 * @param {string} role - 角色标签
 * @param {string} content - 新内容
 * @param {string} level - 级别（success/error/info）
 */
function updateCollabMessage(el, role, content, level) {
  if (!el) return;
  if (level) el.classList.add('collab-' + level);
  var roleEl = el.querySelector('.role');
  var bubbleEl = el.querySelector('.bubble');
  if (roleEl) roleEl.textContent = role;
  if (bubbleEl) bubbleEl.textContent = content;
}
```

- [ ] **Step 4: 验证 @director 检测**

刷新浏览器，在输入框输入 `@director 帮我对比三个方案`，发送。确认：
- 用户消息显示"@director 帮我对比三个方案"
- 紫色 collab 气泡出现："🎯 Director · 已接收任务，正在分派…"
- 气泡更新为"已分派，等待 agent 响应… (op_id: xxx)"
- 普通消息（不带 @director）仍走 /chat/stream

- [ ] **Step 5: Commit**

```bash
git add web/static/js/chat-core.js
git commit -m "feat: add @director prefix detection and dispatchToDirector"
```

---

### Task 3: 分派任务按钮 — 对话框 + 提交逻辑

**Files:**
- Modify: `web/chat.html`（新增分派对话框 modal）
- Modify: `web/css/multiagent.css`（分派对话框样式）
- Modify: `web/static/js/chat-main.js`（绑定按钮 + 对话框逻辑）

**Interfaces:**
- Produces: `#dispatchModal`（分派对话框）、`openDispatchDialog()` 函数
- Consumes: Task 1 的 POST /dispatch；`/api/multiagent/agents` 获取 agent 列表

- [ ] **Step 1: 确认当前状态（验证失败基线）**

点击工作台的"分派任务"按钮。确认：无任何反应（无 click 事件）。

- [ ] **Step 2: 在 chat.html 新增分派对话框 modal**

编辑 `web/chat.html`，在 `</main>` 之后、现有 modal 之前（约 L183）插入：

```html
    <!-- 分派任务对话框 -->
    <div class="modal-overlay" id="dispatchModal" style="display:none">
      <div class="modal-card dispatch-card">
        <div class="modal-header">
          <h3>分派任务</h3>
          <button class="modal-close" id="dispatchCloseBtn">&times;</button>
        </div>
        <div class="modal-body">
          <div class="form-group">
            <label>任务描述</label>
            <textarea class="form-input dispatch-task-input" id="dispatchTaskInput" rows="4" placeholder="描述你要分派给 agent 的任务…"></textarea>
          </div>
          <div class="form-group">
            <label>目标 Agent（可多选，空则广播）</label>
            <div class="dispatch-agent-list" id="dispatchAgentList">
              <p class="wb-empty">加载中…</p>
            </div>
          </div>
          <div class="form-group">
            <label>协作模式</label>
            <div class="dispatch-mode-row">
              <label class="dispatch-mode-opt"><input type="radio" name="dispatchMode" value="dispatch" checked><span>分派</span></label>
              <label class="dispatch-mode-opt"><input type="radio" name="dispatchMode" value="relay"><span>接力</span></label>
              <label class="dispatch-mode-opt"><input type="radio" name="dispatchMode" value="debate"><span>辩论</span></label>
            </div>
          </div>
        </div>
        <div class="modal-footer">
          <button class="wb-btn wb-btn-ghost" id="dispatchCancelBtn">取消</button>
          <button class="wb-btn wb-btn-primary" id="dispatchSubmitBtn" disabled>提交分派</button>
        </div>
      </div>
    </div>
```

- [ ] **Step 3: 在 multiagent.css 追加分派对话框样式**

编辑 `web/css/multiagent.css`，在文件末尾追加：

```css
/* ========== 分派任务对话框 ========== */
.dispatch-card {
  max-width: 480px;
  width: 90%;
}
.dispatch-card .modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 18px;
  border-bottom: 1px solid var(--border);
}
.dispatch-card .modal-header h3 {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0;
}
.modal-close {
  background: transparent;
  border: none;
  font-size: 18px;
  color: var(--text-muted);
  cursor: pointer;
  padding: 0 4px;
  line-height: 1;
}
.modal-close:hover { color: var(--text-primary); }
.dispatch-card .modal-body {
  padding: 16px 18px;
  max-height: 60vh;
  overflow-y: auto;
}
.dispatch-card .form-group {
  margin-bottom: 14px;
}
.dispatch-card .form-group label {
  display: block;
  font-size: 11px;
  font-weight: 600;
  color: var(--text-secondary);
  margin-bottom: 6px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}
.dispatch-task-input {
  width: 100%;
  resize: vertical;
  min-height: 80px;
  font-family: inherit;
  font-size: 13px;
  padding: 8px 10px;
  border-radius: var(--radius-lg);
  border: 1px solid var(--border);
  background: var(--bg-tertiary, var(--bg-secondary));
  color: var(--text-primary);
  box-sizing: border-box;
}
.dispatch-task-input:focus {
  outline: none;
  border-color: var(--accent);
}
.dispatch-agent-list {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  max-height: 120px;
  overflow-y: auto;
  padding: 6px;
  border-radius: var(--radius-lg);
  border: 1px solid var(--border);
  background: var(--bg-tertiary, var(--bg-secondary));
}
.dispatch-agent-chip {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 8px;
  border-radius: 9999px;
  font-size: 11px;
  cursor: pointer;
  user-select: none;
  border: 1px solid var(--border);
  background: var(--bg-secondary);
  color: var(--text-secondary);
  transition: all 0.15s;
}
.dispatch-agent-chip:hover { border-color: var(--accent); }
.dispatch-agent-chip.selected {
  background: rgba(168,85,247,0.12);
  border-color: var(--accent);
  color: var(--accent);
}
.dispatch-mode-row {
  display: flex;
  gap: 12px;
}
.dispatch-mode-opt {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 12px;
  color: var(--text-secondary);
  cursor: pointer;
}
.dispatch-mode-opt input { margin: 0; }
.dispatch-card .modal-footer {
  display: flex;
  gap: 8px;
  padding: 12px 18px;
  border-top: 1px solid var(--border);
}
.dispatch-card .modal-footer .wb-btn { flex: 1; }

/* collab 气泡级别 */
.msg.collab.collab-error .bubble { border-color: rgba(239,68,68,0.3); background: rgba(239,68,68,0.06); }
.msg.collab.collab-success .bubble { border-color: rgba(34,197,94,0.25); background: rgba(34,197,94,0.06); }
```

- [ ] **Step 4: 在 chat-main.js 绑定分派按钮和对话框逻辑**

编辑 `web/static/js/chat-main.js`，在工作台收起按钮代码之后（Task 4 的 `wbCollapseBtn` 块之后）追加：

```javascript
  // 分派任务对话框
  var dispatchBtn = document.getElementById('wbDispatchBtn');
  var dispatchModal = document.getElementById('dispatchModal');
  var dispatchTaskInput = document.getElementById('dispatchTaskInput');
  var dispatchAgentList = document.getElementById('dispatchAgentList');
  var dispatchSubmitBtn = document.getElementById('dispatchSubmitBtn');
  var selectedAgents = [];

  function openDispatchDialog() {
    if (!dispatchModal) return;
    dispatchModal.style.display = 'flex';
    // 加载 agent 列表
    dispatchAgentList.innerHTML = '<p class="wb-empty">加载中…</p>';
    selectedAgents = [];
    fetch('/api/multiagent/agents')
      .then(function(r) { return r.ok ? r.json() : { agents: [] }; })
      .then(function(data) {
        var agents = data.agents || [];
        if (agents.length === 0) {
          dispatchAgentList.innerHTML = '<p class="wb-empty">暂无可用 agent（将广播）</p>';
          return;
        }
        dispatchAgentList.innerHTML = agents.map(function(a) {
          return '<span class="dispatch-agent-chip" data-agent-id="' + (a.agent_id || '') + '">' +
                 (a.agent_id || 'unknown') + ' · ' + (a.role || 'worker') + '</span>';
        }).join('');
        // 绑定 chip 点击
        dispatchAgentList.querySelectorAll('.dispatch-agent-chip').forEach(function(chip) {
          chip.addEventListener('click', function() {
            var id = chip.dataset.agentId;
            chip.classList.toggle('selected');
            if (chip.classList.contains('selected')) {
              if (selectedAgents.indexOf(id) === -1) selectedAgents.push(id);
            } else {
              selectedAgents = selectedAgents.filter(function(x) { return x !== id; });
            }
          });
        });
      })
      .catch(function() {
        dispatchAgentList.innerHTML = '<p class="wb-empty">暂无可用 agent（将广播）</p>';
      });
  }

  function closeDispatchDialog() {
    if (dispatchModal) dispatchModal.style.display = 'none';
    if (dispatchTaskInput) dispatchTaskInput.value = '';
    selectedAgents = [];
  }

  if (dispatchBtn) {
    dispatchBtn.addEventListener('click', openDispatchDialog);
  }
  var dispatchCloseBtn = document.getElementById('dispatchCloseBtn');
  var dispatchCancelBtn = document.getElementById('dispatchCancelBtn');
  if (dispatchCloseBtn) dispatchCloseBtn.addEventListener('click', closeDispatchDialog);
  if (dispatchCancelBtn) dispatchCancelBtn.addEventListener('click', closeDispatchDialog);
  if (dispatchModal) {
    dispatchModal.addEventListener('click', function(e) {
      if (e.target === dispatchModal) closeDispatchDialog();
    });
  }

  // 提交分派
  if (dispatchSubmitBtn && dispatchTaskInput) {
    dispatchTaskInput.addEventListener('input', function() {
      dispatchSubmitBtn.disabled = !dispatchTaskInput.value.trim();
    });
    dispatchSubmitBtn.addEventListener('click', function() {
      var task = dispatchTaskInput.value.trim();
      if (!task) return;
      var mode = 'dispatch';
      var modeEl = document.querySelector('input[name="dispatchMode"]:checked');
      if (modeEl) mode = modeEl.value;

      dispatchSubmitBtn.disabled = true;
      dispatchSubmitBtn.textContent = '分派中…';

      fetch('/api/multiagent/dispatch', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ task: task, target_agents: selectedAgents, mode: mode }),
      })
        .then(function(r) { return r.json(); })
        .then(function(data) {
          if (data.ok) {
            closeDispatchDialog();
            if (typeof appendCollabMessage === 'function') {
              var target = selectedAgents.length > 0 ? selectedAgents.join(', ') : '广播';
              appendCollabMessage('🎯 Director', '已分派给 ' + target + '（' + mode + '模式）');
            }
            if (typeof showToast === 'function') showToast(data.message || '任务已分派', 'success');
          } else {
            throw new Error(data.detail || '分派失败');
          }
        })
        .catch(function(err) {
          if (typeof showToast === 'function') showToast('分派失败: ' + err.message, 'error');
        })
        .finally(function() {
          dispatchSubmitBtn.disabled = false;
          dispatchSubmitBtn.textContent = '提交分派';
        });
    });
  }
```

- [ ] **Step 5: 验证分派对话框**

刷新浏览器，展开 Director 工作台，点击"分派任务"按钮。确认：
- 对话框弹出，含任务输入框、agent 列表、协作模式选择
- 输入任务后"提交分派"按钮启用
- 点击 agent chip 可选中/取消
- 点击"取消"或遮罩层关闭对话框
- 提交后对话框关闭，聊天流出现 collab 气泡

- [ ] **Step 6: Commit**

```bash
git add web/chat.html web/css/multiagent.css web/static/js/chat-main.js
git commit -m "feat: add dispatch task dialog with agent selection and mode"
```

---

### Task 4: 协作消息监听 — SSE message_append → 聊天流气泡

**Files:**
- Modify: `web/static/js/chat-main.js`（新增 multiagent-message 事件监听器）

**Interfaces:**
- Consumes: SSE 的 `CustomEvent("multiagent-message")`；`appendCollabMessage()` from chat-core.js

- [ ] **Step 1: 确认当前状态（验证失败基线）**

在浏览器控制台执行：
```js
window.dispatchEvent(new CustomEvent('multiagent-message', { detail: { messages: [{ from: 'researcher-01', type: 'result', content: '测试结果' }] } }))
```
确认：聊天流无反应（无监听器）。

- [ ] **Step 2: 在 chat-main.js 新增 multiagent-message 监听器**

编辑 `web/static/js/chat-main.js`，在分派对话框代码之后追加：

```javascript
  // 监听 SSE 协作消息事件 → 渲染 collab 气泡
  window.addEventListener('multiagent-message', function(e) {
    var data = e.detail || {};
    var messages = data.messages || (data.message ? [data.message] : []);
    messages.forEach(function(msg) {
      if (!msg || !msg.content) return;
      var role = '';
      var icon = 'ℹ️';
      switch (msg.type) {
        case 'task':
          icon = '🎯';
          role = (msg.from || 'Director') + ' · 分派';
          break;
        case 'result':
          icon = '✅';
          role = (msg.from || 'Agent') + ' · 结果';
          break;
        case 'relay':
          icon = '🔁';
          role = (msg.from || 'Agent') + ' · 接力';
          break;
        default:
          icon = 'ℹ️';
          role = msg.from || '系统';
      }
      if (typeof appendCollabMessage === 'function') {
        appendCollabMessage(icon + ' ' + role, msg.content);
      }
    });
  });
```

- [ ] **Step 3: 验证协作消息监听**

刷新浏览器，在控制台执行：
```js
window.dispatchEvent(new CustomEvent('multiagent-message', { detail: { messages: [{ from: 'researcher-01', type: 'result', content: '方案A最优，因为…' }] } }))
```
确认：
- 聊天流出现紫色 collab 气泡："✅ researcher-01 · 结果 · 方案A最优，因为…"

- [ ] **Step 4: Commit**

```bash
git add web/static/js/chat-main.js
git commit -m "feat: listen for multiagent-message events and render collab bubbles"
```

---

### Task 5: 消息记录展示 — 工作台 Agent 名册 tab 增加"消息记录"

**Files:**
- Modify: `web/js/multiagent-render.js`（新增 renderMessageLog 函数）
- Modify: `web/chat.html`（Agent 名册 tab 增加消息记录折叠区）
- Modify: `web/css/multiagent.css`（消息记录样式）

**Interfaces:**
- Produces: `renderMessageLog(messages)` 函数
- Consumes: `/api/multiagent/messages` 端点

- [ ] **Step 1: 确认当前状态（验证失败基线）**

展开 Director 工作台，切换到"Agent名册"tab。确认：只有 agent 列表，无消息记录。

- [ ] **Step 2: 在 chat.html 的 Agent 名册 tab 增加消息记录折叠区**

编辑 `web/chat.html`，在 `<div class="agents-list" id="agentsList">...</div>` 之后（Agent 名册 tab 内）追加：

```html
          <div class="wb-msg-log-section">
            <button class="wb-msg-log-toggle" id="msgLogToggle">消息记录 ▸</button>
            <div class="wb-msg-log" id="msgLogContainer" style="display:none">
              <p class="wb-empty">点击加载…</p>
            </div>
          </div>
```

- [ ] **Step 3: 在 multiagent.css 追加消息记录样式**

编辑 `web/css/multiagent.css`，末尾追加：

```css
/* ========== 消息记录 ========== */
.wb-msg-log-section { margin-top: 14px; border-top: 1px solid var(--border); padding-top: 10px; }
.wb-msg-log-toggle {
  width: 100%;
  text-align: left;
  background: transparent;
  border: none;
  font-size: 11px;
  font-weight: 600;
  color: var(--accent);
  cursor: pointer;
  padding: 4px 0;
  font-family: inherit;
}
.wb-msg-log-toggle:hover { color: var(--accent-hover, var(--accent)); }
.wb-msg-log {
  margin-top: 8px;
  max-height: 240px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.wb-msg-item {
  padding: 6px 8px;
  border-radius: var(--radius-lg);
  background: var(--bg-tertiary, var(--bg-secondary));
  border: 1px solid var(--border);
  font-size: 11px;
}
.wb-msg-meta {
  display: flex;
  gap: 6px;
  margin-bottom: 2px;
}
.wb-msg-from { font-weight: 600; color: var(--accent); }
.wb-msg-type { color: var(--text-muted); }
.wb-msg-time { color: var(--text-muted); margin-left: auto; }
.wb-msg-content { color: var(--text-secondary); line-height: 1.4; word-break: break-word; }
```

- [ ] **Step 4: 在 multiagent-render.js 新增 renderMessageLog 函数**

编辑 `web/js/multiagent-render.js`，在 `showAlert` 函数之后、`window.MultiagentRender` 导出之前插入：

```javascript
  /**
   * 渲染消息记录到工作台。
   * @param {Array} messages - 消息列表
   */
  function renderMessageLog(messages) {
    var container = document.getElementById("msgLogContainer");
    if (!container) return;

    if (!messages || messages.length === 0) {
      container.innerHTML = '<p class="wb-empty">暂无消息记录</p>';
      return;
    }

    container.innerHTML = messages
      .slice(-50)
      .map(function (msg) {
        var ts = msg.ts || "";
        var tsShort = ts.length > 19 ? ts.substring(11, 19) : ts;
        return (
          '<div class="wb-msg-item">' +
          '<div class="wb-msg-meta">' +
          '<span class="wb-msg-from">' +
          escapeHtml(msg.from || "?") +
          "</span>" +
          '<span class="wb-msg-type">' +
          escapeHtml(msg.type || "") +
          "</span>" +
          '<span class="wb-msg-time">' +
          escapeHtml(tsShort) +
          "</span>" +
          "</div>" +
          '<div class="wb-msg-content">' +
          escapeHtml((msg.content || "").substring(0, 200)) +
          "</div>" +
          "</div>"
        );
      })
      .join("");
  }
```

然后在 `window.MultiagentRender` 导出对象中添加 `renderMessageLog`：

```javascript
  window.MultiagentRender = {
    initContainers: initContainers,
    updateStatus: updateStatus,
    renderAgents: renderAgents,
    showAlert: showAlert,
    renderMessageLog: renderMessageLog,
  };
```

- [ ] **Step 5: 在 chat-main.js 绑定消息记录展开/加载**

编辑 `web/static/js/chat-main.js`，在 multiagent-message 监听器之后追加：

```javascript
  // 消息记录展开/加载
  var msgLogToggle = document.getElementById('msgLogToggle');
  var msgLogContainer = document.getElementById('msgLogContainer');
  if (msgLogToggle && msgLogContainer) {
    var msgLogLoaded = false;
    msgLogToggle.addEventListener('click', function() {
      var isHidden = msgLogContainer.style.display === 'none';
      msgLogContainer.style.display = isHidden ? 'block' : 'none';
      msgLogToggle.textContent = isHidden ? '消息记录 ▾' : '消息记录 ▸';
      if (isHidden && !msgLogLoaded) {
        msgLogContainer.innerHTML = '<p class="wb-empty">加载中…</p>';
        fetch('/api/multiagent/messages?limit=50')
          .then(function(r) { return r.ok ? r.json() : { messages: [] }; })
          .then(function(data) {
            var msgs = data.messages || [];
            msgLogLoaded = true;
            if (window.MultiagentRender && window.MultiagentRender.renderMessageLog) {
              window.MultiagentRender.renderMessageLog(msgs);
            }
          })
          .catch(function() {
            msgLogContainer.innerHTML = '<p class="wb-empty">加载失败</p>';
          });
      }
    });
  }
```

- [ ] **Step 6: 验证消息记录**

刷新浏览器，展开 Director 工作台，切换到"Agent名册"tab。确认：
- 底部出现"消息记录 ▸"折叠按钮
- 点击展开 → 加载消息列表
- 每条消息显示 from、type、时间、content
- 可滚动
- 再次点击折叠

- [ ] **Step 7: Commit**

```bash
git add web/chat.html web/css/multiagent.css web/js/multiagent-render.js web/static/js/chat-main.js
git commit -m "feat: add message log section in workbench roster tab"
```

---

## 完成后验证清单

1. [ ] `POST /api/multiagent/dispatch` 返回 `{ok:true, op_id}`
2. [ ] 输入 `@director 测试任务` → 走 dispatch 路径，不走 /chat/stream
3. [ ] @director 成功后 collab 气泡显示"已分派"
4. [ ] @director 失败（multiagent 未启用）时 toast 提示
5. [ ] 普通消息（不带 @director）仍正常走 /chat/stream
6. [ ] 点击"分派任务"按钮 → 对话框弹出
7. [ ] 对话框中 agent 列表加载正常，可多选
8. [ ] 提交分派 → 对话框关闭 → collab 气泡出现
9. [ ] 控制台 dispatch `multiagent-message` 事件 → 紫色气泡插入聊天流
10. [ ] Agent名册 tab 的"消息记录"可展开/折叠
11. [ ] 消息记录加载并显示最近 50 条
