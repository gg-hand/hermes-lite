# core 性能与健壮性完善 实现计划

> **执行状态（2026-09-10 完成）**：7 任务全部落地，并经**执行后严格审计**（plan-auditor）修复 9 项发现。**D-1 裁决 = 保持 FULL**（用户明确"保持"）→ 任务 1 按降级分支执行（只显式化 `busy_timeout` + 改 `storage_writer.py` docstring 为如实描述，**不**启用 `synchronous=NORMAL`）；任务 2–7 按原计划执行。门禁：`pytest teage_liu2/tests_core` **150 passed**（新增 26 条）、行为套件 **21/21**、`scripts/audit_liu2.ps1` exit=0。实测：605 KiB 快照 `apply_action_batch` **3.156 → 0.016 ms**（审计独立复测 0.0136 ms）；8000 chunk **77.5 → 21.2 ms**；单钩子 **9.5 → 2.5 µs**（保持 FULL，故 B1 的落盘延迟收益未取得，换断电安全）。
>
> **执行期偏差（登记）**：
> 1. **任务 1 降级**（D-1 否决）：测试断言拆成 `journal_mode=wal` + `synchronous=2`(FULL，固化裁决) + `busy_timeout=5000`；后两项在默认值下"天然绿"，判别力由变异验证承担（把 WAL 改成 DELETE → 变红）。
> 2. **任务 3 新增第 4 条用例**：计划的两条红测试在任务 2 落地后已天然绿；执行中发现**真实漏洞** —— `type` 字段为不可哈希 JSON 类型（数组/对象）时 `decode` 抛 `TypeError`（非 `ValueError`），逃出读循环内层 `except` → **整条通道死亡**。补 `test_read_loop_survives_unhashable_type_field`（修复前红）。
> 3. **任务 4 第二个测试的锚点替换**：计划用 `monkeypatch.setattr(snap_mod.json, "dumps", ...)`，但计划同时删除了 `snapshot.py` 的 `import json` → 实际会以 `AttributeError` 直接报错（措辞已按审计更正：不是"静默空断言"）；且 `_json_len` 定义在 `types` 模块、`snapshot` 模块的 `json` 引用已不再用于整快照序列化 → 该 patch 无论如何都不命中。改为 patch `snapshot._json_len`（模块名查找），精确拦截"整快照序列化"（审计以变异验证确认该锚点承重）。
> 4. **任务 5 hooks 用例加强**：计划称"删掉 `except asyncio.TimeoutError` → 测试变红"不成立（异常会落到 `except Exception` 兜底，链仍继续）。改用 `caplog` 断言日志码为 `HOOK_TIMEOUT`（而非泛化为 `HOOK_EXCEPTION`），变异后确实变红。
> 5. **任务 6 与计划一致**（计划第三轮已修正断言位置与 `_timer_task` 创建时登记）。
> 6. **任务 7 的 PENDING 文本修正**：计划要求登记"落盘档位 = WAL + `synchronous=NORMAL`"，因 D-1 否决，实际登记为"**保持 FULL**（逐次 fsync）；`NORMAL` 仅为评估结论"。
> 7. **量化验证口径修正**：`HookChain._call` 的收益必须在**同一事件循环内**测；用 `asyncio.run` 包裹时建 loop 的 ≈0.58 ms 固定开销会完全淹没钩子开销（首测 0.60 ms 为假象，同 loop 内实测 2.5 µs/枝干）。
>
> **执行后审计（plan-auditor，114 tool uses / 730s，严格口径）与修复**：结论"有条件通过（无契约/正确性阻断项）"，发现 9 项，**已全部处理**：
> - **D-1（主要，已修）** `json_depth` 迭代版在"宽而浅"帧上比递归慢且内存放大（70 万容器实测 **378 ms / 42.9 MB** vs 递归 61 ms / 0 MB）——此前"快 ≈2×"的结论只对消息型形状成立。**修法**：由"逐容器压栈"改**迭代器栈 DFS**（栈深 = 嵌套深度）+ 空容器不入栈 + `limit` 早退。复测：宽浅 700k **50 ms**（快于递归 77 ms）、内存 **0 MB**；消息型 0.66 vs 1.21 ms；深 900 层 0.21 ms；`check_frame_depth`(5000 层) 0.012 ms（早退 ≈90×）。
> - **D-2（次要，已修）** `L3BatchSink.close()` 不 await 在途冲刷 → shutdown 期正在投递的那批事件**静默丢失**（审计探针实测 `got == []`）。**修法**：close 内 `await` 在途任务（只吞"自己刚取消的定时窗口"的 CancelledError，外部取消放行），补红测试 `test_close_awaits_inflight_flush`。
> - **#20（覆盖缺口，已补）** `_flush` 的 `while` 续送语义无用例覆盖（审计证明该分支真实可达：1089 次触发中 817 次命中）。补 `test_inflight_flush_continues_draining_new_events`（变异 `while`→`if` 变红）。
> - **D-3（次要，已修）** `check_frame_limits` 在 `decode` 改用 `check_frame_depth` 后成为**零调用方 + 零覆盖**。补 `test_check_frame_limits_semantics` 锁定其三种返回语义。
> - **D-9（nit，已修）** stdio 逐帧异常日志无限速（触发面从"仅 ValueError"扩大到全部异常）→ 只在前 3 次与每第 100 次告警，其余 debug。
> - **D-4 / D-5（nit，已修 docstring）** `_SizeCache` 失效契约补全（`history` 等 base 分量字段 + `messages` 元素原地改写）；`asyncio.timeout` 等价性声明加限定（唯 `timeout_sec<=0` 且首 await 不挂起时不等价，生产不可达）。
> - **D-12（nit，已修文案）** `test_read_loop_survives_dispatch_error` 事由不成立（`_dispatch` 自带隔离）→ docstring 改为如实说明"不承重、仅为不变量护栏"。
> - **D-8（nit，已回写）** 审查报告 §6 第 4 行改为"已升 P1-4 并落地"；本文件偏差#3 措辞按审计更正。
> - **审计确认承重**：快照记账 16 000 步随机差分模糊 **0 处不一致**（含 revision 进位、截尾、回滚两分支）；`apply_action_batch` 稳态复测 **0.0136 ms**（旧 3.2256 ms，**≈237×**）且冷路径无回归；L3 1000 并发投递守恒（delivered/unique 1000、dropped 0、pending 0）；契约面（PROTOCOL 冻结面、11 键、无新依赖）零违规。

> **给执行者:** 按此计划逐任务实现,每个任务走 TDD 循环(红 → 绿 → 重构 → 门禁)。
> 步骤用复选框(`- [ ]`)语法跟踪进度。
> 本计划**不含任何契约变更**:`PROTOCOL/` v1.0.0 冻结面(schema 常量 / 消息集 / 事件面)一字不动。

**目标:** 把上一轮 core 性能审查收集到的**全部**问题逐条复核后落地——消除落盘 fsync 税、事件循环内全量序列化、逐 chunk 超时开销、协议边界重复测量与深嵌套崩溃、L3 批处理契约失效。

**架构:** 全部改动在既有模块内就地收紧,不新增抽象层、不改配置键、不改 wire 契约。三类手段:①把"每轮全量"换成"增量记账 / 迭代扫描 / 更廉价的 asyncio 原语";②把 fsync 降到 WAL 语义下的 `synchronous=NORMAL`(与代码已声明的档位一致);③把"单帧异常即致命"与"批处理记账错乱"降级为可恢复。

**技术栈:** Python 3.11(`asyncio.timeout` / `dataclasses` / `sqlite3`)、SQLite(WAL)、rusqlite(Rust 后端)、pytest + `PROTOCOL/behavior-suite/runner.py`。

## 全局约束

- Python 3.11;全链路 `async/await`;禁止阻塞事件循环。
- 不改 `PROTOCOL/` 任何 schema 常量:`frame_max_bytes=4194304`、`json_max_depth=64`、`RESOURCE_LIMITS` 三项数值、11 键 Snapshot 形状。
- 不改 `config.yaml` 及任何常驻配置键;不新增第三方依赖(仅 stdlib + 既有依赖)。
- 测试文件写入 `teage_liu2/tests_core/`(授权留痕见 `CODEBUDDY.md`「仓库纯净例外(2026-09-10 用户授权)」;`tests_core/` 已在库中);仓库内不得出现临时脚本。
- 每个任务收尾必须跑:`D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q` 全绿。
- 全计划收尾必须跑:`D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`(21/21)+ `powershell -File scripts\audit_liu2.ps1`(exit=0)。
- Windows 控制台 GBK:临时脚本打印中文/emoji 需 `sys.stdout.reconfigure(encoding="utf-8")`。
- `write_to_file` 会吞空行;`replace_in_file` 锚点缩进必须逐字符一致;**每次编辑后必须回读校验**。
- git 写操作(commit/push)一律**先经用户批准**。

### 决策点(执行前必须用户确认)

- **D-1 落盘持久性档位**:`PRAGMA synchronous=NORMAL`(WAL 下)把每次 commit 的 fsync 降为仅 checkpoint 同步 —— 实测单条写 1.23 ms → 0.10 ms(×12)、100 条积压下的 user flush 等待 166.5 ms → 18.6 ms(×9)。代价:**断电/OS 崩溃**可能丢最近若干已提交事务;**进程崩溃不丢**(WAL 已写)。`core/storage_writer.py:11-12` 的 docstring **已经**声明该档位("NORMAL 下 WAL 已写,进程崩溃不丢"),即现状是"实现未兑现文档语义"。
  → 本计划**默认执行**(修文档与实现的不一致),但**必须用户点头**;若用户要求保持 FULL,则任务 1 降级为"仅补 `busy_timeout` 显式化 + docstring 改为如实描述 FULL",其余任务不受影响。

---

## 0. 问题清单审核结论(本轮逐条复核)

> 口径:上表"确认"= 本轮以代码阅读 + 本机实测复核后**成立**;"证伪"= 复核后**不成立,明确不改**;"更正"= 上一轮报告数据有误,以本轮为准。

| ID | 结论 | 证据(本轮实测) | 处置 |
|---|---|---|---|
| **B1** 落盘未设 `synchronous=NORMAL`(每次 commit fsync) | **确认** | 1 KiB 单条写 1.23 ms → `NORMAL` 0.10 ms(×12);100 条积压 user flush 166.5 ms → 18.6 ms;`storage_writer.py:11-12` 已声明 NORMAL 语义;`storage-rust/src/main.rs:41` 同样缺 | 任务 1(需 D-1 批准) |
| **A1** 深嵌套帧 `RecursionError` 逃逸 → stdio 读循环终止(通道死亡) | **确认** | `json.loads(20000 层)` 抛 RecursionError;`json_depth(900 层)` 抛 RecursionError;`decode` 只捕 `JSONDecodeError`(`transport.py:318`);`stdio._read_loop` 的 `except Exception` 在 `while` **之外**(`stdio.py:324-327`)→ 整循环退出 | 任务 2 + 任务 3 |
| **B4'** 入站帧边界校验在事件循环内做 3 遍(loads + 递归 depth + dumps),体积被重复测量 | **确认** | 205.9 KiB 帧:`json_depth` 0.954 ms + `json.dumps` 0.982 ms(合计 2.009 ms);`decode` 已按 `len(line)` 拒绝后又 dumps 求 size;迭代版 depth 0.478 ms(快 2×) | 任务 2 |
| **B3** `_check_snapshot_budget` 全量序列化 = `apply_action_batch` 的 99% 开销,且在事件循环内 | **确认** | 0.61 MiB 快照:含预算 3.156 ms vs **去预算 0.008 ms**(净 99.7%);`to_dict()` 把 `history` 与 `messages` 各序列化一次 → 计费体积约为真实内容 2× | 任务 4 |
| **B2** 逐 chunk `asyncio.wait_for`(≈9.5 µs/chunk) | **确认** | wait_for 9.48 µs vs `asyncio.timeout` **2.49 µs** vs 纯定时器地板 1.2 µs;8000 chunk 77.5 ms → 19.5 ms;嵌套与语义等价已逐条验证(见任务 5) | 任务 5 |
| **A2** `L3BatchSink` 的"64 条先到触发"实际失效 + `_flush_task` 记账竞态 | **确认** | 同一 tick 内 `accept` 500 条 → **0 次 flush**;66 ms 后一次性刷出 1 批 **500 条**(合同要求 ≥64 条即时触发);`_schedule_flush(immediate=True)` 在 `_flush_task` pending 时直接 return(`event_stream.py:212-215`);`_timer` 的 `finally` 无条件置 None(`event_stream.py:217-224`)可能清掉更新的 task 引用 | 任务 6 |
| **A3** `L3BatchSink._buffer` 用 `list.pop(0)`(O(n)) | **确认** | `event_stream.py:204`;同文件 `L3ObserverQueue` 已用 `deque`(`event_stream.py:125`)——不一致 | 任务 6(同处修复) |
| **C1** `hooks._call` 每次 `asyncio.wait_for`(9.5 µs/次) | **确认但小** | 10 枝干 × 4 钩子/轮 = 0.38 ms/轮;同法换 `asyncio.timeout` 可降至 ≈0.10 ms/轮 | 任务 5(顺带) |
| **C5** `count_tokens` / `count_messages_tokens` 每次 `tiktoken.get_encoding` + 逐块 encode | **确认但无调用方** | liu2 内零调用方 | 任务 7 记录(不改代码) |
| **C8** `pipeline._persist` 无 `storage_writer` 时同步直写(事件循环内 sqlite) | **确认(仅测试/CLI 路径)** | `pipeline.py:449-457`;生产装配恒有 writer(`app.py:151`) | 任务 7 记录 |
| **D1**(上轮)"每轮 `list(messages)` 拷贝是 O(n²) 问题" | **证伪** | 100/200/400 条列表拷贝 0.001/0.001/0.002 ms;`incremental_finalize` 0.014→0.18 ms(100→1600 条) | 不改(§0 记录) |
| **D2**(上轮)"`check_frame_limits` 去掉 dumps 可省 90%" | **更正** | `json_depth` 与 `dumps` 各占 ≈50%(205.9 KiB:0.954 / 0.982 ms);去 dumps 省 50%,depth 迭代化再省一半 → 合计 ≈75% | 任务 2 |
| **D3**(上轮)"`json_depth` 便宜、可忽略" | **更正(错)** | 与 `json.dumps` 同量级,且递归版数百层即 RecursionError | 任务 2 |
| **D4**(上轮)"`_convert_messages`/`_strip_thinking_blocks` 逐消息拷贝有成本" | **证伪** | 0.02–0.09 ms(100–400 条),不构成成本 | 不改 |
| **D5**(上轮)"StorageWriter 单写队列是吞吐瓶颈" | **证伪** | 写成本由 fsync 主导(= B1);`NORMAL` 后 100 条积压 flush 仅 18.6 ms | 不改队列设计 |

---

## 1. 文件结构

| 动作 | 文件 | 职责 |
|---|---|---|
| 修改 | `teage_liu2/core/history.py` | 连接 PRAGMA 显式化(WAL / NORMAL / busy_timeout) |
| 修改 | `teage_liu2/core/storage.py` | 同上(通用 doc 平台连接) |
| 修改 | `storage-rust/src/main.rs` | Rust 后端同档位对齐 |
| 修改 | `teage_liu2/core/transport.py` | `json_depth` 迭代化;拆出 `check_frame_depth`;`decode` 以线上字节计体积 + 捕 RecursionError |
| 修改 | `teage_liu2/core/stdio.py` | 单帧异常降级(不终止读循环);`decode` 传 `raw_bytes` |
| 修改 | `teage_liu2/core/types.py` | Snapshot 体积记账缓存字段 + `with_*` 维护/失效 + `_json_len` |
| 修改 | `teage_liu2/core/snapshot.py` | `snapshot_bytes` 增量记账,`_check_snapshot_budget` 零全量序列化 |
| 修改 | `teage_liu2/core/llm.py` | `_with_activity_timeout` 换 `asyncio.timeout` |
| 修改 | `teage_liu2/core/hooks.py` | `_call` 换 `asyncio.timeout` |
| 修改 | `teage_liu2/core/event_stream.py` | L3 即时触发 / task 记账 / `deque` / `close` 兜底 |
| 追加测试 | `tests_core/test_history.py`、`test_storage.py`、`test_transport_limits.py`(新)、`test_stdio_readloop.py`(新)、`test_snapshot_limits.py`、`test_llm_retry.py`、`test_hooks.py`、`test_l3_batch.py`(新) | 每个任务的红/绿锚 |
| 回写文档 | `docs/plans/2026-09-10-liu2-core性能审查报告.md`、`teage_liu2/docs/plans/开发日志.md`、`teage_liu2/PROTOCOL/PENDING.md` | 更正数据 + 变更留痕 |

---

## 任务 1:落盘档位显式化(WAL + synchronous=NORMAL + busy_timeout)

**文件:**
- 修改: `teage_liu2/core/history.py:99-104`
- 修改: `teage_liu2/core/storage.py:90-93`
- 修改: `storage-rust/src/main.rs:41`
- 测试: `teage_liu2/tests_core/test_history.py`(追加)、`teage_liu2/tests_core/test_storage.py`(追加)

**接口:**
- 消费: 无(仅 stdlib `sqlite3`)
- 产出: 两个 store 的连接在 `__init__` 返回时即具备 `journal_mode=wal` / `synchronous=1` / `busy_timeout=5000`

- [ ] **步骤 0:D-1 用户确认**

向用户确认 D-1(见上)。未获批准则**跳过步骤 3-6**,只做步骤 7 的 docstring 更正。

- [ ] **步骤 1:写失败测试**

`teage_liu2/tests_core/test_history.py` 末尾追加:

```python
def test_connection_pragmas_are_explicit(tmp_path):
    """验收:连接档位显式化(WAL + synchronous=NORMAL + busy_timeout)。

    §18.1 的 flush 档语义 = "进程崩溃不丢"(WAL 已写),对应 WAL 下的
    synchronous=NORMAL;缺此设置则退化为逐次 commit fsync(实测 ×12 延迟)。
    """
    store = SQLiteHistoryStore(str(tmp_path / "h.db"))
    assert store.conn.execute("PRAGMA journal_mode").fetchone()[0].lower() == "wal"
    assert store.conn.execute("PRAGMA synchronous").fetchone()[0] == 1  # 1 = NORMAL
    assert store.conn.execute("PRAGMA busy_timeout").fetchone()[0] == 5000
    store.close()
```

`teage_liu2/tests_core/test_storage.py` 末尾追加:

```python
def test_connection_pragmas_are_explicit(tmp_path):
    """验收:通用落盘平台连接档位显式化(与 history 同口径)。"""
    provider = SQLiteStorageProvider(str(tmp_path / "p.db"))
    assert provider.conn.execute("PRAGMA journal_mode").fetchone()[0].lower() == "wal"
    assert provider.conn.execute("PRAGMA synchronous").fetchone()[0] == 1
    assert provider.conn.execute("PRAGMA busy_timeout").fetchone()[0] == 5000
    provider.close()
```

(若 `test_storage.py` 未导入 `SQLiteStorageProvider`,按该文件既有导入风格补上。)

- [ ] **步骤 2:跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_history.py teage_liu2\tests_core\test_storage.py -q`
预期: `synchronous` 必红(实测 sqlite3 默认值为 `2` = FULL);`busy_timeout` 该项**可能已绿** —— 代码未显式传 `timeout`,`sqlite3.connect` 的 `timeout` 默认值 `5.0` 会把它隐式设为 5000,本任务只是把它**显式化**(与 Rust 后端一致),不改变行为。

- [ ] **步骤 3:写最简实现**

`teage_liu2/core/history.py`:把 `SQLiteHistoryStore.__init__`(现为 `history.py:93-104`)里
`self.conn.execute("PRAGMA journal_mode=WAL;")` **这一行替换为下面三行** ——
`self.db_path` / `parent_dir` / `os.makedirs(...)` / `row_factory` / `self._lock` /
`self._init_tables()` / `self._ensure_fts_table()` **全部保持不动**(漏改会丢建表):

```python
        self.conn.execute("PRAGMA journal_mode=WAL;")
        # synchronous=NORMAL(WAL 下):提交不再逐次 fsync,仅 checkpoint 同步。
        # 档位语义 = StorageWriter flush 档所声明的"进程崩溃不丢"(见 storage_writer.py
        # 模块 docstring);代价是断电/OS 崩溃可能丢最近若干已提交事务。
        # 实测 1 KiB 单条写 1.23 ms → 0.10 ms(见 docs/plans/2026-09-10-core性能与健壮性完善-执行计划.md §0)
        self.conn.execute("PRAGMA synchronous=NORMAL;")
        # 显式化 busy 等待(与 storage-rust/src/main.rs 的 busy_timeout=5000 对齐;
        # 此前仅依赖 sqlite3.connect 的 timeout=5.0 默认值)
        self.conn.execute("PRAGMA busy_timeout=5000;")
```

`teage_liu2/core/storage.py`:同样把 `SQLiteStorageProvider.__init__`(`storage.py:84-93`)里
`PRAGMA journal_mode=WAL;` 一行替换为:

```python
        self.conn.execute("PRAGMA journal_mode=WAL;")
        # 与 SQLiteHistoryStore 同档位(同一文件双连接,语义必须一致)
        self.conn.execute("PRAGMA synchronous=NORMAL;")
        self.conn.execute("PRAGMA busy_timeout=5000;")
```

- [ ] **步骤 4:跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q`
预期: PASS(全绿)。

- [ ] **步骤 5:交叉验证(用户可见延迟)**

在仓库外临时目录跑下面脚本(用完即删),确认 user flush 等待从百毫秒级降到十毫秒级:

```python
import asyncio, os, sqlite3, sys, time
sys.path.insert(0, r"d:/dev/teage-liu/teage-liu")
from teage_liu2.core.history import SQLiteHistoryStore
from teage_liu2.core.storage_writer import StorageWriter

DB = r"d:/tmp/_t1.db"
async def main():
    for suffix in ("", "-wal", "-shm"):
        if os.path.exists(DB + suffix): os.remove(DB + suffix)
    store = SQLiteHistoryStore(DB)
    store.ensure_session("s")
    writer = StorageWriter()
    payload = "汉" * 341
    for _ in range(100):
        writer.enqueue_background(lambda: store.log_message("s", "assistant", payload))
    t = time.perf_counter()
    await writer.enqueue_flush(lambda: store.log_message("s", "user", "hi"))
    print("user flush 等待 ms =", round((time.perf_counter() - t) * 1000, 1))
    writer.close(); store.close()
    for suffix in ("", "-wal", "-shm"):
        if os.path.exists(DB + suffix): os.remove(DB + suffix)
asyncio.run(main())
```

预期: ≈18 ms(修前 ≈166 ms)。

- [ ] **步骤 6:Rust 后端对齐(需用户批准 + 停服)**

`storage-rust/src/main.rs:41` 改为:

```rust
    conn.execute_batch("PRAGMA journal_mode=WAL; PRAGMA synchronous=NORMAL; PRAGMA busy_timeout=5000;")
```

运行: `cd storage-rust && cargo build --release`
预期: 零 warning。安装态 exe 更新(`data2/extensions/storage_rust/teage-storage-rust.exe`)需先停 7878 实例并**经用户批准**,属独立动作,不在本任务强制范围。

- [ ] **步骤 7:docstring 与文档更正**

若 D-1 **未**获批准:把 `core/storage_writer.py:11-12` 的"NORMAL 下 WAL 已写"改为如实描述(逐次 fsync),并把本任务标记为"用户否决"。

- [ ] **步骤 8:提交(待用户批准)**

```bash
git add teage_liu2/core/history.py teage_liu2/core/storage.py teage_liu2/tests_core/test_history.py teage_liu2/tests_core/test_storage.py
git commit -m "perf(teage_liu2/core): 落盘档位显式化——WAL + synchronous=NORMAL + busy_timeout(落盘延迟 ×12)"
```

---

## 任务 2:协议边界校验——迭代深度 + 免重复体积测量 + RecursionError 兜底

**文件:**
- 修改: `teage_liu2/core/transport.py:167-193`(`json_depth` / `check_frame_limits`)、`teage_liu2/core/transport.py:308-352`(`decode`)
- 测试: 新建 `teage_liu2/tests_core/test_transport_limits.py`

**接口:**
- 消费: 无
- 产出:
  - `json_depth(obj: Any, depth: int = 0) -> int` —— 语义不变,实现改迭代,不再抛 `RecursionError`
  - `check_frame_depth(payload: Any) -> Optional[str]` —— 新增,仅深度校验
  - `check_frame_limits(payload: Any, max_bytes: int = FRAME_MAX_BYTES) -> Optional[str]` —— 签名不变(深度 + `json.dumps` 体积),保留给宿主出站/编程式调用
  - `TransportFrame.decode(line: str, raw_bytes: Optional[int] = None) -> TransportFrame` —— 新增可选参数

- [ ] **步骤 1:写失败测试**

新建 `teage_liu2/tests_core/test_transport_limits.py`:

```python
"""transport 序列化边界(§15-A7):深度校验健壮性 + 体积口径唯一化。"""
from __future__ import annotations

import json

import pytest

from teage_liu2.core.transport import (
    FRAME_MAX_BYTES,
    JSON_MAX_DEPTH,
    TransportFrame,
    check_frame_depth,
    json_depth,
)


def _ref_depth(obj, depth=0):
    """递归参考实现(契约定义):深度校验必须与它逐值一致。"""
    if isinstance(obj, dict):
        return depth + 1 if not obj else max(_ref_depth(v, depth + 1) for v in obj.values())
    if isinstance(obj, list):
        return depth + 1 if not obj else max(_ref_depth(v, depth + 1) for v in obj)
    return depth


def test_json_depth_matches_reference():
    """验收:迭代实现与递归定义逐值一致(含空容器/纯标量/混合嵌套)。"""
    samples = [
        1, "x", None, [], {}, [[]], [{}], {"a": {}},
        {"a": [{"b": [1, {"c": 2}]}]},
        {"x": [1, 2, 3], "y": {"z": {"w": []}}},
    ]
    for obj in samples:
        assert json_depth(obj) == _ref_depth(obj), obj


def test_json_depth_survives_deep_nesting():
    """验收:深嵌套不再抛 RecursionError(旧实现数百层即崩)。"""
    obj = 1
    for _ in range(2000):
        obj = [obj]
    # 深度 = 最外层到最内层容器的层数(标量不加深度):2000 层列表 → 2000
    assert json_depth(obj) == 2000


def test_decode_rejects_deep_frame_as_value_error():
    """验收:超深帧被拒且异常类型是 ValueError(旧实现在此处抛 RecursionError)。"""
    deep = '{"type":"heartbeat","payload":{"a":' + "[" * 20000 + "]" * 20000 + \
           '},"encoding":"full","protocol_version":"v1.0.0"}'
    with pytest.raises(ValueError):
        TransportFrame.decode(deep)


def test_decode_rejects_frame_over_wire_bytes():
    """验收:体积以线上字节数计(与 stdio 读循环的 len(line_bytes) 同口径)。"""
    padding = {"type": "heartbeat", "payload": {"p": "x" * (FRAME_MAX_BYTES + 10)},
               "encoding": "full", "protocol_version": "v1.0.0"}
    line = json.dumps(padding)
    with pytest.raises(ValueError):
        TransportFrame.decode(line)


def test_decode_accepts_frame_under_limits():
    """验收:合法帧正常解码,raw_bytes 参数不改变结果。"""
    line = json.dumps({"type": "heartbeat", "payload": {"ok": True},
                       "encoding": "full", "protocol_version": "v1.0.0"})
    assert TransportFrame.decode(line).type == "heartbeat"
    assert TransportFrame.decode(line, raw_bytes=len(line.encode("utf-8"))).type == "heartbeat"


def test_check_frame_depth_boundary():
    """验收:深度 64 合法、65 拒绝(常量不得变动)。"""
    ok = {"a": 1}
    for _ in range(JSON_MAX_DEPTH - 1):
        ok = {"a": ok}
    assert check_frame_depth(ok) is None        # 深度恰为 64
    too_deep = {"a": ok}
    assert check_frame_depth(too_deep) is not None   # 深度 65 → 拒绝
```

- [ ] **步骤 2:跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_transport_limits.py -q`
预期: `test_json_depth_survives_deep_nesting` FAIL(RecursionError)、`test_decode_rejects_deep_frame_as_value_error` FAIL(RecursionError 不是 ValueError)、`check_frame_depth` 导入失败;边界用例的精确层数若与实现差一层,按实测校正测试的构造次数(不得放宽断言)。

- [ ] **步骤 3:写最简实现**

`teage_liu2/core/transport.py` 中 `json_depth` 与 `check_frame_limits` 整段替换为:

```python
def json_depth(obj: Any, depth: int = 0) -> int:
    """计算 JSON 结构嵌套深度(迭代实现,显式栈)。

    为什么改迭代(2026-09-10):① 递归版在数百层嵌套即抛 RecursionError(实测 900 层
    崩溃),而 `json.loads` 在数千层才崩 —— 这个窗口让"深度校验"本身成为崩溃源;
    ② 实测迭代版比递归版快约 2×(205.9 KiB 帧 / 800 条消息:0.478 ms vs 0.983 ms)。
    """
    max_depth = depth
    stack: List[Any] = [(obj, depth)]
    while stack:
        node, node_depth = stack.pop()
        if isinstance(node, (dict, list)):
            node_depth += 1
            if node_depth > max_depth:
                max_depth = node_depth
            children = node.values() if isinstance(node, dict) else node
            for value in children:
                if isinstance(value, (dict, list)):
                    stack.append((value, node_depth))
    return max_depth


def check_frame_depth(payload: Any) -> Optional[str]:
    """JSON 深度边界校验(§15-A7)。合法返回 None;非法返回错误描述。"""
    if json_depth(payload) > JSON_MAX_DEPTH:
        return f"payload 嵌套深度超过上限 {JSON_MAX_DEPTH}"
    return None


def check_frame_limits(payload: Any, max_bytes: int = FRAME_MAX_BYTES) -> Optional[str]:
    """序列化边界校验(§15-A7):深度 + 体积。

    保留给宿主**出站**/编程式调用(需要按对象体积判定时)。入站帧请走
    :meth:`TransportFrame.decode` —— 它按线上原始字节数校验,不做重新序列化
    (重新 dumps 与原始字节口径不一致,且每帧多付一次全量序列化)。
    """
    problem = check_frame_depth(payload)
    if problem:
        return problem
    try:
        size = len(json.dumps(payload, ensure_ascii=False))
    except (TypeError, ValueError) as e:
        return f"payload 不可 JSON 序列化: {e}"
    if size > max_bytes:
        return f"payload 体积 {size} 字节超过上限 {max_bytes}"
    return None
```

`TransportFrame.decode` 的签名与首部校验改为:

```python
    @classmethod
    def decode(cls, line: str, raw_bytes: Optional[int] = None) -> "TransportFrame":
        """解码 + 序列化边界校验(§15-A7)。

        raw_bytes: 该帧在通道上的原始字节数(stdio 读循环已持有,传入即免一次
        UTF-8 重编码);未提供时按 ``line`` 的 UTF-8 字节数计算。体积上限一律
        以**线上字节**为准,不再对已解析对象重新 json.dumps。
        """
        size = len(line.encode("utf-8")) if raw_bytes is None else raw_bytes
        if size > FRAME_MAX_BYTES:
            raise ValueError(f"帧超过字节上限 {FRAME_MAX_BYTES}(实际 {size} 字节)")
        try:
            obj = json.loads(line)
        except json.JSONDecodeError as e:
            raise ValueError(f"非法帧:JSON 解析失败: {e}") from e
        except RecursionError as e:
            raise ValueError("非法帧:JSON 嵌套过深(解析超出递归上限)") from e
```

同方法内把原第 331-333 行的体积校验替换为深度校验:

```python
        problem = check_frame_depth(obj)
        if problem:
            raise ValueError(f"非法帧:{problem}")
```

- [ ] **步骤 4:跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q`
然后: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: 全绿;套件 21/21(用例 `transport-frame-10` 不涉及体积/深度边界,已核对)。

- [ ] **步骤 5:提交(待用户批准)**

```bash
git add teage_liu2/core/transport.py teage_liu2/tests_core/test_transport_limits.py
git commit -m "perf(teage_liu2/core): transport 边界校验——json_depth 迭代化 + 入站帧按线上字节计体积"
```

---

## 任务 3:stdio 读循环单帧容错

**文件:**
- 修改: `teage_liu2/core/stdio.py:303-323`(`_read_loop` 帧处理段)、`teage_liu2/core/stdio.py:319`
- 测试: 新建 `teage_liu2/tests_core/test_stdio_readloop.py`

**接口:**
- 消费: `TransportFrame.decode(line, raw_bytes=...)`(任务 2 产出)
- 产出: `_read_loop` 对单帧异常(解码失败/超深/分发异常)只丢弃该帧并继续,绝不终止通道

- [ ] **步骤 1:写失败测试**

新建 `teage_liu2/tests_core/test_stdio_readloop.py`:

```python
"""stdio 读循环健壮性:单帧异常不得终止整条通道(§15-A7 拒绝语义)。"""
from __future__ import annotations

import asyncio

from teage_liu2.core.stdio import StdioChannel
from teage_liu2.core.transport import TransportFrame


class _FakeReader:
    def __init__(self, lines):
        self._lines = list(lines)

    async def readline(self):
        return self._lines.pop(0) if self._lines else b""


class _FakeWriter:
    def __init__(self):
        self.written = []

    def write(self, data):
        self.written.append(data)

    async def drain(self):
        return None


def _run(lines, seen):
    async def host_handler(name, frame):
        seen.append(frame.type)
        return {"result": {}}

    async def go():
        channel = StdioChannel("t", ["x"], host_handler)
        channel._reader = _FakeReader(lines)
        channel._writer = _FakeWriter()
        await channel._read_loop()

    asyncio.run(go())


def test_read_loop_survives_deep_frame():
    """验收:超深帧(解析期 RecursionError)被丢弃,其后的有效帧仍被处理。"""
    seen = []
    deep = ('{"type":"heartbeat","payload":{"a":' + "[" * 20000 + "]" * 20000 +
            '},"encoding":"full","protocol_version":"v1.0.0"}')
    good = TransportFrame("heartbeat", {"ok": True}).encode()
    _run([deep.encode() + b"\n", good.encode() + b"\n", b""], seen)
    assert seen == ["heartbeat"], "非法帧之后的有效帧必须仍被处理(读循环不得退出)"


def test_read_loop_survives_illegal_json():
    """验收:非 JSON 行被丢弃,后续有效帧仍被处理。"""
    seen = []
    good = TransportFrame("heartbeat", {"ok": True}).encode()
    _run([b"not-json\n", good.encode() + b"\n", b""], seen)
    assert seen == ["heartbeat"]
```

- [ ] **步骤 2:跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_stdio_readloop.py -q`
预期: `test_read_loop_survives_deep_frame` FAIL(`seen == []`,RecursionError 终止了读循环);`test_read_loop_survives_illegal_json` 已通过(现有 `except ValueError: continue` 覆盖)。

- [ ] **步骤 3:写最简实现**

`teage_liu2/core/stdio.py` 的 `_read_loop` 内,把"解码 + 分发"两行改为逐帧容错块:

```python
                line = line_bytes.decode("utf-8", errors="replace").rstrip("\n")
                if not line.strip():
                    continue
                try:
                    frame = TransportFrame.decode(line, raw_bytes=len(line_bytes))
                    await self._dispatch(frame)
                except asyncio.CancelledError:
                    raise
                except Exception as e:
                    # 单帧失败(非法/超深/分发异常)只丢该帧并继续 —— 绝不终止读循环。
                    # 读循环终止 = 通道死亡(在途请求失败 + 依赖心跳超时后才重建)。
                    logger.error(
                        "通道 %s 丢弃异常帧并继续(%d 字节): %s",
                        self.name, len(line_bytes), e,
                    )
                    continue
```

- [ ] **步骤 4:跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q`
预期: 全绿。

- [ ] **步骤 5:提交(待用户批准)**

```bash
git add teage_liu2/core/stdio.py teage_liu2/tests_core/test_stdio_readloop.py
git commit -m "fix(teage_liu2/core): stdio 读循环单帧异常降级——非法帧不再终止通道"
```

---

## 任务 4:快照体积增量记账(事件循环内零全量序列化)

**文件:**
- 修改: `teage_liu2/core/types.py:308-406`(Snapshot 字段与 `with_*`)、新增模块级 `_json_len` 与 `_SizeCache`
- 修改: `teage_liu2/core/snapshot.py:32-67`(`_estimate_bytes` → `_json_len`)、`70-157`(`apply_action_batch`)
- 测试: `teage_liu2/tests_core/test_snapshot_limits.py`(追加)

**接口:**
- 消费: 无
- 产出:
  - `teage_liu2/core/types.py::_SizeCache`(frozen dataclass,实现层内部) —— 字段 `messages_ref`(记账时的 `messages` 列表对象,**同一性**校验用)、`msg_bytes`(`len(json.dumps(messages))`)、`base_bytes`(`messages` 置空后整快照的 JSON 长度;`-1` = 未知)
  - `Snapshot._size_cache: Optional[_SizeCache]` —— `None` = 缓存失效/未建立;`messages_ref is not snapshot.messages` 时自动失效
  - `teage_liu2/core/snapshot.py::snapshot_bytes(snapshot: Snapshot) -> int`(模块级函数,**不是** Snapshot 的方法)—— 增量记账值,恒等于 `len(json.dumps(snapshot.to_dict(), ensure_ascii=False))`
  - `teage_liu2/core/types.py::_json_len(obj: Any) -> int` —— 序列化长度,不可序列化返回 0(与旧 `_estimate_bytes` 语义一致)

- [ ] **步骤 1:写失败测试**

`teage_liu2/tests_core/test_snapshot_limits.py` 末尾追加:

```python
def test_incremental_size_matches_ground_truth():
    """验收:增量记账值必须逐点等于 len(json.dumps(to_dict()))(记账正确性唯一锚)。

    覆盖:纯追加(apply_action_batch)、**revision 进位**(9→10 的整数位数变化)、
    with_round 的位数变化、base 失效后的重算、以及**截尾**路径(loop 终止清理会走)。
    """
    from teage_liu2.core.snapshot import snapshot_bytes

    cur = _base()
    assert snapshot_bytes(cur) == _size(cur)
    for content in ("a" * 10, "b" * 1000, "c" * 50_000):
        cur, bad = _append(cur, content)
        assert not bad
        assert snapshot_bytes(cur) == _size(cur)

    # revision 进位:apply_action_batch 会把 revision +1,而 revision 在 to_dict 中
    # 是整数 —— 9→10 会让 JSON 长度 +1,记账必须同步(否则偏差 1 字节)。
    cur = dataclasses.replace(cur, revision=8, _size_cache=None)
    cur, _ = _append(cur, "y")            # revision 9,缓存以 revision=9 建立
    assert cur.revision == 9
    cur, _ = _append(cur, "z")            # revision 10 → 进位必须被记账
    assert cur.revision == 10
    assert snapshot_bytes(cur) == _size(cur)

    cur = cur.with_round(9)
    assert snapshot_bytes(cur) == _size(cur)
    cur = cur.with_round(1234)          # 位数变化 → base 分量必须精确跟随
    assert snapshot_bytes(cur) == _size(cur)
    cur = cur.with_stop(True, "拦截")   # base 失效 → 精确重算
    assert snapshot_bytes(cur) == _size(cur)
    cur = cur.with_extra({"a.b": "c"})
    assert snapshot_bytes(cur) == _size(cur)
    cur = cur.with_messages(cur.messages[:-1])   # 截尾 → 缓存失效并精确重算
    assert snapshot_bytes(cur) == _size(cur)
    cur = cur.with_messages(cur.messages + [{"role": "user", "content": "尾"}] * 3)
    assert snapshot_bytes(cur) == _size(cur)


def test_budget_check_does_not_serialize_whole_snapshot(monkeypatch):
    """验收:缓存建立后,稳态批次不再对整快照 json.dumps(99% 开销来源)。

    首次预算检查允许一次 base 精确计算(缓存为空),之后必须为零。
    """
    import json as _json

    from teage_liu2.core import snapshot as snap_mod

    calls = {"n": 0}
    real_dumps = _json.dumps

    def counting_dumps(obj, *args, **kwargs):
        if isinstance(obj, dict) and "messages" in obj:
            calls["n"] += 1
        return real_dumps(obj, *args, **kwargs)

    monkeypatch.setattr(snap_mod.json, "dumps", counting_dumps)
    cur = _base()
    cur, _ = _append(cur, "x" * 100)     # 首次:允许一次 base 精确计算
    calls["n"] = 0
    for _ in range(3):
        cur, _ = _append(cur, "x" * 100)
    assert calls["n"] == 0, "缓存建立后,追加批次不得整快照序列化"
    assert snap_mod.snapshot_bytes(cur) == _size(cur)
```

- [ ] **步骤 2:跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_snapshot_limits.py -q`
预期: FAIL —— `snapshot_bytes` 尚不存在(ImportError)。

- [ ] **步骤 3:写最简实现**

**(a) `teage_liu2/core/types.py`** —— 模块级新增(放在 `extract_text` 之前):

```python
def _json_len(obj: Any) -> int:
    """JSON 序列化长度(ensure_ascii=False);不可序列化 → 0(与历史 _estimate_bytes 一致)。"""
    try:
        return len(json.dumps(obj, ensure_ascii=False))
    except (TypeError, ValueError):
        return 0


@dataclass(frozen=True)
class _SizeCache:
    """快照体积记账缓存(实现层内部,不进协议序列化)。

    字段:
    - ``messages_ref``:记账时 ``messages`` 的**列表对象** —— 靠同一性判定缓存是否仍
      对应当前消息列表(外部用 ``dataclasses.replace(..., messages=...)`` 绕过
      ``with_messages`` 时会自动失效,不用长度启发式);
    - ``msg_bytes``:``len(json.dumps(messages, ensure_ascii=False))``;
    - ``base_bytes``:``messages`` 置空后**整个快照**的 JSON 长度;``-1`` = 未知。

    有效性前提:快照仅经 ``with_*`` / ``apply_action_batch`` 演进(生产路径如此)。
    外部若用 ``dataclasses.replace`` 直接改 ``messages`` 或标量字段,必须同时传
    ``_size_cache=None`` 显式失效。
    """
    messages_ref: List[Message]
    msg_bytes: int
    base_bytes: int


def _base_unknown(cache: Optional[_SizeCache]) -> Optional[_SizeCache]:
    """保留消息分量、把 base 分量置为未知(-1)。"""
    return None if cache is None else _SizeCache(cache.messages_ref, cache.msg_bytes, -1)
```

**(b) `Snapshot` 新增内部字段**(紧跟 `stop_reason` 之后):

```python
    #: 实现层内部字段(不进协议序列化,11 键保持与 types.schema.json 一致):
    #: 体积记账缓存(见 _SizeCache);None = 失效/未建立。
    #: §15-A6 预算检查据此在稳态下只做常数级算术(不再全量 json.dumps)。
    _size_cache: Optional[_SizeCache] = field(default=None, repr=False, compare=False)
```

**(c) `Snapshot` 的 `with_*` 维护缓存**:

```python
    def with_messages(self, messages: List[Message]) -> "Snapshot":
        """结构共享推进:仅替换 messages(新列表 + 共享元素引用,§18.2)。

        体积记账:新列表是旧列表的"纯追加"(逐元素**同一性**)时增量维护 messages
        JSON 长度(base 分量不含 messages,保持有效);截尾 / 换元素一律置为失效,
        由下一次预算检查精确重算。
        """
        return replace(self, messages=messages, _size_cache=self._cache_for(messages))

    def _cache_for(self, messages: List[Message]) -> Optional[_SizeCache]:
        cache = self._size_cache
        if cache is None or cache.msg_bytes < 0 or cache.messages_ref is not self.messages:
            return None
        old = self.messages
        if len(messages) < len(old):
            return None  # 截尾(loop 终止清理):不做减法记账,直接重算
        if any(a is not b for a, b in zip(old, messages[: len(old)])):
            return None
        msg_bytes = cache.msg_bytes
        for index in range(len(old), len(messages)):
            msg_bytes += _json_len(messages[index]) + (0 if index == 0 else 2)
        return _SizeCache(messages, msg_bytes, cache.base_bytes)

    def with_tools(self, tools: List[Dict[str, Any]]) -> "Snapshot":
        return replace(self, tools=list(tools), _size_cache=_base_unknown(self._size_cache))

    def with_round(self, round_: int) -> "Snapshot":
        """轮次推进:``round`` 在快照 JSON 中为整数,长度可精确差分,base 不失效。"""
        cache = self._size_cache
        if cache is not None and cache.base_bytes >= 0:
            cache = _SizeCache(
                cache.messages_ref,
                cache.msg_bytes,
                cache.base_bytes + len(str(round_)) - len(str(self.round)),
            )
        return replace(self, round=round_, _size_cache=cache)

    def with_extra(self, extra: Dict[str, Any]) -> "Snapshot":
        return replace(self, extra=dict(extra), _size_cache=_base_unknown(self._size_cache))

    def with_stop(self, stop: bool = True, reason: Optional[str] = None) -> "Snapshot":
        # stop_reason 不进 to_dict(仅 stop 进),故只失效 base 分量
        return replace(self, stop=stop, stop_reason=reason,
                       _size_cache=_base_unknown(self._size_cache))
```

**(d) `teage_liu2/core/snapshot.py`** —— 删除 `_estimate_bytes`,改为:

```python
from .types import (
    RESOURCE_LIMITS,
    Snapshot,
    _SizeCache,
    _json_len,
    validate_message_shape,
)
```

```python
def snapshot_bytes(snapshot: Snapshot) -> int:
    """快照序列化体积(增量记账):恒等于 len(json.dumps(snapshot.to_dict()))。

    稳态下只做常数级算术;仅当缓存失效(未建立/messages 列表被换)时才做一次
    精确计算(见 _measure)。
    """
    return _measure(snapshot)[0]


def _measure(snapshot: Snapshot) -> Tuple[int, Snapshot]:
    """返回 (体积, 带缓存的新快照)。

    体积 = base_bytes - 2 + msg_bytes:``to_dict()`` 的 JSON 中,``messages`` 以外的
    部分是固定模板,把 ``[]``(2 字符)换成真实数组即改变 ``msg_bytes - 2`` 字节 ——
    与全量 ``json.dumps`` 精确恒等。
    """
    cache = snapshot._size_cache
    valid = cache is not None and cache.messages_ref is snapshot.messages
    msg_bytes = cache.msg_bytes if valid else -1
    base_bytes = cache.base_bytes if valid else -1
    if msg_bytes < 0:
        msg_bytes = _json_len(snapshot.messages)
    if base_bytes < 0:
        base_bytes = _json_len(replace(snapshot, messages=[]).to_dict())
        if base_bytes == 0:
            # 不可序列化(如 SetExtra 写入非 JSON 值:validate_action 不校验可序列化性)
            # → 与历史 _estimate_bytes 语义一致:体积**恒**视为 0(永不拒绝)。
            # 缓存 base 分量必须存 -1 而非 0:0 会被下一次判定为"已知的 0 字节",
            # 从而使 base-2+msg_bytes 变成可能超限的正数 → 误拒。
            return 0, replace(
                snapshot, _size_cache=_SizeCache(snapshot.messages, msg_bytes, -1)
            )
    cached = replace(
        snapshot, _size_cache=_SizeCache(snapshot.messages, msg_bytes, base_bytes)
    )
    return base_bytes - 2 + msg_bytes, cached


def _check_snapshot_budget(snapshot: Snapshot) -> Tuple[Optional[str], Snapshot]:
    """快照总体积上限检查(§15-A6)。返回 (问题描述, 带体积缓存的新快照)。"""
    limit = RESOURCE_LIMITS["max_snapshot_bytes"]
    size, snapshot = _measure(snapshot)
    if size > limit:
        return (
            f"快照体积 {size} 字节超上限 {limit}(HOOK_INVALID_ACTION)"
        ), snapshot
    return None, snapshot
```

`apply_action_batch` 的记账与收尾(**替换原第 107-157 行**;第 95-96 行 `if not valid: return snapshot, invalid` 早退与第 100-105 行 `messages/tools/extra/system_text/stop/stop_reason` 初始化**保留不动**):

```python
    # 体积记账(§15-A6 增量):缓存失效时分量留 -1,由 _check_snapshot_budget 精确重算
    cache = snapshot._size_cache
    cache_ok = cache is not None and cache.messages_ref is snapshot.messages
    msg_bytes = cache.msg_bytes if cache_ok else -1
    base_bytes = cache.base_bytes if cache_ok else -1

    for a in valid:
        if isinstance(a, AppendMessage):
            budget = _check_message_budget(messages, a.message)
            if budget:
                logger.error("HOOK_INVALID_ACTION: %s", budget)
                invalid.append(budget)
                continue
            was_empty = not messages
            encoded = _json_len(a.message)
            messages = messages + [dict(a.message)]
            if msg_bytes >= 0:
                msg_bytes += encoded + (0 if was_empty else 2)
        elif isinstance(a, SetTools):
            tools = list(a.tools)
            base_bytes = -1
        elif isinstance(a, SetExtra):
            extra[a.key] = a.value
            base_bytes = -1
        elif isinstance(a, SetStop):
            stop = True
            stop_reason = a.reason
            base_bytes = -1
        elif isinstance(a, SetSystem):
            system_text = a.text
            base_bytes = -1
        elif isinstance(a, ModifyToolSchema):
            tools = _modify_tool_schema(tools, a)
            base_bytes = -1

    # revision +1 会让 base 中的整数位数变化(9→10 / 99→100),必须精确差分
    if base_bytes >= 0:
        base_bytes += len(str(snapshot.revision + 1)) - len(str(snapshot.revision))

    new_snapshot = replace(
        snapshot,
        messages=messages,
        tools=tools,
        extra=extra,
        system_text=system_text,
        stop=stop,
        stop_reason=stop_reason,
        revision=snapshot.revision + 1,
        _size_cache=_SizeCache(messages, msg_bytes, base_bytes),
    )

    budget, new_snapshot = _check_snapshot_budget(new_snapshot)
    if budget:
        # §15-A6 T-8 × hooks H-8 一致性(2026-09-10 评审 FIX-1,语义不变):
        # 体积上限只拒绝**本批次的 append 类 action**,set 类一律保留。
        set_names = [type(a).__name__ for a in valid if not isinstance(a, AppendMessage)]
        rolled = list(snapshot.messages)
        safe = replace(
            new_snapshot,
            messages=rolled,
            # 消息回滚 → 消息分量回到入参;set 类保留 → base 分量必须失效
            _size_cache=(
                None if snapshot._size_cache is None
                # 消息分量按回滚后的列表重新度量一遍(不沿用入参缓存值,防"非 None 但陈旧")
                else _SizeCache(rolled, _json_len(rolled), -1)
            ),
        )
        logger.error(
            "HOOK_INVALID_ACTION: %s(本批次 append 类 action 已拒绝,set 类保留:%s)",
            budget, set_names,
        )
        invalid.append(budget)
        return safe, invalid

    return new_snapshot, invalid
```

`_check_message_budget` 内 `_estimate_bytes(message)` 改为 `_json_len(message)`。

`snapshot_with_user_message`(当前无调用方)同步改用 `with_messages`,避免留下失效缓存的口子:

```python
    return snapshot.with_messages(messages + [user_msg])
```

- [ ] **步骤 4:跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q`
预期: 全绿(`test_snapshot_limits.py` 既有 5 条 + 新增 2 条)。
随后跑 `runner.py` 确认 21/21(types 域用例不受影响)。

- [ ] **步骤 5:量化验证**

复用任务 1 步骤 5 的仓库外脚本风格,对 `apply_action_batch` 计时(改造为:构造 600 条 1000 字消息的快照,`apply_action_batch(cur, [AppendMessage(...)])` 取 3 次最小值)。
预期: ≈0.008 ms(修前 3.156 ms)。

- [ ] **步骤 6:提交(待用户批准)**

```bash
git add teage_liu2/core/types.py teage_liu2/core/snapshot.py teage_liu2/tests_core/test_snapshot_limits.py
git commit -m "perf(teage_liu2/core): 快照体积预算改增量记账——去掉每批全量序列化(3.16ms→0.008ms)"
```

---

## 任务 5:超时原语降开销(`asyncio.wait_for` → `asyncio.timeout`)

**文件:**
- 修改: `teage_liu2/core/llm.py:84-109`(`_with_activity_timeout`)
- 修改: `teage_liu2/core/hooks.py:222-242`(`HookChain._call`)
- 测试: `teage_liu2/tests_core/test_llm_retry.py`(追加)、`teage_liu2/tests_core/test_hooks.py`(追加)

**接口:**
- 消费: 无
- 产出: `_with_activity_timeout(aiter, timeout_sec, on_timeout)` 语义不变(正常流透传;超时 → 先 `await on_timeout()` 再抛 `ActivityTimeout`),实现换 `asyncio.timeout`;`HookChain._call` 同样换原语,超时/异常仍返回 `None` 且隔离

**前置事实(已实测,写入代码注释):** ① `asyncio.TimeoutError is TimeoutError` 为真,`except asyncio.TimeoutError` 可捕获 `asyncio.timeout` 的异常;② `asyncio.timeout` 可与 `step.py` 的 `asyncio.timeout(stream_total_timeout)` 嵌套,外层总超时仍独立生效(实测 163 ms 如期触发);③ 超时触发 `on_timeout` 后内部 `await` 收到 `CancelledError`(与 `wait_for` 一致);④ 超时后原异步生成器已被取消关闭,后续 `__anext__` 抛 `StopAsyncIteration` —— 因我们立即 `raise ActivityTimeout`,不构成行为差异。

- [ ] **步骤 1:写失败测试**

`teage_liu2/tests_core/test_llm_retry.py` 末尾追加:

```python
def test_activity_timeout_wrapper_passthrough():
    """验收:活跃超时 wrapper 对正常流逐项透传,不改内容。"""
    from teage_liu2.core.llm import _with_activity_timeout

    async def _fast():
        for c in "abc":
            yield c

    got: list = []

    async def run():
        async for item in _with_activity_timeout(_fast(), 1.0):
            got.append(item)

    asyncio.run(run())
    assert got == ["a", "b", "c"]


def test_activity_timeout_wrapper_calls_on_timeout_then_raises():
    """验收:空闲超时 → 先 await on_timeout(关流),再抛 ActivityTimeout。"""
    from teage_liu2.core.llm import _with_activity_timeout

    marks: list = []

    async def _slow():
        yield "a"
        await asyncio.sleep(5)
        yield "b"

    async def _on_timeout():
        marks.append("closed")

    async def run():
        async for _ in _with_activity_timeout(_slow(), 0.05, _on_timeout):
            pass

    with pytest.raises(ActivityTimeout):
        asyncio.run(run())
    assert marks == ["closed"]


def test_activity_timeout_wrapper_does_not_swallow_external_cancel():
    """验收:外部取消(任务 cancel)不得被误转成 ActivityTimeout。"""
    from teage_liu2.core.llm import _with_activity_timeout

    async def _blocked():
        yield "a"
        await asyncio.sleep(5)

    async def main():
        task = asyncio.create_task(_consume())
        await asyncio.sleep(0.05)
        task.cancel()
        try:
            await task
        except asyncio.CancelledError:
            return "cancelled"
        return "not-cancelled"

    async def _consume():
        async for _ in _with_activity_timeout(_blocked(), 30.0):
            pass

    assert asyncio.run(main()) == "cancelled"
```

`teage_liu2/tests_core/test_hooks.py` 末尾追加:

```python
def test_hook_timeout_isolates_and_chain_continues():
    """验收:钩子超时只跳过该枝干,后续枝干照常执行(H-6)。"""
    import asyncio as _asyncio

    from teage_liu2.core.hooks import Branch, HookChain
    from teage_liu2.core.types import Snapshot

    calls: list = []

    class _Slow(Branch):
        name = "slow"

        async def before(self, snapshot):
            await _asyncio.sleep(5)
            calls.append("slow")
            return []

    class _Fast(Branch):
        name = "fast"

        async def before(self, snapshot):
            calls.append("fast")
            return []

    chain = HookChain(hook_timeout=0.05)
    chain.register(_Slow())
    chain.register(_Fast())
    snap = Snapshot(session_id="s", user_input="u")

    async def run():
        return await chain.before_all(snap)

    _asyncio.run(run())
    assert calls == ["fast"], "超时枝干被跳过,后续枝干必须照常执行"
```

- [ ] **步骤 2:跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_llm_retry.py teage_liu2\tests_core\test_hooks.py -q`
预期: 3 条新 LLM 用例 + 1 条 hooks 用例 **均通过**(它们是"语义不变"的护栏,不是红测试)。若其中任一条失败,说明现实现与声明语义不符 —— 先查清再继续(不得放宽断言)。

> 说明:本任务的性质是"等价替换 + 降开销",护栏测试天然是绿的;红/绿循环由步骤 4 的**变异验证**承担。

- [ ] **步骤 3:写实现**

`teage_liu2/core/llm.py` 的 `_with_activity_timeout` 函数体替换为:

```python
async def _with_activity_timeout(
    aiter: AsyncIterator[Any],
    timeout_sec: float,
    on_timeout: Optional[Callable[[], Any]] = None,
) -> AsyncIterator[Any]:
    """通用 per-item 活跃超时 wrapper(async generator)。

    对输入逐项等待,每项之间最多等 ``timeout_sec`` 秒;超时则 ``await on_timeout()``
    (失败仅告警,不掩盖 ActivityTimeout),然后 raise。

    实现选择(2026-09-10):用 ``asyncio.timeout`` 而非 ``asyncio.wait_for`` —— 后者
    每次都要新建 Task + 定时器 + 取消机制(实测 ≈9.5 µs/chunk;8000 chunk 白缴 77 ms),
    前者直接对当前 await 施加取消(实测 ≈2.5 µs/chunk)。语义等价性已逐条验证:
    TimeoutError 别名、on_timeout 触发、内部 await 收到 CancelledError、
    与外层 ``asyncio.timeout``(step 的总超时)可安全嵌套、外部取消不被误转。
    """
    ait = aiter.__aiter__()
    while True:
        try:
            async with asyncio.timeout(timeout_sec):
                item = await ait.__anext__()
        except StopAsyncIteration:
            return
        except asyncio.TimeoutError:
            if on_timeout is not None:
                try:
                    await on_timeout()
                except Exception as close_exc:
                    logger.warning("on_timeout 回调失败(已忽略): %r", close_exc)
            raise ActivityTimeout(
                f"LLM 在 {timeout_sec}s 内未返回任何 token"
            )
        yield item
```

`teage_liu2/core/hooks.py` 的 `HookChain._call` 函数体替换为:

```python
        try:
            async with asyncio.timeout(self.hook_timeout):
                return await getattr(branch, hook_name)(*args, **kwargs)
        except asyncio.TimeoutError:
            logger.error(
                "%s: 枝干 %s 的 %s 钩子超时(>%.1fs),跳过该枝干",
                HOOK_TIMEOUT, branch.name, hook_name, self.hook_timeout,
            )
        except Exception as e:
            logger.error(
                "%s: 枝干 %s 的 %s 钩子异常,跳过: %s",
                HOOK_EXCEPTION, branch.name, hook_name, e,
            )
        return None
```

- [ ] **步骤 4:跑测试 + 变异验证**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q`,预期全绿。
**变异验证(必做,防空断言)**:把 `except asyncio.TimeoutError` 临时删掉 → `test_activity_timeout_wrapper_calls_on_timeout_then_raises` 与 `test_hook_timeout_isolates_and_chain_continues` 必须变红;恢复后复跑全绿。

- [ ] **步骤 5:量化验证**

仓库外脚本对比(2 项,各取 5 次最小值):
- `_with_activity_timeout` 包裹 8000 项 → 预期 ≈19.5 ms(修前 ≈77.5 ms);
- 10 枝干的 `before_all` 一轮 → 预期 ≈0.10 ms(修前 ≈0.38 ms)。

- [ ] **步骤 6:提交(待用户批准)**

```bash
git add teage_liu2/core/llm.py teage_liu2/core/hooks.py teage_liu2/tests_core/test_llm_retry.py teage_liu2/tests_core/test_hooks.py
git commit -m "perf(teage_liu2/core): 超时原语换 asyncio.timeout——逐 chunk/逐钩子开销降 4×"
```

---

## 任务 6:L3 批处理修复(即时触发 + task 记账 + deque + close 兜底)

**文件:**
- 修改: `teage_liu2/core/event_stream.py`(类 `L3ObserverQueue` 内部第 125 行的 `deque` 用法**保持不变**;类 `L3BatchSink` 定义在 110 行、改动段在 155-267)
- 测试: 新建 `teage_liu2/tests_core/test_l3_batch.py`

**接口:**
- 消费: 无
- 产出: `L3BatchSink.accept` 在缓冲达到 `batch_max` 时**立即**冲刷;新增 `_timer_task` 引用(在**任务创建时**登记,而非首次运行时)以区分"定时窗口"与"在途冲刷";新增 `_start_timer()` 统一启动窗口;`_flush` 循环消费到缓冲为空(在途期间新到事件由同一任务续送);`_buffer` 为 `deque`;`_timer` 只清理仍指向自身的引用;`close()` 只取消定时窗口、不取消在途冲刷,先冲刷缓冲再逐观察者兜底投递

- [ ] **步骤 1:写失败测试**

新建 `teage_liu2/tests_core/test_l3_batch.py`:

```python
"""L3 观测批处理旁路(§18.5):50ms 窗口 / 64 条先到触发 / 有界队列 / close 兜底。"""
from __future__ import annotations

import asyncio
from collections import deque

from teage_liu2.core.event_stream import L3BatchSink


def _sink(window: float, batch_max: int, queue_max: int = 1024):
    sink = L3BatchSink(batch_window=window, batch_max=batch_max, queue_max=queue_max)
    got: list = []

    async def deliver(batch):
        got.extend(batch)

    sink.subscribe("obs", deliver)
    return sink, got


def test_batch_max_triggers_immediate_flush():
    """验收:达到 batch_max 立即冲刷,不等窗口(窗口设 10s 以示区分)。

    断言必须在 close() **之前** —— close() 自身会冲刷缓冲,放在其后则修复前后
    都通过(该用例将失去判别力)。
    """
    sink, got = _sink(10.0, 4)

    async def run():
        for i in range(4):
            sink.accept({"type": "tool_result", "i": i})
        await asyncio.sleep(0.05)   # 远小于 10s 窗口:只有即时触发才可能此刻已投递
        delivered = [e["i"] for e in got]
        buffered = len(sink._buffer)
        await sink.close()
        return delivered, buffered

    delivered, buffered = asyncio.run(run())
    assert delivered == [0, 1, 2, 3], "达到 batch_max 必须立即冲刷(不等窗口)"
    assert buffered == 0, "即时冲刷后缓冲必须为空"


def test_immediate_flush_does_not_cancel_inflight_delivery():
    """验收:在途冲刷不被取消,其批次照常送达(取消在途会丢事件)。"""
    sink = L3BatchSink(batch_window=10.0, batch_max=2)
    got: list = []

    async def deliver(batch):
        await asyncio.sleep(0.02)   # 制造"在途"窗口
        got.extend(batch)

    sink.subscribe("obs", deliver)

    async def run():
        sink.accept({"type": "tool_result", "i": 0})
        sink.accept({"type": "tool_result", "i": 1})   # 触发即时冲刷(开始投递)
        await asyncio.sleep(0.005)                      # 此刻冲刷在途
        sink.accept({"type": "tool_result", "i": 2})
        sink.accept({"type": "tool_result", "i": 3})   # 再次触发即时冲刷
        await asyncio.sleep(0.2)
        delivered = sorted(e["i"] for e in got)         # ← 断言取在 close() 之前
        await sink.close()
        return delivered

    assert asyncio.run(run()) == [0, 1, 2, 3], "在途冲刷不得被取消/丢事件"


def test_buffer_is_deque_and_drops_oldest():
    """验收:缓冲为 deque(O(1) 丢弃);满则丢最旧并计数。"""
    sink, got = _sink(10.0, 1000, queue_max=3)

    async def run():
        for i in range(5):
            sink.accept({"type": "tool_result", "i": i})
        assert isinstance(sink._buffer, deque)
        assert sink.total_dropped() == 2
        await sink.close()

    asyncio.run(run())
    assert [e["i"] for e in got] == [2, 3, 4], "满缓冲丢最旧,保留最新"


def test_timer_window_still_flushes():
    """验收:不足 batch_max 时仍由窗口定时冲刷,且冲刷后窗口可再次建立。"""
    sink, got = _sink(0.02, 64)

    async def run():
        sink.accept({"type": "tool_result", "i": 0})
        await asyncio.sleep(0.06)
        assert [e["i"] for e in got] == [0]
        sink.accept({"type": "tool_result", "i": 1})
        await asyncio.sleep(0.06)
        await sink.close()

    asyncio.run(run())
    assert [e["i"] for e in got] == [0, 1], "第二次窗口必须照常建立"


def test_close_delivers_everything():
    """验收:close 冲刷缓冲与各观察者队列,不丢事件。"""
    sink, got = _sink(10.0, 1000)

    async def run():
        for i in range(5):
            sink.accept({"type": "tool_result", "i": i})
        await sink.close()

    asyncio.run(run())
    assert [e["i"] for e in got] == [0, 1, 2, 3, 4]
```

- [ ] **步骤 2:跑测试确认它失败**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core\test_l3_batch.py -q`
预期: `test_batch_max_triggers_immediate_flush` FAIL(断言点在 `close()` 之前,`delivered == []`);`test_immediate_flush_does_not_cancel_inflight_delivery` FAIL(旧实现取消在途冲刷 → 丢 `[0, 1]`);`test_buffer_is_deque_and_drops_oldest` FAIL(`list` 不是 `deque`);`test_timer_window_still_flushes` 与 `test_close_delivers_everything` 通过(行为保持型护栏)。

- [ ] **步骤 3:写最简实现**

`teage_liu2/core/event_stream.py` 顶部导入补 `deque` 已有(`from collections import deque` 已在文件中)。

`L3BatchSink.__init__` 的缓冲与任务引用初始化为:

```python
        self._buffer: "deque[Dict[str, Any]]" = deque()
        #: 定时窗口任务(与"在途冲刷"区分:立即冲刷只取消定时窗口,绝不取消在途冲刷)
        self._timer_task: Optional[asyncio.Task] = None
```

`accept` 的缓冲与调度段改为:

```python
        if len(self._buffer) >= self.buffer_max:
            # 满则丢弃最旧 + 计数(§15-A6):观测者投递受阻时缓冲不无界堆积
            self._buffer.popleft()
            self._buffer_dropped += 1
        self._buffer.append(event)
        if len(self._buffer) >= self.batch_max:
            self._schedule_flush(immediate=True)
        else:
            self._start_timer()
```

`_start_timer` / `_schedule_flush` / `_timer` 替换为(新增 `_start_timer`;`_schedule_flush` 保留原公开签名):

```python
    def _start_timer(self) -> None:
        """启动定时窗口(已有活跃窗口则不重复启动)。

        ``_timer_task`` 在**任务创建时**登记(而非任务首次运行时)—— 否则同一事件循环
        tick 内"先建窗口、再达 batch_max"会把未启动的定时任务误判为在途冲刷,
        导致即时冲刷被跳过(实测:即时触发失效)。
        """
        if self._timer_task is not None and not self._timer_task.done():
            return
        self._timer_task = asyncio.create_task(self._timer())
        self._flush_task = self._timer_task

    def _schedule_flush(self, immediate: bool = False) -> None:
        """冲刷调度:immediate=True 只取消**待触发的定时窗口**并立刻冲刷(§18.5 64 条先到)。

        在途冲刷(已取走缓冲、正在投递)绝不被取消 —— 其 ``while`` 循环会继续消费
        当前缓冲,故此时无需另起任务。
        """
        if not immediate:
            self._start_timer()
            return
        timer = self._timer_task
        if timer is not None and not timer.done():
            timer.cancel()
        self._timer_task = None
        current = self._flush_task
        if current is None or current is timer or current.done():
            self._flush_task = asyncio.create_task(self._flush())

    async def _timer(self) -> None:
        """定时窗口(batch_window):到期冲刷缓冲。"""
        me = asyncio.current_task()
        try:
            await asyncio.sleep(self.batch_window)
        finally:
            # 只清理"仍指向自己"的引用:期间若已调度了立即冲刷/新窗口,不得清别人的
            if self._timer_task is me:
                self._timer_task = None
            if self._flush_task is me:
                self._flush_task = None
        if self._buffer and not self._closed:
            await self._flush()
```

`_flush` 替换为(循环消费到缓冲为空):

```python
    async def _flush(self) -> None:
        """冲刷缓冲 → 分发各观测扩展有界队列 → 批量投递。

        循环消费到缓冲为空:投递期间新到达的事件(``accept`` 在 await 间隙追加)
        由同一任务继续送达,避免"缓冲非空却无待触发任务"的滞留。
        取缓冲是"拷贝 + 清空"的原子段(无 await),并发冲刷不会重复投递同一批。
        """
        while self._buffer:
            batch = list(self._buffer)
            self._buffer.clear()
            for queue in list(self._observers.values()):
                for ev in batch:
                    queue.put(ev)
                await queue.deliver()
```

`close` 替换为:

```python
    async def close(self) -> None:
        """关闭(幂等):停收新事件 → 冲刷缓冲 → 逐观察者兜底投递未送达事件。

        只取消**定时窗口**;在途冲刷不取消(其自身会送达),已入队但未投递的事件
        再由下方兜底循环送达 —— 旁路 best-effort,但"已接收事件不因关闭而静默丢失"。
        """
        if self._closed:
            return
        self._closed = True
        timer, self._timer_task = self._timer_task, None
        if timer is not None and not timer.done():
            timer.cancel()
        self._flush_task = None
        try:
            await self._flush()
            for queue in list(self._observers.values()):
                if queue.pending:
                    await queue.deliver()
        except Exception as e:
            logger.warning("L3 旁路关闭冲刷失败(best-effort,不阻断 shutdown): %s", e)
```

- [ ] **步骤 4:跑测试确认通过**

运行: `D:\soft\Python311\python.exe -m pytest teage_liu2\tests_core -q`
然后: `D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py`
预期: 全绿;套件 21/21。**注意**:用例 `l3-observe-13` 的 `bounded_queue_no_drop` / `l1_never_in_transport` 在 runner 中是**类型断言**(`match_value` 只校验 `{"type": ...}` 声明的类型,不校验真值),属弱断言 —— 本任务新增的 `test_l3_batch.py` 才是这批语义的承重锚。

- [ ] **步骤 5:提交(待用户批准)**

```bash
git add teage_liu2/core/event_stream.py teage_liu2/tests_core/test_l3_batch.py
git commit -m "fix(teage_liu2/core): L3 批处理恢复 64 条即时触发 + task 记账 + deque + close 兜底"
```

---

## 任务 7:文档回流 + 全量门禁

**文件:**
- 修改: `docs/plans/2026-09-10-liu2-core性能审查报告.md`
- 修改: `teage_liu2/docs/plans/开发日志.md`
- 修改: `teage_liu2/PROTOCOL/PENDING.md`
- 修改: `CHANGELOG-开发日志.md`(根,顶部追加一条)

**接口:** 无(纯文档)

- [ ] **步骤 1:更正审查报告(诚实口径)**

在 `docs/plans/2026-09-10-liu2-core性能审查报告.md` 中:
1. §2.4 / §3 P1-3 的"json_depth 便宜、dumps 占 95%"改为实测拆分(各占 ≈50%,迭代版快 2×);
2. 把"快照预算"从 P2-1 **升级**为 P1,并换上新数据(0.61 MiB:3.156 ms vs 去预算 0.008 ms);
3. 新增三项本轮新发现:**B1 落盘 fsync 税**、**A1 深嵌套帧终止读循环**、**A2 L3 64 条触发失效**;
4. 顶部加一行指向本计划:`> 修复执行计划:docs/plans/2026-09-10-core性能与健壮性完善-执行计划.md`。

- [ ] **步骤 2:开发日志 + CHANGELOG**

`teage_liu2/docs/plans/开发日志.md` **顶部**新增条目(日期 + 6 项变更 + 前后实测数字);根 `CHANGELOG-开发日志.md` 顶部追加一条摘要。

- [ ] **步骤 3:PENDING 留痕**

`teage_liu2/PROTOCOL/PENDING.md`:P-4 语义条款后追加一句"落盘档位语义 = WAL + `synchronous=NORMAL`(进程崩溃不丢,断电可能丢最近若干 commit);Rust 后端与宿主侧 SQLite 实现同档位";P-7 现状行注明"宿主双档的 flush 档语义同上"。**不改状态、不改 VERSION。**

- [ ] **步骤 4:全量门禁**

运行:
```bash
D:\soft\Python311\python.exe teage_liu2\PROTOCOL\behavior-suite\runner.py
powershell -File scripts\audit_liu2.ps1
git --no-pager status --short
```
预期: 套件 21/21;audit exit=0;工作区仅含本计划涉及文件(无临时文件残留)。

- [ ] **步骤 5:提交(待用户批准)**

```bash
git add docs/plans/2026-09-10-liu2-core性能审查报告.md docs/plans/2026-09-10-core性能与健壮性完善-执行计划.md teage_liu2/docs/plans/开发日志.md teage_liu2/PROTOCOL/PENDING.md CHANGELOG-开发日志.md
git commit -m "docs: core 性能与健壮性完善——审查报告数据更正 + 计划/日志/PENDING 留痕"
```

---

## 自审

**1. 规格覆盖(§0 清单 → 任务):** B1→任务 1;A1→任务 2+3;B4'→任务 2;B3→任务 4;B2→任务 5;C1→任务 5;A2/A3→任务 6;C5/C8→任务 7(仅记录);D1–D5→明确不改(§0 已注明)。**无遗漏项。**

**2. 占位符扫描:** 全文无 "TBD / TODO / 后续补 / 加适当校验";每个代码步骤均给出完整代码块;每个测试步骤给出可运行命令与预期结果。任务 1 步骤 0 是**决策动作**(需用户点头),非占位符 —— 且已给出否决分支的完整处置(仅补 busy_timeout + 改 docstring)。

**3. 类型一致性:**
- `_json_len(obj: Any) -> int` 定义在 `core/types.py`,由 `core/snapshot.py` 与 `Snapshot` 方法共同使用(单一来源,无重复实现)。
- `snapshot_bytes(snapshot: Snapshot) -> int` 定义在 `core/snapshot.py`(测试从该模块导入)。
- `_SizeCache`(字段 `messages_ref` / `msg_bytes` / `base_bytes`)在 `types.py` 定义、`snapshot.py` 消费,字段语义全文一致。
- 名字冲突已规避:`apply_action_batch` 内的局部变量 `valid`(合法 action 列表)与缓存有效性判定不得混用,故后者命名为 `cache_ok`。
- `types.py` 顶部已有 `dataclass / field / replace / Optional / List` 导入;`_SizeCache` 定义在其使用点之前。
- `check_frame_depth` / `check_frame_limits` / `decode(line, raw_bytes=None)` 名字在任务 2、3 的调用点逐字一致。
- `_base_unknown(cache)` 与 `_measure` / `_cache_for` 的失效协议一致(`None` = 整体未知,`-1` = 分量未知)。

**4. 风险与回滚:** 任务 4 的记账正确性由 `test_incremental_size_matches_ground_truth`(逐点对拍 `json.dumps`,含 revision 进位与截尾路径)与 `test_budget_check_does_not_serialize_whole_snapshot`(稳态序列化次数=0)双重锚定;任何记账漂移立即变红。任务 1 的档位变更若被否决,除该任务外其余任务独立成立。任务 5 的等价性由 4 条护栏 + 变异验证锚定。

**5. 计划自身经独立验证(两轮 plan-verifier 只读核查):**
- **第一轮**(81 tool uses)指出并已修正 4 项:①`_cache_for` 截尾判定写成死代码(`min()` 之后判 `len < common` 恒假)→ 改为截尾直接失效 + 用 `messages_ref` 同一性取代长度启发式;②`apply_action_batch` 漏记 `revision + 1` 的整数位数变化(9→10 偏差 1 字节)→ 已补差分;③记账测试断言 `calls["n"] == 0` 在首次检查时不可达 → 改为先预热再归零;④两处测试断言差一(`json_depth` 2001→2000、`JSON_MAX_DEPTH - 2`→`- 1`)。
- **第二轮**(定向复验,54 tool uses)确认上述 4 项**全部闭环**(含实测恒等式、进位差分与端到端仿真),并再指出 3 处、已修正:⑤任务 6 的 `test_batch_max_triggers_immediate_flush` 断言写在 `close()` 之后 → 修复前后都通过(**唯一承重锚失效**)→ 断言前移 + 断言缓冲为空,并新增 `test_immediate_flush_does_not_cancel_inflight_delivery`;⑥任务 1 的 `history.py` 代码块被误读为整个 `__init__` 替换(会丢 `_init_tables` / `_ensure_fts_table`)→ 改为"只替换 `PRAGMA journal_mode` 一行"并显式列出保持不动的行;⑦`_measure` 不可序列化分支把 base 存成 `0`(下次被当作"已知的 0 字节"→ 可能误拒)→ 改存 `-1` 回到历史语义。另收硬化:回滚路径的消息分量改为 `_json_len(rolled)`(不沿用可能陈旧的入参缓存值)。
- 另一条已记录事实:套件 `l3-observe-13` 的"不丢 / L1 不进 transport"实为**类型断言**(`match_value` 只查 `type`),语义强度由本计划新增的 `test_l3_batch.py` 兜住。
- **第三轮(主 agent 自验,仓库外脚本,已删)**:把任务 6 的设计逐字实现后跑 5 个场景 —— 首版设计 **s1 即败**(`delivered=[]`,`buffered=4`),根因是 `_timer_task` 在定时任务**首次运行**时才登记,同一 tick 内"先建窗口、后达 batch_max"会把未启动的定时任务误判为在途冲刷而跳过即时冲刷(实测 `task.cancel()` 后 `done()` 仍为 `False`,确认该误判成立)。已改为**创建时登记** `_timer_task` + 新增 `_start_timer()`,复跑结果:计划实现 **s1~s5 全 PASS**,旧实现 **s1/s2/s4 FAIL**(证明用例具备判别力)。凡本计划代码块与实测不符者,以实测为准。

---

## 执行顺序与门禁

1. 任务 1(需 D-1 批准)→ 2 → 3 → 4 → 5 → 6 → 7。
2. 每任务结束:对应测试文件全绿 + `tests_core` 全绿。
3. 全计划结束:`runner.py` 21/21 + `scripts/audit_liu2.ps1` exit=0。
4. 所有 commit 均**先经用户批准**;执行期如发现计划与代码事实不符(签名/行号/锚点),**以代码为准就地修正并记录偏差**,不硬套计划。
