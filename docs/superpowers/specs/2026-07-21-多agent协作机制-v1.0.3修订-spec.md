# Teage Multi-Agent Protocol v1.0.3 修订执行 spec

> 本 spec 是 `2026-07-20-多agent协作机制-design.md`（v1.0.2）的修订执行计划，整合 grill-me 第三轮 6 项决策（Q1-Q6）与 5 子智能体交叉验证发现（15 P0 + 22 P1 + 12 P2），共 55 项变更。完成后 design.md 升级为 v1.0.3。

---

## 0. 元信息

| 字段 | 值 |
|------|---|
| 修订目标版本 | v1.0.3 |
| 修订来源 | grill-me 第三轮 + 子智能体交叉验证（协议一致性 / 并发与锁安全 / 可靠性与故障恢复 / 错误处理与分层阻断 / 实施与集成 共 5 个维度） |
| 修订项总数 | 55（Q1-Q6 决策 6 + P0 15 + P1 22 + P2 12） |
| 修订原则 | 稳定优先 / 流畅度优先 / 易维护 / 易扩展 / 严格对齐 project_memory 硬约束 |
| 影响章节 | §3.0 / §3.3.2 / §3.3.3 / §3.3.4 / §3.3.5 / §3.3.7 / §3.5 / §3.6 / §3.7（新增）/ §4.4 / §7.1 / §7.2 / §8.2 / §8.3 / §10.0 / §10.2 / §10.3 / §10.4 / §10.5 / §10.6 / §11.1 / §11.2 / §11.3 / §17 |
| 不变项 | §1-§2 / §3.1 / §3.2 / §3.3.1 / §3.3.6 / §3.4 / §4.1-§4.3 / §4.5 / §5 / §6 / §7.3 / §8.1 / §8.4-§8.6 / §9 / §12-§16 结构不变，仅可能因前述章节变更同步引用 |

---

## 1. 修订原则

1. **结构不变**：v1.0.2 既有章节结构与编号保持不变，只补 / 改 / 删明确指出的位置
2. **单一事实源**：Q1 字段写权限矩阵与 Q6 CAS 合并策略合为一张"字段属性表"，避免双表脱钩
3. **硬约束优先**：所有修订项必须对齐 project_memory 中的 Hard Constraints，遇到冲突以硬约束为准
4. **开放问题给默认值**：本 spec 第 2 节列出的开放问题，每项给出"推荐默认值 + 理由"，用户未明确否决则按推荐值执行
5. **可回滚**：每章修订独立成段，回滚时按章节粒度恢复
6. **可验证**：每项修订对应至少 1 条可客观判定的验收标准（见第 6 节）

---

## 2. 开放问题与推荐默认值

| # | 开放问题 | 推荐默认值 | 理由 |
|---|---------|-----------|------|
| **O1** | §3.3.7 audit `action` enum：A 扩展至 50+ 值 vs B 保持 13 粗粒度 + `details.reason` 细分 | **B**（粗粒度 + reason） | 13 粗粒度已覆盖所有 action 大类，细分原因放 details.reason 与现有 `details` 字段设计一致；A 方案 50+ enum 维护成本高且新增场景需改 schema |
| **O2** | grace_period 期间原 fencing_token 写入是否允许 | **允许**（grace_until 期间原 token 写入仍被接受，grace_until 过期后拒绝） | 给原持锁者 5 秒缓冲完成在途写入，避免消息丢失；过期后新 token 已生效，旧 token 必须拒绝才能保证 fencing 单调性 |
| **O3** | emergency_release 原子性需 WAL，但 WAL 完整实现在 P1-R1 标为 v1.1 完成，v1.0.3 如何处理 | **v1.0.3 用 best-effort 模式**：先 CAS 释放 → 同步 append_audit，audit 失败则 fallback 写 `audit/audit.jsonl.recovery` 文件（不依赖 audit.lock），启动时 RecoveryCoordinator 检测 `.recovery` 文件并合并到 audit.jsonl | WAL 完整方案需 v1.1 设计，v1.0.3 不能因 WAL 未就绪而阻塞；best-effort + recovery 文件兜底保证 audit 不丢，符合"流畅度优先" |
| **O4** | 集成点 5/6/7 注入位置：ReactLoop 是否有 `_before_speak` / `_build_chat_messages` 方法 | **新增方法**：v1.0.3 在 ReactLoop 中新增 `_before_speak`（消息追加前钩子）和 `_build_chat_messages`（构造 LLM 请求 messages 时钩子）；不修改现有 `run` / `run_stream` / `_execute_tool` 签名 | 现有 ReactLoop 无这两个方法，但消息追加和 LLM 请求构造是天然 hook 点；新增方法不破坏现有调用链 |
| **O5** | §3.3.4 trust_policy.trust_score 是否已定义，priority 排序键能否依赖 | **不依赖 trust_score**：priority 排序键改为 `last_heartbeat_age asc → agent_id asc`（字典序兜底），trust_score 留待 v1.1 引入 | §3.3.4 trust_policy 段无 trust_score 字段定义，v1.0.3 不应假设未定义字段；用 heartbeat 新鲜度 + agent_id 已能提供确定性排序 |
| **O6** | CAS 字段级合并作为"重试耗尽降级"，是否需要配置开关 | **默认开启，可配置关闭**：新增 `multiagent.cas.merge_on_exhausted: true`（默认 true），false 时重试耗尽直接抛 `CASConflictError` 严格阻断 | 字段级合并是流畅度优化，但可能掩盖真实并发问题；提供关闭开关便于调试和严格场景 |
| **O7** | 集成点 2 capabilities 校验下沉 ToolExecutor，multiagent 状态如何注入 | **构造注入**：`ToolExecutor.__init__` 新增可选参数 `multiagent_state: MultiAgentState | None = None`，由 DI 容器在 multiagent.enabled=true 时注入；multiagent_state 持有 `worker_capabilities: set[str]` 和 `agent_id: str` | 与现有 `policy_engine` 注入方式一致；multiagent.enabled=false 时传 None，零侵入 |
| **O8** | 集成点 3/4 改 SessionManager，钩子如何加 | **新增 hook 字段**：SessionManager 新增 `self._multiagent_hooks: list[Callable] = []`，multiagent 模块在容器注册时通过 `session_manager.add_multiagent_hook(on_start, on_end)` 注册；SessionManager 在 session 创建/销毁时遍历调用 | 与现有 SessionManager 结构兼容；multiagent 模块自包含注册逻辑，不污染 SessionManager 业务代码 |
| **O9** | observer 必选字段补全：补 `last_heartbeat + heartbeat_interval_seconds` vs 去 heartbeat 操作 | **补字段**：observer 必选字段扩展为 `agent_id + role + status + last_heartbeat + heartbeat_interval_seconds` | observer 也需要被 Director 监控存活，去掉 heartbeat 会让 observer 静默离线无法检测；补字段与 worker/director 一致更清晰 |
| **O10** | §10.4 容器注册位置未指定，应在哪个文件 | **`teage_liu/lifespan.py`**：multiagent 组件在 lifespan 启动序列中按依赖图注册；`multiagent.enabled=false` 时跳过注册 | lifespan.py 是现有容器初始化入口（与项目硬约束 line 86"容器注册阶段应在路由拆分阶段之前执行"一致） |

---

## 3. 决策总表（55 项）

### 3.1 Q1-Q6 决策（grill-me 第三轮，已用户确认）

| ID | 议题 | 决策 |
|----|------|------|
| Q1 | status.json 写权威 | 方案 B：放宽 §3.3.3 措辞，新增"字段写权限矩阵"小节 |
| Q2 | messages.pending / replay_candidates | 方案 A：拆文件，新增两独立文件，Director flush 时分配全局 seq |
| Q3 | §11.2 引用未定义异常类 | 方案 B：拆三子表（异常类 / 字段标记 / 系统响应动作），§11.1 补 6 异常类 |
| Q4 | §11.2 与 §11.3 重复 | 方案 B：删 §11.3 总表，§11.2 三子表加阻断层列，§11.3 降级为原则说明段 |
| Q5 | §7.1 挂钟 vs §8.2 单调时钟 | 方案 C：删"改用单调时钟"，>5s 放大 grace_period，>60s 暂停锁强制释放 |
| Q6 | CAS 字段级合并分类 | 方案 A：Q1 字段写权限矩阵与 Q6 CAS 合并策略合为"字段属性表" |

### 3.2 P0 阻断问题（15 项，必须修复）

| ID | 章节 | 问题摘要 | 处置方案 |
|----|------|---------|---------|
| P0-1 | §3.3.3 / §7.1 | `last_fencing_token` 字段 schema 缺失但已引用 | §3.3.3 补字段；§7.1 补 `_next_fencing_token` 伪代码（每次读 status.json，CAS 同写 last_fencing_token 与 locks[name].fencing_token） |
| P0-2 | §3.3.3 / §7.1 | `grace_until` / `force_releasing` 字段 schema 缺失，grace 期间写入策略矛盾 | §3.3.3 locks schema 补两字段；§7.1 补 `_mark_grace_period` / `_delayed_clear` 伪代码；明确 grace 期间原 token 允许写入（O2 默认值） |
| P0-3 | §3.3.3 / §11.2 | CAS 重试上限 3 次 vs 2 次矛盾，违反 project_memory 第 17 行 | §3.3.3 改 2 次；§11.2 拆两行：重试中（严格阻断 2 次）+ 重试耗尽降级（软约束字段级合并，受 O6 配置开关控制） |
| P0-4 | §3.3.3 / §8.3 | 自治期 current_turn 写者未定义，与 §3.3.3 矛盾 | §3.3.3 补例外（自治期 Worker 可 CAS 写 current_turn）；§8.3 表格明确此点 |
| P0-5 | §3.3.3 / §11.2 | §3.3.3 line 494"多写者" vs line 557-559"唯一权威写者"语义矛盾 | 按 Q1+Q6 落地：Step 1 已修改 line 494 措辞为"协作写入资源"；Step 2 改写 line 557-559 段落为"Director 主要写者 + Worker 受限写入"并新增"字段属性表"（详见 §4.3） |
| P0-6 | §3.3.3 / §3.3.2 / §4.4 | `director_status` 枚举三处不一致（active/autonomous/recovering vs degraded vs online） | §3.3.3 补正式枚举表 `active / autonomous / recovering / degraded`；§4.4 online→active；§3.3.2 degraded 保留 |
| P0-7 | §3.3.4 / §4.4 | agent_card.status 枚举 vs §4.4 表格用 online 不在枚举 | §4.4 表格状态转换列 online→active |
| P0-8 | §3.3.7 / 全文 | audit `action` enum 仅 13 值，全文用 40+ 个未声明值 | 按 O1 默认值：保持 13 粗粒度 + details.reason 细分，全文替换 |
| P0-9 | §3.7 | 修订历史声明新增 §3.7 但章节完全缺失 | 补 §3.7 命名规范章节（ID / 文件 / 扩展字段 `x_<owner>_<field>` 命名） |
| P0-10 | §3.6 / §3.5 / §10.2 | §3.6 路径示例 `${TEAGE_BB_DIR:./data/blackboard}` 违反硬约束 | 改为 `${TEAGE_BB_DIR}`；§3.6 line 975-994 重复段整段删除，引用 §3.5 |
| P0-11 | §10.4 | `CONFIG_TO_COMPONENTS["multiagent"]` 列表 `audit_logger` 与 2137 行 `multiagent_audit_logger` 矛盾 | 列表项改名 `multiagent_audit_logger` |
| P0-12 | §10.3 | 新增 3 项 `_RESTART_REQUIRED_KEYS` 打破"缩减为 7 项"硬约束 | 仅保留 `multiagent.blackboard_dir`（变 8 项）；`a2a_gateway.listen_port` / `auth_schemes` 改热重载 |
| P0-13 | §11.2 | 危险工具清单 `execute_command/file_delete/mcp_call` 与 project_memory 第 4 行 `execute_command/write_file/call_tool` 不一致 | 全文统一为 `execute_command/write_file/call_tool`；新增配置项 `multiagent.worker.dangerous_tools` 默认即此 3 项 |
| P0-14 | §10.6 | ReactLoop 集成点缺失 NotMyTurnError 触发入口 | 新增集成点 5：发言前轮次校验，注入 `_before_speak`（O4 默认值） |
| P0-15 | §10.6 | ReactLoop 集成点缺失 DirectorUnavailableError 触发入口 | 新增集成点 6：Director 心跳监测后台任务，会话启动时启动 |

### 3.3 P1 关键问题（22 项，发布前修复）

#### 协议层（6 项）

| ID | 章节 | 问题摘要 | 处置方案 |
|----|------|---------|---------|
| P1-1 | §3.3.4 | observer 必选字段与必选操作集矛盾 | 按 O9 默认值：observer 必选字段补 `last_heartbeat + heartbeat_interval_seconds` |
| P1-2 | §3.3.5 / §3.3.2 / §8.2 / §8.3 | Director 通知用 system 还是 broadcast 不明确 | §3.3.5 消息类型语义表补"使用场景示例"列：Director 全局事件用 system，Worker 主动广播用 broadcast |
| P1-3 | §3.3.3 | `phase` 字段无枚举值定义 | 补枚举表 `initializing / active / paused / ended` |
| P1-4 | §3.5 | director_id 无正则约束 | §3.5 ID 正则表补注"director_id 遵循 agent_id 正则 `^[a-z0-9_]{3,32}$`" |
| P1-5 | §9.1 | A2A 翻译规则表遗漏 protocol.md / status.json / audit.jsonl | §9.1 表后补说明"三者为本地实现细节，不暴露给 A2A，无需翻译" |
| P1-6 | §3.3.7 | audit `actor` 字段允许 system/watchdog 但字段定义为 agent_id | §3.3.7 字段说明扩展为"操作者 agent_id 或系统组件名（system/watchdog/director）" |

#### 并发锁（5 项）

| ID | 章节 | 问题摘要 | 处置方案 |
|----|------|---------|---------|
| P1-7 | §3.3.2 | Director 启动互斥锁无 OS 锁未释放 fallback | 明确用 F_SETLK 非阻塞；补 fallback：fcntl 失败 + tick 过期 → emergency_release("director", reason="holder_dead") → 重新 fcntl → 递增 epoch |
| P1-8 | §3.3.7 / §7.1 | audit.lock 与 status.json CAS 嵌套死锁风险 | audit.lock 改用 portalocker 文件锁（`locks/audit.lock` 文件级锁），绕过 CAS；TTL 从 5s 提到 30s；声明锁顺序：status.json CAS > audit.lock，audit.lock 必须是叶子锁 |
| P1-9 | §7.1 | LockManager 单例与内存级竞态未声明 | §7.1 明确 LockManager 是 Worker 进程内单例；`_next_fencing_token` 不维护内存计数器，每次读 status.json（与 P0-1 联动） |
| P1-10 | §7.1 | emergency_release 与 audit 原子性断裂 | 按 O3 默认值：v1.0.3 用 best-effort + `audit/audit.jsonl.recovery` 兜底；v1.1 完整 WAL 方案 |
| P1-11 | §7.1 | renew vs force release 竞态未定义重读策略 | §7.1 补：force release CAS 失败后重读检查 expires_at 已更新则放弃（audit action=lock_force_release, details.reason="holder_renewed"）；renew CAS 失败后重读检查 grace_until 已设置则抛 FencingTokenMismatchError |

#### 可靠性（8 项）

| ID | 章节 | 问题摘要 | 处置方案 |
|----|------|---------|---------|
| P1-12 | §8.1 / §17.1 | WAL 损坏恢复流程未覆盖 | §17.1 P1 项 5 计划列扩展：v1.1 含 WAL schema + rebuild_from_wal + WAL/audit 写入顺序 + WAL 损坏降级 |
| P1-13 | §8.1 / §8.3 | 快照恢复未集成进 §8.3 崩溃恢复流程 | §8.3 第 1 步前插入"0. 检查 snapshots/，存在则解压到临时目录，从 audit.jsonl 过滤 ts > snapshot_ts 重放"；快照恢复写 status.json.recovered 后原子 rename |
| P1-14 | §8.3 | 自治退出 Director 二次崩溃竞态 | `_exit_autonomous_mode` 后立即二次确认 last_director_tick 新鲜，不新鲜则回滚自治 + audit action=recovery_start, details.reason="autonomous_exit_rollback"（v1.0.3 修订：对齐 O1 默认值） |
| P1-15 | §8.2 | watchdog 永久标记未持久化 | 新增 `data_dir/watchdog_state.json` 持久化 `{"mode": "polling_permanent", ...}`；启动时读取，跳过自检；新增配置 `multiagent.watchdog.reset_to_watchdog`（重启生效） |
| P1-16 | §7.1 / §8.3 | fence 范围限定在 LockManager.acquire 未实现 | §7.1 acquire 第 2 步前插入 fence 检查：`if lock_name == "messages" and status.director_status == "recovering": raise LockAcquisitionError(reason="fence_period_messages_blocked")` |
| P1-17 | §3.3.2 / §8.2 | priority 排序键未定义 | 按 O5 默认值：priority 排序键 `last_heartbeat_age asc → agent_id asc`；§3.3.2 补注释，§8.2 LLM 不可用行加注 |
| P1-18 | §3.3.2 | Director 网络分区恢复路径缺失 | §3.3.2 启动互斥锁流程后增"硬超时强抢"分支：director.lock 持续被持有 + tick 陈旧 > 2×heartbeat_timeout → 强制抢占 + 递增 epoch + 广播 director_preempt_started |
| P1-19 | §8.6 | 资源配额 enforcement 完全未定义 | §8.6 补子表：max_tasks_per_agent / max_messages_per_minute / lock_ttl 上限；配置位置 director.md.quotas；Director 在 create_task / grant_turn 时 enforcement，超限拒绝 + audit action=write, details.reason="quota_exceeded"（v1.0.3 修订：对齐 O1 默认值） |

#### 错误处理（3 项）

| ID | 章节 | 问题摘要 | 处置方案 |
|----|------|---------|---------|
| P1-20 | §3.3.5 | InjectionIsolator.audit_log 同步调用违反全链路异步硬约束 | `InjectionIsolator.__init__` 注入 bb_root；两处 `audit_log(...)` 改 `await append_audit(self._bb_root, {...})`；`scan_and_tag` 改 async |
| P1-21 | §11.2 | 系统响应动作类触发主体全部未指定 | §11.2.3 子表新增"触发组件 + 触发任务"两列（DiskFull→Blackboard.atomic_write 前 / WatchdogSelfTestFailed→Watchdog._self_test 每 5 分钟 / LockForceReleaseHolderDead→LockManager._scan_stale_locks 每 30 秒 等） |
| P1-22 | §11.1 | _CATEGORY_ZH 缺 15+ category | 补全：director_signature_failed / injection_suspected / message_truncated / path_normalized / schema_validation_error / disk_full / read_only_fs / clock_drift / watchdog_self_test_failed / recovery_fence_timeout / lock_force_release / a2a_gateway 等 |

#### 实施集成（3 项 — 已在 P0-11/P0-12/P0-14/P0-15 覆盖部分，以下为剩余）

| ID | 章节 | 问题摘要 | 处置方案 |
|----|------|---------|---------|
| P1-23 | §10.6 | 集成点 2 双重拦截违反"单一瓶颈点"硬约束 | 按 O7 默认值：capabilities 校验下沉 ToolExecutor.evaluate_policy 前；删除 ReactLoop._execute_tool 中的 capabilities 校验 |
| P1-24 | §10.6 | 集成点 3/4 假设 ReactLoop 有 _on_session_start/_on_session_end，实际在 SessionManager | 按 O8 默认值：SessionManager 新增 `_multiagent_hooks` 字段，multiagent 模块通过 `add_multiagent_hook(on_start, on_end)` 注册 |
| P1-25 | §3.3.5 / §10.6 | InjectionIsolator.build_llm_context 集成点缺失 | 新增集成点 7：ReactLoop `_build_chat_messages` 调用 `InjectionIsolator.build_llm_context` 包装其他 agent 消息 |

### 3.4 P2 改进建议（12 项，可推迟 v1.1）

| ID | 章节 | 问题摘要 | 处置方案 |
|----|------|---------|---------|
| P2-1 | §11.1 | ErrorStage.PROTOCOL 枚举扩展声明缺失 | §11.1 补"ErrorStage 枚举扩展声明"段，明确 PROTOCOL 是 multiagent 模块扩展值，ReactLoop tool_result 链路仅识别 PRE_EXECUTION/EXECUTION |
| P2-2 | §11.2 | LockAcquisitionError"相同失败"判定模糊 | §11.2 末尾补判定规则表：同 lock_name + 同 current_holder 算相同失败；holder 变化或 lock_name 变化算不同参数 |
| P2-3 | §7.3 | LLM 仲裁器并发无限制 | §7.3 明确 asyncio.Semaphore(1) 串行化，避免 audit 并发写入冲突 |
| P2-4 | §3.3.2 / §7.2 | freeform 模式锁公平性缺失 | §17.1 新增 P1 项"freeform 模式 wait_queue FIFO 公平性" |
| P2-5 | §8.3 | 自治模式 CAS 竞争率未评估 | §17.1 新增 P1 项"自治模式 CAS 冲突率基准测试" |
| P2-6 | §8.5 | 告警自动响应动作未定义 | §17.1 P1 项 9 计划列扩展为"告警规则 / 通知通道 / 自动响应动作映射表" |
| P2-7 | §8.3 | 自治期锁恢复 trade-off 未声明 | §8.3 自治模式表"锁管理"行补注 trade-off + 建议自治期 TTL 缩短为 10s |
| P2-8 | §12.2 | TDD 批次编号与调度增强批次冲突 | §12.2 批次改前缀命名 MA-批次 1-7 |
| P2-9 | §13 | 退出条件 8 缺测试用例 | 验收脚本新增 `pytest tests/multiagent/test_hot_reload_no_regression.py -v` |
| P2-10 | §15 | 验收标准 #6/#7/#9 含主观词 | 改写为可客观判定形式（_RESTART_REQUIRED_KEYS 集合一致 / pytest -k 'not multiagent' 100% 通过 / git log --grep 'test(red):' 校验） |
| P2-11 | §10.2 | multiagent 段未加 _validate_config_schema 校验 | 修改 `teage_liu/config_helpers.py:118`，将 'multiagent' 加入段类型校验元组 |
| P2-12 | §10.4 | 容器注册位置未指定 | 按 O10 默认值：明确在 `teage_liu/lifespan.py` 注册，hot_reloadable 标志：blackboard/a2a_gateway=False，其余=True |

---

## 4. 章节修订详情

> 每项含：位置 / 变更类型（补/改/删）/ 原内容 / 新内容 / 理由 / 依赖

### 4.1 §3.0 版本演进策略

**[改] §3.0 表格"位置"列：protocol_version 行**

- 原内容：`protocol.md / 所有协议文件`
- 新内容：`protocol.md / director.md / status.json / agents/{id}.md`
- 理由：messages.md / tasks/{id}.md / audit.jsonl 单条记录无需版本字段（P-2 协议一致性发现）
- 依赖：无

### 4.2 §3.3.2 Director 协议

**[补] §3.3.2 启动互斥锁流程后"硬超时强抢"分支**

- 位置：line 444-450 之后
- 新内容：
  ```
  3. 若 fcntl 失败 + last_director_tick age > 2 × heartbeat.timeout_seconds：
     a. 调用 emergency_release("director", reason="holder_presumed_dead")
     b. 重新尝试 fcntl
     c. 递增 epoch
     d. 广播 type=system, content="director_preempt_started, epoch=N"
     e. audit action=lock_force_release, details.reason="holder_presumed_dead"（v1.0.3 修订：对齐 O1 默认值，action 用 13 粗粒度枚举之一，细分原因放 details.reason）
  ```
- 理由：P1-18 网络分区恢复路径缺失；spec v1.0.3 修正 audit action 对齐 O1 默认值（原 spec 写 director_lock_preempt 不在 13 枚举中）
- 依赖：P1-7（emergency_release fallback）+ O1 默认值

**[补] §3.3.2 conflict_resolution.fallback_strategy 注释**

- 位置：line 358
- 新内容：`fallback_strategy: priority  # LLM 不可用时降级策略；priority 排序键：last_heartbeat_age asc → agent_id asc（字典序兜底）`
- 理由：P1-17 priority 排序键未定义
- 依赖：无

**[补] §3.3.2 SignatureVerifier 调用契约 + VerifyResult dataclass**

- 位置：line 397-432 之后
- 新内容：
  ```python
  @dataclass
  class VerifyResult:
      level: Literal["ok", "degraded", "distrust"]
      reason: str
      failure_count: int = 0

  # 调用契约：
  # - 收到 VerifyResult.degraded → raise DirectorSignatureError(level="degraded", failure_count=result.failure_count)
  # - 收到 VerifyResult.distrust → raise DirectorSignatureError(level="distrust", failure_count=result.failure_count) + enter_autonomous_mode()
  # DirectorSignatureError 由 SignatureVerifier 调用方（Worker 端 DirectorWriteHandler）抛出
  ```
- 理由：P1（错误处理）P1-1 衔接链路断裂
- 依赖：Q3 已确认补 DirectorSignatureError 异常类

### 4.3 §3.3.3 status.json（最大修订章）

**[改] §3.3.3 line 494**

- 原内容：`status.json 是多写者共享资源（Director + Worker 释放锁时更新），必须用 CAS`
- 新内容：`status.json 是协作写入资源，按字段属性表（见下）区分写权限。所有写入必须用 CAS 保证原子性。`
- 理由：P0-5 多写者语义矛盾
- 依赖：Q1 + Q6

**[改] §3.3.3 line 506**

- 原内容：`# 重试上限 3 次，超限抛 CASConflictError`
- 新内容：`# 重试上限 2 次（对齐项目硬约束第 17 行），超限走 §11.2 字段级合并或抛 CASConflictError`
- 理由：P0-3 CAS 重试上限违反硬约束
- 依赖：无

**[补] §3.3.3 status.json schema 新增字段**

- 位置：line 454-490 status.json 示例内
- 新内容（在顶层字段补）：
  ```json
  "last_fencing_token": 7,
  "phase": "active",
  "recovery_started_at": null,
  "recovery_progress": null,
  "recovery_stage": null
  ```
- 位置：line 472-480 locks[name] schema 内补
  ```json
  "grace_until": null,
  "force_releasing": false
  ```
- 理由：P0-1 / P0-2 / P0-4 字段缺失
- 依赖：Q6 字段属性表

**[补] §3.3.3 director_status 正式枚举表**

- 位置：line 486 之后
- 新内容：
  ```
  | 值 | 含义 | 写入者 | 对应 DirectorHealthState |
  |----|------|--------|--------------------------|
  | active | Director 正常运行 | Director | healthy / degraded |
  | autonomous | 自治模式（Director 不可用） | Worker（心跳超时触发） | offline |
  | recovering | Director 恢复期 fence | Director | - |
  | degraded | Director 签名验证降级 | Worker（SignatureVerifier 标记） | degraded |
  ```
- 理由：P0-6 枚举不一致
- 依赖：无

**[补] §3.3.3 phase 字段枚举表**

- 位置：line 461 之后
- 新内容：
  ```
  | 值 | 含义 |
  |----|------|
  | initializing | 会话初始化中 |
  | active | 会话进行中 |
  | paused | 会话暂停（如所有 agent 离线） |
  | ended | 会话已结束 |
  ```
- 理由：P1-3 phase 无枚举
- 依赖：无

**[补] §3.3.3 字段属性表（Q1 + Q6 合并）**

- 位置：line 557-559 段落改写之后（即上方"Director 主要写者"段落之后），逻辑顺序：CAS 写入协议 → Director 主要写者说明（原 line 557-559 改写）→ 字段属性表（Q1+Q6 合并）
- 新内容：

  | 字段 | 写权限 | CAS 合并策略 | Schema 类型 |
  |------|--------|-------------|-------------|
  | `protocol_version` / `session_id` | Director 独占 | director-authoritative（不可变） | string |
  | `phase` | Director 独占 | director-authoritative | enum |
  | `version` | 系统自动（每次 CAS +1） | 不合并，由 CAS 保证 | int |
  | `epoch` | Director 独占 | director-authoritative | int |
  | `compat_mode` | Director 独占 | director-authoritative | string/null |
  | `current_turn` | Director 独占（自治期 Worker 可 CAS 写，见 P0-4） | director-authoritative | object |
  | `turn_history` | 协作追加 | merge（按 ended_at 排序去重） | list |
  | `active_agents` | Director 独占 | director-authoritative | list |
  | `locks.*` | Worker 可 CAS（Q1） | last-write-wins（按 lock_name 字段粒度） | dict |
  | `last_fencing_token` | Worker 可 CAS（locks 写入时同步递增） | max(existing, new) | int |
  | `last_message_seq` | 协作追加 | max(existing, new) | int |
  | `last_heartbeat.*` | Worker 可 CAS（自己的 agent_id key） | last-write-wins（按 agent_id 字段粒度） | dict |
  | `director_status` | Director 独占（自治期 Worker 可写 autonomous；degraded Worker 可写） | director-authoritative | enum |
  | `director_signature` | Director 独占 | director-authoritative | string |
  | `recovery_progress` / `recovery_stage` / `recovery_started_at` | Director 独占 | director-authoritative | string/null |
  | `extensions.*` | 按 `x_<owner>_` 命名空间归属 | owner-authoritative | any |

  **例外说明**：
  - 自治模式（`director_status="autonomous"`）下 Worker 可 CAS 写 `current_turn` 字段；自治退出后由 Director 接管
  - fence 期（`director_status="recovering"`）所有 Worker 写入被 fence 规则限定（§8.3，仅暂停 messages 锁）

- 理由：Q1 + Q6 合并落地 + P0-4 / P0-5
- 依赖：Q1 / Q6 决策

**[改] §3.3.3 line 557-559 "Director 是 status.json 唯一权威写者"段落**（v1.0.3 spec 补充，Step 2 一并落地）

- 位置：CAS 写入协议代码块之后，"Director 是 status.json 唯一权威写者"段落
- 原内容：
  ```
  **Director 是 status.json 唯一权威写者**（v1.0.1 新增）：
  为简化并发控制，Worker 不直接写 status.json。Worker 释放锁时通过 messages.md `type=system` 通知 Director，Director 串行化所有更新。这与 §5.1"Director 是协议执行者"定位一致。
  ```
- 新内容：
  ```
  **Director 是 status.json 主要写者，Worker 按字段属性表受限写入**（v1.0.3 修订，对齐 Q1 字段属性表）：

  Director 是 status.json 的主要写者，负责 protocol_version / phase / epoch / current_turn / active_agents / director_status / director_signature / recovery_* 等字段的独占写入。Worker 按字段属性表受限写入以下字段：
  - `locks.<lock_name>`：Worker 释放锁时直接 CAS 写入（Q1 决策），无需通过 Director 串行化
  - `last_fencing_token`：Worker 写 locks 时同步递增（max 合并策略）
  - `last_heartbeat.<self_agent_id>`：Worker 仅写自己的 heartbeat key（last-write-wins）
  - `last_message_seq`：Worker 追加消息时 CAS 递增（max 合并策略）
  - `current_turn`：仅自治模式（director_status=autonomous）下 Worker 可 CAS 写（见字段属性表例外）
  - `director_status`：仅 Worker 检测到 Director 心跳超时（写 autonomous）或签名验证降级（写 degraded）时可写

  其他字段（turn_history 由 Director 在推进轮次时追加）由 Director 串行化。这与 §5.1"Director 是协议执行者"定位一致，同时避免 Worker 释放锁等高频操作全部串行到 Director 形成瓶颈。
  ```
- 理由：P0-5 line 494 与 line 509-511 语义矛盾，字段属性表的例外（自治期 Worker 可写 current_turn / Worker 可写 locks）与"Worker 不直接写 status.json"直接冲突；Step 1 已修改 line 494 措辞，Step 2 落地字段属性表时必须同步改写此段落
- 依赖：Q1 字段属性表（Step 2 落地）
- 归属 Step：Step 2（与字段属性表一并落地，避免中间状态矛盾）

### 4.4 §3.3.4 agent_card

**[补] §3.3.4 agent_card schema 新增字段**

- 位置：line 515-552 字段清单内
- 新内容：
  ```yaml
  pid: 12345        # 本地 agent 进程 ID（远程 agent 为 null），Director 用于 os.kill(pid, 0) 探活
  host: null         # 远程 agent 主机名（本地 agent 为 null），用于区分本地/远程探活策略
  ```
- 理由：P1-10（可靠性 P1-R10）+ P1-6（并发）PID 字段缺失
- 依赖：无

**[补] §3.3.4 observer 必选字段**

- 位置：line 580 role 表 observer 行
- 原内容：`| observer | listen / heartbeat | agent_id + role + status |`
- 新内容：`| observer | listen / heartbeat | agent_id + role + status + last_heartbeat + heartbeat_interval_seconds |`
- 理由：P1-1 observer 字段与操作集矛盾
- 依赖：O9 默认值

### 4.5 §3.3.5 messages.md

**[补] §3.3.5 消息类型语义表"使用场景示例"列**

- 位置：line 606-612 表格
- 新增列：
  | type | 使用场景示例 |
  |------|------------|
  | chat | Worker 发言 |
  | system | Director 全局事件（启动/恢复/离线/epoch 变更） |
  | directive | Director 推进轮次指令 |
  | broadcast | Worker 主动广播（如任务状态变更通知） |
  | multicast | Worker 通知部分 agent（如协作任务相关方） |
- 理由：P1-2 Director 通知用 system 还是 broadcast 不明确
- 依赖：无

**[改] §3.3.5 InjectionIsolator 改异步**

- 位置：line 625-692
- 变更：
  1. `__init__(self, bb_root: Path)` 注入 bb_root
  2. `async def scan_and_tag(...)`
  3. 两处 `audit_log(...)` 改 `await append_audit(self._bb_root, {...})`
  4. `build_llm_context` **保持同步**（不改 async）：该方法是纯内存字符串拼接，不涉及 IO 操作，无需改异步；P1-25 集成点 7 调用 `build_llm_context` 时同步调用即可
- 理由：P1-20 违反全链路异步硬约束（仅 scan_and_tag 涉及 audit IO 需改异步；build_llm_context 是纯内存操作，改 async 反而增加无谓的 await 开销）
- 依赖：无

**[补] §3.3.5 messages.pending.md / messages.replay_candidates.md 子 schema**

- 位置：line 692 之后
- 新内容：两个新文件 frontmatter 与 messages.md 一致，但 seq 字段改为 `pending_seq`（仅 pending 内部排序用）；replay_candidates 文件增加 `arbiter_decision: "accept" | "reject" | "pending"` 字段
- 理由：Q2 拆文件落地
- 依赖：Q2 决策

**[补] §3.3.5 messages.pending.md flush 触发时机与流程**（v1.0.3 spec 补充，明确 Q2 落地细节）

- 位置：messages.pending.md 子 schema 之后
- 新内容：
  ```
  **flush 触发时机**：
  - Director 推进轮次到某 agent 时（current_turn.agent_id == pending.from），Director 扫描 messages.pending.md 中 from == 该 agent_id 的所有记录
  - 按 pending_seq 升序处理

  **flush 流程**：
  1. Director 获取 messages 锁
  2. 读取 messages.md 最后一行的 seq，记为 last_seq
  3. 对每条 pending 记录：
     a. 分配全局 seq = last_seq + 1，last_seq 递增
     b. 追加到 messages.md（替换 pending_seq 为 seq，移除 pending_reason / pending_at 字段）
     c. 追加 audit（action=write, details.reason="pending_flushed"）
     d. 从 messages.pending.md 删除该记录
  4. 释放 messages 锁

  **messages.replay_candidates.md flush 流程**：
  - Director LLM 仲裁器周期扫描 arbiter_decision="pending" 的记录
  - 仲裁为 accept 的记录按上述 flush 流程追加到 messages.md（移除 arbiter_* / candidate_* 字段）
  - 仲裁为 reject 的记录保留在 messages.replay_candidates.md，标记 arbiter_decision="reject" + arbiter_reason，不追加
  ```
- 理由：Q2 决策只声明了"Director flush 时分配全局 seq"，未明确触发时机与具体流程；补充避免实现歧义
- 依赖：Q2 决策

### 4.6 §3.3.7 audit

**[改] §3.3.7 audit `action` 字段定义 + actor 字段定义**

- 位置：line 749 / line 738
- 新内容：
  - `action` 字段定义改为"13 粗粒度枚举之一（write/heartbeat/turn_advance/turn_timeout/lock_acquire/lock_release/lock_force_release/register/leave/arbitrate/snapshot/recovery_start/recovery_end），细分原因放 details.reason"
  - `actor` 字段定义改为"操作者 agent_id 或系统组件名（system/watchdog/director）"
- 理由：P0-8（O1 默认值）+ P1-6
- 依赖：O1 默认值

**[改] §3.3.7 append_audit 改用 portalocker**

- 位置：line 763-773
- 新内容：
  ```python
  async def append_audit(bb_root, record):
      """获取 audit.lock（portalocker 文件锁，绕过 CAS）后追加记录，计算 hash 链。
      v1.0.3 修订：portalocker.Lock 是同步锁，用 run_in_executor 包装避免阻塞事件循环。
      audit.jsonl 是 append-only 文件，直接用 append 模式打开写入（不写 .tmp 再 rename，
      因为 rename 会覆盖已有内容，破坏 append-only 语义）。
      """
      import portalocker
      lock_path = bb_root / "locks" / "audit.lock"
      audit_path = bb_root / "audit" / "audit.jsonl"
      loop = asyncio.get_event_loop()

      def _write_with_lock():
          with portalocker.Lock(str(lock_path), timeout=30, fail_when_locked=False):
              # 1. 读取最后一行计算 prev_hash（seek 到末尾前一段读取）
              prev_hash = read_last_hash(audit_path)
              record["prev_hash"] = prev_hash
              record["hash"] = sha256(json.dumps(record, sort_keys=True).encode()).hexdigest()
              # 2. 直接 append 模式写入（portalocker 保证串行化，append 在同文件原子）
              with open(audit_path, "a", encoding="utf-8") as f:
                  f.write(json.dumps(record, ensure_ascii=False) + "\n")
                  f.flush()
                  os.fsync(f.fileno())

      await loop.run_in_executor(None, _write_with_lock)
  ```
- 理由：P1-8 audit.lock 嵌套死锁风险；spec v1.0.3 补充明确 portalocker 同步锁需 run_in_executor 包装、append 模式直接写入（非 rename）
- 依赖：无

### 4.7 §3.5 路径规范

**[补] §3.5 ID 正则表 director_id 注**

- 位置：line 811-817 表后
- 新内容：`注：director_id 遵循 agent_id 正则 ^[a-z0-9_]{3,32}$`
- 理由：P1-4 director_id 无正则
- 依赖：无

### 4.8 §3.6 外部 agent 接入

**[删] §3.6 line 975-994 整段（含 line 983 `${TEAGE_BB_DIR:./data/blackboard}`）**

- 替换为：`路径规范详见 §3.5。（v1.0.3 修订：删除此处重复段，统一在 §3.5 维护）`
- 理由：P0-10 重复段 + 违反硬约束（`${TEAGE_BB_DIR:./data/blackboard}` 含默认值语法，违反"环境变量占位符优先"硬约束）
- 依赖：无
- 注：原 spec §4.8 将"line 983 修改"与"line 975-994 删除"列为两个独立项，但 line 983 实际在重复段内，删除重复段即覆盖 line 983 修改，无需单独处理；spec v1.0.3 合并为一项避免执行混淆

### 4.9 §3.7 命名规范（新增章节）

**[补] §3.7 整章新增**

- 位置：§3.6 之后，§4 之前
- 新内容：
  ```
  ### 3.7 命名规范（v1.0.3 新增）

  #### 3.7.1 ID 命名
  - agent_id / director_id / task_id / artifact_id / lock_name / session_id 遵循 §3.5 正则
  - 命名应语义化，禁止随机串（如 `abc123`）
  - 命名应包含类型前缀（如 `agent_` / `task_` / `director_`）便于 audit 检索

  #### 3.7.2 文件命名
  - 协议文件：小写 + 下划线（如 `agent_card.md` / `messages.md`）
  - 派生文件：主文件名 + `.` + 派生用途（如 `messages.pending.md` / `messages.replay_candidates.md` / `audit.jsonl.recovery`）
  - 快照文件：`snapshot-{ts}.tar.gz`（ts 为 ISO 8601 紧凑格式 `20260720T103000Z`）

  #### 3.7.3 扩展字段命名
  - 所有协议文件 `extensions` 字段下的 key 必须以 `x_<owner>_<field>` 格式命名
  - `<owner>` 是 owner 的简短标识（如 `teage-liu` / `a2a` / `cron`）
  - `<field>` 是字段语义名（如 `required_ops` / `priority`）
  - 示例：`x_teage-liu_required_ops` / `x_a2a_gateway_endpoint` / `x_cron_schedule_id`
  - 命名空间冲突时由 Director 仲裁，先注册方保留
  ```
- 理由：P0-9 §3.7 缺失；spec v1.0.3 修正标题层级为三级标题 `### 3.7`（与 §3.5/§3.6 一致），子章节用四级标题 `#### 3.7.x`
- 依赖：无

### 4.10 §4.4 离线判定

**[改] §4.4 心跳三阶段阈值表**

- 位置：line 1116-1118
- 原内容：
  ```
  | 健康 | age ≤ interval | active |
  | 降级 | interval < age ≤ 2×interval | degraded |
  | 离线 | age > 3×interval | offline |
  ```
- 新内容（对齐 §8.2）：
  ```
  | 健康 | age < 2×interval | active |
  | 降级 | 2×interval ≤ age < timeout | degraded |
  | 离线 | age ≥ timeout | offline（autonomous） |
  ```
- 理由：P0-6（X4 交叉验证）心跳阈值不一致 + P0-7（§4.4 表格用 online 不在枚举，新内容统一用 active 对齐 agent_card.status 枚举）
- 依赖：无

### 4.11 §7.1 三级锁策略

**[补] §7.1 `_next_fencing_token` 伪代码**

- 位置：line 1278 之后
- 新内容：
  ```python
  async def _next_fencing_token(self) -> int:
      """从 status.json.last_fencing_token 递增。每次读 status.json，不维护内存计数器。
      CAS 写入时同写 last_fencing_token 与 locks[name].fencing_token，保证原子性。"""
      current = await read_json(self._bb_root / "status.json")
      new_token = current["last_fencing_token"] + 1
      return new_token  # CAS 失败后重读重算，旧 token 被丢弃（跳号无害）
  ```
- 理由：P0-1 + P1-9
- 依赖：P0-1 字段补充

**[补] §7.1 `_mark_grace_period` / `_delayed_clear` 伪代码**

- 位置：line 1321-1323 之后
- 新内容：
  ```python
  async def _mark_grace_period(self, lock_name: str, existing: dict, ttl_seconds: int = 5):
      """CAS 写入 locks[name].grace_until = now + 5s + force_releasing=true。
      不清除 holder/fencing_token，原持锁者 grace 期间仍可写入。"""
      current = await read_json(self._bb_root / "status.json")
      new_locks = dict(current["locks"])
      new_locks[lock_name] = {
          **existing,
          "grace_until": iso_now_plus(ttl_seconds),
          "force_releasing": True,
      }
      await cas_write_status(self._bb_root, current["version"],
                            {**current, "locks": new_locks}, writer_signature=self._sig)

  async def _delayed_clear(self, lock_name: str, delay: int = 5):
      """5 秒后 CAS 写入 locks[name]=null。CAS 前校验 grace_until 已过。"""
      await asyncio.sleep(delay)
      current = await read_json(self._bb_root / "status.json")
      entry = current["locks"].get(lock_name)
      if not entry or not entry.get("force_releasing"):
          return  # 已被其他路径清除
      if parse_iso(entry["grace_until"]) > now():
          return  # grace 期未过（时钟回退等异常）
      new_locks = dict(current["locks"])
      new_locks.pop(lock_name, None)
      await cas_write_status(self._bb_root, current["version"],
                            {**current, "locks": new_locks}, writer_signature=self._sig)
  ```
- 理由：P0-2 grace_until 字段缺失 + 实现缺失
- 依赖：P0-2 字段补充 + O2 默认值

**[补] §7.1 LockManager 单例声明**

- 位置：line 1255 class 定义之前
- 新内容：`LockManager 是 Worker 进程内单例（DI 容器单例注册），跨 ReactLoop 共享。`
- 理由：P1-9
- 依赖：无

**[补] §7.1 acquire fence 检查**

- 位置：line 1258-1301 acquire 函数第 2 步前
- 新内容：
  ```python
  # fence 期检查（仅 messages 锁受 fence 限制）
  if lock_name == "messages" and current.get("director_status") == "recovering":
      raise LockAcquisitionError(
          reason="fence_period_messages_blocked",
          suggestion="等待 Director 恢复完成（director_status=active）",
      )
  ```
- 理由：P1-16 fence 范围限定未实现
- 依赖：无

**[补] §7.1 `_is_agent_alive` / `_is_heartbeat_stale` 伪代码**

- 位置：line 1445 / line 1451
- 新内容：
  ```python
  async def _is_agent_alive(self, holder: str) -> bool:
      """本地 agent 用 os.kill(pid, 0)；远程 agent 用 heartbeat age。"""
      agent_card = await read_agent_card(self._bb_root, holder)
      if agent_card.get("host"):  # 远程
          return await self._is_heartbeat_stale(holder) is False
      pid = agent_card.get("pid")
      if not pid:
          return False
      try:
          os.kill(pid, 0)
          return True
      except (ProcessLookupError, PermissionError):
          return False

  async def _is_heartbeat_stale(self, holder: str) -> bool:
      """heartbeat age > heartbeat.timeout_seconds 视为 stale。"""
      agent_card = await read_agent_card(self._bb_root, holder)
      age = now() - parse_iso(agent_card["last_heartbeat"])
      return age > timedelta(seconds=agent_card["heartbeat"]["timeout_seconds"])
  ```
- 理由：P1-6（X5 交叉验证）
- 依赖：P1（agent_card pid/host 字段补充）

**[补] §7.1 emergency_release best-effort 模式**

- 位置：line 1405-1423
- 新内容：
  ```python
  # v1.0.3: best-effort + recovery 文件兜底
  # v1.1: 完整 WAL 模式（见 §17.1 P1 项 5）
  await cas_write_status(self._bb_root, current["version"], new_status, writer_signature=self._sig)
  try:
      await append_audit(self._bb_root, {...})
  except Exception as e:
      # audit 写入失败，写 recovery 文件（不依赖 audit.lock）
      recovery_path = self._bb_root / "audit" / "audit.jsonl.recovery"
      async with aiofiles.open(recovery_path, "a") as f:
          await f.write(json.dumps({...}) + "\n")
      await f.flush()
  ```
- 理由：P1-10 + O3 默认值
- 依赖：O3 默认值

**[补] §7.1 renew vs force release 重读策略**

- 位置：line 1338-1352 / line 1303-1336 之后
- 新内容：
  - force release CAS 失败后：重读 status.json，若 `locks[name].expires_at` 已更新且未过期 → 放弃 force release + audit `action=lock_force_release, details.reason="holder_renewed"`
  - renew CAS 失败后：重读 status.json，若 `locks[name].grace_until` 已设置 → 抛 FencingTokenMismatchError（视为锁已被回收）
- 理由：P1-11
- 依赖：P0-2 grace_until 字段

### 4.12 §8.2 故障检测

**[改] §8.2 时钟漂移两行**

- 位置：line 1579-1580
- 原内容：
  ```
  | 时钟漂移 - 警告级 | 偏差 > 5 秒 | 告警 + 锁 TTL 改用单调时钟 |
  | 时钟漂移 - 严重级 | 偏差 > 60 秒 | 拒绝 CAS 写入（防 epoch 判定错误） |
  ```
- 新内容：
  ```
  | 时钟漂移 - 警告级 | 偏差 > 5 秒 | 告警 + 放大 grace_period 至 drift_offset + 2s |
  | 时钟漂移 - 严重级 | 偏差 > 60 秒 | 暂停锁强制释放（防误释放他人锁） |
  ```
- 理由：Q5 决策落地
- 依赖：Q5

**[补] §8.2 watchdog 永久标记持久化**

- 位置：line 1640-1651 之后
- 新内容：
  ```python
  # 自愈超时：永久标记为 polling
  watchdog_state_path = self._data_dir / "watchdog_state.json"
  async with aiofiles.open(watchdog_state_path, "w") as f:
      await f.write(json.dumps({
          "mode": "polling_permanent",
          "set_at": iso_now(),
          "reason": "self_heal_timeout",
      }))
  # 启动时读取：若 mode=polling_permanent 跳过自检直接轮询
  # 用户重置：multiagent.watchdog.reset_to_watchdog=true（重启生效）
  ```
- 理由：P1-15
- 依赖：无

**[补] §8.2 LLM 不可用 priority 排序键注释**

- 位置：line 1583
- 新内容：`仲裁改用 conflict_resolution.fallback_strategy=priority，排序键：last_heartbeat_age asc → agent_id asc（详见 §3.3.2）`
- 理由：P1-17
- 依赖：O5 默认值

### 4.13 §8.3 故障恢复

**[补] §8.3 第 0 步快照恢复**

- 位置：line 1681 第 1 步之前
- 新内容：
  ```
  0. 检查 snapshots/ 是否存在可用的 snapshot-{ts}.tar.gz
     - 存在：解压到临时目录，记录 snapshot_ts
     - 从 audit.jsonl 中过滤 ts > snapshot_ts 的记录重放
     - 快照恢复写 status.json.recovered 后原子 rename 为 status.json
     - 不存在：跳过本步，直接走第 1 步
  ```
- 理由：P1-13
- 依赖：无

**[补] §8.3 自治退出二次确认**

- 位置：line 1862-1867 `_exit_autonomous_mode` 调用之后
- 新内容：
  ```python
  await self._exit_autonomous_mode(director_md["current_epoch"])
  # 二次确认 Director 仍可用
  recheck_md = await read_director_md()
  if now() - parse_iso(recheck_md["last_director_tick"]) > timedelta(
      seconds=recheck_md["heartbeat"]["interval_seconds"]
  ):
      # Director 在退出自治期间再次崩溃，回滚自治
      await self._enter_autonomous_mode(reason="director_re_crashed_during_exit")
      await append_audit(self._bb_root, {
          "action": "recovery_start",
          "details": {"reason": "autonomous_exit_rollback", "director_re_crashed": True},
      })
      return
  ```
- 理由：P1-14
- 依赖：无

**[改] §8.3 自治模式表"轮次推进"行**

- 位置：line 1843
- 原内容：`轮次推进 | Director 写 status.json.current_turn | 时间片轮转：每个 Worker 按 agent_id 字典序轮转，每片 30 秒`
- 新内容：`轮次推进 | 自治期 Worker 可 CAS 写 status.json.current_turn（仅此一字段；自治退出后由 Director 接管，见 §3.3.3 字段属性表例外） | 时间片轮转：每个 Worker 按 agent_id 字典序轮转，每片 30 秒`
- 理由：P0-4
- 依赖：§3.3.3 字段属性表

**[补] §8.3 自治模式表"锁管理"行 trade-off 注释**

- 位置：line 1842
- 新内容（在原描述后补）：`已知 trade-off：自治期锁恢复时间 = TTL + grace_period（默认 32 秒），慢于正常模式的即时 emergency_release。建议自治期将 TTL 缩短为 10 秒（heartbeat.timeout_seconds / 3），通过更频繁续期补偿。`
- 理由：P2-7
- 依赖：无

### 4.14 §10.0 依赖更新

**[改] §10.0 依赖表**

- 原内容：6 项依赖
- 新内容：
  ```
  | 依赖 | 版本 | 状态 |
  |------|------|------|
  | watchdog | >=4.0 | 新增 |
  | aiofiles | >=24.0 | 新增 |
  | portalocker | >=2.7 | 新增 |
  | cryptography | >=42.0 | 新增 |
  | pyyaml | >=6.0 | 已存在（requirements.txt:8），无需动作 |
  | jsonschema | >=4.20 | 修改现有行 requirements.txt:13（>=4.0.0 → >=4.20） |
  ```
- 理由：P1（实施）P1-3 依赖重复声明
- 依赖：无

### 4.15 §10.2 配置段

**[补] §10.2 multiagent 段新增配置项**

- 新内容：
  ```yaml
  multiagent:
    worker:
      dangerous_tools: ["execute_command", "write_file", "call_tool"]  # 与单 agent 安全权限共用，project_memory 第 4 行权威来源
    cas:
      merge_on_exhausted: true  # CAS 重试耗尽后字段级合并降级，false 时严格阻断
    watchdog:
      reset_to_watchdog: false  # 用户手动重置永久 polling 标记（重启生效）
  ```
- 理由：O6 + P0-13 + P1-15
- 依赖：无

**[补] §10.2 末尾"config_helpers 校验"说明**

- 新内容：`修改 teage_liu/config_helpers.py:118，将 'multiagent' 加入 _validate_config_schema 段类型校验元组；新增 multiagent.enabled / role / a2a_gateway.enabled 等关键字段类型与枚举值校验`
- 理由：P2-11
- 依赖：无

### 4.16 §10.3 配置热更新边界

**[改] §10.3 _RESTART_REQUIRED_KEYS**

- 原内容：新增 3 项（multiagent.blackboard_dir / a2a_gateway.listen_port / a2a_gateway.auth_schemes）
- 新内容：仅新增 1 项 `multiagent.blackboard_dir`；a2a_gateway.listen_port / auth_schemes 改热重载（gateway 重建即可）
- 注：`_RESTART_REQUIRED_KEYS` 由 7 项变 8 项，文档明确说明"multiagent.blackboard_dir 是第 8 项，原因是 bb_root 运行时不可迁移；如需严格保持 7 项，可改为环境变量 TEAGE_BB_DIR 注入"
- 理由：P0-12
- 依赖：无

### 4.17 §10.4 容器注册映射

**[改] §10.4 CONFIG_TO_COMPONENTS["multiagent"] 列表**

- 原内容：`"audit_logger"` 项
- 新内容：`"multiagent_audit_logger"` 项（与 2137 行注册键一致）
- 理由：P0-11
- 依赖：无

**[补] §10.4 容器注册位置 + hot_reloadable 标志**

- 新内容：
  ```
  注册位置：teage_liu/lifespan.py（仅 multiagent.enabled=true 时注册）
  注册顺序：blackboard → schema_validator → file_lock → multiagent_audit_logger → agent_registry → director_engine / worker_adapter → watchdog_watcher → recovery_manager → a2a_gateway
  hot_reloadable 标志：
    - blackboard: False（bb_root 不可迁移）
    - a2a_gateway: False（端口绑定）
    - 其余: True
  注册后调用 container.validate() 触发 DFS 环检测
  ```
- 理由：P2-12 + O10 默认值
- 依赖：无

### 4.18 §10.5 REST 端点

**[补] §10.5 认证机制说明**

- 新内容：
  ```
  /blackboard/* 端点默认通过 security.api_key 认证（复用现有 TEAGE_API_KEY，不新增环境变量）
  multiagent.a2a_gateway.enabled=true 时，gateway 层叠加 auth_schemes（oauth2/mtls）做二次认证
  路由注册位置：teage_liu/app.py 现有 12 个 router 之后新增 app.include_router(blackboard_router)
  ```
- 理由：P1-6（实施）
- 依赖：无

### 4.19 §10.6 ReactLoop 集成（重大修订）

**[改] §10.6 集成点 2：capabilities 校验下沉 ToolExecutor**

- 原内容：ReactLoop._execute_tool 内 capabilities 校验
- 新内容：
  ```python
  # teage_liu/agent/tool_executor.py 新增
  def evaluate_policy(self, tool_name, tool_input, session_id, ...):
      # 新增：multiagent capabilities 校验（在 policy_engine 之前）
      if self._multiagent_state and tool_name not in self._multiagent_state.worker_capabilities:
          raise CapabilityNotInCardError(
              tool_name=tool_name,
              agent_id=self._multiagent_state.agent_id,
              declared_capabilities=self._multiagent_state.worker_capabilities,
          )
      # 现有：policy_engine 校验
      ...

  # ToolExecutor.__init__ 新增可选参数
  def __init__(self, ..., multiagent_state: MultiAgentState | None = None):
      self._multiagent_state = multiagent_state
  ```
- 删除 §10.6 集成点 2 中 ReactLoop._execute_tool 的 capabilities 校验代码
- 理由：P1-23 + O7 默认值
- 依赖：O7 默认值

**[改] §10.6 集成点 3/4：改 SessionManager 注册钩子**

- 原内容：ReactLoop._on_session_start / _on_session_end
- 新内容：
  ```python
  # teage_liu/agent/session_manager.py 新增
  class SessionManager:
      def __init__(self, ...):
          self._multiagent_hooks: list[tuple[Callable, Callable]] = []

      def add_multiagent_hook(self, on_start: Callable, on_end: Callable):
          self._multiagent_hooks.append((on_start, on_end))

      async def create_session(self, ...):
          session = ...
          for on_start, _ in self._multiagent_hooks:
              await on_start(session)
          return session

      async def destroy_session(self, session_id, ...):
          for _, on_end in self._multiagent_hooks:
              await on_end(session_id)
          ...

  # multiagent 模块在容器注册时调用
  session_manager.add_multiagent_hook(
      on_start=lambda ctx: agent_registry.register(...),
      on_end=lambda sid: agent_registry.unregister(...),
  )
  ```
- 理由：P1-24 + O8 默认值
- 依赖：O8 默认值

**[补] §10.6 集成点 5：发言前轮次校验**

- 新内容：
  ```python
  # teage_liu/agent/react_loop.py 新增方法
  async def _before_speak(self, agent_id: str, message: dict):
      """发言前轮次校验。freeform 模式不阻断；非 freeform 模式非本机轮次写 pending 队列。"""
      if not self._multiagent_enabled:
          return
      turn_policy = await self._read_turn_policy()
      if turn_policy["mode"] == "freeform":
          return
      current = await self._read_current_turn()
      if current["agent_id"] != agent_id:
          # 写 messages.pending.md（Q2 决策）
          await self._append_pending_message(message)
          raise NotMyTurnError(
              expected_agent=current["agent_id"],
              actual_agent=agent_id,
              turn_started_at=current["started_at"],
          )
  ```
- 理由：P0-14 + O4 默认值
- 依赖：Q2 / O4 默认值

**[补] §10.6 集成点 6：Director 心跳监测后台任务**

- 新内容：
  ```python
  # teage_liu/agent/react_loop.py 会话启动时启动后台任务
  async def _start_director_heartbeat_monitor(self, session_ctx):
      """周期检查 director.md.last_director_tick，超时触发 DirectorUnavailableError。"""
      while True:
          await asyncio.sleep(self._heartbeat_interval)
          director_md = await read_director_md(self._bb_root)
          age = now() - parse_iso(director_md["last_director_tick"])
          if age > timedelta(seconds=director_md["heartbeat"]["timeout_seconds"]):
              await self._enter_autonomous_mode(reason="director_heartbeat_timeout")
              raise DirectorUnavailableError(
                  last_tick=director_md["last_director_tick"],
                  age_seconds=age.total_seconds(),
              )
  ```
- 理由：P0-15 + O4 默认值
- 依赖：O4 默认值

**[补] §10.6 集成点 7：LLM 上下文消息隔离**

- 新内容：
  ```python
  # teage_liu/agent/react_loop.py 新增方法
  async def _build_chat_messages(self, ctx) -> list[dict]:
      raw = await self._read_new_messages_since_last_seq(ctx)
      if self._multiagent_enabled:
          wrapped = self._injection_isolator.build_llm_context(raw)
          return [{"role": "user", "content": wrapped}]
      return raw

  # ReactLoop.__init__ 注入
  def __init__(self, ..., injection_isolator: InjectionIsolator | None = None):
      self._injection_isolator = injection_isolator or InjectionIsolator(bb_root)
  ```
- 理由：P1-25 + O4 默认值
- 依赖：O4 默认值

### 4.20 §11.1 异常分类

**[补] §11.1 新增 6 个异常类**

- 新内容：
  ```python
  @dataclass(kw_only=True)
  class DirectorSignatureError(MultiAgentError):
      level: Literal["degraded", "distrust"]
      failure_count: int
      threshold: int = 3
      stage: ErrorStage = ErrorStage.PROTOCOL
      category: str = "director_signature_failed"

  @dataclass(kw_only=True)
  class SchemaValidationError(MultiAgentError):  # 合并原 ProtocolValidationError
      field_path: str
      error_type: Literal["missing_required", "unknown_field", "type_mismatch", "enum_out_of_range"]
      stage: ErrorStage = ErrorStage.PRE_EXECUTION
      category: str = "schema_validation_error"

  @dataclass(kw_only=True)
  class DiskFullError(MultiAgentError):
      free_bytes: int
      threshold_bytes: int
      stage: ErrorStage = ErrorStage.EXECUTION
      category: str = "disk_full"

  @dataclass(kw_only=True)
  class ReadOnlyFileSystemError(MultiAgentError):
      path: str
      stage: ErrorStage = ErrorStage.EXECUTION
      category: str = "read_only_fs"

  @dataclass(kw_only=True)
  class ClockDriftError(MultiAgentError):
      drift_seconds: float
      threshold_seconds: int
      stage: ErrorStage = ErrorStage.EXECUTION
      category: str = "clock_drift"

  @dataclass(kw_only=True)
  class RecoveryFenceTimeoutError(MultiAgentError):
      fence_started_at: str
      timeout_seconds: int
      stage: ErrorStage = ErrorStage.PROTOCOL
      category: str = "recovery_fence_timeout"

  @dataclass(kw_only=True)
  class LockForceReleaseError(MultiAgentError):
      lock_name: str
      reason: Literal["timeout", "holder_dead", "interrupted"]
      stage: ErrorStage = ErrorStage.EXECUTION
      category: str = "lock_force_release"
  ```

  `ProtocolValidationError` 改名为 `SchemaValidationError`，保留旧名作 alias 一段时间。

  另补占位类：
  ```python
  @dataclass(kw_only=True)
  class A2AGatewayError(MultiAgentError):
      http_status: int
      a2a_error_code: str
      stage: ErrorStage = ErrorStage.EXECUTION
      category: str = "a2a_gateway"

  @dataclass(kw_only=True)
  class A2ATaskStateTransitionError(MultiAgentError):
      task_id: str
      from_state: str
      to_state: str
      stage: ErrorStage = ErrorStage.EXECUTION
      category: str = "a2a_gateway"
  ```
- 理由：Q3 决策 + P1-22 + P1-6（错误处理 A2A 占位）
- 依赖：Q3

**[补] §11.1 _CATEGORY_ZH 补全**

- 新内容（在现有 13 项基础上补）：
  ```python
  "director_signature_failed": "Director 签名验证失败",
  "schema_validation_error": "Schema 校验失败",
  "disk_full": "磁盘空间不足",
  "read_only_fs": "文件系统只读",
  "clock_drift": "时钟漂移",
  "recovery_fence_timeout": "恢复期 fence 超时",
  "lock_force_release": "强制释放锁",
  "a2a_gateway": "A2A 网关错误",
  ```
- 理由：P1-22
- 依赖：无

**[补] §11.1 ErrorStage 枚举扩展声明**

- 新内容：
  ```
  ErrorStage 枚举扩展声明：
  - 现有 tool_error.py 的 ErrorStage 仅含 PRE_EXECUTION / EXECUTION
  - multiagent 模块扩展 ErrorStage 枚举新增 PROTOCOL 值
  - ReactLoop 的 tool_result 链路仅识别 PRE_EXECUTION / EXECUTION
  - PROTOCOL 阶段错误由心跳监测后台任务触发（集成点 6），不走 tool_result 链路
  ```
- 理由：P2-1
- 依赖：无

### 4.21 §11.2 错误处理策略（重大修订：拆三子表）

**[改] §11.2 整章重组**

- 原内容：单一表格 36 行
- 新内容：拆三个子表 + 阻断层定义小表

  #### 11.2.1 异常类处理表（含阻断层列）

  | 异常类 | 阻断层 | 处理 | 重试 | 重试上限 | audit action | 用户提示 |
  |--------|--------|------|------|---------|--------------|---------|
  | SchemaValidationError (必选字段) | 严格阻断 | 拒绝执行 | 否 | - | action=write, details.reason="schema_validation_failed" | 错误提示 |
  | SchemaValidationError (可选字段) | 软约束 | 字段忽略 + 告警 | 否 | - | action=write, details.reason="unknown_field_ignored" | 告警 toast |
  | SchemaValidationError (枚举值越界) | 软约束 | 降级到 nearest valid value | 否 | - | action=write, details.reason="enum_value_coerced" | 告警 toast |
  | NotMyTurnError (freeform 模式) | 不阻断 | 无轮次检查 | - | - | - | 无 |
  | NotMyTurnError (非 freeform 模式) | 软约束 | 写 messages.pending.md + 轮到时 flush | 否 | - | action=write, details.reason="out_of_turn_attempt" | 告警 toast |
  | LockAcquisitionError | 软约束 | 异步队列化 + 60s 超时 + LLM 决策 | 是 | 2 次 | action=lock_acquire, details.reason="acquire_failed" | 告警 toast |
  | AgentOfflineError | 严格阻断 | 拒绝执行 + 通知 Director | 否 | - | action=heartbeat, details.reason="agent_offline" | 错误提示 |
  | DirectorUnavailableError | 软约束 | 触发自治模式切换 | 否 | - | action=recovery_start, details.reason="director_unavailable" | 告警 toast |
  | SchemaVersionMismatchError (major) | 严格阻断 | 拒绝接入 | 否 | - | action=register, details.reason="version_major_mismatch" | 错误提示 |
  | SchemaVersionMismatchError (minor) | 软约束 | 允许接入 + 标记 | 否 | - | action=register, details.reason="version_minor_mismatch" | 告警 toast |
  | FencingTokenMismatchError (首次) | 软约束 | Director LLM 仲裁，有价值入 messages.replay_candidates.md | 否 | - | action=arbitrate, details.reason="fencing_token_mismatch" | 告警 toast |
  | FencingTokenMismatchError (连续 3 次) | 严格阻断 | 拒绝写入 + 标记 distrust | 否 | - | action=arbitrate, details.reason="fencing_token_mismatch_repeated" | 错误提示 |
  | CASVersionMismatchError (重试中) | 严格阻断 | CAS 重试 | 是 | 2 次 | action=write, details.reason="cas_retry" | 无 |
  | CASVersionMismatchError (重试耗尽降级) | 软约束 | 字段级合并（受 multiagent.cas.merge_on_exhausted 配置控制） | 否 | - | action=write, details.reason="cas_merge_fallback" | 告警 toast |
  | CapabilityNotInCardError (非危险首次) | 软约束 | 执行 + 标记 pending + Director 补 card | 否 | - | action=register, details.reason="capability_pending" | 告警 toast |
  | CapabilityNotInCardError (危险工具) | 严格阻断 | 拒绝执行（execute_command/write_file/call_tool） | 否 | - | action=write, details.reason="dangerous_capability_blocked" | 错误提示 |
  | CapabilityNotInCardError (连续 3 次) | 严格阻断 | 拒绝执行 + 标记 distrust | 否 | - | action=arbitrate, details.reason="capability_violation_repeated" | 错误提示 |
  | GhostWriteAttemptError (首次) | 软约束 | Director LLM 仲裁，有价值入 messages.replay_candidates.md | 否 | - | action=arbitrate, details.reason="ghost_write_attempt" | 告警 toast |
  | GhostWriteAttemptError (连续 3 次) | 严格阻断 | 拒绝 + 标记 distrust | 否 | - | action=arbitrate, details.reason="ghost_write_repeated" | 错误提示 |
  | PathTraversalError (`..` / symlink) | 严格阻断 | 拒绝执行 | 否 | - | action=write, details.reason="path_traversal_blocked" | 错误提示 |
  | DirectorSignatureError (单次) | 软约束 | 继续执行 + 标记 degraded | 否 | - | action=write, details.reason="signature_failed" | 告警 toast |
  | DirectorSignatureError (连续 3 次) | 严格阻断 | 进入自治模式 | 否 | - | action=recovery_start, details.reason="signature_distrust" | 错误提示 |
  | DiskFullError | 软约束 | 暂停 snapshots + 允许核心写入 | 否 | - | action=snapshot, details.reason="disk_full" | 告警 toast |
  | ReadOnlyFileSystemError | 严格阻断 | 拒绝写入 + 进入只读模式 | 否 | - | action=write, details.reason="read_only_fs" | 错误提示 |
  | ClockDriftError (>60s) | 软约束 | 暂停锁强制释放 | 否 | - | action=lock_force_release, details.reason="clock_drift_critical" | 告警 toast |
  | RecoveryFenceTimeoutError | 软约束 | 强制退出 fence + 标记 audit | 否 | - | action=recovery_start, details.reason="fence_timeout" | 告警 toast |
  | LockForceReleaseError | 软约束 | audit 记录 + 通知原持锁者 | 否 | - | action=lock_force_release, details.reason=reason 字段 | 告警 toast |
  | A2AGatewayError | 软约束 | v1.1 补充双向映射 | 否 | - | action=write, details.reason="a2a_gateway_error" | 告警 toast |
  | A2ATaskStateTransitionError | 严格阻断 | 拒绝转换 | 否 | - | action=write, details.reason="a2a_invalid_transition" | 错误提示 |

  #### 11.2.2 字段标记处理表

  | 标记类型 | 阻断层 | 处理 | audit action | 用户提示 |
  |---------|--------|------|--------------|---------|
  | InjectionSuspected | 软约束 | build_llm_context 用 `<untrusted_user_message>` 包裹 | action=write, details.reason="injection_suspected" | 告警 toast |
  | MessageTruncated | 软约束 | 截断到 4096 + 标记 truncated=true | action=write, details.reason="message_truncated" | 告警 toast |
  | PathNormalized | 不阻断 | 自动转相对路径 + 标记 | action=write, details.reason="path_normalized" | 无 |

  #### 11.2.3 系统响应动作表（含触发组件 + 触发任务）

  | 动作类型 | 阻断层 | 处理 | 触发组件 | 触发任务 | audit action |
  |---------|--------|------|---------|---------|--------------|
  | DiskFullWarning (<100MB) | 软约束 | 暂停 snapshots + 允许核心写入 | Blackboard | `atomic_write` 前 `_check_disk_usage` | action=snapshot, details.reason="disk_full_warning" |
  | ClockDriftWarning (>5s) | 软约束 | 放大 grace_period 到 drift_offset+2s | TimeSyncMonitor | `_check_clock_drift`（每 60s） | action=heartbeat, details.reason="clock_drift_warning" |
  | WatchdogSelfTestFailed | 软约束 | 降级为轮询 + 5 分钟自愈 | Watchdog | `_self_test`（启动时 + 每 5 分钟） | action=heartbeat, details.reason="watchdog_self_test_failed" |

  #### 11.2.4 阻断层定义小表

  | 阻断层 | 语义 | 进入路径 |
  |--------|------|---------|
  | 严格阻断 | 拒绝操作 + 错误提示 + audit | 异常类直接抛出 |
  | 软约束 | 降级处理 + 告警 + audit，不阻断主流程 | 异常类抛出后由 ReactLoop catch 降级 |
  | 不阻断 | 无任何处理（用于显式声明跳过检查） | 不抛异常 |
  | 应急释放 | 强制清除 + audit（仅锁场景） | LockManager.emergency_release 内部 |

  #### 11.2.5 重试预算与"相同失败"判定

  - 重试预算：最多 2 次（对齐 project_memory 第 17 行），第 3 次相同失败立即终止
  - "相同失败"判定规则：

  | 错误类型 | 相同失败判定 | 不同参数判定 |
  |---------|------------|------------|
  | LockAcquisitionError | 同一 lock_name + 同一 current_holder | holder 变化或 lock_name 变化 |
  | CASVersionMismatchError | 同一 expected_version 连续失败 | 重读后用新 expected_version |
  | 其他可重试错误 | 同一 error_class + 同一 message | 任意字段变化 |

- 理由：Q3 + Q4 + P0-3 + P0-13 + P1-21 + P2-2
- 依赖：Q3 / Q4 / O6

### 4.22 §11.3 分层阻断策略

**[改] §11.3 整章降级**

- 原内容：37 行总表
- 新内容：
  ```
  ## 11.3 分层阻断策略原则

  分层阻断是 v1.0.2 引入的统一阻断策略，v1.0.3 进一步明确为四层：

  1. **严格阻断**：拒绝操作，向用户抛错误提示，audit 记录。用于违反协议必则、安全边界、严重错误场景
  2. **软约束**：降级处理（如字段合并、队列化、LLM 仲裁），告警 toast，audit 记录，不阻断主流程。用于可恢复的次要错误
  3. **不阻断**：显式声明跳过检查（如 freeform 模式下的轮次检查）
  4. **应急释放**：仅锁场景，强制清除锁 + audit 记录 + 通知原持锁者

  设计原则：
  - 稳定优先：严格阻断用于防止数据损坏、安全越权
  - 流畅度优先：软约束用于可恢复错误，避免单点故障阻塞全链路
  - 易维护：每条错误处理策略有明确阻断层 + audit + 用户提示
  - 易扩展：新增错误类型时按"异常类 / 字段标记 / 系统响应动作"三类分别归入 §11.2.x 子表

  具体错误处理策略见 §11.2.1 / §11.2.2 / §11.2.3 三个子表，阻断层定义见 §11.2.4。
  ```
- 理由：Q4 决策落地
- 依赖：Q4

### 4.23 §17 遗留问题与 v1.1 计划

**[补] §17 v1.0.3 修订日志**

- 新内容：
  ```
  ## v1.0.3 修订日志（2026-07-21）

  ### grill-me 第三轮 6 项决策
  - Q1: §3.3.3 新增字段写权限矩阵
  - Q2: §3.2/3.3.5/6.2 新增 messages.pending.md / messages.replay_candidates.md
  - Q3: §11.2 拆三子表，§11.1 补 6 异常类
  - Q4: §11.3 总表删除，§11.2 三子表加阻断层列
  - Q5: §7.1/8.2 删除"改用单调时钟"
  - Q6: §3.3.3 新增字段属性表

  ### 子智能体交叉验证 49 项
  - P0 阻断 15 项（详见第 3.2 节）
  - P1 关键 22 项（详见第 3.3 节）
  - P2 改进 12 项（详见第 3.4 节）

  详见：2026-07-21-多agent协作机制-v1.0.3修订-spec.md
  ```

**[补] §17.1 P1 项扩展**

- P1 项 5（WAL）：计划列扩展为"v1.1 含 WAL schema（op_id/ts/target_file/payload_hash/prev_wal_seq）+ rebuild_from_wal + WAL/audit 写入顺序 + WAL 损坏降级"
- P1 项 6（快照）：计划列扩展为"v1.1 含快照+增量 audit 重放 + status.json.recovered 原子 rename + 快照/audit 协同顺序"
- P1 项 9（告警）：计划列扩展为"v1.1 含告警规则 / 通知通道 / 自动响应动作映射表"
- 新增 P1 项"freeform 模式 wait_queue FIFO 公平性"（P2-4）
- 新增 P1 项"自治模式 CAS 冲突率基准测试"（P2-5）
- 新增 P1 项"Director 锁抢占协议的跨平台实现差异（Windows LockFileEx vs Linux fcntl）"（P1-18）

---

## 5. 执行顺序与依赖图

### 5.1 9 步骤执行顺序

```
Step 1: 协议层基础（§3.0 / §3.3.2 / §3.3.3 / §3.3.4 / §3.3.5 / §3.3.7 / §3.5 / §3.6 / §3.7）
        注：含 §3.0 版本演进策略 + §3.3.2 Director 协议（spec §4.1 / §4.2），不只是 §3.3.3-§3.7
        ↓（无依赖）
Step 2: 字段属性表（§3.3.3，Q1+Q6 合并落地 + line 557-559 段落改写）
        ↓（依赖 Step 1 字段补充）
Step 3: 锁机制补完（§7.1 / §7.2）
        ↓（依赖 Step 1 字段 + Step 2 字段属性表）
Step 4: 可靠性补完（§8.2 / §8.3）
        ↓（依赖 Step 3 锁机制）
Step 5: 错误处理重构（§11.1 / §11.2 / §11.3）
        ↓（依赖 Step 1-4 全部，因为 §11.2 引用所有章节）
Step 6: ReactLoop 集成补完（§10.6）
        ↓（依赖 Step 5 错误类定义）
Step 7: 配置与容器（§10.0 / §10.2 / §10.3 / §10.4 / §10.5）
        ↓（独立，可与 Step 6 并行）
Step 8: audit action 统一（全文替换 40+ 细粒度值）
        ↓（依赖 Step 1 §3.3.7 字段定义）
Step 9: §17 修订日志 + §4.4 心跳阈值统一
        ↓（最后一步，记录所有变更）
```

### 5.2 跨步骤依赖关系

| 依赖项 | 上游 | 下游 |
|--------|------|------|
| status.json 字段补充（last_fencing_token / grace_until / recovery_*） | Step 1 | Step 2 / Step 3 / Step 4 |
| 字段属性表（Q1+Q6） | Step 2 | Step 3 / Step 5 |
| _next_fencing_token 伪代码 | Step 3 | Step 5（错误处理引用） |
| §11.1 异常类定义 | Step 5 | Step 6（集成点 5/6/7 引用异常类） |
| §3.3.7 audit action 字段定义 | Step 1 | Step 8（全文替换） |

### 5.3 并行执行机会

- Step 6（ReactLoop 集成）与 Step 7（配置容器）可并行
- Step 8（audit action 替换）与 Step 9（§17 日志）可并行（不冲突）

---

## 6. 验收标准

### 6.1 章节级验收（每步完成后检查）

| Step | 验收项 | 验收方式 |
|------|--------|---------|
| 1 | §3.3.3 status.json schema 含 last_fencing_token / grace_until / force_releasing / recovery_started_at / recovery_progress / recovery_stage 字段 | grep '"last_fencing_token"' design.md 命中 1+ 次 |
| 1 | §3.3.4 agent_card schema 含 pid / host 字段 | grep 'pid: 12345' design.md 命中 |
| 1 | §3.7 章节存在 | grep '^### 3.7 命名规范' design.md 命中（§3.7 是 §3 的子章节，用三级标题，与 §3.5/§3.6 层级一致） |
| 1 | §3.6 line 975-994 重复段已删除 | grep 'TEAGE_BB_DIR:./data/blackboard' design.md 命中 0 次 |
| 2 | §3.3.3 字段属性表存在且含 15+ 字段 | grep '字段属性表' design.md 命中 1+ 次 |
| 2 | §3.3.3 line 557-559 "Director 唯一权威写者"段落已改写 | grep 'Director 是 status.json 主要写者' design.md 命中 1 次；grep 'Worker 不直接写 status.json' design.md 命中 0 次 |
| 3 | §7.1 含 _next_fencing_token / _mark_grace_period / _delayed_clear / _is_agent_alive 伪代码 | grep 4 个方法名 design.md 各命中 1+ 次 |
| 4 | §8.2 时钟漂移两行无"改用单调时钟" | grep '改用单调时钟' design.md 命中 0 次 |
| 4 | §8.3 第 0 步快照恢复存在 | grep '检查 snapshots/' design.md 命中 |
| 5 | §11.2 含 11.2.1 / 11.2.2 / 11.2.3 / 11.2.4 / 11.2.5 五个小节 | grep '#### 11.2' design.md 命中 5 次 |
| 5 | §11.3 不再含 37 行总表 | grep '统一 15 项修订' design.md 命中 0 次 |
| 6 | §10.6 含集成点 5/6/7 | grep '集成点 5' / '集成点 6' / '集成点 7' design.md 各命中 1+ 次 |
| 7 | §10.4 CONFIG_TO_COMPONENTS["multiagent"] 含 multiagent_audit_logger | grep 'multiagent_audit_logger' design.md 命中 2+ 次 |
| 7 | §10.3 _RESTART_REQUIRED_KEYS 仅新增 multiagent.blackboard_dir | grep 'multiagent.a2a_gateway.listen_port' design.md 命中 0 次（在 _RESTART_REQUIRED_KEYS 段内） |
| 8 | 全文无未声明 audit action 值 | 抽样检查 5 处 action=XXX，details.reason=YYY 格式 |
| 9 | §17 含 v1.0.3 修订日志 | grep 'v1.0.3 修订日志' design.md 命中 |

### 6.2 整体验收（全部完成后检查）

| 验收项 | 验收方式 |
|--------|---------|
| 版本号已升级 | design.md 顶部 revision_notes 含 v1.0.3 条目 |
| 硬约束对齐 | 全文 grep '改用单调时钟' / 'file_delete/mcp_call' / '${TEAGE_BB_DIR:' 均命中 0 次 |
| audit action 枚举对齐 | 全文 grep 'action=director_lock_preempt' / 'action=lock_force_release_aborted' / 'action=autonomous_exit_rollback' / 'action=quota_exceeded' 均命中 0 次（O1 默认值：13 粗粒度枚举 + details.reason 细分） |
| line 557-559 段落改写 | 全文 grep 'Worker 不直接写 status.json' 命中 0 次；grep 'Director 是 status.json 主要写者' 命中 1 次 |
| Q1-Q6 全部落地 | 第 3.1 节 6 项决策在文档中均有对应内容 |
| P0 全部修复 | 第 3.2 节 15 项 P0 在文档中均有对应内容 |
| P1 全部修复 | 第 3.3 节 22 项 P1 在文档中均有对应内容 |
| P2 全部记录 | 第 3.4 节 12 项 P2 在 §17.1 P1 计划列均有对应条目 |

### 6.3 可客观判定的验收标准（替代原 §15 主观项）

| 原 §15 项 | 新验收标准 |
|-----------|----------|
| #6 "配置热更新边界清晰" | `_RESTART_REQUIRED_KEYS` 集合内容与 §10.3 表格完全一致，通过 `pytest tests/test_config_restart_keys.py` 校验 |
| #7 "multiagent.enabled=false 时不影响现有任何功能" | `multiagent.enabled=false` 时 `pytest tests/ -k 'not multiagent'` 100% 通过，且 config.yaml 无 multiagent 段时启动无报错 |
| #9 "TDD 流程" | 每个 PR 必须包含对应失败测试的 git commit 历史（RED → GREEN 两段提交），CI 通过 `git log --grep 'test(red):'` 校验 |

---

## 7. 回滚预案

### 7.1 章节级回滚

每个 Step 完成后立即 git commit，commit message 格式 `v1.0.3 step N: <章节>`。回滚时：

- 单步回滚：`git revert <commit>`
- 整体回滚：`git reset --hard <v1.0.2-final-commit>`

### 7.2 关键节点保护

- Step 5（错误处理重构）完成后必须运行 `pytest tests/multiagent/` 验证不破坏现有测试
- Step 6（ReactLoop 集成）完成后必须运行 `pytest tests/agent/` 验证 ReactLoop 现有功能不破坏
- Step 8（audit action 统一）完成后必须运行 `pytest tests/multiagent/test_audit.py` 验证 audit 链完整

### 7.3 紧急回滚触发条件

- 任一 Step 完成后 `pytest tests/` 失败率 > 5% → 立即回滚该 Step
- v1.0.3 全部完成后整体 `pytest tests/` 失败率 > 10% → 全部回滚到 v1.0.2

---

## 8. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| Step 5 §11.2 重构涉及大量表格重排，可能遗漏场景 | 中 | 中 | 重构后对照 P0-3 / P1-21 / P2-2 检查清单逐项验证 |
| Step 6 集成点 5/6/7 注入 ReactLoop，可能破坏现有 run/run_stream 流程 | 中 | 高 | 新增方法不修改现有签名；TDD 先写失败测试验证 hook 触发 |
| Step 8 全文替换 audit action 可能漏改 | 高 | 低 | grep 全文 `action=` 出现的所有值，对照 §3.3.7 13 粗粒度 enum 校验 |
| Step 4 §8.3 自治退出二次确认可能引入新死锁 | 低 | 高 | 二次确认失败时仅回滚自治模式，不抛异常阻塞主流程 |
| O3 best-effort + recovery 文件方案在 audit 写入失败率高时可能导致 recovery 文件膨胀 | 低 | 中 | 监控 recovery 文件大小，>10MB 时告警；v1.1 WAL 完整方案替换 |

---

## 9. 文件清单

本 spec 涉及的文件变更：

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `teage-liu/docs/superpowers/specs/2026-07-20-多agent协作机制-design.md` | 修改 | 主文档升级 v1.0.2 → v1.0.3 |
| `teage-liu/docs/superpowers/specs/2026-07-21-多agent协作机制-v1.0.3修订-spec.md` | 新增 | 本 spec 文件（修订执行计划） |

后续 v1.0.3 实施阶段（本 spec 确认后启动）将涉及代码变更，列于代码实施 spec 中，不在本 spec 范围。

---

## 10. 待用户确认事项

本 spec 第 2 节列出 10 个开放问题的推荐默认值。用户需在执行前确认：

1. **是否接受所有 10 个推荐默认值**？如有否决，请指出具体编号与替代方案
2. **是否接受第 4 节章节修订详情中的所有变更**？如有调整，请指出具体章节
3. **是否接受第 5 节 9 步执行顺序**？是否需要并行执行某些步骤
4. **是否接受第 6 节验收标准**？是否需要补充额外验收项

确认后我将按第 5 节执行顺序逐 Step 修订 design.md，每步完成后报告进度。
