{
  "id": "error-codes-20",
  "protocol_version": "v1.0.0",
  "description": "错误码日志面锚定(§errors §6 三面模型): 18 码此前在套件中 0 命中。本用例按 scenarios 驱动 HOOK_EXCEPTION / HOOK_TERMINAL_ACTION_IGNORED / TOOL_NO_EXECUTOR / LOOP_MAX_REACHED 四条真实发射路径,经 logging 捕获断言 `CODE: message` 前缀;事件面(LLM_* / HOOK_INVALID_ACTION / CONFIG_*)由用例 16/19/21 与单测覆盖。",
  "setup": {
    "extensions": []
  },
  "inputs": {
    "session_id": "s-20",
    "max_loops": 3,
    "scenarios": [
      { "kind": "raise_on", "hook": "before" },
      { "kind": "terminal_action" },
      { "kind": "tool_path_no_executor" },
      { "kind": "loop_max" }
    ]
  },
  "expected": {
    "error_codes": {
      "must_include": [
        "HOOK_EXCEPTION",
        "HOOK_TERMINAL_ACTION_IGNORED",
        "TOOL_NO_EXECUTOR",
        "LOOP_MAX_REACHED"
      ],
      "must_exclude": []
    }
  }
}
